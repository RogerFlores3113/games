---
phase: 06-game-interface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/globals.css
  - apps/web/lib/suit-visuals.ts
  - apps/web/lib/suit-visuals.test.ts
  - apps/web/components/hanabi/SuitGlyph.tsx
autonomous: true
requirements: [UI-06, UI-08, UI-09]

must_haves:
  truths:
    - "D-06: glyph shape and hue are defined for all seven suits (red, yellow, green, blue, white, rainbow, black) in one module, exhaustively keyed by Suit"
    - "D-05: each suit has a distinct glyph silhouette (unique SVG path) — hue is never the only carrier of suit identity"
    - "D-06: Rainbow's glyph is a flat single-tone 8-point starburst silhouette, with no gradient required to identify it"
    - "D-07: glyphs are inline SVG in the repo (no icon font, no lucide-react suit glyphs, no new dependency); suit hues are new --color-suit-* tokens inside globals.css @theme, with no existing token redefined"
    - "D-10: every suit hue passes WCAG AA (>=4.5:1) against both --color-bg and --color-surface, verified by an automated test that parses globals.css — and because glyph/hue opacity never changes across luminosity steps, this is the contrast at the dimmest step"
    - "D-11 / D-22: clue-highlight and stack-flash keyframes are CSS-only, single-shot, and disabled under prefers-reduced-motion"
  artifacts:
    - path: "apps/web/lib/suit-visuals.ts"
      provides: "SUIT_VISUALS record (label, hueVar, glyphPath) for all 7 suits"
      exports: ["SUIT_VISUALS", "suitVisual"]
    - path: "apps/web/lib/suit-visuals.test.ts"
      provides: "exhaustiveness, uniqueness, and AA-contrast tests"
      contains: "SuitGlyph"
    - path: "apps/web/components/hanabi/SuitGlyph.tsx"
      provides: "inline SVG glyph component"
      exports: ["SuitGlyph"]
    - path: "apps/web/app/globals.css"
      provides: "--color-suit-* and --color-card-glow tokens, keyframes"
      contains: "--color-suit-rainbow"
  key_links:
    - from: "apps/web/components/hanabi/SuitGlyph.tsx"
      to: "apps/web/lib/suit-visuals.ts"
      via: "SUIT_VISUALS lookup"
      pattern: "SUIT_VISUALS\\["
    - from: "apps/web/lib/suit-visuals.ts hueVar"
      to: "apps/web/app/globals.css @theme"
      via: "var(--color-suit-*) static literal strings"
      pattern: "var\\(--color-suit-"
---

<objective>
Create the always-on suit identity system (UI-06): seven hand-authored glyph silhouettes plus seven contrast-verified hue tokens (D-05, D-06, D-07, D-10), the luminosity glow token, and the CSS keyframes for the transient clue highlight and stack-complete flash (D-11, D-22).

Purpose: glyph + hue must be designed jointly with luminosity (roadmap research note). Keeping glyph data in a pure .ts module makes exhaustiveness and contrast machine-verifiable in the DOM-less Vitest project.
Output: globals.css additions, lib/suit-visuals.ts + test, components/hanabi/SuitGlyph.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-game-interface/06-CONTEXT.md
@.planning/phases/06-game-interface/06-UI-SPEC.md
@.planning/phases/06-game-interface/06-PATTERNS.md
@apps/web/app/globals.css

<interfaces>
From packages/rules (via "@games/rules"): `ALL_SUITS` = ["red","yellow","green","blue","white","rainbow","black"] as const; `type Suit`.

Existing @theme tokens (DO NOT redefine): --color-bg #0B0F1A, --color-surface #141A2E, --color-accent #F5B942, --color-destructive #F0453D, --color-text #F4F6FB, --color-text-muted #8B93A8, --color-border #232B45, --color-status-connected, --color-status-disconnected, --size-touch-min, --space-*, --text-*.

UI-SPEC hue table (use these values):
  --color-suit-red #F0645A | --color-suit-yellow #E8C547 | --color-suit-green #4FBF8A | --color-suit-blue #6FA8FF
  --color-suit-white #E7ECF7 | --color-suit-rainbow #C9A8FF | --color-suit-black #B7C2D6
  --color-card-glow #FFD98A
UI-SPEC glyph table: red 5-point star, yellow triangle point-up, green square, blue circle, white diamond, rainbow 8-point starburst (flat fill), black notched hexagon.
UI-SPEC motion: clue-touch highlight 2s (box-shadow 0 0 0 3px var(--color-card-glow) easing out); stack-complete flash 600ms single-shot (filter: drop-shadow(0 0 24px rgba(255,217,138,0.9)) with scale 1 -> 1.08 -> 1); reduced motion -> instant style swap, never hidden.

Tailwind v4 JIT cannot see interpolated class names — use static literal strings or inline style with var(...).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suit tokens, glow token, keyframes, and the SUIT_VISUALS data module with contrast tests</name>
  <files>apps/web/app/globals.css, apps/web/lib/suit-visuals.ts, apps/web/lib/suit-visuals.test.ts</files>
  <read_first>
    - apps/web/app/globals.css (header rules: accent reserved uses, no hex outside @theme, namespace warning)
    - .planning/phases/06-game-interface/06-UI-SPEC.md sections "Color", "Suit Identity System", "Luminosity System", "Fireworks-night motion"
    - packages/rules/src/hanabi/variant.ts (ALL_SUITS)
    - apps/web/lib/hanabi-board-logic.test.ts (test style)
  </read_first>
  <behavior>
    - describe("SuitGlyph visuals"): Object.keys(SUIT_VISUALS) sorted equals [...ALL_SUITS] sorted
    - every glyphPath is a non-empty string and all 7 glyphPaths are pairwise distinct
    - every hueVar matches /^var\(--color-suit-[a-z]+\)$/ and all 7 are distinct; each label is the capitalized suit name ("Red" ... "Black")
    - globals.css (read with node:fs relative to the test file) contains each --color-suit-<suit> token exactly once, inside the @theme block
    - WCAG contrast: for each suit token hex parsed from globals.css, contrast vs --color-bg and vs --color-surface (both parsed from the same file) is >= 4.5
    - --color-card-glow is defined in @theme; none of the pre-existing tokens listed in interfaces appears more than once in the file (no redefinition)
  </behavior>
  <action>
    Write apps/web/lib/suit-visuals.test.ts first (describe name must contain "SuitGlyph" so `-t SuitGlyph` selects it), implementing a small WCAG relative-luminance + contrast-ratio helper inside the test file and reading `../app/globals.css` via `node:fs` + `new URL(..., import.meta.url)` or `path.resolve(__dirname, ...)` (whichever the web Vitest project supports — try import.meta.url first). Confirm red.

    globals.css (per D-07): inside the existing @theme block, after the --color-status-* lines, append a commented group "Phase 6 suit hues (D-05..D-07, D-10) — hue is constant across luminosity steps" with the seven --color-suit-* tokens and --color-card-glow using the interface values. Update the header comment's accent reserved-uses list to add exactly the D-02 in-game uses (active-player hand-container ring/glow, own-hand "Your turn" state accent) and state --color-card-glow is the luminosity signal and never an accent substitute. Outside @theme, add: `@keyframes clue-touch-pulse` (from box-shadow 0 0 0 3px var(--color-card-glow) to box-shadow 0 0 0 0 transparent), `@keyframes stack-complete-flash` (0%/100% transform scale(1) filter none; 50% transform scale(1.08) filter drop-shadow(0 0 24px var(--color-card-glow))), utility classes `.anim-clue-touch { animation: clue-touch-pulse 2s ease-out 1; }` (NO `forwards` fill — this class goes on a dedicated absolutely-positioned overlay span inside the card, never on the luminosity frame element, so it can never override or erase the persistent luminosity box-shadow) and `.anim-stack-flash { animation: stack-complete-flash 600ms ease-out 1; }`, and `@media (prefers-reduced-motion: reduce) { .anim-clue-touch { animation: none; box-shadow: 0 0 0 3px var(--color-card-glow); } .anim-stack-flash { animation: none; filter: drop-shadow(0 0 24px var(--color-card-glow)); } }` — under reduced motion the highlight/flash becomes an instant static style for as long as React keeps the class applied (CLUE_HIGHLIGHT_MS / STACK_FLASH_MS), never disappearing entirely (UI-SPEC). Also add a `.sr-only` class only if Tailwind v4's built-in `sr-only` utility is unavailable (it is built in — do not duplicate). No hex outside @theme (use var() in keyframes; rgba() literals from UI-SPEC are allowed only where the file already permits non-hex values — prefer var(--color-card-glow)).

    apps/web/lib/suit-visuals.ts: export `interface SuitVisual { label: string; hueVar: string; glyphPath: string }` and `export const SUIT_VISUALS: Readonly<Record<Suit, SuitVisual>>` with hueVar as static literal strings like "var(--color-suit-red)" and glyphPath as a single SVG path `d` string on a 24x24 viewBox for each UI-SPEC shape (circle and square expressed as paths so every glyph is one <path>). Author distinct silhouettes: red 5-point star; yellow upward triangle; green rounded-corner-free square inset to ~18px; blue circle r=9; white diamond; rainbow 8-point starburst (clearly more, thinner points than the red star so the two never read alike); black hexagon with a V notch cut into the top edge. Export `suitVisual(suit: Suit): SuitVisual`. Header comment: D-06 (all seven defined now; Phase 7 tests, not designs), D-07 (no icon font / lucide), D-10 (hue never varies with luminosity). Run tests to green.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web -t SuitGlyph</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project web -t SuitGlyph` passes with >=6 tests including the contrast test
    - `grep -c "^  --color-suit-" apps/web/app/globals.css` outputs 7
    - `grep -c "^  --color-bg:" apps/web/app/globals.css` outputs 1 (no redefinition)
    - `grep -n "prefers-reduced-motion" apps/web/app/globals.css` matches
    - `awk '/^@theme/{f=1} f&&/^}/{f=0; next} !f' apps/web/app/globals.css | grep -cE "#[0-9A-Fa-f]{3,8}\b"` outputs 0 (no hex outside @theme)
    - `grep -c "lucide-react" apps/web/lib/suit-visuals.ts` outputs 0
  </acceptance_criteria>
  <done>All seven suit hues and glyph silhouettes exist, are exhaustive and distinct, and pass AA contrast by automated test.</done>
</task>

<task type="auto">
  <name>Task 2: SuitGlyph inline SVG component</name>
  <files>apps/web/components/hanabi/SuitGlyph.tsx</files>
  <read_first>
    - apps/web/lib/suit-visuals.ts (Task 1 output)
    - apps/web/components/Button.tsx (component/clsx conventions)
    - .planning/phases/06-game-interface/06-PATTERNS.md section "SuitGlyph.tsx"
    - .planning/phases/06-game-interface/06-UI-SPEC.md "Card Anatomy" glyph sizes
  </read_first>
  <action>
    Create apps/web/components/hanabi/SuitGlyph.tsx exporting `SuitGlyph({ suit, size, className, title, exposeSuit = false }: { suit: Suit; size: number; className?: string; title?: string; exposeSuit?: boolean })`. Render one `<svg viewBox="0 0 24 24" width={size} height={size} focusable="false">` containing one `<path d={SUIT_VISUALS[suit].glyphPath} />` with `style={{ fill: SUIT_VISUALS[suit].hueVar }}` (inline style with the static var string — never an interpolated Tailwind class). Accessibility and HIDE-01 text rule: the SVG contributes NO text nodes (no <title>/<text> children), because Playwright textContent comparisons in e2e/start-game.spec.ts compare card text across regions; when `title` is provided set `role="img"` and `aria-label={title}`, otherwise `aria-hidden="true"`. Add `data-glyph={suit}` ONLY when `exposeSuit` is true — own-hand callers never pass it (D-15). Do not import lucide-react (D-07). No "use client" needed unless the file uses hooks (it does not); keep it a plain function component usable from client components. Glyph opacity is always 1 (D-10) — never accept an opacity prop.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web && grep -c "SUIT_VISUALS\[" apps/web/components/hanabi/SuitGlyph.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b apps/web` exits 0
    - `grep -cE "<title|<text" apps/web/components/hanabi/SuitGlyph.tsx` outputs 0
    - `grep -c "aria-hidden" apps/web/components/hanabi/SuitGlyph.tsx` >= 1
    - `grep -cE "lucide-react|opacity" apps/web/components/hanabi/SuitGlyph.tsx` outputs 0
    - `grep -c "fill-suit-" apps/web/components/hanabi/SuitGlyph.tsx` outputs 0 (no interpolated Tailwind class names)
  </acceptance_criteria>
  <done>SuitGlyph renders any of the seven suits as a text-free inline SVG in its constant hue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suit value -> DOM attributes | SuitGlyph can emit suit identity into markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | SuitGlyph data-glyph / text nodes leaking suit on own-hand cards (D-15/HIDE-01) | mitigate | no <title>/<text> children; data-glyph only behind opt-in exposeSuit prop that own-hand code never passes (enforced by 06-03 source-scan test) |
| T-06-05 | Tampering | supply chain via a new icon dependency | mitigate | zero new packages; glyphs hand-authored inline SVG (D-07); no package install tasks, so no package legitimacy gate needed |
| T-06-06 | Denial of Service | unreadable suits for colorblind/low-vision players | mitigate | automated AA contrast test for all seven hues vs bg and surface; distinct silhouettes test |
</threat_model>

<verification>
- `npx vitest run --project web` green
- `npx tsc -b apps/web` clean
- `git diff --stat -- apps/web/package.json package-lock.json` empty (no new deps)
</verification>

<success_criteria>
- Seven suits have glyph + hue, AA-verified, defined in one module (D-06)
- Motion is CSS-only with a reduced-motion fallback (D-22)
</success_criteria>

<output>
Create `.planning/phases/06-game-interface/06-02-SUMMARY.md` when done
</output>
