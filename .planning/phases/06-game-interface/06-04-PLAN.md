---
phase: 06-game-interface
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/web/components/hanabi/Table.tsx
  - apps/web/components/hanabi/CluePicker.tsx
  - apps/web/components/hanabi/CardActions.tsx
  - apps/web/components/hanabi/EndOverlay.tsx
autonomous: true
requirements: [UI-01, UI-06, UI-09, UI-10, RULES-11]

must_haves:
  truths:
    - "D-01: the tableau renders clue tokens, fuse tokens, deck count (with final-round counter), discard pile, and every played stack with no menu, drawer, tab, or hover gate"
    - "D-04: deck-count reads 'Final round — N turns left' when finalTurnsRemaining !== null, else 'N cards left in deck'"
    - "D-05: stack heads, discard entries, color-clue buttons, and end-screen stacks all render SuitGlyph with hue"
    - "D-11: a stack reaching 5 plays a single 600ms flash (anim-stack-flash) and completed stacks carry a static glow; no always-running animation"
    - "D-16: Play and Discard act on the selected own card; the clue picker offers every cluable color (variant cluableColors) and ranks 1-5 for the selected teammate"
    - "D-17: hovering OR focusing a clue value reports that clue for preview; values that would touch zero cards on the selected target render disabled (not hidden) with a visible reason"
    - "D-18 / RULES-11: every disabled Play/Discard/Give-clue control shows a visible inline reason from disabledReasonFor, linked by aria-describedby"
    - "D-19: every control is a native button (or link) with a visible focus ring; hover preview has a focus equivalent"
    - "D-20: when the room status is ended, a modal overlay over the still-visible board shows 'Game over', 'Final score: S / MAX — band', the derived end reason when non-null, and every stack with glyph and top rank"
    - "D-21: the overlay's only action is a 'New game' link to /"
  artifacts:
    - path: "apps/web/components/hanabi/Table.tsx"
      provides: "tableau: stacks, tokens, deck/final round, discard"
      exports: ["Table"]
    - path: "apps/web/components/hanabi/CluePicker.tsx"
      provides: "target + color/rank grid + preview + give clue"
      exports: ["CluePicker"]
    - path: "apps/web/components/hanabi/CardActions.tsx"
      provides: "Play / Discard with reasons"
      exports: ["CardActions"]
    - path: "apps/web/components/hanabi/EndOverlay.tsx"
      provides: "end-of-game modal"
      exports: ["EndOverlay"]
  key_links:
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "apps/web/lib/hanabi-visual-logic.ts"
      via: "deckCountText + newlyCompletedStacks"
      pattern: "deckCountText\\(|newlyCompletedStacks\\("
    - from: "apps/web/components/hanabi/CluePicker.tsx"
      to: "apps/web/lib/hanabi-visual-logic.ts"
      via: "disabledReasonFor({ kind: \"clue\" ... })"
      pattern: "disabledReasonFor\\("
    - from: "apps/web/components/hanabi/EndOverlay.tsx"
      to: "apps/web/lib/hanabi-visual-logic.ts"
      via: "endReasonForView + END_REASON_COPY"
      pattern: "endReasonForView\\("
---

<objective>
Build the tableau (D-01, D-04, D-11), the action controls with visible disabled reasons and clue preview (D-16, D-17, D-18, D-19, RULES-11), and the designed end-of-game overlay (D-20, D-21).

Purpose: UI-01 (everything always visible), RULES-11 (visibly unavailable actions), and UI-10 (designed end screen) are delivered here as thin components over 06-01 derivations.
Output: Table.tsx, CluePicker.tsx, CardActions.tsx, EndOverlay.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-game-interface/06-CONTEXT.md
@.planning/phases/06-game-interface/06-UI-SPEC.md
@.planning/phases/06-game-interface/06-PATTERNS.md
@.planning/phases/06-game-interface/06-01-SUMMARY.md
@.planning/phases/06-game-interface/06-02-SUMMARY.md
@apps/web/components/HanabiBoard.tsx

<interfaces>
From apps/web/lib/hanabi-visual-logic.ts (06-01):
  type ActionIntent = { kind: "play"; selectedCardId: string|null } | { kind: "discard"; selectedCardId: string|null } | { kind: "clue"; targetSeatId: string|null; clue: Clue|null }
  interface ActionContext { reconnecting: boolean; ended: boolean; labelFor?: (seatId: string) => string }
  disabledReasonFor(view, intent, ctx): string | null
  endReasonForView(view): "fuses_exhausted" | "all_stacks_complete" | "final_round_elapsed" | null; END_REASON_COPY
  deckCountText(view): string; newlyCompletedStacks(prev, next): Suit[]; STACK_FLASH_MS = 600
From apps/web/lib/hanabi-board-logic.ts: clueTouchCountForTarget, cluableColorsForView, fusesRemainingForView, bandForView
From "@games/rules": RANKS, MAX_FUSES, maxScoreFor, variantConfig, types HanabiView, Clue, Rank, Suit
From apps/web/lib/suit-visuals.ts (06-02): SUIT_VISUALS[suit].label
From apps/web/components/hanabi/SuitGlyph.tsx (06-02): SuitGlyph({ suit, size, title?, exposeSuit? })
From apps/web/components/Button.tsx: Button({ variant: "primary" | "ghost", ...buttonProps }) — primary is the accent fill; UI-SPEC reserves accent fill in-game for "New game" ONLY, so in-game action buttons use variant="ghost"

Next.js 16.3.4 (apps/web/AGENTS.md: read node_modules/next/dist/docs/01-app/03-api-reference/02-components/link.md before using Link).

E2E contracts that MUST hold (existing specs):
  - testids: played-stack-{suit} (textContent must change when topRank changes), discard-pile (each discarded card is an <li> under it), clue-tokens ("{n} clue tokens"), fuse-tokens ("{n} fuses left"), deck-count (textContent is ONLY deckCountText — start-game.spec parses it with parseInt), play-button, discard-button, clue-target-{seatId}, clue-value-{color}, clue-value-{rank}, give-clue-button, game-over-heading ("Game over"), final-score
  - e2e/hanabi-realtime.spec.ts iterates every [data-testid^="clue-value-"] button — no other testid may start with "clue-value-" or "clue-target-" or "played-stack-"
  - RT-04 spec asserts play-button, discard-button, give-clue-button exist and are disabled while reconnecting even with no selection; start-game.spec asserts play/discard exist and are disabled on the waiting player's page — controls are ALWAYS rendered during a game, never hidden
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table — stacks, tokens, deck/final round, discard pile, stack-complete flash</name>
  <files>apps/web/components/hanabi/Table.tsx</files>
  <read_first>
    - apps/web/components/HanabiBoard.tsx lines 183-250 (tableau markup, testids, copy)
    - e2e/start-game.spec.ts lines 100-120 (deck-count parseInt, discard li count, stack text join)
    - apps/web/lib/hanabi-visual-logic.ts (deckCountText, newlyCompletedStacks, STACK_FLASH_MS)
    - .planning/phases/06-game-interface/06-UI-SPEC.md sections "Layout" (center band), "Card Anatomy" (stack/discard 48x64), "Fireworks-night motion", "Preserved Wire/Copy Contracts"
  </read_first>
  <action>
    Create Table.tsx with "use client" exporting `Table({ game }: { game: HanabiView })`. Root `<section data-testid="tableau" aria-label="Table">` using flex-wrap rows with --space-md gaps on a --color-surface panel with --color-border border (dark layered look from existing tokens, D-11/UI-09).

    Row 1 (per D-01): Label "Fireworks" + one element per `game.stacks` entry: `<div data-testid={"played-stack-" + stack.suit} data-top-rank={stack.topRank} data-complete={String(stack.topRank === 5)}>` sized 48x64, rounded-md, containing `<SuitGlyph suit={stack.suit} size={18} exposeSuit />`, a sr-only label span, and a rank numeral span (Label 14px/600) showing topRank or "—" when 0. Completed stacks (topRank 5) get the known-step LUMINOSITY-style static glow border (2px var(--color-card-glow) + 0 0 16px glow) — import LUMINOSITY_FRAME is not available in this wave (06-03 owns it), so inline the same two literal style strings with a comment pointing at luminosity-frame.ts. Stack flash (D-11): keep `useRef` of the previous stacks; in `useEffect` on game.stacks compute `newlyCompletedStacks(prev, game.stacks)` (skip on first render so a refresh does not replay), store the suits in state, and clear them after STACK_FLASH_MS via setTimeout with cleanup; flashing stacks add className "anim-stack-flash". Then the tokens group: `<p data-testid="clue-tokens">{game.clueTokens} clue tokens</p>` next to 8 aria-hidden pip dots (filled --color-text for available, --color-border outline for spent) rendered OUTSIDE the testid element; `<p data-testid="fuse-tokens">{fusesRemainingForView(game)} fuses left</p>` next to MAX_FUSES aria-hidden pips where spent fuses use --color-destructive (UI-SPEC's single new destructive use).

    Row 2: `<p data-testid="deck-count" data-final-round={String(game.finalTurnsRemaining !== null)}>{deckCountText(game)}</p>` — nothing else inside that element (D-04). Then `<div data-testid="discard-pile">` with Label "Discard" and, when non-empty, a `<ul className="flex flex-wrap ...">` with one `<li>` per game.discard entry IN ORDER (UI-SPEC: flat, not grouped) each showing SuitGlyph size 18 exposeSuit + sr-only label + numeral, 48x64-ish compact tile; when empty the existing copy "No cards discarded yet". The discard list must wrap horizontally (flex-wrap, max width of the row) so ~20+ entries stay visible without a drawer.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b apps/web` exits 0
    - `grep -cE 'data-testid="(clue-tokens|fuse-tokens|deck-count|discard-pile|tableau)"' apps/web/components/hanabi/Table.tsx` outputs 5
    - `grep -c 'data-testid={"played-stack-" + stack.suit}' apps/web/components/hanabi/Table.tsx` outputs 1
    - `grep -c "deckCountText(game)" apps/web/components/hanabi/Table.tsx` outputs 1
    - `grep -c "<li" apps/web/components/hanabi/Table.tsx` >= 1
    - `grep -c "anim-stack-flash" apps/web/components/hanabi/Table.tsx` >= 1
    - `grep -c "No cards discarded yet" apps/web/components/hanabi/Table.tsx` outputs 1
  </acceptance_criteria>
  <done>The tableau renders every UI-01 element permanently with glyphs, final-round text, and a one-shot completion flash.</done>
</task>

<task type="auto">
  <name>Task 2: CardActions and CluePicker with disabled reasons and hover/focus preview</name>
  <files>apps/web/components/hanabi/CardActions.tsx, apps/web/components/hanabi/CluePicker.tsx</files>
  <read_first>
    - apps/web/components/HanabiBoard.tsx lines 119-124 and 311-417 (existing controls, testids, captions)
    - apps/web/components/Button.tsx
    - apps/web/lib/hanabi-visual-logic.ts (disabledReasonFor) and apps/web/lib/hanabi-board-logic.ts (clueTouchCountForTarget, cluableColorsForView)
    - e2e/hanabi-realtime.spec.ts lines 14-32 and 211-217 (clue-value iteration; disabled controls while reconnecting)
    - .planning/phases/06-game-interface/06-UI-SPEC.md section "Action Interaction (RULES-11)"
  </read_first>
  <action>
    CardActions.tsx: export `CardActions({ game, selectedCardId, ctx, onPlay, onDiscard })` with ctx: ActionContext. Compute `playReason = disabledReasonFor(game, { kind: "play", selectedCardId }, ctx)` and `discardReason` likewise. Render two `Button variant="ghost"` controls always (never hidden): `data-testid="play-button"` text "Play", `disabled={playReason !== null}`, `aria-describedby="action-reason-play"` when a reason exists, onClick calls onPlay; same for `data-testid="discard-button"` / "Discard" / onDiscard. Beneath each, when its reason is non-null, a visible `<p id="action-reason-play" data-testid="action-reason-play">` (and `action-reason-discard`) in Label size with --color-text-muted (per D-18, never title-only). When both reasons are identical (e.g. "Not your turn"), still render both elements (e2e asserts each by testid) but they may share one visual line.

    CluePicker.tsx: export `CluePicker({ game, targets, clueTarget, clueValue, ctx, onSelectTarget, onSelectValue, onPreview, onGive })` with targets: Array<{ seatId: string; label: string }>, onPreview: (clue: Clue | null) => void. Render (per D-16) Label "Clue"; a row of target buttons `Button variant="ghost" data-testid={"clue-target-" + seatId} aria-pressed={clueTarget === seatId} disabled={ctx.reconnecting || ctx.ended}` with the selected one outlined `2px solid var(--color-text)` (not accent fill — accent fill is reserved for "New game"); Label "Color or rank"; one button per `cluableColorsForView(game)` color with `data-testid={"clue-value-" + color}` containing `<SuitGlyph suit={color} size={16} />` plus visible label text `SUIT_VISUALS[color].label` (D-05: glyph on colour-clue buttons), then one per RANKS value `data-testid={"clue-value-" + rank}` with the numeral. Value button disabled rule (per D-17/D-18): `ctx.reconnecting || ctx.ended || !game.isYourTurn || game.clueTokens <= 0 || (clueTarget !== null && clueTouchCountForTarget(game, clueTarget, clue) === 0)`. Every value button wires `onMouseEnter` and `onFocus` to `onPreview(clue)` and `onMouseLeave` and `onBlur` to `onPreview(null)` (D-19 focus equivalent), `aria-pressed` for the selected value, and onClick `onSelectValue(clue)`. When clueTarget is set and at least one value is disabled only because it touches zero cards, render visible `<p data-testid="zero-touch-reason">Dimmed options wouldn't touch any of {label}'s cards</p>`. Then `Button variant="ghost" data-testid="give-clue-button"` "Give clue", `disabled={clueReason !== null}` where `clueReason = disabledReasonFor(game, { kind: "clue", targetSeatId: clueTarget, clue: clueValue }, ctx)`, onClick onGive, with visible `<p id="action-reason-clue" data-testid="action-reason-clue">{clueReason}</p>` when non-null and aria-describedby. No new testid starting with "clue-value-" or "clue-target-" besides the value/target buttons themselves.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web && npx vitest run --project web</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b apps/web` exits 0
    - `grep -cE 'data-testid="(play-button|discard-button|action-reason-play|action-reason-discard)"' apps/web/components/hanabi/CardActions.tsx` outputs 4
    - `grep -cE 'data-testid="(give-clue-button|action-reason-clue|zero-touch-reason)"' apps/web/components/hanabi/CluePicker.tsx` outputs 3
    - `grep -c "onFocus" apps/web/components/hanabi/CluePicker.tsx` >= 1 and `grep -c "onMouseEnter" apps/web/components/hanabi/CluePicker.tsx` >= 1
    - `grep -c 'variant="primary"' apps/web/components/hanabi/CluePicker.tsx apps/web/components/hanabi/CardActions.tsx` totals 0
    - `grep -c "disabledReasonFor(" apps/web/components/hanabi/CardActions.tsx` >= 2
    - `grep -c "title=" apps/web/components/hanabi/CardActions.tsx` outputs 0 (reasons are visible text, not title-only)
  </acceptance_criteria>
  <done>Actions are always rendered, disabled with visible reasons, and the clue picker previews on hover and focus.</done>
</task>

<task type="auto">
  <name>Task 3: EndOverlay — score / max, band, end reason, stacks, New game</name>
  <files>apps/web/components/hanabi/EndOverlay.tsx</files>
  <read_first>
    - apps/web/components/RefusalCard.tsx (centered card-on-scrim pattern)
    - apps/web/components/HanabiBoard.tsx lines 418-435 (game-over testids/copy)
    - apps/web/components/Button.tsx (primary classes to mirror on a link)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/link.md (Next 16 Link API)
    - .planning/phases/06-game-interface/06-UI-SPEC.md section "End of Game"
  </read_first>
  <action>
    Create EndOverlay.tsx with "use client" exporting `EndOverlay({ game }: { game: HanabiView })` (the board renders it only when view.status === "ended"). Structure (per D-20): a fixed `inset-0` scrim `<div data-testid="end-overlay" style={{ background: "rgba(11, 15, 26, 0.7)" }}>` centering `<div role="dialog" aria-modal="true" aria-labelledby="game-over-heading">` card styled like RefusalCard (surface bg, border, --space-lg padding, max-w-md). Content top to bottom: `<h2 id="game-over-heading" data-testid="game-over-heading">Game over</h2>` (Heading role); `<p data-testid="final-score">Final score: {game.score} / {maxScoreFor(variantConfig(game.variant))} — {bandForView(game)}</p>` (D-20 requires score / max; this extends the Phase 4 string by inserting " / MAX" — no existing spec asserts it; 06-06 asserts the new form); when `endReasonForView(game)` is non-null `<p data-testid="end-reason">{END_REASON_COPY[reason]}</p>` in Label muted, otherwise omit the element; `<ul data-testid="end-stacks">` with one `<li data-testid="end-stack" data-top-rank={stack.topRank} data-complete={String(stack.topRank === 5)}>` per game.stacks entry showing SuitGlyph size 18 with title label + numeral or "—", completed ones with the card-glow border (use testid "end-stack", never "played-stack-" which would double-count e2e stack locators). Single action (D-21): Next `Link` `href="/"` `data-testid="new-game-link"` text "New game", styled with Button.tsx's primary classes (accent fill, --color-bg label, min touch size, focus-visible outline). Move focus to the link on mount via a ref + useEffect (keyboard reachability, D-19). No rematch, no close button, no other action.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b apps/web` exits 0
    - `grep -cE 'data-testid="(end-overlay|game-over-heading|final-score|end-reason|end-stacks|end-stack|new-game-link)"' apps/web/components/hanabi/EndOverlay.tsx` outputs 7
    - `grep -c 'href="/"' apps/web/components/hanabi/EndOverlay.tsx` outputs 1
    - `grep -c "endReasonForView(" apps/web/components/hanabi/EndOverlay.tsx` outputs 1
    - `grep -c 'aria-modal="true"' apps/web/components/hanabi/EndOverlay.tsx` outputs 1
    - `grep -ciE "rematch|play again" apps/web/components/hanabi/EndOverlay.tsx` outputs 0
  </acceptance_criteria>
  <done>The end overlay shows score/max, band, reason, glyph stacks, and only a New game link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click -> onAction -> socket | client intent crosses to the authoritative RoomDO |
| displayLabel -> reason copy | untrusted names interpolated into visible captions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | disabled controls bypassed via devtools | accept | server re-validates every action (04 D-10 closed refusal enum); client disabling is UX only |
| T-06-11 | Tampering (XSS) | names in "wouldn't touch any of {name}'s cards" captions | mitigate | React text interpolation only; no dangerouslySetInnerHTML |
| T-06-12 | Information Disclosure | discard/stack/end-screen glyphs exposing hidden info | accept | discard, stacks, and history are public facts already on every seat's wire view; nothing from own hand is rendered here |
| T-06-13 | Denial of Service | open-redirect style navigation from end screen | mitigate | New game link is a hard-coded relative "/" href, no user-controlled URL |
</threat_model>

<verification>
- `npx tsc -b apps/web` clean
- `npx vitest run --project web` green
- `grep -rn "dangerouslySetInnerHTML" apps/web/components/hanabi` returns nothing
</verification>

<success_criteria>
- UI-01 tableau, RULES-11 reasons, and UI-10 overlay components exist with the testids 06-05/06-06 rely on
</success_criteria>

<output>
Create `.planning/phases/06-game-interface/06-04-SUMMARY.md` when done
</output>
