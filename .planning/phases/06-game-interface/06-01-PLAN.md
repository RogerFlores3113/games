---
phase: 06-game-interface
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/hanabi-visual-logic.ts
  - apps/web/lib/hanabi-visual-logic.test.ts
  - apps/web/lib/hanabi-board-logic.ts
  - apps/web/lib/hanabi-board-logic.test.ts
autonomous: true
requirements: [UI-01, UI-04, UI-05, UI-08, UI-10, RULES-11]

must_haves:
  truths:
    - "D-23: every visual derivation the new board needs is a pure, unit-tested function in apps/web/lib (no DOM types, no React imports)"
    - "D-08: luminosityStepFor returns 'unclued' for zero positive clues (even when negative clues narrowed candidates), 'touched' for >=1 positive clue, 'known' when possibleSuits and possibleRanks both have length 1"
    - "D-12: candidateDisplayFor lists exactly the variant's suits (variantConfig(variant).suits, so Rainbow shows rainbow) and ranks 1-5, each flagged possible or ruled out, plus the confirmed suit/rank only when narrowed to one"
    - "D-15: an identity tripwire test proves the own-hand helpers (luminosityStepFor, candidateDisplayFor) never read any key other than possibleSuits/possibleRanks/positiveClues/negativeClues"
    - "D-14: touchedCardIdsFromLatestClue returns the touchedCardIds of the last clue entry at or after a given history index, and [] when no such clue exists"
    - "D-17: clueTouchIdsForTarget returns exactly the visible target card ids a clue would touch, dispatching through variantConfig (Rainbow touched by every color)"
    - "D-18: disabledReasonFor returns a short reason string for every disabled play/discard/clue state (ended, reconnecting, not your turn, clue tokens full, no clue tokens, nothing selected, clue touches nothing) and null when the action is allowed"
    - "D-20: endReasonForView mirrors checkHanabiGameEnd's fixed order over the redacted view: fuses_exhausted, then all_stacks_complete, then final_round_elapsed, else null"
    - "D-04: deckCountText returns 'Final round — N turns left' when finalTurnsRemaining !== null, else 'N cards left in deck'"
    - "D-01: teammatesInTurnOrder orders otherHands starting with the seat after the viewer in room seat order"
    - "D-11: newlyCompletedStacks returns suits whose topRank went from <5 to 5 between two stack snapshots"
  artifacts:
    - path: "apps/web/lib/hanabi-visual-logic.ts"
      provides: "luminosityStepFor, candidateDisplayFor, touchedCardIdsFromLatestClue, disabledReasonFor, endReasonForView, END_REASON_COPY, deckCountText, teammatesInTurnOrder, newlyCompletedStacks, CLUE_HIGHLIGHT_MS, STACK_FLASH_MS"
      exports: ["luminosityStepFor", "candidateDisplayFor", "touchedCardIdsFromLatestClue", "disabledReasonFor", "endReasonForView", "END_REASON_COPY", "deckCountText", "teammatesInTurnOrder", "newlyCompletedStacks", "CLUE_HIGHLIGHT_MS", "STACK_FLASH_MS"]
    - path: "apps/web/lib/hanabi-visual-logic.test.ts"
      provides: "unit tests + identity tripwire"
      contains: "identity tripwire"
    - path: "apps/web/lib/hanabi-board-logic.ts"
      provides: "clueTouchIdsForTarget"
      exports: ["clueTouchIdsForTarget"]
  key_links:
    - from: "apps/web/lib/hanabi-visual-logic.ts"
      to: "@games/rules"
      via: "variantConfig / maxScoreFor / MAX_FUSES / RANKS imports"
      pattern: "from \"@games/rules\""
    - from: "apps/web/lib/hanabi-visual-logic.ts disabledReasonFor"
      to: "apps/web/lib/hanabi-board-logic.ts predicates"
      via: "isPlayDisabled / isDiscardDisabled / clueTouchCountForTarget"
      pattern: "hanabi-board-logic"
---

<objective>
Build the pure derivation layer (D-23) that every Phase 6 component consumes: luminosity step (D-08), candidate/confirmed display (D-12, D-15), latest-clue touched ids (D-14), clue-preview ids (D-17), disabled reasons (D-18, RULES-11), end reason (D-20), final-round deck text (D-04), teammate turn order (D-01), and newly-completed stacks (D-11).

Purpose: the web Vitest project has no DOM environment (RESEARCH Pitfall 3), so this is the only unit-testable tier of the frontend. Components in later plans only render what these functions return.
Output: hanabi-visual-logic.ts + test, clueTouchIdsForTarget added to hanabi-board-logic.ts + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/06-game-interface/06-CONTEXT.md
@.planning/phases/06-game-interface/06-RESEARCH.md
@.planning/phases/06-game-interface/06-PATTERNS.md
@apps/web/lib/hanabi-board-logic.ts
@apps/web/lib/hanabi-board-logic.test.ts

<interfaces>
From packages/rules/src/index.ts (exports available via "@games/rules"):
- values: variantConfig, handSizeFor, maxScoreFor, ALL_SUITS, RANKS, MAX_FUSES, scoreBand, currentScore
- types: HanabiView, HanabiCardView, Clue, Suit, Rank, VariantConfig, Variant
- NOT exported: ClueFactsView, HistoryEntryView. Derive them with indexed access types instead of editing packages/rules (wire contract frozen):
  export type CardFacts = HanabiCardView["facts"];
  export type HistoryEntry = HanabiView["history"][number];

From packages/rules/src/hanabi/state.ts:
  HanabiCardView = { id; hidden: true; facts: ClueFactsView } | { id; hidden: false; suit: Suit; rank: Rank; facts: ClueFactsView }
  ClueFactsView = { possibleSuits: Suit[]; possibleRanks: Rank[]; positiveClues: Array<{type:"color"|"rank"; value: Suit|Rank}>; negativeClues: same }
  HistoryEntryView clue variant = { turn; type: "clue"; seatId; targetSeatId; clue: {type; value}; touchedCardIds: string[] } (other variants: play, discard, draw)
  HanabiView = { variant; yourSeatId: string|null; yourHand; otherHands: Array<{seatId; cards}>; stacks: Array<{suit; topRank:number}>; discard: Array<{id; suit; rank}>; clueTokens; fuses (USED); deckCount; finalTurnsRemaining: number|null; activeSeatId; isYourTurn; score; history }

From packages/rules/src/hanabi/variant.ts:
  variantConfig(v).suits — base: red,yellow,green,blue,white; rainbow: +rainbow; black: +black
  variantConfig(v).cluableColors — rainbow variant excludes "rainbow"
  colorClueTouches(suit, clueColor), rankClueTouches(rank, clueRank); maxScoreFor(config) = suits.length * 5

From apps/web/lib/hanabi-board-logic.ts (existing, keep unchanged): clueTouchCountForTarget, isPlayDisabled, isDiscardDisabled (true when !isYourTurn || clueTokens >= 8), isGiveClueDisabled, bandForView, fusesRemainingForView, isSeatConnected, turnIndicatorText, cluableColorsForView. baseView(overrides) fixture lives in hanabi-board-logic.test.ts.

Room seat order: RoomView.seats[] (public seats, same order the worker passes as game seatIds — apps/worker/src/room-state.ts `seatIds: state.seats.map(seat => seat.seatId)`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clue-memory derivations — luminosity, candidates, latest-clue ids, completed stacks, identity tripwire</name>
  <files>apps/web/lib/hanabi-visual-logic.ts, apps/web/lib/hanabi-visual-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-board-logic.ts (header-comment + dispatch-through-engine convention to copy)
    - apps/web/lib/hanabi-board-logic.test.ts (baseView fixture shape to copy verbatim)
    - packages/rules/src/hanabi/state.ts (exact view field names)
    - packages/rules/src/hanabi/variant.ts (suits vs cluableColors)
    - packages/rules/src/hanabi/clue-facts.ts (how possibleSuits/possibleRanks are narrowed, so fixtures are realistic)
    - .planning/phases/06-game-interface/06-UI-SPEC.md sections "Luminosity System" and "Own-hand card"
  </read_first>
  <behavior>
    - luminosityStepFor({possibleSuits:[5 suits], possibleRanks:[1..5], positiveClues:[], negativeClues:[]}) === "unclued"
    - luminosityStepFor with negativeClues non-empty and possibleSuits narrowed to 2 but positiveClues [] === "unclued" (D-08 anti-goal)
    - luminosityStepFor with one positive color clue, possibleSuits ["red"], possibleRanks 5 values === "touched"
    - luminosityStepFor with possibleSuits ["red"], possibleRanks [3] === "known" (even if positiveClues is empty — full narrowing via negatives still counts as known per D-08 step 3 trigger)
    - candidateDisplayFor(facts, "base").suits has 5 entries in variantConfig("base").suits order; candidateDisplayFor(facts, "rainbow").suits includes "rainbow"; "black" includes "black"
    - a suit absent from facts.possibleSuits has possible:false; rank entries always 1..5 in RANKS order with possible flag
    - confirmedSuit is the suit only when possibleSuits.length === 1, else null; confirmedRank likewise
    - positiveMarks maps each positive clue to {type:"color", suit} or {type:"rank", rank}, deduplicated, in first-seen order
    - touchedCardIdsFromLatestClue(history) returns the last clue entry's touchedCardIds; with a trailing play+draw after the clue and sinceIndex 0 it still returns that clue's ids; with sinceIndex greater than the clue's index it returns []; empty history returns []
    - newlyCompletedStacks([{suit:"red",topRank:4}], [{suit:"red",topRank:5}]) deep-equals ["red"]; unchanged 5 -> 5 returns []
    - identity tripwire (D-15): luminosityStepFor and candidateDisplayFor, called with a Proxy over a facts object whose get trap records every string key and throws on any key outside {possibleSuits, possibleRanks, positiveClues, negativeClues} (symbol keys ignored), do not throw, and the recorded key set is a subset of those four keys
  </behavior>
  <action>
    RED first: write apps/web/lib/hanabi-visual-logic.test.ts covering every behavior above, copying the baseView(overrides) fixture shape from hanabi-board-logic.test.ts (copy, do not import across test files) plus a factsFor(partial) helper; use describe names exactly `luminosityStepFor`, `candidateDisplayFor`, `touchedCardIdsFromLatestClue`, `newlyCompletedStacks`, and `identity tripwire (D-15)` so VALIDATION.md's `-t` filters match. Run `npx vitest run --project web apps/web/lib/hanabi-visual-logic.test.ts` and confirm it fails; commit `test(06-01): add failing clue-memory derivation tests`.

    GREEN: create apps/web/lib/hanabi-visual-logic.ts with a header comment stating the D-23 / D-15 boundary (pure derivations over the redacted view only; own-hand helpers accept only CardFacts which has no suit/rank; if a helper needs suit/rank for an own-hand card, stop). Export:
    - `type CardFacts = HanabiCardView["facts"]`, `type HistoryEntry = HanabiView["history"][number]`, `type LuminosityStep = "unclued" | "touched" | "known"`.
    - `luminosityStepFor(facts: CardFacts): LuminosityStep` — known if both possible arrays have length 1; else touched if positiveClues.length > 0; else unclued.
    - `interface CandidateDisplay { suits: Array<{ suit: Suit; possible: boolean }>; ranks: Array<{ rank: Rank; possible: boolean }>; confirmedSuit: Suit | null; confirmedRank: Rank | null; positiveMarks: Array<{ type: "color"; suit: Suit } | { type: "rank"; rank: Rank }> }` and `candidateDisplayFor(facts: CardFacts, variant: Variant): CandidateDisplay` — suits iterate `variantConfig(variant).suits`, ranks iterate `RANKS`.
    - `touchedCardIdsFromLatestClue(history: HistoryEntry[], sinceIndex = 0): string[]` — scan backwards from the end to sinceIndex, return a copy of the first clue entry's touchedCardIds found, else [].
    - `newlyCompletedStacks(prev: HanabiView["stacks"], next: HanabiView["stacks"]): Suit[]` — suits where next topRank === 5 and the prev entry for that suit is missing or < 5. Use the literal 5 via `RANKS[RANKS.length - 1]` rather than a bare magic number.
    - `export const CLUE_HIGHLIGHT_MS = 2000;` and `export const STACK_FLASH_MS = 600;` (UI-SPEC durations, D-14 / D-11).
    Import only from "@games/rules" (types + variantConfig, RANKS). No React, no DOM, no suit/rank access on anything typed as a card. Run the web project tests to green; commit `feat(06-01): implement clue-memory derivations`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web apps/web/lib/hanabi-visual-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project web -t luminosityStepFor` passes with >=4 tests
    - `npx vitest run --project web -t candidateDisplayFor` passes and includes a rainbow-variant case
    - `npx vitest run --project web -t touchedCardIdsFromLatestClue` passes with a sinceIndex case
    - `npx vitest run --project web -t "identity tripwire"` passes
    - `grep -v '^\s*//' apps/web/lib/hanabi-visual-logic.ts | grep -v '^\s*\*' | grep -cE "from \"react\"|document\.|window\."` outputs 0
    - `grep -n "export function luminosityStepFor(facts: CardFacts)" apps/web/lib/hanabi-visual-logic.ts` matches
    - `git log --oneline -3` shows a `test(06-01)` commit before the `feat(06-01)` commit
  </acceptance_criteria>
  <done>Clue-memory derivations exist, are pure, and are proven by unit tests including the D-15 identity tripwire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Action/turn/end derivations — clueTouchIdsForTarget, disabledReasonFor, endReasonForView, deckCountText, teammatesInTurnOrder</name>
  <files>apps/web/lib/hanabi-visual-logic.ts, apps/web/lib/hanabi-visual-logic.test.ts, apps/web/lib/hanabi-board-logic.ts, apps/web/lib/hanabi-board-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-visual-logic.ts (Task 1 output)
    - apps/web/lib/hanabi-board-logic.ts (clueTouchCountForTarget dispatch pattern, D-12 boundary comment, MAX_CLUE_TOKENS)
    - apps/web/lib/hanabi-board-logic.test.ts (existing describe blocks to extend)
    - packages/rules/src/hanabi/endgame.ts (checkHanabiGameEnd fixed order)
    - apps/web/components/HanabiBoard.tsx lines 119-124, 330-337, 399-415 (existing caption copy to preserve verbatim)
    - .planning/phases/06-game-interface/06-UI-SPEC.md sections "Action Interaction" and "End of Game"
  </read_first>
  <behavior>
    - clueTouchIdsForTarget(view with seat-b cards [b1 red 3 visible, b2 blue 3 visible], "seat-b", {type:"rank", value:3}) deep-equals ["b1","b2"]; color red -> ["b1"]; unknown seat -> []; rainbow variant with a rainbow card: color clue blue touches it; hidden cards are never included
    - clueTouchCountForTarget(...) === clueTouchIdsForTarget(...).length for the same fixtures (existing tests unchanged and still passing)
    - disabledReasonFor(view, {kind:"play", selectedCardId:"a1"}, {reconnecting:false, ended:false}) === null when isYourTurn
    - ended:true -> "The game has ended" for every kind (checked first)
    - reconnecting:true -> "Reconnecting — actions paused" (checked second)
    - isYourTurn false -> "Not your turn" for play, discard, and clue
    - play with selectedCardId null -> "Select a card in your hand first"
    - discard with clueTokens 8 -> "Clue tokens are full — you can't discard" (checked before the selection check)
    - clue with clueTokens 0 -> "No clue tokens left"
    - clue with targetSeatId null -> "Choose a teammate to clue"; with clue null -> "Choose a color or rank"
    - clue that touches zero cards -> "That clue wouldn't touch any of Bianca's cards" when ctx.labelFor returns "Bianca"
    - endReasonForView: fuses 3 -> "fuses_exhausted" (even if score is max); base score 25 -> "all_stacks_complete"; finalTurnsRemaining 0 -> "final_round_elapsed"; none -> null; black variant uses max 30
    - END_REASON_COPY maps the three reasons to "Three fuses were lost.", "Every stack was completed!", "The deck ran out and the final round elapsed."
    - deckCountText({deckCount:12, finalTurnsRemaining:null}) === "12 cards left in deck"; ({deckCount:0, finalTurnsRemaining:2}) === "Final round — 2 turns left"
    - teammatesInTurnOrder: seatOrder [a,b,c,d], yourSeatId "b", otherHands [a,c,d] -> hands in order [c,d,a]; yourSeatId null or absent from seatOrder -> otherHands unchanged; a hand whose seatId is missing from seatOrder is appended at the end in original order
  </behavior>
  <action>
    RED: extend apps/web/lib/hanabi-board-logic.test.ts with a `describe("clueTouchIdsForTarget")` block, and extend apps/web/lib/hanabi-visual-logic.test.ts with describe blocks named `disabledReasonFor`, `endReasonForView`, `deckCountText`, `teammatesInTurnOrder` for every behavior above. Confirm failure; commit `test(06-01): add failing action and end-state derivation tests`.

    GREEN in hanabi-board-logic.ts: add `export function clueTouchIdsForTarget(view: HanabiView, targetSeatId: string, clue: Clue): string[]` using the same variantConfig dispatch as clueTouchCountForTarget, then reimplement clueTouchCountForTarget as `clueTouchIdsForTarget(...).length` so there is one touch rule. Update the header comment: D-17 (Phase 6) adds the ids form of the existing count; still no new disabling rule in this file.

    GREEN in hanabi-visual-logic.ts (per D-18, D-20, D-04, D-01):
    - `type ActionIntent = { kind: "play"; selectedCardId: string | null } | { kind: "discard"; selectedCardId: string | null } | { kind: "clue"; targetSeatId: string | null; clue: Clue | null }` and `interface ActionContext { reconnecting: boolean; ended: boolean; labelFor?: (seatId: string) => string }`.
    - `disabledReasonFor(view: HanabiView, intent: ActionIntent, ctx: ActionContext): string | null` with the exact check order and copy strings listed in behavior (ended, reconnecting, not your turn, then per-kind). For discard use `isDiscardDisabled(view)` after the turn check to detect full tokens. For clue touch-nothing use `clueTouchCountForTarget(...) === 0`; labelFor defaults to returning "…". Never call canPlay/canDiscard/canClue (they need full state). Preserve the three Phase 4 strings byte-for-byte (em dash U+2014, straight apostrophe).
    - `type EndReason = "fuses_exhausted" | "all_stacks_complete" | "final_round_elapsed"`, `endReasonForView(view: HanabiView): EndReason | null` using MAX_FUSES and maxScoreFor(variantConfig(view.variant)) in engine order, and `END_REASON_COPY: Record<EndReason, string>`.
    - `deckCountText(view: Pick<HanabiView, "deckCount" | "finalTurnsRemaining">): string`.
    - `teammatesInTurnOrder<T extends { seatId: string }>(otherHands: T[], seatOrder: readonly string[], yourSeatId: string | null): T[]`.
    Commit `feat(06-01): implement action, end-reason, deck and turn-order derivations`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project web -t disabledReasonFor` passes with >=10 tests
    - `npx vitest run --project web -t endReasonForView` passes including a black-variant max-30 case
    - `npx vitest run --project web -t clueTouchIdsForTarget` passes including a rainbow case
    - `grep -F "Clue tokens are full — you can't discard" apps/web/lib/hanabi-visual-logic.ts` matches
    - `grep -F "That clue wouldn't touch any of" apps/web/lib/hanabi-visual-logic.ts` matches
    - `grep -F "Final round — " apps/web/lib/hanabi-visual-logic.ts` matches
    - `grep -cE "canPlay|canDiscard|canClue" apps/web/lib/hanabi-visual-logic.ts` outputs 0
    - `npx tsc -b apps/web` exits 0
  </acceptance_criteria>
  <done>All D-23 derivations exist and are unit-tested; the existing board-logic tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server view -> client derivations | Redacted, schema-validated HanabiView enters pure client functions |
| client derivations -> DOM (later plans) | Anything these helpers return may be rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | own-hand helpers (luminosityStepFor, candidateDisplayFor) reintroducing identity (D-15/HIDE-01) | mitigate | helpers typed to CardFacts (no suit/rank fields); Proxy identity tripwire test throws on any non-fact key access |
| T-06-02 | Tampering | client-side legality drifting from engine rules | mitigate | touch/score/fuse rules dispatch through variantConfig, maxScoreFor, MAX_FUSES; no canPlay/canClue in client; server stays authority (04 D-10) |
| T-06-03 | Elevation of Privilege | client disabling treated as enforcement | accept | disabled reasons are UX only; RoomDO re-validates every action and returns the closed refusal enum |
</threat_model>

<verification>
- `npx vitest run --project web` green
- `npx tsc -b apps/web` clean
- No file outside files_modified touched; packages/rules and packages/schema unchanged (`git diff --stat packages/` empty)
</verification>

<success_criteria>
- Every function named in 06-VALIDATION.md's Wave 0 list exists and its `-t` filter selects passing tests
- D-15 tripwire test present and green
</success_criteria>

<output>
Create `.planning/phases/06-game-interface/06-01-SUMMARY.md` when done
</output>
