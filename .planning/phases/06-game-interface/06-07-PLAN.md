---
phase: 06-game-interface
plan: 07
type: execute
wave: 5
depends_on: ["06-06"]
files_modified:
  - .planning/phases/06-game-interface/06-HUMAN-UAT.md
  - .planning/phases/06-game-interface/06-VALIDATION.md
autonomous: false
requirements: [UI-06, UI-08, UI-09, UI-11]

must_haves:
  truths:
    - "D-25: the automated phase gate is green before sign-off is requested: `npm test`, `npx playwright test`, and per-package `npx tsc -b apps/web packages/rules packages/schema apps/worker`"
    - "D-25: the owner visually reviews the designed board at desktop sizes (1280x720 and 1920x1080) in a blocking, never auto-approved checkpoint"
    - "D-25: 06-HUMAN-UAT.md records the owner's reply verbatim — quoted exactly, never paraphrased, summarized, or fabricated; a waiver is recorded verbatim the same way"
    - "D-25: the checkpoint and the UAT record both offer the now-runnable deferred RT-04 real-phone check (docs/manual-checks/mobile-background.md); it is offered, not run or logged, unless the owner performs it"
    - "UI-09 (fireworks night), UI-08 luminosity legibility, and UI-06 glyph distinguishability are not marked passing unless the owner's reply approves them"
    - "D-10: the owner is explicitly asked to confirm the dimmest (unclued) step keeps every suit glyph and hue distinguishable"
  artifacts:
    - path: ".planning/phases/06-game-interface/06-HUMAN-UAT.md"
      provides: "owner visual sign-off, verbatim"
      contains: "Owner's verbatim reply"
    - path: ".planning/phases/06-game-interface/06-VALIDATION.md"
      provides: "per-task map filled and nyquist sign-off"
      contains: "nyquist_compliant:"
  key_links:
    - from: ".planning/phases/06-game-interface/06-HUMAN-UAT.md"
      to: "owner checkpoint reply"
      via: "verbatim blockquote"
      pattern: "^> "
---

<objective>
Close Phase 6 per D-25: run the full automated gate, pause for the owner's visual sign-off on the designed board at desktop sizes, record the reply verbatim, complete the validation contract, and offer the deferred RT-04 real-phone check now that the UI is done.

Purpose: glyph shapes, hues, luminosity steps, and the fireworks-night look are original design work (roadmap research note) that no automated test can approve. Only the owner can.
Output: 06-HUMAN-UAT.md with the verbatim reply; 06-VALIDATION.md completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-game-interface/06-CONTEXT.md
@.planning/phases/06-game-interface/06-VALIDATION.md
@.planning/phases/06-game-interface/06-06-SUMMARY.md
@.planning/phases/01-room-transport-skeleton/01-HUMAN-UAT.md
@docs/manual-checks/mobile-background.md

<interfaces>
Local run (same commands playwright.config.ts uses): worker `npx wrangler dev --port 8787` (from apps/worker, or root per config) and web `npm run dev --workspace apps/web -- -p 3100` with `NEXT_PUBLIC_WORKER_HOST=localhost:8787`; open http://localhost:3100. Two browser windows (one normal, one private) give two seats.
UAT file format precedent: 01-HUMAN-UAT.md (frontmatter status/phase/source/started/updated; ## Current Test; ## Tests with expected/result; ## Summary counts; ## Gaps).
User rule (CLAUDE.md): never push or deploy unless the owner asks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate and prepare a local review session</name>
  <files>.planning/phases/06-game-interface/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/01-room-transport-skeleton/01-HUMAN-UAT.md (UAT format)
    - .planning/phases/06-game-interface/06-VALIDATION.md
    - playwright.config.ts (dev server commands and ports)
    - .planning/phases/06-game-interface/06-01-SUMMARY.md through 06-06-SUMMARY.md (deviations such as widened card sizes, to list for the owner)
  </read_first>
  <action>
    From the repo root with no stray dev servers on ports 3100/8787, run sequentially: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`. All three must exit 0; if any fails, fix within the owning plan's files or stop and report — never request sign-off on a red gate.

    Create .planning/phases/06-game-interface/06-HUMAN-UAT.md following 01-HUMAN-UAT.md's structure with frontmatter `status: pending`, `phase: 06-game-interface`, `source: [06-CONTEXT.md D-25]`, and one test "### 1. Owner visual sign-off on the designed board at desktop sizes (D-25; UI-06, UI-08, UI-09, UI-11)" with `expected:` listing the review points from Task 2's how-to-verify, `result: pending`, plus a "## Owner's verbatim reply" section containing only the placeholder line `(awaiting owner reply)`, a "## Deferred check offered" section stating that docs/manual-checks/mobile-background.md (RT-04 real phone) is now runnable per D-25 and was offered at this checkpoint, and a "## Deviations from UI-SPEC for owner review" list copied from the 06-01..06-06 SUMMARY deviations (or "none recorded"). Then start the worker and web dev servers in the background so the owner can review locally, and note their URLs for the checkpoint.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npm test && npx playwright test && npx tsc -b apps/web packages/rules packages/schema apps/worker && grep -c "awaiting owner reply" .planning/phases/06-game-interface/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx playwright test`, and `npx tsc -b apps/web packages/rules packages/schema apps/worker` all exit 0
    - `grep -c "## Owner's verbatim reply" .planning/phases/06-game-interface/06-HUMAN-UAT.md` outputs 1
    - `grep -c "mobile-background.md" .planning/phases/06-game-interface/06-HUMAN-UAT.md` >= 1
  </acceptance_criteria>
  <done>The gate is green, the UAT record is scaffolded, and a local session is ready for the owner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner visual sign-off on the designed board (D-25)</name>
  <files>.planning/phases/06-game-interface/06-HUMAN-UAT.md (read only during this checkpoint)</files>
  <read_first>
    - .planning/phases/06-game-interface/06-HUMAN-UAT.md
  </read_first>
  <action>Present what-built and how-to-verify to the owner and wait. Do not fill in the reply, do not deploy or push. Capture the owner's reply exactly as sent for Task 3. This checkpoint is never auto-approved (workflow.auto_advance does not apply); the decision is the owner's alone.</action>
  <verify>
    <human-check>Owner reviews the running board at 1280x720 and 1920x1080 and replies with approval, requested changes, or an explicit waiver</human-check>
  </verify>
  <done>The owner's own reply has been received and is held verbatim for Task 3.</done>
  <what-built>The designed Hanabi table replacing the interim board: three-band layout (teammates top, tableau middle, your hand bottom); seven suit glyph silhouettes paired with hues (red star, yellow triangle, green square, blue circle, white diamond, rainbow starburst, black notched hexagon); three luminosity steps on card frames (unclued dim, touched glowing, fully known brightest); own-hand clue memory (confirmed glyph/rank, positive marks, struck-through candidates); clue preview on hover and keyboard focus; disabled actions with visible reasons; transient clue highlight and stack-complete flash (reduced-motion aware); end-of-game overlay with score / max, band, reason, stacks, and New game. Automated gate (npm test, npx playwright test, per-package tsc) is green. Local servers: web http://localhost:3100, worker localhost:8787.</what-built>
  <how-to-verify>
    1. Open http://localhost:3100 in a normal window, create a room (try Base, then Rainbow, then Black), and join from a private window (add more windows for 3-5 players if you like). Start the game.
    2. At a 1280x720 window, then at 1920x1080: confirm clue tokens, fuses, deck count, discard pile, and every stack are visible without opening anything, and nothing needs scrolling at 1280x720.
    3. Confirm the active player is unmistakable (glowing ring on their hand, turn text, and a strong "Your turn" state on your own band when it is you).
    4. Give a few colour and rank clues. Confirm touched cards flash briefly on both screens, then settle into a glow; your own cards show confirmed info and struck-out candidates; refresh the target window and confirm the marks are still there.
    5. Judge the look: is it a dark "fireworks night" table? Are the three luminosity steps easy to tell apart at a glance? At the dimmest (unclued) step, is every suit's glyph and colour still easy to tell apart (D-10)? Would the glyphs work for a colourblind friend with colour ignored?
    6. Hover and Tab through clue options; confirm the preview highlights the right cards with keyboard focus too, and disabled buttons say why.
    7. Play a game to the end (or misplay three times) and review the end screen.
    8. Optional, now unblocked by this UI sign-off (D-25): the deferred real-phone RT-04 check in docs/manual-checks/mobile-background.md. Say whether you want to run it now, later, or keep it waived.
    Reply in your own words: approve, list the changes you want, or waive with a reason.
  </how-to-verify>
  <acceptance_criteria>
    - The owner has replied in their own words; the reply text is captured exactly as sent for Task 3
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what should change (and whether you want the phone check now, later, or waived). Your reply is recorded verbatim.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verbatim reply and complete the validation contract</name>
  <files>.planning/phases/06-game-interface/06-HUMAN-UAT.md, .planning/phases/06-game-interface/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-game-interface/06-HUMAN-UAT.md (Task 1 scaffold)
    - .planning/phases/06-game-interface/06-VALIDATION.md
    - .planning/phases/06-game-interface/06-01-SUMMARY.md through 06-06-SUMMARY.md (task ids and commands actually run)
  </read_first>
  <action>
    In 06-HUMAN-UAT.md replace `(awaiting owner reply)` with the owner's Task 2 reply as a Markdown blockquote (each line prefixed with "> "), text exactly as sent — no paraphrase, no correction, no summary. Set the test's `result:` to `pass` only if the reply approves; `issue` with a `reason:` restating nothing beyond "see verbatim reply" if the owner requested changes; `skipped` if waived. Update frontmatter `status` (complete / issues / complete-waived) and `updated`, and the Summary counts. Under "## Deferred check offered" append the owner's stated choice for the RT-04 phone check using their own words quoted, or "owner did not state" if they did not mention it. If the owner requested changes, list each requested change as a bullet under "## Gaps" quoting the owner, and state in the SUMMARY that Phase 6 is blocked for /gsd:verify-work until a gap-closure plan addresses them.

    In 06-VALIDATION.md: fill the Per-Task Verification Map's Status column green for rows whose commands passed (UI-01..UI-11, RULES-11 mapped to the real test names/specs from 06-01..06-06), set UI-09's manual row to the recorded outcome, tick the Wave 0 checklist items delivered (hanabi-visual-logic.ts + test, clueTouchIdsForTarget tests, suit glyph/hue exhaustiveness test, Playwright UI-11 viewport and UI-02 signal coverage), tick true Validation Sign-Off boxes, and set frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete` only if the owner approved or waived; set "Approval:" to "approved — owner sign-off recorded verbatim in 06-HUMAN-UAT.md on {date}" or "blocked — see 06-HUMAN-UAT.md Gaps".
  </action>
  <verify>
    <automated>cd /home/rflor/games && ! grep -q "awaiting owner reply" .planning/phases/06-game-interface/06-HUMAN-UAT.md && grep -c "^> " .planning/phases/06-game-interface/06-HUMAN-UAT.md && grep -n "Approval:" .planning/phases/06-game-interface/06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "awaiting owner reply" .planning/phases/06-game-interface/06-HUMAN-UAT.md` outputs 0
    - `grep -c "^> " .planning/phases/06-game-interface/06-HUMAN-UAT.md` >= 1, and a distinctive phrase from the owner's reply is found with `grep -F`
    - `grep -n "Approval:" .planning/phases/06-game-interface/06-VALIDATION.md` shows approved or blocked, not pending
    - `grep -c "result: pending" .planning/phases/06-game-interface/06-HUMAN-UAT.md` outputs 0
  </acceptance_criteria>
  <done>The owner's sign-off (or requested changes / waiver) is recorded verbatim and the validation contract reflects the real outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner reply -> planning record | human evidence enters the repository |
| local dev servers -> reviewer | review happens on localhost, not production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | Repudiation | fabricated or paraphrased owner sign-off | mitigate | blocking checkpoint never auto-approved; Task 3 quotes the reply verbatim as a blockquote; acceptance requires a grep -F match of the owner's own phrase |
| T-06-22 | Information Disclosure | HIDE-01 regression shipped under a visual approval | mitigate | sign-off only requested after the full gate (including the strengthened HIDE-01 e2e and D-15 source-scan test) is green |
| T-06-23 | Tampering | unrequested deploy or push during review | mitigate | review runs on local dev servers; CLAUDE.md rule restated — no push or deploy without the owner asking |
</threat_model>

<verification>
- Full gate green before the checkpoint
- 06-HUMAN-UAT.md contains the verbatim reply
- 06-VALIDATION.md no longer pending
- Commit only this plan's files by explicit path
</verification>

<success_criteria>
- D-25 satisfied: automated gate green plus recorded owner visual sign-off (or recorded requested changes blocking verification)
- RT-04 phone check offered with the owner's choice recorded
</success_criteria>

<output>
Create `.planning/phases/06-game-interface/06-07-SUMMARY.md` when done
</output>
