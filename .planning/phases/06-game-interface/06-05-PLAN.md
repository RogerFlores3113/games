---
phase: 06-game-interface
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/components/HanabiBoard.tsx
  - apps/web/app/room/[code]/RoomClient.tsx
  - apps/web/components/ReconnectingBanner.tsx
  - e2e/hanabi-realtime.spec.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-09, UI-11, RULES-11]

must_haves:
  truths:
    - "The interim apps/web/components/HanabiBoard.tsx is deleted and RoomClient renders the new apps/web/components/hanabi/HanabiBoard.tsx with the unchanged { view, onAction, reconnecting } prop contract"
    - "D-01: the board is a three-band layout — teammate hands across the top in turn order after the viewer, tableau in the middle, own hand + controls along the bottom — inside a main element that may scroll vertically but needs no scroll at 1280x720 with 5 players"
    - "D-02: the active seat is marked by the hand-container accent ring, the turn indicator text, and (viewer's turn) the prominent own-band 'Your turn' state"
    - "D-03: ReconnectingBanner (testid reconnecting-banner, text 'Reconnecting…', role status) is kept and restyled as a fixed top overlay; every Phase 4/5 testid still resolves"
    - "D-14: when a new clue lands after mount, its touchedCardIds get data-just-clued=\"true\" for CLUE_HIGHLIGHT_MS on every screen; persistent marks come from facts only"
    - "D-16/D-17: clicking an own card selects it for Play/Discard; clicking a teammate hand or clue-target button selects the clue target; hovering/focusing a clue value (or having one selected) marks exactly clueTouchIdsForTarget ids with data-preview=\"true\""
    - "D-15: the board passes only facts to own-hand cards; the WR-03 HanabiViewSchema.safeParse gate stays in front of every render"
    - "D-20: when view.status is ended, EndOverlay renders over the still-visible board and every action control is disabled"
    - "D-22: no new dependency (no motion package); animation is CSS only"
    - "D-03/D-24: specs updated in the same change — the clue-value helper skips disabled (zero-touch) options and the HIDE-01 assertion compares teammate card-identity text and asserts no identity attributes inside own-hand; full `npx playwright test` is green"
  artifacts:
    - path: "apps/web/components/hanabi/HanabiBoard.tsx"
      provides: "board orchestrator"
      exports: ["HanabiBoard", "HanabiActionRequest", "HanabiBoardProps"]
    - path: "apps/web/app/room/[code]/RoomClient.tsx"
      provides: "imports the new board"
      contains: "components/hanabi/HanabiBoard"
  key_links:
    - from: "apps/web/app/room/[code]/RoomClient.tsx"
      to: "apps/web/components/hanabi/HanabiBoard.tsx"
      via: "import { HanabiBoard }"
      pattern: "components/hanabi/HanabiBoard"
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "@games/schema/games/hanabi"
      via: "HanabiViewSchema.safeParse gate (WR-03)"
      pattern: "HanabiViewSchema\\.safeParse"
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "apps/web/lib/hanabi-visual-logic.ts"
      via: "touchedCardIdsFromLatestClue + teammatesInTurnOrder"
      pattern: "touchedCardIdsFromLatestClue\\(|teammatesInTurnOrder\\("
---

<objective>
Wire the designed board together and swap it in: a thin orchestrator that owns select-then-act state (D-16), clue preview (D-17), the transient just-clued highlight (D-14), and the three-band layout (D-01, D-02), renders EndOverlay at game end (D-20), restyles ReconnectingBanner (D-03), deletes the interim board, and updates the two e2e specs whose helpers the new disabled states would otherwise break — in the same change (D-03, D-24).

Purpose: this is the moment the product's table actually changes; the existing RT-01/RT-03/RT-04/HIDE-01 Playwright proofs must stay green across it.
Output: new orchestrator, RoomClient import swap, interim board removed, banner restyled, spec compatibility edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-game-interface/06-CONTEXT.md
@.planning/phases/06-game-interface/06-UI-SPEC.md
@.planning/phases/06-game-interface/06-03-SUMMARY.md
@.planning/phases/06-game-interface/06-04-SUMMARY.md
@apps/web/components/HanabiBoard.tsx
@apps/web/app/room/[code]/RoomClient.tsx

<interfaces>
Existing prop contract to keep (apps/web/components/HanabiBoard.tsx):
  export type HanabiActionRequest = { type: "play"; cardId } | { type: "discard"; cardId } | { type: "clue"; targetSeatId; clue: Clue };
  export interface HanabiBoardProps { view: RoomView; onAction: (request: HanabiActionRequest) => void; reconnecting?: boolean }
  isHanabiView(game) = HanabiViewSchema.safeParse(game).success; act() no-ops while reconnecting; "Loading game…" fallback when the gate fails.
RoomClient.tsx line 19: `import { HanabiBoard } from "../../../components/HanabiBoard";` — the only consumer; HanabiActionRequest is not imported elsewhere.

From 06-03 (components/hanabi/Hand.tsx): TeammateHand({ hand, label, connected, variant, isActive, isTarget, previewIds, justCluedIds, disabled, onSelectTarget }), OwnHand({ cards, variant, youSeatId, connected, isYourTurn, turnText, selectedCardId, justCluedIds, disabled, onSelectCard })
From 06-04: Table({ game }), CardActions({ game, selectedCardId, ctx, onPlay, onDiscard }), CluePicker({ game, targets, clueTarget, clueValue, ctx, onSelectTarget, onSelectValue, onPreview, onGive }), EndOverlay({ game })
From 06-01: teammatesInTurnOrder(otherHands, seatOrder, yourSeatId), touchedCardIdsFromLatestClue(history, sinceIndex), CLUE_HIGHLIGHT_MS; hanabi-board-logic: clueTouchIdsForTarget, turnIndicatorText(game, seats, labelFor), isSeatConnected
RoomView (from @games/schema): { status: "lobby" | "in_progress" | "ended"; seats: Array<{ seatId; displayLabel; connected; ... }>; youSeatId: string | null; game: unknown; ... }

Spec facts:
  - e2e/hanabi-realtime.spec.ts selectAClueValueThatTouchesSomething clicks EVERY clue-value button in order; Playwright click() waits for enabled, so a disabled zero-touch option would stall it until timeout.
  - e2e/start-game.spec.ts HIDE-01 block (lines 63-84) compares allTextContents of [data-testid^="other-hand-card-"] against own-hand textContent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board orchestrator, RoomClient swap, interim board deletion, banner restyle</name>
  <files>apps/web/components/hanabi/HanabiBoard.tsx, apps/web/components/HanabiBoard.tsx, apps/web/app/room/[code]/RoomClient.tsx, apps/web/components/ReconnectingBanner.tsx</files>
  <read_first>
    - apps/web/components/HanabiBoard.tsx (entire interim file — prop contract, WR-03 gate, act wrapper, labelFor, comments to carry forward)
    - apps/web/app/room/[code]/RoomClient.tsx lines 1-30 and 225-258
    - apps/web/components/ReconnectingBanner.tsx
    - apps/web/components/hanabi/Hand.tsx, Table.tsx, CardActions.tsx, CluePicker.tsx, EndOverlay.tsx (06-03/06-04 outputs — exact prop names)
    - .planning/phases/06-game-interface/06-UI-SPEC.md sections "Layout" and "Active-Player Indication"
  </read_first>
  <action>
    Create apps/web/components/hanabi/HanabiBoard.tsx ("use client"). Move `HanabiActionRequest`, `HanabiBoardProps`, and `isHanabiView` here verbatim (keep the WR-03 comment) and carry forward the interim file's load-bearing own-hand comment, updated to name OwnHandCard's facts-only prop as the enforcement point (D-15).

    State: selectedCardId, clueTarget, clueValue (Clue | null), previewClue (Clue | null), justCluedIds (ReadonlySet<string>, initially empty). Derived: `ended = view.status === "ended"`; `labelFor(seatId)` as in the interim board; `ctx = { reconnecting, ended, labelFor }`; `controlsDisabled = reconnecting || ended`; `teammates = teammatesInTurnOrder(game.otherHands, view.seats.map((s) => s.seatId), game.yourSeatId)` (D-01); `previewIds = new Set(clueTarget && (previewClue ?? clueValue) ? clueTouchIdsForTarget(game, clueTarget, previewClue ?? clueValue) : [])` (D-17 — sustained for the selected value, per UI-SPEC).

    Just-clued effect (D-14): `prevHistoryLength = useRef<number | null>(null)`; in useEffect keyed on `game?.history.length`: if ref is null just record the length (no highlight on mount or refresh); else if length grew, `ids = touchedCardIdsFromLatestClue(game.history, prev)`, and when non-empty set justCluedIds to a new Set(ids) and schedule clearing after CLUE_HIGHLIGHT_MS with a cleanup that clears the timeout; always update the ref. Hooks must run before the `if (!game)` early return (call them unconditionally, guarding on game inside).

    Selection hygiene: in an effect, clear selectedCardId when it no longer exists in game.yourHand; after dispatching a clue clear clueValue and previewClue. `act(request)` no-ops when reconnecting or ended (defense in depth, D-06 from Phase 5 plus D-20).

    Layout (D-01, UI-SPEC): `<main className="flex min-h-screen flex-col gap-[length:var(--space-md)] overflow-y-auto px-[length:var(--space-md)] py-[length:var(--space-sm)]" style={{ backgroundColor: "var(--color-bg)" }}>`; `{reconnecting && <ReconnectingBanner />}`; top band `<div data-testid="teammates-band" className="flex flex-none flex-wrap justify-center gap-[length:var(--space-md)]">` mapping teammates to TeammateHand with `isActive={hand.seatId === game.activeSeatId}`, `isTarget={clueTarget === hand.seatId}`, `connected={isSeatConnected(view.seats, hand.seatId)}`, `disabled={controlsDisabled}`, `onSelectTarget={() => setClueTarget(hand.seatId)}`; center band `<div className="flex min-h-0 flex-1 justify-center"><Table game={game} /></div>`; bottom band `<div className="flex flex-none flex-wrap items-start justify-center gap-[length:var(--space-lg)]">` with OwnHand (`turnText={turnIndicatorText(game, view.seats, labelFor)}`, `isYourTurn={game.isYourTurn && !ended}`, `connected` from isSeatConnected for view.youSeatId, `disabled={controlsDisabled}`, onSelectCard sets selectedCardId), CardActions (onPlay/onDiscard call act with selectedCardId), and CluePicker (targets from teammates with labelFor, onSelectTarget/onSelectValue setters, onPreview=setPreviewClue, onGive calls act with clueTarget/clueValue). When ended render `<EndOverlay game={game} />` as the last child. Keep the "Loading game…" fallback for a failed gate. Keep the turn indicator rendered while ended (it is inside OwnHand) — the overlay covers it.

    RoomClient.tsx: change only the import to `import { HanabiBoard } from "../../../components/hanabi/HanabiBoard";`. Delete apps/web/components/HanabiBoard.tsx (`git rm`; do not commit yet — Task 2 commits Tasks 1 and 2 together). Confirm `grep -rn "components/HanabiBoard\"" apps` finds nothing.

    ReconnectingBanner.tsx (D-03 restyle only): keep role="status", data-testid="reconnecting-banner", and the exact text "Reconnecting…"; restyle to a fixed overlay pinned to the top center (`fixed left-1/2 top-[length:var(--space-sm)] z-40 -translate-x-1/2`, surface bg, border, subtle shadow) so it does not push the three bands down; update its trailing comment from "Phase 6 restyles" to record that Phase 6 restyled it. No accent, no destructive color, no animation.

    Do not add any npm dependency (D-22).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web && npx vitest run --project web && test ! -f apps/web/components/HanabiBoard.tsx && npm run build:web</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f apps/web/components/HanabiBoard.tsx` succeeds
    - `grep -c "components/hanabi/HanabiBoard" "apps/web/app/room/[code]/RoomClient.tsx"` outputs 1
    - `grep -c "HanabiViewSchema.safeParse" apps/web/components/hanabi/HanabiBoard.tsx` outputs 1
    - `grep -cE "touchedCardIdsFromLatestClue\(|teammatesInTurnOrder\(|clueTouchIdsForTarget\(" apps/web/components/hanabi/HanabiBoard.tsx` >= 3
    - `grep -c "<EndOverlay" apps/web/components/hanabi/HanabiBoard.tsx` outputs 1
    - `grep -c "Reconnecting…" apps/web/components/ReconnectingBanner.tsx` outputs 1 and `grep -c 'data-testid="reconnecting-banner"' apps/web/components/ReconnectingBanner.tsx` outputs 1
    - `git diff --stat -- apps/web/package.json package-lock.json` is empty
    - `npx tsc -b apps/web` and `npm run build:web` exit 0
  </acceptance_criteria>
  <done>The designed board is live in RoomClient, the interim board is gone, and the web app type-checks and builds.</done>
</task>

<task type="auto">
  <name>Task 2: Same-change spec compatibility and full Playwright run</name>
  <files>e2e/hanabi-realtime.spec.ts, e2e/start-game.spec.ts</files>
  <read_first>
    - e2e/hanabi-realtime.spec.ts (entire file; helper at lines 14-32)
    - e2e/start-game.spec.ts (entire file; HIDE-01 block lines 63-84)
    - e2e/helpers.ts (OTHER_HAND_SELECTOR)
    - apps/web/components/hanabi/TeammateCard.tsx and OwnHandCard.tsx (card-identity, data-glyph, confirmed-suit markup)
    - playwright.config.ts (ports E2E_WEB_PORT / E2E_WORKER_PORT)
  </read_first>
  <action>
    e2e/hanabi-realtime.spec.ts: rewrite `selectAClueValueThatTouchesSomething(page)` so it iterates `[data-testid^="clue-value-"]` buttons, skips any whose `isEnabled()` is false (zero-touch options are now disabled per D-17), clicks enabled ones, and returns once `give-clue-button` is enabled; keep the same thrown error when none work. Update its doc comment to note D-17 disabled zero-touch options. Change nothing else in the file in this task.

    e2e/start-game.spec.ts HIDE-01 block: replace the two `[data-testid^="other-hand-card-"]` allTextContents reads with `[data-testid^="other-hand-card-"] [data-testid="card-identity"]` (teammate identity text such as "Red 3"), keep the `length > 0` guards and the not.toContain loops, and add per page: `await expect(page.getByTestId("own-hand").locator('[data-glyph], [data-testid="card-identity"]')).toHaveCount(0)` (no clues have been given at that point, so no own card may carry any suit-exposing marker). Update the comment to say the Phase 6 board renders identity text only inside card-identity, so the comparison stays exact rather than vacuous. Change nothing else.

    Run the full suite from the repo root with no stray dev servers on the configured ports: `npx playwright test`. Every existing spec (create-room, join-room, seat-list, seat-takeover, in-progress-arrival, smoke, start-game, hanabi-realtime) must pass. If a failure is caused by a preserved testid/copy the new components changed, fix the component (06-03/06-04 files may be touched only for that reason; note it in the SUMMARY), not the assertion. Commit Task 1 and Task 2 as ONE atomic commit after this run is green: `feat(06-05): swap in designed Hanabi board with spec compatibility`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` exits 0 with every pre-existing test passing
    - `grep -c "isEnabled()" e2e/hanabi-realtime.spec.ts` >= 1
    - `grep -c "card-identity" e2e/start-game.spec.ts` >= 2
    - `grep -c "data-glyph" e2e/start-game.spec.ts` >= 1
    - `git log -1 --stat` shows the board files and both spec files in the same commit
  </acceptance_criteria>
  <done>The designed board ships with every Phase 1-5 end-to-end proof still green, including a strengthened HIDE-01 check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket RoomView -> board render | server-pushed view enters the client; validated by HanabiViewSchema |
| board -> onAction -> RoomDO | client intent to server authority |
| test evidence -> CI | e2e specs are the HIDE-01/RT proofs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Information Disclosure | own-hand identity reintroduced during wiring (D-15/HIDE-01) | mitigate | orchestrator passes facts only to OwnHand/OwnHandCard; strengthened start-game HIDE-01 assertion (card-identity text + zero data-glyph/card-identity inside own-hand) |
| T-06-15 | Tampering | malformed or schema-drifted view rendering | mitigate | WR-03 HanabiViewSchema.safeParse gate kept verbatim; failed gate renders "Loading game…" |
| T-06-16 | Repudiation / Tampering | weakening e2e proofs to make the swap pass | mitigate | task forbids changing assertions except the two named compat edits; component is fixed instead; single atomic commit shows both |
| T-06-17 | Denial of Service | actions sent from a stale view while reconnecting or after game end | mitigate | act() no-ops when reconnecting or ended; controls disabled via ctx |
</threat_model>

<verification>
- `npx tsc -b apps/web packages/rules packages/schema apps/worker` clean
- `npm test` green
- `npx playwright test` green
- `grep -rn "dangerouslySetInnerHTML" apps/web/components` returns nothing
</verification>

<success_criteria>
- ROADMAP Phase 6 success criteria 1-2 visible in the running app (tableau always visible, active player marked, own hand face down, disabled actions with reasons)
- No regression in Phase 1-5 e2e proofs
</success_criteria>

<output>
Create `.planning/phases/06-game-interface/06-05-SUMMARY.md` when done
</output>
