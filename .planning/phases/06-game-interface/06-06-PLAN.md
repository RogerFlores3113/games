---
phase: 06-game-interface
plan: 06
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - e2e/helpers.ts
  - e2e/start-game.spec.ts
  - e2e/hanabi-realtime.spec.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-06, UI-10, UI-11, RULES-11]

must_haves:
  truths:
    - "D-24: Playwright against the real worker proves tableau elements (clue tokens, fuse tokens, deck count, discard pile, every played stack) are in the viewport with no interaction"
    - "D-24 / D-02: Playwright proves the active-player indicator moves (data-active on the teammate hand container and own-band, data-your-turn on turn-indicator) after a turn passes"
    - "D-24 / D-14: a clue marks exactly the previewed cards on the giver's screen and the same number of own-hand slots on the target's screen (transient data-just-clued and persistent data-luminosity), and the target's marks are identical after the target refreshes with no transient replay"
    - "D-24 / D-18: disabled controls are disabled with a visible reason (Not your turn, Clue tokens are full — you can't discard, Select a card in your hand first, Reconnecting — actions paused)"
    - "D-24 / D-20 / D-21: a game played to its end shows the overlay with 'Game over', 'Final score: S / MAX — band', a derived end reason, one end-stack per variant suit, a New game link to /, and disabled action controls"
    - "D-01 / UI-11: with 5 players at 1280x720 the page does not scroll and every tableau element is in the viewport; at 1024x768 there is no horizontal overflow and every tableau element is reachable and visible"
    - "D-05 / UI-06: every teammate card and every played stack carries a suit glyph (data-glyph) by default"
  artifacts:
    - path: "e2e/helpers.ts"
      provides: "startGameWithPlayers helper for N-player games"
      exports: ["startGameWithPlayers"]
    - path: "e2e/start-game.spec.ts"
      provides: "UI-01/UI-03/UI-06/RULES-11 assertions, UI-10 end-screen test, UI-11 viewport test"
      contains: "end-overlay"
    - path: "e2e/hanabi-realtime.spec.ts"
      provides: "UI-02 + UI-04 clue-marking + refresh-persistence test"
      contains: "data-just-clued"
  key_links:
    - from: "e2e/start-game.spec.ts"
      to: "apps/web/components/hanabi/EndOverlay.tsx"
      via: "end-overlay / final-score / end-reason / end-stack / new-game-link testids"
      pattern: "end-overlay"
    - from: "e2e/hanabi-realtime.spec.ts"
      to: "apps/web/components/hanabi/TeammateCard.tsx + OwnHandCard.tsx"
      via: "data-preview / data-just-clued / data-luminosity attributes"
      pattern: "data-luminosity"
---

<objective>
Extend the existing Playwright specs (not a parallel harness, per D-24) to prove the designed board end to end against the real worker: UI-01 always-visible tableau, UI-02 moving active-player indicator, UI-03/UI-06 face-up glyph cards, UI-04 clue marks on both screens persisting across the target's refresh, RULES-11 disabled-with-reason controls, UI-10 end screen, and UI-11 desktop viewport usability.

Purpose: component behavior can only be proven in a real browser (no DOM unit environment). These are the automated half of D-25's phase gate.
Output: extended start-game.spec.ts and hanabi-realtime.spec.ts, one new helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-game-interface/06-CONTEXT.md
@.planning/phases/06-game-interface/06-05-SUMMARY.md
@e2e/helpers.ts
@e2e/start-game.spec.ts
@e2e/hanabi-realtime.spec.ts

<interfaces>
Board testids/attributes (from 06-03/06-04/06-05):
  tableau; played-stack-{suit} [data-top-rank, data-complete, contains [data-glyph]]; clue-tokens; fuse-tokens; deck-count [data-final-round]; discard-pile > ul > li
  teammates-band; other-hand-{seatId} [data-active, data-target]; other-hand-card-{id} [data-luminosity unclued|touched|known, data-preview, data-just-clued] > [data-testid="card-identity"] > svg[data-glyph]
  own-band [data-active]; turn-indicator [data-your-turn]; own-hand > own-hand-slot-{n} [data-luminosity, data-selected, data-just-clued]
  play-button, discard-button, action-reason-play, action-reason-discard; clue-target-{seatId}; clue-value-{color|rank}; give-clue-button; action-reason-clue; zero-touch-reason
  end-overlay; game-over-heading; final-score ("Final score: S / MAX — band"); end-reason (one of "Three fuses were lost.", "Every stack was completed!", "The deck ran out and the final round elapsed."); end-stacks > end-stack; new-game-link (href "/")
  reconnecting-banner
Reason copy (06-01 disabledReasonFor): "The game has ended", "Reconnecting — actions paused", "Not your turn", "Select a card in your hand first", "Clue tokens are full — you can't discard", "No clue tokens left", "Choose a teammate to clue", "Choose a color or rank", "That clue wouldn't touch any of {name}'s cards"
Existing helpers (e2e/helpers.ts): createRoom(page, {name, variant?}), joinAs(context, code, name), expectSeatCount(page, n), OTHER_HAND_SELECTOR, seatIdOfOtherPlayer(observer), startTwoPlayerGame(hostPage, browser, names?) -> { code, contextB, pageB, activePage, passivePage }
Playwright default viewport is 1280x720 (playwright.config.ts sets none). Rooms allow 2-5 seats; 4-5 players get 4-card hands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: start-game.spec — UI-01/UI-03/UI-06/RULES-11 assertions, end-screen test, 5-player viewport test</name>
  <files>e2e/helpers.ts, e2e/start-game.spec.ts</files>
  <read_first>
    - e2e/helpers.ts (entire file)
    - e2e/start-game.spec.ts (entire file, after 06-05's HIDE-01 edit)
    - e2e/in-progress-arrival.spec.ts (existing multi-context pattern for many players)
    - apps/web/components/hanabi/Table.tsx, CardActions.tsx, EndOverlay.tsx (exact testids and copy)
    - apps/web/lib/hanabi-visual-logic.ts (exact reason strings and END_REASON_COPY)
  </read_first>
  <action>
    e2e/helpers.ts: add `startGameWithPlayers(hostPage: Page, browser: Browser, names: string[], options?: { variant?: Variant }): Promise<{ code: string; pages: Page[]; contexts: BrowserContext[] }>` — names[0] is the host (createRoom), each further name joins in its own new context via joinAs, waits for expectSeatCount(hostPage, names.length), clicks start-game, and waits for own-hand visible on every page. pages[0] is hostPage; contexts excludes the host's context. Doc comment references D-24 / UI-11.

    e2e/start-game.spec.ts, existing test (Black variant, 2 players) — insert right after both own-hand elements are visible and BEFORE any click (UI-01, "without interaction"): for hostPage, `toBeInViewport()` on tableau, clue-tokens, fuse-tokens, deck-count, discard-pile; `[data-testid^="played-stack-"]` toHaveCount(6) (Black = 6 suits) and each nth(i) toBeInViewport and contains exactly one `[data-glyph]`; every `[data-testid^="other-hand-card-"]` contains one `[data-testid="card-identity"] [data-glyph]` (UI-03/UI-06; loop over count). After the existing waiting-page disabled assertions add: waitingPage action-reason-play toHaveText("Not your turn"); activePage action-reason-discard toHaveText("Clue tokens are full — you can't discard"); activePage action-reason-play toHaveText("Select a card in your hand first"). After the existing own-hand-slot-1 click, add activePage action-reason-play toHaveCount(0) and own-hand-slot-1 toHaveAttribute("data-selected", "true"). Keep every existing assertion.

    New test "UI-10: a game played to its end shows the designed end overlay (D-20/D-21)": `test.setTimeout(240_000)`; startTwoPlayerGame(hostPage, browser); loop up to 80 iterations: break if hostPage end-overlay isVisible(); pick `active` as whichever page's turn-indicator textContent is exactly "Your turn"; on active click own-hand-slot-1, expect play-button enabled, click it, then `await expect.poll(async () => (await hostPage.getByTestId("end-overlay").isVisible()) || ((await active.getByTestId("turn-indicator").textContent()) ?? "") !== "Your turn").toBe(true)`. After the loop, on BOTH pages: end-overlay visible; game-over-heading toHaveText("Game over"); final-score toHaveText(/^Final score: \d+ \/ 25 — .+$/); end-reason toHaveText one of the three END_REASON_COPY strings (regex alternation, escape the period and exclamation mark); `[data-testid="end-stack"]` toHaveCount(5); new-game-link toHaveAttribute("href", "/"); play-button, discard-button, give-clue-button toBeDisabled; tableau still attached (board visible behind overlay). Fail with a clear message if the loop exhausts without an overlay. (Playing slot 1 every turn ends a 2-player game by three misplays or by deck exhaustion — both are legal outcomes; the test asserts structure, never a specific score.)

    New test "UI-11: five players fit a 1280x720 desktop without scrolling and stay usable at 1024 wide (D-01)": `test.setTimeout(120_000)`; startGameWithPlayers(hostPage, browser, ["Roger","Bianca","Chen","Dara","Eli"]); on hostPage at the default 1280x720: `[data-testid^="other-hand-card-"]` count is 16 (4 teammates x 4 cards); `await hostPage.evaluate(() => document.documentElement.scrollHeight <= window.innerHeight + 1)` is true and likewise scrollWidth <= innerWidth + 1; tableau, clue-tokens, fuse-tokens, deck-count, discard-pile, own-hand, turn-indicator, and every played-stack toBeInViewport. Then `hostPage.setViewportSize({ width: 1024, height: 768 })`: scrollWidth <= clientWidth + 1 (no horizontal overflow); for each of the same elements `scrollIntoViewIfNeeded()` then toBeVisible(); own-hand-slot-1 click succeeds and gets data-selected "true". Close all extra contexts at the end.

    Run `npx playwright test e2e/start-game.spec.ts`. If the 1280x720 no-scroll or 1024 overflow assertion fails, fix the layout in the owning component (06-03/06-04/06-05 files: card widths within the limits those plans set, band gaps/padding) — never relax the assertion — and record the component change in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/start-game.spec.ts` exits 0 with 3 tests passing
    - `grep -c "toBeInViewport" e2e/start-game.spec.ts` >= 8
    - `grep -c "end-overlay" e2e/start-game.spec.ts` >= 2 and `grep -c "new-game-link" e2e/start-game.spec.ts` >= 1
    - `grep -c "setViewportSize" e2e/start-game.spec.ts` >= 1
    - `grep -c "Not your turn" e2e/start-game.spec.ts` >= 1
    - `grep -c "export async function startGameWithPlayers" e2e/helpers.ts` outputs 1
  </acceptance_criteria>
  <done>UI-01, UI-03, UI-06, UI-10, UI-11 and RULES-11 reasons are proven in a real browser against the real worker.</done>
</task>

<task type="auto">
  <name>Task 2: hanabi-realtime.spec — active indicator moves, clue marks on both screens, persistence across target refresh</name>
  <files>e2e/hanabi-realtime.spec.ts</files>
  <read_first>
    - e2e/hanabi-realtime.spec.ts (entire file, after 06-05's helper edit)
    - e2e/helpers.ts (startTwoPlayerGame, seatIdOfOtherPlayer)
    - apps/web/components/hanabi/HanabiBoard.tsx (preview sustained for selected clue; just-clued not replayed on mount)
    - apps/web/components/hanabi/TeammateCard.tsx, OwnHandCard.tsx, Hand.tsx (attribute names)
  </read_first>
  <action>
    Add a new test inside the "Hanabi realtime proofs" describe: "UI-02 + UI-04: the active marker moves and a clue marks the same cards on the giver's and target's screens, persisting across the target's refresh (D-02/D-14/D-24)". Steps:
    1. `const { contextB, activePage, passivePage } = await startTwoPlayerGame(hostPage, browser)`. `giverSeat = await seatIdOfOtherPlayer(passivePage)`; `targetSeat = await seatIdOfOtherPlayer(activePage)`.
    2. UI-02 before: activePage own-band data-active "true", activePage turn-indicator data-your-turn "true"; passivePage own-band data-active "false"; passivePage `other-hand-${giverSeat}` data-active "true"; activePage `other-hand-${targetSeat}` data-active "false".
    3. Giver: click `clue-target-${targetSeat}`; call the existing selectAClueValueThatTouchesSomething(activePage). Collect preview ids: `activePage.locator(\`[data-testid="other-hand-${targetSeat}"] [data-preview="true"]\`)` — read each element's data-testid into `previewTestIds`; expect length > 0 and store N.
    4. Click give-clue-button. Transient (D-14), polled immediately: passivePage `[data-testid="own-hand"] [data-just-clued="true"]` toHaveCount(N); activePage `[data-testid="other-hand-${targetSeat}"] [data-just-clued="true"]` toHaveCount(N).
    5. Persistent marks: on activePage each id in previewTestIds toHaveAttribute("data-luminosity", /^(touched|known)$/) and `[data-testid="other-hand-${targetSeat}"] [data-luminosity]:not([data-luminosity="unclued"])` toHaveCount(N); on passivePage `[data-testid^="own-hand-slot-"]:not([data-luminosity="unclued"])` toHaveCount(N). Then after CLUE_HIGHLIGHT_MS the transient clears: passivePage `[data-just-clued="true"]` toHaveCount(0) with timeout 5000 while the persistent count stays N.
    6. UI-02 after: passivePage own-band data-active "true" and turn-indicator toHaveText("Your turn"); activePage `other-hand-${targetSeat}` data-active "true"; activePage own-band data-active "false".
    7. Refresh persistence (D-14 "survive refresh with no client memory"): record `before = await passivePage.locator('[data-testid^="own-hand-slot-"]').evaluateAll(els => els.map(e => e.getAttribute("data-luminosity")))`; `await passivePage.reload()`; expect refusal-card toHaveCount(0) and own-hand visible; `await expect.poll(() => passivePage.locator('[data-testid^="own-hand-slot-"]').evaluateAll(els => els.map(e => e.getAttribute("data-luminosity")))).toEqual(before)`; non-unclued slot count still N; `[data-just-clued="true"]` toHaveCount(0) (no replay on mount).
    8. `await contextB.close()`.

    In the existing RT-04 network-drop test, after the three toBeDisabled assertions on droppingPage add `await expect(droppingPage.getByTestId("action-reason-play")).toHaveText("Reconnecting — actions paused")`. Change no other existing assertion.

    Run `npx playwright test e2e/hanabi-realtime.spec.ts`, then the full `npx playwright test`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` exits 0 (all specs, including the new realtime test)
    - `grep -c "data-just-clued" e2e/hanabi-realtime.spec.ts` >= 3
    - `grep -c "data-luminosity" e2e/hanabi-realtime.spec.ts` >= 3
    - `grep -c "data-active" e2e/hanabi-realtime.spec.ts` >= 4
    - `grep -c "reload()" e2e/hanabi-realtime.spec.ts` >= 2
    - `grep -c "Reconnecting — actions paused" e2e/hanabi-realtime.spec.ts` outputs 1
  </acceptance_criteria>
  <done>UI-02 and UI-04 (including persistence across refresh) are proven on two real browsers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e specs -> phase gate evidence | these tests are the proof HIDE-01/UI claims rest on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Repudiation | assertions relaxed to pass a failing layout or leak | mitigate | tasks require fixing components, never relaxing assertions; existing assertions left unchanged |
| T-06-19 | Information Disclosure | end-to-end identity leak on own hand not caught | mitigate | 06-05's strengthened HIDE-01 assertion kept; new tests only read facts-derived attributes (data-luminosity) on own cards, never identity |
| T-06-20 | Denial of Service | flaky long-running end-game loop masking regressions | mitigate | bounded 80-iteration loop with explicit failure message and per-step expect.poll instead of sleeps |
</threat_model>

<verification>
- `npx playwright test` green, including the three start-game tests and the new realtime test
- `npm test` still green
</verification>

<success_criteria>
- Every D-24 bullet has a passing Playwright assertion against the real worker
</success_criteria>

<output>
Create `.planning/phases/06-game-interface/06-06-SUMMARY.md` when done
</output>
