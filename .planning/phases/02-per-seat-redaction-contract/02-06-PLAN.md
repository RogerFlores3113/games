---
phase: 02-per-seat-redaction-contract
plan: 06
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified:
  - .planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md
autonomous: false
requirements: [HIDE-01, HIDE-02, HIDE-03, HIDE-04]

must_haves:
  truths:
    - "The full automated gate is green on the integrated tree: npm test (all four Vitest projects including D-09 structural test and D-11 layers 1-3), per-project tsc, worker and web builds, and the full Playwright suite"
    - "HIDE-01 / D-01 / D-03: a human confirms in two real browsers that each player sees the other's card but not their own, and that DevTools WebSocket frames for a seat never contain that seat's own card value"
    - "HIDE-02 / D-08: the human confirms the only frames observed are joined/state/error/refused/superseded, and every state frame's game.yourCard contains only id and hidden"
  artifacts:
    - path: ".planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md"
      provides: "validation map marked green, nyquist_compliant true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "npm test"
      to: "apps/worker/src/source-structure.test.ts"
      via: "standard suite run fails the build on a bypass (HIDE-04 / D-09)"
      pattern: "source-structure"
---

<objective>
Run the phase-wide automated gate on the integrated tree, record results in VALIDATION.md, and get a human to confirm the redaction contract in real browsers and real DevTools WebSocket frames.

Purpose: The ROADMAP success criteria are phrased in terms of inspecting raw network payloads; the automated layers prove it, and one human pass confirms it end to end before Phase 3 builds on it.
Output: Updated 02-VALIDATION.md; human approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md
@.planning/phases/02-per-seat-redaction-contract/02-04-SUMMARY.md
@.planning/phases/02-per-seat-redaction-contract/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-wide automated gate and VALIDATION.md sign-off</name>
  <files>.planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md
    - package.json (scripts; note `npm run typecheck` is broken because there is no root tsconfig.json, so use per-project tsc)
    - .planning/phases/02-per-seat-redaction-contract/02-04-SUMMARY.md and 02-05-SUMMARY.md (any deviations to reflect)
  </read_first>
  <action>
    Run, in order, and capture exit codes:
    1. npm test
    2. npx tsc -p packages/rules/tsconfig.json --noEmit
    3. npx tsc -p packages/schema/tsconfig.json --noEmit
    4. npx tsc -p apps/worker/tsconfig.json --noEmit
    5. npx tsc -p apps/web/tsconfig.json --noEmit
    6. npm run build:worker
    7. npm run build:web
    8. npx playwright test (the full suite, not just the two edited specs, since the adapter swap touches every in-game path)
    If any command fails, diagnose and fix within the file that owns the failure only if the fix is a clear defect in this phase's work; otherwise stop and report the failure in the SUMMARY without marking validation green.
    On all-green, update 02-VALIDATION.md:
    - Set frontmatter status: complete, nyquist_compliant: true, wave_0_complete: true.
    - Set every Per-Task Verification Map row's Status to ✅ green.
    - Tick every Wave 0 Requirements and Validation Sign-Off checkbox.
    - Set "Approval:" to "automated gate green {date}; manual check pending Task 2".
    Paste the test-count summary line from npm test and the Playwright pass count into a short "Gate Results" section at the end of the file.
  </action>
  <verify>
    <automated>npm test && npx tsc -p apps/worker/tsconfig.json --noEmit && npx tsc -p apps/web/tsconfig.json --noEmit && npx tsc -p packages/rules/tsconfig.json --noEmit && npx tsc -p packages/schema/tsconfig.json --noEmit && npm run build:worker && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the action list exits 0
    - `grep -c "nyquist_compliant: true" .planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md` outputs 1
    - `grep -c "⬜ pending" .planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md` outputs 0
    - `grep -c "## Gate Results" .planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md` outputs 1
  </acceptance_criteria>
  <done>The integrated phase passes every automated layer and VALIDATION.md records it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of redaction in real browsers and raw WebSocket frames</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/02-per-seat-redaction-contract/02-VALIDATION.md (Manual-Only Verifications row)
    - .planning/phases/02-per-seat-redaction-contract/02-UI-SPEC.md (expected copy and blank own-card tile)
  </read_first>
  <action>Start the worker and web dev servers for the user if they are not already running (the automated gate in Task 1 must be green first), present the how-to-verify steps below, and wait for the resume signal. Do not modify any source file during this checkpoint; any reported leak is recorded in the SUMMARY and routed to gap closure.</action>
  <verify>
    <automated>npx vitest run --project worker source-structure room-do</automated>
  </verify>
  <done>User typed "approved", or every reported issue is recorded with window, step, and frame contents.</done>
  <what-built>The forehead-card toy game with per-seat whitelist projection, a strict view schema gate, a single #send chokepoint, and three automated leak-test layers. All automated checks are green (Task 1).</what-built>
  <how-to-verify>
    1. From the repo root run `npm run dev --workspace apps/worker` in one terminal and `npx next dev -p 3100` inside apps/web in another (or reuse the Playwright webServer commands from playwright.config.ts).
    2. Open http://localhost:3100 in a normal window, create a room as "Roger", and copy the link.
    3. Open the link in a private/incognito window, join as "Bianca", and press Start game in Roger's window.
    4. Confirm in both windows: the other player's card shows a star name; "Your card" is a blank tile; exactly one window reads "Your turn — guess your card"; 16 guess buttons exist and are enabled only in that window.
    5. In Bianca's window open DevTools, then Network, then filter WS, then select the /parties/room/ connection and open Messages. Read the star name Roger's window shows for Bianca's card. Search Bianca's messages for that star name: it must not appear in any frame. Expand the latest "state" frame: view.game.yourCard must contain only "id" and "hidden".
    6. Take a guess in the active window. Confirm both windows show one revealed entry, "13 left in deck", and the turn indicator swaps. Repeat step 5 on the newest frames.
    7. Refresh Bianca's window (reconnect via saved seat token). Confirm she returns to the same game with her card still blank, and her new "joined" frame also lacks her card's star name.
    8. Confirm every frame type seen is one of joined, state, error, refused, superseded.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what leaked or looked wrong (window, step, frame contents)</resume-signal>
  <acceptance_criteria>
    - The user responds "approved", OR any reported issue is logged in the SUMMARY with window, step, and frame contents
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable Object -> browser WebSocket | The raw frames a player can inspect in DevTools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22 | Information Disclosure | end-to-end integrated send path | mitigate | Full automated gate (layers 1-3, structural test, Playwright) on the merged tree plus manual DevTools frame inspection including reconnect |
| T-02-23 | Repudiation | validation record | accept | VALIDATION.md gate results are a local planning record; git history provides the audit trail |
</threat_model>

<verification>
- Task 1 automated gate all green
- Human approval recorded
</verification>

<success_criteria>
- ROADMAP Phase 2 success criteria 1-3 each have automated evidence and a manual confirmation
</success_criteria>

<output>
Create `.planning/phases/02-per-seat-redaction-contract/02-06-SUMMARY.md` when done
</output>
