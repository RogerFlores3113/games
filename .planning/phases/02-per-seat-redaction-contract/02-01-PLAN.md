---
phase: 02-per-seat-redaction-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rules/src/shuffle.ts
  - packages/rules/src/shuffle.test.ts
  - packages/rules/src/forehead-card.ts
  - packages/rules/src/forehead-card.test.ts
  - packages/rules/src/forehead-card-leak-check.ts
  - packages/rules/src/forehead-card-leak-check.test.ts
  - packages/rules/src/forehead-card.property.test.ts
  - packages/rules/src/adapter.test.ts
  - packages/rules/src/index.ts
autonomous: true
requirements: [HIDE-01, HIDE-03, HIDE-04]

must_haves:
  truths:
    - "D-01: foreheadCardGame deals one hidden card per seat from a seeded 16-card deck; the active seat guesses its own card; the card is revealed publicly (right or wrong) and a replacement is drawn; checkGameEnd returns { score, reason } once the deck is empty"
    - "D-04: every dealt/drawn card carries an opaque 8-lowercase-letter id minted at deal/draw time from a PRNG stream independent of the deck shuffle; undealt deck entries carry no id at all"
    - "D-05: toPlayerView whitelist-constructs every card; the viewer's own card is { id, hidden: true } with no value key; no spread, omit, delete, or null/undefined assignment appears in projection code"
    - "D-11 layer 1: a fast-check property over random seeds, seat counts 2-5, and random legal guess sequences proves no seat's toPlayerView ever carries its own card's identity"
    - "D-12: the leak checker asserts structural absence (\"value\" in entry) AND raw-string absence, using card values that are unique per card instance so a string scan cannot pass or fail by coincidence"
    - "D-13: a canary suite runs the checker against deliberately leaky projections (own value present, value: null, value: undefined, own card moved into otherCards, deck leaked, seed leaked) and asserts each is reported"
    - "D-14: the leak checker treats the server seed and every undealt deck value as forbidden in every seat's view"
  artifacts:
    - path: "packages/rules/src/shuffle.ts"
      provides: "zero-dependency cyrb128 + sfc32 seeded PRNG, Fisher-Yates shuffle, card-id minting"
      exports: ["seedToRngState", "nextRandom", "shuffleWithSeed", "mintCardId"]
    - path: "packages/rules/src/forehead-card.ts"
      provides: "the toy secret-holding GameAdapter"
      exports: ["foreheadCardGame", "FOREHEAD_CARD_VALUES"]
    - path: "packages/rules/src/forehead-card-leak-check.ts"
      provides: "the single shared leak checker used by all three D-11 layers"
      exports: ["checkSeatViewForLeaks", "secretsForSeat"]
    - path: "packages/rules/src/forehead-card.property.test.ts"
      provides: "D-11 layer 1 fast-check property"
      contains: "fc.assert"
  key_links:
    - from: "packages/rules/src/forehead-card.ts"
      to: "packages/rules/src/shuffle.ts"
      via: "createInitialState shuffles and mints ids from seed"
      pattern: "shuffleWithSeed\\(|mintCardId\\("
    - from: "packages/rules/src/forehead-card.property.test.ts"
      to: "packages/rules/src/forehead-card-leak-check.ts"
      via: "every generated seat view is checked"
      pattern: "checkSeatViewForLeaks\\("
    - from: "packages/rules/src/index.ts"
      to: "packages/rules/src/forehead-card-leak-check.ts"
      via: "re-export so apps/worker layers 2 and 3 import the SAME checker"
      pattern: "forehead-card-leak-check"
---

<objective>
Build the toy "forehead card" secret-holding game adapter in the zero-dependency rules package, plus the single shared leak checker, its canary suite, and the D-11 layer-1 property test.

Purpose: Give the redaction chokepoint something real to hide (Hanabi's three visibility classes: visible-to-others-not-self, hidden-from-all deck, public-after-reveal), and build the one leak checker that every later layer reuses, proven able to fail.
Output: shuffle.ts, forehead-card.ts, forehead-card-leak-check.ts, their tests, the layer-1 property test, and index.ts exports. The D-15 counter is NOT deleted here (room-state.ts and CounterGame.tsx still import it); Plan 05 deletes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md
@.planning/phases/02-per-seat-redaction-contract/02-RESEARCH.md
@.planning/phases/02-per-seat-redaction-contract/02-PATTERNS.md
@packages/rules/src/adapter.ts
@packages/rules/src/counter-game.ts
@packages/rules/src/adapter.test.ts

<interfaces>
Existing contract (packages/rules/src/adapter.ts, unchanged by this plan):
- GameAdapter<TState, TAction> { readonly id: string; createInitialState(input: { seatIds: readonly string[]; variant: Variant; seed: string }): TState; applyAction(state: TState, actorSeatId: string, request: unknown): AdapterResult<TState>; toPlayerView(state: TState, seatId: string): unknown; checkGameEnd(state: TState): GameEndResult | null }
- AdapterResult<TState> = { ok: true; state: TState } | { ok: false; error: "not_your_turn" | "invalid_action" | "game_over" }
- GameEndResult = { score: number; reason: string }
- adapter.test.ts exports describeAdapterConformance(name, adapter, sampleActions) and has a purity test listing scanned files.

The server seed (apps/worker/src/seat-identity.ts mintGameSeed) is 32 lowercase hex characters.

NEW contract this plan creates (Plan 02's Zod schema, Plan 03's worker wiring, and Plan 05's web UI are written against exactly these names, so do not rename):

packages/rules/src/forehead-card.ts
- FOREHEAD_CARD_VALUES: readonly tuple, declared "as const", exactly these 16 strings in this order: "Altair", "Sirius", "Antares", "Pollux", "Castor", "Capella", "Arcturus", "Procyon", "Regulus", "Bellatrix", "Aldebaran", "Betelgeuse", "Achernar", "Canopus", "Fomalhaut", "Mimosa"
- type ForeheadCardValue = (typeof FOREHEAD_CARD_VALUES)[number]
- type ForeheadCard = { readonly id: string; readonly value: ForeheadCardValue }
- type RevealedCard = { readonly id: string; readonly seatId: string; readonly value: ForeheadCardValue; readonly correct: boolean }
- type ForeheadCardState = { readonly seatIds: readonly string[]; readonly turnIndex: number; readonly hands: readonly { readonly seatId: string; readonly card: ForeheadCard }[]; readonly deck: readonly ForeheadCardValue[]; readonly revealed: readonly RevealedCard[]; readonly score: number; readonly idRng: readonly [number, number, number, number] }
- type ForeheadCardAction = { type: "guess"; value: ForeheadCardValue }
- type HiddenCardView = { id: string; hidden: true }
- type VisibleCardView = { id: string; hidden: false; value: ForeheadCardValue }
- type ForeheadCardView = { yourCard: HiddenCardView; otherCards: Array<{ seatId: string; card: HiddenCardView | VisibleCardView }>; revealed: Array<{ id: string; seatId: string; value: ForeheadCardValue; correct: boolean }>; deckCount: number; activeSeatId: string; isYourTurn: boolean; score: number }
  (View types use plain mutable Array and non-readonly fields on purpose, so ForeheadCardView stays assignable to Plan 02's z.infer type.)
- foreheadCardGame: GameAdapter<ForeheadCardState, ForeheadCardAction> with id "forehead-card"

packages/rules/src/forehead-card-leak-check.ts
- interface SeatSecrets { readonly ownCard: { readonly id: string; readonly value: string } | null; readonly forbiddenTokens: readonly string[] }
- secretsForSeat(state: ForeheadCardState, seatId: string, seed: string | undefined): SeatSecrets
- checkSeatViewForLeaks(input: { view: unknown; serialized: string; secrets: SeatSecrets }): string[]   (empty array means clean)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zero-dependency seeded PRNG, shuffle, and card-id minting (shuffle.ts)</name>
  <files>packages/rules/src/shuffle.ts, packages/rules/src/shuffle.test.ts</files>
  <read_first>
    - packages/rules/src/adapter.ts (the determinism contract on createInitialState and the FDN-02 zero-dependency rule)
    - packages/rules/src/adapter.test.ts (the purity test's forbidden-token list, so this file never trips it)
    - .planning/phases/02-per-seat-redaction-contract/02-PATTERNS.md section "packages/rules/src/shuffle.ts"
  </read_first>
  <behavior>
    - seedToRngState("abc", "deck") called twice returns deep-equal 4-tuples; seedToRngState("abc", "deck") differs from seedToRngState("abc", "card-ids")
    - nextRandom(state) returns { value, state } where value is an integer in [0, 2^32) and the input tuple is not mutated (Object.isFrozen not required; compare against a structuredClone snapshot)
    - shuffleWithSeed(items, seed, stream) returns a new array that is a permutation of items (same sorted contents), leaves the input untouched, and is identical for the same (seed, stream)
    - Across 20 distinct 32-hex seeds, shuffleWithSeed of FOREHEAD-sized input (16 items) produces at least 2 distinct orders (not a constant permutation)
    - mintCardId(rng, taken) returns { id, rng } where id matches /^[a-z]{8}$/, id is not in taken, and repeated minting with the returned rng and a growing taken set yields 16 unique ids
  </behavior>
  <action>
    Create packages/rules/src/shuffle.ts with zero imports. Implement the public-domain cyrb128 string hash (produces four 32-bit words) and the public-domain sfc32 generator (128-bit state). Rationale to record in the file header: a 32-bit PRNG seed (mulberry32) would let a player brute-force 2^32 shuffles against the cards they can see and recover their own card and the deck order; sfc32 seeded with all four cyrb128 words keeps the searchable state at 128 bits, matching mintGameSeed's 128-bit secret.
    Exports:
    - seedToRngState(seed: string, stream: string): readonly [number, number, number, number]. Hashes the string formed by stream + ":" + seed with cyrb128. Different stream names yield independent generators from one seed.
    - nextRandom(state: readonly [number, number, number, number]): { value: number; state: readonly [number, number, number, number] }. One pure sfc32 step. Returns a NEW tuple and never mutates the input. value is an unsigned 32-bit integer (use >>> 0).
    - shuffleWithSeed<T>(items: readonly T[], seed: string, stream: string): T[]. Fisher-Yates over a copy, index j = Math.floor((value / 4294967296) * (i + 1)).
    - mintCardId(rng: readonly [number, number, number, number], taken: ReadonlySet<string>): { id: string; rng: readonly [number, number, number, number] }. Builds 8 characters, each "abcdefghijklmnopqrstuvwxyz"[value % 26] from successive nextRandom calls, and retries while the id is in taken. Lowercase-only letters are deliberate: ids can never contain a digit (so they cannot be a deck index) or a capitalized card-value token.
    Write shuffle.test.ts first (RED), covering every behavior bullet, then implement (GREEN). Use Vitest imports only (describe, expect, it from "vitest").
  </action>
  <verify>
    <automated>npx vitest run --project rules shuffle && npx tsc -p packages/rules/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import" packages/rules/src/shuffle.ts` outputs 0
    - `grep -E "export function (seedToRngState|nextRandom|shuffleWithSeed|mintCardId)" packages/rules/src/shuffle.ts | wc -l` outputs 4
    - `grep -n "Math.random" packages/rules/src/shuffle.ts` returns no matches
    - `npx vitest run --project rules shuffle` exits 0 with at least 5 passing tests
    - `npx tsc -p packages/rules/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>Deterministic, 128-bit-state, zero-dependency PRNG, shuffle, and id minting exist with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The forehead-card toy adapter with whitelist projection (forehead-card.ts)</name>
  <files>packages/rules/src/forehead-card.ts, packages/rules/src/forehead-card.test.ts, packages/rules/src/adapter.test.ts, packages/rules/src/index.ts</files>
  <read_first>
    - packages/rules/src/adapter.ts
    - packages/rules/src/counter-game.ts (copy its adapter object shape, exact-key hostile-input guard, and new-object-literal non-mutation discipline, not its rules)
    - packages/rules/src/counter-game.test.ts (structuredClone snapshot-diff non-mutation tests to mirror)
    - packages/rules/src/adapter.test.ts (describeAdapterConformance signature and the purity file list)
    - packages/rules/src/index.ts
    - packages/rules/src/shuffle.ts (from Task 1)
    - .planning/phases/02-per-seat-redaction-contract/02-RESEARCH.md "Pattern 1" and "Pitfall 4"
  </read_first>
  <behavior>
    - createInitialState({ seatIds: ["a","b","c"], variant: "base", seed }) yields hands of length 3 in seatIds order, deck length 13, revealed [], score 0, turnIndex 0; the multiset of hand values plus deck equals FOREHEAD_CARD_VALUES exactly; the same input twice is deep-equal; two different seeds give different deck orders for at least one of 10 seed pairs
    - Every hand card id matches /^[a-z]{8}$/ and ids are unique; deck entries are bare value strings (no id)
    - applyAction by the active seat with { type: "guess", value: <its true value> } returns ok, appends { id, seatId, value, correct: true } to revealed, score +1, the seat's hand holds a NEW card whose id differs from the revealed id and whose value was deck[0], deck shrinks by 1, turnIndex advances modulo seat count
    - A wrong guess behaves identically except correct: false and score unchanged
    - Rejections: out-of-turn seat -> { ok: false, error: "not_your_turn" }; payloads "guess", null, { type: "guess" }, { type: "guess", value: "Pluto" }, { type: "guess", value: "Altair", score: 99 }, { type: "increment" } -> { ok: false, error: "invalid_action" }; any guess once deck is empty -> { ok: false, error: "game_over" }; input state deep-equals its structuredClone snapshot after both accepted and rejected calls
    - checkGameEnd is null while deck.length > 0 and { score, reason: "deck_exhausted" } when deck.length === 0; a 2-seat game ends after exactly 14 accepted guesses
    - toPlayerView(state, seatId): yourCard has exactly the keys ["hidden","id"] (sorted Object.keys) and hidden === true; otherCards lists every other seat in seat order with hidden: false and the true value; the result has exactly the keys activeSeatId, deckCount, isYourTurn, otherCards, revealed, score, yourCard; isYourTurn is true for exactly one seat; the redaction behavior is identical when checkGameEnd is non-null (no end-of-game reveal branch)
    - toPlayerView for a seatId not in hands returns yourCard { id: "unseated", hidden: true } and every hand as a hidden card in otherCards (fails closed: an unknown viewer sees less, never more)
    - No FOREHEAD_CARD_VALUES entry is a substring of any other entry, of any view key name (yourCard, otherCards, revealed, deckCount, activeSeatId, isYourTurn, score, hidden, value, seatId, id, correct), and each entry has length >= 6
  </behavior>
  <action>
    Create packages/rules/src/forehead-card.ts exporting exactly the names and types in the interfaces block (D-01, D-04, D-05). Its only import is from "./shuffle" and a type import from "./adapter". Toy rules, chosen under CONTEXT.md Claude's Discretion: a 16-card deck whose card values are 16 unique star names (FOREHEAD_CARD_VALUES). Values are unique per card instance on purpose, per D-12: a seat's own value, and every undealt value, can then never legitimately appear anywhere in that seat's payload, so a raw-string scan is unambiguous.
    createInitialState: deck = shuffleWithSeed(FOREHEAD_CARD_VALUES, seed, "deck"). Deal one card per seat in seatIds order from the front of the deck. Mint each dealt card's id with mintCardId, starting from seedToRngState(seed, "card-ids") and a taken set of already-minted ids. Store the final generator state in idRng. The deck field holds only the undealt values. Per D-04, ids are minted at deal time and never derived from the pre-shuffle index or deck position.
    applyAction: validate the request with an exact-key guard modeled on counter-game.ts isIncrementRequest. It must be a non-null object with exactly the two own keys "type" and "value", type === "guess", and value included in FOREHEAD_CARD_VALUES; otherwise return invalid_action. Then check the actor is seatIds[turnIndex] (not_your_turn), then deck.length === 0 (game_over). On accept, build brand-new object literals:
    - the revealed entry
    - a new hands array, where only the actor's entry gets a new card: value deck[0], id from mintCardId with a taken set of every hand id plus every revealed id
    - deck as deck.slice(1)
    - score incremented when correct
    - turnIndex advanced
    - the new idRng
    Never mutate the input.
    toPlayerView (D-05): whitelist construction only. Build yourCard as the object literal { id: ownCard.id, hidden: true }. For each other seat, build { seatId, card: { id, hidden: false, value } }. Build revealed entries field by field ({ id, seatId, value, correct }). Add deckCount: deck.length, activeSeatId, isYourTurn, and score. The following are forbidden anywhere in this file, and the file header must say so with a D-05 citation: the spread operator in object literals, delete, Object.assign, any omit helper, and any assignment of null or undefined to a value key. The view must never include seatIds order beyond otherCards, the deck, idRng, or the seed. There is no branch on game end (RESEARCH.md Pitfall 4).
    checkGameEnd: deck.length === 0 returns { score, reason: "deck_exhausted" }, otherwise null.
    Write forehead-card.test.ts first covering every behavior bullet (RED), then implement (GREEN).
    In adapter.test.ts:
    - add describeAdapterConformance("forehead-card", foreheadCardGame, [{ type: "guess", value: "Altair" }]) next to the existing counter call. Leave the counter call; Plan 05 removes it.
    - append "shuffle.ts" and "forehead-card.ts" to the purity test's files array.
    In index.ts, add a value export of foreheadCardGame and FOREHEAD_CARD_VALUES, and type exports of ForeheadCardState, ForeheadCardAction, ForeheadCardView, ForeheadCardValue, HiddenCardView, VisibleCardView, and RevealedCard. Keep the counter exports.
  </action>
  <verify>
    <automated>npx vitest run --project rules && npx tsc -p packages/rules/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' packages/rules/src/forehead-card.ts | grep -v '^\s*\*' | grep -cE '\.\.\.|delete |Object\.assign|omit\(|value: (null|undefined)'` outputs 0
    - `grep -c 'id: "forehead-card"' packages/rules/src/forehead-card.ts` outputs 1
    - `grep -c 'describeAdapterConformance("forehead-card"' packages/rules/src/adapter.test.ts` outputs 1
    - `grep -E '"shuffle.ts"|"forehead-card.ts"' packages/rules/src/adapter.test.ts | wc -l` outputs at least 1 line containing both names (files array)
    - `grep -c "foreheadCardGame" packages/rules/src/index.ts` outputs at least 1
    - `cat packages/rules/package.json` still shows no "dependencies" key
    - `npx vitest run --project rules` exits 0, including the forehead-card adapter conformance suite and the purity suite
    - `npx tsc -p packages/rules/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>foreheadCardGame implements GameAdapter with whitelist projection, opaque deal-time ids, deterministic seeded deck, a game_over/ended path, and passes unit and conformance tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared leak checker, D-13 canary, and D-11 layer-1 property test</name>
  <files>packages/rules/src/forehead-card-leak-check.ts, packages/rules/src/forehead-card-leak-check.test.ts, packages/rules/src/forehead-card.property.test.ts, packages/rules/src/adapter.test.ts, packages/rules/src/index.ts</files>
  <read_first>
    - packages/rules/src/forehead-card.ts (from Task 2)
    - packages/rules/src/adapter.test.ts (fast-check import style: `import fc from "fast-check"`)
    - .planning/phases/02-per-seat-redaction-contract/02-RESEARCH.md "Code Examples: Leak checker shared by all three D-11 layers" and "Pitfall 2"
    - .planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md D-11 through D-14
  </read_first>
  <behavior>
    - Clean baseline: for a real 3-seat state (seed "0123456789abcdef0123456789abcdef"), checkSeatViewForLeaks on each seat's real toPlayerView with JSON.stringify of it and secretsForSeat(state, seat, seed) returns []
    - Canary A (own value present): yourCard { id, hidden: true, value: ownValue } reports reasons including "structural:yourCard-has-value" and "string:own-value"
    - Canary B (value: null): reports "structural:yourCard-has-value"
    - Canary C (value: undefined): the test first asserts "value" in yourCard is true and JSON.stringify output does not contain "value" for that card, then asserts "structural:yourCard-has-value" is reported (the string scan alone would miss it)
    - Canary D (own card moved into otherCards as { seatId: viewer, card: { id: ownId, hidden: false, value: ownValue } } with a clean yourCard): reports "structural:own-card-id-has-value" and "string:own-value"
    - Canary E (a hidden card anywhere carrying a value key, e.g. otherCards entry { id, hidden: true, value: "x" }): reports "structural:hidden-card-has-value"
    - Canary F (deck leaked under an arbitrary key such as debugDeck: state.deck): reports one "string:forbidden-token:<token>" reason per undealt value
    - Canary G (seed leaked under an arbitrary key): reports "string:forbidden-token:<seed>"
    - Canary H (own value embedded in an unrelated string field, e.g. note: "maybe " + ownValue): reports "string:own-value" only
    - secretsForSeat returns ownCard equal to the seat's hand card and forbiddenTokens equal to the deck values followed by the seed when the seed is defined; ownCard is null for an unknown seat
    - Layer 1 property: for random seat count 2-5, random 16-byte hex seed, and a random array (length 0-20) of guess indices 0-15, every state from initial through each accepted guess (stopping when checkGameEnd is non-null) yields [] for every seat
  </behavior>
  <action>
    Create packages/rules/src/forehead-card-leak-check.ts with a type-only import from "./forehead-card" and no other imports. It exports SeatSecrets, secretsForSeat, and checkSeatViewForLeaks, exactly as the interfaces block defines them. The file header must state that this is the single leak checker imported by all three D-11 layers (this package's property test, apps/worker's wire property test, and apps/worker's wrangler-dev integration test). Reusing it is what makes the D-13 canary evidence for every layer.
    checkSeatViewForLeaks walks input.view recursively over arrays and plain objects and collects reason strings:
    - "structural:yourCard-has-value": an object has a yourCard key whose value is a non-null object for which "value" in that object is true. Per D-12 use the in operator, never a truthiness or undefined comparison.
    - "structural:hidden-card-has-value": an object has hidden === true and "value" in it.
    - "structural:own-card-id-has-value": secrets.ownCard is non-null, an object's id === secrets.ownCard.id, and "value" in it.
    - "string:own-value": secrets.ownCard is non-null and input.serialized.includes(secrets.ownCard.value).
    - "string:forbidden-token:" + token: for each token in secrets.forbiddenTokens (tokens of length 0 are skipped), input.serialized.includes(token).
    Deduplicate the reasons and return them in insertion order. Per D-14, secretsForSeat puts the undealt deck values and the seed into forbiddenTokens.
    Write forehead-card-leak-check.test.ts as the D-13 canary suite covering behavior bullets 1 through 9. Leaky views are built as new object literals in the test, never by mutating the real view.
    Create forehead-card.property.test.ts for D-11 layer 1. Use fc.assert(fc.property(...), { numRuns: 200 }). The arbitraries are:
    - fc.integer({ min: 2, max: 5 }) for seat count
    - fc.uint8Array({ minLength: 16, maxLength: 16 }) mapped to lowercase hex, mirroring mintGameSeed
    - fc.array(fc.nat({ max: 15 }), { maxLength: 20 }) for guesses
    Seat ids are "seat-0" through "seat-N". For each guess index, apply { type: "guess", value: FOREHEAD_CARD_VALUES[index] } as state.seatIds[state.turnIndex] and expect ok. After every state, including the initial one, run the checker for every seat against both the in-memory view object and its JSON.stringify string. Also add a deterministic test that plays a 5-seat game to completion (11 guesses) and checks every seat at every step, including the final ended state.
    Append "forehead-card-leak-check.ts" to adapter.test.ts's purity files array. In index.ts, export checkSeatViewForLeaks and secretsForSeat, plus the type SeatSecrets.
  </action>
  <verify>
    <automated>npx vitest run --project rules && npx tsc -p packages/rules/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"value" in' packages/rules/src/forehead-card-leak-check.ts` outputs at least 3
    - `grep -cE '\.value === undefined|\.value == null' packages/rules/src/forehead-card-leak-check.ts` outputs 0
    - `grep -c "numRuns: 200" packages/rules/src/forehead-card.property.test.ts` outputs 1
    - `grep -c "checkSeatViewForLeaks" packages/rules/src/index.ts` outputs at least 1
    - `grep -c '"forehead-card-leak-check.ts"' packages/rules/src/adapter.test.ts` outputs 1
    - `npx vitest run --project rules forehead-card-leak-check` exits 0 with at least 9 passing tests
    - `npx vitest run --project rules` exits 0
    - Manual canary sanity (do not commit): temporarily change the projection so yourCard includes value, confirm `npx vitest run --project rules forehead-card.property` FAILS, then revert. Record the observation in the SUMMARY.
  </acceptance_criteria>
  <done>One exported leak checker detects structural and raw-string leaks of own value, deck, and seed; the canary suite proves every detection path fires; the layer-1 property passes over 200 random games.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter state -> per-seat view | Full game state (all hands, deck, seed-derived order) is reduced to one seat's view; this is the leak boundary |
| client -> applyAction request | The guess payload is hostile network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | foreheadCardGame.toPlayerView | mitigate | Whitelist object-literal construction; own card is { id, hidden: true } with no value key (D-05); unit test asserts exact key sets; canary + property tests (D-11 layer 1, D-13) |
| T-02-02 | Information Disclosure | shuffle.ts PRNG seeding | mitigate | sfc32 with 128-bit state from cyrb128 of the full seed, so the deck order cannot be recovered by brute-forcing a 32-bit seed space from visible cards |
| T-02-03 | Information Disclosure | card ids (D-04) | mitigate | Ids minted at deal/draw from an independent "card-ids" PRNG stream, lowercase letters only, never the pre-shuffle index; deck entries carry no id |
| T-02-04 | Tampering | applyAction request parsing | mitigate | Exact two-key guard (type, value) with value membership in FOREHEAD_CARD_VALUES; extra keys rejected invalid_action; conformance suite's fc.jsonValue hostile-payload property asserts no throw |
| T-02-05 | Repudiation / silent failure | leak checker itself | mitigate | D-13 canary suite proves every detection path reports a leak, including value: undefined which JSON.stringify hides |
</threat_model>

<verification>
- `npx vitest run --project rules` exits 0 (shuffle, forehead-card unit, conformance, purity, canary, layer-1 property)
- `npx tsc -p packages/rules/tsconfig.json --noEmit` exits 0
- packages/rules/package.json still declares no dependencies (FDN-02)
</verification>

<success_criteria>
- foreheadCardGame exists, conforms to GameAdapter, and is exported from @games/rules
- The single shared leak checker is exported from @games/rules and proven able to fail by the canary suite
- The layer-1 property test passes with numRuns 200
</success_criteria>

<output>
Create `.planning/phases/02-per-seat-redaction-contract/02-01-SUMMARY.md` when done
</output>
