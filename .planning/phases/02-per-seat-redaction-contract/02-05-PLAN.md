---
phase: 02-per-seat-redaction-contract
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - apps/web/components/ForeheadCardGame.tsx
  - apps/web/components/CounterGame.tsx
  - apps/web/app/room/[code]/RoomClient.tsx
  - e2e/start-game.spec.ts
  - e2e/in-progress-arrival.spec.ts
  - packages/rules/src/counter-game.ts
  - packages/rules/src/counter-game.test.ts
  - packages/rules/src/index.ts
  - packages/rules/src/adapter.test.ts
  - packages/rules/src/adapter.ts
autonomous: true
requirements: [HIDE-01]

must_haves:
  truths:
    - "D-03: once a game starts, each browser shows every other seat's card face up, its own card as a blank face-down tile, the revealed pile with score and deck count, a turn indicator, and one guess button per possible value enabled only on the viewer's turn"
    - "D-03: when the room status is ended, the guess controls are removed and Game over / Final score: {score} is shown above the still-visible revealed pile, with no reveal of any hidden card"
    - "D-02: CounterGame.tsx, counter-game.ts, counter-game.test.ts, and every counter export are deleted; no counterGame/CounterState/CounterAction/CounterView/CounterGame identifier remains in apps, packages, or e2e"
    - "D-02: e2e/start-game.spec.ts and e2e/in-progress-arrival.spec.ts drive the toy game and pass"
    - "HIDE-01 (browser surface): a player's own-card tile renders no text, and the value that a teammate sees for that player's card is absent from that player's own-card tile"
  artifacts:
    - path: "apps/web/components/ForeheadCardGame.tsx"
      provides: "minimal toy game screen"
      contains: "own-card"
    - path: "apps/web/app/room/[code]/RoomClient.tsx"
      provides: "renders ForeheadCardGame for in_progress and ended rooms"
      contains: "ForeheadCardGame"
    - path: "e2e/start-game.spec.ts"
      provides: "end-to-end toy game turn flow"
      contains: "guess-button-"
  key_links:
    - from: "apps/web/app/room/[code]/RoomClient.tsx"
      to: "apps/web/components/ForeheadCardGame.tsx"
      via: "onGuess sends game_action guess"
      pattern: "type: \"guess\""
    - from: "apps/web/components/ForeheadCardGame.tsx"
      to: "@games/rules"
      via: "FOREHEAD_CARD_VALUES for guess buttons, ForeheadCardView type"
      pattern: "FOREHEAD_CARD_VALUES"
---

<objective>
Replace the counter screen with the minimal toy game UI, update the two counter-driven Playwright specs, and delete every remaining trace of the D-15 counter.

Purpose: D-03 requires the redaction contract to be observable in a real browser; D-02 requires the counter to be deleted, not kept alongside.
Output: ForeheadCardGame.tsx, RoomClient.tsx swap, updated e2e specs, counter files and exports removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md
@.planning/phases/02-per-seat-redaction-contract/02-UI-SPEC.md
@.planning/phases/02-per-seat-redaction-contract/02-01-SUMMARY.md
@apps/web/AGENTS.md
@apps/web/components/CounterGame.tsx
@apps/web/app/room/[code]/RoomClient.tsx
@e2e/start-game.spec.ts
@e2e/in-progress-arrival.spec.ts

<interfaces>
From @games/rules (Plan 01):
- FOREHEAD_CARD_VALUES: readonly tuple of 16 star names ("Altair", "Sirius", "Antares", "Pollux", "Castor", "Capella", "Arcturus", "Procyon", "Regulus", "Bellatrix", "Aldebaran", "Betelgeuse", "Achernar", "Canopus", "Fomalhaut", "Mimosa")
- type ForeheadCardValue
- type ForeheadCardView = { yourCard: { id: string; hidden: true }; otherCards: Array<{ seatId: string; card: { id: string; hidden: true } | { id: string; hidden: false; value: ForeheadCardValue } }>; revealed: Array<{ id: string; seatId: string; value: ForeheadCardValue; correct: boolean }>; deckCount: number; activeSeatId: string; isYourTurn: boolean; score: number }
- Deck: 16 cards; with 2 seats, 14 remain after dealing; each accepted guess draws one.

From @games/schema: RoomView { code, variant, status: "lobby" | "in_progress" | "ended", hostSeatId, youSeatId, seats: PublicSeat[] ({ seatId, displayLabel, connected, isHost }), game: unknown }; ClientMessage game_action { type: "game_action"; request: unknown }.

Existing web pieces: apps/web/components/Button.tsx (variant "primary" | "ghost", disabled), CounterGame.tsx styling (Tailwind classes plus inline style referencing var(--color-*), var(--space-*), var(--text-*)), RoomClient.tsx lines 18 (import) and 209-214 (render).

UI-SPEC copy contract (exact strings):
- Turn indicator: "Your turn — guess your card" / "Waiting for {displayLabel}"
- Own card label: "Your card"; other card label: "{displayLabel}"
- Guess group heading: "Guess your card"; guess button label: the bare value
- Revealed heading: "Revealed"; empty: "No cards revealed yet"
- Deck count: "{n} left in deck"
- End: "Game over" and "Final score: {score}"
UI-SPEC testids: own-card, turn-indicator, guess-button-{value}, revealed-list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ForeheadCardGame screen and RoomClient swap</name>
  <files>apps/web/components/ForeheadCardGame.tsx, apps/web/components/CounterGame.tsx, apps/web/app/room/[code]/RoomClient.tsx</files>
  <read_first>
    - apps/web/AGENTS.md, then the relevant client-component guidance under apps/web/node_modules/next/dist/docs/ or node_modules/next/dist/docs/ (this Next.js version has breaking changes; this task only writes a "use client" component with no new routing or data APIs)
    - apps/web/components/CounterGame.tsx (structure, type guard, styling tokens, data-testid convention, deliberate-bareness header comment)
    - apps/web/components/Button.tsx (props)
    - apps/web/app/room/[code]/RoomClient.tsx
    - apps/web/app/globals.css (confirm token names --color-surface, --color-border, --space-sm/lg/xl, --text-heading, --text-label exist before using them)
    - .planning/phases/02-per-seat-redaction-contract/02-UI-SPEC.md (layout, colors, copy, testids)
  </read_first>
  <action>
    Create apps/web/components/ForeheadCardGame.tsx as a "use client" component (D-03). It exports the interface ForeheadCardGameProps { view: RoomView; onGuess: (value: ForeheadCardValue) => void } and the function ForeheadCardGame.
    Imports:
    - type RoomView from "@games/schema"
    - FOREHEAD_CARD_VALUES and types ForeheadCardView, ForeheadCardValue from "@games/rules"
    - Button from "./Button"
    Add a type guard isForeheadCardView(game: unknown) that checks for a non-null object with yourCard, otherCards (array), and revealed (array). Resolve displayLabel by seatId from view.seats, falling back to an ellipsis character.
    Render top to bottom per UI-SPEC "Screens & States":
    1. A p with data-testid="turn-indicator". Its text is "Your turn — guess your card" when game.isYourTurn and the status is not ended; otherwise "Waiting for {label of activeSeatId}". Color is var(--color-accent) on your turn, otherwise var(--color-text-muted). When status is ended, do not render the indicator.
    2. A row with data-testid="other-cards", holding one tile per otherCards entry with data-testid="other-card" and data-seat-id={seatId}. Each tile has a Label-role displayLabel above it and a surface/border tile showing card.value in the Heading role. If an entry's card is hidden, show an empty tile.
    3. The own card: "Your card" label, then an empty tile with data-testid="own-card", using var(--color-surface) fill and a var(--color-border) outline. It has NO children, no glyph, and no "?" (UI-SPEC: the face-down tile is deliberately blank). It never reads anything from yourCard except id, which may be used as a key.
    4. If view.status is not "ended": a "Guess your card" heading and a wrapping sm-gap row with one Button per FOREHEAD_CARD_VALUES entry. Each button has variant="primary", data-testid={"guess-button-" + value}, disabled={!game.isYourTurn}, onClick calls onGuess(value), and its label is the value. If Button does not forward data-* props, wrap each Button in a span carrying the testid and note it in the SUMMARY; prefer forwarding if Button already spreads rest props. If view.status is "ended": a "Game over" heading (Heading role) plus a paragraph data-testid="final-score" reading "Final score: {score}", with no guess controls rendered at all.
    5. A surface strip with a "Revealed" label and a ul data-testid="revealed-list". Each entry is an li data-testid="revealed-entry" reading "{displayLabel}: {value}". Show "No cards revealed yet" when empty. Below it, p data-testid="score" reading "Score: {score}" (running score; UI-SPEC gives no exact copy, chosen under D-03 discretion) and p data-testid="deck-count" reading "{deckCount} left in deck" in muted color.
    Use only existing theme tokens via Tailwind arbitrary values and inline style, as CounterGame.tsx does. No new colors, sizes, animation, or icons. If the view is not a ForeheadCardView, render the same shell with the turn indicator reading "Waiting for …" and nothing else. Copy CounterGame.tsx's deliberate-bareness header comment, updated to say the screen exists to prove D-01/D-03 redaction in a browser and is deleted in Phase 4.
    Delete apps/web/components/CounterGame.tsx with git rm.
    In RoomClient.tsx, change the import to ForeheadCardGame from "../../../components/ForeheadCardGame". Replace the final return with the ForeheadCardGame element: view={view} and onGuess sending { type: "game_action", request: { type: "guess", value } }. This branch already covers both in_progress and ended because only lobby returns earlier. Change nothing else in RoomClient.tsx.
  </action>
  <verify>
    <automated>npx tsc -p apps/web/tsconfig.json --noEmit && npx vitest run --project web && npm run build:web</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/components/CounterGame.tsx` fails (file deleted)
    - `grep -c "ForeheadCardGame" "apps/web/app/room/[code]/RoomClient.tsx"` outputs at least 2
    - `grep -c 'type: "guess"' "apps/web/app/room/[code]/RoomClient.tsx"` outputs 1
    - `grep -c 'data-testid="own-card"' apps/web/components/ForeheadCardGame.tsx` outputs 1
    - `grep -c "Your turn — guess your card" apps/web/components/ForeheadCardGame.tsx` outputs 1
    - `grep -c "left in deck" apps/web/components/ForeheadCardGame.tsx` outputs 1
    - `grep -c "Final score: " apps/web/components/ForeheadCardGame.tsx` outputs 1
    - `grep -cE "#[0-9A-Fa-f]{6}" apps/web/components/ForeheadCardGame.tsx` outputs 0 (tokens only)
    - `grep -c "yourCard.value" apps/web/components/ForeheadCardGame.tsx` outputs 0
    - `npx tsc -p apps/web/tsconfig.json --noEmit`, `npx vitest run --project web`, and `npm run build:web` exit 0
  </acceptance_criteria>
  <done>The in-game screen renders the toy per UI-SPEC with a blank own-card tile and sends guess actions; the counter component is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update counter-driven Playwright specs to the toy game</name>
  <files>e2e/start-game.spec.ts, e2e/in-progress-arrival.spec.ts</files>
  <read_first>
    - e2e/start-game.spec.ts
    - e2e/in-progress-arrival.spec.ts
    - e2e/helpers.ts (createRoom, joinAs, expectSeatCount)
    - playwright.config.ts (webServer starts web on 3100 and worker on 8787)
    - apps/web/components/ForeheadCardGame.tsx (Task 1 testids and copy)
  </read_first>
  <action>
    In e2e/start-game.spec.ts, keep all lobby, variant, and gating assertions before the start click unchanged. Rename the test title to reference D-02/D-03 and the toy game instead of the counter. After startButton.click(), replace the counter assertions with:
    1. The own-card testid is visible on both pages.
    2. The turn indicator: exactly one page's text equals "Your turn — guess your card" and the other contains "Waiting for".
    3. HIDE-01 browser surface:
       - On the host page, read the text of the other-card tile whose data-seat-id is not the host's own seat: the locator for other-card, filtered to the single entry. This is Bianca's card value.
       - Assert that value is one of the guess button labels.
       - Assert that pageB's own-card tile has text "" and pageB's own-card inner text does not contain that value.
       - Do the symmetric check using pageB's other-card value against the host page's own-card.
    4. Guess controls: getByTestId(/^guess-button-/) has count 16 on both pages; on the active page the first one is enabled and on the waiting page the first one is disabled.
    5. The active page clicks its first guess button. Then on both pages revealed-entry has count 1, deck-count has text "13 left in deck", the active page's turn-indicator contains "Waiting for", and the waiting page's turn-indicator equals "Your turn — guess your card".
    6. Keep the final assertion that variant-picker has count 0 on the host page.
    Do not import from @games/rules in e2e files: Playwright's TS transform has no root tsconfig paths here. Derive values from the DOM.
    In e2e/in-progress-arrival.spec.ts, replace both counter-value testid usages with own-card: visible on the host after start, and count 0 on pageC. Leave everything else unchanged.
    Run the two specs. If the Playwright browsers are missing, run `npx playwright install chromium` first; that is not a package install.
  </action>
  <verify>
    <automated>npx playwright test e2e/start-game.spec.ts e2e/in-progress-arrival.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "counter-value" e2e/` shows 0 for every file
    - `grep -c 'getByRole("button", { name: "+1" })' e2e/start-game.spec.ts` outputs 0
    - `grep -c "13 left in deck" e2e/start-game.spec.ts` outputs 1
    - `grep -c 'getByTestId("own-card")' e2e/in-progress-arrival.spec.ts` outputs at least 2
    - `grep -c "@games/rules" e2e/start-game.spec.ts` outputs 0
    - `npx playwright test e2e/start-game.spec.ts e2e/in-progress-arrival.spec.ts` exits 0
  </acceptance_criteria>
  <done>Both previously counter-driven specs pass against the toy game, including a browser-surface own-card redaction check.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the D-15 counter from packages/rules</name>
  <files>packages/rules/src/counter-game.ts, packages/rules/src/counter-game.test.ts, packages/rules/src/index.ts, packages/rules/src/adapter.test.ts, packages/rules/src/adapter.ts</files>
  <read_first>
    - packages/rules/src/index.ts
    - packages/rules/src/adapter.test.ts
    - packages/rules/src/adapter.ts (doc comments mentioning the counter)
    - apps/worker/src/room-state.ts (confirm Plan 03 already removed counter usage)
  </read_first>
  <action>
    Per D-02:
    - git rm packages/rules/src/counter-game.ts and packages/rules/src/counter-game.test.ts.
    - In packages/rules/src/index.ts, remove the counterGame export and the CounterAction/CounterState/CounterView type exports. Rewrite the stale header comment lines about the D-15 counter to say the package carries the adapter contract, the forehead-card toy (Phase 2, deleted in Phase 4), and its shared leak checker.
    - In adapter.test.ts, remove the counterGame import, the describeAdapterConformance("counter", ...) call, and "counter-game.ts" from the purity files array. Update the doc comment "keep it free of counter-specific assumptions" to say "game-specific".
    - In adapter.ts, update doc comments only: line 3's "D-15 counter placeholder in this phase" becomes "Phase 2's forehead-card toy"; the id doc's `"counter"` example becomes `"forehead-card"`; line ~61's "trivial counter game" becomes "toy game". The interface itself stays unchanged.
    - Leave the fixture strings adapterId "counter" in apps/worker/src/persistence.test.ts and scheduler.test.ts. They are opaque persisted-data fixtures, and the new Plan 03 persistence test deliberately uses "counter" to model a pre-swap room.
  </action>
  <verify>
    <automated>npx vitest run --project rules && npx tsc -p packages/rules/tsconfig.json --noEmit && npx tsc -p apps/worker/tsconfig.json --noEmit && npx tsc -p apps/web/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/rules/src/counter-game.ts` fails and `test -f packages/rules/src/counter-game.test.ts` fails
    - `grep -rnE "counterGame|CounterState|CounterAction|CounterView|CounterGame" apps packages e2e --include=*.ts --include=*.tsx | grep -v node_modules | grep -v "/.next/" | grep -v "/.wrangler/"` returns no matches
    - `grep -c '"counter-game.ts"' packages/rules/src/adapter.test.ts` outputs 0
    - `npx vitest run --project rules` exits 0
    - All tsc commands in verify exit 0
  </acceptance_criteria>
  <done>No counter code, export, component, or test remains; every package still type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-pushed RoomView -> browser DOM | The client renders only what it received; it holds no own-card identity to render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Information Disclosure | ForeheadCardGame own-card tile | mitigate | Tile renders no children and never reads a value from yourCard; e2e asserts the tile is empty and lacks the value a teammate sees |
| T-02-20 | Spoofing / Tampering | onGuess -> game_action payload | accept | Client sends only { type: "guess", value }; the server adapter's exact-key guard and turn check are authoritative (Plan 01), so a tampered client gains nothing |
| T-02-21 | Information Disclosure | end-of-game screen | mitigate | Ended state shows only score and the already-public revealed list; no reveal-all branch (RESEARCH Pitfall 4) |
</threat_model>

<verification>
- `npx playwright test e2e/start-game.spec.ts e2e/in-progress-arrival.spec.ts` exits 0
- `npx vitest run --project rules --project web` exits 0
- tsc for packages/rules, apps/worker, apps/web exits 0; `npm run build:web` exits 0
- Counter identifiers grep returns nothing
</verification>

<success_criteria>
- Two browsers can play the toy: each sees others' cards, not its own
- D-15 counter fully deleted
</success_criteria>

<output>
Create `.planning/phases/02-per-seat-redaction-contract/02-05-SUMMARY.md` when done
</output>
