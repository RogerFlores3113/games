---
phase: 02-per-seat-redaction-contract
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - apps/worker/src/source-structure.test.ts
  - apps/worker/src/room-do.ts
  - apps/worker/src/room-do.test.ts
autonomous: true
requirements: [HIDE-01, HIDE-02, HIDE-04]

must_haves:
  truths:
    - "D-08: exactly one method in the worker, RoomDO#send, calls connection.send; joined, state, refused, superseded, and error frames all pass through it"
    - "D-09: a Vitest structural source test over every non-test file in apps/worker/src (comment-stripped, string-literal-aware) fails the build unless .send( occurs once, encodeServerMessage( is called once, toSeatView( has one call site, toPlayerView( is called only inside toSeatView, and broadcast( occurs zero times"
    - "D-09 canary: the structural test's comment stripper and counter are themselves proven to count a deliberately doubled .send( fixture as 2 and to ignore .send( inside comments"
    - "D-10: join, live update, and reconnect all obtain their view from the single #viewFor -> projectSeatView path; no resume serializer exists"
    - "D-07: when projectSeatView returns null, RoomDO sends that connection an error frame with detail view_unavailable and no view, and continues serving other seats"
    - "D-11 layer 3: an integration test against a live wrangler dev worker captures every frame each of 3 seats receives across lobby join, game start, two live guesses, and a seat-token reconnect, and runs the shared leak checker over each raw frame"
  artifacts:
    - path: "apps/worker/src/source-structure.test.ts"
      provides: "D-09 structural chokepoint audit"
      contains: "broadcast"
    - path: "apps/worker/src/room-do.ts"
      provides: "#send single outbound chokepoint; #viewFor via projectSeatView"
      contains: "#send("
    - path: "apps/worker/src/room-do.test.ts"
      provides: "D-11 layer 3 live-wire leak test"
      contains: "checkSeatViewForLeaks"
  key_links:
    - from: "apps/worker/src/room-do.ts #viewFor"
      to: "apps/worker/src/seat-projection.ts projectSeatView"
      via: "sole call site"
      pattern: "projectSeatView\\("
    - from: "apps/worker/src/room-do.ts #send"
      to: "@games/schema encodeServerMessage"
      via: "connection.send(encodeServerMessage(frame))"
      pattern: "connection\\.send\\(encodeServerMessage\\("
    - from: "apps/worker/src/room-do.test.ts"
      to: "@games/rules checkSeatViewForLeaks"
      via: "same checker as layers 1 and 2"
      pattern: "checkSeatViewForLeaks\\("
---

<objective>
Consolidate every outbound frame in the Durable Object behind a single #send method, route #viewFor through the fail-closed projectSeatView gate, enforce the chokepoint with a build-failing structural source test, and prove on the real wire (D-11 layer 3) that no seat ever receives its own secret.

Purpose: HIDE-02 ("no path that bypasses it") is only credible if a second send path makes the build fail; HIDE-01/HIDE-04 are only credible if the real frames from a real worker are checked, including reconnect.
Output: source-structure.test.ts (written first, RED), refactored room-do.ts (GREEN), layer-3 integration test appended to room-do.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md
@.planning/phases/02-per-seat-redaction-contract/02-RESEARCH.md
@.planning/phases/02-per-seat-redaction-contract/02-03-SUMMARY.md
@apps/worker/src/room-do.ts
@apps/worker/src/seat-projection.ts

<interfaces>
From apps/worker/src/seat-projection.ts (Plan 03):
- type ProjectedRoomView = RoomView & { readonly [brand]: true }
- type OutboundFrame = non-view ServerMessage members | joined with view: ProjectedRoomView | state with view: ProjectedRoomView | error with optional detail ErrorDetail
- projectSeatView(room: RoomState, seatId: string): ProjectedRoomView | null
- validateGameView(view: RoomView): ProjectedRoomView | null

From @games/schema: encodeServerMessage(msg: ServerMessage): string; ErrorDetailSchema enum ["view_unavailable"]; SUPERSEDED_CLOSE_CODE; ROOM_ABANDONED_CLOSE_CODE.
From @games/rules: FOREHEAD_CARD_VALUES, checkSeatViewForLeaks({ view, serialized, secrets }): string[], type SeatSecrets.

Current apps/worker/src/room-do.ts send sites (all to be routed through #send):
- line 125 onMessage bad_request error
- line 142 not_seated error
- lines 149, 161, 172, 184 result.reason errors (set_variant, start_game, game_action, leave)
- line 329 #handleJoin already-seated bad_request
- line 342 refused
- line 359 superseded.send superseded
- lines 368-375 joined (view from #viewFor)
- line 401 #pushState state (view from #viewFor)
Current #viewFor (line 387) returns toSeatView(room, seatId). Comment lines 10-17, 381-386, 391-395 mention toSeatView( / connection.send / broadcast in prose.

Current apps/worker/src/room-do.test.ts harness helpers: spawnWrangler, waitForReady, killAndWait, roomUrl, openSocket(code), collectMessages(ws) -> { raw: string[]; parsed: Parsed[]; waitFor(predicate, timeoutMs); waitForClose }, send(ws, msg), mintRoomCode from ./seat-identity. The D-17 restart test is the LAST test in the describe block and replaces the global `child` process; add the new test BEFORE it.

Non-test source files in apps/worker/src today: index.ts, origin.ts, persistence.ts, room-do.ts, room-state.ts, scheduler.ts, seat-identity.ts, seat-naming.ts, plus Plan 03's game-registration.ts and seat-projection.ts. origin.ts and others may contain string literals with "//" (URLs), so a naive line-comment regex would corrupt the scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-09 structural chokepoint test (RED against current room-do.ts)</name>
  <files>apps/worker/src/source-structure.test.ts</files>
  <read_first>
    - apps/worker/src/room-do.ts
    - apps/worker/src/room-state.ts
    - apps/worker/src/seat-projection.ts
    - apps/worker/src/game-registration.ts
    - apps/worker/src/origin.ts and apps/worker/src/index.ts (string literals containing //)
    - .planning/phases/02-per-seat-redaction-contract/02-RESEARCH.md "Pitfall 3" and "Code Examples: Structural source test"
  </read_first>
  <behavior>
    - Helper canaries (run on inline fixture strings, not files):
      - stripComments of `const u = "http://x"; a.send(1)` still contains `a.send(1)`
      - stripComments of `// a.send(1)` and of `/* a.send(1) */` contains no `.send(`
      - stripComments of a template literal containing `//` preserves the code after it
      - countMatches(/\.send\(/g) on a two-send fixture returns 2
    - Real-source assertions over every *.ts file in apps/worker/src whose name does not end in .test.ts (recursive), after stripComments:
      - A1: total `\.send\(` matches === 1, all in room-do.ts, and room-do.ts contains `connection.send(encodeServerMessage(`
      - A2: total `\bencodeServerMessage\(` matches === 1 (room-do.ts)
      - A3: room-do.ts has exactly 1 method definition matching /^\s*#send\(/m and at least 8 `this.#send(` calls
      - A4: `toSeatView\(` matches minus `function toSeatView\(` matches === 1 total, located in seat-projection.ts
      - A5: `toPlayerView\(` total === 1, located in room-state.ts inside the text slice from `function toSeatView(` to the next `\nexport function ` (or end of file)
      - A6: `\bbroadcast\(` total === 0
      - A7: `projectSeatView\(` calls (excluding `function projectSeatView\(`) === 1, in room-do.ts; `validateGameView\(` calls (excluding its definition) === 1, in seat-projection.ts
      - A8 (D-10): room-do.ts has exactly 1 `#viewFor` method definition (/^\s*#viewFor\(/m), exactly 1 `type: "joined"` and exactly 1 `type: "state"` occurrence
      - A9 (D-06): `foreheadCardGame` and `@games/schema/games/` appear only in game-registration.ts
      - A10: the scanned file list includes room-do.ts, room-state.ts, seat-projection.ts, and game-registration.ts (guards against a silently empty scan)
  </behavior>
  <action>
    Create apps/worker/src/source-structure.test.ts. Its file header documents D-08/D-09/D-10/D-06, why comments must be stripped (RESEARCH.md Pitfall 3), and why exact counts are asserted: a too-high count means a bypass, a too-low count means the refactor silently dropped a path.
    Implement stripComments(source: string): string as a single left-to-right character scanner, NOT regexes. It tracks four states: code, single-quoted string, double-quoted string, and template literal. It handles backslash escapes inside literals. In code state, it removes from "//" to end of line and from "/*" through "*/", replacing removed characters with spaces but keeping newlines so line structure is preserved. It never treats "//" or "/*" inside a literal as a comment. Regex literals are not handled (none of the scanned files use regex literals containing // or /*); record that assumption in a comment, plus an A10-style guard.
    Implement countMatches(text: string, pattern: RegExp): number, which requires the global flag.
    Discover files with readdirSync(dir, { recursive: true }) under fileURLToPath(new URL("./", import.meta.url)), keeping names ending ".ts" and not ".test.ts". Implement every behavior bullet as its own it(...). Failure messages should name the file(s) and counts found. Use imports from "vitest", "node:fs", "node:url", "node:path" only.
    Run it now: the helper canaries and A6/A9/A10 should pass, and A1, A2, A3, A4, and A7 must FAIL against the current room-do.ts. Record the RED run output in the SUMMARY. Commit the failing test as the RED step (test(02-04): ...).
  </action>
  <verify>
    <automated>npx vitest run --project worker source-structure -t "helper" && ! npx vitest run --project worker source-structure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function stripComments" apps/worker/src/source-structure.test.ts` outputs 1
    - `grep -c "function countMatches" apps/worker/src/source-structure.test.ts` outputs 1
    - `grep -c "readdirSync" apps/worker/src/source-structure.test.ts` outputs at least 1
    - The helper-canary tests pass: `npx vitest run --project worker source-structure -t "helper"` exits 0 (name the canary describe block with the word "helper")
    - Before Task 2, the full file FAILS: `npx vitest run --project worker source-structure` exits non-zero, with A1 reporting more than 1 `.send(`
  </acceptance_criteria>
  <done>A build-failing structural audit exists, its helpers are canary-proven, and it currently reports the scattered send sites.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate RoomDO outbound frames behind #send and fail-closed #viewFor (GREEN)</name>
  <files>apps/worker/src/room-do.ts</files>
  <read_first>
    - apps/worker/src/room-do.ts (entire file)
    - apps/worker/src/seat-projection.ts
    - apps/worker/src/source-structure.test.ts (Task 1 — the exact counts to satisfy)
    - packages/schema/src/messages.ts (ErrorDetailSchema)
  </read_first>
  <action>
    Edit apps/worker/src/room-do.ts (D-08, D-07, D-10):
    - Imports. Remove toSeatView from the "./room-state" import. Add projectSeatView and type OutboundFrame and ProjectedRoomView from "./seat-projection". Keep encodeServerMessage imported from "@games/schema"; it will now have exactly one call.
    - Add a private method named #send taking (connection: Connection, frame: OutboundFrame) and returning void, whose body is exactly one statement: connection.send(encodeServerMessage(frame)). Give it a doc comment stating it is the ONLY place in the worker that writes to a socket (D-08), that joined/state frames can only carry a ProjectedRoomView obtained from #viewFor, and that source-structure.test.ts enforces this.
    - Change #viewFor(room: RoomState, seatId: string) to return ProjectedRoomView | null and to have exactly one statement: return projectSeatView(room, seatId).
    - Replace every existing send site (see interfaces list: lines 125, 142, 149, 161, 172, 184, 329, 342, 359, 368-375, 401) with this.#send(target, frameLiteral). For superseded, the target is the superseded connection: this.#send(superseded, { type: "superseded" }). Keep every close/setState call and ordering exactly as is (CR-01: setState(null) before send/close on superseded).
    - In #handleJoin, after await this.#commit(...), compute const view = this.#viewFor(result.state, result.seatId). If view is null, send { type: "error", code: "bad_request", detail: "view_unavailable" }; otherwise send the joined frame with seatId, seatToken, view. Then call #pushState() in both cases, so other seats still receive the seat change.
    - In #pushState, per connection, compute view via #viewFor. If null, send the same error frame to that connection and continue; otherwise send { type: "state", view }. Use exactly one `type: "state"` literal and one `type: "joined"` literal in the file (A8).
    - Rewrite the file header invariant list (lines 10-17) and the #viewFor / #pushState doc comments to describe the new chain: #send is the sole socket writer; #viewFor -> projectSeatView -> toSeatView -> adapter.toPlayerView -> strict schema is the sole view source; join, live update, and reconnect share it (D-10); the room-wide partyserver broadcast helper is never used. Comments may mention these names freely; the structural test strips comments.
    Do not add any new message type, resume path, or change to onConnect (D-10: first join and reconnect remain the same join handler).
  </action>
  <verify>
    <automated>npx vitest run --project worker source-structure && npx tsc -p apps/worker/tsconfig.json --noEmit && npm run build:worker</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project worker source-structure` exits 0 (all A1-A10 and helper canaries pass)
    - `grep -c "this.#send(" apps/worker/src/room-do.ts` outputs at least 8
    - `grep -c "return projectSeatView(room, seatId);" apps/worker/src/room-do.ts` outputs 1
    - `grep -c '"view_unavailable"' apps/worker/src/room-do.ts` outputs at least 1
    - `npx tsc -p apps/worker/tsconfig.json --noEmit` exits 0
    - `npm run build:worker` exits 0
    - Manual structural canary (do not commit): add a second `connection.send("x")` line anywhere in room-do.ts, confirm `npx vitest run --project worker source-structure` fails on A1, revert. Record in SUMMARY.
  </acceptance_criteria>
  <done>Every outbound frame flows through #send, every view through the fail-closed #viewFor, and the structural test is green.</done>
</task>

<task type="auto">
  <name>Task 3: D-11 layer 3 live-wire leak test (join, live update, reconnect)</name>
  <files>apps/worker/src/room-do.test.ts</files>
  <read_first>
    - apps/worker/src/room-do.test.ts (entire file: harness helpers, the CR-03 test, and the D-17 restart test which must stay last)
    - packages/rules/src/forehead-card-leak-check.ts (checker contract; SeatSecrets shape)
    - packages/rules/src/forehead-card.ts (FOREHEAD_CARD_VALUES, view shape)
    - .planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md D-11, D-12, D-14
  </read_first>
  <action>
    Add one test to room-do.test.ts's describe block, placed immediately BEFORE the D-17 restart test (which replaces the shared wrangler child and must remain last). Name it "HIDE-01 / HIDE-04 (D-11 layer 3): no seat's raw frames carry its own card, the undealt deck, across join, live update, and seat-token reconnect". Give it a 40_000 ms timeout.
    Import FOREHEAD_CARD_VALUES and checkSeatViewForLeaks from "@games/rules".
    Flow:
    1. Open three sockets and join them in order as "Alice", "Bob", "Cara", collecting with collectMessages and waiting for each joined frame (these lobby frames have game null). Keep each seat's seatId and seatToken.
    2. Alice (host) sends start_game; wait until all three have a state frame with view.status "in_progress".
    3. Find the socket whose latest in_progress view has game.isYourTurn true and send { type: "game_action", request: { type: "guess", value: FOREHEAD_CARD_VALUES[0] } }. Wait until all three sockets have a state frame with game.revealed length 1. Repeat for the next active seat with FOREHEAD_CARD_VALUES[1], waiting for revealed length 2.
    4. Reconnect: close Bob's socket, open a new socket, send { type: "join", displayName: "Bob", seatToken: bobToken }, and wait for its joined frame whose view.game is non-null. That frame is the reconnect capture and must be checked explicitly.
    Leak evaluation after all captures:
    - Build a frames list of { seatId, raw, parsed } from every collector's raw/parsed arrays (Bob's original and reconnect sockets both map to Bob's seatId).
    - Build cardValueById: for every parsed frame with a non-null view.game, record each otherCards[].card that has hidden false (id -> value) and each revealed[] entry (id -> value).
    - seenValues = the set of all values in cardValueById. forbiddenTokens = FOREHEAD_CARD_VALUES filtered to values not in seenValues; assert its length is at least 10 (non-vacuous; 3 dealt + 2 drawn + at most 2 revealed-only means at most 5-7 seen).
    - For each frame: if view.game is null, ownCard = null. Otherwise ownId = view.game.yourCard.id, ownValue = cardValueById.get(ownId), expect ownValue toBeDefined() (fail loudly rather than skip; with 3 seats every hand is visible to 2 other seats in the same push), and ownCard = { id: ownId, value: ownValue }.
    - Expect checkSeatViewForLeaks({ view: parsed, serialized: raw, secrets: { ownCard, forbiddenTokens } }) toEqual([]).
    - Also assert: at least 3 game-bearing frames were checked per seat, and the reconnect joined frame was among the checked frames.
    Add a comment that the seed (D-14) is not observable from the client side and is covered by layers 1 and 2; this layer checks own value and undealt deck on genuine workerd frames.
    Close all sockets at the end.
  </action>
  <verify>
    <automated>npx vitest run --project worker room-do</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-11 layer 3" apps/worker/src/room-do.test.ts` outputs at least 1
    - `grep -c "checkSeatViewForLeaks(" apps/worker/src/room-do.test.ts` outputs at least 1
    - `grep -n "D-11 layer 3" apps/worker/src/room-do.test.ts` line number is lower than `grep -n "D-17: persisted room state survives" apps/worker/src/room-do.test.ts` line number
    - `grep -c "toBeGreaterThanOrEqual(10)" apps/worker/src/room-do.test.ts` outputs at least 1
    - `npx vitest run --project worker room-do` exits 0 (all existing integration tests plus the new one)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Real frames from a live worker, including a reconnect, are proven free of each seat's own card and the undealt deck by the shared checker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable Object -> WebSocket (per connection) | Bytes leave the server; every frame must be per-seat projected and validated |
| future code changes -> send path | A later edit could add a second send or broadcast; the structural test is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Information Disclosure | secondary/forgotten send path in apps/worker/src | mitigate | Single #send; source-structure.test.ts asserts exact counts of .send(, encodeServerMessage(, toSeatView(, toPlayerView(, projectSeatView(, and zero broadcast( across all non-test worker files (D-08, D-09) |
| T-02-15 | Information Disclosure | structural test false negative via comment/string handling | mitigate | Literal-aware stripComments with canary fixtures; A10 guards against an empty file scan; manual doubled-send canary recorded |
| T-02-16 | Information Disclosure | validation failure branch | mitigate | #viewFor null -> error frame with closed detail view_unavailable and no view (D-07); other seats unaffected |
| T-02-17 | Information Disclosure | reconnect path serving a different serializer | mitigate | A8 asserts single #viewFor and single joined/state literals (D-10); layer-3 test checks the actual reconnect joined frame |
| T-02-18 | Denial of Service | a thrown error inside a send tearing down the room | accept | encodeServerMessage only throws on a server-side shape bug already prevented by OutboundFrame typing and projectSeatView validation; behavior unchanged from Phase 1 |
</threat_model>

<verification>
- `npx vitest run --project worker` exits 0 (source-structure, room-do integration including layer 3, seat-projection, redaction-wire, and all prior worker tests)
- `npm test` exits 0
- `npx tsc -p apps/worker/tsconfig.json --noEmit` and `npm run build:worker` exit 0
</verification>

<success_criteria>
- One socket writer (#send) and one view source (#viewFor -> projectSeatView) in the worker, enforced by a build-failing structural test
- Layer-3 live-wire leak test passes, including the reconnect frame
</success_criteria>

<output>
Create `.planning/phases/02-per-seat-redaction-contract/02-04-SUMMARY.md` when done
</output>
