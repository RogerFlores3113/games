---
phase: 07-variant-support-rainbow-black
plan: 09
type: execute
wave: 1
depends_on: ["07-07"]
gap_closure: true
files_modified:
  - packages/rules/src/hanabi/variant.ts
  - packages/rules/src/hanabi/variant.test.ts
  - packages/rules/src/hanabi/variant-matrix.test.ts
  - packages/rules/src/hanabi/legality.test.ts
  - packages/rules/src/hanabi/actions.test.ts
  - packages/rules/src/hanabi/clue-facts.test.ts
  - packages/rules/src/hanabi/nameable-colour.property.test.ts
  - packages/rules/src/index.ts
  - apps/worker/src/room-state.test.ts
  - apps/web/lib/hanabi-board-logic.ts
  - apps/web/lib/hanabi-board-logic.test.ts
  - apps/web/components/hanabi/TeammateCard.tsx
  - apps/web/components/hanabi/CluePopover.tsx
  - apps/web/lib/clue-popover-render.test.ts
  - e2e/variant-black.spec.ts
  - e2e/helpers.ts
autonomous: true
requirements: [RULES-03, RULES-14, UI-07]

must_haves:
  truths:
    - "In the Black variant, a colour clue naming \"black\" is refused by the engine and by the server with clue_color_not_nameable"
    - "No colour clue ever touches a Black tile, in any variant; rank clues still touch Black tiles"
    - "Rainbow is unchanged: every nameable colour touches it, it is never nameable, and its copy counts are untouched"
    - "In Black, a rainbow tile's popover shows exactly Red, Yellow, Green, Blue, White (no Black, no Rainbow)"
    - "A Black tile's popover shows only the number button, with no colour button and no colour row"
  artifacts:
    - path: "packages/rules/src/hanabi/variant.ts"
      provides: "per-suit SuitRule table with colorTouch (named / every / never); cluableColors and colorClueTouches derived from it"
      contains: "colorTouch"
    - path: "apps/web/lib/hanabi-board-logic.ts"
      provides: "colorClueOptionsFor(view, suit): the nameable colours that touch a tile of that suit"
      contains: "colorClueOptionsFor"
    - path: "e2e/variant-black.spec.ts"
      provides: "live proof: five-colour rainbow row, rank-only Black popover, colour clue never rings Black tiles"
  key_links:
    - from: "apps/web/components/hanabi/TeammateCard.tsx"
      to: "colorClueOptionsFor"
      via: "popover colour options derived from the variant's own touch predicate"
      pattern: "colorClueOptionsFor\\("
    - from: "packages/rules/src/hanabi/legality.ts canClue"
      to: "config.cluableColors"
      via: "unchanged nameable-colour check now refusing black"
      pattern: "cluableColors.includes"
---

<objective>
Close UAT gap 2 ("black is not a color that accepts hints. You cannot hint at the color black."): Black is never a nameable colour clue and no colour clue ever touches a Black tile. Only rank clues touch Black. This REVERSES plan 07-07's "Black is nameable / a Black clue touches Rainbow" behaviour, which the owner has said is wrong, and supersedes 07-CONTEXT.md D-08.

Purpose: the engine, server and popover must match the owner's rules for Black.
Output: a per-suit rule table in variant.ts (colour-touch kind), the Black tile popover reduced to the number button, the rainbow popover back to five colours, and every 07-07 test that asserted the old behaviour rewritten in the same task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
@.planning/phases/07-variant-support-rainbow-black/07-CONTEXT.md
@.planning/phases/07-variant-support-rainbow-black/07-07-SUMMARY.md

<executor_rules>
- One commit per task, with a normal `git commit` (hooks run; NEVER `--no-verify`). Stage files explicitly by path; never `git add -A` or `git add .`.
- NEVER run `git stash`, `git reset`, `git checkout <file>`, `git restore`, `git clean` or any other destructive git command.
- Never stage `.planning/config.json`, `apps/web/next-env.d.ts`, any `*.tsbuildinfo`, any `.gitkeep`, `.planning/debug/`, `apps/web/AGENTS.md`, `apps/web/CLAUDE.md`.
- No push, no deploy.
- Before ANY Playwright run, kill the orphaned dev-server trees on ports 3100 and 8787 by PID, walking each listener up to its process-group root, and never touching `/tmp/games-preexisting` (ports 3101/8788; confirm via `readlink /proc/<pid>/cwd` before any kill): `for port in 3100 8787; do for pid in $(ss -ltnpH "sport = :$port" | grep -o 'pid=[0-9]*' | cut -d= -f2 | sort -u); do case "$(readlink /proc/$pid/cwd)" in /tmp/games-preexisting*) continue;; esac; pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; done; sleep 2`, then confirm with `ss -ltnp` and `-KILL` survivors.
- After the plan's last task, restart both dev servers in the background exactly as `playwright.config.ts` does: web `NEXT_PUBLIC_WORKER_HOST=localhost:8787 NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS=1000 NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS=1000 npm run dev --workspace apps/web -- -p 3100`; worker (cwd `apps/worker`) `npx wrangler dev --port 8787 --var SOCKET_STALE_MS:5000 --var ZOMBIE_SWEEP_INTERVAL_MS:1000`. Confirm 3100 and 8787 listen.
- Binding owner decisions: Black keeps its silver colour and art, no variant label, no ruled-out clue information, no rank numerals on card faces.
</executor_rules>

<interfaces>
Current packages/rules/src/hanabi/variant.ts (to be restructured, exports must stay importable):
- `ALL_SUITS`, `Suit`, `RANKS`, `Rank`, `BASE_RANK_COUNTS`, `SINGLE_RANK_COUNTS`
- `interface VariantConfig { variant; suits; cluableColors; rankCountsFor(suit); colorClueTouches(suit, clueColor); rankClueTouches(rank, clueRank) }`
- module-private `rainbowAwareColorClueTouches`, `BASE_CONFIG`, `RAINBOW_CONFIG`, `BLACK_CONFIG` (BLACK_CLUABLE currently includes "black" -- WRONG)
- `variantConfig(variant)`, `handSizeFor(n)`, `maxScoreFor(config)`

packages/rules/src/hanabi/legality.ts:189 -- `if (clue.type === "color" && !config.cluableColors.includes(clue.value)) return { legal: false, reason: "clue_color_not_nameable" };` (unchanged; it starts refusing "black" once cluableColors drops it)

apps/web/components/hanabi/TeammateCard.tsx:108-116 -- `colorNameable = cluableColorsForView(game).includes(card.suit)`; `colorRow = !colorNameable ? cluableColorsForView(game) : null` (WRONG for Black: it would show a colour row on a Black tile)

apps/web/components/hanabi/CluePopover.tsx -- props `{ suit, rank, colorDisabled, rankDisabled, colorRow: readonly Suit[] | null, onGiveColor, onGiveRank }`; testids `tile-clue-popover`, `tile-clue-color`, `clue-color-row`, `tile-clue-color-{suit}`, `tile-clue-rank`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine and server -- Black is never nameable and never touched by a colour clue</name>
  <files>packages/rules/src/hanabi/variant.ts, packages/rules/src/index.ts, packages/rules/src/hanabi/variant.test.ts, packages/rules/src/hanabi/variant-matrix.test.ts, packages/rules/src/hanabi/legality.test.ts, packages/rules/src/hanabi/actions.test.ts, packages/rules/src/hanabi/clue-facts.test.ts, packages/rules/src/hanabi/nameable-colour.property.test.ts, apps/worker/src/room-state.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/variant.ts (whole file, including its header comment, which claims Black is nameable and must be rewritten)
    - packages/rules/src/hanabi/legality.ts (canClue, cardsTouchedByClue)
    - packages/rules/src/hanabi/clue-facts.ts (narrowing filters through colorClueTouches)
    - packages/rules/src/hanabi/variant.test.ts, variant-matrix.test.ts (lines ~85-110 assert colorClueTouches("black","black") true and ("rainbow","black") true), legality.test.ts (~165-200), actions.test.ts (~640-690), clue-facts.test.ts, nameable-colour.property.test.ts
    - apps/worker/src/room-state.test.ts (the existing clue_color_not_nameable case near line 988)
    - packages/rules/src/index.ts (public exports)
  </read_first>
  <behavior>
    - variantConfig("black").cluableColors deep-equals ["red","yellow","green","blue","white"]
    - variantConfig("black").colorClueTouches("black", c) is false for every c in ALL_SUITS; same for variantConfig("base") and ("rainbow")
    - variantConfig("black").colorClueTouches("rainbow", c) is true for every nameable c; ("rainbow","black") is false
    - variantConfig("black").rankClueTouches still touches a Black card of that rank (rank clues unchanged)
    - canClue and applyHanabiAction refuse {type:"color", value:"black"} in the Black variant with clue_color_not_nameable, even when the target holds Black tiles
    - applyClueToSlotFacts with a positive colour clue removes "black" from possibleSuits; a negative colour clue leaves "black" in possibleSuits and removes "rainbow"
    - The worker's room action path answers a Black-variant colour clue naming "black" with an action refusal whose detail is "clue_color_not_nameable"
  </behavior>
  <action>
    Per UAT gap 2 (owner, verbatim: "You cannot hint at the color black"), supersedes D-08 and 07-07. In variant.ts introduce an exported type `ColorTouch = "named" | "every" | "never"` ("named": the suit is itself a nameable colour and is touched only by a clue naming it; "every": touched by every nameable colour clue and never nameable, i.e. Rainbow; "never": touched by no colour clue and never nameable, i.e. Black), and an exported `SuitRule` interface holding `colorTouch` and `rankCounts`. Add one module-level frozen table `SUIT_RULES: Readonly<Record<Suit, SuitRule>>`: the five colours are named with BASE_RANK_COUNTS, rainbow is every with BASE_RANK_COUNTS, black is never with SINGLE_RANK_COUNTS (plan 07-10 changes Black's counts and adds direction; do not change counts here). Add `suitRule(suit): SuitRule` to the `VariantConfig` interface. Replace the three hand-written configs with one private builder `buildVariantConfig(variant, suits)` that derives `cluableColors` as the suits whose rule is "named" (in `suits` order), `rankCountsFor(suit)` from the table, and `colorClueTouches(suit, clueColor)` generically: named means suit === clueColor; every means cluableColors.includes(clueColor); never means false. Suit lists stay: base = five colours, rainbow = five + rainbow, black = five + rainbow + black. Delete `rainbowAwareColorClueTouches` and `BLACK_CLUABLE`; there must be no string comparison against the literal black suit anywhere in the logic of variant.ts. Rewrite the file header comment: remove the "Black is a normally color-cluable suit" resolved-question text and the "including a Black clue" wording, and state the owner's rule (2026-09-18) that Black is never nameable and never touched by colour. Export `ColorTouch` and `SuitRule` types from packages/rules/src/index.ts. legality.ts needs no logic change (its cluableColors check now refuses "black"); only fix any comment there that says black is nameable in Black. Update every test that asserted the old behaviour IN THIS TASK: variant.test.ts (drop the black-is-nameable and Black-clue-touches-rainbow cases, add the behaviour cases above), variant-matrix.test.ts lines ~100-110 (black never touched, rainbow not touched by "black"), legality.test.ts and actions.test.ts (add "black" to the non-nameable colours refused in the Black variant; the D-04 loops that iterate non-nameable colours must now include black for Black), clue-facts.test.ts (positive colour clue drops black; negative keeps black), nameable-colour.property.test.ts (the property must still hold and must generate "black" as an attempted colour in Black). In apps/worker/src/room-state.test.ts add a Black-variant case beside the existing clue_color_not_nameable test that sends a colour clue naming "black" at a seat holding a Black tile and expects the same refusal shape with detail "clue_color_not_nameable".
  </action>
  <verify>
    <automated>npx vitest run packages/rules apps/worker/src/room-state.test.ts && npx tsc -b packages/rules packages/schema apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "colorTouch" packages/rules/src/hanabi/variant.ts` shows the SuitRule field and the three table kinds "named", "every", "never"
    - `grep -nE "BLACK_CLUABLE|rainbowAwareColorClueTouches" packages/rules/src/hanabi/variant.ts` returns nothing
    - `grep -v '^\s*//' packages/rules/src/hanabi/variant.ts | grep -v '^\s*\*' | grep -cE '=== "black"|suit === .black.'` returns 0
    - `grep -rn 'colorClueTouches("black", "black")).toBe(true)\|colorClueTouches("rainbow", "black")).toBe(true)' packages/rules/src` returns nothing
    - `npx vitest run packages/rules apps/worker/src/room-state.test.ts` exits 0
    - `npx tsc -b packages/rules packages/schema apps/worker` exits 0
  </acceptance_criteria>
  <done>The engine and worker refuse a "black" colour clue with clue_color_not_nameable, no colour clue touches Black in any variant, Rainbow behaves exactly as in the Rainbow variant, and every test asserting 07-07's nameable-Black behaviour is rewritten. Committed as one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Popover -- five-colour rainbow row, number-only Black tile, live e2e proof</name>
  <files>apps/web/lib/hanabi-board-logic.ts, apps/web/lib/hanabi-board-logic.test.ts, apps/web/components/hanabi/TeammateCard.tsx, apps/web/components/hanabi/CluePopover.tsx, apps/web/lib/clue-popover-render.test.ts, e2e/variant-black.spec.ts, e2e/helpers.ts</files>
  <read_first>
    - apps/web/lib/hanabi-board-logic.ts (cluableColorsForView, clueTouchIdsForTarget, disabledReasonFor)
    - apps/web/lib/hanabi-board-logic.test.ts
    - apps/web/components/hanabi/TeammateCard.tsx (lines ~80-230: colour/rank gating and the CluePopover call)
    - apps/web/components/hanabi/CluePopover.tsx (whole file: single-button mode, row mode, edge-flip logic keyed on colorRow)
    - apps/web/lib/clue-popover-render.test.ts (six-colour Black row cases to rewrite)
    - e2e/variant-black.spec.ts (whole file: BLACK_ROW_COLORS, BLACK-POPOVER-FIT, the "Black clue rings rainbow and black tiles" test, the 65-card retry comment)
    - e2e/helpers.ts (openTileCluePopover and giveAnyLegalClue doc comments, lines ~90-170)
  </read_first>
  <behavior>
    - colorClueOptionsFor(view, "red") in base returns ["red"]; ("rainbow") in rainbow and in black returns the five colours in cluableColors order; ("black") in black returns []
    - CluePopover with no colour options renders tile-clue-rank and renders neither tile-clue-color nor clue-color-row
    - In Black, a rainbow tile's popover renders exactly five tile-clue-color-{suit} buttons: red, yellow, green, blue, white
    - A red/yellow/green/blue/white/rainbow tile's popover is otherwise unchanged from today
  </behavior>
  <action>
    Per UAT gap 2 and D-05/D-07 (rainbow row shares the rank button's disabled gate; no disabled-reason text). In hanabi-board-logic.ts add and export `colorClueOptionsFor(view: HanabiView, suit: Suit): readonly Suit[]`, returning `variantConfig(view.variant).cluableColors` filtered through `config.colorClueTouches(suit, colour)`. This is the single, variant-agnostic source of which colour buttons a tile offers: one option equal to the tile's own suit gives single-button mode, several give row mode, none gives no colour control. In TeammateCard.tsx replace the `colorNameable`/`colorRow` derivation with `colorClueOptionsFor(game, card.suit)` (hidden cards offer no options): `colorRow` is the options array when it has more than one entry, otherwise null; add a boolean `colorOffered` (options length greater than 0); `colorDisabled` for single-button mode keeps today's `disabledReasonFor` colour-clue check, and row mode keeps sharing `rankDisabled`. Rewrite the long comment block there so it describes the options rule and says Black offers no colour. In CluePopover.tsx add a prop `colorOffered: boolean`; when false, render no colour element at all (neither `tile-clue-color` nor `clue-color-row`), only the bold `tile-clue-rank` button, keeping the popover's existing centred positioning and styling; correct the prop doc comments that say "Black is nameable" and "six entries ending in Black". Update clue-popover-render.test.ts in this task: the Black rainbow row has five entries (no Black), and add a render case for colorOffered=false asserting only tile-clue-rank exists. Add colorClueOptionsFor cases to hanabi-board-logic.test.ts. In e2e/variant-black.spec.ts: BLACK_ROW_COLORS becomes the five colours; rewrite the test title and body so it proves (a) a rainbow tile's popover offers exactly the five colours and still fits on screen at the hand edges (BLACK-POPOVER-FIT keeps its edge-fit and in-viewport assertions, logging the new row width W), (b) clicking a teammate's Black tile opens a popover with `tile-clue-rank` and zero `tile-clue-color` / `clue-color-row` matches, and (c) giving a nameable colour from the rainbow tile's row rings the rainbow tile and the named suit's tiles on the receiver but never a Black tile (replacing the old "Black clue rings rainbow and black tiles" assertion). If the retry loop needs a Black tile visible as well as a rainbow tile, extend the retry condition rather than skipping the check. Fix the 65-card comment only if the retry maths is stated (the deck count changes in 07-10; say "Black deck" without a number). In e2e/helpers.ts update the openTileCluePopover/giveAnyLegalClue doc comments to name the third popover shape (Black tile: rank only); the count-guarded logic already falls back to the rank button, so no logic change is expected. Run the Black and Rainbow e2e specs after the port-kill in executor_rules.
  </action>
  <verify>
    <automated>npx vitest run apps/web && npx tsc -b apps/web && npx playwright test e2e/variant-black.spec.ts e2e/variant-rainbow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function colorClueOptionsFor" apps/web/lib/hanabi-board-logic.ts` matches
    - `grep -n "colorClueOptionsFor(" apps/web/components/hanabi/TeammateCard.tsx` matches
    - `grep -n "colorOffered" apps/web/components/hanabi/CluePopover.tsx` matches
    - `grep -n '"white", "black"' e2e/variant-black.spec.ts` returns nothing
    - `npx vitest run apps/web` exits 0 and `npx tsc -b apps/web` exits 0
    - `npx playwright test e2e/variant-black.spec.ts e2e/variant-rainbow.spec.ts` exits 0
  </acceptance_criteria>
  <done>A Black tile's popover shows only the number; a rainbow tile's popover in Black shows the five nameable colours; unit, render and live e2e tests prove it. Committed as one commit; dev servers restarted per executor_rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> worker WebSocket | a client can send any clue value, including a colour the UI never offers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | forged `{type:"color", value:"black"}` clue | mitigate | canClue's cluableColors check refuses it with clue_color_not_nameable; worker room-state test proves the refusal through the action path |
| T-07-09-02 | Information Disclosure | clue-fact narrowing | accept | narrowing only filters through the public touch predicate; no hidden-card identity is added to any view |
</threat_model>

<verification>
- `npx vitest run packages/rules apps/worker apps/web` exits 0
- `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
- `npx playwright test e2e/variant-black.spec.ts e2e/variant-rainbow.spec.ts` exits 0
</verification>

<success_criteria>
Black cannot be named as a colour clue and no colour clue touches it, on the server and in the UI; Rainbow is unchanged; all 07-07 assertions to the contrary are rewritten.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-09-SUMMARY.md` when done
</output>
