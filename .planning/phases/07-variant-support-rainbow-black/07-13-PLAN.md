---
phase: 07-variant-support-rainbow-black
plan: 13
type: execute
wave: 5
depends_on: ["07-09", "07-10", "07-11", "07-12"]
gap_closure: true
files_modified:
  - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [RULES-02, RULES-03, RULES-13, RULES-14, UI-07, BOARD-01, BOARD-05, UI-11]

must_haves:
  truths:
    - "The full gate is green from a clean port state (npm test, npx playwright test, npx tsc -b apps/web packages/rules packages/schema apps/worker) before the owner is asked anything"
    - "07-HUMAN-UAT.md records the round-2 gate results, the before/after discard tile measurements from 07-12, and a fresh how-to-verify checklist covering gaps 2, 3 and 4"
    - "Every earlier owner reply and every earlier gap/gate section in 07-HUMAN-UAT.md is preserved verbatim"
    - "REQUIREMENTS.md RULES-02 and RULES-13 wording matches the owner's reversed-Black rules"
    - "No owner reply is recorded by this plan and the phase is NOT marked complete"
  artifacts:
    - path: ".planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md"
      provides: "round-2 gate results + discard before/after + owner checklist"
      contains: "Gap closure 07-09..07-13"
  key_links:
    - from: "07-HUMAN-UAT.md round-2 gate section"
      to: "npm test / npx playwright test / npx tsc -b exit codes"
      via: "recorded command output"
      pattern: "playwright"
---

<objective>
Close the round-2 gap loop. Run the full automated gate from a clean dev-server state and record the results in 07-HUMAN-UAT.md, along with the discard tile before/after measurements and the design decision on stack representation. Then hand gaps 2, 3 and 4 back to the owner at a blocking checkpoint.

This plan SUPERSEDES 07-08-PLAN.md, which is still open at its checkpoint and asked the owner to verify the 07-07 behaviour ("Black is nameable", 65 tiles) that the owner has since rejected. Leave 07-08-PLAN.md untouched; its checkpoint is answered by the owner reply already recorded in 07-HUMAN-UAT.md, and this plan's checkpoint replaces it.

Purpose: only the owner can sign off the Black rules and the layout swap.
Output: an updated 07-HUMAN-UAT.md, a corrected REQUIREMENTS.md wording for RULES-02/RULES-13, and a waiting owner checkpoint. This plan does NOT record an owner reply and does NOT mark the phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
@.planning/phases/07-variant-support-rainbow-black/07-09-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-10-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-11-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-12-SUMMARY.md
@playwright.config.ts

<executor_rules>
- One commit per task, with a normal `git commit` (hooks run; NEVER `--no-verify`). Stage files explicitly by path; never `git add -A` or `git add .`.
- NEVER run `git stash`, `git reset`, `git checkout <file>`, `git restore`, `git clean` or any other destructive git command.
- Never stage `.planning/config.json`, `apps/web/next-env.d.ts`, any `*.tsbuildinfo`, any `.gitkeep`, `.planning/debug/`, `apps/web/AGENTS.md`, `apps/web/CLAUDE.md`.
- No push, no deploy. Do not edit 07-08-PLAN.md.
- Before ANY Playwright run, kill the orphaned dev-server trees on ports 3100 and 8787 by PID, walking each listener up to its process-group root, and never touching `/tmp/games-preexisting` (ports 3101/8788; confirm via `readlink /proc/<pid>/cwd` before any kill): `for port in 3100 8787; do for pid in $(ss -ltnpH "sport = :$port" | grep -o 'pid=[0-9]*' | cut -d= -f2 | sort -u); do case "$(readlink /proc/$pid/cwd)" in /tmp/games-preexisting*) continue;; esac; pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; done; sleep 2`, then confirm with `ss -ltnp` and `-KILL` survivors.
- After the gate, restart both dev servers in the background exactly as `playwright.config.ts` does: web `NEXT_PUBLIC_WORKER_HOST=localhost:8787 NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS=1000 NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS=1000 npm run dev --workspace apps/web -- -p 3100`; worker (cwd `apps/worker`) `npx wrangler dev --port 8787 --var SOCKET_STALE_MS:5000 --var ZOMBIE_SWEEP_INTERVAL_MS:1000`. Confirm 3100 and 8787 listen.
</executor_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate from a clean port state; write the round-2 section of 07-HUMAN-UAT.md; align RULES-02/RULES-13 wording</name>
  <files>.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md (whole file: keep every existing section verbatim, including all "Owner's verbatim reply" sections, "## Gaps", "## Gap closure 07-06..07-08 -- gate results" and "## Gaps (round 2)")
    - 07-09..07-12 SUMMARYs (test counts, the playedRanks design decision, the DISCARD_TILE_MEASURE before/after lines, any cap changes)
    - .planning/REQUIREMENTS.md (RULES-02 line ~44 still says "the Black suit instead holding a single copy of each rank"; RULES-13 line ~55 says "Completing a stack with a 5")
    - playwright.config.ts (dev-server env vars)
  </read_first>
  <action>
    Kill the orphaned dev-server trees per executor_rules, and record the PIDs, process-group roots and cwds killed plus the preexisting pair left alone. Run from the repo root, once each and in sequence: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`, and the no-hex guard `grep -rnE "#[0-9A-Fa-f]{6}\b" apps/web/components apps/web/lib --include=*.tsx --include=*.ts` (only the known pre-existing matches in note-box-render.test.ts, settings-modal-render.test.ts and tile-color.test.ts are allowed). If anything fails, fix the root cause in a separate commit (message prefix `fix(07-13):`) and re-run the whole gate; never weaken an assertion to go green. Restart the dev servers per executor_rules. Then APPEND to 07-HUMAN-UAT.md, after "## Gaps (round 2)", a section "## Gap closure 07-09..07-13 -- gate results" containing: the port-kill record; a results table (command, PASS/FAIL, counts, duration); a "Stack representation" paragraph stating the 07-10 design decision (engine and wire carry playedRanks in play order, direction lives in the variant's per-suit rule, the client renders slot i = playedRanks[i], ROOM_SCHEMA_VERSION 4); a "Discard tile size (1280x720)" table with the BEFORE and AFTER DISCARD_TILE_MEASURE values from 07-12 (tile, area, box, turn sign); a "Current status" block with the dev-server URLs; and a "Sign-off checklist for Black round 2" matching the how-to-verify list in Task 2. Update the frontmatter `updated:` timestamp and add the 07-09..07-13 plans to `source:`, leaving `status:` as pending. In REQUIREMENTS.md amend only the text of RULES-02 (Black holds three 5s, two each of 4/3/2 and one 1, "amended 2026-09-18 by the owner") and RULES-13 (completing a stack, meaning its last tile in play order: a 5, or a reversed Black stack's 1, regains a clue token unless at 8). Leave checkboxes and the traceability table unchanged.
  </action>
  <verify>
    <automated>grep -q "Gap closure 07-09..07-13" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md && grep -q "Owner's verbatim reply to the 7-suit Black checkpoint" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md && grep -q "## Gaps (round 2)" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md && grep -q "Sign-off checklist for Black round 2" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx playwright test` and `npx tsc -b apps/web packages/rules packages/schema apps/worker` each exited 0 in the final run recorded in 07-HUMAN-UAT.md
    - 07-HUMAN-UAT.md still contains "## Owner's verbatim reply (2026-09-18)", "Owner's verbatim reply to the 7-suit Black checkpoint", "## Gaps", "## Gap closure 07-06..07-08" and "## Gaps (round 2)"
    - 07-HUMAN-UAT.md contains "Gap closure 07-09..07-13", "playedRanks", and two DISCARD_TILE_MEASURE values (before and after)
    - `grep -n "status: pending" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md` matches
    - `grep -n "three 5s" .planning/REQUIREMENTS.md` matches on the RULES-02 line
    - `git status --porcelain -- .planning/phases/07-variant-support-rainbow-black/07-08-PLAN.md` prints nothing and 07-08-PLAN.md has no commit from this plan
    - 3100 and 8787 are listening again (`ss -ltnp`)
  </acceptance_criteria>
  <done>The gate is green and recorded, the measurements and the design decision are documented, the requirement wording matches the owner's rules, and the dev servers are up for the owner. Committed as one commit (docs(07-13)).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner verification of Black round 2 (not nameable, reversed, discard/turn-sign swap)</name>
  <files>.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md (Task 1 output)
  </read_first>
  <action>Present the checklist below to the owner and stop, waiting for the reply. This plan does not record the reply and does not mark the phase complete. The orchestrator or next session writes the reply verbatim to 07-HUMAN-UAT.md and turns any reported issue into a new numbered gap. This checkpoint supersedes the still-open 07-08 checkpoint.</action>
  <what-built>Black now follows your rules. "Black" is never a colour clue and no colour clue touches a Black tile, so only numbers do. Black is reversed: three 5s, two each of 4, 3 and 2, and one 1, played 5 down to 1. The Black deck is 70 tiles and the max score is still 35. Rainbow is unchanged. On the board, the discard pile has moved into the large area under the tokens and deck, with bigger tiles (before/after sizes are in 07-HUMAN-UAT.md), and the turn sign has moved into the small top-right spot. The full gate is green.</what-built>
  <how-to-verify>
    1. Open http://localhost:3100 in two or three browser windows at 1280x720 (five for the tightest board). Create a room, choose Black, seat everyone, start.
    2. The deck counter starts at 70 minus the dealt hands (for example "50 x" with five players). The Play area shows seven columns: red, yellow, green, blue, white, rainbow, black. Black is still silver with its double-ring burst, and there is no variant label.
    3. On your turn, click a teammate's Black tile. The popover shows only the number button, with no colour button.
    4. Click a teammate's rainbow tile. The popover shows Red, Yellow, Green, Blue and White (no Black, no Rainbow) with the number below. Give one of those colours: on the receiver's screen the rainbow tile and that colour's tiles ring, and no Black tile does.
    5. Play Black in reverse. A Black 5 is the first Black tile that plays successfully; a Black 1 (or any rank other than 5) on the empty Black column is a misplay. After the 5, only a 4 plays, and so on. The Black column fills from the top: 5 first, then 4 under it. The empty column stays blank.
    6. Play the Black 1 last to complete the column: it flashes like any completed stack, and you get a clue token back if you are below 8.
    7. Look at the right-hand side of the board. The turn sign ("Your turn!" in violet, or "{name}'s turn") is in the small spot to the right of the tokens. The discard pile fills the big area underneath with noticeably larger tiles. Discard several tiles: nothing on the board grows or moves, nothing scrolls, and dragging to rearrange, group-by-suit, the expand button and the drag hover glow all still work.
    8. Play the Black game to the end: the end overlay reads "/ 35". Regression: a Rainbow game has six columns and ends "/ 30"; a base game has five and ends "/ 25".
  </how-to-verify>
  <verify>
    <automated>grep -q "Sign-off checklist for Black round 2" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The checklist has been presented to the owner and execution is paused at this checkpoint
    - No owner reply has been written by this plan and the phase is not marked complete
  </acceptance_criteria>
  <done>The owner has the live board and the checklist in front of them, and the phase waits on their reply.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev servers | Owner verification runs against localhost only; no deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Tampering | forged "black" colour clue | mitigate | full gate re-runs the engine, worker and property tests added in 07-09 |
| T-07-13-02 | Information Disclosure | own-hand identity leak with the new playedRanks wire shape | mitigate | full gate re-runs the redaction and leak-check property tests over all variants |
| T-07-13-03 | Tampering | port kill hitting /tmp/games-preexisting | mitigate | cwd check via /proc/<pid>/cwd before any kill; ports 3101/8788 never targeted |
</threat_model>

<verification>
- Full gate green after a clean port kill; results, the design decision and discard before/after sizes recorded in 07-HUMAN-UAT.md
- Owner checkpoint presented; no reply recorded; phase not marked complete; 07-08-PLAN.md unchanged
</verification>

<success_criteria>
The owner is looking at a green, live build of gaps 2, 3 and 4 with a precise checklist, and the phase waits on their verdict.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-13-SUMMARY.md` when done
</output>
