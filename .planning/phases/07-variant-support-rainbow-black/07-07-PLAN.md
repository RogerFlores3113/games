---
phase: 07-variant-support-rainbow-black
plan: 07
type: execute
wave: 2
depends_on: ["07-06"]
gap_closure: true
files_modified:
  - packages/rules/src/hanabi/variant.ts
  - packages/rules/src/hanabi/variant.test.ts
  - packages/rules/src/hanabi/variant-matrix.test.ts
  - packages/rules/src/hanabi/deck.test.ts
  - packages/rules/src/hanabi/clue-facts.test.ts
  - packages/rules/src/hanabi/legality.test.ts
  - packages/rules/src/hanabi/endgame.ts
  - packages/rules/src/hanabi/termination.property.test.ts
  - apps/web/lib/hanabi-visual-logic.test.ts
  - apps/web/lib/hanabi-discard-logic.test.ts
  - apps/web/lib/hanabi-board-logic.test.ts
  - e2e/start-game.spec.ts
  - e2e/board-fit-black.spec.ts
  - apps/web/lib/clue-popover-render.test.ts
  - apps/web/components/hanabi/CluePopover.tsx
  - e2e/variant-black.spec.ts
autonomous: true
requirements: [RULES-02, RULES-03, RULES-14, UI-07]

must_haves:
  truths:
    - "A Black game deals 65 tiles over 7 suits: red, yellow, green, blue, white at 10 each, Rainbow at 10 (three 1s, two each of 2/3/4, one 5, unchanged from the Rainbow variant), Black at 5 (one per rank)"
    - "Black's max score is 35; the end overlay reads '/ 35'; scoreBand scales to 35"
    - "Inside Black, every nameable colour clue (red, yellow, green, blue, white AND black) touches Rainbow tiles; 'rainbow' is never nameable and is rejected with clue_color_not_nameable"
    - "A black tile's popover shows a single 'Black' button; a rainbow tile's popover in Black shows a row of six colours — Red, Yellow, Green, Blue, White, Black — each in its own hue, that stays fully on screen at a hand's edge"
    - "Giving 'Black' from a rainbow tile rings the receiver's rainbow tiles and black tiles in Black's colour"
    - "Base (5 suits, /25) and Rainbow (6 suits, /30) are unchanged"
  artifacts:
    - path: "packages/rules/src/hanabi/variant.ts"
      provides: "7-suit BLACK_CONFIG composed from the Rainbow rule, no special-casing outside variant.ts"
      contains: "\"rainbow\", \"black\""
    - path: "e2e/variant-black.spec.ts"
      provides: "Live 7-suit Black proof: six-colour row, Black touches Rainbow, popover fit at hand edges"
      contains: "tile-clue-color-black"
  key_links:
    - from: "packages/rules/src/hanabi/variant.ts BLACK_CONFIG.suits"
      to: "deck.ts buildDeck / endgame.ts maxScoreFor / Table.tsx stacks"
      via: "config.suits iteration"
      pattern: "config\\.suits"
    - from: "apps/web/lib/hanabi-board-logic.ts cluableColorsForView"
      to: "CluePopover colour row"
      via: "variantConfig(view.variant).cluableColors"
      pattern: "cluableColors"
---

<objective>
Close owner gap 1 in the rules engine and the UI: "black variant has 5 colors + rainbow + black. do not adjust number of rainbow tiles." Black becomes 7 suits, 65 tiles, max score 35. Inside Black, Rainbow keeps its Rainbow-variant rules (every colour clue touches it, it is never nameable) and Black stays its own nameable colour.

**Explicit rule decision:** a Black colour clue DOES touch Rainbow tiles. Under the Rainbow rule, every nameable colour touches rainbow, and in this variant Black is nameable. So `colorClueTouches("rainbow", "black")` is true in Black, and a positive Black clue narrows a card's candidate suits to [rainbow, black].

The engine stays parametrized, not special-cased (RULES-03: suit count comes from variant config). All the change lives in `BLACK_CONFIG` inside `variant.ts`. Deck construction, `maxScoreFor`, `scoreBand`, `checkHanabiGameEnd`, projection, worker and schema already iterate `config.suits` or accept all seven suits, so they need no code change. The scout confirmed that: the schema `SuitSchema` already lists all 7, there are no stack or deck bounds, and the worker has no suit list.

Purpose: the owner's Black variant, correct end to end, including the rainbow tile's popover.
Output: 7-suit BLACK_CONFIG, every test that asserted 6 suits or /30 for Black updated in the same task, a live Black e2e, and after-change board measurements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
@.planning/phases/07-variant-support-rainbow-black/07-CONTEXT.md
@.planning/phases/07-variant-support-rainbow-black/07-UI-SPEC.md
@.planning/phases/07-variant-support-rainbow-black/07-06-SUMMARY.md
@packages/rules/src/hanabi/variant.ts
@e2e/variant-rainbow.spec.ts

<interfaces>
From packages/rules/src/hanabi/variant.ts (current):
- ALL_SUITS = ["red","yellow","green","blue","white","rainbow","black"]; BASE_SUITS = the first five
- BASE_RANK_COUNTS {1:3,2:2,3:2,4:2,5:1}; SINGLE_RANK_COUNTS {1:1,...,5:1}
- RAINBOW_CONFIG: suits [...BASE_SUITS,"rainbow"], cluableColors BASE_SUITS, rankCountsFor -> BASE_RANK_COUNTS, colorClueTouches(suit, c) = suit === "rainbow" ? true : suit === c
- BLACK_CONFIG (to change): suits [...BASE_SUITS,"black"], cluableColors = BLACK_SUITS, rankCountsFor(suit) = suit === "black" ? SINGLE : BASE, colorClueTouches = exact match
- maxScoreFor(config) = config.suits.length * 5

Tests currently asserting the OLD Black shape (update in Task 1):
- packages/rules/src/hanabi/variant.test.ts: "black variant has 6 suits" (length 6), "black variant's cluableColors has 6 entries", "maxScoreFor: base 25, rainbow 30, black 30"
- packages/rules/src/hanabi/variant-matrix.test.ts: EXPECTED_SUIT_COUNT black 6, EXPECTED_DECK_SIZE black 55, EXPECTED_MAX_SCORE black 30; the black block near line 98
- packages/rules/src/hanabi/deck.test.ts: EXPECTED_TOTALS black 55, title "(50 / 60 / 55)", "the other five suits contribute 10 cards each"
- packages/rules/src/hanabi/clue-facts.test.ts ~line 77: "black variant, positive black color clue narrows to exactly black" expects ["black"] (now ["rainbow","black"])
- packages/rules/src/hanabi/termination.property.test.ts comment "(Rainbow/Black, 55-60 cards)"; endgame.ts header comment "Rainbow/Black's 30-point maximum"
- apps/web/lib/hanabi-visual-logic.test.ts ~line 260: "uses 30 as the black variant's max score"
- apps/web/lib/hanabi-discard-logic.test.ts ~line 83: black group order ends "white","black" (now "white","rainbow","black")
- apps/web/lib/hanabi-board-logic.test.ts ~line 341: cluableColorsForView black contains black
- e2e/start-game.spec.ts ~lines 133-137: "Black variant has 6 suits" + `toHaveCount(6)`; ~line 294 UI10_VARIANTS black { maxScore: 30, columns: 6 }
- e2e/board-fit-black.spec.ts: EXPECTED_BLACK_COLUMNS = 6 (from 07-06)
Already correct, re-run only: legality.test.ts nameable loop, nameable-colour.property.test.ts, actions.test.ts D-04 loop, apps/worker/src/room-state.test.ts ~line 969 ({variant:"black", value:"rainbow"} rejected), redaction/conservation/projection property tests.

UI (from 07-03): TeammateCard decides row vs single button with `cluableColorsForView(game).includes(card.suit)`; CluePopover row = `data-testid="clue-color-row"`, entries `tile-clue-color-{value}`, aria-label "Give a {Label} clue", `width: max-content`, flex-nowrap, horizontal anchor flips (left:0 / right:0) when the centered box would leave the viewport. A single button is `data-testid="tile-clue-color"`. Once BLACK_CONFIG.cluableColors = [...BASE_SUITS,"black"] and "rainbow" is absent, a rainbow tile in Black automatically gets a six-entry row and a black tile keeps its single button, with no UI code change.

e2e/variant-rainbow.spec.ts: `startRainbowGameWithVisibleRainbowTile` retry pattern (lines 40-115), `resolveSuitRgb(page, suit)` DOM-probe colour resolver, ring check via `hint-color-ring` boxShadow and `data-hints="true"`, `ownHandCardIds`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Black 7 suits in BLACK_CONFIG and update every 6-suit / 30-point Black assertion in the same change</name>
  <files>packages/rules/src/hanabi/variant.ts, packages/rules/src/hanabi/variant.test.ts, packages/rules/src/hanabi/variant-matrix.test.ts, packages/rules/src/hanabi/deck.test.ts, packages/rules/src/hanabi/clue-facts.test.ts, packages/rules/src/hanabi/legality.test.ts, packages/rules/src/hanabi/endgame.ts, packages/rules/src/hanabi/termination.property.test.ts, apps/web/lib/hanabi-visual-logic.test.ts, apps/web/lib/hanabi-discard-logic.test.ts, apps/web/lib/hanabi-board-logic.test.ts, e2e/start-game.spec.ts, e2e/board-fit-black.spec.ts</files>
  <read_first>
    - packages/rules/src/hanabi/variant.ts (whole file, including the header's "resolved open question" and Pitfall 3 comments)
    - packages/rules/src/hanabi/variant.test.ts, variant-matrix.test.ts, deck.test.ts (lines 1-60), clue-facts.test.ts (lines 60-90), legality.test.ts (lines 160-235)
    - apps/web/lib/hanabi-visual-logic.test.ts (~line 255-265), hanabi-discard-logic.test.ts (~line 70-95), hanabi-board-logic.test.ts (~line 330-350)
    - e2e/start-game.spec.ts (lines 125-160 and 285-350), e2e/helpers.ts `playUntilGameEnds` (its iteration cap semantics)
    - e2e/board-fit-black.spec.ts (from 07-06)
  </read_first>
  <behavior>
    - variantConfig("black").suits equals ["red","yellow","green","blue","white","rainbow","black"] (length 7)
    - variantConfig("black").cluableColors equals ["red","yellow","green","blue","white","black"] (no "rainbow")
    - In Black, colorClueTouches("rainbow", c) is true for every c in cluableColors, including "black"; colorClueTouches("black","black") true; colorClueTouches("black","red") false; colorClueTouches("red","black") false
    - In Black, rankCountsFor("rainbow") is BASE_RANK_COUNTS and rankCountsFor("black") is SINGLE_RANK_COUNTS
    - buildDeck(variantConfig("black")).length is 65; 10 rainbow cards (three 1s, two 2s/3s/4s, one 5); 5 black cards (one per rank)
    - maxScoreFor(variantConfig("black")) is 35; base 25 and rainbow 30 unchanged
    - Black: a positive black clue narrows possibleSuits to ["rainbow","black"]; a negative black clue removes both rainbow and black
    - Black: canClue naming "black" against a hand whose only candidate is a rainbow card is legal and touches that rainbow card; naming "rainbow" is rejected with clue_color_not_nameable
    - web: endReasonForView for black at score 35 is all_stacks_complete and at score 30 is null; groupDiscardsBySuit([], "black") orders ...,"white","rainbow","black"; cluableColorsForView(black) contains "black" and not "rainbow"
  </behavior>
  <action>
    RED first: update the rules and web unit tests listed below to the new 7-suit expectations, run `npx vitest run --project rules --project web`, and confirm they fail against the old config. Then change the config (GREEN).

    In `packages/rules/src/hanabi/variant.ts` (per the owner gap in 07-HUMAN-UAT.md), redefine Black. Set `BLACK_SUITS = [...BASE_SUITS, "rainbow", "black"]`, which is ALL_SUITS order. Set `cluableColors` to a new `BLACK_CLUABLE = [...BASE_SUITS, "black"]`. Set `rankCountsFor(suit)` to `suit === "black" ? SINGLE_RANK_COUNTS : BASE_RANK_COUNTS`, so Rainbow keeps full 10-card counts ("do not adjust number of rainbow tiles"). Set `colorClueTouches(suit, clueColor)` to `suit === "rainbow" ? true : suit === clueColor`, the same rule as Rainbow. Hoist that shared predicate into one module-level `rainbowAwareColorClueTouches` function used by both RAINBOW_CONFIG and BLACK_CONFIG, rather than duplicating it. Update the file header and BLACK_CONFIG comments to say: Black is 7 suits (five colours + Rainbow + Black), per the owner's decision of 2026-09-18, which supersedes the old 6-suit Black. Inside Black, Rainbow follows the Rainbow rule, and because Black is nameable, a Black clue touches Rainbow tiles. Rainbow is never nameable. Do not add any `variant === "black"` branch anywhere outside variant.ts. `maxScoreFor`, `buildDeck` and `checkHanabiGameEnd` already derive everything from `config.suits`. Update the endgame.ts header comment's "Rainbow/Black's 30-point maximum" to "Rainbow's 30 / Black's 35-point maximum" (comment only). Update the termination.property.test.ts comment to "(Rainbow/Black, 60-65 cards)". Keep MAX_SIMULATED_TURNS = 500, which is still ample.

    Rules tests:
    - variant.test.ts: black has 7 suits, including rainbow and black. cluableColors has 6 entries, includes black, and excludes rainbow. Add "black: every nameable colour, including black, touches rainbow" and "black: rankCountsFor(rainbow) is BASE_RANK_COUNTS". The maxScoreFor title and assertion become black 35.
    - variant-matrix.test.ts: EXPECTED_SUIT_COUNT black 7, EXPECTED_DECK_SIZE black 65, EXPECTED_MAX_SCORE black 35. In the black block, add that config.cluableColors excludes rainbow, colorClueTouches("rainbow","black") is true, and the deck holds 10 rainbow cards.
    - deck.test.ts: EXPECTED_TOTALS black 65 and the title "(50 / 60 / 65)". Retitle "the other five suits contribute 10 cards each" to "every non-black suit (five colours + rainbow) contributes 10 cards", and assert the rainbow split 3/2/2/2/1 explicitly.
    - clue-facts.test.ts: the positive-black test becomes "narrows to rainbow or black" with `["rainbow","black"]`. Add a negative-black case that excludes both.
    - legality.test.ts: add a Black case where a hand of [rainbow 2, red 3] given "black" is legal and touches exactly the rainbow card.

    Web tests:
    - hanabi-visual-logic.test.ts: black all_stacks_complete at 35, plus a case that 30 in black is NOT an end.
    - hanabi-discard-logic.test.ts: black order ends "white","rainbow","black".
    - hanabi-board-logic.test.ts: black cluableColorsForView contains black and not rainbow, with length 6.

    E2E, in this same task, because the engine change alters them:
    - e2e/start-game.spec.ts: the comment near line 133 becomes "Black variant has 7 suits (5 colours + Rainbow + Black)" and `toHaveCount(6)` becomes 7. UI10_VARIANTS black becomes `{ maxScore: 35, columns: 7 }`, still with no per-variant branch. Read `playUntilGameEnds`: if its cap (80) can be exhausted by a 65-tile two-player game (55 cards left to draw, plus clues and the final round), raise the shared cap for all rows uniformly (e.g. 120) and the test timeout if needed, then note it in the SUMMARY.
    - e2e/board-fit-black.spec.ts: `EXPECTED_BLACK_COLUMNS = 7`.

    Run `npx vitest run --project rules --project web --project worker --project schema` and `npx tsc -b apps/web packages/rules packages/schema apps/worker`. Grep for leftovers: `grep -rnE "black.{0,40}(\b30\b|\b55\b|6 suits|six)" packages apps/web/lib apps/web/components apps/worker/src e2e --include=*.ts --include=*.tsx`. Fix any remaining Black-specific 6-suit/30/55 assertion or comment it finds. Leave Rainbow's own 6/30/60 alone.

    Kill the 3100/8787 dev-server trees by PID, walking each listener up to its process-group root and never touching `/tmp/games-preexisting` on 3101/8788: `for port in 3100 8787; do for pid in $(ss -ltnpH "sport = :$port" | grep -o 'pid=[0-9]*' | cut -d= -f2 | sort -u); do case "$(readlink /proc/$pid/cwd)" in /tmp/games-preexisting*) continue;; esac; pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; done; sleep 2`, then confirm the ports are empty with `ss -ltnp`. This matters doubly here, because `reuseExistingServer` would otherwise reuse a worker running the OLD rules. Then run `npx playwright test e2e/board-fit-black.spec.ts e2e/start-game.spec.ts` (the full file, including UI-10 x3, both UI-11 tests with the gap-11 board-zoom scale-up, and "UAT gap 1 fixed geometry"). Record the board-fit JSON in the SUMMARY under "After (7-column Black rendered)", with a before/after table against the 07-06 baseline: Play region width, the 7 column boxes, slot size (must still be 50x65), tableau width, free horizontal width, and scroll sizes at 1280 and 1024. STOP condition (owner decision "just widen the board a bit to make space for black in that format"): if the tile size changed, any column is clipped, the tableau passes 1280, or the page scrolls at 1280x720, STOP. Do not commit this task, do not shrink anything and do not use any fallback. Report tile size, each region width, board width and overflow px for the owner.

    Commit once, with normal hooks and no `--no-verify`: `feat(07-07): make Black a 7-suit variant (5 colours + Rainbow + Black), max score 35`. Stage only this task's files. Never stage `.planning/config.json`, `apps/web/next-env.d.ts`, `*.tsbuildinfo`, `.gitkeep`, `.planning/debug/`, `apps/web/AGENTS.md` or `apps/web/CLAUDE.md`. Never run `git stash`, `git reset`, `git checkout <file>` or any other destructive git command. No push, no deploy.
  </action>
  <verify>
    <automated>npx vitest run --project rules --project web --project worker --project schema && npx tsc -b apps/web packages/rules packages/schema apps/worker && npx playwright test e2e/board-fit-black.spec.ts e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"rainbow", "black"\]' packages/rules/src/hanabi/variant.ts` (or equivalent BLACK_SUITS literal containing both) matches; `grep -c 'variant === "black"' packages/rules/src/hanabi/*.ts apps/web/lib/*.ts apps/web/components/hanabi/*.tsx | grep -v test` shows no new production branch
    - variant.test.ts asserts black suits length 7 and maxScoreFor black 35; deck.test.ts EXPECTED_TOTALS has `black: 65`; variant-matrix.test.ts has `black: 7`, `black: 65`, `black: 35`
    - clue-facts.test.ts expects `["rainbow", "black"]` for a positive black clue in Black
    - e2e/start-game.spec.ts has `{ variant: "black" as const, maxScore: 35, columns: 7 }` and no `toHaveCount(6)` in the Black lobby test; e2e/board-fit-black.spec.ts has `EXPECTED_BLACK_COLUMNS = 7`
    - All four vitest projects, tsc, board-fit, UI-10 (base/rainbow/black), UI-11 and "UAT gap 1 fixed geometry" pass
    - SUMMARY has the after-change board measurements with slot size unchanged at 50x65
  </acceptance_criteria>
  <done>Black deals 65 tiles across 7 suits with max 35, Rainbow inside Black follows the Rainbow rule (a Black clue touches it), every old Black assertion is updated, and the 7-column board is measured and green.</done>
</task>

<task type="auto">
  <name>Task 2: Six-colour row on a rainbow tile in Black — render test, live e2e (Black touches Rainbow, popover fits at hand edges)</name>
  <files>apps/web/lib/clue-popover-render.test.ts, apps/web/components/hanabi/CluePopover.tsx, e2e/variant-black.spec.ts</files>
  <read_first>
    - apps/web/lib/clue-popover-render.test.ts (the rainbow-row test above line 110 and the "black view, black card" test ~line 122; renderPopover/baseView/card helpers)
    - apps/web/components/hanabi/CluePopover.tsx (doc comments lines 10-60 that say Black never triggers the row; anchor-flip logic lines 60-110)
    - apps/web/components/hanabi/TeammateCard.tsx (colorNameable / row decision)
    - e2e/variant-rainbow.spec.ts (whole file: retry helper, resolveSuitRgb, ring assertions, popover in-viewport check)
    - e2e/helpers.ts (startGameWithPlayers, OTHER_HAND_SELECTOR, ownHandCardIds, openTileCluePopover)
  </read_first>
  <action>
    Render test in `apps/web/lib/clue-popover-render.test.ts`: add "black view, rainbow card: a six-entry colour row ending in Black, no Rainbow entry (07-07 gap)". It uses `baseView({ variant: "black" })` and `card({ suit: "rainbow", rank: 2 })`. Assert `clue-color-row` is present and the singular `tile-clue-color` is absent. Assert the entries `tile-clue-color-red|yellow|green|blue|white|black` appear in that order (index comparison, like the existing Rainbow-row test), with `aria-label="Give a Black clue"` present and no `tile-clue-color-rainbow`. The rank button still follows the row. Keep the existing "black view, black card: single Black button" test unchanged. It must still pass, because a black tile's suit is nameable.

    `CluePopover.tsx` needs doc comments only, with no behaviour change. Correct the statements that Black never triggers the row. The row appears for any tile whose suit is not in the variant's `cluableColors`, which means rainbow tiles in both Rainbow and Black. In Black the row has six entries, including Black, and a black tile keeps the single button. If `TeammateCard.tsx`'s comment near line 89 says the same thing, leave that file alone unless the claim is now false. It is not in files_modified, so just note it in the SUMMARY.

    Create `e2e/variant-black.spec.ts` with `test.use({ viewport: { width: 1280, height: 720 } })`. Add a local helper `startBlackGameWithVisibleRainbowTile(hostPage, browser)` modelled on `startRainbowGameWithVisibleRainbowTile`. It starts a **5-seat** Black game (["Roger","Bianca","Chen","Dara","Eli"]; 4 teammate hands of 4 tiles give about a 93% hit rate per attempt with 10 rainbow tiles in 65) and retries up to 6 attempts until the single active page sees a teammate rainbow tile. It returns pages, contexts, the active page, the rainbow card id, its seat id and the receiver page. Use no seed or deck override (WR-07 / T-07-02). Copy the `resolveSuitRgb` DOM-probe approach. Do not import from variant-rainbow.spec.ts, and do not refactor that file.

    Test A, "Black: a rainbow tile offers six nameable colours ending in Black, fits on screen at the hand edges, and a Black clue rings rainbow and black tiles":
    (1) Record the `tableau` box. On the active page, open every visible teammate rainbow tile's popover in turn. For each, assert the row's entries are exactly red, yellow, green, blue, white, black in order, that each entry's computed `color` equals `resolveSuitRgb` for its suit, that there is no `tile-clue-color-rainbow` and no menuitem matching /rainbow/i, and that the popover box lies within 0..1280 x 0..720. Close it (Escape or an outside click, as the Rainbow spec does).
    (2) Prove the edge fit arithmetically too, since the rainbow tile may not sit at an edge. Record the row popover's measured width W. Measure the leftmost and rightmost teammate tiles across all `OTHER_HAND_SELECTOR` hands. Assert `W <= 1280 - leftmostTile.x` (left-anchored flip fits) and `W <= rightmostTile.x + rightmostTile.width` (right-anchored flip fits). Log W and both edges with the prefix `BLACK-POPOVER-FIT `.
    (3) Assert the `tableau` box is identical (within 0.5px) after opening and closing the popovers, which proves fixed geometry.
    (4) Open a black teammate tile's popover if one is visible and assert it has the single `tile-clue-color` labelled Black and no row. If none is visible, rely on the render test and annotate the skip.
    (5) Re-open the rainbow tile's popover and click `tile-clue-color-black`. On the receiver page, poll until every own-hand card whose suit is rainbow or black has `data-hints="true"` and a `hint-color-ring` whose boxShadow contains Black's resolved rgb, and every other card has no ring. Map the receiver's own card ids to suits using the giver page's view of that seat's tiles (`[data-glyph]`), exactly as the Rainbow spec does. This is the live proof that a Black clue touches Rainbow.

    Test B, "Black: 7 columns and the discard overlay fit": on a fresh 5-seat Black game, assert 7 `played-stack-{suit}` columns in order red, yellow, green, blue, white, rainbow, black. Open the discard overlay (`discard-toggle`), assert it is within the viewport with no page scroll, then close it.

    Kill the 3100/8787 trees exactly as in Task 1, skipping `/tmp/games-preexisting`. Run `npx playwright test e2e/variant-black.spec.ts --repeat-each=2` and `npx playwright test e2e/variant-rainbow.spec.ts` (the Rainbow regression). Record the BLACK-POPOVER-FIT numbers in the SUMMARY. If the six-entry row does not fit at an edge, widen nothing and shrink nothing; report it. The fallback within 07-CONTEXT's discretion is the existing anchor clamp, never text below `--text-label`. After the final Playwright run, restart both dev servers in the background with playwright.config.ts's env: web `NEXT_PUBLIC_WORKER_HOST=localhost:8787 NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS=1000 NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS=1000 npm run dev --workspace apps/web -- -p 3100`; worker (cwd apps/worker) `npx wrangler dev --port 8787 --var SOCKET_STALE_MS:5000 --var ZOMBIE_SWEEP_INTERVAL_MS:1000`. Confirm both listen.

    Commit once, with normal hooks and no `--no-verify`: `test(07-07): prove the six-colour rainbow row and Black-touches-Rainbow in the 7-suit Black variant`. Stage only this task's three files. The same forbidden-staging and no-destructive-git rules apply. No push, no deploy.
  </action>
  <verify>
    <automated>npx vitest run --project web apps/web/lib/clue-popover-render.test.ts && npx tsc -b apps/web && npx playwright test e2e/variant-black.spec.ts e2e/variant-rainbow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - clue-popover-render.test.ts contains a Black+rainbow-card test asserting `tile-clue-color-black` inside `clue-color-row` and no `tile-clue-color-rainbow`
    - `e2e/variant-black.spec.ts` exists, contains `tile-clue-color-black`, `BLACK-POPOVER-FIT`, `hint-color-ring`, and no `seed`/deck-override mechanism (`grep -niE "DEV_FORCE|deck override|seed:" e2e/variant-black.spec.ts` returns nothing but comments)
    - `npx playwright test e2e/variant-black.spec.ts --repeat-each=2` passes; variant-rainbow.spec.ts still passes
    - SUMMARY records popover width W and the leftmost/rightmost tile edges
  </acceptance_criteria>
  <done>In Black, a rainbow tile offers Red/Yellow/Green/Blue/White/Black, the popover fits at the hand edges without moving the board, a black tile keeps its single Black button, and a Black clue visibly rings rainbow and black tiles on the receiver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> worker (WebSocket action frames) | A forged colour clue crosses here; legality is decided server-side from variantConfig |
| worker -> client (per-seat views) | 7-suit Black state is projected per seat; own-hand identities must never leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | forged `{type:"color", value:"rainbow"}` clue in Black | mitigate | BLACK cluableColors excludes rainbow; canClue's clue_color_not_nameable check (07-01) plus existing worker test (room-state.test.ts black/rainbow case) and nameable-colour property test re-run in Task 1 |
| T-07-07-02 | Information Disclosure | projection of 7-suit Black hands | mitigate | redaction/projection/leak-check property tests iterate variantConfig suits and re-run green in Task 1's vitest run |
| T-07-07-03 | Information Disclosure | seed/deck override for the e2e | mitigate | retry-based helper only; grep acceptance check forbids any override |
| T-07-07-04 | Denial of Service | in-flight 6-suit Black games persisted in a Durable Object before this change would compute max 35 and never reach all_stacks_complete | accept | no deploy in this gap closure; local dev only; such games still end on fuses or final round |
</threat_model>

<verification>
- All four vitest projects and the four-project tsc are green
- e2e: board-fit, start-game (UI-10 x3, UI-11, gap 1), variant-black, variant-rainbow all green after a clean port kill
- Before/after board and popover measurements recorded in the SUMMARY
</verification>

<success_criteria>
The Black variant plays as the owner specified: 7 suits, 65 tiles, max 35. Rainbow follows its own rules inside Black, a Black clue touches Rainbow, the popover row shows six colours and fits, and base and Rainbow are unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-07-SUMMARY.md` when done
</output>
