---
phase: 07-variant-support-rainbow-black
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/components/hanabi/CluePopover.tsx
  - apps/web/components/hanabi/TeammateCard.tsx
  - apps/web/lib/clue-popover-render.test.ts
  - e2e/helpers.ts
  - e2e/hanabi-table-polish.spec.ts
  - e2e/hanabi-realtime.spec.ts
autonomous: true
requirements: [RULES-14]

must_haves:
  truths:
    - "D-05: clicking a teammate tile whose suit is not in cluableColorsForView(game) opens the quick-clue popover with a compact row of the variant's nameable colours (red, yellow, green, blue, white in cluableColors order), each label in its own --color-suit-* hue, with the bold number button unchanged below; one click sends an ordinary colour clue for that colour"
    - "D-05: detection is variant-agnostic (cluableColorsForView(game).includes(card.suit)), never a suit === 'rainbow' literal"
    - "D-06: non-rainbow tiles keep exactly today's single colour button"
    - "D-07: every row entry shares the rank button's disabledReasonFor gate; no entry is individually disabled; no disabled-reason text"
    - "D-08: a black tile in Black shows a single 'Black' colour button"
    - "D-09/D-10: hintDisplayFor, HintIndicator and cluePulseColorFor are untouched"
    - "No 'Rainbow' colour option is ever rendered in any popover"
    - "The popover stays an absolute overlay inside the data-clue-tile wrapper, adds no flow height, and flips its horizontal anchor so the wider row never leaves the viewport at hand edges; UI-11 and 'UAT gap 1 fixed geometry' stay green"
    - "e2e helpers openTileCluePopover/giveAnyLegalClue understand the row, so any-variant games can give a colour clue from a rainbow tile"
  artifacts:
    - path: "apps/web/components/hanabi/CluePopover.tsx"
      provides: "colour row rendering mode"
      contains: "tile-clue-color-"
    - path: "apps/web/components/hanabi/TeammateCard.tsx"
      provides: "variant-agnostic row detection and wiring"
      contains: "cluableColorsForView"
    - path: "apps/web/lib/clue-popover-render.test.ts"
      provides: "render proof of D-05..D-08"
      contains: "renderToStaticMarkup"
    - path: "e2e/helpers.ts"
      provides: "row-aware popover helpers"
      contains: "colorRowButtons"
  key_links:
    - from: "TeammateCard.tsx"
      to: "CluePopover colorRow prop"
      via: "cluableColorsForView(game) when the tile's own suit is not nameable"
      pattern: "colorRow"
    - from: "CluePopover row entry onClick"
      to: "onGiveClue({ type: \"color\", value })"
      via: "onGiveColor(value) callback"
      pattern: "onGiveColor\\("
---

<objective>
Make every legal Rainbow colour clue reachable from the UI (RULES-14, success criterion 1): a rainbow tile's quick-clue popover shows a compact row of the five nameable colours instead of today's permanently disabled single colour button (owner-confirmed 2026-09-18). Update the e2e helpers and every affected locator in the same plan so no stale assertion survives.

Purpose: closes the UI half of RULES-14; the server half landed in 07-01, so each row entry sends a clue the patched canClue accepts.
Output: CluePopover row mode with anchor flip, TeammateCard wiring, clue-popover-render.test.ts, row-aware e2e helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-variant-support-rainbow-black/07-CONTEXT.md
@.planning/phases/07-variant-support-rainbow-black/07-UI-SPEC.md
@.planning/phases/07-variant-support-rainbow-black/07-PATTERNS.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md

<interfaces>
From apps/web/components/hanabi/CluePopover.tsx (current):
`export interface CluePopoverProps { suit: Suit; rank: Rank; colorDisabled: boolean; rankDisabled: boolean; onGiveColor: () => void; onGiveRank: () => void; }`
Root: `<div data-testid="tile-clue-popover" role="menu" aria-label="Give a clue" className="absolute z-20 flex flex-col items-stretch gap-[length:var(--space-xs)] rounded-md border-2 p-[length:var(--space-xs)]" style={{ top: "100%", left: "50%", transform: "translateX(-50%)", marginTop: 4, backgroundColor: "var(--color-surface)", borderColor: "var(--color-border)", minWidth: 64 }}>`
Colour button: `data-testid="tile-clue-color"`, `role="menuitem"`, `aria-label={`Give a ${suitVisual.label} clue`}`, class string with text-[length:var(--text-label)] (not bold), `style={{ color: suitVisual.hueVar, lineHeight: "var(--text-label--line-height)" }}`.
Rank button: `data-testid="tile-clue-rank"`, `font-bold`, `aria-label={`Give a ${rank} clue`}`.

From apps/web/components/hanabi/TeammateCard.tsx (lines ~83-106, ~203-213): `rankDisabled`, `colorNameable = !card.hidden && cluableColorsForView(game).includes(card.suit)`, `colorDisabled`, `openable = !rankDisabled`; wrapper `<span data-clue-tile={card.id} className="relative inline-block">`; popover rendered with `onGiveColor={() => onGiveClue({ type: "color", value: card.suit })}`.

From apps/web/lib/hanabi-board-logic.ts: `cluableColorsForView(view: HanabiView): readonly Suit[]`, `disabledReasonFor(game, { kind: "clue", targetSeatId, clue }, ctx)`.

From apps/web/lib/suit-visuals.ts: `SUIT_VISUALS[suit].label`, `.hueVar` (var(--color-suit-*)).

From apps/web/components/hanabi/HanabiBoard.tsx (lines ~165-188): outside-click listener keeps the popover open only for pointerdown inside `[data-clue-tile="{openId}"]` — the row MUST render inside that wrapper.

From e2e/helpers.ts: `openTileCluePopover(page, targetSeatId, tileIndex=0): Promise<{ colorButton: Locator; rankButton: Locator } | null>`; `giveAnyLegalClue(page, targetSeatId)`; `giveAnyLegalClueToAnyTeammate(page)`.
Callers reading `opened.colorButton`: e2e/hanabi-realtime.spec.ts ~181-184; e2e/hanabi-table-polish.spec.ts giveClueOfKind ~43-60 and ~561.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rainbow tile colour row in CluePopover and TeammateCard, with anchor flip and a render test</name>
  <files>apps/web/components/hanabi/CluePopover.tsx, apps/web/components/hanabi/TeammateCard.tsx, apps/web/lib/clue-popover-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/CluePopover.tsx (whole file)
    - apps/web/components/hanabi/TeammateCard.tsx (whole file; comment near lines 83-92 documents today's disabled-rainbow behaviour and must be rewritten)
    - apps/web/components/hanabi/HanabiBoard.tsx (lines ~160-190, outside-click listener)
    - apps/web/lib/hanabi-board-logic.ts (cluableColorsForView, disabledReasonFor, ActionContext)
    - apps/web/lib/own-hand-render.test.ts (baseView HanabiView fixture and how TeammateHand is rendered with renderToStaticMarkup)
    - apps/web/lib/own-hand-source.test.ts (the scanned own-hand path list — this task must not import anything new into own-hand files)
    - .planning/phases/07-variant-support-rainbow-black/07-UI-SPEC.md (Layout, Interaction Contract points 1-8, Copywriting)
  </read_first>
  <behavior>
    - Rainbow view, visible rainbow card, TeammateCard open=true: markup has exactly five `tile-clue-color-{suit}` buttons in order red, yellow, green, blue, white; no `data-testid="tile-clue-color"` single button; no `tile-clue-color-rainbow`; the popover fragment has no "Rainbow" text and no aria-label "Give a Rainbow clue"; each entry's aria-label is "Give a {Label} clue" and its style color is that suit's hueVar; the rank button (`tile-clue-rank`, bold) follows the row.
    - Rainbow view, red card: single `tile-clue-color` button labelled "Red", no row (D-06).
    - Black view, black card: single `tile-clue-color` button labelled "Black" (D-08).
    - Base view, red card: markup of the popover is unchanged from before this task (single button, same classes/style).
    - Rainbow view, rainbow card, isYourTurn true and clueTokens > 0: all five entries enabled; isYourTurn false (or clueTokens 0): all five entries disabled — the row's disabled state equals the rank button's (D-07). No disabled-reason text anywhere.
  </behavior>
  <action>
    RED: create apps/web/lib/clue-popover-render.test.ts (renderToStaticMarkup, node environment, fixtures copied locally from own-hand-render.test.ts's baseView — do not import across test files) rendering `TeammateCard` directly with `open: true` and each case in the behavior list; slice out the popover fragment by locating `data-testid="tile-clue-popover"`. Confirm the rainbow-row cases fail.

    GREEN, CluePopover.tsx (per D-05/D-07 and UI-SPEC): change `onGiveColor` to `(value: Suit) => void` and add prop `colorRow: readonly Suit[] | null`. When `colorRow` is null, render the existing single colour button byte-for-byte as today except its onClick calls `onGiveColor(suit)`. When `colorRow` is an array, render in its place one `<div data-testid="clue-color-row" className="flex flex-row flex-nowrap gap-[length:var(--space-xs)]">` containing one `<button type="button" role="menuitem">` per entry with `data-testid={`tile-clue-color-${value}`}`, `aria-label={`Give a ${SUIT_VISUALS[value].label} clue`}`, `disabled={colorDisabled}`, the exact class string of today's colour button (label size, not bold), `style={{ color: SUIT_VISUALS[value].hueVar, lineHeight: "var(--text-label--line-height)" }}`, text `SUIT_VISUALS[value].label`, `onClick={() => onGiveColor(value)}`. The container testid deliberately does not start with "tile-clue-color". The rank button is unchanged and stays below. In row mode only, add `width: "max-content"` to the root style (an absolute box inside a narrow tile wrapper otherwise shrink-wraps and would stack the labels vertically), and implement the anchor flip required by UI-SPEC Layout: a `useLayoutEffect` (row mode only) measuring the root's `getBoundingClientRect()` against `document.documentElement.clientWidth`; state `align: "center" | "start" | "end"`; "center" keeps today's `left: 50%; transform: translateX(-50%)`, "start" uses `left: 0` with no transform, "end" uses `right: 0`, `left: auto`, no transform; flip to "start" when the centred rect's left < 0 and to "end" when its right > clientWidth. Only the popover's own position changes — never any board element. Single-button mode keeps its current style object and runs no measurement. Rainbow never appears in the row because the row IS `cluableColors`. Update the component doc comment to describe both modes and cite D-05/D-07. No new tokens, no hex (UI-SPEC Color).

    GREEN, TeammateCard.tsx (per D-05..D-08): keep `colorNameable = !card.hidden && cluableColorsForView(game).includes(card.suit)` as the sole detection rule (never `card.suit === "rainbow"`). Compute `colorRow = !card.hidden && !colorNameable ? cluableColorsForView(game) : null`. For the single button keep the existing colorDisabled computation; when `colorRow` is non-null pass `colorDisabled={rankDisabled}` (D-07: a rainbow tile is touched by every nameable colour, so the only gate is the shared ended/reconnecting/not-your-turn/no-tokens gate the rank button already computes via disabledReasonFor — no per-entry gate). Pass `colorRow` and `onGiveColor={(value) => onGiveClue({ type: "color", value })}`. Rewrite the comment near lines 83-92 to describe the row (it currently documents the pre-Phase-7 disabled-rainbow behaviour). Do not touch hanabi-hint-logic.ts, HintIndicator.tsx or cluePulseColorFor (D-09/D-10). Do not add imports to any own-hand file.

    Finally grep for other CluePopover consumers: `grep -rn "CluePopover\|onGiveColor" apps/web --include=*.tsx --include=*.ts` and update any caller to the new signature.
  </action>
  <verify>
    <automated>npx vitest run --project web -t "popover|own-hand" && npx vitest run --project web && npm run typecheck && ! grep -nE "#[0-9A-Fa-f]{3,8}\b" apps/web/components/hanabi/CluePopover.tsx apps/web/components/hanabi/TeammateCard.tsx && ! grep -nE "suit === \"rainbow\"|=== \"rainbow\"" apps/web/components/hanabi/CluePopover.tsx apps/web/components/hanabi/TeammateCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/clue-popover-render.test.ts exists, contains "tile-clue-color-red" and "tile-clue-color-rainbow" (the latter only in a not-present assertion) and a Black-case assertion for label "Black"
    - CluePopover.tsx contains `colorRow`, `data-testid="clue-color-row"`, `tile-clue-color-${` and `useLayoutEffect`
    - TeammateCard.tsx contains `cluableColorsForView(game).includes(card.suit)` and `colorRow` and no `=== "rainbow"`
    - `git diff --name-only -- apps/web/lib/hanabi-hint-logic.ts apps/web/components/hanabi/HintIndicator.tsx apps/web/app/globals.css apps/web/lib/suit-visuals.ts apps/web/lib/own-hand-source.test.ts` prints nothing
    - `npx vitest run --project web` exits 0 (own-hand-source and own-hand-render guards included) and `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>A rainbow tile's popover offers exactly the five nameable colours sharing one gate, every other tile is unchanged, no "Rainbow" option exists, and the wider popover flips its anchor instead of leaving the viewport.</done>
</task>

<task type="auto">
  <name>Task 2: Row-aware e2e helpers and every affected locator updated; geometry and popover e2e regressions green</name>
  <files>e2e/helpers.ts, e2e/hanabi-table-polish.spec.ts, e2e/hanabi-realtime.spec.ts</files>
  <read_first>
    - e2e/helpers.ts (openTileCluePopover and giveAnyLegalClue lines ~90-160, including their doc comments)
    - e2e/hanabi-table-polish.spec.ts (giveClueOfKind ~lines 43-66, the "UAT gap 16" test ~305-383, "UAT gap 34" ~511-578)
    - e2e/hanabi-realtime.spec.ts (UI-02 + UI-04 test ~lines 162-200)
    - e2e/start-game.spec.ts (UI-11 ~line 404 and "UAT gap 1 fixed geometry" ~line 829 — run, not edited)
    - playwright.config.ts (webServer ports 3100/8787, reuseExistingServer)
  </read_first>
  <action>
    First enumerate every affected locator: run `grep -rn "tile-clue-color\|colorButton\|openTileCluePopover\|giveAnyLegalClue\|giveClueOfKind" e2e/` and record the list in the SUMMARY; every hit must be either updated here or explicitly confirmed unaffected (base-variant only and still valid).

    In e2e/helpers.ts: extend `openTileCluePopover`'s return type to `{ colorButton: Locator; colorRowButtons: Locator; rankButton: Locator } | null`, where `colorButton` stays `page.getByTestId("tile-clue-color")` (exact match, single-button mode) and `colorRowButtons` is `page.getByTestId("tile-clue-popover").locator('[data-testid^="tile-clue-color-"]')`. Rewrite `giveAnyLegalClue` so it never calls `isEnabled()` on a locator with zero matches (Playwright would wait and throw on a rainbow tile): if `await colorButton.count() > 0` and enabled, click it; else if `await colorRowButtons.count() > 0` and the first entry is enabled, click the first entry; else fall back to the rank button as today. Rewrite both doc comments: remove the pre-Phase-7 text saying a rainbow tile's colour is non-nameable and falls back to rank; describe the row (D-05). Keep every existing caller's behaviour for base games.

    In hanabi-table-polish.spec.ts `giveClueOfKind` and the tile loop near line 561, and in hanabi-realtime.spec.ts near lines 181-184, apply the same count-guarded pattern (a colour request may use the single button or the first row entry) so these helpers are variant-safe; do not change what the tests assert for base games. If the returned colour label is used to look up an expected ring colour, read it from the clicked button's text.

    Before any Playwright run, kill the full orphaned dev-server tree on ports 3100/8787 by PID (reused stale servers poisoned several prior runs) using exactly: `for pid in $(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null); do pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; sleep 2; fuser -k 3100/tcp 8787/tcp 2>/dev/null; sleep 1; test -z "$(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null)"` — it must end with no listener on either port. Then run the regression set in the verify command. If a geometry test fails, fix CluePopover (Task 1's file) rather than the test: the popover must add no flow height.
  </action>
  <verify>
    <automated>for pid in $(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null); do pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; sleep 2; fuser -k 3100/tcp 8787/tcp 2>/dev/null; sleep 1; test -z "$(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null)" && npx playwright test e2e/start-game.spec.ts -g "UI-11|fixed geometry" && npx playwright test e2e/hanabi-table-polish.spec.ts -g "UAT gap 16|HINT-01|UAT gap 34|UAT gap 37" && npx playwright test e2e/hanabi-realtime.spec.ts -g "RT-01|UI-02"</automated>
  </verify>
  <acceptance_criteria>
    - e2e/helpers.ts contains `colorRowButtons` and `[data-testid^="tile-clue-color-"]`
    - `grep -n "falling back to rank" e2e/helpers.ts` and `grep -n "non-nameable suit, e.g. Rainbow" e2e/helpers.ts` print nothing
    - every `colorButton.isEnabled()` call in e2e/ is preceded by a `count()` guard: `grep -rn "colorButton.isEnabled()" e2e/` hits are each inside a branch guarded by `colorButton.count()`
    - the port-kill step leaves `lsof -ti tcp:3100 -ti tcp:8787` empty before Playwright starts
    - Playwright: "UI-11" (all matching tests), "UAT gap 1 fixed geometry", "UAT gap 16", "HINT-01", "UAT gap 34", "UAT gap 37", "RT-01", "UI-02" all pass
  </acceptance_criteria>
  <done>The e2e harness can give a colour clue from a rainbow tile through the real row, no locator still assumes the pre-Phase-7 disabled button, and the 1280x720 floor fit and fixed geometry are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI -> WebSocket action | The row only constructs `{type:"clue", clue:{type:"color", value}}` from cluableColors; the server (07-01) remains the authority |
| own-hand identity boundary | Own-hand components must never receive suit/rank; this plan edits teammate-tile code only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | CluePopover row values | mitigate | Row values come only from cluableColorsForView (never offers "rainbow"); server-side canClue guard from 07-01 rejects anything else regardless of the UI |
| T-07-04 | Information Disclosure | own-hand files | mitigate | No own-hand file edited or given new imports; own-hand-source.test.ts and own-hand-render.test.ts run in verify and must stay green |
| T-07-05 | Denial of Service (UX) | popover geometry | mitigate | Popover stays absolute inside data-clue-tile; anchor flip moves only the popover; UI-11 and fixed-geometry e2e in verify |
</threat_model>

<verification>
- `npx vitest run --project web` and `npm run typecheck` green
- Port kill, then the Playwright regression set in Task 2 green
</verification>

<success_criteria>
Every legal Rainbow colour clue is givable from the UI via a rainbow tile's five-colour row, nothing else about the popover or board changes, and the e2e harness is updated in the same plan.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-03-SUMMARY.md` when done
</output>
