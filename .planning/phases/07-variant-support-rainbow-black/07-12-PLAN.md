---
phase: 07-variant-support-rainbow-black
plan: 12
type: execute
wave: 4
depends_on: ["07-11"]
gap_closure: true
files_modified:
  - e2e/discard-layout.spec.ts
  - apps/web/lib/layout-budget.ts
  - apps/web/lib/layout-budget.test.ts
  - apps/web/components/hanabi/Table.tsx
  - apps/web/lib/table-render.test.ts
  - e2e/start-game.spec.ts
  - e2e/board-fit-black.spec.ts
  - e2e/hanabi-table-polish.spec.ts
autonomous: true
requirements: [BOARD-01, UI-11]

must_haves:
  truths:
    - "The discard pile occupies the large lower area of the right-hand board side (below the tokens and deck counter), using that area's full width and height"
    - "The turn sign sits in the small top-right spot the discard vacated, still an aria-live polite status region reading \"Your turn!\" in violet or \"{name}'s turn\""
    - "Discard tiles are substantially larger than before, with before and after sizes measured live at 1280x720 and recorded"
    - "Every reserved region keeps a fixed box as tiles accumulate (UAT gap 1), proven by the updated fixed-geometry e2e, which now also measures turn-sign and a discard tile"
    - "The board still fits 1280x720 with no scroll and still zoom-scales on larger windows"
    - "Discard drag reordering, group-by-suit, the expand-to-overlay button and the drop-zone hover highlight all still work"
  artifacts:
    - path: "apps/web/lib/layout-budget.ts"
      provides: "DISCARD_AREA_WIDTH_PX/HEIGHT_PX, TURN_SIGN_WIDTH_PX/HEIGHT_PX, discard tile size derived from the new area"
      contains: "TURN_SIGN_WIDTH_PX"
    - path: "e2e/discard-layout.spec.ts"
      provides: "live measurement of discard tile, discard area and turn sign at 1280x720 (DISCARD_TILE_MEASURE log line) plus the new arrangement's position asserts"
  key_links:
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "layout-budget.ts discard/turn-sign constants"
      via: "fixed width/height reservations on the discard box and the turn sign"
      pattern: "TURN_SIGN_(WIDTH|HEIGHT)_PX"
---

<objective>
Close UAT gap 4 ("can we take the discard area and swap that into the space that says "X's turn"? should give the discard pile more real estate to breathe and thus a larger tile size"). On the right-hand side of the board, the compact discard box (top-right, beside the tokens) swaps places with the turn sign (the large area below the tokens and deck counter). The discard pile then fills that full lower area with larger tiles, and the turn sign moves into the small top-right spot.

Purpose: give the discard pile room and legible tiles without changing the board's fixed geometry or its 1280x720 fit.
Output: a live measurement spec that records the before and after sizes, the swapped layout, and every layout test updated to prove the new arrangement (never weakened).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
@.planning/phases/07-variant-support-rainbow-black/07-UI-SPEC.md

<executor_rules>
- One commit per task, with a normal `git commit` (hooks run; NEVER `--no-verify`). Stage files explicitly by path; never `git add -A` or `git add .`.
- NEVER run `git stash`, `git reset`, `git checkout <file>`, `git restore`, `git clean` or any other destructive git command.
- Never stage `.planning/config.json`, `apps/web/next-env.d.ts`, any `*.tsbuildinfo`, any `.gitkeep`, `.planning/debug/`, `apps/web/AGENTS.md`, `apps/web/CLAUDE.md`.
- No push, no deploy.
- Before ANY Playwright run, kill the orphaned dev-server trees on ports 3100 and 8787 by PID, walking each listener up to its process-group root, and never touching `/tmp/games-preexisting` (ports 3101/8788; confirm via `readlink /proc/<pid>/cwd` before any kill): `for port in 3100 8787; do for pid in $(ss -ltnpH "sport = :$port" | grep -o 'pid=[0-9]*' | cut -d= -f2 | sort -u); do case "$(readlink /proc/$pid/cwd)" in /tmp/games-preexisting*) continue;; esac; pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; done; sleep 2`, then confirm with `ss -ltnp` and `-KILL` survivors.
- After the plan's last task, restart both dev servers in the background exactly as `playwright.config.ts` does: web `NEXT_PUBLIC_WORKER_HOST=localhost:8787 NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS=1000 NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS=1000 npm run dev --workspace apps/web -- -p 3100`; worker (cwd `apps/worker`) `npx wrangler dev --port 8787 --var SOCKET_STALE_MS:5000 --var ZOMBIE_SWEEP_INTERVAL_MS:1000`. Confirm 3100 and 8787 listen.
- Binding owner decisions: Black keeps its silver colour and art, no variant label, no ruled-out clue information, no rank numerals on card faces. Empty discard shows no placeholder copy (gap 24). The Play area and its tile size (50x65) are untouched.
</executor_rules>

<interfaces>
Today's right-hand side (apps/web/components/hanabi/Table.tsx ~272-460): a flex-col wrapper `{ height: BOARD_INNER_PX, gap: MIDDLE_GAP_PX }` containing
  (a) a top row `flex items-start gap-[--space-md]` holding the token column (`width: TOKEN_AREA_WIDTH_PX`: TokenColumn, then the deck-count row `height: DECK_COUNTER_PX`) and the bordered compact Discard box (`width: DISCARD_COMPACT_WIDTH_PX` 140, `height: DISCARD_COMPACT_PX` 156) with header (AreaLabel "Discard", `discard-group-by-suit`, `discard-toggle`) and the `discard-pile` zone (ref discardZoneRef, data-discard-count, data-view, data-drop-state, dropZoneHighlightStyle on the OUTER non-clipping div per gap 31; inner div clips) whose tiles are `discard-tile-{id}` FireworkCardFace at DISCARD_TILE_WIDTH_PX x DISCARD_TILE_HEIGHT_PX (16 x 22);
  (b) the `turn-sign` div (`role="status" aria-live="polite"`, `flex flex-1`, colour `--color-turn` when isYourTurn) filling the leftover height (about 227px by the comment's maths: BOARD_INNER_PX 383 - 156 - gap).
layout-budget.ts: BOARD_INNER_PX = max(PLAY_AREA_HEIGHT_PX, TOKEN_COLUMN_TOTAL_HEIGHT_PX, DISCARD_COMPACT_PX) = 383; TOKEN_AREA_WIDTH_PX = 58; TOKEN_COLUMN_TOTAL_HEIGHT_PX = 156; DISCARD_ROWS = 5; discardCompactHeightPx(rows); --space-md is 16px (apps/web/app/globals.css).
Measured 07-06/07-07 (1280x720, 5 seats, Black): clue/fuse/deck/discard widths 58 / 27 / 55 / 130px; tableau width 673px; no scroll.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live discard/turn-sign measurement spec, run against today's layout to record the BEFORE numbers</name>
  <files>e2e/discard-layout.spec.ts</files>
  <read_first>
    - e2e/helpers.ts (startTwoPlayerGame, giveAnyLegalClueToAnyTeammate, dragLocatorTo, seatIdOfOtherPlayer)
    - e2e/hanabi-table-polish.spec.ts (~lines 195-215: how a real discard is performed by dragging own-hand-slot-N onto discard-pile)
    - apps/web/components/hanabi/Table.tsx (discard-pile, discard-tile-{id}, turn-sign testids)
    - playwright.config.ts (viewport and webServer)
  </read_first>
  <action>
    Create e2e/discard-layout.spec.ts with one test, "DISCARD-LAYOUT: measure discard tile, discard area and turn sign at 1280x720". It sets the viewport to 1280x720, starts a two-player base game, makes the first active player give any legal clue (so a clue token is spent), and has the other player discard a tile by dragging an own-hand slot onto `discard-pile` (same pattern as hanabi-table-polish.spec.ts). It then waits for `data-discard-count` to be "1" on the measuring page and measures the bounding boxes of the first `[data-testid="discard-pile"] [data-testid^="discard-tile-"]`, `discard-pile`, the bordered discard box (the `discard-pile` element's closest ancestor with a border, found via evaluate), `turn-sign`, `clue-tokens` and `deck-count`. It prints one line `DISCARD_TILE_MEASURE tile=WxH area=WxH box=WxH turnSign=WxH` (rounded to 0.1px) via console.log, and asserts only layout-independent facts for now: the tile box has positive width and height, and there is no document scroll at 1280x720. Run it after the port-kill in executor_rules against the CURRENT (unswapped) layout and copy the logged BEFORE line verbatim into this task's commit message body and into the plan SUMMARY under "Discard tile size -- before". Expected before tile is about 16x22 px; record whatever is measured.
  </action>
  <verify>
    <automated>npx playwright test e2e/discard-layout.spec.ts --reporter=line 2>&1 | grep "DISCARD_TILE_MEASURE"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/discard-layout.spec.ts` succeeds
    - `grep -n "DISCARD_TILE_MEASURE" e2e/discard-layout.spec.ts` matches
    - `npx playwright test e2e/discard-layout.spec.ts` exits 0 and its output contains a `DISCARD_TILE_MEASURE tile=` line
    - The commit message body contains the BEFORE `DISCARD_TILE_MEASURE` line
  </acceptance_criteria>
  <done>A committed, passing measurement spec exists, and the before discard tile, area and turn-sign sizes at 1280x720 are recorded. Committed as one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Swap discard and turn sign; derive a larger discard tile; update every layout test to the new arrangement</name>
  <files>apps/web/lib/layout-budget.ts, apps/web/lib/layout-budget.test.ts, apps/web/components/hanabi/Table.tsx, apps/web/lib/table-render.test.ts, e2e/discard-layout.spec.ts, e2e/start-game.spec.ts, e2e/board-fit-black.spec.ts, e2e/hanabi-table-polish.spec.ts</files>
  <read_first>
    - apps/web/lib/layout-budget.ts (whole file, especially lines ~185-320 and the header maths comment)
    - apps/web/lib/layout-budget.test.ts (DISCARD_* and BOARD_INNER_PX asserts at ~95-125)
    - apps/web/components/hanabi/Table.tsx (whole render, lines ~225-522, including every gap 21/22/31/37 comment)
    - apps/web/lib/table-render.test.ts (discard/turn-sign structure assertions)
    - apps/web/components/hanabi/useDiscardDrag.ts and apps/web/lib/hanabi-discard-drag-logic.ts (drag reads registered tile rects; confirm it is tile-size agnostic)
    - apps/web/components/hanabi/DiscardOverlay.tsx (unchanged; confirm it does not import DISCARD_TILE_* sizes, or keep its own size if it does)
    - `grep -rn "DISCARD_TILE_\|DISCARD_COMPACT_\|DISCARD_ROWS" apps/web e2e --include=*.ts --include=*.tsx` (every consumer of the constants being replaced)
    - e2e/start-game.spec.ts (BOARD-01..05 test at ~523-600: "Discard sits to the RIGHT of the clue/fuse token column" asserts; "UAT gap 1 fixed geometry" at ~784-1060 with regionsToMeasure)
    - e2e/board-fit-black.spec.ts (~95-140 token/deck/discard/turn-sign measurement and BoardFitMeasurements)
    - e2e/hanabi-table-polish.spec.ts (~820-900 turn-sign test: live region, inside tableau, no scroll; discard hover highlight at ~875-900; DISC-01 reorder ~968+)
    - e2e/discard-layout.spec.ts (Task 1)
  </read_first>
  <action>
    Per UAT gap 4. layout-budget.ts: add `RIGHT_ROW_GAP_PX = 16`, mirroring --space-md (the gap Table.tsx already uses between the token column and the box beside it). Add `TURN_SIGN_WIDTH_PX = 140` and `TURN_SIGN_HEIGHT_PX = TOKEN_COLUMN_TOTAL_HEIGHT_PX`: the small top-right spot the discard vacates, same reservation as today's compact box. Add `DISCARD_AREA_WIDTH_PX = TOKEN_AREA_WIDTH_PX + RIGHT_ROW_GAP_PX + TURN_SIGN_WIDTH_PX` (the full right-side width) and `DISCARD_AREA_HEIGHT_PX = BOARD_INNER_PX - TOKEN_COLUMN_TOTAL_HEIGHT_PX - MIDDLE_GAP_PX` (the full lower area). Define BOARD_INNER_PX as max(PLAY_AREA_HEIGHT_PX, TOKEN_COLUMN_TOTAL_HEIGHT_PX), dropping the old compact-discard term. This avoids circularity, keeps Play dominant at 383, and gives the discard area whatever lies below the top row; layout-budget.test.ts asserts DISCARD_AREA_HEIGHT_PX is at least 200. Replace the fixed 16x22 discard tile with a pure exported function `discardTileSizeFor(innerWidth, innerHeight, minVisibleTiles)`. It returns `{ tileWidth, tileHeight, columns, rows }`: the largest integer tileWidth, with tileHeight = round(tileWidth * 22 / 16) to keep today's aspect, such that at least minVisibleTiles tiles fit in the inner box (area minus label row, label gap and padding, with DISCARD_ROW_GAP_PX gaps). Export `DISCARD_MIN_VISIBLE_TILES = 24`; a deeper pile still clips and the overlay shows the rest. Export DISCARD_TILE_WIDTH_PX/HEIGHT_PX computed from it; expect roughly 30x41. Remove DISCARD_COMPACT_WIDTH_PX, DISCARD_COMPACT_PX, DISCARD_ROWS and discardCompactHeightPx once no consumer remains, and rewrite the header maths comment for the swap. Table.tsx: in the right-hand flex-col, the top row keeps the token column and puts the `turn-sign` div where the discard box was, with fixed `width: TURN_SIGN_WIDTH_PX, height: TURN_SIGN_HEIGHT_PX`. The turn sign keeps its testid, role="status", aria-live="polite", violet `--color-turn` when isYourTurn, muted otherwise, centred text. Allow wrapping (break-words, text-center) so a long "{name}'s turn" never overflows the box. The bordered discard box (AreaLabel "Discard", discard-group-by-suit, discard-toggle with their out-of-flow hit areas, the non-clipping outer `discard-pile` zone carrying dropZoneHighlightStyle and data attributes, and the clipping inner tile container) moves below the top row at fixed `width: DISCARD_AREA_WIDTH_PX, height: DISCARD_AREA_HEIGHT_PX`, rendering tiles at the new DISCARD_TILE size. Rewrite the gap 21/22/37 comments to describe the swap (owner gap 4, 2026-09-18). Nothing grows with pile size. Update tests in this task. layout-budget.test.ts: replace the DISCARD_COMPACT/DISCARD_ROWS asserts with the new constants' identities, assert BOARD_INNER_PX stays 383, and add discardTileSizeFor cases including DISCARD_TILE_WIDTH_PX at least 24. table-render.test.ts gets the new structure. e2e/start-game.spec.ts BOARD-01..05: replace "discard x right of the token column" with "discard-pile top below deck-count bottom" plus "turn-sign x right of clue-tokens and fuse-tokens right edges". The "UAT gap 1 fixed geometry" test adds "turn-sign" to regionsToMeasure, asserts the same position relations at start and at end, and asserts a discard tile's box is identical when the pile has 1 tile and when it has at least 8. Its existing equal-box assertions stay. e2e/board-fit-black.spec.ts: keep its measurements, add turnSignHeight and discardHeight, and keep the no-scroll asserts. e2e/hanabi-table-polish.spec.ts turn-sign test: keep live-region, text and inside-tableau asserts, add "turn-sign sits above discard-pile". Its discard hover-highlight and DISC-01 reorder tests must pass unchanged, or with only coordinate/selector adjustments. e2e/discard-layout.spec.ts: tighten to the new arrangement (tile width at least 24px, tile at least 1.5x the recorded before width; discard-pile below deck-count; turn-sign right of clue-tokens; discard box width at least 200px), and keep printing the DISCARD_TILE_MEASURE line. Run the four specs after the port-kill in executor_rules and record the AFTER line in the commit body and SUMMARY under "Discard tile size -- after", beside the before line.
  </action>
  <verify>
    <automated>npx vitest run apps/web && npx tsc -b apps/web && npx playwright test e2e/discard-layout.spec.ts e2e/start-game.spec.ts e2e/board-fit-black.spec.ts e2e/hanabi-table-polish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const TURN_SIGN_WIDTH_PX\|export const TURN_SIGN_HEIGHT_PX\|export const DISCARD_AREA_WIDTH_PX\|export const DISCARD_AREA_HEIGHT_PX\|export function discardTileSizeFor" apps/web/lib/layout-budget.ts` returns 5 lines
    - `grep -rn "DISCARD_COMPACT_WIDTH_PX\|DISCARD_COMPACT_PX\|discardCompactHeightPx" apps/web e2e --include=*.ts --include=*.tsx` returns nothing
    - `grep -n "TURN_SIGN_WIDTH_PX" apps/web/components/hanabi/Table.tsx` matches
    - `grep -n '"turn-sign"' e2e/start-game.spec.ts` matches inside regionsToMeasure
    - `npx vitest run apps/web` exits 0; `npx tsc -b apps/web` exits 0
    - `npx playwright test e2e/discard-layout.spec.ts e2e/start-game.spec.ts e2e/board-fit-black.spec.ts e2e/hanabi-table-polish.spec.ts` exits 0
    - The logged AFTER tile width is at least 1.5x the BEFORE width recorded in Task 1
  </acceptance_criteria>
  <done>The discard pile fills the large lower right-hand area with tiles at least 1.5x larger, and the turn sign sits in the small top-right spot. Fixed geometry, the 1280x720 fit, drag, group-by-suit, overlay and hover highlight all hold, and the before/after tile sizes are recorded. Committed as one commit; dev servers restarted per executor_rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | layout-only change; no new data crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-01 | Information Disclosure | discard tiles | accept | discard identities are already public to every seat; only rendered size changes |
| T-07-12-02 | Tampering | port kill hitting /tmp/games-preexisting | mitigate | cwd check via /proc/<pid>/cwd before any kill; ports 3101/8788 never targeted |
</threat_model>

<verification>
- `npx vitest run apps/web` and `npx tsc -b apps/web` exit 0
- The four Playwright specs listed in Task 2 exit 0
- The SUMMARY records the before and after DISCARD_TILE_MEASURE lines
</verification>

<success_criteria>
The discard pile and the turn sign have swapped places, discard tiles are measurably larger (both sizes recorded), and every fixed-geometry and fit proof has been updated to the new arrangement and still passes, with none weakened.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-12-SUMMARY.md` when done
</output>
