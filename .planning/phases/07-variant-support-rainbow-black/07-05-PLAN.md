---
phase: 07-variant-support-rainbow-black
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
autonomous: false
requirements: [RULES-14, UI-07]

must_haves:
  truths:
    - "The full gate (npm test && npm run typecheck && npx playwright test) is green from a clean port state before the owner is asked anything"
    - "D-05..D-08: the owner confirms the rainbow tile's five-colour row looks right, including at the left and right hand edges at 1280x720"
    - "D-12: the owner confirms rainbow tiles are visually distinct from the --color-turn turn highlight on a live Rainbow board (no code change; verify-only)"
    - "D-13/D-14: the owner confirms no critical-card or early-unwinnable behaviour exists and a Black game ends only on fuses, a perfect score or the final round (verify-only)"
    - "D-15: Black keeps its silver colour and art (owner answer 2, verify-only); no variant label appears on the board (owner answer 3)"
    - "D-09/D-10: a rainbow tile hit by a colour clue shows that clue's colour ring and pulse (verify-only)"
    - "The owner plays full games in base, Rainbow and Black and signs off"
  artifacts:
    - path: ".planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md"
      provides: "gate results and owner sign-off record"
      contains: "Gate results"
  key_links:
    - from: "07-HUMAN-UAT.md gate section"
      to: "npm test / npm run typecheck / npx playwright test exit codes"
      via: "recorded command output"
      pattern: "playwright"
---

<objective>
Close the phase: run the full automated gate from a clean dev-server state, record the results, then ask the owner to confirm Rainbow and Black on a live board. This is the phase's owner sign-off checkpoint.

Purpose: D-12..D-15 are verify-only decisions and the rainbow row's look at hand edges is an aesthetic judgement; both need the owner.
Output: 07-HUMAN-UAT.md with gate results and the owner's reply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-variant-support-rainbow-black/07-CONTEXT.md
@.planning/phases/07-variant-support-rainbow-black/07-UI-SPEC.md
@.planning/phases/07-variant-support-rainbow-black/07-VALIDATION.md
@.planning/phases/07-variant-support-rainbow-black/07-01-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-02-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-03-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-04-SUMMARY.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate from a clean port state and record it</name>
  <files>.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md (structure of "Gate results" and "Sign-off checklist" sections to mirror)
    - .planning/phases/07-variant-support-rainbow-black/07-VALIDATION.md (Manual-Only Verifications table)
    - playwright.config.ts
  </read_first>
  <action>
    Kill the full orphaned dev-server tree on ports 3100/8787 by PID first, exactly: `for pid in $(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null); do pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; sleep 2; fuser -k 3100/tcp 8787/tcp 2>/dev/null; sleep 1; test -z "$(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null)"`. Then run `npm test && npm run typecheck && npx playwright test` from the repo root. If anything fails, stop and fix it in the owning plan's files (re-running the port kill before every Playwright retry) — never ask the owner to review a red gate. Also run the no-hex guard `grep -rnE "#[0-9A-Fa-f]{6}\b" apps/web/components apps/web/lib --include=*.tsx --include=*.ts` and confirm no new matches versus HEAD before this phase. Create 07-HUMAN-UAT.md with: a "Gate results" section (command, exit code, test counts for vitest projects and Playwright, date), and a "Sign-off checklist prepared for the owner" section listing the checks in Task 2 with the decision ids they confirm.
  </action>
  <verify>
    <automated>for pid in $(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null); do pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; sleep 2; fuser -k 3100/tcp 8787/tcp 2>/dev/null; sleep 1; test -z "$(lsof -ti tcp:3100 -ti tcp:8787 2>/dev/null)" && npm test && npm run typecheck && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npm run typecheck` and `npx playwright test` each exit 0 in the same run after the port kill
    - 07-HUMAN-UAT.md exists with sections "Gate results" and "Sign-off checklist prepared for the owner", the latter citing D-05, D-08, D-09, D-12, D-13, D-14, D-15
  </acceptance_criteria>
  <done>The whole suite is green from a clean state and the owner checklist is written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner sign-off on live Rainbow and Black boards</name>
  <files>.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md (Task 1 output)
  </read_first>
  <action>Start the dev servers after the port kill (`npm run dev --workspace apps/web -- -p 3100` and, under apps/worker, `npx wrangler dev --port 8787`), present the checklist below to the owner, wait for the reply, and record it verbatim in 07-HUMAN-UAT.md under "Owner's verbatim reply". Any reported issue becomes a numbered gap in the same file for gap-closure planning.</action>
  <what-built>Server rejects any non-nameable colour clue (clue_color_not_nameable); a rainbow tile's quick-clue popover offers a row of the five nameable colours; engine and e2e prove full games and scoring in base, Rainbow and Black; UI-07 gradient and silhouette checks. Full gate green (see 07-HUMAN-UAT.md).</what-built>
  <how-to-verify>
    1. Open http://localhost:3100 in two or three browser windows at 1280x720, create a room, choose Rainbow, seat everyone, start.
    2. On your turn, click a teammate's rainbow tile. Confirm the popover shows Red, Yellow, Green, Blue, White in their own colours with the bold number below, and no "Rainbow" option (D-05). If a rainbow tile sits at the far left or right of a hand, confirm the popover stays fully on screen and nothing on the board moves.
    3. Click one colour. On the receiver's screen, the rainbow tile and every tile of that colour get that colour's ring and pulse (D-09/D-10).
    4. Confirm rainbow tiles read clearly as rainbow and are clearly different from the violet turn highlight (D-12).
    5. Confirm there is no variant label on the board (owner answer 3).
    6. Play a Rainbow game to the end: the end overlay reads "/ 30".
    7. Start a Black game: a black tile's popover shows a single "Black" button (D-08); Black is still silver with its double-ring burst (D-15); play to the end, "/ 30"; nothing warns about last copies and the game ends only on fuses, a perfect score or the final round (D-13/D-14).
    8. Play one base game to the end: "/ 25".
  </how-to-verify>
  <verify>
    <automated>grep -q "Owner's verbatim reply" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 07-HUMAN-UAT.md contains an "Owner's verbatim reply" section with the owner's words
    - any issue the owner raises is recorded as a numbered gap
  </acceptance_criteria>
  <done>The owner has approved, or every issue raised is recorded as a gap.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev servers | Sign-off runs against localhost only; no deploy in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | forged "rainbow" clue frame | mitigate | Full gate re-runs 07-01's engine, adapter, worker and property tests before sign-off |
| T-07-02 | Information Disclosure | deck/seed override in production | mitigate | Gate confirms no override exists (07-04 chose zero-code); apps/worker/wrangler.jsonc unchanged this phase |
</threat_model>

<verification>
- Full gate green after port kill; owner reply recorded
</verification>

<success_criteria>
Phase 7 is signed off by the owner on live Rainbow and Black boards with a green full gate.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-05-SUMMARY.md` when done
</output>
