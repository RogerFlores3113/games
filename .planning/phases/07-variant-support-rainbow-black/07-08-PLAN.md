---
phase: 07-variant-support-rainbow-black
plan: 08
type: execute
wave: 3
depends_on: ["07-06", "07-07"]
gap_closure: true
files_modified:
  - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
autonomous: false
requirements: [RULES-02, RULES-03, RULES-14, UI-07]

must_haves:
  truths:
    - "The full gate is green from a clean port state (npm test, npx playwright test, npx tsc -b apps/web packages/rules packages/schema apps/worker) before the owner is asked anything"
    - "07-HUMAN-UAT.md records the gap-closure gate results, the before/after board and popover measurements from 07-06/07-07, and a fresh how-to-verify checklist for the 7-suit Black variant"
    - "The owner's earlier replies and the Gaps section are preserved; no owner reply is recorded by this plan and the phase is NOT marked complete"
  artifacts:
    - path: ".planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md"
      provides: "gap-closure gate results + 7-suit Black owner checklist"
      contains: "Gap closure 07-06..07-08"
  key_links:
    - from: "07-HUMAN-UAT.md gap-closure gate section"
      to: "npm test / npx playwright test / npx tsc -b exit codes"
      via: "recorded command output"
      pattern: "playwright"
---

<objective>
Close the gap loop: run the full automated gate from a clean dev-server state, record the results and the before/after measurements in 07-HUMAN-UAT.md, then hand the 7-suit Black variant back to the owner at a blocking checkpoint.

Purpose: the owner found the gap (Black must include Rainbow) and decided the board fit ("just widen the board a bit to make space for black in that format"). Only the owner can sign it off.
Output: an updated 07-HUMAN-UAT.md and a waiting owner checkpoint. This plan does NOT record an owner reply and does NOT mark the phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md
@.planning/phases/07-variant-support-rainbow-black/07-06-SUMMARY.md
@.planning/phases/07-variant-support-rainbow-black/07-07-SUMMARY.md
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate from a clean port state and write the gap-closure section of 07-HUMAN-UAT.md</name>
  <files>.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md (whole file: keep every existing section, including both "Owner's verbatim reply" sections and "## Gaps")
    - .planning/phases/07-variant-support-rainbow-black/07-06-SUMMARY.md and 07-07-SUMMARY.md (before/after board table, BLACK-POPOVER-FIT numbers, any playUntilGameEnds cap change)
    - playwright.config.ts (dev-server env vars)
  </read_first>
  <action>
    Kill the orphaned dev-server trees on ports 3100/8787 by PID, walking each listener up to its process-group root, and never touch `/tmp/games-preexisting` (3101/8788, checked via /proc/<pid>/cwd): `for port in 3100 8787; do for pid in $(ss -ltnpH "sport = :$port" | grep -o 'pid=[0-9]*' | cut -d= -f2 | sort -u); do case "$(readlink /proc/$pid/cwd)" in /tmp/games-preexisting*) continue;; esac; pgid=$(ps -o pgid= -p "$pid" | tr -d ' '); if [ -n "$pgid" ] && [ "$pgid" != "$(ps -o pgid= -p $$ | tr -d ' ')" ]; then kill -TERM -- "-$pgid" 2>/dev/null; fi; kill -TERM "$pid" 2>/dev/null; done; done; sleep 2`. Confirm 3100 and 8787 are empty with `ss -ltnp`, sending -KILL to survivors if needed. Record the PIDs, process-group roots and cwds you killed, and the preexisting pair you left alone.

    From the repo root, run in sequence: `npm test`, then `npx playwright test`, then `npx tsc -b apps/web packages/rules packages/schema apps/worker`. Also run the no-hex guard `grep -rnE "#[0-9A-Fa-f]{6}\b" apps/web/components apps/web/lib --include=*.tsx --include=*.ts` and confirm it shows only the pre-existing matches the earlier gate listed (note-box-render.test.ts, settings-modal-render.test.ts, tile-color.test.ts).

    If anything fails, stop and fix it in the owning gap plan's files (07-06 or 07-07), with one commit per fix, re-running the port kill before every Playwright retry. Never ask the owner to review a red gate. Never shrink tiles to make a test pass. The owner's STOP rule from 07-06 applies: if the board does not fit without shrinking, stop and report instead.

    Edit 07-HUMAN-UAT.md and append; do not rewrite anything above:
    (a) Frontmatter: set `updated:` to now (UTC ISO). Set `status: pending`, meaning awaiting the owner's re-verification. Keep `phase` and `started`, and add `07-06/07-07/07-08 gap closure` to `source`.
    (b) Under "## Gaps", item 1, append one indented line: "Fix implemented in 07-06 (board widened for a 7th column, tiles unchanged) and 07-07 (7-suit Black engine + six-colour rainbow row); awaiting owner verification." Do not mark it resolved.
    (c) A new section "## Gap closure 07-06..07-08 — gate results", with a table of command, result, and counts (vitest files/tests, Playwright passed/total, tsc exit code, no-hex guard), plus the port-kill record. Then a "Board fit (1280x720, 5 seats, Black)" before/after table copied from the summaries: tile (rank slot) size before and after (must be 50x65 both), Play region width (328 -> 382), tableau width, free horizontal width, the token, deck and discard region widths, and scroll sizes at 1280 and 1024. Then the popover fit numbers (row width W, leftmost and rightmost tile edges).
    (d) A new section "## Sign-off checklist for the 7-suit Black variant (prepared for the owner)", containing the how-to-verify list from Task 2 verbatim.
    Do NOT add an owner reply section, do NOT write "approved", and do NOT change ROADMAP/STATE to complete the phase.

    Restart both dev servers in the background with playwright.config.ts's env vars. Web: `NEXT_PUBLIC_WORKER_HOST=localhost:8787 NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS=1000 NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS=1000 npm run dev --workspace apps/web -- -p 3100`. Worker (cwd apps/worker): `npx wrangler dev --port 8787 --var SOCKET_STALE_MS:5000 --var ZOMBIE_SWEEP_INTERVAL_MS:1000`. Confirm both listen with `ss -ltnp`, and add a "Current status" line with the URLs to the new section.

    Commit once, with normal hooks and no `--no-verify`: `docs(07-08): record gap-closure gate and 7-suit Black owner checklist`. Stage only 07-HUMAN-UAT.md, plus any fix files if a fix was needed (each fix gets its own commit). Never stage `.planning/config.json`, `apps/web/next-env.d.ts`, `*.tsbuildinfo`, `.gitkeep`, `.planning/debug/`, `apps/web/AGENTS.md` or `apps/web/CLAUDE.md`. Never run `git stash`, `git reset`, `git checkout <file>` or any other destructive git command. No push, no deploy.
  </action>
  <verify>
    <automated>npm test && npx tsc -b apps/web packages/rules packages/schema apps/worker && grep -q "Gap closure 07-06..07-08" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md && grep -q "Sign-off checklist for the 7-suit Black variant" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx playwright test` and `npx tsc -b apps/web packages/rules packages/schema apps/worker` each exit 0 in the same run after the port kill (Playwright is run once in the action; counts recorded)
    - 07-HUMAN-UAT.md still contains both prior "Owner's verbatim reply" sections and "## Gaps" unchanged apart from the appended status line
    - New sections "Gap closure 07-06..07-08 — gate results" (with the before/after board table showing tile size 50x65 both times) and "Sign-off checklist for the 7-suit Black variant" exist
    - No new owner-reply section is added; the file does not say the phase is complete
    - Dev servers listening on 3100 and 8787 at the end
  </acceptance_criteria>
  <done>The gate is green, the evidence and the owner checklist are in 07-HUMAN-UAT.md, and live servers are up for the owner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner re-verification of the 7-suit Black variant</name>
  <files>.planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md (Task 1 output)
  </read_first>
  <action>Present the checklist below to the owner and stop, waiting for the owner's reply. This plan does not record the reply and does not mark the phase complete. The orchestrator or the next session handles the reply, writing it verbatim to 07-HUMAN-UAT.md and turning any reported issue into a new numbered gap.</action>
  <what-built>Black is now 7 suits: the five colours, Rainbow at its normal copies (three 1s, two each of 2/3/4, one 5), and Black at one copy per rank. That is 65 tiles and a max score of 35. Inside Black, Rainbow follows its Rainbow rules: every colour clue touches it, including a Black clue, and "Rainbow" can never be named. The board was widened for a seventh column, per your "just widen the board a bit" decision, and tiles are the same size as before (50x65). A rainbow tile's popover in Black offers Red, Yellow, Green, Blue, White and Black. The full gate is green (see 07-HUMAN-UAT.md).</what-built>
  <how-to-verify>
    1. Open http://localhost:3100 in two or three browser windows at 1280x720 (five if you want the tightest board). Create a room, choose Black, seat everyone, start.
    2. The Play area shows seven columns in the order red, yellow, green, blue, white, rainbow, black. The board is a little wider than before, tiles are the same size as in Rainbow and base, and nothing scrolls or is cut off.
    3. On your turn, click a teammate's rainbow tile. The popover shows Red, Yellow, Green, Blue, White and Black in their own colours, with the bold number below and no "Rainbow" option. If the rainbow tile sits at the far left or right of a hand, the popover stays fully on screen and nothing on the board moves.
    4. Click Black on that rainbow tile. On the receiver's screen, the rainbow tile and every black tile get Black's ring and pulse.
    5. Click a teammate's black tile. It shows a single "Black" button. Black is still silver with its double-ring burst, and there is no variant label anywhere on the board.
    6. The deck counter at the start reflects a 65-tile deck (65 minus the dealt hands). Play the Black game to the end: the end overlay reads "/ 35". Nothing warns about last copies, and the game ends only on fuses, a perfect score or the final round.
    7. Regression: a Rainbow game still has six columns and ends "/ 30"; a base game has five columns and ends "/ 25".
  </how-to-verify>
  <verify>
    <automated>grep -q "Sign-off checklist for the 7-suit Black variant" .planning/phases/07-variant-support-rainbow-black/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The checklist has been presented to the owner and execution is paused at this checkpoint
    - No owner reply has been written by this plan and the phase is not marked complete
  </acceptance_criteria>
  <done>The owner has the live 7-suit Black board and checklist in front of them; the phase waits on their reply.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev servers | Owner verification runs against localhost only; no deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | forged "rainbow" colour clue in 7-suit Black | mitigate | full gate re-runs the 07-01 nameable-colour engine/adapter/worker/property tests with the new BLACK_CONFIG |
| T-07-08-02 | Information Disclosure | own-hand identity leak in 7-suit projections | mitigate | full gate re-runs redaction/leak-check property tests over all variants |
| T-07-08-03 | Tampering | port kill hitting /tmp/games-preexisting | mitigate | cwd check via /proc/<pid>/cwd before any kill; ports 3101/8788 never targeted |
</threat_model>

<verification>
- Full gate green after a clean port kill; results, board before/after and popover fit recorded in 07-HUMAN-UAT.md
- Owner checkpoint presented; no reply recorded; phase not marked complete
</verification>

<success_criteria>
The 7-suit Black variant is back in front of the owner with a green gate and measured evidence that the board was widened without shrinking tiles.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-08-SUMMARY.md` when done
</output>
