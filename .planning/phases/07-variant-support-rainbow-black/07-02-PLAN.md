---
phase: 07-variant-support-rainbow-black
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rules/src/hanabi/variant-matrix.test.ts
  - apps/web/lib/suit-visuals.test.ts
  - apps/web/lib/rainbow-art-render.test.ts
autonomous: true
requirements: [UI-07, RULES-02, RULES-03]

must_haves:
  truths:
    - "D-16: in each of base, Rainbow and Black the engine reaches each end condition — fuses_exhausted, final_round_elapsed after deck exhaustion, and all_stacks_complete at a perfect 25/30/30 through a constructed state — and checkHanabiGameEnd's score, maxScoreFor and scoreBand agree"
    - "D-14: no early 'unwinnable' end exists in any variant — a Black state with a discarded black card whose max is unreachable still returns null from checkHanabiGameEnd until a real end condition fires"
    - "D-13: no variant-specific criticality code is added — the tests exercise only existing engine exports"
    - "D-11 / UI-07: rainbow's glyphPath and silhouette differ from every other suit, and SuitGlyph renders a url(#…) linearGradient fill for rainbow"
    - "D-11 / UI-07: a rainbow face renders the gradient at every call site it appears — teammate tile, played-stack slot, compact discard tile and discard overlay — with each url(#id) resolving to a linearGradient defined in the same markup"
    - "D-15: Black art is unchanged — no edit to suit-visuals.ts or globals.css"
  artifacts:
    - path: "packages/rules/src/hanabi/variant-matrix.test.ts"
      provides: "per-variant end-condition coverage"
      contains: "all_stacks_complete"
    - path: "apps/web/lib/rainbow-art-render.test.ts"
      provides: "gradient-at-every-size render check"
      contains: "renderToStaticMarkup"
  key_links:
    - from: "apps/web/lib/rainbow-art-render.test.ts"
      to: "PlayedStack, Table, DiscardOverlay, TeammateHand"
      via: "renderToStaticMarkup of the real call-site components"
      pattern: "url\\(#"
---

<objective>
Prove, rather than rebuild, the variant parametrization the engine and art already have: every variant reaches every end condition with consistent scoring (D-16), and the rainbow suit is unambiguous — distinct silhouette plus gradient fill rendered at every size (D-11, UI-07). Test-only plan; no production file changes.

Purpose: success criterion 3 (variants need no special-casing) at engine level, and UI-07's automated half.
Output: extended variant-matrix.test.ts, an explicit UI-07 assertion in suit-visuals.test.ts, new rainbow-art-render.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-variant-support-rainbow-black/07-CONTEXT.md
@.planning/phases/07-variant-support-rainbow-black/07-PATTERNS.md
@.planning/phases/07-variant-support-rainbow-black/07-VALIDATION.md

<interfaces>
From packages/rules/src/hanabi/endgame.ts: `export type EndReason = "fuses_exhausted" | "all_stacks_complete" | "final_round_elapsed"`; `currentScore(state)`; `scoreBand(score, maxScore)` ("Oh no!" at 0, "Legendary" at max); `checkHanabiGameEnd(state): GameEndResult | null` checks fuses >= MAX_FUSES, then score === maxScore, then finalTurnsRemaining === 0.

From packages/rules/src/hanabi/state.ts: `HanabiState = { variant, seatIds, turnIndex, hands, deck, stacks, discard, discardOrder, clueTokens, fuses, finalTurnsRemaining, history }`.

From packages/rules/src/hanabi/variant-matrix.test.ts: VARIANTS, EXPECTED_MAX_SCORE {base:25, rainbow:30, black:30}, VALID_BANDS, one sweep loop with `variantsSwept` counter asserted === 3; imports hanabiGame (./adapter), checkHanabiGameEnd, maxScoreFor, variantConfig, enumerateLegalActions, currentActorSeatId.

From apps/web/components/hanabi/SuitGlyph.tsx (lines ~55-78): for rainbow renders `<defs><linearGradient id={gradientId}>` with RAINBOW_GRADIENT_STOPS and `<path style={{ fill: isRainbow ? url(#gradientId) : visual.hueVar }}>`.

FireworkCardFace call sites: TeammateCard.tsx (CARD_WIDTH x CARD_HEIGHT), PlayedStack.tsx line ~98 (RANK_SLOT_WIDTH_PX x RANK_SLOT_HEIGHT_PX), Table.tsx line ~457 compact discard (DISCARD_TILE_WIDTH_PX x DISCARD_TILE_HEIGHT_PX, testid discard-tile-{id}), DiscardOverlay.tsx line ~198 (48 x 64, testid discard-overlay-card).

Existing render-test fixtures to copy (do not import across test files): apps/web/lib/stack-render.test.ts (renderStack(topRank, suit)), apps/web/lib/discard-overlay-render.test.ts, apps/web/lib/table-render.test.ts, apps/web/lib/own-hand-render.test.ts (baseView HanabiView fixture, TeammateHand), apps/web/lib/firework-card-render.test.ts (existing rainbow gradient test at 64x84).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Every variant reaches every end condition with agreeing score, maxScore and band (D-16, D-14)</name>
  <files>packages/rules/src/hanabi/variant-matrix.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/variant-matrix.test.ts (whole file)
    - packages/rules/src/hanabi/endgame.ts (checkHanabiGameEnd order, scoreBand)
    - packages/rules/src/hanabi/state.ts (HanabiState, StackEntry, HandSlot shapes)
    - packages/rules/src/hanabi/termination.property.test.ts (the deterministic deck-exhaustion case, to copy its final-round driving approach)
    - packages/rules/src/hanabi/legality.ts (MAX_FUSES, MAX_CLUE_TOKENS)
  </read_first>
  <action>
    Per D-16, add to variant-matrix.test.ts (keep the existing sweep untouched) a new describe "variant matrix: every end condition per variant (D-16)" with one sweep over VARIANTS guarded by its own `variantsSwept` counter asserted === 3. For each variant, starting from `hanabiGame.createInitialState({ seatIds: ["seat-0","seat-1","seat-2"], variant, seed })` and overriding fields on the returned object (the legality.test.ts buildState + override technique; no new production helper):
    (a) fuses_exhausted: set fuses to MAX_FUSES - 1 and make the active seat's first slot a card that cannot be played on the current stacks (e.g. a rank-5 of the variant's first suit while that stack is at 0); apply `{type:"play", cardId}` through hanabiGame.applyAction; assert checkHanabiGameEnd returns reason "fuses_exhausted", score equals currentScore of the resulting state, band equals scoreBand(score, maxScoreFor(variantConfig(variant))).
    (b) all_stacks_complete: set every stack in variantConfig(variant).suits to topRank 5 except the last suit at 4, and put that suit's rank-5 card in the active seat's first slot; play it; assert reason "all_stacks_complete", score === EXPECTED_MAX_SCORE[variant] === maxScoreFor(config), band "Legendary". This includes the Black and Rainbow sixth suit as the completing suit.
    (c) final_round_elapsed: set deck to a single card and drive the game with enumerateLegalActions/currentActorSeatId (preferring a discard or clue that is legal, never a play that would change fuses to 3) until checkHanabiGameEnd is non-null; assert reason "final_round_elapsed", that exactly seatIds.length turns elapsed after the deck emptied, and score/maxScore/band agree.
    Per D-14 add one Black-specific-data but branch-free assertion inside the same sweep: construct a state where a card of the variant's last suit at rank 1 is in `discard` and that suit's stack is 0 with other rank-1 copies absent from deck and hands only where the variant's rankCountsFor says the count is 1 — simplest form: for every variant, after moving every copy of (lastSuit, rank 1) into discard, checkHanabiGameEnd still returns null while fuses < 3, deck non-empty and finalTurnsRemaining is null. Write the copy count from `variantConfig(variant).rankCountsFor(lastSuit)[1]`, not a literal, so the same body serves all three variants (D-13: no variant-specific indicator or end rule exists or is added).
    Use no `if (variant === ...)` branches in the new block; per-variant expectations come from EXPECTED_MAX_SCORE and variantConfig only.
  </action>
  <verify>
    <automated>npx vitest run --project rules -t variant</automated>
  </verify>
  <acceptance_criteria>
    - variant-matrix.test.ts contains the strings "fuses_exhausted", "all_stacks_complete", "final_round_elapsed" and "Legendary"
    - the new describe contains `expect(variantsSwept).toBe(3)`
    - `grep -nE "variant === \"(rainbow|black|base)\"" packages/rules/src/hanabi/variant-matrix.test.ts` shows no new match inside the new describe (only the pre-existing blocks)
    - `npx vitest run --project rules` exits 0
    - `git diff --name-only` for this task lists only packages/rules/src/hanabi/variant-matrix.test.ts
  </acceptance_criteria>
  <done>Each variant is proven to end by fuses, by a perfect score and by the final round, with score, maxScoreFor and scoreBand consistent, and no early unwinnable end — all in one branch-free sweep.</done>
</task>

<task type="auto">
  <name>Task 2: UI-07 rainbow distinguishability — distinct glyph and gradient at every call-site size (D-11)</name>
  <files>apps/web/lib/suit-visuals.test.ts, apps/web/lib/rainbow-art-render.test.ts</files>
  <read_first>
    - apps/web/lib/suit-visuals.test.ts (pairwise-distinct glyphPath test ~line 62, silhouette test ~line 125, gap 38 turn-vs-rainbow test ~line 144)
    - apps/web/components/hanabi/SuitGlyph.tsx (gradient id generation and fill style)
    - apps/web/components/hanabi/FireworkCard.tsx (FireworkCardFace props)
    - apps/web/lib/firework-card-render.test.ts (existing rainbow gradient test and occurrence-count helper)
    - apps/web/lib/stack-render.test.ts, apps/web/lib/discard-overlay-render.test.ts, apps/web/lib/table-render.test.ts, apps/web/lib/own-hand-render.test.ts (fixtures for PlayedStack, DiscardOverlay, Table, TeammateHand)
    - apps/web/components/hanabi/PlayedStack.tsx, Table.tsx (compact discard ~line 440-465), DiscardOverlay.tsx (~line 180-205), TeammateCard.tsx (face render ~line 151)
  </read_first>
  <action>
    Per D-11, in suit-visuals.test.ts add an it block titled "UI-07: rainbow's glyphPath and silhouette differ from every other suit's" that iterates every suit except rainbow and asserts SUIT_VISUALS.rainbow.glyphPath !== that suit's glyphPath and the silhouette descriptor differs (reuse the existing silhouette field the "silhouette descriptor" test reads). Do not modify suit-visuals.ts, SuitGlyph.tsx or globals.css (D-15: Black stays silver; no art change).

    Create apps/web/lib/rainbow-art-render.test.ts (node environment, renderToStaticMarkup, same style as the existing *-render.test.ts files; copy fixtures locally, never import from another test file). Header comment cites UI-07 and D-11. Add a small helper that, given markup, extracts every `url(#ID)` reference and every `<linearGradient id="ID"` definition and asserts: at least one reference exists, and every referenced ID is defined in the same markup. Then one it block per call site, each rendering the real component with a rainbow card and a Rainbow-variant view:
    - teammate tile: TeammateHand (as own-hand-render.test.ts renders it) with baseView({ variant: "rainbow" }) and one visible rainbow card in otherHands;
    - played-stack slot: PlayedStack for suit "rainbow" at topRank >= 1 (stack-render.test.ts fixture shape);
    - compact discard: Table (table-render.test.ts fixture) with a rainbow card in discard, locating the discard-tile-{id} fragment;
    - discard overlay: DiscardOverlay (discard-overlay-render.test.ts fixture) with a rainbow card in discard.
    Each block asserts the gradient-reference helper passes for the rainbow card's fragment. Add one control block: the same four renders with a red card contain no `url(#` in the red card's fragment (the gradient is rainbow-only, so it is a real distinguishing channel). If extracting a card's fragment is awkward for Table, render a discard containing only the one card so the whole discard region is that card.
  </action>
  <verify>
    <automated>npx vitest run --project web -t "rainbow|UI-07" && npx vitest run --project web</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/rainbow-art-render.test.ts exists and contains "renderToStaticMarkup", "linearGradient", and it-titles naming teammate tile, played-stack, compact discard and discard overlay
    - suit-visuals.test.ts contains an it title starting with "UI-07:"
    - `git diff --name-only -- apps/web/lib/suit-visuals.ts apps/web/components apps/web/app/globals.css` prints nothing
    - `grep -nE "#[0-9A-Fa-f]{6}\b" apps/web/lib/rainbow-art-render.test.ts` prints nothing
    - `npx vitest run --project web` exits 0
  </acceptance_criteria>
  <done>Automated tests prove rainbow has a unique silhouette and renders a resolvable gradient fill at all four sizes it appears, while non-rainbow faces do not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-only plan; no production code, no network surface, no new input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | test fixtures constructing HanabiState directly | accept | Constructed states live only in *.test.ts files excluded from worker/web bundles; no production helper or override is added |
</threat_model>

<verification>
- `npx vitest run --project rules` and `npx vitest run --project web` green
- `npm run typecheck` green
</verification>

<success_criteria>
Engine end conditions and scoring are proven per variant with one branch-free body, and rainbow's two identity channels (silhouette, gradient) are asserted automatically at every rendered size.
</success_criteria>

<output>
Create `.planning/phases/07-variant-support-rainbow-black/07-02-SUMMARY.md` when done
</output>
