---
phase: 01-room-transport-skeleton
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.base.json
  - .gitignore
  - vitest.config.ts
  - playwright.config.ts
  - e2e/smoke.spec.ts
  - packages/schema/package.json
  - packages/schema/tsconfig.json
  - packages/schema/src/index.ts
  - packages/rules/package.json
  - packages/rules/tsconfig.json
  - packages/rules/src/index.ts
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - apps/worker/wrangler.jsonc
  - apps/worker/src/index.ts
  - apps/worker/src/smoke.test.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - docs/deployment.md
autonomous: false
requirements: [FDN-01]
user_setup:
  - service: cloudflare
    why: "Deploying apps/worker and its Durable Object namespace (needed from Plan 11 onward; not needed for Wave 0 dry-runs)"
    env_vars: []
    dashboard_config:
      - task: "Create a Cloudflare account on the Workers Free plan and run `npx wrangler login` locally"
        location: "dash.cloudflare.com"
  - service: vercel
    why: "Deploying apps/web (needed from Plan 11 onward)"
    env_vars: []
    dashboard_config:
      - task: "Connect this git repository to a Vercel project with Root Directory = apps/web and enable 'Include files outside the Root Directory in the Build Step'"
        location: "vercel.com project settings"

must_haves:
  truths:
    - "`npx vitest run` from the repo root discovers and runs tests in packages/rules, packages/schema, apps/worker and apps/web (Vitest 4 `projects`, not the removed `vitest.workspace.ts`)"
    - "`npx wrangler deploy --dry-run` from apps/worker succeeds while importing symbols from both @games/schema and @games/rules — retiring research assumption A3 (npm-workspace symlink/hoisting) before any real logic is written"
    - "`npx next build` from apps/web succeeds while importing a symbol from @games/schema — retiring Pitfall 5 (Vercel Root Directory isolation) locally"
    - "The TypeScript version is an explicit exact pin on the 5.x line, not whatever `npm install typescript` resolves (research assumption A1)"
    - "`npx playwright test` runs the smoke spec against locally started next dev + wrangler dev"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Root Vitest 4 config using the `projects` field"
      contains: "projects"
    - path: "playwright.config.ts"
      provides: "E2E harness booting next dev + wrangler dev via webServer"
      contains: "webServer"
    - path: "apps/worker/wrangler.jsonc"
      provides: "SQLite-backed Durable Object binding required by the Workers Free plan"
      contains: "new_sqlite_classes"
    - path: "package.json"
      provides: "npm workspaces root with exact TypeScript pin"
      contains: "workspaces"
    - path: "docs/deployment.md"
      provides: "Recorded monorepo deploy-shape settings (Vercel root-directory toggle, wrangler workspace resolution result)"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "@games/schema"
      via: "workspace import resolved by wrangler's bundler"
      pattern: "from \"@games/schema\""
    - from: "apps/web/app/page.tsx"
      to: "@games/schema"
      via: "workspace import resolved by the Next.js build"
      pattern: "from \"@games/schema\""
---

<objective>
Stand up the greenfield monorepo skeleton and the complete test harness, and retire the two deployment-shape risks (wrangler + npm workspaces, Vercel root-directory isolation) before any room, seat, or game code is written.

Purpose: Every later plan in this phase runs `npx vitest run` and imports across workspace packages. If either the workspace resolution or the test discovery is broken, that failure surfaces as a confusing error deep inside real logic instead of here, where it is a two-line fix.
Output: npm-workspaces monorepo (`apps/web`, `apps/worker`, `packages/rules`, `packages/schema`), root Vitest 4 config using `projects`, Playwright config with a passing smoke spec, a dry-run-clean `wrangler.jsonc`, and `docs/deployment.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-room-transport-skeleton/01-RESEARCH.md
@.planning/phases/01-room-transport-skeleton/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm TypeScript major-version pin and the vitest legitimacy flag</name>
  <read_first>
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Assumptions Log" (row A1)
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Package Legitimacy Audit" (the `vitest` SUS row and the `typescript` row)
    - CLAUDE.md § "Technology Stack" → Core Technologies table, TypeScript row ("5.7+")
  </read_first>
  <decision>Which TypeScript major line does this repo pin, and do we accept the researcher's disposition on the one flagged package?</decision>
  <context>
`npm view typescript version` currently resolves to 7.0.2 — a major ahead of the "TypeScript 5.7+" that CLAUDE.md's stack table specifies and that Next.js 16 / Vitest 4 / wrangler 4 tooling was validated against in research. Letting `npm install typescript` grab `latest` silently adopts an unvalidated compiler for the whole monorepo. Separately, `slopcheck` flagged `vitest` as SUS purely on name-similarity to `vite` (`TYPOSQUAT_RISK`); the researcher verified this as a false positive against the npm registry, the vitest-dev GitHub org, and vitest's explicit presence in CLAUDE.md's already-locked stack. Package-legitimacy checkpoints are never auto-approvable, so this needs your eyes even though the recommendation is to proceed.
  </context>
  <options>
    <option id="pin-5x">
      <name>Pin TypeScript to the latest 5.x (recommended)</name>
      <pros>Matches CLAUDE.md's stated constraint; matches what every other pinned version in RESEARCH.md was verified against; zero risk of a compiler rewrite breaking Next.js's TS integration or Vitest transforms mid-phase</pros>
      <cons>Will need a deliberate upgrade later if the ecosystem moves to 7.x</cons>
    </option>
    <option id="pin-7x">
      <name>Pin TypeScript to 7.0.2</name>
      <pros>Current; avoids a future migration</pros>
      <cons>No tool in this stack was verified against it in research; a from-scratch compiler rewrite is the single worst thing to debug during a greenfield scaffold</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — human decision, no automated check. Executor records the choice in the plan SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - The chosen option id is recorded verbatim in the plan SUMMARY under "Decisions".
    - The user has explicitly acknowledged the `vitest` SUS flag as accepted (false positive) or rejected it.
  </acceptance_criteria>
  <action>Present the two options above to the user, wait for a selection, and record the chosen option id plus the vitest acknowledgement in the plan SUMMARY. Do not run `npm install typescript` before the answer arrives — the whole point is that the version is chosen, not defaulted.</action>
  <done>The user has selected `pin-5x` or `pin-7x` and has explicitly accepted or rejected the `vitest` SUS flag; both are recorded in the SUMMARY.</done>
  <resume-signal>Select: `pin-5x` or `pin-7x`, and state "vitest accepted" or describe an objection.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the npm-workspaces monorepo skeleton with four packages</name>
  <files>package.json, package-lock.json, tsconfig.base.json, .gitignore, packages/schema/package.json, packages/schema/tsconfig.json, packages/schema/src/index.ts, packages/rules/package.json, packages/rules/tsconfig.json, packages/rules/src/index.ts, apps/worker/package.json, apps/worker/tsconfig.json, apps/worker/wrangler.jsonc, apps/worker/src/index.ts, apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.ts, apps/web/app/layout.tsx, apps/web/app/page.tsx</files>
  <read_first>
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Architecture Patterns" → "Recommended Project Structure" and the `wrangler.jsonc` code example
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Standard Stack" (every version number below comes from there)
    - CLAUDE.md § "Shared Types Strategy" (why `packages/rules` and `packages/schema` must be framework-free)
  </read_first>
  <action>
Create a private npm-workspaces root `package.json` with `"private": true`, `"workspaces": ["apps/*", "packages/*"]`, `"type": "module"`, and scripts `test` (`vitest run`), `test:e2e` (`playwright test`), `typecheck` (`tsc -b`), `build:worker` (`npm run build --workspace apps/worker`), `build:web` (`npm run build --workspace apps/web`).

Pin dependencies to these exact versions verified in RESEARCH.md § Standard Stack (exact pins, no `^`, so a later `npm install` cannot drift the stack mid-phase): `partyserver@0.5.10`, `partysocket@1.3.0`, `wrangler@4.128.0`, `@cloudflare/workers-types@5.20260902.1`, `next@16.3.4`, `react@19.2.8`, `react-dom@19.2.8`, `zod@4.5.4`, `zustand@5.0.15`, `clsx@2.1.1`, `nanoid@6.0.1`, `lucide-react` (latest at install), `tailwindcss@4.3.3`, `@tailwindcss/postcss` (matching tailwind minor), `vitest@4.1.11`, `fast-check@4.9.0`, `@playwright/test@1.62.1`. For TypeScript, resolve the exact version with `npm view "typescript@^5" version` and pin that literal string (unless Task 1 selected `pin-7x`, in which case pin `7.0.2`).

Place `typescript`, `vitest`, `fast-check`, `@playwright/test` as root devDependencies. Place `wrangler` and `@cloudflare/workers-types` as devDependencies of `apps/worker`, `partyserver` as a dependency of `apps/worker`. Place `next`, `react`, `react-dom`, `partysocket`, `zustand`, `clsx`, `lucide-react` as dependencies of `apps/web`, with `tailwindcss` and `@tailwindcss/postcss` as its devDependencies. Place `zod` as a dependency of `packages/schema`. `packages/rules` declares NO dependencies at all — not even `@games/schema` — because FDN-02 requires the rules engine to build and test in complete isolation.

Name the workspace packages `@games/schema` and `@games/rules`, each `"private": true`, `"type": "module"`, with `"main"`/`"types"` pointing at `src/index.ts` and an `"exports"` map of `{".": "./src/index.ts"}` — consumed as TypeScript source, no build step, since both consumers (Next.js/Turbopack and wrangler/esbuild) transpile TS themselves.

Create `tsconfig.base.json` at the root with `"target": "ES2022"`, `"lib": ["ES2022"]`, `"module": "ESNext"`, `"moduleResolution": "Bundler"`, `"strict": true`, `"noUncheckedIndexedAccess": true`, `"verbatimModuleSyntax": true`, `"skipLibCheck": true`, `"noEmit": true`, and a `"paths"` map aliasing `@games/schema` → `packages/schema/src/index.ts` and `@games/rules` → `packages/rules/src/index.ts`. Every package `tsconfig.json` extends it; `apps/worker`'s adds `"types": ["@cloudflare/workers-types"]`; `apps/web`'s adds `"lib": ["ES2022", "DOM", "DOM.Iterable"]` and the Next.js plugin.

Add a monorepo smoke sentinel to each shared package so cross-package resolution is provable by grep, not by vibes: export `const SCHEMA_SMOKE = "schema-smoke-ok"` from `packages/schema/src/index.ts` and `const RULES_SMOKE = "rules-smoke-ok"` from `packages/rules/src/index.ts`.

Create `apps/worker/wrangler.jsonc` with `"name": "games-worker"`, `"main": "src/index.ts"`, `"compatibility_date": "2026-09-01"`, no `nodejs_compat` flag, a Durable Object binding `{"name": "ROOM", "class_name": "RoomDO"}`, and `"migrations": [{"tag": "v1", "new_sqlite_classes": ["RoomDO"]}]` — `new_sqlite_classes` is mandatory, since the Workers Free plan only supports SQLite-backed Durable Objects.

Create a placeholder `apps/worker/src/index.ts` that imports `SCHEMA_SMOKE` and `RULES_SMOKE`, exports a stub `class RoomDO` extending `DurableObject` (the real `partyserver`-based implementation lands in Plan 07), and a default fetch handler returning a 200 text response containing both sentinel values. Create a minimal `apps/web` App Router app: `next.config.ts`, `app/layout.tsx` (html/body shell only), and `app/page.tsx` rendering `SCHEMA_SMOKE` imported from `@games/schema`.

Write `.gitignore` covering `node_modules`, `.next`, `dist`, `.wrangler`, `test-results`, `playwright-report`, `.vercel`, `.env*.local`.
  </action>
  <verify>
    <automated>npm install && npx tsc -b --dry 2>/dev/null; npx tsc --noEmit -p apps/worker/tsconfig.json && npx tsc --noEmit -p packages/rules/tsconfig.json && npx tsc --noEmit -p packages/schema/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` exits 0 and `node -e "const p=require('./package.json');process.exit(JSON.stringify(p.workspaces)==='[\"apps/*\",\"packages/*\"]'?0:1)"` exits 0.
    - `node -p "require('./package.json').devDependencies.typescript"` prints an exact version with no leading `^` or `~`, matching the major chosen in Task 1.
    - `grep -v '^\s*//' apps/worker/wrangler.jsonc | grep -q new_sqlite_classes` exits 0, and `grep -v '^\s*//' apps/worker/wrangler.jsonc | grep -q nodejs_compat` exits 1.
    - `grep -q 'SCHEMA_SMOKE' apps/worker/src/index.ts` and `grep -q 'RULES_SMOKE' apps/worker/src/index.ts` both exit 0.
    - `node -e "const d=require('./packages/rules/package.json').dependencies;process.exit(!d||Object.keys(d).length===0?0:1)"` exits 0 — `packages/rules` has zero dependencies.
    - All three `tsc --noEmit` invocations above exit 0.
  </acceptance_criteria>
  <done>Four workspace packages exist, install cleanly, typecheck cleanly, and the DO binding is declared SQLite-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Vitest 4 `projects` discovery and the Playwright harness</name>
  <files>vitest.config.ts, playwright.config.ts, e2e/smoke.spec.ts, apps/worker/src/smoke.test.ts, packages/schema/src/index.ts, packages/rules/src/index.ts</files>
  <read_first>
    - .planning/phases/01-room-transport-skeleton/01-VALIDATION.md § "Test Infrastructure" and § "Wave 0 Requirements"
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Common Pitfalls" → Pitfall 6 (Vitest 4 removed `vitest.workspace.ts`)
    - package.json and tsconfig.base.json (created in Task 2 — the `paths` aliases must be mirrored into the Vitest resolver)
  </read_first>
  <action>
Create a single root `vitest.config.ts` (do NOT create `vitest.workspace.ts` — it was removed in Vitest 4 and silently discovers zero tests). Use `defineConfig({ test: { projects: [...] } })` with four inline project objects named `schema`, `rules`, `worker`, `web`, each setting `test.name` and `test.root` to the corresponding package directory, and each declaring `resolve.alias` entries mapping `@games/schema` and `@games/rules` to their `src/index.ts` absolute paths so tests resolve workspace imports identically to the bundlers.

Add one real (not skipped) test per project so discovery is provable and a broken `projects` config fails loudly instead of silently passing with zero tests: co-locate `*.test.ts` files next to the sentinels — assert `SCHEMA_SMOKE === "schema-smoke-ok"` in `packages/schema`, `RULES_SMOKE === "rules-smoke-ok"` in `packages/rules`, and in `apps/worker/src/smoke.test.ts` assert that the worker module re-exports both sentinels. For `web`, add a trivial test asserting the `@games/schema` alias resolves from inside `apps/web`.

Create `playwright.config.ts` at the repo root with `testDir: "./e2e"`, a single chromium project, `use.baseURL` of `http://localhost:3000`, `reporter: "list"`, `retries: 0`, and two `webServer` entries: `npm run dev --workspace apps/web` on port 3000 and `npx wrangler dev --port 8787` in `apps/worker` on port 8787, both with `reuseExistingServer: !process.env.CI`. Add matching `dev` scripts to each app's `package.json`.

Create `e2e/smoke.spec.ts` asserting that `GET /` returns HTTP 200 and the page body contains `schema-smoke-ok`, and that `GET http://localhost:8787/` returns 200 with a body containing `rules-smoke-ok`. This proves both dev servers boot and both resolve workspace imports at runtime.

Do not use any watch-mode flag in any script — every command must be a single-shot run that exits.
  </action>
  <verify>
    <automated>npx vitest run --reporter=basic && npx playwright install --with-deps chromium && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vitest.workspace.ts` exits 1 (the file must NOT exist).
    - `grep -c 'projects' vitest.config.ts` is at least 1.
    - `npx vitest run` exits 0 and its output names all four projects (`schema`, `rules`, `worker`, `web`) with at least one passing test each — verified by `npx vitest run --reporter=json` reporting `numTotalTests >= 4` and `numFailedTests == 0`.
    - `npx playwright test` exits 0 with the smoke spec passing.
    - `grep -rn -- '--watch' package.json apps/*/package.json packages/*/package.json` returns no matches.
  </acceptance_criteria>
  <done>`npx vitest run` and `npx playwright test` both pass from a clean checkout, with real assertions in all four projects.</done>
</task>

<task type="auto">
  <name>Task 4: Retire the two deploy-shape risks with dry-run builds and record the result</name>
  <files>docs/deployment.md, apps/worker/package.json, apps/web/package.json</files>
  <read_first>
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Common Pitfalls" → Pitfall 4 (wrangler + npm workspaces symlink/hoisting, assumption A3) and Pitfall 5 (Vercel "Include files outside the Root Directory")
    - .planning/phases/01-room-transport-skeleton/01-RESEARCH.md § "Environment Availability" (Cloudflare/Vercel accounts are user prerequisites)
    - apps/worker/wrangler.jsonc and apps/worker/src/index.ts (created in Task 2 — these are what must bundle)
  </read_first>
  <action>
Add a `build` script to `apps/worker/package.json` running `wrangler deploy --dry-run --outdir dist` and a `build` script to `apps/web/package.json` running `next build`. Both must run without any Cloudflare or Vercel credentials — `--dry-run` bundles without authenticating, which is exactly why this risk can be retired now, before the user has finished account setup.

Run both builds. If `wrangler deploy --dry-run` fails to resolve `@games/schema` or `@games/rules`, assumption A3 is confirmed broken: fix it by adding an explicit esbuild alias via wrangler's `alias` config field in `wrangler.jsonc` mapping both package names to their `src/index.ts` paths, and record that this workaround was required. Do not work around it by copying files or by removing the workspace import — the cross-package import is the thing under test.

Write `docs/deployment.md` recording, as a checklist a human can follow: (1) the exact Vercel project settings required — Root Directory `apps/web`, and **"Include files outside the Root Directory in the Build Step" must be ENABLED**, without which the `@games/schema` import fails on Vercel while succeeding locally; (2) the wrangler deploy command and whether the alias workaround was needed; (3) the two user prerequisites (Cloudflare account + `wrangler login`; Vercel project connected to the repo) with a note that Plan 11 depends on both. Leave a `## Custom domain` section heading with a `TODO(Plan 11)` marker — Plan 11 fills it in.
  </action>
  <verify>
    <automated>npm run build --workspace apps/worker && npm run build --workspace apps/web && grep -q 'schema-smoke-ok' apps/worker/dist/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build --workspace apps/worker` exits 0 and produces `apps/worker/dist/index.js`.
    - `grep -q 'schema-smoke-ok' apps/worker/dist/index.js` and `grep -q 'rules-smoke-ok' apps/worker/dist/index.js` both exit 0 — proving wrangler's bundler resolved BOTH hoisted workspace packages (assumption A3 retired).
    - `npm run build --workspace apps/web` exits 0.
    - `grep -v '^#' docs/deployment.md | grep -qi 'Include files outside the Root Directory'` exits 0.
    - `grep -q 'TODO(Plan 11)' docs/deployment.md` exits 0.
  </acceptance_criteria>
  <done>Both deploy targets bundle cross-package imports successfully offline, and the human-only deploy settings are written down before anyone needs them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Third-party package code executes at install and at build time |
| local dev → Cloudflare/Vercel | Deploy credentials are not present yet; all Wave 0 builds must work offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | npm installs in Task 2 | mitigate | RESEARCH.md § Package Legitimacy Audit ran slopcheck against every package; the single SUS entry (`vitest`, TYPOSQUAT_RISK on name-similarity to `vite`) and the `typescript` version drift are gated behind the blocking human checkpoint in Task 1. Every version is an exact pin, so a later `npm install` cannot silently pull a different tarball. |
| T-1-07 | Tampering | `apps/worker/wrangler.jsonc` migrations | mitigate | Declare `new_sqlite_classes` only. A KV-backed `new_classes` migration is unavailable on the Free plan and would fail at first real deploy; asserting on it by grep in Task 2 catches the mistake at scaffold time. |
| T-1-08 | Information Disclosure | committed config | accept | No secrets exist yet in this phase; `.gitignore` covers `.env*.local` and `.vercel` pre-emptively. Cloudflare/Vercel tokens live in the user's local CLI config, never in the repo. |
</threat_model>

<verification>
1. `npm install && npx vitest run && npx playwright test` — all green from a clean checkout.
2. `npm run build --workspace apps/worker && npm run build --workspace apps/web` — both succeed with no network credentials.
3. `test -f vitest.workspace.ts` returns non-zero.
4. `docs/deployment.md` exists and names the Vercel root-directory toggle.
</verification>

<success_criteria>
- A four-package npm-workspaces monorepo exists and typechecks.
- Vitest 4 discovers ≥4 tests across 4 named projects via `projects`; Playwright runs a real smoke spec against both dev servers.
- Both deploy targets bundle a cross-workspace import offline, retiring research assumptions A3 and Pitfall 5 locally.
- TypeScript is pinned to an exact, human-confirmed version.
</success_criteria>

<output>
Create `.planning/phases/01-room-transport-skeleton/01-01-SUMMARY.md` when done. Record: the TypeScript version chosen, whether the wrangler `alias` workaround was needed (assumption A3 outcome), and the exact `npx vitest run` project names later plans must target.
</output>
