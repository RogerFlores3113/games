---
phase: 05-reconnect-session-durability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/tsconfig.json
  - packages/schema/src/constants.ts
  - packages/schema/src/constants.test.ts
  - apps/worker/src/room-do.ts
  - apps/worker/src/room-do.test.ts
  - apps/worker/src/source-structure.test.ts
autonomous: true
requirements: [RT-04, RT-05]

must_haves:
  truths:
    - "D-04: heartbeat interval, pong timeout, server staleness threshold, and zombie-sweep interval are single named exports in packages/schema/src/constants.ts, and a unit test proves SOCKET_STALE_MS exceeds 60s (Chrome hidden-tab timer throttling) and exceeds 2x HEARTBEAT_INTERVAL_MS"
    - "D-02: a raw `__ping__` text frame sent to a live `wrangler dev` RoomDO is answered with a raw `__pong__` text frame and produces NO `error`, `state`, or `joined` frame (proves setWebSocketAutoResponse answers it without onMessage)"
    - "D-02/D-13: setWebSocketAutoResponse is registered exactly once, inside onStart, and the A1/A2/A3/A8 single-writer counts in source-structure.test.ts are unchanged"
    - "Research Pitfall 1: apps/worker type-checks against @cloudflare/workers-types/experimental so DurableObjectState exposes the auto-response API"
  artifacts:
    - path: "apps/worker/tsconfig.json"
      provides: "experimental workers-types surface"
      contains: "@cloudflare/workers-types/experimental"
    - path: "packages/schema/src/constants.ts"
      provides: "Phase 5 heartbeat constants and STALE_SOCKET_CLOSE_CODE"
      contains: "SOCKET_STALE_MS"
    - path: "apps/worker/src/room-do.ts"
      provides: "auto-response registration in onStart"
      contains: "setWebSocketAutoResponse"
    - path: "apps/worker/src/room-do.test.ts"
      provides: "heartbeat spike test + pong-tolerant, ping-by-default harness"
      contains: "__pong__"
  key_links:
    - from: "apps/worker/src/room-do.ts"
      to: "packages/schema/src/constants.ts"
      via: "import HEARTBEAT_PING, HEARTBEAT_PONG from @games/schema"
      pattern: "new WebSocketRequestResponsePair\\(HEARTBEAT_PING, HEARTBEAT_PONG\\)"
    - from: "apps/worker/src/room-do.test.ts"
      to: "RoomDO auto-response"
      via: "raw ws.send(HEARTBEAT_PING) in openSocket heartbeat"
      pattern: "HEARTBEAT_PING"
---

<objective>
Wave 0 foundation for Phase 5: expose the Durable Object auto-response API to the type checker, lock every D-04 timing constant in one place, register the hibernation-safe ping/pong pair in RoomDO, and SPIKE-PROVE against a real `wrangler dev` that the runtime answers the ping without invoking `onMessage` — before plans 05-02/05-03 build D-02/D-03 on top of that assumption (RESEARCH.md Open Question 1 / Assumption A3).

Purpose: Every later plan depends on (a) these constants and (b) auto-response actually working locally. If the spike fails, the phase must stop here rather than build a zombie sweep on an unproven primitive.
Output: tsconfig fix, constants + invariant test, onStart registration, a pong-tolerant ping-by-default test harness, a spike test, and a structural test pinning the heartbeat outside the `#send` chokepoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-reconnect-session-durability-hardening/05-CONTEXT.md
@.planning/phases/05-reconnect-session-durability-hardening/05-RESEARCH.md
@.planning/phases/05-reconnect-session-durability-hardening/05-PATTERNS.md

<interfaces>
Locked timing decisions (Claude's discretion per D-04, fixed here — later plans import, never re-decide):
- HEARTBEAT_PING = "__ping__"  (raw text frame, never JSON.stringify'd — Pitfall 2 byte-for-byte match)
- HEARTBEAT_PONG = "__pong__"
- HEARTBEAT_INTERVAL_MS = 20_000   (client ping cadence; keeps running while hidden)
- HEARTBEAT_PONG_TIMEOUT_MS = 10_000  (client force-reconnects if nothing heard within this after a ping)
- SOCKET_STALE_MS = 75_000   (server: a seated socket with no auto-response/bind within this is a zombie). Rationale: Chrome intensive throttling fires hidden-tab chained timers at most once per minute, so a threshold <= 60s would falsely disconnect a desktop player who alt-tabbed to the voice call; 75s is the smallest value with margin, giving teammates "disconnected" in 75-90s ("about a minute", D-04).
- ZOMBIE_SWEEP_INTERVAL_MS = 15_000  (grid step for the zombie_sweep timer, 05-02)
- STALE_SOCKET_CLOSE_CODE = 4003  (NON-terminal: client must reconnect after it; do not add to isTerminalCloseCode)

From node_modules/@cloudflare/workers-types/experimental/index.d.ts:
  interface DurableObjectState {
    setWebSocketAutoResponse(maybeReqResp?: WebSocketRequestResponsePair): void;
    getWebSocketAutoResponseTimestamp(ws: WebSocket): Date | null;
  }
  declare class WebSocketRequestResponsePair { constructor(request: string, response: string); }

From apps/worker/src/room-do.ts (current):
  async onStart(): Promise<void>  // lines 111-119; reruns on EVERY hibernation wake (via partyserver #ensureInitialized)
  #send(connection, frame)        // the ONLY connection.send site (A1)
  onMessage: parseClientMessage(String(raw)) failure -> this.#send(connection, { type: "error", code: "bad_request" })

From apps/worker/src/room-do.test.ts (current harness, lines 52-178):
  function spawnWrangler(): ChildProcess   // args: wrangler dev --port 18787 --persist-to <dir> --log-level error
  function openSocket(code: string): Promise<WebSocket>
  function collectMessages(ws): { raw: string[]; parsed: Parsed[]; waitFor; waitForClose }   // currently JSON.parse's EVERY frame — a raw "__pong__" would throw inside the listener
  function send(ws, msg): void   // JSON.stringify

From apps/worker/src/source-structure.test.ts:
  helpers findFilesWithMatch(pattern), countMatches(text, pattern), strippedByFile Map (comments stripped)
  A1: exactly 1 `.send(` across worker non-test files; A2: 1 encodeServerMessage(; A3: 1 `#send(` def, >= 8 this.#send( calls; A8: 1 #viewFor def, 1 type:"joined", 1 type:"state"

Typecheck command for this repo (no root tsconfig.json; root `tsc -b` fails with pre-existing TS5083):
  npx tsc -b apps/web packages/rules packages/schema apps/worker
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose experimental workers-types and lock D-04 heartbeat constants</name>
  <files>apps/worker/tsconfig.json, packages/schema/src/constants.ts, packages/schema/src/constants.test.ts</files>
  <read_first>
    - apps/worker/tsconfig.json
    - packages/schema/src/constants.ts
    - packages/schema/src/index.ts (confirms `export * from "./constants"`)
    - packages/schema/src/index.test.ts (test style for the schema project)
    - .planning/phases/05-reconnect-session-durability-hardening/05-RESEARCH.md (Pitfall 1, Pitfall 2, Open Question 2)
  </read_first>
  <behavior>
    - SOCKET_STALE_MS > 60_000 (exceeds Chrome's once-per-minute hidden-tab timer throttle)
    - SOCKET_STALE_MS > 2 * HEARTBEAT_INTERVAL_MS
    - HEARTBEAT_PONG_TIMEOUT_MS < HEARTBEAT_INTERVAL_MS
    - ZOMBIE_SWEEP_INTERVAL_MS < SOCKET_STALE_MS
    - HEARTBEAT_PING !== HEARTBEAT_PONG, both non-empty, and neither parses as JSON (JSON.parse throws), so neither can ever satisfy ClientMessageSchema/ServerMessageSchema
    - STALE_SOCKET_CLOSE_CODE === 4003 and differs from SUPERSEDED_CLOSE_CODE and ROOM_ABANDONED_CLOSE_CODE
  </behavior>
  <action>
    Per Research Pitfall 1: in apps/worker/tsconfig.json change the `types` array from `["@cloudflare/workers-types", "node"]` to `["@cloudflare/workers-types/experimental", "node"]`. Change nothing else in that file.

    RED: create packages/schema/src/constants.test.ts (vitest, schema project) asserting every bullet in <behavior>, importing from "./constants". Run it and confirm it fails (exports missing).

    GREEN: append to packages/schema/src/constants.ts a "Phase 5 (D-02/D-03/D-04)" block with exactly these named exports and values from the <interfaces> block: HEARTBEAT_PING = "__ping__", HEARTBEAT_PONG = "__pong__", HEARTBEAT_INTERVAL_MS = 20_000, HEARTBEAT_PONG_TIMEOUT_MS = 10_000, SOCKET_STALE_MS = 75_000, ZOMBIE_SWEEP_INTERVAL_MS = 15_000, STALE_SOCKET_CLOSE_CODE = 4003. Each gets a `/** D-xx: ... */` doc comment in this file's existing style explaining WHY: the ping/pong literals are raw non-JSON text answered by setWebSocketAutoResponse, sit outside ServerMessageSchema, carry no state, and must match byte-for-byte (D-02, Pitfall 2); SOCKET_STALE_MS carries the Chrome-throttling rationale and the "about a minute" D-04 intent; STALE_SOCKET_CLOSE_CODE is deliberately non-terminal so a socket closed by a false-positive sweep reconnects on its own (D-03). Also update the file's top header comment to mention Phase 5 D-04 values are fixed here too. Do not change any existing constant.

    Verify the worker still type-checks with the experimental types (the experimental subpath is a superset; if any existing worker file fails to compile, fix only the type reference, not runtime logic, and record it as a deviation).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project schema constants && npx tsc -b packages/schema apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@cloudflare/workers-types/experimental"' apps/worker/tsconfig.json` outputs 1
    - `grep -E 'export const (HEARTBEAT_PING|HEARTBEAT_PONG|HEARTBEAT_INTERVAL_MS|HEARTBEAT_PONG_TIMEOUT_MS|SOCKET_STALE_MS|ZOMBIE_SWEEP_INTERVAL_MS|STALE_SOCKET_CLOSE_CODE) ' packages/schema/src/constants.ts | wc -l` outputs 7
    - `grep -n 'export const SOCKET_STALE_MS = 75_000' packages/schema/src/constants.ts` matches
    - `npx vitest run --project schema constants` passes with all six behavior cases
    - `npx tsc -b packages/schema apps/worker` exits 0
  </acceptance_criteria>
  <done>Worker compiles against experimental types; seven Phase 5 constants exported from @games/schema with rationale comments; invariant test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register auto-response in onStart and spike-prove it against wrangler dev</name>
  <files>apps/worker/src/room-do.ts, apps/worker/src/room-do.test.ts</files>
  <read_first>
    - apps/worker/src/room-do.ts (onStart lines 111-119, onMessage 132-137, file header invariants)
    - apps/worker/src/room-do.test.ts (harness lines 1-178 in full; the robustness tests at ~458-523; D-17 eviction test ~812-858)
    - node_modules/partyserver/dist/index.js lines 55-120 (Connection IS the underlying WebSocket via Object.defineProperties)
    - packages/schema/src/constants.ts (after Task 1)
  </read_first>
  <behavior>
    - Spike: a joined socket that sends raw HEARTBEAT_PING receives a raw frame exactly equal to HEARTBEAT_PONG within 3s
    - Spike: in the 1500ms after sending the ping, that socket receives zero parsed frames of type "error", "state", or "joined" (a ping reaching onMessage would produce an `error`/`bad_request` frame)
    - Spike: three pings in a row yield three pongs, and a subsequent legal `set_variant` still produces a `state` frame (socket still usable)
    - All pre-existing room-do.test.ts tests still pass with the new ping-by-default harness
  </behavior>
  <action>
    Harness first (room-do.test.ts):
    (a) Make `collectMessages` pong-tolerant: in the message listener, if the text equals HEARTBEAT_PONG, increment a new `pongs` counter on the returned object (expose it as a getter or `{ count: number }` holder) and return WITHOUT pushing to `raw` or `parsed`; otherwise keep today's behavior. Any other non-JSON frame should be pushed to `raw` and skipped for `parsed` (try/catch) rather than throwing inside the listener.
    (b) Give `openSocket` an optional second parameter `options?: { heartbeat?: boolean }` defaulting heartbeat to true. When true, start a `setInterval` every 1000ms that sends the raw literal HEARTBEAT_PING via `ws.send(HEARTBEAT_PING)` only while `ws.readyState === WebSocket.OPEN`, and clear the interval on the socket's `close` event. This makes every existing test's socket behave like a real client so the 05-02 zombie sweep will not reap them; later tests opt out with `{ heartbeat: false }` to simulate a half-open peer. Add a header comment explaining this and citing D-02/D-15.
    (c) Import HEARTBEAT_PING, HEARTBEAT_PONG from "@games/schema".

    RED: add a new `describe("Phase 5 heartbeat spike (D-02, RESEARCH Open Question 1)")` block implementing the three behavior cases, using `openSocket(code, { heartbeat: false })` so only the test's explicit pings are sent. Run `npx vitest run --project worker room-do -t "heartbeat spike"` and confirm the pong assertion fails (no auto-response registered yet; the ping currently reaches onMessage and yields an error frame).

    GREEN (room-do.ts): import HEARTBEAT_PING and HEARTBEAT_PONG from "@games/schema". In `onStart`, before the `loadRoom` call, add `this.ctx.setWebSocketAutoResponse(new WebSocketRequestResponsePair(HEARTBEAT_PING, HEARTBEAT_PONG));` with a `/** D-02 */`-style comment: registered once per DO instance (applies to every hibernatable socket), re-armed on every wake because onStart reruns, answered by the runtime without waking the DO or entering onMessage/#send, so it is a deliberate, tested exception to the single-writer invariant (not a second writer). Also add a 4th bullet to the file's header invariant list documenting this exception. Do NOT touch `#send`, `onMessage`, or add any branch that recognizes the ping in onMessage.

    STOP CONDITION: if after the GREEN change the spike still gets no pong or still gets an error frame under `wrangler dev`, do NOT work around it (no onMessage ping branch, no #send pong). Stop, leave the failing test in place, and report in the SUMMARY that Assumption A3 is false so the orchestrator can re-plan D-02/D-03.

    Run the full room-do integration file to prove the ping-by-default harness broke nothing.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project worker room-do && npx tsc -b apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setWebSocketAutoResponse(new WebSocketRequestResponsePair(HEARTBEAT_PING, HEARTBEAT_PONG))" apps/worker/src/room-do.ts` outputs 1
    - `grep -n "HEARTBEAT_PING" apps/worker/src/room-do.ts` shows no match inside `onMessage` (the only non-import occurrence is in onStart)
    - `grep -n "heartbeat: false" apps/worker/src/room-do.test.ts` matches at least once (spike opts out)
    - `npx vitest run --project worker room-do -t "heartbeat spike"` passes all three spike cases
    - `npx vitest run --project worker room-do` passes every pre-existing test
    - `npx tsc -b apps/worker` exits 0
  </acceptance_criteria>
  <done>Auto-response registered in onStart; live wrangler dev answers `__ping__` with `__pong__` and no error/state frame; existing integration suite green under ping-by-default harness.</done>
</task>

<task type="auto">
  <name>Task 3: Structural proof that the heartbeat bypasses the #send chokepoint (D-13)</name>
  <files>apps/worker/src/source-structure.test.ts</files>
  <read_first>
    - apps/worker/src/source-structure.test.ts (whole file; helpers at lines ~100-200, A1-A10)
    - apps/worker/src/room-do.ts (after Task 2)
  </read_first>
  <action>
    Add a new `describe("Phase 5 heartbeat / RT-05 structural audit (D-02, D-13)")` block reusing the existing `listSourceFiles`/`readStripped`/`countMatches` helpers (build a local `strippedByFile` map the same way the D-09 describe does; do not write a new search helper). Add these `it`s, each titled with its decision ID:
    - "P5-1 (D-02): setWebSocketAutoResponse( appears exactly once across worker non-test sources, in room-do.ts, inside onStart": count across all files === 1, file is room-do.ts, and the text between `async onStart(` and the next `async onConnect(` contains it.
    - "P5-2 (D-02/D-13): the #send method body and onMessage body never mention HEARTBEAT_PING, HEARTBEAT_PONG, or the literal __ping__/__pong__": slice room-do.ts from the method DEFINITION `#send(connection: Connection, frame: OutboundFrame)` (not a `this.#send(` call site) up to `async #pushState`, and from `async onMessage(` to `async onClose(`, and assert zero matches of `/HEARTBEAT_P(ING|ONG)|__p[io]ng__/g` in both slices.
    - "P5-3 (D-13): the single-writer counts are unchanged by Phase 5": re-assert exactly 1 `.send(` total (in room-do.ts), exactly 1 `encodeServerMessage(`, exactly 1 `#send(` definition, exactly 1 `type: "joined"` and exactly 1 `type: "state"` in room-do.ts, and zero occurrences of `/type:\s*"(resume|resumed|reconnect|reconnected)"/g` across all worker non-test sources AND in packages/schema/src/messages.ts (read it with readFileSync relative to the repo; no resume-specific frame type exists).
    Do not modify A1-A10. Run the file.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project worker source-structure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'P5-[123]' apps/worker/src/source-structure.test.ts` outputs at least 3
    - `npx vitest run --project worker source-structure` passes, including the unchanged A1-A10 tests
    - `git diff apps/worker/src/source-structure.test.ts` shows only added lines (no removed lines inside A1-A10)
  </acceptance_criteria>
  <done>Structural tests pin the heartbeat outside #send/onMessage, pin setWebSocketAutoResponse to onStart, and prove no resume-specific frame type exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> RoomDO WebSocket | Untrusted text frames, now including the raw heartbeat literal |
| Cloudflare runtime auto-response -> browser | Runtime-generated pong, bypasses DO code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | heartbeat pong frame | mitigate | Pong is the fixed literal `__pong__` registered via setWebSocketAutoResponse; P5-2 structural test proves it never passes through #send and carries no view/state |
| T-05-02 | Denial of Service | ping flood from a client | accept | Auto-response is answered by the runtime without waking the DO or running JS; no DO duration or storage cost; Cloudflare edge limits apply |
| T-05-03 | Tampering | malformed ping reaching onMessage (byte mismatch, wake race) | mitigate | onMessage's existing parseClientMessage rejects non-JSON with `bad_request` and the socket stays usable (existing robustness test + spike case 3) |
| T-05-04 | Elevation of Privilege | a second writer path introduced by the heartbeat | mitigate | P5-3 re-asserts A1/A2/A3/A8 counts unchanged; P5-1 pins setWebSocketAutoResponse to exactly one call in onStart |
</threat_model>

<verification>
- `npx vitest run --project schema constants` green
- `npx vitest run --project worker room-do` green including the heartbeat spike
- `npx vitest run --project worker source-structure` green including P5-1..P5-3
- `npx tsc -b packages/schema apps/worker` clean
- When committing, stage only this plan's files_modified by explicit path (never .planning/config.json, apps/web/next-env.d.ts, or *.tsbuildinfo)
</verification>

<success_criteria>
- Research Wave 0 prerequisites closed: experimental types exposed; wrangler dev proven to honor setWebSocketAutoResponse without onMessage
- D-04 timing values locked as named constants with an invariant test
- D-02/D-13 structural exception documented and enforced by tests
</success_criteria>

<output>
Create `.planning/phases/05-reconnect-session-durability-hardening/05-01-SUMMARY.md` when done
</output>
