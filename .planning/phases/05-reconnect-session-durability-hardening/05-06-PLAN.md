---
phase: 05-reconnect-session-durability-hardening
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - docs/manual-checks/mobile-background.md
  - .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md
autonomous: false
requirements: [RT-04, RT-06, RT-08]

must_haves:
  truths:
    - "D-16: docs/manual-checks/mobile-background.md exists, follows cold-start.md's structure (Why / Procedure / Log / When to re-run / What failure looks like), and covers a real phone backgrounded or locked for 10+ minutes mid-game plus the second-tab `Use this tab` reclaim"
    - "D-16: the automated phase gate is green before sign-off is requested: `npm test`, `npx playwright test`, and per-package `npx tsc -b apps/web packages/rules packages/schema apps/worker`"
    - "D-16: the Log row records the owner's verbatim reply to the checkpoint, quoted exactly, never paraphrased, estimated, or fabricated; if the owner waives the check, the waiver is recorded verbatim the same way (cold-start.md precedent)"
    - "RT-04 is not marked passing unless the owner's reply reports the same seat and turn resumed after 10+ minutes (or explicitly waives)"
  artifacts:
    - path: "docs/manual-checks/mobile-background.md"
      provides: "manual real-phone check for RT-04 with owner sign-off log"
      contains: "## Log"
    - path: ".planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md"
      provides: "per-task verification map filled, nyquist sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "docs/manual-checks/mobile-background.md Log"
      to: "owner checkpoint reply"
      via: "verbatim quote"
      pattern: "\\| 20[0-9]{2}-"
---

<objective>
Close Phase 5's one unautomatable criterion (D-16): write the real-phone "10+ minutes backgrounded" manual check, run the full automated gate, pause for the owner to perform the check on a real device, and record their reply verbatim.

Purpose: CDP freeze and setOffline (05-05) cannot faithfully reproduce OS-level tab suspension on a phone (Research Pitfall 4, Assumption A2). The real phone check converts that assumption into evidence.
Output: docs/manual-checks/mobile-background.md with a signed Log row, and 05-VALIDATION.md completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-reconnect-session-durability-hardening/05-CONTEXT.md
@.planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md
@.planning/phases/05-reconnect-session-durability-hardening/05-05-SUMMARY.md
@docs/manual-checks/cold-start.md
@docs/deployment.md

<interfaces>
Production hosts (docs/deployment.md): web https://games.rogerflores.dev (Vercel, deploys from the repo), Worker games-worker at https://games-worker.rflores3113.workers.dev (`npx wrangler deploy` from apps/worker).
Production must NOT set: SOCKET_STALE_MS, ZOMBIE_SWEEP_INTERVAL_MS (Worker vars) or NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS / NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS (Vercel env). Defaults: ping 20s, pong timeout 10s, stale 75s, sweep 15s.
UI copy to reference in the procedure: "Reconnecting…", "Connected"/"Disconnected", "Waiting for {name} — disconnected", "This room was opened in another tab.", "Use this tab".
User rule (CLAUDE.md): never push unless the owner asks — deployment for this check is the owner's action or explicit instruction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the mobile-background manual check and run the full automated gate</name>
  <files>docs/manual-checks/mobile-background.md</files>
  <read_first>
    - docs/manual-checks/cold-start.md (template to copy structurally)
    - docs/deployment.md (hosts, deploy commands, env var notes)
    - .planning/phases/05-reconnect-session-durability-hardening/05-CONTEXT.md (D-01..D-16)
    - packages/schema/src/constants.ts (production default timings to cite)
  </read_first>
  <action>
    Create docs/manual-checks/mobile-background.md titled "# Mobile Background Check (RT-04, RT-06, RT-08)" with exactly these sections in cold-start.md's style:
    - "## Why this check exists": the product's primary path is friends on a voice call with the game on a phone that gets locked or switched away from (05-CONTEXT specifics); iOS/Android suspend tabs and silently half-open sockets; CI's CDP freeze and setOffline (e2e/hanabi-realtime.spec.ts Phase 5 tests) do not reproduce real OS suspension (Research Pitfall 4, A1/A2); cite the production timings (ping 20s, pong timeout 10s, server stale 75s, sweep 15s) so the observer knows what "about a minute" means.
    - "## Procedure": numbered steps. (1) Confirm production Worker and Vercel builds include Phase 5 and that no heartbeat override vars are set in either (list the four names). (2) Laptop creates a room at https://games.rogerflores.dev; phone (real device, mobile browser, cellular or wifi noted) joins; start the game and take at least one action so it is mid-game. (3) Make it the PHONE's turn. (4) Lock the phone or switch to another app for at least 10 minutes of stopwatch time. (5) On the laptop, within ~2 minutes confirm the phone's seat shows "Disconnected" and the turn indicator reads "Waiting for {name} — disconnected", and that nothing about the game changes while waiting. (6) Unlock/return to the browser tab on the phone; start a stopwatch; record seconds until the "Reconnecting…" banner (if shown at all) clears and the phone shows "Your turn" in the same seat with the same hand/clues/deck count and no join form, no reload. (7) Laptop shows "Connected" and "Waiting for {name}". (8) Second-tab handoff (the seat token lives in one browser's storage, so this is done within the phone's browser): on the phone, open the room link in a second tab; confirm the first tab shows "This room was opened in another tab." with "Use this tab"; tap "Use this tab" in the first tab and confirm it reclaims the seat while the laptop still shows exactly one hand for that player. (9) Append a row to the Log below with real values — do not estimate or backfill.
    - "## Log" table with columns: Date | Device / browser | Minutes backgrounded | Seconds to seated after return | Same seat + turn? | Teammate saw Disconnected? | Use this tab OK? | Owner's verbatim reply. Leave the table with only the header row (Task 3 adds the row).
    - "## When to re-run": after Phase 6 (board rewrite), after any change to room-socket.ts / heartbeat constants / RoomDO onAlarm, after a partysocket/partyserver/wrangler upgrade, and whenever a friend reports losing their seat.
    - "## What failure looks like": phone lands on the join form or a new seat; phone stuck on "Reconnecting…" or "Connecting to room…" for more than ~10s after return; laptop never shows "Disconnected" for a phone gone 10 minutes, or shows it for a player who is present; the turn was skipped or the game changed while the phone was away; "Use this tab" produced two hands for one player or the tabs flipped back on their own. End with "If any of these are observed, do not mark RT-04 as passing".

    Then run the full gate from the repo root, sequentially, with no stray dev servers on ports 3100/8787: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`. All three must pass before Task 2. If any fails, fix within the owning plan's scope or stop and report — do not request sign-off on a red gate.
  </action>
  <verify>
    <automated>cd /home/rflor/games && test -f docs/manual-checks/mobile-background.md && grep -c "^## " docs/manual-checks/mobile-background.md && npm test && npx playwright test && npx tsc -b apps/web packages/rules packages/schema apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^## (Why this check exists|Procedure|Log|When to re-run|What failure looks like)$" docs/manual-checks/mobile-background.md | wc -l` outputs 5
    - `grep -n "10 minutes" docs/manual-checks/mobile-background.md` and `grep -n "Use this tab" docs/manual-checks/mobile-background.md` match
    - `grep -n "do not estimate or backfill" docs/manual-checks/mobile-background.md` matches
    - `grep -n "Owner's verbatim reply" docs/manual-checks/mobile-background.md` matches the Log header
    - `npm test`, `npx playwright test`, and the per-package tsc command all exit 0
  </acceptance_criteria>
  <done>The manual check document exists and the full automated gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner runs the real-phone background check</name>
  <files>docs/manual-checks/mobile-background.md (read only during this checkpoint)</files>
  <read_first>
    - docs/manual-checks/mobile-background.md
  </read_first>
  <action>Present the what-built and how-to-verify below to the owner and wait. Do not deploy, push, or fill in the Log. Capture the owner's reply exactly as sent for Task 3. This checkpoint is never auto-approved (workflow.auto_advance does not apply).</action>
  <verify>
    <human-check>Owner performs docs/manual-checks/mobile-background.md on a real phone and replies with results or an explicit waiver</human-check>
  </verify>
  <done>The owner's own reply (results or waiver) has been received and is held verbatim for Task 3.</done>
  <what-built>Phase 5 reconnect hardening: server heartbeat auto-response and zombie sweep, client resume-on-visible/online with heartbeat, "Reconnecting…" banner with disabled controls, per-seat Connected/Disconnected on the board with "Waiting for {name} — disconnected", and "Use this tab" reclaim. Automated gate (npm test, npx playwright test, per-package tsc) is green. The manual procedure is at docs/manual-checks/mobile-background.md.</what-built>
  <how-to-verify>
    1. Deploy Phase 5 to production if it is not already live: Worker via `npx wrangler deploy` from apps/worker, web via your usual Vercel production deploy (Claude will not push or deploy without your instruction). Confirm none of SOCKET_STALE_MS, ZOMBIE_SWEEP_INTERVAL_MS, NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS, NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS are set in production.
    2. Follow every numbered step in docs/manual-checks/mobile-background.md with a real phone and a laptop, backgrounding or locking the phone for at least 10 minutes by stopwatch.
    3. Reply with: date, phone model and browser, minutes backgrounded, seconds until seated after returning, whether the same seat and turn came back, whether the laptop showed "Disconnected" and "Waiting for {name} — disconnected", whether "Use this tab" worked without a duplicate hand, and anything odd. Or reply with an explicit waiver and its reason.
  </how-to-verify>
  <acceptance_criteria>
    - The owner has replied in their own words; the reply text is captured exactly as sent for Task 3
  </acceptance_criteria>
  <resume-signal>Paste your results in your own words (or an explicit waiver with a reason). Your reply is recorded verbatim.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the owner's verbatim sign-off and complete the validation contract</name>
  <files>docs/manual-checks/mobile-background.md, .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md</files>
  <read_first>
    - docs/manual-checks/mobile-background.md (after Task 1)
    - .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md
    - .planning/phases/05-reconnect-session-durability-hardening/05-01-SUMMARY.md through 05-05-SUMMARY.md (task IDs and commands actually run)
  </read_first>
  <action>
    Append exactly one row to the Log table in docs/manual-checks/mobile-background.md. Fill the structured columns ONLY with values the owner explicitly stated in the Task 2 reply; any value they did not state is written as "not stated". The "Owner's verbatim reply" column contains the owner's reply quoted exactly (replace literal `|` characters with `\|` and newlines with `<br>` so the table renders; change nothing else). Never invent, round, or paraphrase. If the reply is a waiver, write "WAIVED" in the structured columns and still quote the reply verbatim, mirroring cold-start.md's waiver row. If the reply reports any item from "What failure looks like", do NOT mark RT-04 passing: note it in the SUMMARY as a blocking gap for /gsd:verify-work.

    In 05-VALIDATION.md: replace the four "(filled by planner)" rows' Task ID/Plan/Wave cells with the real task references (RT-04: 05-02 T3, 05-03 T1/T2, 05-05 T2, 05-06 T2; RT-05: 05-01 T3, 05-02 T2/T3; RT-06: 05-02 T3, 05-04 T1/T2, 05-05 T2; RT-08: 05-02 T3, 05-03 T2, 05-04 T3, 05-05 T3) and set Status to green for rows whose commands passed in Task 1; tick the Wave 0 checklist items that were delivered (tsconfig types, spike test, manual-check doc); tick the Validation Sign-Off boxes that are true; set frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, and `status: complete` only if the owner did not report a failure; set "Approval:" to "approved — owner sign-off recorded verbatim in docs/manual-checks/mobile-background.md on {date}" (or "blocked — see SUMMARY" on failure).
  </action>
  <verify>
    <automated>cd /home/rflor/games && grep -cE "^\| 20[0-9]{2}-[0-9]{2}-[0-9]{2} " docs/manual-checks/mobile-background.md && grep -n "nyquist_compliant:" .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md && ! grep -n "(filled by planner)" .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| 20[0-9]{2}-[0-9]{2}-[0-9]{2} " docs/manual-checks/mobile-background.md` outputs 1
    - the Log row's last cell contains the owner's reply text exactly (spot-check: a distinctive phrase from the reply is found by `grep -F`)
    - `grep -c "(filled by planner)" .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md` outputs 0
    - `grep -n "Approval:" .planning/phases/05-reconnect-session-durability-hardening/05-VALIDATION.md` shows approved or blocked, not pending
  </acceptance_criteria>
  <done>Owner sign-off is recorded verbatim and the validation contract reflects the real outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner reply -> planning record | Human evidence entering the repository |
| production config -> deployed app | Test-only timing vars must be absent in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-23 | Repudiation | fabricated or paraphrased sign-off | mitigate | Task 3 quotes the reply verbatim, fills unstated fields with "not stated", blocking checkpoint cannot be auto-approved |
| T-05-24 | Denial of Service | test timing overrides present in production causing false disconnects | mitigate | procedure step 1 and checkpoint step 1 require confirming the four override vars are unset in Worker and Vercel |
| T-05-25 | Tampering | unreviewed deploy/push by the executor | mitigate | deployment is the owner's action per CLAUDE.md "never push unless asked"; the checkpoint states Claude will not deploy without instruction |
</threat_model>

<verification>
- Full gate green: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`
- Log row present with verbatim owner reply
- 05-VALIDATION.md no longer pending
- Stage only this plan's files_modified by explicit path when committing
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 1's literal "10+ minutes backgrounded" is backed by recorded real-device evidence or an explicit verbatim waiver
- D-16 gate satisfied
</success_criteria>

<output>
Create `.planning/phases/05-reconnect-session-durability-hardening/05-06-SUMMARY.md` when done
</output>
