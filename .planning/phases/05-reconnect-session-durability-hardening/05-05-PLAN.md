---
phase: 05-reconnect-session-durability-hardening
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-04"]
files_modified:
  - playwright.config.ts
  - e2e/helpers.ts
  - e2e/hanabi-realtime.spec.ts
  - e2e/seat-takeover.spec.ts
autonomous: true
requirements: [RT-04, RT-05, RT-06, RT-08]

must_haves:
  truths:
    - "D-14/RT-04: with the network dropped mid-game via context.setOffline, the dropped player sees `Reconnecting…` over the last board with Play/Discard/Give clue disabled; after setOffline(false) the banner disappears and the player is back in the SAME seat (same other-hand-{seatId} testid on the other page) with the same turn, clue tokens, and deck count"
    - "D-14/RT-06/D-08: during that drop the other player sees `seat-status-{seatId}` flip to `Disconnected` and, when the absent player is active, `Waiting for {name} — disconnected`; clue tokens and deck count do not change while they are away; after return it shows `Connected` and `Waiting for {name}` / `Your turn` again"
    - "D-14/RT-04: a page frozen via CDP Page.setWebLifecycleState with document visibility emulated hidden is detected as Disconnected by the other player, and on active + visible it returns to the same seat and turn without a reload"
    - "D-14/RT-08/D-10/D-11: mid-game, a second tab on the same seat supersedes the first (which shows `This room was opened in another tab.` and `Use this tab`); clicking `Use this tab` reclaims the seat and supersedes the second tab; the other player's view keeps exactly one other hand with the same seatId throughout, and the two tabs do not flip back on their own over several heartbeat intervals"
    - "D-15: e2e runs use shortened, injected timing (web NEXT_PUBLIC_HEARTBEAT_* and worker --var SOCKET_STALE_MS/ZOMBIE_SWEEP_INTERVAL_MS) so no spec waits real minutes"
  artifacts:
    - path: "playwright.config.ts"
      provides: "injected Phase 5 timing for both dev servers"
      contains: "SOCKET_STALE_MS"
    - path: "e2e/helpers.ts"
      provides: "startTwoPlayerGame, emulateVisibility, freezePage/resumePage helpers"
      contains: "Page.setWebLifecycleState"
    - path: "e2e/hanabi-realtime.spec.ts"
      provides: "RT-04/RT-06 network-drop and backgrounded-tab proofs"
      contains: "setOffline"
    - path: "e2e/seat-takeover.spec.ts"
      provides: "RT-08 mid-game second tab + Use this tab proof"
      contains: "use-this-tab-button"
  key_links:
    - from: "e2e/hanabi-realtime.spec.ts"
      to: "apps/web/components/HanabiBoard.tsx testids"
      via: "reconnecting-banner, seat-status-{seatId}, turn-indicator"
      pattern: "seat-status-"
    - from: "playwright.config.ts"
      to: "apps/worker RoomDO Env overrides"
      via: "wrangler dev --var"
      pattern: "--var SOCKET_STALE_MS"
---

<objective>
Prove the browser-visible Phase 5 behavior end to end against the real worker and web app (D-14): network drop and return, a frozen/backgrounded tab, per-seat disconnected indicators with pause-in-place, and second-tab supersede plus "Use this tab" reclaim — by extending the existing Playwright specs, not adding a parallel harness.

Purpose: Unit and socket tests prove the halves; only a real browser proves partysocket, the visibility/online listeners, the store, and the board work together.
Output: config timing injection, shared helpers, three new e2e tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-reconnect-session-durability-hardening/05-CONTEXT.md
@.planning/phases/05-reconnect-session-durability-hardening/05-RESEARCH.md
@.planning/phases/05-reconnect-session-durability-hardening/05-02-SUMMARY.md
@.planning/phases/05-reconnect-session-durability-hardening/05-04-SUMMARY.md

<interfaces>
From playwright.config.ts (current): WEB_PORT 3100, WORKER_PORT 8787, timeout 30_000, expect.timeout 10_000, single chromium project,
  webServer[0] command `npm run dev --workspace apps/web -- -p 3100`, webServer[1] command `npx wrangler dev --port 8787` cwd apps/worker, both reuseExistingServer: !process.env.CI

From e2e/helpers.ts: createRoom(page, { name, variant? }) -> code; joinAs(context, code, name) -> Page; expectSeatCount(page, n)

From e2e/hanabi-realtime.spec.ts: selectAClueValueThatTouchesSomething(page); RT-01 and RT-03 tests show the start-game + active/passive detection pattern (turn-indicator === "Your turn"), clue flow (clue-target-*, clue-value-*, give-clue-button), and seat identity via the OTHER page's `other-hand-{seatId}` testid

From e2e/seat-takeover.spec.ts: selfSeatRow/selfSeatId helpers (lobby only); second tab = contextB.newPage() sharing localStorage; superseded text assertion

UI testids/copy (05-04): reconnecting-banner ("Reconnecting…"), seat-status-{seatId} with data-connected="true|false" and text Connected/Disconnected, turn-indicator ("Your turn" | "Waiting for X" | "Waiting for X — disconnected"), use-this-tab-button ("Use this tab"), play-button, discard-button, give-clue-button, clue-tokens, deck-count, own-hand, other-hand-{seatId}

Injected e2e timing (D-15): web NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS=1000, NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS=1000; worker SOCKET_STALE_MS=5000, ZOMBIE_SWEEP_INTERVAL_MS=1000 (stale 5s > 2x interval 1s with throttling-free visible test pages; disconnected visible within ~6-7s)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timing injection and shared e2e helpers</name>
  <files>playwright.config.ts, e2e/helpers.ts</files>
  <read_first>
    - playwright.config.ts
    - e2e/helpers.ts
    - e2e/hanabi-realtime.spec.ts (RT-03 test, for the start-game pattern to extract)
    - apps/web/lib/room-socket.ts (confirm NEXT_PUBLIC_HEARTBEAT_* names)
    - apps/worker/src/room-do.ts (confirm Env override names)
  </read_first>
  <action>
    playwright.config.ts: add constants `E2E_HEARTBEAT_INTERVAL_MS = "1000"`, `E2E_HEARTBEAT_PONG_TIMEOUT_MS = "1000"`, `E2E_SOCKET_STALE_MS = 5000`, `E2E_ZOMBIE_SWEEP_INTERVAL_MS = 1000` with a D-15 comment. Web server: add `env: { ...process.env, NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS: E2E_HEARTBEAT_INTERVAL_MS, NEXT_PUBLIC_HEARTBEAT_PONG_TIMEOUT_MS: E2E_HEARTBEAT_PONG_TIMEOUT_MS }` (spread process.env explicitly so PATH survives regardless of Playwright's merge behavior; if the type of `env` rejects undefined values, filter them). Worker server: change the command to `npx wrangler dev --port ${WORKER_PORT} --var SOCKET_STALE_MS:${E2E_SOCKET_STALE_MS} --var ZOMBIE_SWEEP_INTERVAL_MS:${E2E_ZOMBIE_SWEEP_INTERVAL_MS}`. Add a comment warning that with reuseExistingServer locally, an already-running `next dev`/`wrangler dev` started WITHOUT these values will make the Phase 5 specs time out — stop stray servers on 3100/8787 before running. Leave the REMOTE_BASE_URL branch unchanged (Phase 5 timing specs are skipped remotely, see Task 2).

    e2e/helpers.ts: add
    - `startTwoPlayerGame(hostPage: Page, browser: Browser, names = { host: "Roger", guest: "Bianca" })` -> `{ code, contextB, pageB, activePage, passivePage }`: createRoom, joinAs in a new context, expectSeatCount 2, click start-game, wait for own-hand on both, pick active via turn-indicator === "Your turn" (extracted from the RT-03 pattern; do not change RT-01/RT-03 themselves).
    - `OTHER_HAND_SELECTOR = '[data-testid^="other-hand-"]:not([data-testid^="other-hand-card-"])'` (exported const; the bare prefix also matches every `other-hand-card-{id}` card) and `seatIdOfOtherPlayer(observer: Page): Promise<string>` reading the first OTHER_HAND_SELECTOR element's testid and stripping the `other-hand-` prefix.
    - `emulateVisibility(page: Page, state: "hidden" | "visible")`: page.evaluate that redefines `document.visibilityState` and `document.hidden` via Object.defineProperty (configurable: true) and dispatches `new Event("visibilitychange")` on document.
    - `freezePage(page)` / `resumePage(page)`: create a CDP session via `page.context().newCDPSession(page)` and send `Page.setWebLifecycleState` with state "frozen" / "active" (return/accept the session so resume reuses it).
    Each helper gets a short doc comment; freeze/visibility helpers cite D-14 and Research Pitfall 4 (assert observable outcomes, not whether the socket actually closed).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test --list && grep -n "Page.setWebLifecycleState" e2e/helpers.ts && grep -n "\-\-var SOCKET_STALE_MS" playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "NEXT_PUBLIC_HEARTBEAT_INTERVAL_MS" playwright.config.ts` matches
    - `grep -n -- "--var SOCKET_STALE_MS" playwright.config.ts` and `grep -n -- "--var ZOMBIE_SWEEP_INTERVAL_MS" playwright.config.ts` match
    - `grep -E "export async function (startTwoPlayerGame|seatIdOfOtherPlayer|emulateVisibility|freezePage|resumePage)" e2e/helpers.ts | wc -l` outputs 5
    - `npx playwright test --list` exits 0 (config and helpers compile)
  </acceptance_criteria>
  <done>E2E servers start with shortened injected timing and specs share start-game, visibility, and freeze helpers.</done>
</task>

<task type="auto">
  <name>Task 2: RT-04/RT-06 proofs — network drop and frozen background tab mid-game</name>
  <files>e2e/hanabi-realtime.spec.ts</files>
  <read_first>
    - e2e/hanabi-realtime.spec.ts (whole file)
    - e2e/helpers.ts (after Task 1)
    - apps/web/components/HanabiBoard.tsx (testids after 05-04)
    - .planning/phases/05-reconnect-session-durability-hardening/05-RESEARCH.md (Pitfall 4, Assumptions A1/A2)
  </read_first>
  <action>
    Add a new `test.describe("Phase 5 reconnect hardening (RT-04 + RT-06 + D-14)")` with `test.skip(!!process.env.PLAYWRIGHT_BASE_URL, "needs locally injected heartbeat timing (D-15)")` and `test.setTimeout(90_000)` for both tests.

    Test A "RT-04/RT-06: a network drop mid-game shows Reconnecting…, the table pauses in place for the other player, and the player returns to the same seat and turn":
    1. startTwoPlayerGame; make the game genuinely mid-game by giving one clue from activePage (selectAClueValueThatTouchesSomething), then wait until passivePage turn-indicator is "Your turn". The now-active page is the DROPPING page (so the waiting text applies); the other is the OBSERVER. Use the dropping page's browser context for setOffline (host page uses the default `context` fixture; guest uses contextB — pick the right one).
    2. Record on the observer: droppedSeatId = seatIdOfOtherPlayer(observer); dropped display name from the observer's turn-indicator (text after "Waiting for "); clue-tokens and deck-count texts. Record on the dropping page: clue-tokens, deck-count, own-hand slot count.
    3. `await droppingContext.setOffline(true)`.
    4. Dropping page: expect reconnecting-banner visible with text "Reconnecting…" (timeout 15_000); expect play-button, discard-button, give-clue-button disabled; expect own-hand still visible (last view kept, not the "Connecting to room" page: `getByText(/Connecting to room/)` has count 0).
    5. Observer: expect `seat-status-${droppedSeatId}` to have attribute data-connected "false" and text containing "Disconnected" (timeout 20_000); expect turn-indicator toHaveText `Waiting for ${name} — disconnected`; expect clue-tokens and deck-count unchanged (D-08 pause in place).
    6. `await droppingContext.setOffline(false)`.
    7. Dropping page: expect reconnecting-banner count 0 (timeout 15_000); turn-indicator "Your turn"; clue-tokens/deck-count equal the recorded values; own-hand slot count unchanged.
    8. Observer: seat-status data-connected "true" with "Connected"; turn-indicator `Waiting for ${name}` exactly; seatIdOfOtherPlayer(observer) === droppedSeatId (same seat, not a new one).
    9. Close contextB.

    Test B "RT-04: a frozen, hidden tab is detected as disconnected and resumes its seat on return without a reload":
    1. startTwoPlayerGame; SLEEPER = pageB (guest), OBSERVER = hostPage. Record sleeperSeatId from the observer, observer turn-indicator text, and sleeper turn-indicator text.
    2. emulateVisibility(pageB, "hidden"); freezePage(pageB).
    3. Observer: seat-status-{sleeperSeatId} data-connected "false" (timeout 25_000). This is the server zombie sweep (D-03) catching a socket whose JS is frozen — per Pitfall 4 do not assert whether the socket itself closed.
    4. resumePage(pageB); emulateVisibility(pageB, "visible").
    5. Sleeper: without calling reload, expect reconnecting-banner count 0 and own-hand visible (timeout 15_000) and turn-indicator equals its recorded text.
    6. Observer: seat-status data-connected "true"; seatIdOfOtherPlayer(observer) === sleeperSeatId; observer turn-indicator equals its recorded text.
    If CDP freeze is unsupported in the installed Chromium (the send throws), fall back to only emulateVisibility hidden + `contextB.setOffline(true)` for the sleep window, and record the fallback in the SUMMARY (A2 risk is backstopped by the 05-06 manual phone check).

    Do not modify the existing RT-01/RT-03 tests. Never call page.reload in these tests.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test hanabi-realtime</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setOffline(true)" e2e/hanabi-realtime.spec.ts` and `grep -n "setOffline(false)" e2e/hanabi-realtime.spec.ts` match
    - `grep -n "— disconnected" e2e/hanabi-realtime.spec.ts` matches
    - `grep -n "freezePage(" e2e/hanabi-realtime.spec.ts` matches
    - the new describe block contains no `reload(` call (`awk '/Phase 5 reconnect hardening/,0' e2e/hanabi-realtime.spec.ts | grep -c "reload("` outputs 0)
    - `npx playwright test hanabi-realtime` passes all tests (RT-01, RT-03, and the two new Phase 5 tests)
  </acceptance_criteria>
  <done>Real-browser proof that drop/return and freeze/resume keep the same seat and turn, with truthful indicators and a paused table.</done>
</task>

<task type="auto">
  <name>Task 3: RT-08 proof — mid-game second tab supersede and Use this tab reclaim without duplication</name>
  <files>e2e/seat-takeover.spec.ts</files>
  <read_first>
    - e2e/seat-takeover.spec.ts (whole file)
    - e2e/helpers.ts (after Task 1)
    - apps/web/app/room/[code]/RoomClient.tsx (superseded branch after 05-04)
  </read_first>
  <action>
    Add a second test in the existing describe (do not modify the existing test): "RT-08 (D-10/D-11/D-12): mid-game, a second tab supersedes the first, Use this tab reclaims the seat, and the seat is never duplicated or ping-ponged". Skip when PLAYWRIGHT_BASE_URL is set; test.setTimeout(90_000).
    1. startTwoPlayerGame(hostPage, browser). Observer = hostPage; the seat under test is pageB's. Record bSeatId = seatIdOfOtherPlayer(hostPage), pageB own-hand slot count, pageB turn-indicator text, and hostPage count of `OTHER_HAND_SELECTOR` elements (expect 1).
    2. pageB2 = contextB.newPage(); goto `/room/${code}`. Expect pageB2 own-hand visible (no join form: `getByLabel("Your name")` count 0) and its turn-indicator equals pageB's recorded text.
    3. pageB: expect text "This room was opened in another tab." and `use-this-tab-button` visible and enabled.
    4. hostPage: OTHER_HAND_SELECTOR count still 1; seatIdOfOtherPlayer(hostPage) === bSeatId; seat-status-{bSeatId} data-connected "true" (winner stays connected — CR-01, D-12).
    5. Click pageB `use-this-tab-button`. Expect pageB own-hand visible and the superseded text gone (timeout 15_000); expect pageB2 shows "This room was opened in another tab." and its own `use-this-tab-button`.
    6. hostPage: OTHER_HAND_SELECTOR count still 1, same bSeatId, data-connected "true".
    7. No ping-pong (D-11): wait 5_000ms (more than 4 injected heartbeat intervals and one full injected stale window, justified in a comment as a deliberate negative-window wait) and then assert pageB still shows own-hand, pageB2 still shows the superseded text, and hostPage OTHER_HAND_SELECTOR count still 1 with data-connected "true".
    8. Close contextB.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test seat-takeover</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "use-this-tab-button" e2e/seat-takeover.spec.ts` matches at least twice
    - `grep -n "RT-08" e2e/seat-takeover.spec.ts` matches the new test title
    - `npx playwright test seat-takeover` passes both the existing RT-07 test and the new RT-08 test
    - `npx playwright test` (full e2e suite) passes
  </acceptance_criteria>
  <done>Second-tab supersede and one-click reclaim are proven mid-game with one seat, one hand, a connected winner, and no automatic flip-back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> dev servers | Test-only timing overrides injected via env/--var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | e2e timing overrides leaking into production config | mitigate | overrides live only in playwright.config.ts webServer commands/env; remote runs (PLAYWRIGHT_BASE_URL) skip Phase 5 timing specs and start no local servers; 05-06 gate confirms Vercel/Worker production vars are unset |
| T-05-21 | Spoofing | second tab duplicating or stealing a seat (RT-08) | mitigate | Task 3 asserts one other-hand with the same seatId and a connected winner before and after reclaim and across a negative window |
| T-05-22 | Repudiation | flaky proof masking a real regression (Pitfall 4) | mitigate | assertions target observable outcomes with bounded timeouts; CDP-unsupported fallback is recorded, and the real-phone manual check (05-06) backstops A1/A2 |
</threat_model>

<verification>
- `npx playwright test` green locally with no stray dev servers on 3100/8787
- `npm test` still green
- Stage only this plan's files_modified by explicit path when committing
</verification>

<success_criteria>
- ROADMAP Phase 5 success criteria 1, 3, and 4 demonstrated in a real browser against the real worker
- D-14 satisfied by extending the existing specs, no parallel harness
</success_criteria>

<output>
Create `.planning/phases/05-reconnect-session-durability-hardening/05-05-SUMMARY.md` when done
</output>
