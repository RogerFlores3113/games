---
phase: 03-hanabi-rules-engine
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - packages/rules/src/hanabi/projection.ts
  - packages/rules/src/hanabi/projection.test.ts
  - packages/rules/src/hanabi/hanabi-leak-check.ts
  - packages/rules/src/hanabi/hanabi-leak-check.test.ts
  - packages/rules/src/hanabi/adapter.ts
  - packages/rules/src/index.ts
  - packages/rules/src/adapter.test.ts
autonomous: true
requirements: [HIDE-05, RULES-19, FDN-02]

must_haves:
  truths:
    - "D-07: a card in the viewer's own hand structurally lacks suit and rank keys — the object literal never mentions them — while other seats' hands carry full identity"
    - "D-07: toPlayerView is built by field-by-field whitelist construction with no spread, delete, Object.assign or omit helper anywhere in the function"
    - "D-07: an unseated or unknown viewer fails closed — every hand renders hidden, never more than the least-privileged seat sees"
    - "D-21: the Hanabi leak checker compares structurally and by typed identity rather than scanning for a bare number, closing finding WR-03, because ranks 1-5 legitimately collide with token counts and scores"
    - "D-22: a canary suite of deliberately leaky projections proves the checker can fail, and every condition-guarded assertion loop also asserts it ran at least once, closing finding WR-02"
    - "D-03: hanabiGame implements the unmodified five-member GameAdapter interface"
    - "D-01/FDN-02: the hanabi modules import nothing from @games/schema, apps/worker or apps/web, and packages/rules still declares zero runtime dependencies"
    - "D-02: the forehead-card toy, its leak checker and its tests remain present and green"
    - "RULES-19: two games created from the same seed produce deep-equal initial state, proven by the shared adapter conformance suite"
    - "HIDE-05: applyAction never throws for any JSON value, proven by the shared conformance suite's hostile-payload property"
  artifacts:
    - path: "packages/rules/src/hanabi/projection.ts"
      provides: "toHanabiPlayerView whitelist per-seat projection"
      contains: "export function toHanabiPlayerView"
    - path: "packages/rules/src/hanabi/hanabi-leak-check.ts"
      provides: "secretsForHanabiSeat and checkHanabiViewForLeaks"
      contains: "export function checkHanabiViewForLeaks"
    - path: "packages/rules/src/hanabi/adapter.ts"
      provides: "hanabiGame, the five-member GameAdapter implementation"
      contains: "export const hanabiGame"
  key_links:
    - from: "packages/rules/src/hanabi/adapter.ts"
      to: "packages/rules/src/hanabi/projection.ts"
      via: "toPlayerView delegates to toHanabiPlayerView, the only exit point from state"
      pattern: "toHanabiPlayerView"
    - from: "packages/rules/src/index.ts"
      to: "packages/rules/src/hanabi/adapter.ts"
      via: "barrel export of hanabiGame for Phase 4's registration point"
      pattern: "export \\{ hanabiGame \\}"
    - from: "packages/rules/src/adapter.test.ts"
      to: "packages/rules/src/hanabi/adapter.ts"
      via: "second describeAdapterConformance call site"
      pattern: "describeAdapterConformance\\(\"hanabi\""
---

<objective>
Build the redaction layer and close the adapter: the per-seat whitelist projection, a leak checker generalized for `{suit, rank}` identities (closing WR-03), its canary suite (closing WR-02), the composed `hanabiGame` adapter, and the barrel and conformance wiring.

Purpose: this is the single most load-bearing correctness pattern in the project. Phase 2 proved it against star names that cannot collide with anything; Hanabi's ranks are the integers 1-5, which collide with clue tokens, fuses, scores and stack heights, so the checker's detection method must change even though its architecture does not.
Output: `projection.ts`, `hanabi-leak-check.ts`, `hanabi/adapter.ts`, updated barrel and conformance suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hanabi-rules-engine/03-CONTEXT.md
@.planning/phases/03-hanabi-rules-engine/03-RESEARCH.md
@.planning/phases/03-hanabi-rules-engine/03-PATTERNS.md
@.planning/phases/03-hanabi-rules-engine/03-03-SUMMARY.md
@.planning/phases/02-per-seat-redaction-contract/02-REVIEW.md
@packages/rules/src/forehead-card.ts
@packages/rules/src/forehead-card-leak-check.ts
@packages/rules/src/adapter.ts
@packages/rules/src/index.ts

<interfaces>
<!-- Plans 01-03 created variant/state/deck/history/clue-facts/legality/actions/endgame. -->

NEW — packages/rules/src/hanabi/projection.ts:
```typescript
export function toHanabiPlayerView(state: HanabiState, seatId: string): HanabiView;
```

NEW — packages/rules/src/hanabi/hanabi-leak-check.ts:
```typescript
export interface HanabiSeatSecrets {
  /** The viewer's own cards — identity that must never appear in their view. */
  readonly ownCards: readonly { readonly id: string; readonly suit: Suit; readonly rank: Rank }[];
  /** The {suit,rank} pairs the viewer MAY legitimately see (other seats' hands,
   *  the discard pile, played stacks), as a multiset of "suit:rank" keys. */
  readonly allowedIdentityCounts: Readonly<Record<string, number>>;
  /** Raw strings that must never appear in the serialized view (the server seed). */
  readonly forbiddenTokens: readonly string[];
}

export function secretsForHanabiSeat(
  state: HanabiState, seatId: string, seed?: string,
): HanabiSeatSecrets;

export function checkHanabiViewForLeaks(input: {
  view: unknown; serialized: string; secrets: HanabiSeatSecrets;
}): string[];
```

Leak reason strings this checker emits:
  "structural:own-card-id-has-identity"      — an object whose id matches an own card carries a suit or rank key
  "structural:hidden-card-has-identity"      — an object with hidden === true carries a suit or rank key
  "structural:your-hand-entry-has-identity"  — any entry under yourHand carries a suit or rank key
  "typed:identity-count-exceeded:{suit}:{rank}" — more {suit,rank} pairs appear in the view than the viewer may legitimately see
  "string:forbidden-token:{token}"           — the seed (or another forbidden raw string) appears in the serialized view

NEW — packages/rules/src/hanabi/adapter.ts:
```typescript
export const hanabiGame: GameAdapter<HanabiState, HanabiAction> = {
  id: "hanabi",
  createInitialState,   // variantConfig + dealInitialHands + empty stacks/discard/history, clueTokens 8, fuses 0, finalTurnsRemaining null
  applyAction,          // delegates to applyHanabiAction
  toPlayerView,         // delegates to toHanabiPlayerView
  checkGameEnd,         // delegates to checkHanabiGameEnd
};
```

MODIFIED — packages/rules/src/index.ts, adding alongside the existing
forehead-card block (which stays until Phase 4 deletes the toy):
```typescript
export { hanabiGame } from "./hanabi/adapter";
export { variantConfig, handSizeFor, maxScoreFor, ALL_SUITS, RANKS } from "./hanabi/variant";
export { canPlay, canDiscard, canClue, cardsTouchedByClue } from "./hanabi/legality";
export { currentScore, scoreBand } from "./hanabi/endgame";
export { checkHanabiViewForLeaks, secretsForHanabiSeat } from "./hanabi/hanabi-leak-check";
export type { HanabiAction, HanabiState, HanabiView, HanabiCardView, ClueFacts, Clue } from "./hanabi/state";
export type { Suit, Rank, VariantConfig } from "./hanabi/variant";
export type { HanabiSeatSecrets } from "./hanabi/hanabi-leak-check";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-seat whitelist projection</name>
  <files>packages/rules/src/hanabi/projection.ts, packages/rules/src/hanabi/projection.test.ts</files>
  <read_first>
    - packages/rules/src/forehead-card.ts (lines 14-19 the forbidden-construct header, lines 169-213 toPlayerView's three-part structure — this is the template)
    - packages/rules/src/hanabi/state.ts (HanabiView and HanabiCardView shapes)
    - .planning/phases/03-hanabi-rules-engine/03-PATTERNS.md (projection.ts section)
    - .planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md (D-05 whitelist construction, carried forward verbatim)
  </read_first>
  <behavior>
    - every entry in yourHand has hidden === true and structurally lacks both suit and rank keys, asserted with the `in` operator
    - every entry in yourHand carries its card id and its clue facts
    - every entry in otherHands carries hidden === false plus suit, rank, id and clue facts
    - public fields (stacks, discard, clueTokens, fuses, deckCount, finalTurnsRemaining, activeSeatId, variant, score, history) are identical across all seats' views for the same state
    - the view carries no seed field and no deck contents — deckCount is a number only
    - an unseated viewer (a seat id not in the game) gets yourSeatId null, an empty yourHand, and EVERY hand rendered under otherHands with hidden === true and no suit or rank keys (fails closed)
    - isYourTurn is true only for the active seat
    - toHanabiPlayerView is pure: calling it twice returns deep-equal views and never returns the state object itself
  </behavior>
  <action>
    Create `packages/rules/src/hanabi/projection.ts` exporting `toHanabiPlayerView`. Copy `forehead-card.ts:14-19`'s header comment near-verbatim, updated for Hanabi: object spread, `delete`, `Object.assign`, any omit helper, and assigning null or undefined to a `suit`/`rank` key are all FORBIDDEN in this file; every returned object is built field-by-field from named values so an own-hand card structurally cannot carry a `suit` or `rank` key — not because they were stripped, but because the literal never mentions them.

    Follow the toy's three-part structure exactly: (1) compute the public fields once, before any branch, since they are identical for every seat; (2) a fail-closed branch when the viewer's hand is not found, rendering every hand hidden and `yourSeatId` null; (3) the seated branch, mapping the viewer's own slots to `{ id, hidden: true, facts }` literals and every other seat's slots to `{ id, hidden: false, suit, rank, facts }` literals. Map clue facts into the view's plain-array form field by field. Compute `score` via `currentScore(state)` from `endgame.ts`.

    Create `packages/rules/src/hanabi/projection.test.ts` covering every `<behavior>` bullet, with key-presence assertions written as `expect("suit" in card).toBe(false)` rather than value comparisons, and with a per-seat loop over a dealt 4-player game in each of the three variants (D-10). Any assertion loop over seats or cards must also assert the loop ran a nonzero number of times (D-22/WR-02).
  </action>
  <verify>
    <automated>npx vitest run --project rules packages/rules/src/hanabi/projection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project rules packages/rules/src/hanabi/projection.test.ts` exits 0
    - `grep -vE '^\s*[/*]' packages/rules/src/hanabi/projection.ts | grep -cE '\.\.\.|delete |Object\.assign|omit\(|suit: (null|undefined)|rank: (null|undefined)'` returns 0
    - projection.test.ts contains `"suit" in` and `"rank" in` assertions expecting false for own-hand cards
    - projection.test.ts contains an unseated-viewer test asserting every rendered card has hidden === true
    - projection.test.ts contains at least one `toBeGreaterThan(0)` non-vacuousness assertion on a loop counter
    - `npx vitest run --project rules` exits 0
  </acceptance_criteria>
  <done>Every seat's view carries full identity for other hands and structurally no identity for its own, an unseated viewer sees strictly less, and no forbidden construct appears in the projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalized leak checker and canary suite</name>
  <files>packages/rules/src/hanabi/hanabi-leak-check.ts, packages/rules/src/hanabi/hanabi-leak-check.test.ts</files>
  <read_first>
    - packages/rules/src/forehead-card-leak-check.ts (whole file — the structural walk with the `in` operator is kept; the substring detection method is what changes)
    - packages/rules/src/forehead-card-leak-check.test.ts (the canary suite shape and the clean-baseline-first convention)
    - .planning/phases/02-per-seat-redaction-contract/02-REVIEW.md (WR-02 and WR-03 — both closed by this task)
    - packages/rules/src/hanabi/projection.ts (the real view shape the checker walks)
  </read_first>
  <behavior>
    - clean baseline: the real toHanabiPlayerView output for every seat of a dealt game in all three variants produces zero leak reasons
    - Canary A: an own-hand entry carrying suit and rank reports "structural:your-hand-entry-has-identity"
    - Canary B: an own-hand entry carrying suit: null reports a leak (key presence, not truthiness)
    - Canary C: an own-hand entry carrying suit: undefined reports a leak even though JSON.stringify drops the key — proving the checker is structural, not string-based
    - Canary D: an own card's id appearing under otherHands with its true suit and rank reports "structural:own-card-id-has-identity"
    - Canary E: a card with hidden === true carrying a rank reports "structural:hidden-card-has-identity"
    - Canary F: the server seed appearing under an arbitrary debug key reports "string:forbidden-token:{seed}"
    - Canary G: the undealt deck dumped under a debug key reports at least one "typed:identity-count-exceeded:" reason, because it pushes visible identity counts past what the viewer may legitimately see
    - Canary H: a view carrying one extra copy of an own card's {suit, rank} under a benign-looking key reports a typed count-exceeded reason even though that pair also legitimately appears elsewhere
    - every canary asserts the specific expected reason is CONTAINED in the returned array
  </behavior>
  <action>
    Create `packages/rules/src/hanabi/hanabi-leak-check.ts` exporting the three names in `<interfaces>`. The header comment must state the D-21/WR-03 rationale explicitly: Hanabi identities are the integers 1-5 and a small suit vocabulary, which legitimately collide with clue tokens, fuses, scores and stack heights, so a raw substring scan for a rank would produce both false positives and false negatives; detection is therefore structural (key presence on own-hand and own-id-bearing objects) plus typed multiset comparison, with raw-string scanning retained only for the server seed, which cannot legitimately appear in any view (D-14).

    Keep `forehead-card-leak-check.ts`'s recursive `walkStructural` architecture and its `in`-operator discipline (never truthiness — `JSON.stringify` drops undefined-valued keys, so a truthiness check would miss `suit: undefined`). Change the leak conditions to: any object reachable under a `yourHand` key that has `"suit" in obj` or `"rank" in obj`; any object with `obj.hidden === true` that has either key; any object whose `id` equals one of `secrets.ownCards`' ids that has either key. Add the typed multiset check as a separate pass: collect every object in the view having both a `"suit"` and a `"rank"` key into counts keyed `"{suit}:{rank}"`, and emit `typed:identity-count-exceeded:{suit}:{rank}` for any key whose observed count exceeds `secrets.allowedIdentityCounts[key] ?? 0`. This is the sound replacement for a bare-number scan: duplicate identities legitimately exist in a Hanabi deck, so only an excess count proves a leak.

    `secretsForHanabiSeat(state, seatId, seed)` computes `ownCards` from that seat's slots, and `allowedIdentityCounts` as the multiset of `{suit, rank}` pairs the viewer may legitimately see: every other seat's hand cards, the whole discard pile, plus one entry per played card implied by each stack's `topRank`. `forbiddenTokens` is `[seed]` when a seed is supplied, otherwise empty.

    Create `packages/rules/src/hanabi/hanabi-leak-check.test.ts` with `describe("leak", ...)` (the literal string "leak" is required by 03-VALIDATION.md). Put the clean-baseline test FIRST, as `forehead-card-leak-check.test.ts:12-22` does, so a checker stuck always reporting leaks is caught. Then one `it` per canary above. Per D-22/WR-02, any assertion loop guarded by a condition must be preceded by an explicit non-vacuousness assertion that the guarded collection is non-empty — this is the exact defect WR-02 found in Canary F of the Phase 2 suite, so do not reproduce its shape.
  </action>
  <verify>
    <automated>npx vitest run --project rules packages/rules/src/hanabi/hanabi-leak-check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project rules -t "leak"` matches at least one test and exits 0
    - hanabi-leak-check.test.ts contains at least 8 `it(` blocks plus a clean-baseline test declared before any canary
    - hanabi-leak-check.test.ts contains at least one `toBeGreaterThan(0)` assertion guarding a filtered collection before a loop (WR-02 closure)
    - `grep -c 'in obj\|in yourCard\|in entry' packages/rules/src/hanabi/hanabi-leak-check.ts` returns at least 2 (key-presence, not truthiness)
    - `grep -c 'typed:identity-count-exceeded' packages/rules/src/hanabi/hanabi-leak-check.ts` returns at least 1
    - the undefined-valued-key canary passes, proving structural detection survives JSON.stringify
    - `npx vitest run --project rules` exits 0
  </acceptance_criteria>
  <done>The leak checker detects Hanabi identity leaks structurally and by typed multiset rather than by scanning for a bare number, a canary suite proves it can fail on eight distinct leak shapes, and no canary can degrade to zero assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Compose hanabiGame, export it, and extend the conformance suite</name>
  <files>packages/rules/src/hanabi/adapter.ts, packages/rules/src/index.ts, packages/rules/src/adapter.test.ts</files>
  <read_first>
    - packages/rules/src/adapter.ts (the five-member interface and its three invariants — unmodified per D-03)
    - packages/rules/src/forehead-card.ts (lines 103-126 the adapter object-literal shape)
    - packages/rules/src/adapter.test.ts (describeAdapterConformance's signature, the line-85 call-site style, and the purity file list at lines 95-113)
    - packages/rules/src/index.ts (the existing barrel block the Hanabi section mirrors)
  </read_first>
  <behavior>
    - hanabiGame.id === "hanabi"
    - createInitialState deals hands per variant and seat count, sets clueTokens to 8, fuses to 0, finalTurnsRemaining to null, and empty stacks entries (one per configured suit at topRank 0), discard and history
    - describeAdapterConformance("hanabi", ...) passes: deterministic createInitialState, non-mutation on accepted and rejected actions, pure per-seat toPlayerView, no throw for any fc.jsonValue payload, and a numeric checkGameEnd score
    - the purity test's file list includes every hanabi source module and none of them contains "node:", "from \"fs\"", "partyserver" or "cloudflare:"
    - importing from @games/rules exposes hanabiGame and the Hanabi types without importing @games/schema
  </behavior>
  <action>
    Create `packages/rules/src/hanabi/adapter.ts` composing the modules from plans 01-03 into the five-member `GameAdapter` object literal named `hanabiGame`, with `id: "hanabi"`, exactly the shape of `foreheadCardGame` at `forehead-card.ts:103`. `createInitialState({ seatIds, variant, seed })` resolves the config via `variantConfig(variant)`, deals via `dealInitialHands`, and builds the initial `HanabiState` as a named field-by-field literal with `clueTokens` set to `MAX_CLUE_TOKENS`, `fuses` 0, `finalTurnsRemaining` null, `stacks` as one `{ suit, topRank: 0 }` entry per configured suit in config order, and empty `discard` and `history`. `applyAction`, `toPlayerView` and `checkGameEnd` delegate to `applyHanabiAction`, `toHanabiPlayerView` and `checkHanabiGameEnd` respectively and contain no logic of their own. The header comment must state that `toPlayerView` is the only exit point from state and that this file adds no whole-state serializer.

    Edit `packages/rules/src/index.ts` adding the Hanabi export block from `<interfaces>` alongside the existing forehead-card block, which stays untouched (D-02 — the toy is deleted in Phase 4, not here). Update the file's header comment to describe both games rather than replacing the toy's description.

    Edit `packages/rules/src/adapter.test.ts`: add one `describeAdapterConformance("hanabi", hanabiGame, [...])` call next to the existing line-85 call, with sample actions `[{ type: "discard", cardId: "zzzzzzzz" }, { type: "clue", targetSeatId: "seat-b", clue: { type: "rank", value: 1 } }]` so both a rejected and a plausibly-accepted branch are exercised for the non-mutation check. Extend the purity test's `files` array with the hanabi module paths (`hanabi/variant.ts`, `hanabi/state.ts`, `hanabi/deck.ts`, `hanabi/history.ts`, `hanabi/clue-facts.ts`, `hanabi/legality.ts`, `hanabi/actions.ts`, `hanabi/endgame.ts`, `hanabi/projection.ts`, `hanabi/hanabi-leak-check.ts`, `hanabi/adapter.ts`) so FDN-02's zero-Node/zero-Worker guarantee covers Hanabi too. Do not rewrite or fork `describeAdapterConformance` itself.
  </action>
  <verify>
    <automated>npx vitest run --project rules packages/rules/src/adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project rules -t "conformance"` matches both the forehead-card and hanabi suites and exits 0
    - `grep -c 'describeAdapterConformance(' packages/rules/src/adapter.test.ts` returns 3 (one definition, two call sites)
    - `grep -c 'hanabi/' packages/rules/src/adapter.test.ts` returns at least 11 (every hanabi module in the purity file list)
    - `grep -c 'export { hanabiGame }' packages/rules/src/index.ts` returns 1
    - `grep -c 'foreheadCardGame' packages/rules/src/index.ts` returns at least 1 (the toy's exports survive — D-02)
    - `grep -c 'interface GameAdapter' packages/rules/src/adapter.ts` returns 1 and that interface still declares exactly five members
    - `npm test` exits 0 and `npx tsc -p packages/rules/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
  <done>hanabiGame implements the unmodified adapter contract, passes the shared conformance and purity suites, and is exported from the package barrel ready for Phase 4's registration point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HanabiState → HanabiView | The only place game state becomes something a client could receive; everything crossing it must be whitelist-constructed |
| viewer seat id → projection | A seat id that is unknown, stale or spoofed must yield strictly less information, never more |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | An own-hand card carrying suit or rank in the projected view | mitigate | D-07 whitelist construction with no spread/delete/omit, enforced by a grep gate over comment-stripped source plus key-presence tests |
| T-03-15 | Information Disclosure | A clue-fact object accidentally carrying the resolved identity instead of the narrowed candidate set | mitigate | Clue facts are mapped field-by-field into the view; the typed multiset check would flag an excess `{suit, rank}` pair regardless of the key it hides under |
| T-03-16 | Information Disclosure | An unseated or stale-token viewer receiving a seated view | mitigate | Fail-closed branch renders every hand hidden, mirroring `forehead-card.ts:179-195`; covered by an explicit unit test |
| T-03-17 | Information Disclosure | The server seed or undealt deck order reaching a view (D-14) | mitigate | `HanabiState` carries no seed and no RNG state; `deckCount` is a number; the checker scans the serialized view for the seed as a forbidden token |
| T-03-18 | Repudiation | A leak checker that cannot fail, giving false assurance | mitigate | D-22 canary suite with a clean baseline first and explicit non-vacuousness assertions (WR-02 closure) |
| T-03-19 | Denial of Service | `applyAction` throwing on a hostile JSON payload | mitigate | `describeAdapterConformance`'s `fc.jsonValue()` property, reused unmodified for `hanabiGame` |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this phase; `packages/rules` stays zero-dependency (FDN-02) |
</threat_model>

<verification>
- `npx vitest run --project rules` green, including both adapter conformance suites
- `npm test` green (D-02)
- `npx tsc -p packages/rules/tsconfig.json --noEmit` exits 0
- `packages/rules/package.json` still declares no `dependencies`
</verification>

<success_criteria>
- No seat's view contains its own cards' identities, structurally or by typed count
- An unseated viewer fails closed
- The leak checker is proven able to fail on eight distinct Hanabi leak shapes, with no vacuous canary
- `hanabiGame` passes the shared conformance and purity suites and is exported from `@games/rules`
</success_criteria>

<output>
Create `.planning/phases/03-hanabi-rules-engine/03-04-SUMMARY.md` when done
</output>
