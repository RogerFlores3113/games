---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 03
subsystem: ui
tags: [hanabi, hints, preferences, safe-storage, pure-functions, vitest]

# Dependency graph
requires:
  - phase: 06.2-01
    provides: "positiveClues append-only guarantee (clue-facts.ts), asserted by clue-facts.test.ts"
  - phase: 06.2-02
    provides: "--color-tile-preset-* @theme tokens consumed by TILE_COLOR_PRESETS"
provides:
  - "hanabi-hint-logic.ts: hintDisplayFor (facts -> colour/number hints) and hintsVisibleForCard (turn-based hint clear rule)"
  - "keep-hints-pref.ts: per-player keep-hints-visible toggle, safe-storage-backed, defaults off"
  - "tile-color-pref.ts: fixed five-preset tile-colour palette and per-player preference, safe-storage-backed"
affects: [06.2-04, 06.2-05, 06.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Turn-based (not time-based) derived visibility: hint lifetime computed synchronously from history on every render, no timer/ref (mirrors touchedCardIdsFromLatestClue in hanabi-visual-logic.ts)"
    - "Preference pair pattern: readXPref()/writeXPref() through safe-storage.ts only, unrecognised stored values degrade to documented default (mirrors hanabi-discard-logic.ts)"

key-files:
  created:
    - apps/web/lib/hanabi-hint-logic.ts
    - apps/web/lib/hanabi-hint-logic.test.ts
    - apps/web/lib/keep-hints-pref.ts
    - apps/web/lib/tile-color-pref.ts
    - apps/web/lib/tile-color.test.ts
  modified: []

key-decisions:
  - "hintDisplayFor reads only facts.positiveClues; negativeClues/possibleSuits/possibleRanks are never touched, enforced by an acceptance grep (D-07)"
  - "hintsVisibleForCard treats draw history entries as non-clearing (only play/discard/clue entries count as 'the next player acting')"
  - "TILE_COLOR_PRESETS cssValue values are all var(--color-*)/color-mix(...) references, never raw hex, enforced by a source-scan test"

patterns-established:
  - "Pure facts-only hint derivation, no React/timers/storage in hanabi-hint-logic.ts"
  - "safe-storage.ts remains the single storage wrapper; no new wrapper introduced by either preference module"

requirements-completed: []  # HINT-01..04 and TILE-03 are NOT marked complete by this plan (see note below) — no user-visible behaviour ships yet.

# Metrics
duration: 3min
completed: 2026-09-17
---

# Phase 06.2 Plan 03: Hint Logic and Preference Modules Summary

**Pure, tested client logic for hint derivation (colour/number channels from `positiveClues` only) and two safe-storage-backed personal preferences (keep-hints toggle, five-preset tile-colour palette) — no UI wiring yet.**

## Performance

- **Duration:** ~3 min (task execution; investigation/reading time not included)
- **Started:** 2026-09-17T21:12:00Z (approx, first commit landed 14:13:37-07:00)
- **Completed:** 2026-09-17T21:14:43Z
- **Tasks:** 2
- **Files modified:** 5 (all new)

## Accomplishments
- `hanabi-hint-logic.ts` exports `hintDisplayFor(facts)` (pure, facts-only, D-06 composition, D-07 no negative info) and `hintsVisibleForCard(history, cardId, { keepVisible })` (pure, turn-based clear rule, D-05) — 14 passing unit tests cover every behaviour bullet in the plan, including the "draws don't clear hints" edge case and purity checks.
- `keep-hints-pref.ts` and `tile-color-pref.ts` persist two new personal preferences exclusively through the existing `safe-storage.ts` wrapper, both defaulting safely on unrecognised stored values — 11 passing tests including a source scan proving no hex literals and no wire-reaching imports in either file.

## Task Commits

Each task was committed atomically:

1. **Task 1: hanabi-hint-logic.ts — facts-only hint derivation and the turn-based clear rule** - `d9b8285` (test+feat, combined single commit per this plan's tdd_mode setting)
2. **Task 2: keep-hints and tile-colour preferences on safe-storage** - `a277845` (feat)

**Plan metadata:** committed separately after this SUMMARY (see final commit below)

_Note: the orchestrator's `tdd_mode` is `false` for this phase even though the plan's tasks are marked `tdd="true"`; both tasks were implemented and verified in the RED-then-GREEN order the plan's `<behavior>` blocks specify, but landed as one commit per task rather than split test/feat commits. Every behaviour bullet has a matching `it(...)`; nothing shipped untested._

## Files Created/Modified
- `apps/web/lib/hanabi-hint-logic.ts` - `HintDisplay`, `hintDisplayFor`, `hintsVisibleForCard`
- `apps/web/lib/hanabi-hint-logic.test.ts` - 14 tests covering composition, de-duplication, D-07 exclusion, both keepVisible branches, draw-does-not-clear, purity
- `apps/web/lib/keep-hints-pref.ts` - `KEEP_HINTS_KEY`, `readKeepHintsPref`, `writeKeepHintsPref`
- `apps/web/lib/tile-color-pref.ts` - `TileColorId`, `TILE_COLOR_PRESETS`, `TILE_COLOR_KEY`, `readTileColorPref`, `writeTileColorPref`
- `apps/web/lib/tile-color.test.ts` - 11 tests covering both preference modules plus a source-scan for hex literals and wire-reaching imports

## Decisions Made
- Followed the plan's D-05/D-06/D-07/D-13/D-14 decisions exactly as specified; no new decisions required beyond minor wording choices in doc comments (see Deviations below).
- Named the acceptance-criteria pattern for "no timer/ref" as literal identifier avoidance in doc comments (see deviation #1) rather than reinterpreting the rule.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Doc-comment prose accidentally tripped the plan's own acceptance-criteria greps**
- **Found during:** Task 1 verification (`grep -nE 'setTimeout|useRef|useEffect|localStorage'`) and Task 2 verification (`grep -nE 'room-socket|room-store|nanoid'`)
- **Issue:** Explanatory doc comments in `hanabi-hint-logic.ts` and `tile-color-pref.ts` named the exact anti-patterns being avoided (e.g., "not modeled with `setTimeout`"), which made the acceptance-criteria grep (designed to catch actual usage) also match the prose explaining why those patterns are absent.
- **Fix:** Reworded both comments to describe the same constraints without using the literal forbidden identifiers (e.g., "a delayed-clear timer" instead of "`setTimeout`", "the websocket connection layer" instead of "room-socket").
- **Files modified:** `apps/web/lib/hanabi-hint-logic.ts`, `apps/web/lib/tile-color-pref.ts`
- **Verification:** Both greps now return empty/exit 1 as required; full test suite and `tsc -b apps/web` still pass.
- **Committed in:** `d9b8285` (Task 1), `a277845` (Task 2) — fixed inline before each task's commit, not a separate commit.

---

**Total deviations:** 1 auto-fixed (Rule 1 - wording-only bug, no logic change)
**Impact on plan:** Cosmetic only; no behavior, test coverage, or acceptance criteria were weakened.

## Issues Encountered
- The plan's Task 2 acceptance criterion `grep -c 'safe-storage' ... is 1 for each file` returns `2` for both files in the shipped code, because each file's header doc comment also mentions "safe-storage.ts" in prose alongside the actual import line. The substantive requirement — both preferences route exclusively through `safe-storage.ts` and no second wrapper — is satisfied and tested; this is a documented, harmless discrepancy from the letter of that one grep count, not a functional gap.

## User Setup Required
None - no external service configuration required.

## Requirements Note

Per this plan's `<requirements_discipline>` instruction: HINT-01, HINT-02, HINT-03, HINT-04, and TILE-03 are listed in this plan's frontmatter `requirements` field, but **none are marked complete in REQUIREMENTS.md by this plan**. This plan is pure-logic groundwork only (data derivation + storage preferences) with zero UI wiring or rendering — no player can see a hint indicator, toggle keep-hints, or pick a tile colour yet. Those requirements should be marked complete only once a later plan (06.2-04 or later, per the phase's component-wiring tasks) actually renders these behaviours end to end.

## Next Phase Readiness
- `hintDisplayFor`/`hintsVisibleForCard` are ready for `HintIndicator.tsx` (06.2-04+) to consume directly — own-hand-safe, pure, fully tested.
- `keep-hints-pref.ts`/`tile-color-pref.ts` are ready for the keep-hints toggle control and `TileColorPicker.tsx` to consume.
- No blockers for subsequent waves in this phase.

---
*Phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens*
*Completed: 2026-09-17*

## Self-Check: PASSED

All 5 created files found on disk; both task commits (`d9b8285`, `a277845`) found in git log.
