---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 16
type: execute
wave: 5
depends_on: ["06.2-15"]
gap_closure: true
files_modified:
  - apps/web/components/hanabi/Table.tsx
  - apps/web/components/hanabi/TokenColumn.tsx
  - apps/web/lib/layout-budget.ts
  - apps/web/lib/layout-budget.test.ts
  - apps/web/lib/table-render.test.ts
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-04, BOARD-05, UI-11]

must_haves:
  truths:
    - "The board's three regions — Play, deck-and-discard, tokens — each occupy a fixed reserved box"
    - "The board does not grow or reflow as cards are played, discarded or tokens are spent"
    - "The Play area shows one column per suit with ranks filling downward"
    - "A deep discard pile fills its reserved box and clips, rather than pushing the board taller"
  artifacts:
    - path: "apps/web/components/hanabi/Table.tsx"
      provides: "three fixed-width, fixed-height board regions on one wooden panel"
      contains: "BOARD_INNER_PX"
  key_links:
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "apps/web/lib/layout-budget.ts"
      via: "every region's height and width read from the ledger"
      pattern: "BOARD_INNER_PX|PLAY_AREA_WIDTH_PX|TOKEN_AREA_WIDTH_PX"
---

<objective>
Close UAT gap 1 at the board level: rebuild `Table.tsx` so every area reserves its space up front and nothing on the board grows or reflows as tiles accumulate.

Purpose: the owner's core complaint — "The board should have fixed spaces for where things go. The board should NOT be expanding as people fill it up - things should be taking up space that's already portioned out!" The Play grid (06.2-15) and the token area (06.2-14) are already fixed reservations; this plan lays them out beside a fixed deck-and-discard column on one panel whose height is a constant.
Output: a three-region board whose rendered box is identical in an empty game and a nearly-finished one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-07-SUMMARY.md

<interfaces>
Every testid and data-attribute the drag system and the e2e suite depend on, all of which must survive this rework (proven when `e2e/hanabi-table-polish.spec.ts` passes unchanged): `tableau`, `play-zone` + `data-drop-state`, `played-stack-{suit}` + `data-top-rank`/`data-complete`, `clue-tokens`/`fuse-tokens` + `data-count`, `deck-count` + `data-final-round`, `discard-pile` + `data-discard-count`/`data-view`/`data-drop-state`, `discard-toggle`, `discard-tile-{id}` + `data-dragging`, `drop-reason-play`, `drop-reason-discard`.

Ledger constants now available: `BOARD_INNER_PX` (244), `BOARD_PANEL_PADDING_PX` (8), `PLAY_AREA_PX`, `PLAY_AREA_WIDTH_PX` (208), `AREA_LABEL_PX`, `AREA_PADDING_PX`, `SUIT_COLUMN_GAP_PX`, `DECK_COUNTER_PX` (40), `MIDDLE_GAP_PX` (4), `DISCARD_AREA_PX` (200), `TOKEN_AREA_HEIGHT_PX`, `TOKEN_AREA_WIDTH_PX`, `playGridHeightPx()`, `playColumnWidthPx()`.

Existing helpers in `Table.tsx` to keep as they are: `resolveDiscardOrder` (T-06.2-15's defensive sequencing), `applyPendingOrder` for the optimistic discard drag, `dropZoneHighlightStyle`, `AreaLabel`, the stack-flash ref/timer effect (WR-01 — the clear timer must stay in a ref, never an effect cleanup), and the `DiscardView` compact/expanded preference.

Deprecated shims this plan removes: `LEFT_COLUMN_PX` (the alias left in the ledger by 06.2-15) and `TokenColumn`'s unused `columnHeightPx` prop (left accepted-and-ignored by 06.2-14).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three fixed board regions on one panel</name>
  <files>apps/web/components/hanabi/Table.tsx, apps/web/lib/table-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx (whole file)
    - apps/web/lib/table-render.test.ts (whole file — the render contract being extended)
    - apps/web/lib/layout-budget.ts (the reserved constants)
    - apps/web/components/hanabi/PlayedStack.tsx (the suit-column contract from 06.2-15)
  </read_first>
  <behavior>
    - The `tableau` element's inline style fixes its height from `BOARD_INNER_PX` plus panel padding, and that style string is identical for an empty game and a game with full stacks, 30 discards and 0 tokens.
    - `play-zone` renders one `played-stack-{suit}` per suit in `game.stacks` order, laid out as a row of columns with `SUIT_COLUMN_GAP_PX` between them.
    - The play region's inline width is `PLAY_AREA_WIDTH_PX` at 5 and at 6 suits.
    - `discard-pile`'s inline height is `DISCARD_AREA_PX` minus its own label chrome, at 0 discards and at 30.
    - `deck-count` still renders `deckCountText(game)` with `data-final-round`.
    - Every testid listed in `<interfaces>` is present.
  </behavior>
  <action>
    Restructure the `tableau` section into one `.board-surface` panel containing exactly three sibling regions in a flex row with `--space-md` between them, `items-start`, and the panel's own height fixed at `BOARD_INNER_PX + 2 * BOARD_PANEL_PADDING_PX`.

    Region 1 — Play (left, fixed): inline `{ width: PLAY_AREA_WIDTH_PX, height: PLAY_AREA_PX }`, `overflow: hidden`, a `--color-border` outline, rounded, and the `AreaLabel` "Play" occupying `AREA_LABEL_PX`. Inside it, the `play-zone` div (keeping `playZoneRef`, `data-drop-state` and the drop highlight/reason behaviour verbatim) is a flex ROW of `PlayedStack` columns with `SUIT_COLUMN_GAP_PX` between them, `items-start`, `flex-wrap: nowrap`. Fixed width means six suits fit without wrapping; do not add `flex-wrap`, because wrapping is exactly the reflow gap 1 forbids.

    Region 2 — Deck and Discard (middle, flexes horizontally, fixed vertically): a column of inline height `BOARD_INNER_PX`, `flex: 1`, `min-width: 0`. Top child: the deck counter row at `DECK_COUNTER_PX`, keeping `FireworkCardBack` (reduced to 28x38 so it fits the shorter row) beside the `deck-count` paragraph. Then `MIDDLE_GAP_PX`. Then the Discard area at `DISCARD_AREA_PX`: the same outline/label treatment, the existing header row with `AreaLabel` "Discard" and the `discard-toggle` button unchanged, and the `discard-pile` div beneath it with `flex-wrap: wrap` and `overflow: hidden`, filling the remaining reserved height exactly. Keep `discardZoneRef`, `data-discard-count`, `data-view`, `data-drop-state`, the drop-reason span, the per-tile drag wiring and the empty-state copy. Add a comment recording that a pile deeper than the reserved box clips by design and the full pile is read through the expanded overlay — that is the fixed-geometry trade the owner asked for.

    Region 3 — Tokens (right, fixed): `<TokenColumn clueTokens={game.clueTokens} fusesRemaining={fusesRemaining} />` with no height prop, inside a wrapper of inline width `TOKEN_AREA_WIDTH_PX`.

    Delete the `LEFT_COLUMN_PX` import and every use of it. Keep the `DiscardOverlay` render, the flash effect and all preference handling untouched.

    Extend `table-render.test.ts` with the `<behavior>` assertions, including the two identical-style comparisons (empty vs full game) built from the file's existing view fixtures — add a "full board" fixture with all stacks at rank 5, 30 discards, `clueTokens: 0` and 0 fuses remaining.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web table-render stack-render token-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - a test asserts the `tableau` element's inline style string is identical between the empty-game and full-board fixtures
    - a test asserts `discard-pile`'s inline style string is identical at 0 and 30 discards
    - a test asserts the play region's inline width equals `PLAY_AREA_WIDTH_PX` for both a 5-suit and a 6-suit fixture
    - every testid in `<interfaces>` is asserted present by `table-render.test.ts`
    - `grep -v '^\s*\*' apps/web/components/hanabi/Table.tsx | grep -c 'LEFT_COLUMN_PX'` returns 0
    - `npx vitest run --project web table-render` passes
  </acceptance_criteria>
  <done>The board is three reserved boxes whose rendered geometry does not change with game content.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the deprecated height shims and prove the board end to end</name>
  <files>apps/web/components/hanabi/TokenColumn.tsx, apps/web/lib/layout-budget.ts, apps/web/lib/layout-budget.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/TokenColumn.tsx (the deprecated `columnHeightPx` prop)
    - apps/web/lib/layout-budget.ts (the deprecated `LEFT_COLUMN_PX` alias)
    - apps/web/lib/token-render.test.ts (any remaining call sites passing the prop)
  </read_first>
  <action>
    Delete `columnHeightPx` from `TokenColumnProps` and from the component's doc comment, now that `Table.tsx` no longer passes it. Delete `LEFT_COLUMN_PX` from the ledger and from `layout-budget.test.ts`. Run `grep -rn "columnHeightPx\|LEFT_COLUMN_PX" apps e2e` and remove every remaining reference, including in test files.

    Then run the browser suite that exercises the board and fix anything this rework broke that is inside this plan's files. If a failure requires changing a file this plan does not own (an e2e spec, `HanabiBoard.tsx`, a drag hook), stop and report rather than widening scope — 06.2-19 owns the e2e reconciliation and 06.2-13 owns `HanabiBoard.tsx`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npm test && npx tsc -b apps/web packages/rules packages/schema apps/worker && npx playwright test e2e/hanabi-table-polish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "columnHeightPx\|LEFT_COLUMN_PX" apps e2e | grep -v node_modules` returns nothing
    - `npm test` passes
    - `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
    - `npx playwright test e2e/hanabi-table-polish.spec.ts` passes, or any failure is reported with the out-of-scope file it requires
  </acceptance_criteria>
  <done>No derived-height shim remains, and the reworked board passes the existing browser suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server game view → board render | stacks, discard pile and token counts all size the board's content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-15 | Tampering | resolveDiscardOrder | mitigate | carried forward unchanged — unknown `discardOrder` ids are skipped and unlisted discards appended, so a malformed frame degrades rather than hiding a discarded tile |
| T-06.2-39 | Denial of Service | discard region | mitigate | the discard box is a fixed reservation with `overflow: hidden`, so an unbounded pile can no longer push the page past 720px; the full pile stays reachable through the expanded overlay |
| T-06.2-40 | Information disclosure | board regions | accept | every element rendered here is public game state already present in each seat's view; no own-hand data reaches `Table.tsx` |
</threat_model>

<verification>
- `npm test` passes
- `npx playwright test e2e/hanabi-table-polish.spec.ts` passes
- `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
</verification>

<success_criteria>
The board is a fixed panel of three reserved regions whose rendered geometry is provably identical between an empty game and a full one, with every pre-existing testid intact.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-16-SUMMARY.md` when done
</output>
