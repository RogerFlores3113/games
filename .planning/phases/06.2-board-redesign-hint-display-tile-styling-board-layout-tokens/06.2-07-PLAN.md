---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 07
type: execute
wave: 4
depends_on: ["06.2-01", "06.2-02", "06.2-05", "06.2-06"]
files_modified:
  - apps/web/components/hanabi/Table.tsx
  - apps/web/lib/table-render.test.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-05, TILE-02, DISC-01]

must_haves:
  truths:
    - "BOARD-01: Play sits at the top of the board and Discard at the bottom, each inside a subtle outline with a visible label"
    - "BOARD-04: the deck counter shows the remaining count beside a small tile back, between Play and Discard"
    - "BOARD-02: the clue and fuse token column sits on the right of the board, stretched to the same height as the Play/Deck/Discard column"
    - "BOARD-05: every card in a played stack is visible"
    - "TILE-02: the board panel carries the wooden surface and the 06.1 city-at-night background is still visible around it"
    - "The discard area lists every discarded tile in the server's shared discardOrder"
    - "The reworked board still fits 1280x720 with no vertical scrollbar, checked in a real browser before later plans build on this layout"
  artifacts:
    - path: "apps/web/components/hanabi/Table.tsx"
      provides: "two-column board: Play/Deck/Discard left, token column right, wooden surface"
      contains: "TokenColumn"
    - path: "apps/web/lib/table-render.test.ts"
      provides: "render assertions for labels, deck counter, token column placement and discardOrder sequencing"
  key_links:
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "apps/web/components/hanabi/TokenColumn.tsx"
      via: "right-hand sibling in an items-stretch flex row, given the left column's height"
      pattern: "TokenColumn"
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "HanabiView.discardOrder"
      via: "discard tiles rendered in the server-supplied order"
      pattern: "discardOrder"
---

<objective>
Rebuild the tableau as the owner described it: a wooden board with a labelled Play area on top, the deck counter in the middle, a labelled Discard area at the bottom, and the clue/fuse token column down the right.

Purpose: BOARD-01..05 and TILE-02 (D-11, D-15..D-22), plus the client presentation half of DISC-01's shared order. This is the single riskiest layout change in the phase, so it consumes only already-tested components and the height ledger.
Output: reworked `Table.tsx` and a render test for the new structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md

<interfaces>
Components to consume (06.2-05): `TokenColumn({ clueTokens, fusesRemaining, columnHeightPx })`, `PlayedStack({ stack, flashing })`.
From 06.2-02: `.board-surface` class; layout-budget constants LEFT_COLUMN_PX, PLAY_AREA_PX, DECK_COUNTER_PX, DISCARD_AREA_PX, TABLE_BAND_MIN_PX.
From 06.2-01: `HanabiView.discardOrder: string[]` — the shared arrangement; `game.discard` remains the id/suit/rank list to look tiles up in.
Existing Table.tsx behaviour that must survive the rework (read the current file before editing): the stack-completion flash effect and its ref-based clear timer (WR-01 — do not convert it to effect cleanup); the discard view preference (`readDiscardViewPref`/`writeDiscardViewPref`) and the `DiscardOverlay` expanded view; `playZoneRef`/`discardZoneRef` forwarding and `dropStatus` highlight/reason rendering for the drag zones; `deckCountText(game)` and its final-round semantics.
Testids and attributes existing e2e depends on — every one must still exist after this rework: `tableau`, `play-zone` (+`data-drop-state`), `played-stack-{suit}` (+`data-top-rank`, `data-complete`), `clue-tokens`, `fuse-tokens`, `deck-count` (+`data-final-round`), `discard-pile` (+`data-discard-count`, `data-view`, `data-drop-state`), `discard-toggle`, `drop-reason-play`, `drop-reason-discard`.
Copy contract (UI-SPEC): area labels "Play" and "Discard"; deck counter is the number beside the tile-back glyph; discard empty state "No tiles discarded yet".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-column board skeleton, wooden surface, labelled areas</name>
  <files>apps/web/components/hanabi/Table.tsx, apps/web/lib/table-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx (whole file)
    - apps/web/lib/layout-budget.ts (06.2-02)
    - apps/web/app/globals.css (the `.board-surface` and `.table-backdrop` rules)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Layout — Height Budget, Board Layout)
  </read_first>
  <action>
    Per D-16/D-17 and the UI-SPEC centre-band diagram. Restructure the `tableau` section into a single `flex-row` with `items-stretch` and exactly two children: a left column and the token column. Give the section the `.board-surface` class for the wooden treatment (TILE-02) and keep the page's `.table-backdrop` city background untouched behind it. The left column stacks, in order: the Play area, the deck counter, the Discard area. Play and Discard each get a `1px solid var(--color-border)` rounded outline with a Label-role caption inset at its top-left reading "Play" and "Discard" respectively, styled like the existing disabled-reason caption rather than as a heading. Sizes come from layout-budget constants — no new pixel literals in this file. Keep `playZoneRef` on the Play area, `discardZoneRef` on the Discard area, and keep the `dropStatus` highlight and `drop-reason-play`/`drop-reason-discard` rendering exactly as today. Preserve the stack-flash effect verbatim, including its ref-held clear timer and the WR-01 comment.
    Create table-render.test.ts with the renderToStaticMarkup scaffold asserting: both area labels are present, `tableau` carries the board-surface class, the left column and token column are siblings in one row, and every testid in the Interfaces list is still emitted for a representative mid-game view.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web table-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'board-surface' apps/web/components/hanabi/Table.tsx` matches
    - `grep -n '>Play<\|>Discard<' apps/web/components/hanabi/Table.tsx` matches both labels
    - `grep -n 'layout-budget' apps/web/components/hanabi/Table.tsx` matches and `grep -nE 'style=\{\{[^}]*(width|height): [0-9]' apps/web/components/hanabi/Table.tsx` shows no new raw pixel sizes
    - table-render.test.ts asserts every testid from the Interfaces list
  </acceptance_criteria>
  <done>The board reads as a wooden surface with a labelled Play area above a labelled Discard area, and nothing the drag system or e2e depends on was dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Token column, fanned stacks and the deck counter</name>
  <files>apps/web/components/hanabi/Table.tsx, apps/web/lib/table-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx (post-Task-1 structure; the old clue/fuse dot rows at ~203-244 and deck-count paragraph at ~247-255)
    - apps/web/components/hanabi/TokenColumn.tsx, apps/web/components/hanabi/PlayedStack.tsx (06.2-05)
    - apps/web/components/hanabi/FireworkCard.tsx (FireworkCardBack for the deck counter)
    - apps/web/lib/hanabi-visual-logic.ts (deckCountText) and apps/web/lib/hanabi-board-logic.ts (fusesRemainingForView)
  </read_first>
  <action>
    Per D-18/D-19/D-21/D-22. Replace the two 8px-dot token blocks with a single `TokenColumn` rendered as the row's right-hand child, passed `LEFT_COLUMN_PX` as its `columnHeightPx` so the column is sized from the left column and can never force the board taller (RESEARCH.md Pitfall 1). Replace the inline played-stack markup with `PlayedStack` per stack inside the Play area, passing the existing flashing-suit set. Move the deck counter between Play and Discard and render it as the remaining count beside a small `FireworkCardBack` (about 32x44), keeping the `deck-count` testid, `data-final-round`, and `deckCountText`'s final-round semantics as the accessible text. Delete every now-unused import and the inlined completed-stack glow literals that moved into `PlayedStack`.
    Extend table-render.test.ts: a view with 5 clue tokens and 2 fuses renders exactly 5 clue tokens and 2 fuse artworks with both text counts present; the deck counter renders between the Play and Discard areas in document order; a stack at rank 3 renders three card faces.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web table-render token-render stack-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'TokenColumn\|PlayedStack' apps/web/components/hanabi/Table.tsx` matches both
    - `grep -n 'h-2 w-2 rounded-full' apps/web/components/hanabi/Table.tsx` returns nothing (old dot rows gone)
    - `grep -n 'FireworkCardBack' apps/web/components/hanabi/Table.tsx` matches in the deck-counter block
    - table-render.test.ts asserts exact token counts and the deck counter's position between the two areas
  </acceptance_criteria>
  <done>Tokens are physical art on the right that disappear as they are spent, stacks show every firework, and the deck counter sits where the owner asked for it.</done>
</task>

<task type="auto">
  <name>Task 3: Discard area shows every tile in the shared order, and an early 1280x720 fit check</name>
  <files>apps/web/components/hanabi/Table.tsx, apps/web/lib/table-render.test.ts, e2e/start-game.spec.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx (post-Task-2 Discard area; the current grouped-by-suit summary at ~319-342)
    - apps/web/components/hanabi/DiscardOverlay.tsx (the existing full-art tile presentation to match)
    - apps/web/lib/hanabi-discard-logic.ts (groupDiscardsBySuit, the view preference)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Shared Discard Order)
    - e2e/start-game.spec.ts (the existing UI-11 1280x720 test)
    - apps/web/lib/layout-budget.ts (06.2-02)
  </read_first>
  <action>
    Per DISC-01's presentation half (D-23/D-28) — the drag interaction itself lands in 06.2-08, so this task renders the order only. In the Discard area, render one tile per entry of `game.discardOrder`, resolving each id against `game.discard`, in that exact order, each with `data-testid="discard-tile-{id}"` and the existing DiscardOverlay tile art/sizing; ignore any id in `discardOrder` with no matching discard entry rather than throwing, and append any discard entry missing from `discardOrder` at the end so a stale frame can never hide a card. Keep `data-discard-count`, the empty state (copy: "No tiles discarded yet"), the `discard-toggle` button and its expanded `DiscardOverlay` path intact. Use the height ledger's discard-area figure and let the row wrap rather than grow unbounded.
    Extend table-render.test.ts: with `discardOrder` deliberately different from `discard`'s array order, the rendered tile sequence follows `discardOrder`; with an empty discard the empty-state copy renders.
    Then add a lightweight early fit check to e2e/start-game.spec.ts, beside the existing UI-11 test: with the board loaded at 1280x720, assert `document.scrollingElement.scrollHeight <= window.innerHeight + 1` (no vertical scrollbar). Every major height contributor except the drag preview exists once this plan lands, so an overrun must be found and fixed HERE rather than after 06.2-08 and 06.2-09 build on this layout. Fix an overrun by adjusting the offending component and its layout-budget constant together, keeping layout-budget.test.ts's sum assertion true, and record the change as a deviation. This is deliberately not the full worst-case suite — the five-seat, deep-discard, per-testid `toBeInViewport()` verification stays in 06.2-10.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web table-render discard-overlay-render && npx tsc -b apps/web && npx playwright test e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'discardOrder' apps/web/components/hanabi/Table.tsx` matches
    - `grep -n 'discard-tile-' apps/web/components/hanabi/Table.tsx apps/web/lib/table-render.test.ts` matches in both
    - `grep -n 'No tiles discarded yet' apps/web/components/hanabi/Table.tsx` matches
    - table-render.test.ts's order-follows-discardOrder assertion passes with a non-insertion order fixture
    - e2e/start-game.spec.ts contains the new no-vertical-scrollbar assertion at 1280x720 and it passes
    - `npx vitest run --project web layout-budget` still passes if any ledger constant was adjusted
    - `npx playwright test e2e/start-game.spec.ts` passes, including the UI-11 viewport test
  </acceptance_criteria>
  <done>Every discarded tile is on the board in the order the server says, and the reworked board is confirmed to fit 1280x720 before anything is layered on top of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server view → board render | Only public fields (stacks, discard, discardOrder, tokens, deckCount) are rendered here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-15 | Tampering | a discardOrder containing unknown or missing ids | mitigate | Render resolves ids defensively: unknown ids are skipped, unlisted discards are appended, so a malformed frame degrades rather than hiding or crashing (Task 3) |
| T-06.2-16 | Denial of Service | unbounded discard growth blowing the height budget | mitigate | Discard row uses the ledger's area height and wraps; the worst-case fit is verified in 06.2-10 |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; the wood texture is CSS, not an asset |
</threat_model>

<verification>
- `npx vitest run --project web` green, `npx tsc -b apps/web` exits 0
- `npx playwright test e2e/start-game.spec.ts e2e/hanabi-table-polish.spec.ts` passes
</verification>

<success_criteria>
The tableau is the board the owner described — wooden surface, labelled Play above Discard, deck counter between, token art down the right that shrinks as tokens are spent, every firework visible, every discarded tile shown in the shared order — with every pre-existing testid and drag hook intact.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-07-SUMMARY.md` when done
</output>
