---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 04
type: execute
wave: 2
depends_on: ["06.2-02", "06.2-03"]
files_modified:
  - apps/web/components/hanabi/HintIndicator.tsx
  - apps/web/components/hanabi/OwnHandCard.tsx
  - apps/web/components/hanabi/TeammateCard.tsx
  - apps/web/components/hanabi/Hand.tsx
  - apps/web/components/hanabi/MarksZone.tsx
  - apps/web/components/hanabi/CandidateStrip.tsx
  - apps/web/lib/hanabi-visual-logic.ts
  - apps/web/lib/hanabi-visual-logic.test.ts
  - apps/web/lib/hint-render.test.ts
  - apps/web/lib/own-hand-source.test.ts
  - apps/web/lib/own-hand-render.test.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [HINT-01, HINT-02, HINT-04, TILE-01, TILE-03]

must_haves:
  truths:
    - "HINT-01: a colour clue tints the touched tile in that suit's own hue and keeps a non-colour marker, so the highlight survives a grayscale check"
    - "HINT-02: a number clue renders that numeral on the touched tile's back"
    - "HINT-04: the automatic clue-mark pip rows are gone — MarksZone and CandidateStrip no longer exist and nothing replaces their negative/ruled-out display"
    - "TILE-01: a tile renders as a raised, opaque object distinct from the board surface beneath it"
    - "D-15 holds: own-hand components still receive facts and ids only, and own-hand markup stays byte-identical with and without card identity"
  artifacts:
    - path: "apps/web/components/hanabi/HintIndicator.tsx"
      exports: ["OwnHintIndicator", "TeammateHintIndicator"]
      provides: "tile tint, non-colour suit marker, numeral-on-back"
    - path: "apps/web/lib/hint-render.test.ts"
      provides: "tint/marker/numeral render assertions plus a no-hex source scan"
  key_links:
    - from: "apps/web/components/hanabi/OwnHandCard.tsx"
      to: "apps/web/components/hanabi/HintIndicator.tsx"
      via: "OwnHintIndicator rendered over the card back with facts only"
      pattern: "OwnHintIndicator"
    - from: "apps/web/components/hanabi/HintIndicator.tsx"
      to: "apps/web/lib/hanabi-hint-logic.ts"
      via: "hintDisplayFor(facts) + hintsVisibleForCard(history, id, ...)"
      pattern: "hintDisplayFor"
---

<objective>
Move hint feedback onto the tile itself and delete the pip band: a colour clue tints the tile and shows a faint suit silhouette, a number clue stamps its numeral on the tile back, and tiles gain their raised physical surface.

Purpose: HINT-01, HINT-02, HINT-04, TILE-01 and the tile-colour rendering half of TILE-03 (D-01..D-04, D-06, D-07, D-12).
Output: `HintIndicator.tsx`, reworked card and hand components, `MarksZone.tsx`/`CandidateStrip.tsx` deleted, guard tests and e2e locators updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md

<interfaces>
From 06.2-03 apps/web/lib/hanabi-hint-logic.ts:
- `hintDisplayFor(facts: CardFacts): { colorHints: Suit[]; numberHints: Rank[] }`
- `hintsVisibleForCard(history: HistoryEntryView[], cardId: string, { keepVisible: boolean }): boolean`
From 06.2-02 apps/web/lib/layout-budget.ts: TEAMMATE_CARD_HEIGHT_PX (78), OWN_CARD_HEIGHT_PX (100), NOTE_ROW_PX (20), TEAMMATE_BAND_PX (140), OWN_BAND_PX (180).
From 06.2-02 globals.css: `--color-tile-shadow`, `--color-tile-preset-*`.
From 06.2-03 apps/web/lib/tile-color-pref.ts: `TILE_COLOR_PRESETS`, `TileColorId` (this plan consumes a resolved `tileColor` CSS value prop; 06.2-05 wires the picker that supplies it).
Existing components: `FireworkCardBack({ width, height })` (picture-frame back, the surface a number hint is stamped on), `FireworkCardFace({ suit, rank, width, height, exposeSuit, showBurstCount })`, `SuitGlyph({ suit, size, className, title, exposeSuit })`, `LUMINOSITY_FRAME` (luminosity-frame.ts — unchanged, still the luminosity signal; the new tile shadow is an object-on-surface cue, not a luminosity signal).
Current props: `OwnHandCardProps { facts, slotNumber, selected, justClued, disabled, onSelect, dragging, dragOffset, onPointerDown }` (CARD_WIDTH 88, CARD_HEIGHT 100); `TeammateCardProps { card, preview, justClued }` (CARD_WIDTH 64, CARD_HEIGHT 78).
Guard tests: own-hand-source.test.ts holds a hand-maintained path list — OWN_HAND_CARD_PATH, CANDIDATE_STRIP_PATH, MARKS_ZONE_PATH, NOTE_CHIP_PATH, USE_HAND_DRAG_PATH — plus per-file describes at lines ~147-235. own-hand-render.test.ts imports `CandidateStripProps` (line 15) and asserts a marks zone (line ~110) and a note chip (line ~114).
`candidateDisplayFor`/`CandidateDisplay`/`CandidateStripProps` consumers today: MarksZone.tsx, CandidateStrip.tsx, TeammateCard.tsx, OwnHandCard.tsx, Hand.tsx, own-hand-render.test.ts, hanabi-visual-logic.test.ts. `luminosityStepFor` (same module) stays — it is a separate export with its own consumers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HintIndicator — tint, non-colour marker, numeral on the back</name>
  <files>apps/web/components/hanabi/HintIndicator.tsx, apps/web/lib/hint-render.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-hint-logic.ts (06.2-03)
    - apps/web/components/hanabi/SuitGlyph.tsx
    - apps/web/components/hanabi/MarksZone.tsx (the own-hand props discipline to carry over)
    - apps/web/lib/firework-card-render.test.ts (renderToStaticMarkup scaffold and the no-hex source scan, lines ~1-19 and ~91-98)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Color section: HINT-01 tint, HINT-02 numeral, D-06 composition)
  </read_first>
  <behavior>
    - With a colour hint present, the rendered markup contains a tint layer whose background references color-mix with that suit's var(--color-suit-*) and a faint SuitGlyph marker in the top-left corner
    - With a number hint present, the markup contains a numeral chip in the bottom-right corner carrying data-testid="hint-numeral" and the numeral text
    - With both present, both the tint layer and the numeral chip are in the markup and neither overlaps the other's corner (independent channels, D-06)
    - With no hints, the component renders nothing (empty markup)
    - With visible=false, the component renders nothing regardless of hints (the keep-hints OFF path)
    - The own-hand variant's props type has no card, suit or rank member; passing the same facts twice renders byte-identical markup
    - The teammate variant renders the same tint/marker/numeral composition from the same hint inputs
  </behavior>
  <action>
    Per D-02/D-03/D-04/D-06 and the UI-SPEC Color section. Create HintIndicator.tsx exporting `OwnHintIndicator({ facts, visible, width, height, testId })` and `TeammateHintIndicator({ facts, visible, width, height, testId })`. Both call `hintDisplayFor(facts)` and render an absolutely-positioned overlay sized to the tile: a full-face tint layer using `color-mix(in srgb, var(--color-suit-{suit}) 22%, var(--color-surface))` for the first colour hint, a `SuitGlyph` at opacity 0.35 in the top-left as the non-colour marker (D-04), and a Label-role numeral chip in the bottom-right for the first number hint on a `--color-surface` chip. Colour values come only from existing tokens — no hex literal.
    The own-hand variant must keep the D-15 discipline verbatim from MarksZone: props are `facts` and layout/id values only, never a card object, never a bare suit/rank prop. Passing the clued suit into SuitGlyph is safe (the clue already revealed that suit to the viewer) but `exposeSuit` must stay false on the own-hand path so no `data-glyph` identity marker is emitted there; document this distinction in the file header along with D-07 (no ruled-out information is rendered here or anywhere else, owner-confirmed).
    Create hint-render.test.ts using firework-card-render.test.ts's `renderToStaticMarkup` + `countOccurrences` scaffold, covering every behaviour bullet plus a source scan asserting HintIndicator.tsx contains no hex colour literal.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hint-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9A-Fa-f]{3,8}' apps/web/components/hanabi/HintIndicator.tsx` returns nothing
    - `grep -vE '^\s*(//|\*|/\*)' apps/web/components/hanabi/HintIndicator.tsx | grep -cE 'exposeSuit\s*(=|:)\s*\{?true' ` is 0
    - `grep -n 'hint-numeral' apps/web/lib/hint-render.test.ts apps/web/components/hanabi/HintIndicator.tsx` matches in both
    - every behaviour bullet has a matching `it(...)` in hint-render.test.ts
  </acceptance_criteria>
  <done>A tile can show its hints as a tint, a silhouette and a numeral, with no identity leak and no hardcoded colour.</done>
</task>

<task type="auto">
  <name>Task 2: Tiles carry their own hints; the pip band is deleted</name>
  <files>apps/web/components/hanabi/OwnHandCard.tsx, apps/web/components/hanabi/TeammateCard.tsx, apps/web/components/hanabi/Hand.tsx, apps/web/components/hanabi/MarksZone.tsx, apps/web/components/hanabi/CandidateStrip.tsx, apps/web/lib/hanabi-visual-logic.ts, apps/web/lib/hanabi-visual-logic.test.ts, apps/web/lib/own-hand-source.test.ts, apps/web/lib/own-hand-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/OwnHandCard.tsx
    - apps/web/components/hanabi/TeammateCard.tsx
    - apps/web/components/hanabi/Hand.tsx
    - apps/web/components/hanabi/MarksZone.tsx
    - apps/web/lib/own-hand-source.test.ts (path list lines ~14-37 and the per-file describes ~147-235)
    - apps/web/lib/own-hand-render.test.ts
    - apps/web/lib/hanabi-visual-logic.ts (candidateDisplayFor and luminosityStepFor)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md (own-hand-source.test.ts section — the mandatory extension/removal rules)
  </read_first>
  <action>
    Per HINT-04, TILE-01 and D-12. Delete MarksZone.tsx and CandidateStrip.tsx. Remove `candidateDisplayFor`, `CandidateDisplay` and `CandidateStripProps` from hanabi-visual-logic.ts and its test once no consumer remains (keep `luminosityStepFor` and every other export untouched); if any surviving consumer is found, stop and report rather than leaving a partially removed export.
    OwnHandCard: drop the marks-zone-era imports, render `OwnHintIndicator` layered over the existing `FireworkCardBack` with `facts` and a new `hintsVisible: boolean` prop supplied by the caller, and give the card its raised tile surface — a new `tileColor?: string` prop (defaulting to the slate preset value) used as the card background, `1px solid var(--color-border)`, and `box-shadow: 0 2px 4px var(--color-tile-shadow)` composed with (not replacing) the existing LUMINOSITY_FRAME border/glow. Keep the button semantics, the slot label, the testid `own-hand-slot-{n}`, the data attributes, and the "renders NO identity signal" header comment; add a data attribute `data-hints` reflecting whether a hint overlay is currently rendered, for e2e.
    TeammateCard: same tile-surface treatment and `tileColor` prop, plus `TeammateHintIndicator` layered over the face with the same `hintsVisible` prop; keep `card-identity`/`data-glyph` behaviour, luminosity frame, preview ring and the testid `other-hand-card-{id}` unchanged.
    Hand.tsx: remove every `MarksZone` render; own slots keep a fixed `NOTE_ROW_PX`-tall note row above each card holding the existing `NoteChip` (06.2-05 replaces the chip with the always-visible box — do not change the chip here); teammate slots lose their band entirely. Thread `hintsVisible` and `tileColor` down from new `OwnHandProps`/`TeammateHandProps` fields, defaulting `hintsVisible` to true and `tileColor` to the slate value so callers not yet updated still compile. Import band and card heights from layout-budget.ts instead of writing new pixel literals, and update the card-size deviation comments to point at the ledger.
    own-hand-source.test.ts: in this same task remove CANDIDATE_STRIP_PATH, MARKS_ZONE_PATH and their describes (a readFileSync on a deleted path throws at collection time for the whole suite), and add HINT_INDICATOR_PATH with the same three checks the removed files had — comment-stripped no suit/rank property access or destructuring, no FORBIDDEN_TOKENS (exposeSuit/data-suit/card-identity/data-glyph), and a props type with no card member.
    own-hand-render.test.ts: drop the `CandidateStripProps` import and its `AssertNever` line, replace the marks-zone assertion with a hint-overlay assertion, keep the note-chip assertion, keep and re-verify the byte-identical visible-vs-identity-stripped assertion and the identical-card-back assertion, and add the new `hintsVisible`/`tileColor` props to the fixture (the 06.1-12 precedent for fixture updates).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/web/components/hanabi/MarksZone.tsx apps/web/components/hanabi/CandidateStrip.tsx` reports both missing
    - `grep -rn 'candidateDisplayFor\|CandidateStrip\|MarksZone' apps/web --include=*.ts --include=*.tsx` returns nothing
    - `grep -n 'HINT_INDICATOR_PATH' apps/web/lib/own-hand-source.test.ts` matches; `grep -n 'MARKS_ZONE_PATH\|CANDIDATE_STRIP_PATH' apps/web/lib/own-hand-source.test.ts` returns nothing
    - `grep -n 'color-tile-shadow' apps/web/components/hanabi/OwnHandCard.tsx apps/web/components/hanabi/TeammateCard.tsx` matches in both
    - `grep -n 'layout-budget' apps/web/components/hanabi/Hand.tsx` matches
    - `npx vitest run --project web` is fully green, including own-hand-render and own-hand-source
  </acceptance_criteria>
  <done>Hints live on the tiles, the pip band is gone from the tree and from the guards, and tiles read as raised objects.</done>
</task>

<task type="auto">
  <name>Task 3: Repair e2e locators for the removed pip band</name>
  <files>e2e/start-game.spec.ts</files>
  <read_first>
    - e2e/start-game.spec.ts (the own-hand marks assertions around lines 150-200 and the UI-11 1280x720 test)
    - apps/web/components/hanabi/OwnHandCard.tsx (post-Task-2 testids and data attributes)
    - apps/web/components/hanabi/TeammateCard.tsx
  </read_first>
  <action>
    Re-point every assertion that targeted `marks-zone-*`, `suit-pips`, `rank-pips`, `confirmed-suit`, `confirmed-rank` or `data-told` at the new hint surfaces: a clued own-hand slot exposes `data-hints="true"` and, for a number clue, a `hint-numeral` descendant; a clued teammate card exposes the same. Keep unchanged every assertion that guards the identity boundary (own-hand slots contain no `[data-glyph]` and no `card-identity`; a teammate `card-identity` contains exactly one `[data-glyph]`) and the UI-11 `toBeInViewport()` test. Do not add new scenarios here — 06.2-10 owns the new e2e coverage; this task only stops existing specs from asserting against deleted markup.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'marks-zone\|suit-pips\|rank-pips\|data-told' e2e/start-game.spec.ts` returns nothing
    - `grep -n 'data-glyph' e2e/start-game.spec.ts` still matches the identity-boundary assertions
    - `npx playwright test e2e/start-game.spec.ts` passes
  </acceptance_criteria>
  <done>The existing end-to-end suite passes against the new tile-borne hints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server view → own-hand render | The viewer's own hidden cards must stay indistinguishable in markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-08 | Information Disclosure | OwnHintIndicator threaded with a real suit/rank | mitigate | Own-hand variant takes `facts` only; own-hand-source.test.ts gains HINT_INDICATOR_PATH with the full scan; own-hand-render.test.ts byte-identical guard retained (Task 2) |
| T-06.2-09 | Information Disclosure | own-hand tint derived from a card's real suit rather than its clued suit | mitigate | Tint is derived from `hintDisplayFor(facts).colorHints`, which reads `positiveClues` only; `exposeSuit` stays false on the own-hand path (Task 1 acceptance grep) |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run --project web` green, `npx tsc -b apps/web` exits 0
- `npx playwright test e2e/start-game.spec.ts` passes, including the UI-11 1280x720 viewport test
</verification>

<success_criteria>
Clued tiles show their own hints in colour, silhouette and numeral; the pip rows and their code are deleted; tiles look like raised objects; and every identity-boundary guard still passes with the new components covered.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-04-SUMMARY.md` when done
</output>
