---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 13
type: execute
wave: 2
depends_on: ["06.2-12"]
gap_closure: true
files_modified:
  - apps/web/components/hanabi/SettingsModal.tsx
  - apps/web/components/hanabi/TileColorPicker.tsx
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/lib/settings-modal-render.test.ts
  - apps/web/lib/note-box-render.test.ts
  - e2e/hanabi-table-polish.spec.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [TILE-03, HINT-03, UI-11]

must_haves:
  truths:
    - "A gear icon in the top right opens a centre-justified settings modal"
    - "Tile colour, volume, mute and hint persistence are all inside that modal and nowhere else"
    - "The bottom controls row holds only own hand, play/discard and the clue picker"
    - "Escape closes the modal and the board behind it is untouched"
  artifacts:
    - path: "apps/web/components/hanabi/SettingsModal.tsx"
      provides: "the centre-justified settings dialog holding every non-play control"
      contains: "settings-modal"
    - path: "apps/web/lib/settings-modal-render.test.ts"
      provides: "render contract for the gear trigger, the modal's four controls and the emptied bottom row"
  key_links:
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "apps/web/components/hanabi/SettingsModal.tsx"
      via: "settings-toggle button opens SettingsModal with the existing audio/keepHints/tileColor state"
      pattern: "SettingsModal"
---

<objective>
Close UAT gap 8: move every non-play control out of the bottom row and into a centre-justified settings modal opened by a gear icon in the top right.

Purpose: the owner's verbatim instruction — "move the settings to a settings modal - open it with a gear icon in the top right. settings button should open a center-justified settings modal with card/tile color, volume slider, and hint persistence" — plus the follow-up answer "move everything in… mute joins the volume slider inside the modal". The bottom row keeps only own hand, play/discard and the clue picker, which also relieves the row's width pressure at 1280px.
Output: a `SettingsModal` component, a gear trigger that costs no flow height, a bottom row stripped to play controls, and updated render/e2e coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md

<interfaces>
Controls being relocated, with the testids that must survive the move (e2e and unit tests reference all of them):
- `AudioControls` (apps/web/components/hanabi/AudioControls.tsx) — `audio-mute-toggle` (aria-label "Mute sound"/"Unmute sound"), `audio-volume` (aria-label "Sound volume"). Props: `muted`, `volume`, `onToggleMute`, `onVolumeChange`.
- keep-hints toggle — inlined in `HanabiBoard.tsx` (~lines 392-420), `data-testid="keep-hints-toggle"`, `aria-pressed`, aria-labels "Keep hints visible" / "Clear hints after each move", `Eye`/`EyeOff` from lucide-react.
- `TileColorPicker` (apps/web/components/hanabi/TileColorPicker.tsx) — `tile-color-picker-toggle` (aria-label "Choose tile colour"), `tile-color-picker-panel` (aria-label "Tile colour presets"), `tile-color-swatch-{id}` with `aria-pressed`. Props: `value: TileColorId`, `onChange`.

State already owned by `HanabiBoard`: `audio` (from `useHanabiAudio`), `keepHints`/`handleToggleKeepHints`, `tileColorId`/`handleTileColorChange`. None of it moves — only where it is rendered changes.

Existing overlay precedent for Escape-to-close and a centred panel: `apps/web/components/hanabi/DiscardOverlay.tsx`. Follow its keydown handling, backdrop, `role="dialog"`/`aria-modal` and focus behaviour rather than inventing a second dialog pattern.

Out-of-flow 44px touch target pattern (project convention, three existing instances): the enlarged hit-area span is an absolutely positioned `aria-hidden` span that is a DESCENDANT (last child) of its button — never a sibling. A sibling span sits on top of the button and swallows every click (the production bug fixed in 06.2-10, commit `3d1a919`).

Height constraint: the gear trigger must add ZERO flow height to any band — it is absolutely positioned relative to the in-game `<main>`, which already has `px-[length:var(--space-md)] py-[3px]`. UI-11's 1280x720 fit has no slack.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsModal component and an embedded tile-colour swatch grid</name>
  <files>apps/web/components/hanabi/SettingsModal.tsx, apps/web/components/hanabi/TileColorPicker.tsx, apps/web/lib/settings-modal-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/DiscardOverlay.tsx (the dialog/backdrop/Escape precedent to mirror)
    - apps/web/components/hanabi/TileColorPicker.tsx (whole file — the toggle + panel + swatch structure being given an embedded mode)
    - apps/web/components/hanabi/AudioControls.tsx (the component rendered inside the modal unchanged)
    - apps/web/lib/note-box-render.test.ts (the render-test style, including its existing `tile-color-picker-toggle` assertion)
  </read_first>
  <behavior>
    - Renders nothing when `open` is false: the markup contains no `settings-modal` testid.
    - When `open` is true: renders `data-testid="settings-modal"` with `role="dialog"`, `aria-modal="true"` and an accessible name "Settings".
    - The open modal contains all four control groups: `audio-volume`, `audio-mute-toggle`, `keep-hints-toggle`, and `tile-color-swatch-slate` through `tile-color-swatch-charcoal`.
    - The swatch grid inside the modal is rendered directly — the markup contains no `tile-color-picker-toggle` when `TileColorPicker` is given `embedded`.
    - `TileColorPicker` without `embedded` renders exactly as before: a `tile-color-picker-toggle` button that gates the panel.
    - Escape fires `onClose` exactly once; a click on the backdrop fires `onClose`; a click inside the panel does not.
  </behavior>
  <action>
    Add an `embedded?: boolean` prop to `TileColorPicker`. When true it renders only the swatch grid (`tile-color-picker-panel` and the five `tile-color-swatch-{id}` buttons, each keeping its current aria-label and `aria-pressed`), with no toggle button and no open/closed state. When false or absent, behaviour is byte-identical to today — the default path is what `note-box-render.test.ts` already asserts, so that assertion must keep passing unchanged.

    Create `SettingsModal.tsx`. Props: `open: boolean`, `onClose: () => void`, `muted`, `volume`, `onToggleMute`, `onVolumeChange`, `keepHints: boolean`, `onToggleKeepHints: () => void`, `tileColorId: TileColorId`, `onTileColorChange: (id: TileColorId) => void`. Returns null when `open` is false.

    When open, render a fixed full-viewport backdrop (`position: fixed; inset: 0`, a scrim in the `rgba(11, 15, 26, N)` style already used by `EndOverlay`/`DiscardOverlay`, no hex literal) with `display: flex; align-items: center; justify-content: center` so the panel is centre-justified per the owner's wording. The panel itself: `data-testid="settings-modal"`, `role="dialog"`, `aria-modal="true"`, `aria-label="Settings"`, `--color-surface` background, `--color-border` border, rounded, padded with `--space-lg`, and a heading "Settings" using the existing Heading typographic role.

    Inside the panel, four labelled rows in this order, each label using the Label role and `--color-text-muted`:
    1. "Tile colour" — `<TileColorPicker embedded value={tileColorId} onChange={onTileColorChange} />`.
    2. "Sound" — `<AudioControls muted={muted} volume={volume} onToggleMute={onToggleMute} onVolumeChange={onVolumeChange} />` rendered unchanged, so `audio-mute-toggle` and `audio-volume` keep their exact testids and aria-labels.
    3. "Hints" — the keep-hints toggle button moved verbatim out of `HanabiBoard.tsx`: `data-testid="keep-hints-toggle"`, the same two aria-labels, `aria-pressed`, `Eye`/`EyeOff`, and the enlarged touch-target span kept as a DESCENDANT of the button.
    4. A close button: `data-testid="settings-close"`, `aria-label="Close settings"`.

    Escape key: attach a `keydown` listener on `window` while open, calling `onClose` on `Escape`, removed on close/unmount — mirror `DiscardOverlay`'s implementation rather than writing a second variant. Backdrop click calls `onClose`; the panel stops propagation.

    No new `@theme` token, no hex literal, no eighth `--color-accent` use (focus rings are the already-reserved use and are fine).

    Write `settings-modal-render.test.ts` first (RED), using `renderToStaticMarkup` in the style of `note-box-render.test.ts`, covering every bullet in `<behavior>` that is observable in static markup; assert the Escape/backdrop handlers exist by source-scanning for the `keydown` registration if a static render cannot exercise them.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web settings-modal-render note-box-render audio-controls-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/settings-modal-render.test.ts` exists and asserts `settings-modal`, `role="dialog"`, `aria-modal`, and the presence of `audio-volume`, `audio-mute-toggle`, `keep-hints-toggle` and all five `tile-color-swatch-*` ids in the open render
    - a test asserts the closed render contains no `settings-modal`
    - a test asserts the embedded `TileColorPicker` render contains no `tile-color-picker-toggle` while the default render still does
    - `grep -n 'inset' apps/web/components/hanabi/SettingsModal.tsx` shows the touch-target span inside the keep-hints button element, not after its closing tag
    - `npx vitest run --project web settings-modal-render note-box-render audio-controls-render` passes
    - `npx tsc -b apps/web` exits 0
  </acceptance_criteria>
  <done>A centre-justified settings dialog exists with tile colour, volume, mute and hint persistence inside it, proven by render tests.</done>
</task>

<task type="auto">
  <name>Task 2: Gear trigger in the top right, bottom row stripped to play controls</name>
  <files>apps/web/components/hanabi/HanabiBoard.tsx, apps/web/lib/settings-modal-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/HanabiBoard.tsx (whole file — the in-game `<main>` wrapper ~line 285 and the bottom-controls-row block ~lines 330-436 being emptied)
    - apps/web/components/hanabi/SettingsModal.tsx (the component created in Task 1)
    - apps/web/lib/layout-budget.ts (OWN_BAND_PX's doc comment — the ledger describing what this row costs)
  </read_first>
  <action>
    In `HanabiBoard.tsx`:

    Add `const [settingsOpen, setSettingsOpen] = useState(false);` beside the existing preference state. Do not move, rename or re-derive `audio`, `keepHints`, `handleToggleKeepHints`, `tileColorId`, `handleTileColorChange` or `tileColorCss` — only their render location changes.

    Add the gear trigger as an absolutely positioned child of the in-game `<main>` (the `table-backdrop` element): `position: absolute`, top/right offsets of `--space-sm`, `data-testid="settings-toggle"`, `aria-label="Open settings"`, `aria-expanded={settingsOpen}`, a lucide `Settings` icon at 20px, a 44px hit area via the descendant inset-span pattern, and a `z-index` above the board but below the modal. Give `<main>` `position: relative` so the trigger anchors to it. The trigger must be out of flow so it adds no height to any band — UI-11 has zero slack.

    Render `<SettingsModal open={settingsOpen} onClose={() => setSettingsOpen(false)} ... />` as the last child of `<main>`, after `FlyToLayer`, before/alongside `EndOverlay`.

    Delete from the `bottom-controls-row` div: the `<AudioControls .../>` element, the keep-hints toggle button and its wrapper span, the `<TileColorPicker .../>` element, and the now-empty grouping `<div className="flex items-center gap-[length:var(--space-xs)]">` that held them. Also delete the stale `fix(06.2-10)` comment that explains the ordering of that group against `CluePicker`, since the group no longer exists. The row must end up with exactly three children in this order: `OwnHand`, `CardActions`, `CluePicker`. Remove the now-unused `Eye`/`EyeOff`, `AudioControls`, `TileColorPicker` and `CONTROLS_ROW_PX` imports if nothing else in the file uses them.

    Extend `settings-modal-render.test.ts` with a `HanabiBoard`-level assertion (using the existing view fixtures from `table-render.test.ts` as the model for building a `RoomView`): the default board markup contains `settings-toggle` and does NOT contain `audio-mute-toggle`, `audio-volume`, `keep-hints-toggle` or `tile-color-swatch-slate` — proving those controls now live only inside the closed modal.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web settings-modal-render && npx tsc -b apps/web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AudioControls\|TileColorPicker\|keep-hints-toggle' apps/web/components/hanabi/HanabiBoard.tsx` returns 0 when comment lines are excluded (`grep -v '^\s*\(//\|\*\|/\*\)' apps/web/components/hanabi/HanabiBoard.tsx | grep -c 'AudioControls\|TileColorPicker\|keep-hints-toggle'` returns 0)
    - `grep -q 'data-testid="settings-toggle"' apps/web/components/hanabi/HanabiBoard.tsx` succeeds
    - the `bottom-controls-row` div's direct children are exactly `OwnHand`, `CardActions`, `CluePicker`
    - a test asserts the default `HanabiBoard` markup contains `settings-toggle` and none of the four relocated control testids
    - `npm test` passes (846+ tests, no regressions outside the files this plan owns)
  </acceptance_criteria>
  <done>The gear opens the settings modal, and the bottom row holds only play controls.</done>
</task>

<task type="auto">
  <name>Task 3: Update the e2e specs that reached the relocated controls directly</name>
  <files>e2e/hanabi-table-polish.spec.ts, e2e/start-game.spec.ts</files>
  <read_first>
    - e2e/hanabi-table-polish.spec.ts (the TILE-03 test ~lines 442-470 and the HINT-03 keep-hints test ~lines 370-432)
    - e2e/start-game.spec.ts (the UI-11 fit test's `audio-mute-toggle` in-viewport assertion ~line 419)
    - apps/web/components/hanabi/SettingsModal.tsx
  </read_first>
  <action>
    Every existing e2e path that clicked a relocated control now has to open the modal first. Update, do not delete, these tests:

    1. `hanabi-table-polish.spec.ts` TILE-03: replace `getByTestId("tile-color-picker-toggle").click()` with `getByTestId("settings-toggle").click()`, then click the swatch inside `settings-modal`, then close the modal with `settings-close` (or Escape) before asserting tile appearance. The post-reload re-check does the same. Keep every existing assertion about persistence and about the other player's tiles being unaffected.
    2. `hanabi-table-polish.spec.ts` HINT-03: the keep-hints toggle is now reached through `settings-toggle` → `keep-hints-toggle` → close. Keep the on/off/persist assertions unchanged.
    3. `start-game.spec.ts` UI-11 fit test: replace the `audio-mute-toggle` `toBeInViewport()` assertion with `settings-toggle`, since the audio control is no longer on the board. Add an assertion that `settings-modal` has count 0 by default.
    4. Add one new test to `hanabi-table-polish.spec.ts`: open the settings modal, assert `audio-volume`, `audio-mute-toggle`, `keep-hints-toggle` and `tile-color-swatch-plum` are all visible inside it, press Escape, and assert `settings-modal` has count 0 and the board is still interactive (the own hand is still visible).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tile-color-picker-toggle' e2e/hanabi-table-polish.spec.ts` returns 0
    - `grep -c 'settings-toggle' e2e/hanabi-table-polish.spec.ts` is at least 3
    - `grep -c 'audio-mute-toggle' e2e/start-game.spec.ts` returns 0
    - a test named for the settings modal exists and asserts all four controls inside it plus Escape-to-close
    - `npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts` passes
  </acceptance_criteria>
  <done>The browser suite reaches every relocated control through the modal and still proves each preference's behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → client render | tile-colour, keep-hints and volume preferences read back from storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-32 | Tampering | SettingsModal preference controls | mitigate | no storage code changes — `readTileColorPref`/`readKeepHintsPref` keep their existing unknown-value-degrades-to-default guards (T-06.2-07); the modal only calls the existing handlers |
| T-06.2-33 | Information disclosure | settings state | accept | every control in the modal is a browser-local personal preference that never reaches the wire; no game state is rendered in the dialog |
| T-06.2-34 | Denial of Service | modal over an active game | mitigate | the modal is a pure overlay — it never disables or unmounts the board, and Escape plus an explicit close button both dismiss it, asserted in e2e |
</threat_model>

<verification>
- `npm test` passes
- `npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts` passes
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
A gear in the top right opens a centre-justified modal holding tile colour, volume, mute and hint persistence; the bottom row holds only own hand, play/discard and the clue picker; every relocated control is still proven to work in a real browser.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-13-SUMMARY.md` when done
</output>
