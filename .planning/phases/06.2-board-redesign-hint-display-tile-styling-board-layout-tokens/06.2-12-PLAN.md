---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - apps/web/public/backgrounds/wood-board.webp
  - apps/web/public/backgrounds/city-fireworks.webp
  - apps/web/public/backgrounds/CREDITS.md
  - apps/web/app/globals.css
  - apps/web/lib/background-asset.test.ts
autonomous: true
requirements: [TILE-02]

must_haves:
  truths:
    - "The board surface is a real sourced wood-texture photograph, not a CSS-generated gradient"
    - "The page background is a city on the water with fireworks, not the old plain city-at-night skyline"
    - "Every sourced image has an author, source URL, licence and licence URL recorded in CREDITS.md"
  artifacts:
    - path: "apps/web/public/backgrounds/wood-board.webp"
      provides: "dark wood board texture, sourced and licence-verified"
    - path: "apps/web/public/backgrounds/city-fireworks.webp"
      provides: "city-on-water-with-fireworks page background, sourced and licence-verified"
    - path: "apps/web/public/backgrounds/CREDITS.md"
      provides: "per-image attribution record for all three background assets"
  key_links:
    - from: "apps/web/app/globals.css .board-surface"
      to: "/backgrounds/wood-board.webp"
      via: "background-image url()"
      pattern: "url\\([\"']/backgrounds/wood-board\\.webp[\"']\\)"
    - from: "apps/web/app/globals.css .table-backdrop"
      to: "/backgrounds/city-fireworks.webp"
      via: "background-image url()"
      pattern: "url\\([\"']/backgrounds/city-fireworks\\.webp[\"']\\)"
---

<objective>
Close UAT gaps 2 and 9: replace the CSS-generated "gross monotone" board texture with a sourced dark-wood image, and replace the city-at-night page background with a city-on-the-water-with-fireworks image. Both licence-verified and credited.

Purpose: the owner explicitly rejected the generated texture ("don't generate the texture either — find something free online that looks good, looks board game-y") and the current background ("the background image is also bad"). This reverses decision D-11's CSS-first preference via D-11's own documented fallback.
Output: two new WebP assets, a per-image CREDITS.md, rewired `.board-surface` and `.table-backdrop` rules, and an asset test covering all three images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-05-SUMMARY.md

<interfaces>
The 06.1-05 precedent (read its SUMMARY before sourcing anything): Unsplash and Pexels both return bot-challenge pages (Anubis / Cloudflare) to scripted `curl`, so their licence pages cannot be genuinely verified non-interactively. Wikimedia Commons works: `https://commons.wikimedia.org/w/api.php?action=query&prop=imageinfo&iiprop=extmetadata&titles=File:NAME&format=json` returns machine-readable `LicenseShortName`, `LicenseUrl`, `Artist` and `AttributionRequired`. Downloads are re-encoded through PIL before committing, which strips any embedded payload from the original file.

Existing CSS (apps/web/app/globals.css):
- `.table-backdrop` (~line 233): `background-color: var(--color-bg)` + `linear-gradient(rgba(11, 15, 26, 0.75), rgba(11, 15, 26, 0.75))` scrim over `url("/backgrounds/city-night.webp")`, `cover`, `center`, `fixed`.
- `.board-surface` (~line 254): `color-mix(in srgb, var(--color-surface) 85%, black 15%)` plus two `repeating-linear-gradient` grain layers and `background-blend-mode: overlay, multiply`. These grain layers are the generated texture the owner rejected.

Binding CSS rules (globals.css header, enforced by `apps/web/lib/theme-tokens.test.ts` and `background-asset.test.ts`): no hex literal anywhere outside the `@theme` block; `rgba()` derived from a documented token value is permitted; `--color-accent` keeps exactly its seven documented uses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source, licence-verify and optimize the wood and fireworks images</name>
  <files>apps/web/public/backgrounds/wood-board.webp, apps/web/public/backgrounds/city-fireworks.webp, apps/web/public/backgrounds/CREDITS.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-05-SUMMARY.md (the sourcing precedent and why Commons is the working source)
    - apps/web/public/backgrounds/CREDITS.md (the exact record format to extend — Source URL / Author / Author URL / License / License URL / License grant (quoted) / Retrieved / Modifications)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md (gaps 2 and 9, the owner's verbatim wording)
  </read_first>
  <action>
    Source two images and prove each one's licence before committing it.

    Image A — `wood-board.webp`: a dark wood surface texture that reads as a board-game table. Search Wikimedia Commons categories such as "Wood textures", "Wooden boards", "Dark wood" via the Commons API search endpoint. Requirements: dark (so light text and tiles stay legible over it), even grain across the frame, no objects/text/people in shot, tileable-looking or large enough that a `cover` crop shows only grain.

    Image B — `city-fireworks.webp`: a city on the water with fireworks in the background, per the owner's literal description. Search Commons categories such as "Fireworks over water", "New Year fireworks", "Fireworks and skylines". Requirements: night scene, waterfront skyline visible, fireworks present, dark enough that the existing 75% `--color-bg` scrim keeps card and text contrast, no readable text, no identifiable people.

    For each: fetch `action=query&prop=imageinfo&iiprop=extmetadata` for the file page and record `LicenseShortName`, `LicenseUrl`, `Artist`, `AttributionRequired`. Only a clearly free licence (CC0, CC BY, CC BY-SA, or public domain) is acceptable. Download the original, resize to 1920px wide with PIL/LANCZOS, re-encode as WebP quality 80, and confirm the result is at most 300000 bytes — lower the quality until it is. Re-encoding through PIL is the T-06.2-SC mitigation: never commit the bytes fetched from the network directly.

    Rewrite `CREDITS.md` as three `## <filename>` sections (the existing `city-night.webp` record is kept verbatim as its own section even though the file is being replaced — note in that section that it is superseded by `city-fireworks.webp` and delete `city-night.webp` from `public/backgrounds/` only after `globals.css` no longer references it in Task 2). Each new section carries the same line-prefixed fields the existing record uses: `Source URL:`, `Author:`, `Author URL:`, `License:`, `License URL:`, `License grant (quoted):`, `Retrieved:`, `Modifications:`.

    Escape hatch (do not work around it): if neither image can be sourced with a genuinely fetched, machine-verifiable free licence, stop and report. Do not commit an unverified image, do not fall back to a generated texture, and do not invent a credit line.
  </action>
  <verify>
    <automated>cd /home/rflor/games && python3 -c "
import sys
for p in ['apps/web/public/backgrounds/wood-board.webp','apps/web/public/backgrounds/city-fireworks.webp']:
    b=open(p,'rb').read()
    assert b[0:4]==b'RIFF' and b[8:12]==b'WEBP', p
    assert 0 < len(b) <= 300000, (p, len(b))
    print(p, len(b), 'ok')
c=open('apps/web/public/backgrounds/CREDITS.md').read()
for name in ['wood-board.webp','city-fireworks.webp']:
    assert name in c, name
for field in ['Source URL:','Author:','License:','License URL:']:
    assert c.count(field) >= 3, field
print('credits ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - both files exist, start with RIFF/WEBP magic bytes, and are each ≤ 300000 bytes
    - `grep -c '^Source URL: https://' apps/web/public/backgrounds/CREDITS.md` returns at least 3
    - `grep -c '^License URL: https://' apps/web/public/backgrounds/CREDITS.md` returns at least 3
    - CREDITS.md contains a `## wood-board.webp` and a `## city-fireworks.webp` section
    - the SUMMARY records, for each new image, the exact Commons API URL queried and the `LicenseShortName` it returned
  </acceptance_criteria>
  <done>Two free-licensed, size-bounded, re-encoded WebP assets are committed with a machine-verifiable attribution record for each.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the sourced images into .board-surface and .table-backdrop</name>
  <files>apps/web/app/globals.css, apps/web/lib/background-asset.test.ts</files>
  <read_first>
    - apps/web/app/globals.css (the `.table-backdrop` and `.board-surface` rules and the file header's no-hex-outside-@theme rule)
    - apps/web/lib/background-asset.test.ts (whole file — the assertions being generalised from one image to three)
    - apps/web/lib/theme-tokens.test.ts (the no-hex-outside-@theme and reduced-motion-pairing source scan this must not break)
  </read_first>
  <action>
    In `globals.css`:

    Rewrite `.board-surface` to paint the sourced wood image: `background-color: var(--color-surface)` as the under-fill, `background-image` = a darkening `linear-gradient(rgba(11, 15, 26, 0.35), rgba(11, 15, 26, 0.35))` over `url("/backgrounds/wood-board.webp")`, `background-size: cover`, `background-position: center`, keeping the existing `border: 1px solid var(--color-border)` and `border-radius: 12px`. Delete both `repeating-linear-gradient` grain layers and the `background-blend-mode` line — those are the generated texture the owner rejected. Replace the rule's doc comment: it currently documents the CSS-only texture as a D-11 decision; the new comment must state that D-11's CSS-first preference is reversed by owner review (UAT gap 2) via D-11's own explicit image fallback, and name the CREDITS.md record.

    Rewrite `.table-backdrop`'s `url("/backgrounds/city-night.webp")` to `url("/backgrounds/city-fireworks.webp")`, keeping the 0.75 scrim, `cover`, `center` and `fixed` exactly as they are. Update its comment to name the new image.

    Delete `apps/web/public/backgrounds/city-night.webp` once no CSS references it.

    Use no hex literal anywhere outside `@theme` — the `rgba(11, 15, 26, N)` form is the existing, permitted derivation of `--color-bg`'s documented value.

    In `background-asset.test.ts`: generalise the asset test from one file to a table of two (`wood-board.webp`, `city-fireworks.webp`), each asserted for RIFF/WEBP magic bytes and the 300000-byte cap. Assert CREDITS.md carries a `Source URL:`/`Author:`/`License:`/`License URL:` line for each. Replace the `.table-backdrop` rule assertion's filename, and add a `.board-surface` rule assertion: it references `url("/backgrounds/wood-board.webp")`, sets `background-size: cover`, contains NO `repeating-linear-gradient` and no `background-blend-mode`, and contains no hex literal. Keep the existing repo-wide "no hex outside @theme" assertion untouched. Assert `city-night.webp` appears nowhere in `globals.css`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web background-asset theme-tokens && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'repeating-linear-gradient' apps/web/app/globals.css` returns 0 inside the `.board-surface` rule (verify by reading the rule; the file may legitimately use the function elsewhere — if it does, assert on the rule body only)
    - `grep -q 'url("/backgrounds/wood-board.webp")' apps/web/app/globals.css` succeeds
    - `grep -q 'url("/backgrounds/city-fireworks.webp")' apps/web/app/globals.css` succeeds
    - `grep -c 'city-night' apps/web/app/globals.css` returns 0, and `apps/web/public/backgrounds/city-night.webp` no longer exists
    - `npx vitest run --project web background-asset theme-tokens` passes
    - `npx playwright test -g "UI-11"` still passes (the backdrop swap must not change layout)
  </acceptance_criteria>
  <done>The board paints a sourced wood photograph and the page paints a sourced city-with-fireworks photograph, both covered by asset tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → repository | downloaded third-party image bytes enter the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-SC | Tampering | downloaded image files | mitigate | every downloaded file is re-decoded and re-encoded through PIL before being committed, stripping embedded metadata/payloads (the 06.1-05 T-06.1-17 precedent); no package-manager install occurs in this plan |
| T-06.2-30 | Repudiation | licence provenance | mitigate | licence is read from the Commons API's `extmetadata` in this session and quoted into CREDITS.md with the exact source URL; an unverifiable image is not committed at all |
| T-06.2-31 | Denial of Service | page weight | mitigate | both assets capped at 300000 bytes and asserted by `background-asset.test.ts` |
</threat_model>

<verification>
- `npx vitest run --project web background-asset theme-tokens` passes
- `npx playwright test -g "UI-11"` passes
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
The board's wood and the page's background are sourced photographs with verifiable free licences recorded per image, the generated grain gradients are gone, and the asset test covers every background file in the directory.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-12-SUMMARY.md` when done
</output>
