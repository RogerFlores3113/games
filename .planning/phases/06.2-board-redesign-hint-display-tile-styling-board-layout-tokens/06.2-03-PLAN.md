---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/hanabi-hint-logic.ts
  - apps/web/lib/hanabi-hint-logic.test.ts
  - apps/web/lib/keep-hints-pref.ts
  - apps/web/lib/tile-color-pref.ts
  - apps/web/lib/tile-color.test.ts
autonomous: true
requirements: [HINT-01, HINT-02, HINT-03, HINT-04, TILE-03]

must_haves:
  truths:
    - "D-05: with keep-hints OFF (the default) a tile's hints clear once the next player has acted; with it ON they persist until the card leaves the hand"
    - "Hint visibility is derived synchronously from facts and history on every render — no setTimeout, no ref-based clear, no effect cleanup (RESEARCH.md Pitfall 4)"
    - "D-06: a colour hint and a number hint on the same tile both survive into the display shape — they are independent channels, not mutually exclusive"
    - "D-07: no ruled-out / negative information appears anywhere in the hint display shape"
    - "D-13/D-14: the tile colour preference is a fixed preset palette, stored per player through safe-storage.ts, and never leaves the browser"
  artifacts:
    - path: "apps/web/lib/hanabi-hint-logic.ts"
      exports: ["hintDisplayFor", "hintsVisibleForCard"]
      provides: "pure, facts-only hint derivation and the turn-based clear rule"
    - path: "apps/web/lib/keep-hints-pref.ts"
      exports: ["readKeepHintsPref", "writeKeepHintsPref"]
    - path: "apps/web/lib/tile-color-pref.ts"
      exports: ["TILE_COLOR_PRESETS", "readTileColorPref", "writeTileColorPref"]
  key_links:
    - from: "apps/web/lib/keep-hints-pref.ts"
      to: "apps/web/lib/safe-storage.ts"
      via: "safeGetItem/safeSetItem — the only storage wrapper"
      pattern: "safe-storage"
    - from: "apps/web/lib/tile-color-pref.ts"
      to: "globals.css tile preset tokens"
      via: "each preset's cssValue references var(--color-tile-preset-*)"
      pattern: "--color-tile-preset"
---

<objective>
Build the pure client logic the hint system and the two new personal preferences rest on, before any component renders them.

Purpose: HINT-01..04's data derivation (D-05/D-06/D-07) and TILE-03's storage/palette (D-13/D-14). Keeping these pure and tested first is what stops the hint-persistence timer bug 06.1 already fixed once from reappearing.
Output: `hanabi-hint-logic.ts`, `keep-hints-pref.ts`, `tile-color-pref.ts` with unit coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md

<interfaces>
From apps/web/lib/hanabi-visual-logic.ts:
- `CardFacts` — the facts-only type own-hand components are allowed to receive (possibleSuits, possibleRanks, positiveClues, negativeClues); `candidateDisplayFor(facts, variant)` is its existing pure-derivation precedent.
From @games/rules: `HistoryEntryView` union with members typed "play" | "discard" | "clue" | "draw"; the clue member carries `{ turn, type, seatId, targetSeatId, clue: { type: "color" | "rank"; value }, touchedCardIds: string[] }`.
From apps/web/lib/safe-storage.ts: `safeGetItem(key)`, `safeSetItem(key, value)`, `safeRemoveItem(key)` — SSR-safe and non-throwing. Never write a second wrapper.
From apps/web/lib/hanabi-notes.ts: the key-scoping convention to mirror (a small exported key builder plus read/write pair).
Resolved during planning (06.2-01 Task 3 asserts it): `applyClueToSlotFacts` in packages/rules only ever APPENDS to `positiveClues`. Hint display is therefore a pure function of current facts; no client-side accumulation state is needed.
Preset palette (UI-SPEC): slate (default, `color-mix(in srgb, var(--color-surface) 100%, transparent)`), warm-sand, cool-teal, plum, charcoal — the last four use the `--color-tile-preset-*` tokens added by 06.2-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hanabi-hint-logic.ts — facts-only hint derivation and the turn-based clear rule</name>
  <files>apps/web/lib/hanabi-hint-logic.ts, apps/web/lib/hanabi-hint-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-visual-logic.ts (CardFacts, candidateDisplayFor, touchedCardIdsFromLatestClue)
    - apps/web/lib/hanabi-visual-logic.test.ts (test style to mirror)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md (Pattern 1, Pitfall 4)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Hint Display System)
  </read_first>
  <behavior>
    - hintDisplayFor(facts) returns { colorHints, numberHints }, each a de-duplicated array in first-told order, derived only from facts.positiveClues
    - hintDisplayFor ignores negativeClues, possibleSuits and possibleRanks entirely (D-07)
    - hintDisplayFor on a card told red then 3 returns one colour hint and one number hint (D-06 composition input)
    - hintDisplayFor on facts with no positive clues returns two empty arrays
    - hintsVisibleForCard(history, cardId, { keepVisible: true }) is true whenever the card has ever been touched by a clue in history
    - hintsVisibleForCard(history, cardId, { keepVisible: false }) is true when the most recent non-draw history entry is a clue whose touchedCardIds contains cardId
    - hintsVisibleForCard(history, cardId, { keepVisible: false }) is false once any later play, discard or clue entry follows that clue
    - draw entries appended after a clue do NOT clear hints (a draw is not a player acting)
    - both functions are pure: calling them twice with the same inputs returns equal results and touches no timer, ref or storage
  </behavior>
  <action>
    Per D-05/D-06/D-07 and RESEARCH.md Pattern 1. Create apps/web/lib/hanabi-hint-logic.ts exporting `HintDisplay`, `hintDisplayFor(facts: CardFacts): HintDisplay`, and `hintsVisibleForCard(history: HistoryEntryView[], cardId: string, options: { keepVisible: boolean }): boolean`. Derive both entirely from the inputs on each call — no module-level mutable state, no setTimeout, no ref. Write a file header recording: (a) this module is own-hand-safe, taking `CardFacts` only and never a card object, suit or rank; (b) hint persistence is TURN-based, derived from history, never time-based — the `justCluedIds` 2000ms flash pattern in HanabiBoard.tsx is reserved for the unrelated clue-touch flash and must not be copied here (06.1 CR-01); (c) negative/ruled-out information is deliberately absent per D-07, owner-confirmed, and must not be reintroduced.
    Create hanabi-hint-logic.test.ts covering every behaviour bullet, building history fixtures as plain `HistoryEntryView` arrays in the style of hanabi-visual-logic.test.ts.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-hint-logic && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'setTimeout|useRef|useEffect|localStorage' apps/web/lib/hanabi-hint-logic.ts` returns nothing
    - `grep -vE '^\s*(//|\*|/\*)' apps/web/lib/hanabi-hint-logic.ts | grep -cE '\.suit|\.rank|negativeClues|possibleSuits|possibleRanks'` is 0
    - every behaviour bullet has a matching `it(...)` in hanabi-hint-logic.test.ts
    - `npx vitest run --project web hanabi-hint-logic` passes
  </acceptance_criteria>
  <done>Hint content and hint lifetime are both pure functions of data already on screen, proven by unit tests, with no timer anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: keep-hints and tile-colour preferences on safe-storage</name>
  <files>apps/web/lib/keep-hints-pref.ts, apps/web/lib/tile-color-pref.ts, apps/web/lib/tile-color.test.ts</files>
  <read_first>
    - apps/web/lib/safe-storage.ts
    - apps/web/lib/hanabi-notes.ts (key-scoping convention)
    - apps/web/lib/hanabi-discard-logic.ts (readDiscardViewPref/writeDiscardViewPref — the closest existing preference pair)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Tile-colour preset palette, keep-hints toggle)
  </read_first>
  <behavior>
    - readKeepHintsPref() returns false (hints clear after each move) when nothing is stored, and when the stored value is unrecognised
    - writeKeepHintsPref(true) then readKeepHintsPref() returns true; writeKeepHintsPref(false) round-trips likewise
    - TILE_COLOR_PRESETS has exactly five entries with ids slate, warm-sand, cool-teal, plum, charcoal, each carrying a human label used as the picker's aria-label and a CSS value referencing only var(--color-*) tokens
    - readTileColorPref() returns "slate" when nothing is stored or the stored id is not a known preset
    - writeTileColorPref(id) then readTileColorPref() returns that id
    - neither module imports room-socket, room-store, nanoid, or anything that can reach the wire — both are browser-local only (D-13)
  </behavior>
  <action>
    Per D-05 and D-13/D-14. Create keep-hints-pref.ts exporting `KEEP_HINTS_KEY`, `readKeepHintsPref()`, `writeKeepHintsPref(value: boolean)`, routed exclusively through safe-storage.ts. Scope the key per player, not per room or per card — the preference survives across rooms.
    Create tile-color-pref.ts exporting `TileColorId`, `TILE_COLOR_PRESETS` (the five presets above, in picker order, each `{ id, label, cssValue }` where slate's cssValue is the surface-derived `color-mix(...)` and the other four reference the `--color-tile-preset-*` tokens), `TILE_COLOR_KEY`, `readTileColorPref()`, `writeTileColorPref(id)`. No raw hex anywhere in the file.
    Create tile-color.test.ts covering every behaviour bullet for both modules (mirroring safe-storage.test.ts's storage-stubbing approach), including a source scan asserting neither file contains a hex literal or an import of room-socket/room-store/nanoid.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web tile-color && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'safe-storage' apps/web/lib/keep-hints-pref.ts apps/web/lib/tile-color-pref.ts` is 1 for each file
    - `grep -nE '#[0-9A-Fa-f]{3,8}' apps/web/lib/tile-color-pref.ts` returns nothing
    - `grep -c 'id:' apps/web/lib/tile-color-pref.ts` shows five presets and the test asserts the exact id list
    - `npx vitest run --project web tile-color` passes
  </acceptance_criteria>
  <done>Both personal preferences persist through the one storage wrapper, default safely, and carry a token-only palette ready for the picker UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| own-hand facts → display shape | A facts-only derivation must never widen into card identity |
| browser storage → render | Untrusted/legacy stored values are parsed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-06 | Information Disclosure | hanabi-hint-logic.ts | mitigate | Module accepts `CardFacts` only; acceptance grep proves no `.suit`/`.rank` read; downstream components are covered by own-hand-source.test.ts in 06.2-04 |
| T-06.2-07 | Tampering | localStorage preference values | mitigate | Both readers validate against a known set and fall back to the documented default rather than trusting the stored string |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run --project web hanabi-hint-logic tile-color` green
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
Hint content, hint lifetime, the keep-hints toggle state and the tile-colour preference all exist as tested, pure or storage-only modules with no React, no timers and no identity leaks.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-03-SUMMARY.md` when done
</output>
