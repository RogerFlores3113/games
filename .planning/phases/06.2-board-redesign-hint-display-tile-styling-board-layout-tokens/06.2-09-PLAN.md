---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 09
type: execute
wave: 5
depends_on: ["06.2-06"]
files_modified:
  - apps/web/lib/hanabi-drag-logic.ts
  - apps/web/lib/hanabi-drag-logic.test.ts
  - apps/web/components/hanabi/useHandDrag.ts
  - apps/web/components/hanabi/Hand.tsx
  - apps/web/components/hanabi/OwnHandCard.tsx
  - apps/web/lib/own-hand-source.test.ts
  - apps/web/lib/own-hand-render.test.ts
autonomous: true
requirements: [DRAG-01]

must_haves:
  truths:
    - "DRAG-01: while a tile is being dragged within the hand, the other tiles slide aside to open a gap at the current drop position"
    - "Under prefers-reduced-motion the gap still appears, with no animated transition"
    - "The shift-aside is a pure function of the drag state and the hand order — it introduces no new legality, no new action, and no timer"
    - "D-15 holds: useHandDrag and OwnHandCard still read card ids and facts only"
  artifacts:
    - path: "apps/web/lib/hanabi-drag-logic.ts"
      exports: ["shiftOffsetsForDrag"]
      provides: "per-slot shift offsets for the drop-gap preview"
  key_links:
    - from: "apps/web/components/hanabi/Hand.tsx"
      to: "apps/web/lib/hanabi-drag-logic.ts shiftOffsetsForDrag"
      via: "offset applied as a transform on each non-dragged own-hand slot"
      pattern: "shiftOffsetsForDrag"
---

<objective>
Make the drop position legible during a hand reorder: the other tiles shift aside to open the gap where the dragged tile will land.

Purpose: DRAG-01 (D-08) — the owner's first listed request from the 06.1 sign-off.
Output: a pure offset calculation, the hook exposing the current drop index, and the animated (and reduced-motion) rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md

<interfaces>
From apps/web/lib/hanabi-drag-logic.ts: `reorderedCardIds(handIds, draggedId, targetIndex)`, `resolveDropTarget(...)`, `applyPendingOrder`, `exceedsDragThreshold`, `DropTarget` (`kind` includes "slots" | "play" | "discard" | "none").
From apps/web/components/hanabi/useHandDrag.ts: `dragState` carries `cardId` and `offset` today and already resolves a slot drop target from registered slot rects; `registerSlot` is the per-slot rect registration Hand passes down; the hook returns `pendingOrder`, `onCardPointerDown`, `registerSlot`, `consumeClickSuppression`, `droppedCardIdsRef`, `playZoneRef`, `discardZoneRef`.
From 06.2-02 globals.css: the `.tile-shift` class (`transition: transform 150ms ease-out`) with its paired prefers-reduced-motion `transition: none` block — use the class, do not inline a new transition.
Own-hand card width today: 88px (OwnHandCard CARD_WIDTH) with the hand row's gap — the shift distance is one slot pitch, derived from the registered slot rects rather than hardcoded.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shiftOffsetsForDrag — pure gap geometry</name>
  <files>apps/web/lib/hanabi-drag-logic.ts, apps/web/lib/hanabi-drag-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-drag-logic.ts (whole file)
    - apps/web/lib/hanabi-drag-logic.test.ts (existing reorder-branch coverage)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Drag shift-aside)
  </read_first>
  <behavior>
    - With no drag in progress, every slot's offset is 0
    - Dragging slot 0 toward slot 2 shifts slots 1 and 2 left by one slot pitch and leaves later slots at 0
    - Dragging slot 3 toward slot 1 shifts slots 1 and 2 right by one slot pitch
    - The dragged tile's own offset is always 0 (it follows the pointer, not the gap)
    - A target index equal to the dragged tile's current index yields all-zero offsets
    - A target index out of range is clamped to the ends rather than producing an offset for a non-existent slot
    - The resulting order implied by the offsets always matches reorderedCardIds for the same inputs
  </behavior>
  <action>
    Add `shiftOffsetsForDrag(handIds, draggedId, targetIndex, slotPitchPx)` to hanabi-drag-logic.ts, returning a per-id offset map (or an array aligned to handIds) in pixels along the hand's axis. Keep it pure and framework-free, matching the file's existing style and its "no HTML5 DnD / pointer-events only" header. Extend hanabi-drag-logic.test.ts with the behaviour bullets, including the consistency check against `reorderedCardIds`.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-drag-logic && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'shiftOffsetsForDrag' apps/web/lib/hanabi-drag-logic.ts apps/web/lib/hanabi-drag-logic.test.ts` matches in both
    - every behaviour bullet has a matching `it(...)`
    - `npx vitest run --project web hanabi-drag-logic` passes
  </acceptance_criteria>
  <done>The gap geometry is decided by a tested pure function, consistent with the reorder it previews.</done>
</task>

<task type="auto">
  <name>Task 2: Tiles slide aside during the drag</name>
  <files>apps/web/components/hanabi/useHandDrag.ts, apps/web/components/hanabi/Hand.tsx, apps/web/components/hanabi/OwnHandCard.tsx, apps/web/lib/own-hand-source.test.ts, apps/web/lib/own-hand-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/useHandDrag.ts (whole file)
    - apps/web/components/hanabi/Hand.tsx (own-hand slot render, post-06.2-06)
    - apps/web/components/hanabi/OwnHandCard.tsx (the drag transform block)
    - apps/web/lib/own-hand-source.test.ts (USE_HAND_DRAG_PATH and OWN_HAND_CARD_PATH describes)
    - apps/web/app/globals.css (`.tile-shift` and its reduced-motion pairing)
  </read_first>
  <action>
    Per D-08. Expose the current slot drop index and the measured slot pitch from `useHandDrag`'s returned drag state (derived from the already-registered slot rects — no new measurement mechanism, no new listener). In Hand.tsx compute `shiftOffsetsForDrag` for the own-hand row while a drag is in flight and pass each slot's offset into `OwnHandCard` as a `shiftOffsetPx` prop. In OwnHandCard apply the offset as a `transform: translateX(...)` on the slot wrapper with the `.tile-shift` class so the motion (and its reduced-motion static form) comes from globals.css, composed with — not replacing — the existing drag-follow transform on the dragged card itself. The dragged card keeps `pointer-events: none` on its moving layer as today.
    Update own-hand-source.test.ts only if the changed files need their scans re-asserted (the existing USE_HAND_DRAG_PATH and OWN_HAND_CARD_PATH describes must still pass with the new prop), and update own-hand-render.test.ts's fixture with the new no-op `shiftOffsetPx` prop, re-verifying the byte-identical guard (the 06.1-12 precedent for fixture updates).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web && npx playwright test e2e/hanabi-table-polish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'shiftOffsetsForDrag' apps/web/components/hanabi/Hand.tsx` matches
    - `grep -n 'tile-shift' apps/web/components/hanabi/OwnHandCard.tsx` matches and `grep -nE 'transition:.*transform' apps/web/components/hanabi/OwnHandCard.tsx` shows no inline duplicate of the transition
    - `grep -nE 'onDragStart|dataTransfer|draggable=' apps/web/components/hanabi/useHandDrag.ts` returns nothing
    - `npx vitest run --project web` fully green including own-hand-render and own-hand-source
  </acceptance_criteria>
  <done>A drag now shows where the tile will land, and reduced-motion users still see the gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| own-hand render | A new per-slot prop must not smuggle identity onto own-hand components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-20 | Information Disclosure | shiftOffsetPx prop path | mitigate | Offset is a number derived from ids and rects only; own-hand-source and own-hand-render guards re-run in Task 2 |
| T-06.2-21 | Tampering | reorder submitted from a stale preview | accept | Unchanged from 06.1 — the server's `canReorder` permutation check already rejects a stale hand order |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; no drag library introduced |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0
- `npx playwright test e2e/hanabi-table-polish.spec.ts` passes
</verification>

<success_criteria>
Dragging a tile inside your hand visibly opens a gap at the drop position, animated normally and static under reduced motion, with no change to reorder legality and no weakened identity guard.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-09-SUMMARY.md` when done
</output>
