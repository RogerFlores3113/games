---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 08
type: execute
wave: 5
depends_on: ["06.2-01", "06.2-07"]
files_modified:
  - apps/web/lib/hanabi-discard-drag-logic.ts
  - apps/web/lib/hanabi-discard-drag-logic.test.ts
  - apps/web/components/hanabi/useDiscardDrag.ts
  - apps/web/components/hanabi/Table.tsx
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/components/hanabi/DiscardOverlay.tsx
  - apps/web/lib/discard-overlay-render.test.ts
autonomous: true
requirements: [DISC-01]

must_haves:
  truths:
    - "DISC-01: any seated player can drag any discarded tile to a new position and every player sees the same resulting arrangement"
    - "D-27: the dragging player sees their pending order immediately and silently reconciles to the server's order on the next frame — no error toast, no dialog"
    - "The discard drag uses the project's pointer-event architecture; no HTML5 drag-and-drop and no drag library is introduced"
    - "reorderDiscard is dispatched through the same act() chokepoint as every other action and is blocked while reconnecting or ended"
  artifacts:
    - path: "apps/web/lib/hanabi-discard-drag-logic.ts"
      exports: ["reorderedDiscardIds", "discardDropIndex", "requestForDiscardDrop"]
      provides: "pure discard-pile drop resolution"
    - path: "apps/web/components/hanabi/useDiscardDrag.ts"
      exports: ["useDiscardDrag"]
      provides: "pointer-tracked discard drag with optimistic order and timeout fallback"
  key_links:
    - from: "apps/web/components/hanabi/useDiscardDrag.ts"
      to: "apps/web/components/hanabi/HanabiBoard.tsx act()"
      via: "onDropRequest dispatches { type: 'reorderDiscard', cardIds }"
      pattern: "reorderDiscard"
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "apps/web/components/hanabi/useDiscardDrag.ts"
      via: "per-tile pointerdown handlers and slot registration in the discard area"
      pattern: "useDiscardDrag"
---

<objective>
Make the shared discard arrangement interactive: any seated player can drag a discarded tile into a new position, and the change propagates to every screen through the server.

Purpose: the client half of DISC-01 (D-24..D-28), built on the same pointer-drag architecture as hand reorder rather than a second paradigm.
Output: `hanabi-discard-drag-logic.ts`, `useDiscardDrag.ts`, discard tiles draggable in both the board's discard area and the expanded overlay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md

<interfaces>
Existing pure drag helpers to mirror (apps/web/lib/hanabi-drag-logic.ts): `reorderedCardIds(handIds, draggedId, targetIndex)` — remove-then-insert with clamping, lines ~81-95; `applyPendingOrder<T extends { id: string }>(cards, pendingIds)` — generic, reusable verbatim for discard tiles, lines ~117-137; `requestForDrop(...)` — returns null for a no-op drop, lines ~142-169; `exceedsDragThreshold`.
Existing hook to mirror (apps/web/components/hanabi/useHandDrag.ts, whole file): window-level pointermove/pointerup/pointercancel listeners; `setPointerCapture` on pointerdown; `game`/`ctx`/`onDropRequest` read through refs inside window handlers so a stale closure never resolves against an old frame; pending order cleared whenever `game` changes identity; `PENDING_ORDER_TIMEOUT_MS = 2000` fallback; the `if (ctx.reconnecting || ctx.ended) return;` gate on pointerdown.
Difference from hand drag: every drop inside the discard area is a reorder — there is no play/discard outcome and no per-seat ownership check (D-25), so the zone model is a single array of tile rects.
From 06.2-01: `HanabiView.discardOrder`; the server validates the submitted ids are an exact permutation and rejects a stale submission (D-26), which is the entire stale-handling mechanism — do not re-derive legality on the client.
From 06.2-07: discard tiles render with `data-testid="discard-tile-{id}"` in `discardOrder` sequence inside the Discard area.
HanabiBoard.tsx: `HanabiActionRequest` union (~lines 31-35) and `act()` (~158) — `act` needs no change, only the union does.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure discard-pile drop resolution</name>
  <files>apps/web/lib/hanabi-discard-drag-logic.ts, apps/web/lib/hanabi-discard-drag-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-drag-logic.ts (whole file)
    - apps/web/lib/hanabi-drag-logic.test.ts (test style)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md (hanabi-discard-drag-logic section)
  </read_first>
  <behavior>
    - reorderedDiscardIds moves the dragged id to the target index, preserving every other id's relative order, and clamps an out-of-range index to the ends
    - reorderedDiscardIds returns the input order unchanged when the dragged id is not present
    - discardDropIndex maps a pointer position against an array of tile rects to the index the tile would land at, including the position past the last tile
    - requestForDiscardDrop returns null when the resulting order equals the current order (a no-op drop sends nothing)
    - requestForDiscardDrop returns { type: "reorderDiscard", cardIds } with the full new order otherwise
    - the module performs no legality checks of its own and imports nothing from React or the socket layer
  </behavior>
  <action>
    Per D-26/D-27 and the PATTERNS mapping. Create hanabi-discard-drag-logic.ts exporting `reorderedDiscardIds`, `discardDropIndex`, `requestForDiscardDrop` and any small rect type they need, mirroring hanabi-drag-logic.ts's function shapes and early-return discipline. Reuse `applyPendingOrder` from hanabi-drag-logic.ts by import rather than copying it — it is already generic over `{ id: string }`. Document in the header that stale submissions are rejected server-side by `canReorderDiscard`'s permutation check and that no client-side locking or conflict resolution exists by design (D-27, last write wins).
    Create hanabi-discard-drag-logic.test.ts covering every behaviour bullet.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-discard-drag-logic && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^import .*(react|room-socket|room-store)" apps/web/lib/hanabi-discard-drag-logic.ts` returns nothing
    - `grep -n 'applyPendingOrder' apps/web/lib/hanabi-discard-drag-logic.ts` shows it is imported, not redefined
    - every behaviour bullet has a matching `it(...)` in the test file
  </acceptance_criteria>
  <done>Discard drop resolution is a pure, tested function with no duplicated helpers.</done>
</task>

<task type="auto">
  <name>Task 2: useDiscardDrag and the reorderDiscard dispatch</name>
  <files>apps/web/components/hanabi/useDiscardDrag.ts, apps/web/components/hanabi/Table.tsx, apps/web/components/hanabi/HanabiBoard.tsx</files>
  <read_first>
    - apps/web/components/hanabi/useHandDrag.ts (whole file)
    - apps/web/components/hanabi/Table.tsx (post-06.2-07 Discard area and tile rendering)
    - apps/web/components/hanabi/HanabiBoard.tsx (HanabiActionRequest union, act(), the drag wiring for the hand)
    - apps/web/lib/hanabi-discard-drag-logic.ts (Task 1)
  </read_first>
  <action>
    Per D-24/D-25/D-27. Create useDiscardDrag.ts following useHandDrag.ts's structure: `setPointerCapture` on pointerdown, window-level pointermove/pointerup/pointercancel, refs for `game`/`ctx`/`onDropRequest`, `exceedsDragThreshold` before a visual drag starts, exactly one `onDropRequest` per completed drop, pending order cleared whenever `game` changes identity, and the same 2000ms pending-order timeout fallback. Its zone model is a single registered array of discard tile rects; every drop resolves through `requestForDiscardDrop`. Gate pointerdown on `ctx.reconnecting || ctx.ended` exactly as the hand hook does. No ownership or turn check — any seated player may drag any tile (D-25).
    Add `| { type: "reorderDiscard"; cardIds: string[] }` to `HanabiActionRequest` in HanabiBoard.tsx, call `useDiscardDrag` there with `onDropRequest: act` alongside the existing `useHandDrag` call (unconditionally, above the early return, so hook order never changes), and pass the resulting handlers plus the optimistic order down to `Table`. In Table.tsx, register each discard tile's rect, attach the pointerdown handler per tile, apply the pending order through `applyPendingOrder` over the resolved `discardOrder` tiles, and expose `data-dragging` on the tile being dragged. No change is needed in RoomClient.tsx — it forwards any `HanabiActionRequest` with a freshly minted actionId.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web packages/rules packages/schema apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'reorderDiscard' apps/web/components/hanabi/HanabiBoard.tsx` matches the union member
    - `grep -nE 'onDragStart|dataTransfer|draggable=' apps/web/components/hanabi/useDiscardDrag.ts apps/web/components/hanabi/Table.tsx` returns nothing (no HTML5 DnD)
    - `grep -n 'reconnecting' apps/web/components/hanabi/useDiscardDrag.ts` matches the pointerdown gate
    - `grep -c 'useDiscardDrag(' apps/web/components/hanabi/HanabiBoard.tsx` is 1 and the call sits above the `if (!game)` early return
    - `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
  </acceptance_criteria>
  <done>Dragging a discarded tile sends one reorderDiscard through the standard chokepoint and shows the new order immediately.</done>
</task>

<task type="auto">
  <name>Task 3: The expanded discard overlay follows and edits the same order</name>
  <files>apps/web/components/hanabi/DiscardOverlay.tsx, apps/web/lib/discard-overlay-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/DiscardOverlay.tsx (whole file)
    - apps/web/lib/discard-overlay-render.test.ts (whole file)
    - apps/web/components/hanabi/useDiscardDrag.ts (Task 2)
  </read_first>
  <action>
    Give `DiscardOverlay` a `discardOrder` prop and render its tiles in that order (resolving ids against `discard`, skipping unknown ids and appending unlisted discards, matching Table's defensive rule), and accept the same drag handlers so a tile can be rearranged from the expanded view as well as the board. Keep the dialog role, the close affordance, the empty-state copy and the existing `discard-overlay-card` testid.
    Extend discard-overlay-render.test.ts (do not replace it): with a `discardOrder` differing from `discard`'s array order, the rendered tile sequence follows `discardOrder`; the empty-state test still passes.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web discard-overlay-render table-render && npx tsc -b apps/web && npx playwright test e2e/hanabi-table-polish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'discardOrder' apps/web/components/hanabi/DiscardOverlay.tsx` matches
    - discard-overlay-render.test.ts contains an assertion that the tile sequence follows a non-insertion discardOrder
    - `npx playwright test e2e/hanabi-table-polish.spec.ts` passes
  </acceptance_criteria>
  <done>Both discard views show and edit one shared arrangement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input → reorderDiscard payload | Client-composed ids cross to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-17 | Tampering | a client sending ids not currently in the pile | mitigate | Server-side `canReorderDiscard` permutation check (06.2-01); the client deliberately performs no legality derivation of its own |
| T-06.2-18 | Repudiation | conflicting simultaneous reorders | accept | D-27 last-write-wins is the chosen behaviour for a small group on a voice call; rejection-on-stale already prevents a lost card |
| T-06.2-19 | Tampering | actions fired against a stale view while reconnecting | mitigate | pointerdown gate plus `act()`'s existing reconnecting/ended no-op (Task 2) |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; no drag library is introduced (RESEARCH.md "Don't Hand-Roll") |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
- `npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts` passes
</verification>

<success_criteria>
Any seated player can rearrange the discard pile by dragging, the arrangement is server-authoritative and identical everywhere, and a stale drag silently reconciles instead of erroring.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-08-SUMMARY.md` when done
</output>
