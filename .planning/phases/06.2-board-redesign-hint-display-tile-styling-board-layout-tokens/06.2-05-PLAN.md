---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 05
type: execute
wave: 2
depends_on: ["06.2-02"]
files_modified:
  - apps/web/components/hanabi/ClueTokenArt.tsx
  - apps/web/components/hanabi/FuseTokenArt.tsx
  - apps/web/components/hanabi/TokenColumn.tsx
  - apps/web/components/hanabi/PlayedStack.tsx
  - apps/web/lib/token-render.test.ts
  - apps/web/lib/stack-render.test.ts
autonomous: true
requirements: [BOARD-02, BOARD-03, BOARD-05]

must_haves:
  truths:
    - "BOARD-02: clue tokens render as black discs with a blue question mark in a vertical line, and fuse tokens as black discs with a yellow explosion ringed orange-red, beside them"
    - "BOARD-03: a spent token is absent from the DOM, not dimmed, and a text count remains for screen readers and tests"
    - "BOARD-05: a played stack shows every card it contains, fanned horizontally so the row's height is unchanged"
    - "Token art and stack fan are inline SVG/CSS built from @theme tokens — no image asset, no new dependency, no hex literal"
  artifacts:
    - path: "apps/web/components/hanabi/ClueTokenArt.tsx"
      exports: ["ClueTokenArt"]
    - path: "apps/web/components/hanabi/FuseTokenArt.tsx"
      exports: ["FuseTokenArt"]
    - path: "apps/web/components/hanabi/TokenColumn.tsx"
      exports: ["TokenColumn"]
      provides: "height-bounded vertical clue+fuse column with text counts"
    - path: "apps/web/components/hanabi/PlayedStack.tsx"
      exports: ["PlayedStack"]
      provides: "horizontally fanned stack showing every played rank"
  key_links:
    - from: "apps/web/components/hanabi/TokenColumn.tsx"
      to: "apps/web/lib/layout-budget.ts"
      via: "tokenPitchPx(columnHeightPx, tokenCount) sizes the discs from the left column's height"
      pattern: "tokenPitchPx"
    - from: "apps/web/components/hanabi/PlayedStack.tsx"
      to: "apps/web/lib/layout-budget.ts"
      via: "fannedStackWidth / FAN_PEEK_PX"
      pattern: "FAN_PEEK_PX"
---

<objective>
Build the board's new physical artefacts as standalone, separately tested components: the clue and fuse token art, the vertical token column that shrinks as tokens are spent, and the fanned played stack.

Purpose: BOARD-02, BOARD-03, BOARD-05 (D-18/D-19/D-20/D-22). Keeping them out of `Table.tsx` means each is render-tested on its own before the layout rework consumes them.
Output: four components and two render-test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md

<interfaces>
From 06.2-02 apps/web/lib/layout-budget.ts: LEFT_COLUMN_PX (260), MAX_TOKEN_COUNT (11), TOKEN_GAP_PX, PLAYED_CARD_WIDTH_PX (48), PLAYED_CARD_HEIGHT_PX (64), FAN_PEEK_PX (16), `tokenPitchPx(columnHeightPx, tokenCount)`, `fannedStackWidth(cardCount)`.
From 06.2-02 globals.css: `--color-token-disc`, `--color-token-fuse-rim`; existing `--color-suit-blue`, `--color-suit-yellow`, `--color-card-glow`, `--color-border`, `--color-text`, `--color-text-muted`.
Art conventions (06.2-PATTERNS.md): `SuitGlyph.tsx`'s `<svg viewBox aria-hidden focusable="false">` shell plus `useId()` for any gradient/pattern id; `suit-visuals.ts`'s `CARD_BACK_ART` layered-path-data-as-typed-array shape (`{ d, fillVar, opacity, fillRule? }`) is the convention for describing multi-layer art as data rather than JSX.
Existing components this plan reuses: `FireworkCardFace({ suit, rank, width, height, exposeSuit, showBurstCount })` for each card in a fanned stack; `SuitGlyph` for an empty stack's placeholder.
Testids Table.tsx currently exposes and 06.2-07 must preserve: `clue-tokens`, `fuse-tokens`, `played-stack-{suit}` with `data-top-rank` and `data-complete`. This plan's components must carry those same testids/attributes so the layout rework is a drop-in.
From @games/rules: `MAX_FUSES`, `RANKS`; `fusesRemainingForView(game)` lives in apps/web/lib/hanabi-board-logic.ts (fuses remaining, not used).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clue and fuse token art</name>
  <files>apps/web/components/hanabi/ClueTokenArt.tsx, apps/web/components/hanabi/FuseTokenArt.tsx, apps/web/lib/token-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/SuitGlyph.tsx
    - apps/web/lib/suit-visuals.ts (CARD_BACK_ART layered-art pattern, ~lines 261-281)
    - apps/web/lib/firework-card-render.test.ts (render scaffold + no-hex source scan + the gradient-id non-collision test)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Clue token art, Fuse token art)
  </read_first>
  <behavior>
    - ClueTokenArt renders one svg with a disc filled from var(--color-token-disc) and a question-mark glyph in var(--color-suit-blue)
    - FuseTokenArt renders one svg with a disc filled from var(--color-token-disc), an explosion in var(--color-suit-yellow) and a rim stroked with var(--color-token-fuse-rim)
    - Both accept a `size` prop and emit width/height equal to it
    - Both are aria-hidden with focusable="false" (the accessible information is the text count beside the column, not the art)
    - Rendering several instances on one page produces no duplicate SVG ids
    - Neither source file contains a hex colour literal
  </behavior>
  <action>
    Per D-18/D-20 and the owner's literal descriptions. Create ClueTokenArt.tsx and FuseTokenArt.tsx as inline-SVG components following SuitGlyph's shell exactly, with the path data expressed as a typed layer array in the `CARD_BACK_ART` style and every fill/stroke supplied as a `var(--color-*)` token. Use `useId()` for any gradient or pattern id. The fuse explosion silhouette must stay visually distinct from every existing suit burst silhouette (it is board furniture, not a suit).
    Create token-render.test.ts with firework-card-render.test.ts's scaffold covering every behaviour bullet, including the no-hex source scan for both files and an id-collision test rendering several tokens in one markup string.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web token-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9A-Fa-f]{3,8}' apps/web/components/hanabi/ClueTokenArt.tsx apps/web/components/hanabi/FuseTokenArt.tsx` returns nothing
    - `grep -n 'color-token-disc' apps/web/components/hanabi/ClueTokenArt.tsx apps/web/components/hanabi/FuseTokenArt.tsx` matches in both
    - `grep -n 'color-token-fuse-rim' apps/web/components/hanabi/FuseTokenArt.tsx` matches
    - every behaviour bullet has a matching `it(...)` in token-render.test.ts
  </acceptance_criteria>
  <done>The two token artworks exist as token-only inline SVG, tested and collision-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TokenColumn — vertical line, spent tokens removed, counts retained</name>
  <files>apps/web/components/hanabi/TokenColumn.tsx, apps/web/lib/token-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx (the current clue-tokens/fuse-tokens blocks, lines ~203-244, for the testids and count copy to preserve)
    - apps/web/lib/layout-budget.ts (06.2-02)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Board Layout token column, Height Budget)
  </read_first>
  <behavior>
    - With 5 clue tokens remaining, exactly 5 clue-token artworks are in the markup — spent tokens are absent, not dimmed or transparent
    - With 2 fuses remaining, exactly 2 fuse-token artworks are in the markup
    - The text counts "{n} clues left" and "{n} fuses left" are always present, including at zero tokens
    - The element carrying data-testid="clue-tokens" exposes data-count equal to the remaining clue count; the same for data-testid="fuse-tokens"
    - Token disc size is derived from the supplied column height via tokenPitchPx and never exceeds it — with the smallest supported column height the total rendered token height stays within the column height
    - The column renders clue tokens and fuse tokens as two vertical runs within one right-hand column
  </behavior>
  <action>
    Per D-16/D-18/D-19 and the UI-SPEC height budget. Create TokenColumn.tsx exporting `TokenColumn({ clueTokens, fusesRemaining, columnHeightPx })`. Render a flex column sized to `columnHeightPx` containing a clue run and a fuse run; compute the disc size from `tokenPitchPx(columnHeightPx, clueTokens + fusesRemaining)` clamped to a sensible minimum and maximum, so the column never forces the board to grow (the explicit Pitfall 1 mitigation: the column is sized FROM the left column's height, never the reverse). Render only the remaining tokens. Keep the existing `clue-tokens` and `fuse-tokens` testids on the elements carrying the text counts, add `data-count`, and give each token `data-testid="clue-token"`/`"fuse-token"` for counting. Copy strings exactly: "{n} clues left", "{n} fuses left".
    Extend token-render.test.ts with the behaviour bullets above.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web token-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'tokenPitchPx' apps/web/components/hanabi/TokenColumn.tsx` matches
    - `grep -n 'clues left\|fuses left' apps/web/components/hanabi/TokenColumn.tsx` matches both strings
    - `grep -nE 'opacity|dim' apps/web/components/hanabi/TokenColumn.tsx` shows no spent-token dimming path
    - token-render.test.ts asserts exact token counts for at least two different remaining values per token type
  </acceptance_criteria>
  <done>The token column shows only what is left, stays inside its height budget, and still reports counts as text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlayedStack — every card visible, fanned horizontally</name>
  <files>apps/web/components/hanabi/PlayedStack.tsx, apps/web/lib/stack-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx (the current played-stack block, lines ~149-199 — testids, data attributes, empty state and the completed-stack glow to preserve)
    - apps/web/components/hanabi/FireworkCard.tsx
    - apps/web/lib/layout-budget.ts (06.2-02)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Fanned played stacks)
  </read_first>
  <behavior>
    - A stack with topRank 3 renders three FireworkCardFace cards, ranks 1, 2 and 3, each with its own data-testid="played-stack-{suit}-card-{rank}"
    - A complete stack (topRank 5) renders five cards and keeps data-complete="true" plus the existing completed-stack glow styling
    - An empty stack (topRank 0) renders the existing faint SuitGlyph placeholder and no card faces
    - The container keeps data-testid="played-stack-{suit}" and data-top-rank equal to the stack's topRank
    - The container's height equals PLAYED_CARD_HEIGHT_PX regardless of card count, and its width equals fannedStackWidth(cardCount)
    - Each additional card is offset horizontally by FAN_PEEK_PX and layered above the previous one
  </behavior>
  <action>
    Per D-22 and the UI-SPEC's "fan the cheap axis" rule. Create PlayedStack.tsx exporting `PlayedStack({ stack, flashing })` where `stack` is a `HanabiView["stacks"][number]`. Render a relatively-positioned box of fixed `PLAYED_CARD_HEIGHT_PX` height and `fannedStackWidth(topRank)` width containing one `FireworkCardFace` per rank from 1 to topRank, each absolutely positioned at `index * FAN_PEEK_PX` with an increasing z-index so the top rank sits frontmost, `exposeSuit` and `showBurstCount` as today. Preserve the existing testids, `data-top-rank`, `data-complete`, the `anim-stack-flash` class when flashing, the completed-stack border/box-shadow values currently inlined in Table.tsx, the empty-stack placeholder and the `sr-only` suit name. Height must never grow with card count.
    Create stack-render.test.ts covering every behaviour bullet with the renderToStaticMarkup scaffold.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web stack-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FAN_PEEK_PX\|fannedStackWidth' apps/web/components/hanabi/PlayedStack.tsx` matches
    - `grep -n 'played-stack-' apps/web/components/hanabi/PlayedStack.tsx` shows both the container and per-card testids
    - stack-render.test.ts asserts a five-card complete stack and a three-card partial stack render the expected card counts
    - `npx vitest run --project web stack-render` passes
  </acceptance_criteria>
  <done>Every firework in a stack is visible without spending vertical space.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public view fields → board furniture | Only already-public values (clueTokens, fuses, stacks) reach these components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-10 | Information Disclosure | PlayedStack rendering identities | accept | Played stacks are public information by the rules of the game; no own-hand data reaches these components |
| T-06.2-11 | Tampering | design-system drift via inline hex | mitigate | Per-file no-hex source scans in token-render.test.ts (Task 1) |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run --project web token-render stack-render` green
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
The board's token art, the shrinking vertical token column and the fully visible fanned stacks exist as tested components, sized from the height ledger, ready for the layout rework to drop in.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-05-SUMMARY.md` when done
</output>
