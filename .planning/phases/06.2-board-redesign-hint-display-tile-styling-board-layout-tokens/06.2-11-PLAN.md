---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 11
type: execute
wave: 7
depends_on: ["06.2-10"]
files_modified:
  - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
autonomous: false
requirements: [HINT-01, HINT-02, HINT-03, HINT-04, NOTE-03, DRAG-01, TILE-01, TILE-02, TILE-03, BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-05, DISC-01]

must_haves:
  truths:
    - "The full gate is green before the owner is asked to look: vitest, playwright and tsc across all four projects"
    - "The owner sees the reworked board and rules on the five design decisions this phase left to Claude's discretion"
    - "The owner is explicitly asked to re-confirm D-07 (ruled-out information is gone with no replacement), since it was an unattended default"
    - "The owner's reply is recorded verbatim, with no fabricated observations"
  artifacts:
    - path: ".planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md"
      provides: "the owner's verbatim sign-off and any follow-on requests, in the 06.1-HUMAN-UAT.md format"
  key_links:
    - from: "06.2-HUMAN-UAT.md"
      to: ".planning/ROADMAP.md phase 06.2"
      via: "gaps recorded as follow-on requests, not defects, per the 06.1 precedent"
      pattern: "## Gaps"
---

<objective>
Close the phase the way 06.1 closed: run the whole gate, then put the reworked board in front of the owner for the visual decisions only they can make, and record what they say word for word.

Purpose: the phase gate plus owner sign-off on the original design work this phase produced — hint composition, the wooden texture, the fanned-stack overlap, token sizing and the tile-colour palette.
Output: `06.2-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md

<interfaces>
Precedent to follow exactly: 06.1-15's gate-then-sign-off shape, and 06.1-HUMAN-UAT.md's document format (a checks section, then a `## Gaps` section holding the owner's verbatim follow-on requests, labelled as requests rather than defects).
Outstanding owner decisions this phase deferred (UI-SPEC "Outstanding owner action"): D-06 hint composition (tint plus corner glyph plus corner numeral), the wooden-texture recipe, the fanned-stack overlap amount, the token disc size the height budget produced, and the five-preset tile-colour palette.
D-07 context for the re-confirmation ask: the owner was asked during discussion whether to keep ruled-out information in a quieter form or on hover, and answered "Let it go". This is the first time they will see the board without it.
Recording discipline carried from 06.1/04-08: record the owner's plain reply verbatim; never fabricate observations, frame contents or approvals they did not state; if they do not address something, write "owner did not state".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full phase gate</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-VALIDATION.md
  </read_first>
  <action>
    Run the phase gate in order and capture the results for the SUMMARY: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`. If anything fails, fix it or stop and report before the checkpoint — the owner is never asked to review a red build.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npm test && npx playwright test && npx tsc -b apps/web packages/rules packages/schema apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - all three commands exit 0
    - the SUMMARY records the test counts for the vitest and playwright runs
  </acceptance_criteria>
  <done>The build is green across unit, browser and type checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner visual sign-off on the reworked board</name>
  <files>(no files modified - owner review only)</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Outstanding owner action)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md (D-06, D-07, Claude's Discretion)
  </read_first>
  <action>
    Start the dev servers, tell the owner exactly how to reach the board, walk them through the ten review steps below, and pause for their reply. Do not proceed, summarise, or interpret on their behalf - wait for the resume signal.
  </action>
  <verify>
    <human-check>The owner has seen the live board and answered all ten review items, including the D-07 re-confirmation.</human-check>
  </verify>
  <acceptance_criteria>
    - The owner has been shown the board and asked all ten review items, including the explicit D-07 re-confirmation
    - The owner's reply has been received before any further work starts
  </acceptance_criteria>
  <done>The owner has reviewed the board and replied.</done>
  <what-built>
    The full 06.2 board redesign: clue-coloured hint tints with a non-colour marker and number hints stamped on the tile back, the keep-hints toggle, the pip rows removed, an always-visible autosaving note box above every own tile, raised tiles on a wooden board, a tile-colour picker, Play above Discard with labelled outlines, the deck counter between them, clue and fuse token art in a vertical column on the right that shrinks as tokens are spent, fully visible fanned played stacks, tiles that shift aside during a drag, and a shared discard order any player can rearrange.
  </what-built>
  <how-to-verify>
    1. Start the app and open a room in two browser windows, seat both, and start a game.
    2. Give a colour clue and a number clue, including one tile that receives both — judge the hint composition (D-06): the tint across the tile, the faint suit silhouette top-left, the numeral bottom-right.
    3. Toggle "Keep hints visible" on and off and take a turn each way — confirm the persistence behaviour is what you wanted.
    4. Look at the board surface: is the wooden texture right, with the city-at-night background still visible around it? Do the tiles read as objects resting on it?
    5. Cycle the tile-colour picker through all five presets and confirm each stays readable against the suit colours, glyphs and hint tints.
    6. Judge the token art and its sizing: black discs with a blue question mark, black discs with a yellow explosion and orange-red rim, in a vertical line on the right, shrinking as they are spent.
    7. Judge the fanned stacks: is every firework in a stack clearly visible at this overlap?
    8. Type in a note box, drag a tile within your hand to see the gap, and drag a discarded tile to a new position — confirm the other window sees the same discard order.
    9. Explicitly re-confirm D-07: ruled-out suits and ranks are no longer shown anywhere, in any form. You chose "Let it go" during discussion; this is the first time you are seeing it. Say if you want it back.
    10. Confirm the board fits your window without scrolling.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what you want changed — anything new is recorded as a follow-on request, not a defect.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the owner's reply verbatim</name>
  <files>.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md (document format)
  </read_first>
  <action>
    Create 06.2-HUMAN-UAT.md in the 06.1 format: the gate results from Task 1, the ten review items from Task 2 with the owner's response to each (writing "owner did not state" wherever they said nothing), the owner's reply quoted verbatim, an explicit line recording their D-07 answer, and a `## Gaps` section listing any new requests as follow-on requests for a future phase rather than defects in this one. Do not paraphrase an approval the owner did not give and do not invent observations.
  </action>
  <verify>
    <automated>cd /home/rflor/games && test -f .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md && grep -q "## Gaps" .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - the file exists with the gate results, the per-item responses, the verbatim reply and a `## Gaps` section
    - the D-07 re-confirmation has an explicit recorded answer or an explicit "owner did not state"
    - no observation appears that the owner did not make
  </acceptance_criteria>
  <done>The phase's sign-off is on record in the project's own format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification and record-keeping only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-24 | Repudiation | fabricated sign-off | mitigate | Task 3 requires the owner's verbatim reply and "owner did not state" for anything unanswered (the 04-08/06.1-15 precedent) |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npm test`, `npx playwright test` and `npx tsc -b apps/web packages/rules packages/schema apps/worker` all exit 0 before the checkpoint
- 06.2-HUMAN-UAT.md exists with a verbatim owner reply
</verification>

<success_criteria>
The phase gate is green, the owner has seen and ruled on the board and on every design decision left to Claude's discretion, and their words are on record.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-11-SUMMARY.md` when done
</output>
