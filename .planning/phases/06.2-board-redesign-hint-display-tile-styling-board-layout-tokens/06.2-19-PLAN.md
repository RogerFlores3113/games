---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 19
type: execute
wave: 8
depends_on: ["06.2-18"]
gap_closure: true
files_modified:
  - e2e/start-game.spec.ts
  - apps/web/lib/layout-budget.ts
  - apps/web/lib/layout-budget.test.ts
  - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
autonomous: true
requirements: [UI-11, BOARD-01, BOARD-02, BOARD-03, BOARD-05, TILE-02, TILE-03]

must_haves:
  truths:
    - "Five seats with a six-suit variant and a deep discard pile fit 1280x720 with no scrollbar"
    - "The board's rendered box is the same early in a game and late in a game, measured in a real browser"
    - "The layout ledger's numbers match what the browser actually renders"
    - "The UI-SPEC describes the board that now exists, not the one the owner rejected"
  artifacts:
    - path: "e2e/start-game.spec.ts"
      provides: "worst-case fit check plus a fixed-geometry measurement that compares the board box across game progress"
    - path: ".planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md"
      provides: "the design contract updated for the sourced wood texture, the column grid, token sizing, the settings modal and fixed geometry"
  key_links:
    - from: "e2e/start-game.spec.ts"
      to: "apps/web/lib/layout-budget.ts"
      via: "measured band heights reconciled against the ledger constants"
      pattern: "layout-budget"
---

<objective>
Prove in a real browser that the reworked board holds the 1280x720 no-scroll fit and that its geometry is genuinely fixed, reconcile the ledger with the measurement, and bring the UI-SPEC back in line with what was built.

Purpose: gaps 3, 5 and 8 all moved height around, and the fit currently passes at exactly 720px with zero slack after a regression that cost this phase a day. The fit is a first-class, separately verified task here — never a side effect of the last visual change. The UI-SPEC still documents a CSS-generated texture, a horizontal fan, derived token pitch and bottom-row settings, all of which the owner overturned.
Output: a measured, green worst-case fit, a ledger that matches the render, and a UI-SPEC that matches the board.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/deferred-items.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md

<interfaces>
The worst case, from this phase's own history: 5 seats, the Black variant (6 cluable colours — the largest clue-picker row, and the case that broke the fit once already), a deep discard pile (~30 tiles) and advanced played stacks. The last measurement before the rework: `teammates` 117px, `tableau` 282px, bottom controls row 309px, page total exactly 720px with zero slack.

The rules that produced the last regression, both still binding: (1) the ledger holds measured numbers, not guessed ones — if the render disagrees with the ledger, the ledger is corrected to the render, never the reverse, and the correction is explained in the constant's doc comment; (2) a component's height change and its ledger entry land in the same commit.

`e2e/start-game.spec.ts` already contains: an early no-vertical-scrollbar check after game start, a 5-player fit test (~line 390) with `toBeInViewport()` assertions, and a worst-case UI-11 test (~line 690) that plays the game forward, logs the reached discard count and max stack rank, and reconciles measured band heights against the ledger. Extend these; do not write a parallel suite.

Existing helpers: `startGameWithPlayers(hostPage, browser, names)` and `createRoom(page, { name, variant })` in `e2e/helpers.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the reworked board at 1280x720 and reconcile the ledger</name>
  <files>e2e/start-game.spec.ts, apps/web/lib/layout-budget.ts, apps/web/lib/layout-budget.test.ts</files>
  <read_first>
    - e2e/start-game.spec.ts (the 5-player fit test ~lines 385-425 and the worst-case UI-11 test ~lines 640-730)
    - apps/web/lib/layout-budget.ts (whole file, as rewritten by 06.2-14/15/16)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/deferred-items.md (the measurement history and the ledger-correction rule)
  </read_first>
  <action>
    Run the worst case in a real browser and record the numbers before changing anything: start a 5-seat Black-variant game, play forward to a deep discard pile, and measure `teammates-band`, `tableau`, `bottom-controls-row` and `document.documentElement.scrollHeight` at 1280x720. Put the measured table in the SUMMARY.

    Then reconcile: for every band whose measured height differs from its ledger constant, correct the constant to the measurement plus a stated margin and explain the correction in its doc comment, in the same style the existing `OWN_BAND_PX` comment uses. The settings-modal move (06.2-13) removed three controls from the bottom row, so `OWN_BAND_PX` is expected to fall — do not leave a stale high number that hides future regressions. Keep the four-band sum assertion in `layout-budget.test.ts` passing against the corrected values.

    If the page does not fit, fix it within the geometry this phase owns — shrinking a reserved region's constant and its component together, in one commit — and re-measure. Do not close the gap by deleting a feature, by making a region grow-on-demand again, or by reverting a gap fix. If the only way to fit is to change a file outside `apps/web/lib/layout-budget.ts`, `apps/web/components/hanabi/Table.tsx`, `PlayedStack.tsx` or `TokenColumn.tsx`, stop and report with the measurement table.

    Extend the worst-case UI-11 test to also assert the newly-added elements are in the viewport: `settings-toggle`, `discard-group-by-suit`, and the rank-5 slot of at least one played column (`played-slot-{suit}-5` or `played-stack-{suit}-card-5`).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/start-game.spec.ts && npx vitest run --project web layout-budget</automated>
  </verify>
  <acceptance_criteria>
    - the SUMMARY contains a measured before/after table with `teammates-band`, `tableau`, `bottom-controls-row` and page total at 1280x720, 5 seats, Black variant
    - every ledger constant this plan changes has a doc comment naming the measurement it came from
    - `npx playwright test e2e/start-game.spec.ts` passes, including the worst-case UI-11 test
    - `layout-budget.test.ts`'s four-band sum assertion passes against the corrected constants
  </acceptance_criteria>
  <done>The worst case fits 1280x720 and the ledger's numbers are the browser's numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the board's geometry is fixed, in a browser</name>
  <files>e2e/start-game.spec.ts</files>
  <read_first>
    - e2e/start-game.spec.ts (the worst-case UI-11 test and its play-forward loop)
    - apps/web/components/hanabi/Table.tsx (the three reserved regions)
  </read_first>
  <action>
    Add a test that makes gap 1 mechanically checkable rather than a matter of judgement: in a 5-seat Black-variant game, record the bounding boxes of `tableau`, `play-zone`, `discard-pile` and `clue-tokens` immediately after the game starts; play forward until at least eight tiles are discarded, at least one stack has advanced and at least one clue token has been spent; then record the same four boxes again and assert each one's `width` and `height` are unchanged (allow a 1px tolerance for sub-pixel rounding only). Assert the page still has no vertical scrollbar at the end.

    This is the owner's core complaint turned into an assertion — "things should be taking up space that's already portioned out" — so name the test for UAT gap 1 and reference it in the test's own comment.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/start-game.spec.ts -g "fixed"</automated>
  </verify>
  <acceptance_criteria>
    - a test compares `tableau`, `play-zone`, `discard-pile` and `clue-tokens` bounding boxes before and after real game progress and asserts width and height are unchanged within 1px
    - the same test asserts at least eight discards and one spent clue token were actually reached before the second measurement
    - `npx playwright test e2e/start-game.spec.ts` passes
  </acceptance_criteria>
  <done>The board provably does not grow or reflow as tiles accumulate.</done>
</task>

<task type="auto">
  <name>Task 3: Update the UI-SPEC to the board that now exists</name>
  <files>.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (whole file — every section this rework contradicts)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md (the owner's verbatim reply and the ten gaps)
    - apps/web/lib/layout-budget.ts (the reconciled constants to quote)
  </read_first>
  <action>
    Revise the UI-SPEC so the contract matches the built board rather than being silently contradicted. Add a dated "Owner review 2026-09-17 — revisions" note near the top recording that the owner rejected the first implementation and which decisions that overturned, then update each affected section in place:

    - "Wooden board texture (D-11, D-15)": replaced by a sourced, licensed wood image recorded in `apps/web/public/backgrounds/CREDITS.md`; the CSS-gradient recipe is removed, taken via D-11's own explicit image fallback. Note the background image is now the city-on-the-water-with-fireworks asset.
    - "Layout — Height Budget": replace the left-column/right-column stacked model with the three fixed regions (Play, deck-and-discard, tokens) and quote the reconciled constants from Task 1. State the new binding rule: every region reserves height and width up front and content fills it.
    - "Fanned played stacks (BOARD-05, D-22)": replaced by one column per suit with five reserved rank slots filling top to bottom; quote the slot dimensions.
    - Clue/fuse token section: 40px discs (2x), the clue disc's blue outline matching the fuse rim, a fixed 8-slot and 3-slot reservation, and the count text moved to `sr-only` — update the Copywriting Contract rows for "{n} clues left"/"{n} fuses left" to say screen-reader-only.
    - "Tile surface vs. board surface" and the tile-colour palette: the preset is a translucent overlay painted on top of the card art, not an opaque fill; quote the `color-mix(..., transparent)` form.
    - Component Notes: add `SettingsModal.tsx`, note that `AudioControls`, the keep-hints toggle and `TileColorPicker` now render inside it and that the bottom row holds only `OwnHand`, `CardActions` and `CluePicker`; add the gear trigger and its top-right placement.
    - Shared Discard Order: add the group-by-suit action as a shared re-sort through `reorderDiscard`, explicitly not a local view toggle.
    - Leave the Checker Sign-Off block intact and append a line noting these revisions post-date that sign-off and are owner-driven.

    Do not rewrite sections the owner did not overturn — the hint composition, note box, drag shift-aside and D-07 ruled-out decision all stand.
  </action>
  <verify>
    <automated>cd /home/rflor/games && grep -c "wood-board.webp\|city-fireworks.webp\|SettingsModal\|column per suit\|sr-only" .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - the UI-SPEC contains an "Owner review" revisions note naming the overturned decisions, including D-11 and D-22
    - `grep -c 'repeating-linear-gradient' 06.2-UI-SPEC.md` returns 0
    - `grep -c 'fan' 06.2-UI-SPEC.md` returns 0 outside the revisions note that records the fan's removal
    - the spec names `wood-board.webp`, `city-fireworks.webp`, `SettingsModal.tsx`, the 40px token disc, the five reserved rank slots and the translucent tile overlay
    - the Copywriting Contract's token-count rows say the text is screen-reader-only
  </acceptance_criteria>
  <done>The design contract describes the board that exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan measures and documents; it introduces no new runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-45 | Repudiation | layout ledger | mitigate | every constant changed here carries a doc comment naming the live measurement it came from, so a future regression can be traced to a real number rather than a guess |
| T-06.2-39 | Denial of Service | fixed board regions | mitigate | the browser fixed-geometry test asserts the board's boxes do not grow with game progress, which is what keeps a long game from scrolling the page |
</threat_model>

<verification>
- `npx playwright test e2e/start-game.spec.ts` passes, including the worst case and the fixed-geometry test
- `npx vitest run --project web layout-budget` passes
- the UI-SPEC no longer describes the CSS texture, the fan or bottom-row settings
</verification>

<success_criteria>
The reworked board fits 1280x720 in the worst case the build supports, its geometry is proven fixed by measurement rather than assertion, the ledger matches the render, and the design contract matches the board.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-19-SUMMARY.md` when done
</output>
