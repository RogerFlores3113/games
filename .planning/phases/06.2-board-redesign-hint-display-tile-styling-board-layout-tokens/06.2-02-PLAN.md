---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/globals.css
  - apps/web/lib/theme-tokens.test.ts
  - apps/web/lib/layout-budget.ts
  - apps/web/lib/layout-budget.test.ts
autonomous: true
requirements: [TILE-01, TILE-02, TILE-03, DRAG-01]

must_haves:
  truths:
    - "Every new colour this phase needs exists as a named, documented @theme token — no new hex literal lands outside the @theme block"
    - "--color-accent still has exactly its seven reserved uses; this phase adds none"
    - "A wooden board surface is available as a CSS-only class built from existing tokens, with the 06.1 city-at-night background still behind it"
    - "The drag shift-aside motion and any new animation have a paired prefers-reduced-motion static end state"
    - "The 1280x720 height ledger exists as one named module that later plans import instead of re-inventing scattered pixel constants"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "7 new @theme tokens, .board-surface wooden texture, .tile-shift transition"
      contains: "--color-token-disc"
    - path: "apps/web/lib/layout-budget.ts"
      provides: "named band/element heights and the 1280x720 ledger"
      exports: ["TEAMMATE_BAND_PX", "OWN_BAND_PX", "TABLE_BAND_MIN_PX", "VIEWPORT_TEST_HEIGHT_PX"]
    - path: "apps/web/lib/theme-tokens.test.ts"
      provides: "source scan proving the new tokens exist, are documented, and no hex sits outside @theme"
  key_links:
    - from: "apps/web/lib/layout-budget.ts"
      to: "the UI-11 1280x720 no-scroll constraint"
      via: "sum assertion in layout-budget.test.ts"
      pattern: "VIEWPORT_TEST_HEIGHT_PX"
---

<objective>
Lay the shared foundations every visual task in this phase depends on: the seven new `@theme` colour tokens, the wooden board surface and shift-aside motion classes, and a single named height ledger for the 1280x720 fit.

Purpose: UI-SPEC Color section (the location-change fix the UI checker required), TILE-01/TILE-02 surfaces, DRAG-01's motion, and RESEARCH.md Pitfall 1 — the ledger must exist as a first-class artifact, not be discovered when an e2e assertion fails.
Output: extended `globals.css`, `layout-budget.ts`, and source-scan tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md

<interfaces>
Existing tokens (globals.css, do not redefine): --color-bg #0B0F1A, --color-surface #141A2E, --color-accent #F5B942, --color-destructive #F0453D, --color-text, --color-text-muted, --color-border #232B45, --color-suit-{red,yellow,green,blue,white,rainbow,black}, --color-card-glow #FFD98A, --size-touch-min 44px, --space-{xs,sm,md,lg,xl,2xl,3xl}, --text-{body,label,heading,display}.
Existing animation conventions: `.drag-snap` (transition timing to reuse for shift-aside), `.anim-stack-flash`, `.fly-to-ghost`, each with a paired `@media (prefers-reduced-motion: reduce)` block that swaps in the static end state.
Existing exception precedent: `.table-backdrop`'s scrim is an existing token's value at partial opacity, documented in the header — the only prior rgba-outside-@theme case.
UI-SPEC height ledger (source of the numbers below): teammates band 140px, own band 180px, centre band ~360px available of which the left column is ~260px (Play ~100, deck counter ~48, Discard ~112) and the right token column stretches to the same height; played stacks fan horizontally with 16px peek per additional card; note row 20px; teammate card 78px; own card 100px; controls row 44px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seven documented @theme tokens</name>
  <files>apps/web/app/globals.css, apps/web/lib/theme-tokens.test.ts</files>
  <read_first>
    - apps/web/app/globals.css (header comment block and the whole @theme block)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Color section, new-token table)
    - apps/web/lib/suit-visuals.test.ts (the existing token/contrast source-scan style)
  </read_first>
  <action>
    Add exactly these tokens to the `@theme` block, grouped under one new comment paragraph written in the same voice as the existing `--color-accent` and suit-hue paragraphs: `--color-tile-preset-warm-sand: #3A3220`, `--color-tile-preset-cool-teal: #1A3236`, `--color-tile-preset-plum: #2A1F35`, `--color-tile-preset-charcoal: #1C1F26`, `--color-token-disc: #000000`, `--color-token-fuse-rim: #E8722C`, `--color-tile-shadow: rgba(0, 0, 0, 0.4)`. The comment must state, per the UI-SPEC: the four preset tokens are decorative personal-preference tile fills (TILE-03) and never a suit/accent/destructive signal; `--color-token-disc` is token art only and stays pure black independent of any future `--color-bg` change; `--color-token-fuse-rim` is the owner's literal orange-red rim, distinct from `--color-suit-yellow` and `--color-destructive`, token art only; `--color-tile-shadow` is an opacity-only black following the `.table-backdrop` scrim's "derive from a fixed value, document why" discipline. Extend the file header to record that phase 06.2 adds these seven and adds ZERO new `--color-accent` uses (still exactly seven).
    Create apps/web/lib/theme-tokens.test.ts: read globals.css with readFileSync, assert each of the seven token names is declared inside the `@theme` block, assert each has an adjacent documenting comment paragraph mentioning it or its group, assert no hex literal appears in the file outside the `@theme` block (extract the @theme block by brace matching first, then scan the remainder for `#[0-9A-Fa-f]{3,8}`), and assert the count of `--color-accent` references in the file's non-comment CSS is unchanged from its current value.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web theme-tokens</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--color-tile-preset-' apps/web/app/globals.css` is 4
    - `grep -n -- '--color-token-disc\|--color-token-fuse-rim\|--color-tile-shadow' apps/web/app/globals.css` matches all three
    - theme-tokens.test.ts asserts zero hex literals outside the @theme block and passes
    - `npx vitest run --project web theme-tokens` passes
  </acceptance_criteria>
  <done>Every colour later plans need is a named, documented token, and a test fails the build if a hex literal escapes the @theme block.</done>
</task>

<task type="auto">
  <name>Task 2: Wooden board surface and shift-aside motion classes</name>
  <files>apps/web/app/globals.css, apps/web/lib/theme-tokens.test.ts</files>
  <read_first>
    - apps/web/app/globals.css (the `.table-backdrop`, `.drag-snap`, `.anim-stack-flash` and prefers-reduced-motion blocks)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Wooden board texture, Drag shift-aside)
  </read_first>
  <action>
    Per D-11/D-15 and D-08. Add a `.board-surface` class implementing the UI-SPEC's CSS-only wooden treatment: a base `linear-gradient` over `color-mix(in srgb, var(--color-surface) 85%, #000 15%)` plus two `repeating-linear-gradient` grain layers at 4-8% opacity, layered with `background-image`/`background-blend-mode`, plus the `border`/`border-radius` that makes the panel read as a board. No hex literal outside `@theme` — the only permitted raw value is the `#000`/`rgba(0,0,0,…)` mix already sanctioned by the header's documented discipline, and it must carry a comment saying so. The class applies to the tableau panel only, never to `body`/`main`, so 06.1's city-at-night `.table-backdrop` stays visible around the board's edges.
    Add a `.tile-shift` class carrying only `transition: transform 150ms ease-out` reusing `.drag-snap`'s timing/easing, for the DRAG-01 shift-aside, and a paired `@media (prefers-reduced-motion: reduce)` block that sets `transition: none` so the shifted end state (the visible gap) still renders with no animation.
    Extend theme-tokens.test.ts with assertions: `.board-surface` exists and references `--color-surface`; `.tile-shift` exists; every animation/transition class name introduced in this file appears inside at least one `@media (prefers-reduced-motion: reduce)` block (compute the class-name set from the file rather than hardcoding a list).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web theme-tokens && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\.board-surface' apps/web/app/globals.css` matches and the rule references `var(--color-surface)`
    - `grep -n '\.tile-shift' apps/web/app/globals.css` matches, and `.tile-shift` also appears within a prefers-reduced-motion block
    - `grep -n 'table-backdrop' apps/web/app/globals.css` still matches (06.1 background untouched)
    - theme-tokens.test.ts's reduced-motion pairing assertion passes for every animated class
  </acceptance_criteria>
  <done>The board can be given a wooden surface and tiles can shift aside, both from CSS built on existing tokens, with reduced-motion handled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The 1280x720 height ledger as a named module</name>
  <files>apps/web/lib/layout-budget.ts, apps/web/lib/layout-budget.test.ts</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Layout — Height Budget)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md (Pitfall 1)
    - apps/web/components/hanabi/OwnHandCard.tsx (CARD_WIDTH/CARD_HEIGHT constants and their deviation comments)
    - apps/web/components/hanabi/TeammateCard.tsx (same)
  </read_first>
  <behavior>
    - TEAMMATE_BAND_PX + OWN_BAND_PX + TABLE_BAND_MIN_PX + BOARD_CHROME_PX is less than or equal to VIEWPORT_TEST_HEIGHT_PX (720)
    - TEAMMATE_BAND_PX accounts for seat label + teammate card height + status row + gaps, with no marks band
    - OWN_BAND_PX accounts for turn indicator + own card height + controls row + note row + gaps, with no marks band
    - PLAY_AREA_PX + DECK_COUNTER_PX + DISCARD_AREA_PX equals LEFT_COLUMN_PX, and the token column's per-token pitch derived from LEFT_COLUMN_PX and MAX_TOKEN_COUNT is at least 16px
    - fannedStackWidth(cardCount) returns PLAYED_CARD_WIDTH_PX for one card and adds FAN_PEEK_PX per additional card
  </behavior>
  <action>
    Create apps/web/lib/layout-budget.ts as the single source of truth RESEARCH.md Pitfall 1 requires. Export named numeric constants with a doc comment per constant naming the UI-SPEC row it comes from: VIEWPORT_TEST_HEIGHT_PX (720), VIEWPORT_TEST_WIDTH_PX (1280), BOARD_CHROME_PX (page padding and inter-band gaps), TEAMMATE_BAND_PX (140), OWN_BAND_PX (180), TABLE_BAND_MIN_PX (260), TEAMMATE_CARD_HEIGHT_PX (78), OWN_CARD_HEIGHT_PX (100), NOTE_ROW_PX (20), CONTROLS_ROW_PX (44), SEAT_LABEL_PX (20), TURN_INDICATOR_PX (24), PLAY_AREA_PX (100), DECK_COUNTER_PX (48), DISCARD_AREA_PX (112), LEFT_COLUMN_PX (260), MAX_TOKEN_COUNT (11 = 8 clue + 3 fuse), TOKEN_GAP_PX, PLAYED_CARD_WIDTH_PX (48), PLAYED_CARD_HEIGHT_PX (64), FAN_PEEK_PX (16). Export `tokenPitchPx(columnHeightPx, tokenCount)` = (columnHeightPx - (tokenCount - 1) * TOKEN_GAP_PX) / tokenCount and `fannedStackWidth(cardCount)`. Add a file header stating the ledger rule: later plans import these constants instead of writing pixel literals, and the token column is sized FROM the left column's height, never the other way round.
    Create layout-budget.test.ts asserting every behaviour bullet.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web layout-budget && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/layout-budget.ts` exports all constants and both helper functions listed above
    - the sum assertion in layout-budget.test.ts is present and passing
    - `npx vitest run --project web layout-budget` passes
  </acceptance_criteria>
  <done>The height ledger is written down, imported-from, and machine-checked before any layout code is written against it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan changes CSS tokens and a constants module only; no input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-05 | Tampering | globals.css design-system drift | mitigate | theme-tokens.test.ts fails the build on a hex literal outside @theme or a new `--color-accent` use |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; a wood-texture asset fallback (D-11) would require CREDITS.md plus the full Package Legitimacy Gate before adoption |
</threat_model>

<verification>
- `npx vitest run --project web theme-tokens layout-budget` green
- `npx tsc -b apps/web` exits 0
- `npm test` still green (no existing test regressions from globals.css changes)
</verification>

<success_criteria>
The seven new tokens, the wooden surface class, the shift-aside transition and the height ledger all exist, are documented, and are guarded by tests that fail on drift.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-02-SUMMARY.md` when done
</output>
