---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 10
type: execute
wave: 6
depends_on: ["06.2-07", "06.2-08", "06.2-09"]
files_modified:
  - e2e/hanabi-table-polish.spec.ts
  - e2e/start-game.spec.ts
  - apps/web/lib/layout-budget.ts
  - apps/web/lib/layout-budget.test.ts
autonomous: true
requirements: [HINT-01, HINT-02, HINT-03, HINT-04, NOTE-03, DRAG-01, BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-05, DISC-01, TILE-03]

must_haves:
  truths:
    - "A colour clue tints the touched tile and a number clue stamps its numeral, proven in a live browser"
    - "Keep-hints ON survives the next player's move and a page refresh; OFF clears after the next move"
    - "A note typed in the always-visible box persists across a refresh and is absent from the other player's page"
    - "A discard reorder by one player is visible on the other player's page and survives a refresh"
    - "The board fits 1280x720 with no scrollbar in the worst-case game the build supports"
  artifacts:
    - path: "e2e/hanabi-table-polish.spec.ts"
      provides: "live-browser coverage for hints, note box, drag shift-aside and shared discard order"
    - path: "e2e/start-game.spec.ts"
      provides: "board-layout assertions and the extended UI-11 no-scroll check"
  key_links:
    - from: "e2e/start-game.spec.ts UI-11 test"
      to: "apps/web/lib/layout-budget.ts"
      via: "measured band heights reconciled against the ledger constants"
      pattern: "layout-budget"
---

<objective>
Prove the phase's behaviour in a real browser, and close out RESEARCH.md Pitfall 1 by measuring the 1280x720 fit against the written height ledger instead of assuming it.

Purpose: automated verification for HINT-01..04, NOTE-03, DRAG-01, BOARD-01..05, DISC-01 and TILE-03, plus the UI-11 regression the whole phase threatens.
Output: extended e2e specs and a ledger reconciled with measured reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-VALIDATION.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md

<interfaces>
Existing specs: `e2e/hanabi-table-polish.spec.ts` already covers the 06.1 note flow (~lines 243-260) and a discard drag (~lines 143-152) — extend these rather than writing a parallel suite. `e2e/start-game.spec.ts` holds the UI-11 1280x720 `toBeInViewport()` assertions (~lines 390-397) and the identity-boundary assertions.
Harness facts carried from earlier phases: the web dev server is pinned to port 3100 (playwright.config.ts, overridable via E2E_WEB_PORT/E2E_WORKER_PORT); a gesture unlock for audio must use `page.mouse.click(x, y)` at a fixed coordinate, never `locator('body').click()`, on the `min-h-screen overflow-y-auto` table backdrop (06.1-14).
Testids available after 06.2-04..09: `own-hand-slot-{n}` with `data-hints`, `hint-numeral`, `other-hand-card-{id}`, `discard-tile-{id}` with `data-dragging`, `clue-token`/`fuse-token` with `clue-tokens`/`fuse-tokens` counts, `deck-count`, `play-zone`, `discard-pile`, `tableau`, the keep-hints toggle's two aria-labels, and `aria-label="Choose tile colour"` plus the five swatch labels.
Server-side guarantee under test for DISC-01: `discardOrder` is shared state, so a reorder on page A must appear on page B without a refresh (the RT-01 propagation pattern already used in this suite).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hints, the note box and the tile-colour picker in a live browser</name>
  <files>e2e/hanabi-table-polish.spec.ts</files>
  <read_first>
    - e2e/hanabi-table-polish.spec.ts (whole file — the existing two-page room setup helper and the note-flow test)
    - e2e/start-game.spec.ts (clue-giving helper flow)
    - apps/web/components/hanabi/HintIndicator.tsx, apps/web/components/hanabi/NoteBox.tsx, apps/web/components/hanabi/TileColorPicker.tsx
  </read_first>
  <action>
    Add scenarios to the existing spec, reusing its room-setup helpers:
    (1) HINT-01/HINT-02: give a colour clue from page A and assert the touched tiles on page B carry `data-hints="true"` with the tint marker present; give a number clue and assert a `hint-numeral` appears on the touched tiles with the clued numeral as its text.
    (2) HINT-03: with keep-hints OFF (default), after the next player acts the touched tile's `data-hints` returns to "false"; turn keep-hints ON via its aria-labelled control, repeat, and assert the hints remain; reload the page and assert the toggle state persisted.
    (3) HINT-04: assert no pip-row testid exists anywhere on the board.
    (4) NOTE-03: type into an own-hand note box without pressing Enter or blurring, wait for the debounce, reload, and assert the text is still there; assert the same card on the other player's page shows no note text.
    (5) TILE-03: open the tile-colour picker, choose a non-default preset, assert the own-hand tiles' background changed, reload and assert the choice persisted, and assert the other page's tiles are unchanged.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/hanabi-table-polish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - the spec contains a test per scenario (1)-(5) above
    - `grep -n 'hint-numeral\|data-hints' e2e/hanabi-table-polish.spec.ts` matches
    - `npx playwright test e2e/hanabi-table-polish.spec.ts` passes
  </acceptance_criteria>
  <done>Hint display, hint persistence, private notes and the tile-colour preference are proven end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Board layout, token removal, fanned stacks and the shared discard order</name>
  <files>e2e/start-game.spec.ts, e2e/hanabi-table-polish.spec.ts</files>
  <read_first>
    - e2e/start-game.spec.ts (board assertions and the UI-11 test)
    - e2e/hanabi-table-polish.spec.ts (the existing discard-drag test around lines 143-152)
    - apps/web/components/hanabi/Table.tsx, apps/web/components/hanabi/TokenColumn.tsx, apps/web/components/hanabi/PlayedStack.tsx
  </read_first>
  <action>
    In start-game.spec.ts add board-layout assertions: the Play area and Discard area each render their label and outline and sit in that vertical order; the deck counter sits between them in document order and shows the remaining count beside the tile back; the token column sits to the right of the left column (compare bounding boxes); the clue-token element count equals the remaining clue count and drops by one after a clue is given, with the "{n} clues left" text updating; after a misplay the fuse-token count drops by one; a stack played to rank 3 renders three card faces (BOARD-05).
    In hanabi-table-polish.spec.ts add the DISC-01 scenario: with two pages in one room and at least three discarded tiles, drag a tile in page A's discard area to a new position, assert page A's `discard-tile-*` order changed, assert page B shows the identical order without a manual refresh, then reload page B and assert the order survived. Add a DRAG-01 assertion inside the existing hand-drag test: mid-drag, a non-dragged slot carries a non-zero transform (the gap), which returns to zero after the drop.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/start-game.spec.ts e2e/hanabi-table-polish.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'clues left\|fuses left' e2e/start-game.spec.ts` matches
    - `grep -n 'discard-tile-' e2e/hanabi-table-polish.spec.ts` matches in the two-page ordering test
    - the spec asserts page B sees the new order with no reload, and again after a reload
    - both spec files pass
  </acceptance_criteria>
  <done>The new board layout and the shared discard arrangement are proven in a real two-player session.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the 1280x720 worst case and reconcile the height ledger</name>
  <files>e2e/start-game.spec.ts, apps/web/lib/layout-budget.ts, apps/web/lib/layout-budget.test.ts</files>
  <read_first>
    - e2e/start-game.spec.ts (the existing UI-11 1280x720 test)
    - apps/web/lib/layout-budget.ts (06.2-02)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md (Pitfall 1)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Layout — Height Budget, Mandatory verification task)
  </read_first>
  <action>
    Extend the UI-11 test to the worst case this build supports: five seats, the largest suit count the create-room flow currently offers (prefer Rainbow or Black; if the variant is not selectable end to end yet, use base with the discard pile driven to at least eight tiles and at least one stack fanned to rank 3, and record in the SUMMARY which case was exercised and why). At 1280x720 assert: `document.scrollingElement.scrollHeight` is not greater than its `clientHeight` (zero vertical scrollbar), and `toBeInViewport()` holds for every existing testid the test already checks plus `tableau`, the token column, `deck-count`, the Play and Discard areas, every teammate hand and every own-hand slot.
    In the same test, measure the actual rendered heights of the teammates band, the tableau band and the own band via bounding boxes and assert each is within a small tolerance of the corresponding `layout-budget.ts` constant. If a measured value falls outside tolerance, update the ledger constant to the measured value and adjust the component that owns that height so the sum assertion in layout-budget.test.ts still holds — the ledger and the rendered board must agree when this task is done. Record every ledger change in the SUMMARY as a deviation.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/start-game.spec.ts && npx vitest run --project web layout-budget</automated>
  </verify>
  <acceptance_criteria>
    - the UI-11 test asserts zero vertical scroll at 1280x720 for a five-seat game
    - the test compares at least the three band heights against layout-budget constants
    - `npx vitest run --project web layout-budget` still passes, including the sum-under-720 assertion
    - the SUMMARY records the worst case exercised and any ledger constant changed
  </acceptance_criteria>
  <done>The fit is measured, not assumed, and the written ledger matches the board that actually renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two browser sessions → one room | Cross-page assertions confirm shared state is shared and private state is not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-22 | Information Disclosure | private notes leaking to another seat | mitigate | Task 1 scenario (4) asserts the other page shows no note text |
| T-06.2-23 | Information Disclosure | own-hand identity via new hint markup | mitigate | The pre-existing identity-boundary e2e assertions are retained unchanged and re-run here |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npm test` fully green
- `npx playwright test` fully green
- `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
</verification>

<success_criteria>
Every behavioural requirement in the phase has a passing automated browser proof, and the 1280x720 fit is measured against a written ledger that matches the rendered board.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-10-SUMMARY.md` when done
</output>
