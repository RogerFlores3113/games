---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 20
type: execute
wave: 9
depends_on: ["06.2-19"]
gap_closure: true
files_modified:
  - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
autonomous: false
requirements: [BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-05, TILE-01, TILE-02, TILE-03, DISC-01, HINT-03, UI-11]

must_haves:
  truths:
    - "The full gate is green before the owner is asked to look at anything"
    - "The owner has seen the reworked board and answered each of the ten gaps"
    - "The owner's reply is recorded verbatim, not summarised or interpreted"
  artifacts:
    - path: ".planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md"
      provides: "a second review round with the owner's verbatim reply and a per-gap verdict"
  key_links:
    - from: "the gate run"
      to: "the owner checkpoint"
      via: "the checkpoint is only reached after all three gate commands exit 0"
      pattern: "npm test"
---

<objective>
Run the full phase gate, then put the reworked board in front of the owner and record their answer to each of the ten gaps verbatim.

Purpose: the last review ended in "no, bad job" with a fully green automated gate — every gap was a visual or interaction judgement no test could catch. The gate proves nothing regressed; only the owner can close these gaps.
Output: a green gate and a second review round appended to `06.2-HUMAN-UAT.md` with the owner's exact words.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md

<interfaces>
Gate commands and the counts they hit before this gap-closure round: `npm test` (71 files, 846 tests), `npx playwright test` (40 tests), `npx tsc -b apps/web packages/rules packages/schema apps/worker` (exit 0). Expect higher counts now; a lower count means tests were deleted rather than updated and must be explained.

Dev server invocation that matches `playwright.config.ts`: `npm run dev --workspace apps/web -- -p 3100` and, under `apps/worker`, `npx wrangler dev --port 8787`. Free the ports first with `fuser -k 3100/tcp 8787/tcp`. Web at http://localhost:3100; the worker is dialled automatically.

Known pre-existing flakes that are NOT this round's regressions (recorded in `deferred-items.md`): `e2e/hanabi-realtime.spec.ts`'s RT-04 frozen-tab and network-drop tests, and the "UI-02 + UI-04" `data-luminosity` preview/actual mismatch. If one of these fails, re-run it in isolation and record the outcome; do not chase it and do not let it block the checkpoint.

Decisions that are still in force and must not be presented as open: no ruled-out clue information anywhere (D-07, the owner already said "Let it go"), no rank numerals on card faces, hints on the tile with the keep-hints toggle, and the 06.1 art decisions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full phase gate</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/deferred-items.md (the known flakes not to chase)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md (the previous round's gate table, for the counts to compare against)
  </read_first>
  <action>
    From the repo root, free the dev ports, then run the gate in order and capture each result for the SUMMARY: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`. Record the test counts and compare them against the previous round's 846 unit / 40 e2e — a drop in either needs an explanation in the SUMMARY.

    If anything fails, fix it or stop and report before the checkpoint. The owner is never shown a red build. A failure in one of the two known pre-existing flakes is re-run in isolation and recorded, not chased.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npm test && npx playwright test && npx tsc -b apps/web packages/rules packages/schema apps/worker</automated>
  </verify>
  <acceptance_criteria>
    - all three commands exit 0
    - the SUMMARY records the unit and e2e test counts and compares them to 846 / 40
    - any known-flake re-run is recorded with its isolated result
  </acceptance_criteria>
  <done>The build is green across unit, browser and type checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner sign-off on the reworked board</name>
  <files>.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-HUMAN-UAT.md (the ten gaps and the owner's previous verbatim reply)
  </read_first>
  <action>
    Start both dev servers, tell the owner exactly how to reach the board, walk them through the ten checks below one at a time, and pause. Do not proceed, summarise, interpret or argue on their behalf — wait for the resume signal.

    When the reply arrives, append a new `## Review round 2 — <date>` section to `06.2-HUMAN-UAT.md` containing: the gate table from Task 1, the owner's reply quoted verbatim in full, and a per-gap verdict line for all ten gaps (accepted / rejected / not stated) derived only from what they actually said. If a gap is not addressed, write "owner did not state" — never infer approval from silence.
  </action>
  <verify>
    <human-check>The owner has seen the live board and answered all ten review items.</human-check>
  </verify>
  <acceptance_criteria>
    - the owner has been shown the board and asked all ten review items
    - `06.2-HUMAN-UAT.md` contains a "Review round 2" section with the reply quoted verbatim and a verdict line for each of the ten gaps
    - no gap is marked accepted on the strength of silence
  </acceptance_criteria>
  <done>The owner has reviewed the board and their exact words are on record.</done>
  <what-built>
    The ten rejected items, reworked: a board whose Play, deck-and-discard and token regions each reserve their space so nothing grows or reflows as tiles accumulate; a sourced, licensed dark-wood board texture replacing the generated one; a city-on-the-water-with-fireworks background; a play area where each suit is a column and ranks fill downward through five reserved slots; clue tokens with a blue outline matching the fuses; both token types at twice their previous size in a fixed 8-and-3 slot reservation; the "{n} clues left"/"{n} fuses left" text removed from the screen and left only for screen readers; the tile colour as a translucent layer that tints and darkens the card beneath instead of covering it; a gear in the top right opening a centre-justified settings modal with tile colour, volume, mute and hint persistence, leaving the bottom row with only the hand, play/discard and the clue picker; and a group-by-suit action that re-sorts the discard for every player, after which dragging still works.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3100 in two browser windows, create and join a room, seat both, choose the Black variant, and start a game.
    2. Board geometry (gap 1): watch the board while several tiles are played and discarded and a clue token is spent — nothing should move, resize or reflow. Areas should look portioned out from the start.
    3. Board colour and texture (gap 2): does the wood read as a real board-game surface? Is the city-with-fireworks background visible around the board's edges?
    4. Play area (gap 3): each suit is a column, each next rank sits directly below the previous, empty slots hold their place. Is it intuitive?
    5. Clue token outline (gap 4): do the clue tokens read as outlined in blue the way the fuses are outlined?
    6. Token size (gap 5): are the clue and fuse tokens big enough now?
    7. Token text (gap 6): confirm no "X clues left" / "X fuses left" text is on screen anywhere.
    8. Tile colour (gap 7): open the gear menu, change the tile colour through every preset, and confirm each one tints and darkens the card underneath rather than hiding it.
    9. Settings modal (gap 8): the gear is in the top right; the modal is centre-justified and holds tile colour, volume, mute and hint persistence; the bottom row has only the hand, play/discard and the clue picker.
    10. Background image (gap 9): is the city-on-the-water-with-fireworks image the right one?
    11. Discard grouping (gap 10): discard several mixed-suit tiles, click "Group discard by suit" on one window, confirm the other window shows the same new order, then drag a tile and confirm both windows agree.
    12. Anything still wrong, and overall approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is still wrong, gap by gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan runs the gate and records a human review; it ships no runtime change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-46 | Repudiation | owner sign-off record | mitigate | the reply is quoted verbatim into `06.2-HUMAN-UAT.md` with a per-gap verdict, and silence is recorded as "owner did not state" rather than inferred approval |
</threat_model>

<verification>
- `npm test`, `npx playwright test` and `npx tsc -b apps/web packages/rules packages/schema apps/worker` all exit 0 before the checkpoint
- `06.2-HUMAN-UAT.md` contains a "Review round 2" section with a verbatim owner reply and ten verdict lines
</verification>

<success_criteria>
The gate is green, the owner has seen the reworked board and ruled on every one of the ten gaps they raised, and their words are on record.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-20-SUMMARY.md` when done
</output>
