---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 06
type: execute
wave: 3
depends_on: ["06.2-03", "06.2-04"]
files_modified:
  - apps/web/components/hanabi/NoteBox.tsx
  - apps/web/components/hanabi/NoteChip.tsx
  - apps/web/components/hanabi/TileColorPicker.tsx
  - apps/web/components/hanabi/Hand.tsx
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/lib/hanabi-notes.ts
  - apps/web/lib/hanabi-notes.test.ts
  - apps/web/lib/note-box-render.test.ts
  - apps/web/lib/note-chip-render.test.ts
  - apps/web/lib/own-hand-source.test.ts
  - apps/web/lib/own-hand-render.test.ts
autonomous: true
requirements: [NOTE-03, HINT-03, TILE-03]

must_haves:
  truths:
    - "NOTE-03: every own-hand tile has an always-visible faint note box above it that accepts typed text, autosaves as you type, and is never rendered on a teammate's tile"
    - "HINT-03: a keep-hints toggle in the controls row switches hint persistence and the choice survives a refresh"
    - "TILE-03: a tile-colour picker offers the five presets, changes only this player's view, and survives a refresh"
    - "Notes stay browser-local and private — the note components import nothing that can reach the wire"
    - "D-15 holds: the note box and picker take ids only, and own-hand markup stays byte-identical with and without identity"
  artifacts:
    - path: "apps/web/components/hanabi/NoteBox.tsx"
      exports: ["NoteBox"]
      provides: "always-visible autosaving 20px note box"
    - path: "apps/web/components/hanabi/TileColorPicker.tsx"
      exports: ["TileColorPicker"]
      provides: "five-swatch preset picker with 44px targets"
  key_links:
    - from: "apps/web/components/hanabi/NoteBox.tsx"
      to: "apps/web/lib/hanabi-notes.ts"
      via: "readNote on mount, debounced writeNote on change"
      pattern: "writeNote"
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "apps/web/lib/keep-hints-pref.ts"
      via: "mount-effect read, write on toggle, passed down as hintsVisible inputs"
      pattern: "KeepHintsPref"
---

<objective>
Replace the click-to-reveal note chip with an always-visible autosaving note box, and add the two personal controls this phase introduces: the keep-hints toggle and the tile-colour picker.

Purpose: NOTE-03 (D-09/D-10), HINT-03's control surface (D-05), TILE-03's picker (D-13/D-14).
Output: `NoteBox.tsx`, `TileColorPicker.tsx`, the keep-hints toggle wired through `HanabiBoard.tsx`, `NoteChip.tsx` deleted, guard tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md

<interfaces>
From apps/web/lib/hanabi-notes.ts: `NOTE_MAX_LENGTH` (currently 20 — D-10 reconsiders it), `readNote(roomCode, seatId, cardId)`, `writeNote(...)`, `pruneNotesForSeat`, `clearNotesForRoom`. Storage layer is unchanged by this plan apart from the cap.
From 06.2-03: `readKeepHintsPref()`, `writeKeepHintsPref(value)`, `TILE_COLOR_PRESETS`, `readTileColorPref()`, `writeTileColorPref(id)`.
From 06.2-04: `OwnHandProps`/`TeammateHandProps` already carry `hintsVisible` and `tileColor`; Hand renders a fixed `NOTE_ROW_PX` (20px) note row above each own-hand card currently holding `NoteChip`.
NoteChip patterns that are load-bearing and must carry over verbatim (06.2-PATTERNS.md): props are ids only (`roomCode`, `seatId`, `cardId`, `slotNumber`) — never a card object; the stored value is loaded in a mount effect, never in the state initializer, so SSR and first client render both show empty; `pointerdown` stop-propagation so a click inside the control never starts a card drag; the out-of-flow 44px touch target `<span aria-hidden="true" className="absolute" style={{ inset: ... }} />` (hand-rolled consistently, no shared helper — that extraction is explicitly deferred).
HanabiBoard.tsx: controls row is the bottom flex row rendering OwnHand, CardActions, CluePicker, AudioControls (~lines 259-305); `act()` (~line 158) is the action chokepoint; `game.history` is available for the hint lifetime derivation.
Copy contract (UI-SPEC): toggle `aria-label="Keep hints visible"` when off and `aria-label="Clear hints after each move"` when on; picker button `aria-label="Choose tile colour"`; swatch labels "Slate tile colour", "Warm sand tile colour", "Cool teal tile colour", "Plum tile colour", "Charcoal tile colour"; empty focused note placeholder "Note…".
Icons: `lucide-react` `Eye`/`EyeOff` for the toggle — UI chrome only, no new dependency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NoteBox replaces NoteChip</name>
  <files>apps/web/components/hanabi/NoteBox.tsx, apps/web/components/hanabi/NoteChip.tsx, apps/web/lib/hanabi-notes.ts, apps/web/lib/hanabi-notes.test.ts, apps/web/lib/note-box-render.test.ts, apps/web/lib/note-chip-render.test.ts, apps/web/lib/own-hand-source.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/NoteChip.tsx (whole file)
    - apps/web/lib/note-chip-render.test.ts
    - apps/web/lib/hanabi-notes.ts
    - apps/web/lib/own-hand-source.test.ts (NOTE_CHIP_PATH block, ~lines 204-220)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Always-visible note box)
  </read_first>
  <behavior>
    - NoteBox renders an always-present editable control with a faint border in its resting state — there is no button-then-input mode switch
    - Server-rendered markup shows the empty state regardless of stored content (mount-effect load, SSR-safe)
    - Typing calls writeNote once per debounce window rather than on every keystroke, and never requires Enter or blur to persist
    - Input longer than NOTE_MAX_LENGTH is truncated to the cap
    - The control's flow height is exactly NOTE_ROW_PX while its hit area reaches at least --size-touch-min via the out-of-flow inset span
    - pointerdown inside the note box does not propagate to the card's drag handler
    - The props type has no card, suit or rank member
    - NoteBox source imports nothing from room-socket, room-store, nanoid, or an onAction/send path
  </behavior>
  <action>
    Per D-09/D-10. Create NoteBox.tsx with `NoteChipProps`'s exact prop shape (`roomCode`, `seatId`, `cardId`, `slotNumber`), carrying over NoteChip's mount-effect read, its pointerdown stop-propagation and its out-of-flow touch-target span verbatim, and replacing the editing-state visibility gate with resting/hover/focus styling per the UI-SPEC (resting: 1px `--color-border`; hover: border toward `--color-text-muted`, no layout shift; focus: `--color-accent` outline, which is the existing reserved focus-ring use and not an eighth accent use). Autosave with a debounced `writeNote` on change; clear any pending debounce timer on unmount. Raise `NOTE_MAX_LENGTH` in hanabi-notes.ts from 20 to 40 per D-10 and update hanabi-notes.test.ts's cap assertions; do not change any other part of the storage layer.
    Delete NoteChip.tsx and note-chip-render.test.ts. In the same task update own-hand-source.test.ts: remove NOTE_CHIP_PATH and its describes, add NOTE_BOX_PATH with the same three checks (comment-stripped no suit/rank access or destructuring, no FORBIDDEN_TOKENS, props type has no card member).
    Create note-box-render.test.ts covering every behaviour bullet, carrying over note-chip-render.test.ts's source-scan assertion that the file cannot reach the wire.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web note-box-render own-hand-source hanabi-notes && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/web/components/hanabi/NoteChip.tsx apps/web/lib/note-chip-render.test.ts` reports both missing and `grep -rn 'NoteChip' apps/web --include=*.ts --include=*.tsx` returns nothing
    - `grep -n 'NOTE_BOX_PATH' apps/web/lib/own-hand-source.test.ts` matches; `grep -n 'NOTE_CHIP_PATH' apps/web/lib/own-hand-source.test.ts` returns nothing
    - `grep -n 'NOTE_MAX_LENGTH = 40' apps/web/lib/hanabi-notes.ts` matches
    - note-box-render.test.ts asserts the debounced write and the SSR-empty first render
  </acceptance_criteria>
  <done>Each own tile has a live, always-present, autosaving private note box and the old chip is gone from the tree and the guards.</done>
</task>

<task type="auto">
  <name>Task 2: Note box in every own-hand slot</name>
  <files>apps/web/components/hanabi/Hand.tsx, apps/web/lib/own-hand-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Hand.tsx (post-06.2-04 own slot structure and the note row)
    - apps/web/components/hanabi/NoteBox.tsx (Task 1)
    - apps/web/lib/own-hand-render.test.ts
    - apps/web/lib/layout-budget.ts (NOTE_ROW_PX, OWN_BAND_PX)
  </read_first>
  <action>
    Swap the `NoteChip` render in OwnHand's note row for `NoteBox`, keeping the row's fixed `NOTE_ROW_PX` height so the own band's ledger figure is unchanged, and keeping OwnHand's body touching only `card.id` and `card.facts`. TeammateHand still renders no note surface (D-03 carried: notes are own-hand only). Update own-hand-render.test.ts: replace the note-chip assertion with a note-box assertion, keep the "TeammateHand renders no note surface" assertion, and re-verify the byte-identical visible-vs-identity-stripped and identical-card-back assertions still hold with the box present.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web own-hand-render own-hand-source && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'NoteBox' apps/web/components/hanabi/Hand.tsx` matches inside the own-hand slot render only
    - `grep -n 'NOTE_ROW_PX' apps/web/components/hanabi/Hand.tsx` matches
    - own-hand-render.test.ts asserts a note box in own-hand markup and none in teammate markup
    - `npx vitest run --project web own-hand-render own-hand-source` passes
  </acceptance_criteria>
  <done>The note box is on the board, in the height it was budgeted, without weakening the identity guards.</done>
</task>

<task type="auto">
  <name>Task 3: Keep-hints toggle and tile-colour picker in the controls row</name>
  <files>apps/web/components/hanabi/TileColorPicker.tsx, apps/web/components/hanabi/HanabiBoard.tsx, apps/web/components/hanabi/Hand.tsx, apps/web/lib/note-box-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/HanabiBoard.tsx (controls row ~259-305, act() ~158, the justCluedIds effect ~101-114 for the pattern NOT to copy)
    - apps/web/components/hanabi/AudioControls.tsx (icon-button-opens-panel pattern and its 44px target)
    - apps/web/lib/keep-hints-pref.ts, apps/web/lib/tile-color-pref.ts (06.2-03)
    - apps/web/lib/hanabi-hint-logic.ts (06.2-03)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-UI-SPEC.md (Copywriting Contract, Tile colour picker)
  </read_first>
  <action>
    Per D-05/D-13/D-14. Create TileColorPicker.tsx: an icon-only button (`aria-label="Choose tile colour"`, 44px target via the out-of-flow inset span) that opens a small swatch grid of `TILE_COLOR_PRESETS`, each swatch a button with its preset aria-label and its `cssValue` as background, calling back with the chosen id and marking the active preset with `aria-pressed`. No accent colour beyond the existing focus ring.
    In HanabiBoard.tsx: hold `keepHints` and `tileColor` in component state, loaded in mount effects (never in the state initializer, so SSR stays stable), written back through `writeKeepHintsPref`/`writeTileColorPref` on change. Render the keep-hints toggle (lucide `Eye`/`EyeOff`, the two aria-labels from the copy contract, 44px target) and `TileColorPicker` in the existing bottom controls row beside `AudioControls` — not as a new row, so the ledger's controls-row height is unchanged. Compute each card's hint visibility synchronously per render with `hintsVisibleForCard(game.history, card.id, { keepVisible: keepHints })` and pass the resulting set (plus the resolved tile-colour CSS value) down to `OwnHand`/`TeammateHand`, which already accept `hintsVisible`/`tileColor` from 06.2-04. Do NOT add a timer, ref-based clear or effect cleanup for hint lifetime — the `justCluedIds` 2000ms flash stays exactly as it is and is unrelated (06.1 CR-01, RESEARCH.md Pitfall 4).
    Extend note-box-render.test.ts (or add assertions in the nearest existing render test for controls) with a TileColorPicker render check: five swatches with the contracted aria-labels, and the toggle's two aria-label states.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web && npx playwright test e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Keep hints visible\|Clear hints after each move\|Choose tile colour' apps/web/components/hanabi/HanabiBoard.tsx apps/web/components/hanabi/TileColorPicker.tsx` matches all three strings
    - `grep -n 'hintsVisibleForCard' apps/web/components/hanabi/HanabiBoard.tsx` matches
    - `grep -cE 'setTimeout' apps/web/components/hanabi/HanabiBoard.tsx` is unchanged from before this task (only the pre-existing clue-flash timer)
    - `npx vitest run --project web` and `npx playwright test e2e/start-game.spec.ts` both pass
  </acceptance_criteria>
  <done>A player can keep hints on screen and recolour their tiles, both choices persisting locally, with hint lifetime derived from turn history alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note text → browser storage | Free-text stays local and is never serialised to the wire |
| own-hand render | Controls sitting on own tiles must not carry identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-12 | Information Disclosure | NoteBox reaching the wire | mitigate | Source scan in note-box-render.test.ts forbids room-socket/room-store/nanoid/send imports (Task 1) |
| T-06.2-13 | Information Disclosure | own-hand props widening via new controls | mitigate | NOTE_BOX_PATH added to own-hand-source.test.ts; own-hand-render byte-identical guard re-verified (Tasks 1-2) |
| T-06.2-14 | Tampering | stored tile-colour/keep-hints values | mitigate | Readers validate against the known preset set and fall back to defaults (06.2-03) |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; `lucide-react` is already pinned at 1.39.0 |
</threat_model>

<verification>
- `npx vitest run --project web` green, `npx tsc -b apps/web` exits 0
- `npx playwright test e2e/start-game.spec.ts` passes, including the UI-11 viewport test
</verification>

<success_criteria>
Every own tile carries a live private note box, the controls row gains the keep-hints toggle and the tile-colour picker, both preferences persist, and no guard test was weakened to get there.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-06-SUMMARY.md` when done
</output>
