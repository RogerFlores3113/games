---
phase: 06.2-board-redesign-hint-display-tile-styling-board-layout-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rules/src/hanabi/state.ts
  - packages/rules/src/hanabi/adapter.ts
  - packages/rules/src/hanabi/actions.ts
  - packages/rules/src/hanabi/legality.ts
  - packages/rules/src/hanabi/projection.ts
  - packages/rules/src/hanabi/actions.test.ts
  - packages/rules/src/hanabi/legality.test.ts
  - packages/rules/src/hanabi/projection.test.ts
  - packages/rules/src/hanabi/clue-facts.test.ts
  - packages/rules/src/hanabi/discard-order.property.test.ts
  - packages/schema/src/games/hanabi.ts
autonomous: true
requirements: [DISC-01]

must_haves:
  truths:
    - "D-23: the discard arrangement is server state — every seat's projected view carries the identical discardOrder"
    - "D-25: any seated player may reorder the discard at any time while the game is in progress, on or off turn, spending no turn, token or fuse and writing no history entry"
    - "D-26: a submission whose ids are not an exact permutation of the current discard ids is rejected"
    - "A newly discarded card (deliberate discard or misplay) always enters at the END of the current arrangement, never disturbing the existing order"
    - "discardOrder is always an exact permutation of state.discard's ids after any sequence of actions"
  artifacts:
    - path: "packages/rules/src/hanabi/state.ts"
      provides: "HanabiState.discardOrder, HanabiAction reorderDiscard variant, HanabiView.discardOrder"
      contains: "discardOrder"
    - path: "packages/rules/src/hanabi/legality.ts"
      exports: ["canReorderDiscard"]
      provides: "permutation + seated-actor validation mirroring canReorder"
    - path: "packages/schema/src/games/hanabi.ts"
      provides: "HanabiViewSchema.discardOrder wire field"
      contains: "discardOrder"
    - path: "packages/rules/src/hanabi/discard-order.property.test.ts"
      provides: "permutation invariant + per-seat identity of discardOrder over 200 random games"
  key_links:
    - from: "packages/rules/src/hanabi/actions.ts applyReorderDiscard"
      to: "packages/rules/src/hanabi/legality.ts canReorderDiscard"
      via: "legality gate before the state literal"
      pattern: "canReorderDiscard"
    - from: "packages/rules/src/hanabi/projection.ts toHanabiPlayerView"
      to: "HanabiView.discardOrder"
      via: "one shared const included verbatim in both return literals"
      pattern: "discardOrder"
---

<objective>
Add the shared, server-authoritative discard arrangement to the Hanabi engine and the wire schema: a new `discardOrder` state/view field and a `reorderDiscard` action that any seated player may submit at any time.

Purpose: DISC-01. This is the only server-side item in phase 06.2 and it has a complete precedent (`reorder`, 06.1-01), so it lands first and independently of every client visual task.
Output: `HanabiState.discardOrder`, `HanabiAction` `reorderDiscard`, `canReorderDiscard`, `applyReorderDiscard`, `HanabiView.discardOrder`, `HanabiViewSchema.discardOrder`, unit coverage and a permutation property test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-CONTEXT.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-RESEARCH.md
@.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md

<interfaces>
Resolved during planning — do not re-derive:

1. Every `HanabiState` object literal in the tree (each must gain `discardOrder`):
   - packages/rules/src/hanabi/adapter.ts:~36 (createInitialState) → `discardOrder: []`
   - packages/rules/src/hanabi/actions.ts applyPlay (~line 286 literal; `discard` is rebuilt at ~245 on a misplay)
   - packages/rules/src/hanabi/actions.ts applyDiscard (~line 351 literal; `discard` rebuilt at ~311)
   - packages/rules/src/hanabi/actions.ts applyClue (~line 408 literal)
   - packages/rules/src/hanabi/actions.ts applyReorder (~line 446 literal)
   - test fixtures: actions.test.ts:41 (baseState), legality.test.ts:37 (buildState), projection.test.ts:24
2. `AdapterError` (packages/rules/src/adapter.ts) is a CLOSED union mirrored 1:1 by `ErrorDetail` in packages/schema. Its members are exactly:
   not_your_turn | invalid_action | game_over | card_not_in_hand | no_clue_tokens | clue_touches_nothing | clue_target_invalid | discard_at_max_clues.
   Do NOT widen it in this plan — `canReorderDiscard` reuses existing members (see Task 2).
3. `Legality = { legal: true } | { legal: false; reason: AdapterError }` (legality.ts:25).
4. `HanabiState` field order (state.ts:32-44): variant, seatIds, turnIndex, hands, deck, stacks, discard, clueTokens, fuses, finalTurnsRemaining, history. `discardOrder` goes directly after `discard` in the type and in every literal.
5. `HanabiView` (state.ts:104-119) is deliberately non-readonly plain arrays; `discardOrder: string[]` sits directly after `discard`.
6. `HanabiViewSchema` is `z.strictObject` (packages/schema/src/games/hanabi.ts:138) — the schema field and the rules type MUST land in the SAME task and commit (RESEARCH.md Pitfall 5).
7. Resolved open question (RESEARCH.md Open Question 2): a newly discarded card id is APPENDED to the end of `discardOrder`, in both the deliberate-discard branch and the misplay branch of applyPlay.
8. `apps/worker/src/room-state.ts` needs no edit — `applyGameAction`/`toSeatView` delegate generically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: discardOrder field through state, every action literal, projection and the wire schema</name>
  <files>packages/rules/src/hanabi/state.ts, packages/rules/src/hanabi/adapter.ts, packages/rules/src/hanabi/actions.ts, packages/rules/src/hanabi/projection.ts, packages/schema/src/games/hanabi.ts, packages/rules/src/hanabi/actions.test.ts, packages/rules/src/hanabi/legality.test.ts, packages/rules/src/hanabi/projection.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/state.ts
    - packages/rules/src/hanabi/actions.ts (applyPlay, applyDiscard, applyClue, applyReorder and their state literals)
    - packages/rules/src/hanabi/projection.ts (toHanabiPlayerView, both return literals)
    - packages/rules/src/hanabi/adapter.ts (createInitialState)
    - packages/schema/src/games/hanabi.ts (HanabiViewSchema)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md (projection + schema sections)
  </read_first>
  <action>
    Per D-23/D-28 and RESEARCH.md Pitfall 5. In state.ts add `readonly discardOrder: readonly string[];` to `HanabiState` immediately after `discard`, add `discardOrder: string[];` to `HanabiView` immediately after `discard`, and add the `HanabiAction` variant `{ readonly type: "reorderDiscard"; readonly cardIds: readonly string[] }` after the existing `reorder` variant. Document on the state field that it is the shared, player-arranged order of the discard pile, always an exact permutation of `discard`'s ids, and that a newly discarded card appends to the end.
    In adapter.ts's createInitialState literal add `discardOrder: []`.
    In actions.ts add `discardOrder` to EVERY state literal, field-by-field, with no spread, no Object.assign, no omit helper (the file's existing discipline — a missing field must surface as a tsc error, never be silenced by a spread): applyClue and applyReorder carry `discardOrder: state.discardOrder` forward unchanged; applyDiscard computes a local appended array alongside its existing `const discard = [...state.discard, card];` and returns the appended value; applyPlay's misplay branch (the same branch that rebuilds `discard` with the misplayed card) appends that card's id in the same way, while its successful-play path carries the field forward unchanged.
    In projection.ts compute `const discardOrder = [...state.discardOrder];` ONCE in the shared public block beside the existing `const discard = ...`, and include the same `discardOrder,` identifier verbatim in BOTH the fail-closed unseated return literal and the seated return literal — never recomputed per branch (D-28).
    In packages/schema/src/games/hanabi.ts add `discardOrder: z.array(z.string().min(1)),` as a new top-level field of `HanabiViewSchema`, sibling of `discard`, in the same commit as the state.ts change.
    Update the three test fixtures listed in the Interfaces block so they construct `discardOrder` consistently with their `discard` fixture value.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b packages/rules packages/schema apps/worker apps/web && npx vitest run --project rules</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'discardOrder' packages/rules/src/hanabi/actions.ts` is at least 6 (four existing branches plus the two append sites)
    - `grep -c 'discardOrder' packages/rules/src/hanabi/projection.ts` is exactly 3 (one const, two return literals)
    - `grep -n 'discardOrder' packages/schema/src/games/hanabi.ts` matches inside HanabiViewSchema
    - `grep -nE '\.\.\.state[,}]|Object\.assign' packages/rules/src/hanabi/actions.ts` returns nothing
    - `npx tsc -b packages/rules packages/schema apps/worker apps/web` exits 0
    - `npx vitest run --project rules` passes with no test file changes beyond the three fixtures
  </acceptance_criteria>
  <done>discardOrder exists end to end from state through projection to the wire schema, every action branch carries or appends it, and the whole monorepo type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canReorderDiscard, isReorderDiscardRequest, applyReorderDiscard and dispatch</name>
  <files>packages/rules/src/hanabi/legality.ts, packages/rules/src/hanabi/actions.ts, packages/rules/src/hanabi/legality.test.ts, packages/rules/src/hanabi/actions.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/legality.ts (canReorder, lines ~98-128, and isGameOver)
    - packages/rules/src/hanabi/actions.ts (isReorderRequest ~116-125, parseHanabiRequest ~132, applyReorder ~421-453, applyHanabiAction dispatch ~468)
    - packages/rules/src/hanabi/legality.test.ts (existing canReorder blocks)
    - packages/rules/src/hanabi/actions.test.ts (existing reorder blocks)
    - .planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-PATTERNS.md (legality + actions sections)
  </read_first>
  <behavior>
    - canReorderDiscard rejects with `game_over` when the game is over
    - canReorderDiscard rejects with `invalid_action` when actorSeatId is not in state.seatIds (there is no per-seat hand to fall back on — ASVS V4 note in RESEARCH.md Security Domain)
    - canReorderDiscard rejects with `card_not_in_hand` when the submitted ids differ in length, contain a duplicate, or contain an id not currently in state.discard
    - canReorderDiscard allows a seated non-active player to reorder (off-turn is legal, D-25)
    - canReorderDiscard allows an exact permutation of the current discard ids, including the identity permutation
    - isReorderDiscardRequest accepts exactly `{ type: "reorderDiscard", cardIds: string[] }` and rejects extra keys, missing keys, a wrong type literal, and a non-string array member
    - applyReorderDiscard sets state.discardOrder to the submitted ids and changes nothing else: turnIndex, clueTokens, fuses, history, hands, discard and finalTurnsRemaining are all identical to the input state
  </behavior>
  <action>
    Per D-24/D-25/D-26/D-29. In legality.ts add `canReorderDiscard(state, actorSeatId, cardIds): Legality` directly below `canReorder`, copying `canReorder`'s check shape exactly (game-over gate, length match, set-size match, membership loop) with three changes: the current id list comes from `state.discard.map((c) => c.id)`, there is no hand lookup and no ownership check at all, and the not-found branch becomes a `state.seatIds.includes(actorSeatId)` guard returning `invalid_action`. Do not widen `AdapterError` — reuse `game_over`, `invalid_action` and `card_not_in_hand` as stated in the behaviour list, and document in a comment why `card_not_in_hand` is the reused literal for a discard-set mismatch (closed wire enum, deliberately not widened for a reversible shared-workspace action).
    In actions.ts add `isReorderDiscardRequest` as a verbatim copy of `isReorderRequest` with the `"reorderDiscard"` type literal, register it in `parseHanabiRequest`'s guard chain, add `applyReorderDiscard(state, actorSeatId, cardIds)` that gates on `canReorderDiscard` and returns the FULL field-by-field state literal with `discardOrder: [...cardIds]` and every other field carried from `state`, and add the dispatch line for `action.type === "reorderDiscard"` beside the existing reorder dispatch.
    Add the unit blocks from the behaviour list to legality.test.ts and actions.test.ts, mirroring the existing canReorder/applyReorder block structure and fixtures.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project rules legality actions && npx tsc -b packages/rules packages/schema</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'canReorderDiscard' packages/rules/src/hanabi/legality.ts packages/rules/src/hanabi/actions.ts` matches in both files
    - `grep -n 'reorderDiscard' packages/rules/src/hanabi/actions.ts` shows the guard, the parse chain entry and the dispatch line
    - `grep -vE '^\s*(//|\*|/\*)' packages/rules/src/adapter.ts | grep -c 'card_not_in_discard'` is 0 (AdapterError not widened)
    - every behaviour bullet has a matching `it(...)` in legality.test.ts or actions.test.ts
    - `npx vitest run --project rules legality actions` passes
  </acceptance_criteria>
  <done>A seated player's reorderDiscard request is validated and applied by the engine, and an unseated, stale, or malformed request is refused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: permutation property test and the positiveClues append-only assertion</name>
  <files>packages/rules/src/hanabi/discard-order.property.test.ts, packages/rules/src/hanabi/clue-facts.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/redaction.property.test.ts (whole file — the fc.assert structure and the reorder-driving branch, lines ~40-88)
    - packages/rules/src/hanabi/clue-facts.ts (applyClueToSlotFacts)
    - packages/rules/src/hanabi/clue-facts.test.ts
    - packages/rules/src/hanabi/test-support.ts
  </read_first>
  <behavior>
    - Over 200 random games per the existing VARIANTS/seat-count/seed generators, after EVERY applied action, state.discardOrder has the same length as state.discard, contains no duplicate id, and contains exactly the ids in state.discard
    - After a successful reorderDiscard, every seat's toHanabiPlayerView(state, seatId).discardOrder is deeply equal to the submitted ids and identical across all seats (D-28)
    - A card newly discarded after a reorder appears LAST in discardOrder while the earlier arrangement is unchanged
    - clue-facts: applying any sequence of clues to one card's facts only ever grows positiveClues — its previous entries remain, in order, as a prefix of the new array (resolves RESEARCH.md Open Question 1 / Assumption A1, the premise hint display depends on)
  </behavior>
  <action>
    Create discard-order.property.test.ts following redaction.property.test.ts's structure verbatim (fc.assert + fc.property, the same variant/seat-count/seed/action-index generators, `{ numRuns: 200 }`), driving random legal actions and interleaving `reorderDiscard` submissions built by shuffling the current `state.discard` ids by the step index. Assert the permutation invariant after every step and the per-seat identity assertion after every accepted reorderDiscard, per the behaviour list. Add the "new discard appends last, earlier arrangement preserved" case as a deterministic `it` in the same file rather than a property.
    Add the positiveClues append-only `it` to clue-facts.test.ts, exercising `applyClueToSlotFacts` repeatedly with alternating colour/rank clues and both wasTouched values, asserting the previous positiveClues array is always a prefix of the next. Include a one-line comment recording that this is the assertion phase 06.2's hint display relies on (hints are a pure function of current facts because facts never shrink).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project rules discard-order clue-facts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rules/src/hanabi/discard-order.property.test.ts` exists and contains `numRuns: 200`
    - the file asserts both the permutation invariant and cross-seat identity of the projected discardOrder
    - `grep -n 'positiveClues' packages/rules/src/hanabi/clue-facts.test.ts` matches the new append-only test
    - `npx vitest run --project rules discard-order clue-facts` passes
  </acceptance_criteria>
  <done>The discard arrangement is proven to stay a permutation of the pile and identical for every seat, and hint display's "facts never shrink" premise is an asserted fact rather than an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RoomDO WebSocket | A client-authored `reorderDiscard` payload crosses into engine state |
| engine state → per-seat view | Projection is the single exit point from secret state to the wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-01 | Tampering | `reorderDiscard` payload | mitigate | `isReorderDiscardRequest` exact-own-key guard plus `canReorderDiscard`'s exact-permutation check against `state.discard` ids (Task 2) |
| T-06.2-02 | Elevation of Privilege | unseated/spectator actor | mitigate | `state.seatIds.includes(actorSeatId)` gate returning `invalid_action` (Task 2) |
| T-06.2-03 | Information Disclosure | `discardOrder` in the seat view | mitigate | Field carries only already-public discarded-card ids and is computed once in the public block (Task 1); existing leak canaries (`hanabi-leak-check.test.ts`, `redaction.property.test.ts`) still run |
| T-06.2-04 | Denial of Service | repeated reorder spam | accept | Action is idempotent, writes no history, consumes no token; existing per-seat actionId dedup already bounds duplicate submissions |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no packages (RESEARCH.md Package Legitimacy Audit: not applicable); if any install becomes necessary, stop and run the full Package Legitimacy Gate first |
</threat_model>

<verification>
- `npx tsc -b apps/web packages/rules packages/schema apps/worker` exits 0
- `npx vitest run --project rules` fully green, including the existing leak and redaction property tests
</verification>

<success_criteria>
The engine accepts a `reorderDiscard` from any seated player, rejects stale/malformed/unseated submissions, appends newly discarded cards to the end of the arrangement, and emits one identical `discardOrder` to every seat through the strict wire schema.
</success_criteria>

<output>
Create `.planning/phases/06.2-board-redesign-hint-display-tile-styling-board-layout-tokens/06.2-01-SUMMARY.md` when done
</output>
