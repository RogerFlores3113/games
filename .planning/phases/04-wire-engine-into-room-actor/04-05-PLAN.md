---
phase: 04-wire-engine-into-room-actor
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - apps/worker/src/room-do.test.ts
autonomous: true
requirements: [RT-09]

must_haves:
  truths:
    - "D-15/RT-09: deliberately double-sending a byte-identical clue frame with the same actionId applies it exactly once — clueTokens, history.length, activeSeatId/isYourTurn and deckCount are all unchanged by the second send."
    - "D-08: the dedup still holds when the retry arrives after a genuine Durable Object eviction, proving the key is persisted rather than held in memory."
  artifacts:
    - path: "apps/worker/src/room-do.test.ts"
      provides: "RT-09 double-send integration proof plus the forced-eviction retry proof"
      contains: "actionId"
  key_links:
    - from: "apps/worker/src/room-do.test.ts"
      to: "apps/worker/src/room-state.ts"
      via: "real WebSocket frames exercising the persisted dedup branch"
      pattern: "actionId"
---

<objective>
Prove RT-09 on the real wire against a live `wrangler dev` instance: a duplicated `game_action` applies exactly once, and that guarantee survives a genuine Durable Object eviction (D-08, D-15).

Purpose: Playwright offers no reliable hook for forcing a byte-identical duplicate frame, so the socket-level harness already built for the D-17 eviction test is the right tool. A clue is the action that breaks naive idempotency, so a clue is what must be double-sent.
Output: two new test cases inside the existing `wrangler dev` + raw WebSocket harness — no second harness.

Both tests exercise the dedup branch that plan `04-04` adds to `room-state.ts`, which is why this plan depends on it. The Hanabi repointing of this file is already done: plan `04-03` repointed CR-03 (its Task 2) and rebuilt the layer-3 frame-capture leak proof (its Task 4) in the same plan as the engine swap, so `room-do.test.ts` contains no toy-shaped construct when this plan starts. Do not re-do that work, and do not regress it.

This plan adds NO production code. If a test fails, fix the production defect it found in the plan that owns that code, not by weakening the assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/04-wire-engine-into-room-actor/04-PATTERNS.md

<interfaces>
Harness helpers already defined in apps/worker/src/room-do.test.ts — reuse, do not reinvent:
  spawnWrangler(), waitForReady(), killAndWait(proc), openSocket(code),
  collectMessages(ws) -> { raw, parsed, waitFor(predicate, timeoutMs), waitForClose },
  send(ws, msg), mintRoomCode()

The file's own comment (lines 112-119) warns that messages arrive in tight bursts,
so tests MUST search by predicate rather than assuming "the Nth" or "the most
recent" message.

Wire-visible Hanabi view fields available for assertions (HanabiView — history IS
on the wire and IS populated by toHanabiPlayerView):
  yourHand[], otherHands[{seatId, cards[]}], stacks[], discard[],
  clueTokens, fuses, deckCount, finalTurnsRemaining, activeSeatId,
  isYourTurn, score, history[]

Already Hanabi-shaped when this plan starts (done in plan 04-03):
  the CR-03 test (~L396-429) sends a legal rank clue and waits on `history.length`;
  the layer-3 HIDE-01/HIDE-04 test (~L497-684) uses `checkHanabiViewForLeaks` and
  `history.length` predicates. The file's `findActiveSeat` helper and the Hanabi
  `RoomViewShape`/`GameViewShape` type aliases from that rebuild are available to
  reuse here.

From plan 04-04, the dedup branch under test:
  applyGameAction(state, actorSeatId, actionId, request, now) — a seat whose
  `lastAppliedActionId` equals `actionId` short-circuits to `{ ok: true, state }`
  unchanged, and the caller still commits and pushes that seat's current view.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove a double-sent clue applies exactly once on the live wire</name>
  <read_first>
    - apps/worker/src/room-do.test.ts (the harness helpers, the Hanabi type aliases and `findActiveSeat` helper left by plan 04-03, and the existing "count frames before, then wait for a frame at an index past that" idiom used around lines 310-315)
    - apps/worker/src/room-state.ts (the dedup branch from plan 04-04, so the assertions match its actual behavior)
  </read_first>
  <files>apps/worker/src/room-do.test.ts</files>
  <action>
Add a test named for RT-09/D-15 that: mints a room, joins two sockets, starts the game, and identifies the active seat from its own latest game-bearing frame. From the active seat's view, choose a clue that is guaranteed legal — target the other seat and name a rank that appears on at least one of that seat's visible cards in `otherHands[].cards[]`.

Build the clue frame ONCE as a single object with a fixed literal `actionId`, so the second send is byte-identical. Send it, wait for the resulting `state` frame, and capture `clueTokens`, `history.length`, `activeSeatId`, `isYourTurn` and `deckCount` from the actor's view.

Then send the EXACT SAME object again. Critically, do not wait on a predicate that the first frame already satisfies — the dedup branch re-sends an identical view, so a naive `waitFor` would match the already-received frame and the test would pass without ever observing the second response. Record `c.parsed.length` before the second send and wait for a `state` frame whose index in `c.parsed` is at or beyond that recorded length, following the idiom this file already uses for the post-grace `set_variant` assertion. Add a comment explaining this trap explicitly.

Assert on the view from that second frame: `clueTokens` is unchanged (NOT decremented twice), `history.length` is unchanged, `activeSeatId` and `isYourTurn` are unchanged, and `deckCount` is unchanged. Then, as a positive control proving the seat is not simply frozen, send a THIRD frame with a DIFFERENT `actionId` carrying a legal action for the now-active seat and assert the state does advance.
  </action>
  <acceptance_criteria>
    - A test exists whose name references RT-09 and double-sends a `clue` (not a play or discard).
    - The duplicate send reuses the identical frame object and the identical `actionId` literal.
    - The test waits for a frame at an index at or beyond the pre-send `parsed.length`, with a comment naming the "identical resent view" trap.
    - The test asserts all five of `clueTokens`, `history.length`, `activeSeatId`, `isYourTurn`, `deckCount` are unchanged after the duplicate.
    - A positive-control assertion proves a different `actionId` still advances the game.
    - Fault injection: temporarily change the dedup comparison in `room-state.ts` to always be false; this test must FAIL on the `clueTokens` assertion. Revert after confirming.
    - `npm run test:integration --workspace apps/worker` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration --workspace apps/worker</automated>
  </verify>
  <done>RT-09 is proven on the real wire with the one action type that naive idempotency would miss.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the dedup survives a genuine Durable Object eviction</name>
  <read_first>
    - apps/worker/src/room-do.test.ts (the D-17 forced-restart test — the `killAndWait(child)` / `child = spawnWrangler()` / `waitForReady()` sequence and its 40s timeout)
    - apps/worker/src/persistence.ts (what actually survives a restart)
  </read_first>
  <files>apps/worker/src/room-do.test.ts</files>
  <action>
Add a test named for D-08/RESEARCH.md Pitfall 1 that proves the idempotency key is persisted rather than held in memory. Join two sockets, start the game, and send a legal clue with a fixed literal `actionId` from the active seat, waiting for the applied `state` frame and capturing `clueTokens` and `history.length`.

Close the sockets, allow a brief moment for the close-driven mutation to persist, then force a genuine eviction exactly as the D-17 test does: `killAndWait(child)`, reassign `child = spawnWrangler()`, and `await waitForReady()`. Nothing in memory survives this.

Reconnect with the actor's saved seat token, wait for the `joined` frame carrying a non-null game, and resend the SAME `actionId` with the same clue payload. Assert `clueTokens` and `history.length` are still the post-first-application values — if the key had lived only in memory, the retry would re-apply and spend a second token, which is precisely the failure this test exists to catch.

Give the test the same extended timeout the D-17 test uses (40s), since it pays for a full process kill and respawn. Add a comment stating why a socket close alone would be insufficient evidence: the Durable Object can survive a client disconnect entirely in memory, so only killing the process proves persistence.
  </action>
  <acceptance_criteria>
    - A test exists whose name references the persisted idempotency key and D-08.
    - It calls `killAndWait(child)` and reassigns `child = spawnWrangler()` before the retry.
    - The retry reuses the same `actionId` literal as the original send.
    - It asserts `clueTokens` and `history.length` are unchanged after the post-restart retry.
    - The test declares an explicit timeout of at least 40000 ms.
    - Fault injection: temporarily stop persisting `lastAppliedActionId` (drop it from the seats map in `room-state.ts`); this test must FAIL. Revert after confirming.
    - `npm run test:integration --workspace apps/worker` passes, and an unfiltered `npm test` is green.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration --workspace apps/worker && npm test</automated>
  </verify>
  <done>The dedup key is proven durable across a real eviction, which is the case hibernation actually produces in production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw WebSocket client -> Durable Object | Hostile/duplicate frames arrive here with no browser in the way |
| Durable Object memory vs. storage | Hibernation wipes memory; only storage survives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering / Repudiation | replayed byte-identical `game_action` | mitigate | Proven on the live wire with a duplicated clue frame; asserts `clueTokens` and `history.length` are unchanged (D-15) |
| T-04-22 | Tampering | dedup key lost on eviction | mitigate | Dedicated forced-restart test kills the whole `wrangler dev` process group and retries the same `actionId` (Pitfall 1) |
| T-04-24 | Repudiation | a test passing vacuously against a resent identical view | mitigate | The duplicate-send assertion waits for a frame at an index past the pre-send count, plus a positive control proving a different id still advances state |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan; the `wrangler dev` harness and its deps are pre-existing exact pins. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run test:integration --workspace apps/worker` green.
- `npm test` green, unfiltered.
- Each of the two tests fails under its documented fault injection before the fix is reverted.
</verification>

<success_criteria>
- A double-sent clue applies exactly once on a real WebSocket against a real workerd instance.
- The same holds when the retry arrives after a forced process kill and respawn.
- The Hanabi repointing done in plan `04-03` is still intact — no toy-shaped construct reappears.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-05-SUMMARY.md` when done.
</output>
