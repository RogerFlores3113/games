---
phase: 04-wire-engine-into-room-actor
plan: 07
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - e2e/start-game.spec.ts
  - e2e/in-progress-arrival.spec.ts
  - e2e/hanabi-realtime.spec.ts
autonomous: true
requirements: [RT-01, RT-03]

must_haves:
  truths:
    - "RT-01/D-14: a clue, play or discard taken in one browser appears on another player's screen in near real time with no manual refresh and no page.reload() anywhere on that path."
    - "RT-03/D-14: a player who refreshes mid-game rejoins their exact same seat with full game state and no lost turn — same seat id, same hand size, same turn, same token counts."
    - "D-14: both proofs extend the existing e2e specs against the real worker rather than adding a parallel harness."
  artifacts:
    - path: "e2e/hanabi-realtime.spec.ts"
      provides: "RT-01 and RT-03 browser-level proofs"
      min_lines: 60
  key_links:
    - from: "e2e/hanabi-realtime.spec.ts"
      to: "apps/web/components/HanabiBoard.tsx"
      via: "binds to the board's stable data-testid contract"
      pattern: "turn-indicator|own-hand|clue-tokens"
---

<objective>
Prove RT-01 and RT-03 in real browsers against the real worker (D-14).

Purpose: these are two of the phase's three success criteria, and both are browser-level claims — an action appearing on someone else's screen without a refresh, and a refresh landing you back in your own seat mid-game.
Output: the two existing toy-driven specs repointed to Hanabi, plus one new spec carrying the RT-01 and RT-03 proofs.

RT-09 is deliberately NOT tested here — Playwright offers no reliable hook for forcing a byte-identical duplicate frame, so it lives at the socket level in plan `04-05`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/04-wire-engine-into-room-actor/04-UI-SPEC.md

<interfaces>
Existing e2e helpers (e2e/helpers.ts) — reuse, do not reinvent:
  createRoom(page, { name, variant? }) -> room code   // host ends up seated in the lobby
  joinAs(context, code, name) -> Page                  // a second player in a fresh context
  expectSeatCount(page, n)

Board testids available from plan 04-06:
  turn-indicator, own-hand, own-hand-slot-{n}, other-hand-{seatId},
  other-hand-card-{cardId}, played-stack-{suit}, discard-pile, clue-tokens,
  fuse-tokens, deck-count, play-button, discard-button, clue-target-{seatId},
  clue-value-{value}, give-clue-button, game-over-heading, final-score

Turn indicator copy (UI-SPEC Copywriting Contract): "Your turn" when it is yours,
"Waiting for {displayLabel}" otherwise. The toy's "Your turn — guess your card"
no longer exists.

Seat-token persistence: the client stores the seat token in localStorage keyed by
room code, and `useRoomSocket`'s onOpen replays it on every open — which is what
makes a plain page.reload() reattach to the same seat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the existing specs from the toy to the Hanabi board</name>
  <read_first>
    - e2e/start-game.spec.ts (the full file — the gating assertions, the variant lock, the two-context setup, and every toy testid it binds to)
    - e2e/in-progress-arrival.spec.ts (the `own-card` assertions at lines 23 and 38)
    - e2e/helpers.ts (createRoom / joinAs / expectSeatCount)
    - apps/web/components/HanabiBoard.tsx (the real rendered testids and copy)
  </read_first>
  <files>e2e/start-game.spec.ts, e2e/in-progress-arrival.spec.ts</files>
  <action>
In `start-game.spec.ts`, keep everything that tests ROOM-06 gating and the variant lock unchanged: the disabled start button with the "Need 2–5 players" caption, the zero-"ready"-text assertion, the Black variant radio round-trip, the joiner having no variant picker, and the variant picker disappearing once the game starts.

Replace the toy board assertions with Hanabi equivalents: `own-card` becomes `own-hand`; the 16 `guess-button-*` controls become the board's play/discard/give-clue controls; `revealed-entry`/`revealed-list`/`score` assertions become assertions on the table (`clue-tokens`, `deck-count`, `discard-pile` or a played stack, as fits the action taken). Update the turn-indicator expectations to the UI-SPEC copy: exactly one page reads "Your turn" and the other contains "Waiting for".

Preserve the spec's HIDE-01 browser-surface check in Hanabi form, because it is the only place the redaction claim is asserted against rendered DOM: assert each page's own-hand slots render no suit or rank text, while that same player's cards ARE visible as text in the OTHER page's `other-hand-*` region. Derive the expected text from what the other page actually renders rather than hardcoding a card identity, since the deck order is seeded server-side and secret.

In `in-progress-arrival.spec.ts`, change both `own-card` references to `own-hand`. Everything else in that file — the refusal copy, the absence of a partial table behind the refusal, the full-room case — stays exactly as it is.
  </action>
  <acceptance_criteria>
    - `grep -c 'own-card\|guess-button\|revealed-entry\|revealed-list' e2e/start-game.spec.ts e2e/in-progress-arrival.spec.ts` outputs `0` for both files.
    - `start-game.spec.ts` still asserts the disabled start button, the "Need 2–5 players" caption, the zero-"ready"-text check, and the Black variant round-trip.
    - The HIDE-01 browser assertion still exists, now asserting own-hand slots carry no suit/rank text while the other page shows that player's cards.
    - The turn-indicator assertions use "Your turn" and "Waiting for".
    - `npx playwright test e2e/start-game.spec.ts e2e/in-progress-arrival.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/start-game.spec.ts e2e/in-progress-arrival.spec.ts</automated>
  </verify>
  <done>The existing browser specs drive the real Hanabi board and keep their ROOM-06 and HIDE-01 coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Prove RT-01 — an action appears on the other screen with no refresh</name>
  <read_first>
    - e2e/start-game.spec.ts (the two-browser-context setup pattern to copy)
    - e2e/helpers.ts (createRoom / joinAs)
    - apps/web/components/HanabiBoard.tsx (the clue control flow: select target, select value, submit)
  </read_first>
  <files>e2e/hanabi-realtime.spec.ts</files>
  <action>
Create `e2e/hanabi-realtime.spec.ts` with a test named for RT-01. Create a room as host, join a second player in a fresh context, start the game, and determine which page is active by reading the turn indicators.

From the ACTIVE page, take a clue action against the other seat, choosing a clue value the board itself shows as enabled so the action cannot be refused for touching zero cards. Before acting, capture the PASSIVE page's `clue-tokens` text.

Then assert the passive page updates on its own: its `clue-tokens` text changes to the decremented value, and its turn indicator flips to "Your turn", using Playwright's auto-retrying assertions. The load-bearing constraint is that the passive page must never be reloaded — assert this structurally by ensuring the test body contains no `reload()` call on the passive page, and state that in a comment as the actual point of the test.

Add a second assertion pass in the same test proving the same holds for a non-clue action: have the now-active page discard or play a slot, and assert the other page's board reflects it (deck count, discard pile, or a played stack) without a reload.
  </action>
  <acceptance_criteria>
    - `e2e/hanabi-realtime.spec.ts` contains a test whose name references RT-01.
    - `grep -c 'reload()' e2e/hanabi-realtime.spec.ts` outputs `0` within the RT-01 test body (a reload appears only in the RT-03 test added in Task 3).
    - The test asserts the passive page's `clue-tokens` value changed and its turn indicator became "Your turn".
    - The test covers both a clue and a play-or-discard action.
    - `npx playwright test e2e/hanabi-realtime.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/hanabi-realtime.spec.ts</automated>
  </verify>
  <done>An action taken in one browser is proven to reach another player's screen with no manual refresh.</done>
</task>

<task type="auto">
  <name>Task 3: Prove RT-03 — a mid-game refresh returns to the same seat with full state</name>
  <read_first>
    - e2e/hanabi-realtime.spec.ts (the RT-01 test from Task 2, whose setup this reuses)
    - apps/web/lib/room-socket.ts (the onOpen seat-token replay that makes reattachment work)
    - apps/web/lib/seat-token.ts (where the seat token is persisted per room code)
  </read_first>
  <files>e2e/hanabi-realtime.spec.ts</files>
  <action>
Add a second test to the same spec, named for RT-03. Set up a two-player in-progress game as in Task 2, and take at least one action first so the reload is genuinely mid-game rather than at the opening state — a reload at turn zero would prove much less.

Before reloading, capture from the reloading page: its own seat's `data-seat-id` (or its own-hand slot count), the `clue-tokens` text, the `deck-count` text, and its turn-indicator text.

Call `page.reload()` on that page, then assert every captured value comes back identical: the same seat (not a new one), the same number of own-hand slots, the same clue-token and deck-count readings, and the same turn indicator — proving no turn was lost and the game was not restarted. Also assert the other player's page was unaffected throughout.

Add an assertion that the reloaded page is NOT showing the in-progress refusal card, since a seat-token failure would surface as exactly that and would otherwise look superficially like a working page.
  </action>
  <acceptance_criteria>
    - A test named for RT-03 exists in `e2e/hanabi-realtime.spec.ts` and calls `page.reload()`.
    - At least one game action is taken BEFORE the reload.
    - Post-reload assertions cover seat identity, own-hand slot count, clue tokens, deck count, and the turn indicator.
    - The test asserts `refusal-card` has count 0 after the reload.
    - `npx playwright test e2e/hanabi-realtime.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/hanabi-realtime.spec.ts</automated>
  </verify>
  <done>A mid-game refresh is proven to restore the same seat with full state and the same turn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM -> redaction claim | The rendered page is the surface a player could inspect |
| localStorage seat token -> seat reclaim | A refresh re-presents a capability token to reclaim a seat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-30 | Information Disclosure | rendered own-hand markup | mitigate | The repointed `start-game.spec.ts` asserts own-hand slots render no suit/rank text while the same cards are visible on the other player's page |
| T-04-31 | Spoofing | seat reclaim after refresh | mitigate | RT-03 asserts the SAME seat comes back and that no refusal card is shown; RT-07 seat-hijack coverage remains in the existing `seat-takeover.spec.ts`, unchanged by this phase |
| T-04-32 | Repudiation | a realtime test passing because of an incidental reload | mitigate | The RT-01 test body contains no `reload()`, asserted by an explicit grep criterion |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; `@playwright/test` (1.62.1) is a pre-existing exact pin. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx playwright test` green across the whole e2e suite.
- The RT-01 test contains no reload; the RT-03 test reloads only after an action has been taken.
</verification>

<success_criteria>
- A clue, play or discard in one browser appears on the other player's screen without a refresh.
- A mid-game refresh returns the player to the same seat with full state and the same turn.
- The existing lobby, gating and refusal specs still pass against the new board.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-07-SUMMARY.md` when done.
</output>
