---
phase: 04-wire-engine-into-room-actor
plan: 08
type: execute
wave: 8
depends_on: ["04-07"]
files_modified:
  - packages/rules/src/index.ts
  - packages/rules/src/adapter.test.ts
  - packages/schema/package.json
  - packages/schema/src/games/subpath.test.ts
  - tsconfig.base.json
  - vitest.config.ts
  - apps/worker/src/source-structure.test.ts
autonomous: false
requirements: [RT-01, RT-03, RT-09]

must_haves:
  truths:
    - "D-01: the forehead-card toy is deleted outright — its rules module, leak checker, tests, schema module, UI component, and every export of those names from packages/rules/src/index.ts."
    - "D-03: a zero-occurrence regression check proves the toy identifier is gone from the worker, distinct from the confinement check that proves hanabiGame lives only in game-registration.ts."
    - "D-16: the phase gate is the full suite plus the e2e specs — npm test and npx playwright test green, and tsc -b clean."
  artifacts:
    - path: "packages/rules/src/index.ts"
      provides: "Barrel carrying only the adapter contract and the Hanabi engine"
      contains: "hanabiGame"
  key_links:
    - from: "apps/worker/src/source-structure.test.ts"
      to: "packages/rules/src/index.ts"
      via: "zero-occurrence proof that the deleted toy name is gone"
      pattern: "foreheadCardGame"
---

<objective>
Delete the forehead-card toy outright (D-01) and run the phase gate (D-16).

Purpose: Phases 2 and 3 both promised this deletion; it happens here. The toy existed to prove the redaction contract cheaply before the real engine existed, and that proof has now been repointed to Hanabi in plans `04-03` and `04-05`.
Output: the toy removed from `packages/rules`, `packages/schema`, `apps/web` and the build wiring, a zero-occurrence regression check, and a green full-suite gate.

Deletion is safe ONLY because the leak coverage was repointed first. If `npx vitest run --project worker redaction-wire` is not currently green against Hanabi, stop and fix that before deleting anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/04-wire-engine-into-room-actor/04-VALIDATION.md

<interfaces>
Files to DELETE (the confirmed coupling sweep):
  packages/rules/src/forehead-card.ts
  packages/rules/src/forehead-card-leak-check.ts
  packages/rules/src/forehead-card.test.ts
  packages/rules/src/forehead-card-leak-check.test.ts
  packages/rules/src/forehead-card.property.test.ts
  packages/schema/src/games/forehead-card.ts
  packages/schema/src/games/forehead-card.test.ts
  apps/web/components/ForeheadCardGame.tsx

Files to MODIFY (references that would otherwise dangle):
  packages/rules/src/index.ts            lines 18-30: the toy export block
  packages/rules/src/adapter.test.ts     line 7 import, line 86 conformance call,
                                         and the purity file list entries
                                         "forehead-card.ts" / "forehead-card-leak-check.ts"
                                         (that list is read with readFileSync — a stale
                                         entry throws rather than failing softly)
  packages/schema/package.json           the "./games/forehead-card" exports entry
  tsconfig.base.json                     the forehead-card paths entry
  vitest.config.ts                       the forehead-card alias in ALL FOUR projects
  packages/schema/src/games/subpath.test.ts   the forehead-card import and assertions
  apps/worker/src/source-structure.test.ts    the zero-occurrence assertion added in 04-03

DELIBERATELY NOT CHANGED: comment prose in packages/rules/src/hanabi/*.ts and
packages/schema/src/constants.ts that cites forehead-card.ts as the template it was
modelled on. That is historical design rationale, not a live dependency, which is why
the regression check below targets the IDENTIFIER `foreheadCardGame` and file existence
rather than the word "forehead".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the toy and every dangling reference to it</name>
  <read_first>
    - packages/rules/src/index.ts (the toy export block at lines 18-30 and the Hanabi block at 32-39 that stays)
    - packages/rules/src/adapter.test.ts (the import, the `describeAdapterConformance("forehead-card", ...)` call, and the purity file list)
    - packages/schema/src/games/subpath.test.ts (the forehead assertions to drop and the Hanabi ones added in plan 04-01 that stay)
    - packages/schema/package.json, tsconfig.base.json, vitest.config.ts (the three wiring locations)
  </read_first>
  <files>packages/rules/src/index.ts, packages/rules/src/adapter.test.ts, packages/schema/package.json, packages/schema/src/games/subpath.test.ts, tsconfig.base.json, vitest.config.ts</files>
  <action>
Delete the eight files listed in the Interfaces block using `git rm`.

In `packages/rules/src/index.ts`, remove the toy export block (the `foreheadCardGame`/`FOREHEAD_CARD_VALUES` value exports, the six toy type exports, and the `checkSeatViewForLeaks`/`secretsForSeat`/`SeatSecrets` leak-checker exports). Keep the adapter-contract exports and the entire Hanabi block. Update the file header: it currently describes the package as carrying the toy "deleted in Phase 4 once the real Hanabi engine lands" — restate it as carrying the `GameAdapter` contract and the Hanabi engine with its leak checker.

In `packages/rules/src/adapter.test.ts`, remove the `foreheadCardGame` import and its `describeAdapterConformance("forehead-card", ...)` call, keeping the Hanabi conformance call and the reusable `describeAdapterConformance` helper itself (it is deliberately game-agnostic). Remove `"forehead-card.ts"` and `"forehead-card-leak-check.ts"` from the purity test's file list — that list is read with `readFileSync`, so a stale entry throws.

Remove the `"./games/forehead-card"` entry from `packages/schema/package.json`'s exports, the forehead-card entry from `tsconfig.base.json`'s paths, and the forehead-card alias from all four Vitest project alias blocks. Preserve the Hanabi subpath alias ordering — it must still precede the bare `@games/schema` key everywhere.

In `packages/schema/src/games/subpath.test.ts`, drop the forehead-card import and its assertions, keeping the Hanabi subpath test, the barrel-purity test, and the `RoomViewSchema.game` stays-`z.unknown()` test.
  </action>
  <acceptance_criteria>
    - All eight listed files no longer exist (`git status` shows them as deleted).
    - `grep -rl 'foreheadCardGame\|FOREHEAD_CARD_VALUES\|FOREHEAD_CARD_GAME_ID\|ForeheadCardView\|ForeheadCardState\|ForeheadCardGame\|checkSeatViewForLeaks\|secretsForSeat' --include=*.ts --include=*.tsx --include=*.json . | grep -v node_modules` returns NOTHING.
    - `grep -rc 'forehead-card' packages/schema/package.json tsconfig.base.json vitest.config.ts` outputs `0` for each.
    - The `@games/schema/games/hanabi` alias still precedes the bare `@games/schema` alias in all four Vitest projects and in `tsconfig.base.json`.
    - `npx tsc -b` exits 0 and `npm test` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npm test</automated>
  </verify>
  <done>The toy is gone from source, tests, schema, UI and build wiring, with no dangling references.</done>
</task>

<task type="auto">
  <name>Task 2: Make the deletion non-reversible by accident</name>
  <read_first>
    - apps/worker/src/source-structure.test.ts (the A9 confinement assertion and the zero-occurrence assertion added in plan 04-03)
  </read_first>
  <files>apps/worker/src/source-structure.test.ts</files>
  <action>
Strengthen the zero-occurrence regression assertion added in plan `04-03` now that the files themselves are gone: keep the assertion that `foreheadCardGame` appears in zero scanned worker sources, and add an assertion that the deleted toy modules no longer exist on disk, checking `packages/rules/src/forehead-card.ts` and `packages/schema/src/games/forehead-card.ts` with `existsSync` and expecting `false`.

Add a comment stating precisely what this check does and does not claim: it targets the IDENTIFIER and the FILES, deliberately not the word "forehead", because several `packages/rules/src/hanabi/*.ts` files and `packages/schema/src/constants.ts` legitimately cite `forehead-card.ts` in comment prose as the template they were modelled on. A word-level check would either fail on that historical rationale or pressure someone into deleting design context that is still worth reading.

Re-confirm A9 still asserts `hanabiGame` is confined to `game-registration.ts` with a non-vacuity floor of at least one hit, so the pair of checks together means: the new game's name lives in exactly one file, and the old game's name lives in none.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project worker source-structure` is green.
    - The spec asserts `existsSync` is false for both deleted toy module paths.
    - A9 still asserts `hanabiGame` is confined to `game-registration.ts` with a count of at least 1.
    - The comment explaining identifier-vs-prose scoping is present.
    - Fault injection: temporarily recreate `packages/rules/src/forehead-card.ts` as an empty file; the existence assertion must FAIL. Delete it again after confirming.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project worker source-structure</automated>
  </verify>
  <done>Reintroducing the toy, by file or by identifier, fails the build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase gate — full suite, e2e, and a real two-player game</name>
  <read_first>
    - .planning/phases/04-wire-engine-into-room-actor/04-VALIDATION.md (the sampling contract and the per-requirement verification map)
    - .planning/ROADMAP.md (Phase 4's three success criteria)
  </read_first>
  <action>
Run the D-16 phase gate in this order and report each result before pausing: `npx tsc -b`, then `npm test` (all four Vitest projects), then `npx playwright test` (the full e2e suite). All three must exit 0. If any is red, fix the failure and re-run the whole gate rather than proceeding with a partially green suite.

Then start both dev servers so the user can verify by hand: `npm run dev --workspace apps/worker` and `npm run dev --workspace apps/web` (port 3100). Leave them running and hand over to the numbered verification steps below. Do not mark the phase complete until the user responds.
  </action>
  <what-built>
The complete phase: the real Hanabi engine wired through the Phase 2 registration seam, a strict Hanabi view schema at the fail-closed send gate, per-seat `actionId` dedup persisted across hibernation, a widened closed refusal vocabulary, the interim playable board, RT-01/RT-03 browser proofs, RT-09 socket-level proof, and the forehead-card toy deleted.

Before pausing, run the D-16 gate and report the results:
  npx tsc -b
  npm test
  npx playwright test
All three must be green. If any is red, fix it before requesting verification.
  </what-built>
  <how-to-verify>
1. Start the worker (`npm run dev --workspace apps/worker`) and the web app (`npm run dev --workspace apps/web`, port 3100).
2. Open http://localhost:3100, create a room as "Roger", and copy the room link.
3. Open the link in a second browser profile or private window and join as "Bianca".
4. Start the game from the host window. Both windows should show a Hanabi table: your own hand as blank slots labelled "Slot 1".., the other player's hand face up with written suit and rank, played stacks, discard, clue tokens (8), fuses, and deck count.
5. Confirm your own hand shows NO suit or rank anywhere, while the other window shows those same cards face up.
6. On the active window, give a clue to the other seat. Confirm WITHOUT refreshing that the other window updates: clue tokens drop to 7 and the turn indicator flips.
7. Confirm the clued slots in the receiving player's own hand now show a fact chip such as "Told: Red" or "Told: 3".
8. Refresh the non-host window mid-game (F5). Confirm it returns to the SAME seat with the same hand size, the same token counts, and the same turn — not a refusal screen and not a fresh lobby.
9. Try to discard while clue tokens are at 8: the Discard button should be disabled with the "Clue tokens are full — you can't discard" caption. Try to act when it is not your turn: controls should be disabled.
10. Open DevTools -> Network -> the WebSocket connection -> Messages in either window. Inspect a `state` frame's `view.game.yourHand` and confirm each entry has `id`, `hidden: true` and `facts` and structurally NO `suit` and NO `rank` keys.
  </how-to-verify>
  <acceptance_criteria>
    - `npx tsc -b`, `npm test`, and `npx playwright test` all exit 0.
    - The user confirms a two-player base game is playable end to end.
    - The user confirms an action appears on the other screen with no refresh (RT-01).
    - The user confirms a mid-game refresh returns the same seat with full state (RT-03).
    - The user confirms own-hand entries in a raw WebSocket frame carry no `suit`/`rank` keys.
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe what you saw that was wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted toy code paths | Every removed path is one fewer place per-seat redaction could be skipped |
| full-suite gate | The last automated checkpoint before the phase is declared done |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33 | Information Disclosure | leak coverage removed along with the toy | mitigate | Deletion is sequenced AFTER the Hanabi repoint in plans `04-03` and `04-05`; this plan's objective states the precondition explicitly |
| T-04-34 | Tampering | toy reintroduced later, bypassing the Hanabi gate | mitigate | Zero-occurrence identifier check plus `existsSync` file assertions in the structural audit |
| T-04-35 | Information Disclosure | own-hand identity on the real wire | mitigate | Human DevTools inspection of an actual `state` frame in the blocking checkpoint, backing the automated layer-1/2/3 proofs |
| T-04-36 | Repudiation | phase declared done on a partially green suite | mitigate | The checkpoint requires `tsc -b`, `npm test` and `npx playwright test` all green before the human verification begins (D-16) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed across this entire phase; every dependency used was a pre-existing exact pin. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc -b` clean.
- `npm test` green across schema, rules, worker, and web.
- `npx playwright test` green across the full e2e suite.
- Human confirmation of a real two-player game, RT-01, RT-03, and a clean WebSocket frame.
</verification>

<success_criteria>
- The forehead-card toy exists nowhere in source, tests, schema, UI, or build wiring.
- Reintroducing it fails the build.
- The full D-16 gate is green and a real two-player base game has been played end to end.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-08-SUMMARY.md` when done.
</output>
