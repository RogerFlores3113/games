---
phase: 04-wire-engine-into-room-actor
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rules/src/index.ts
  - apps/web/lib/hanabi-board-logic.ts
  - apps/web/lib/hanabi-board-logic.test.ts
  - apps/web/components/HanabiBoard.tsx
autonomous: true
gap_closure: true
requirements: [RT-01]

must_haves:
  truths:
    - "A fresh game's fuse counter reads '3 fuses left'"
    - "After one misplay the fuse counter reads '2 fuses left'; at game over from fuses it reads '0 fuses left'"
    - "Engine and wire semantics of `fuses` are unchanged: HanabiState.fuses and HanabiView.fuses still count fuses USED (0 at start, game over at MAX_FUSES)"
  artifacts:
    - path: "packages/rules/src/index.ts"
      provides: "MAX_FUSES exported from the rules barrel"
      contains: "MAX_FUSES"
    - path: "apps/web/lib/hanabi-board-logic.ts"
      provides: "fusesRemainingForView(view) = MAX_FUSES - view.fuses"
      exports: ["fusesRemainingForView"]
    - path: "apps/web/lib/hanabi-board-logic.test.ts"
      provides: "Regression tests for remaining-fuse display value"
      contains: "fusesRemainingForView"
  key_links:
    - from: "apps/web/components/HanabiBoard.tsx"
      to: "apps/web/lib/hanabi-board-logic.ts"
      via: "fuse-tokens paragraph renders fusesRemainingForView(game)"
      pattern: "fusesRemainingForView\\(game\\)"
    - from: "apps/web/lib/hanabi-board-logic.ts"
      to: "packages/rules/src/index.ts"
      via: "import { MAX_FUSES } from @games/rules"
      pattern: "MAX_FUSES"
---

<objective>
Close the single UAT gap from 04-UAT.md (test 10, minor): the board's fuse counter renders `game.fuses` (fuses USED) under the label "fuses left", so it counts up 0 to 3 instead of down 3 to 0.

Purpose: The label must match the number. Fix is display-only; the engine's `fuses` field (used-count, incremented on misplay, game over at `>= MAX_FUSES`) and the wire schema stay exactly as they are.
Output: `MAX_FUSES` exported from `@games/rules`, a `fusesRemainingForView` helper in the board-logic module (which already hosts view-derived values like `bandForView`), the board rendering it, and unit regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-UAT.md
@packages/rules/src/index.ts
@apps/web/lib/hanabi-board-logic.ts
@apps/web/lib/hanabi-board-logic.test.ts

<interfaces>
From packages/rules/src/hanabi/legality.ts (line 23, currently NOT re-exported by the barrel):
  export const MAX_FUSES = 3;
  export const MAX_CLUE_TOKENS  (also exists; do not touch)

From packages/rules/src/index.ts (current legality export line):
  export { canPlay, canDiscard, canClue, cardsTouchedByClue } from "./hanabi/legality";

From apps/web/lib/hanabi-board-logic.ts:
  import type { HanabiView, Clue, Suit } from "@games/rules";
  import { variantConfig, maxScoreFor, scoreBand } from "@games/rules";
  export function bandForView(view: HanabiView): string;   // pattern to follow

From apps/web/components/HanabiBoard.tsx:
  lines 9-15: import { bandForView, cluableColorsForView, isDiscardDisabled, isGiveClueDisabled, isPlayDisabled } from "../lib/hanabi-board-logic";
  lines 199-201: <p data-testid="fuse-tokens" ...>{game.fuses} fuses left</p>

From apps/web/lib/hanabi-board-logic.test.ts:
  function baseView(overrides: Partial<HanabiView> = {}): HanabiView   // fuses: 0 by default

No e2e spec reads `fuse-tokens` or "fuses left" (grep confirmed) — no Playwright changes needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export MAX_FUSES and add fusesRemainingForView with regression tests</name>
  <files>packages/rules/src/index.ts, apps/web/lib/hanabi-board-logic.ts, apps/web/lib/hanabi-board-logic.test.ts</files>
  <behavior>
    - fusesRemainingForView(baseView()) === 3 (fresh game: fuses used 0)
    - fusesRemainingForView(baseView({ fuses: 1 })) === 2 (one misplay)
    - fusesRemainingForView(baseView({ fuses: 3 })) === 0 (game over from fuses)
    - fusesRemainingForView(baseView({ fuses: MAX_FUSES })) === 0, with MAX_FUSES imported from "@games/rules" (proves the barrel export)
  </behavior>
  <action>
    RED: in apps/web/lib/hanabi-board-logic.test.ts add `fusesRemainingForView` to the import from "./hanabi-board-logic", add `MAX_FUSES` as a value import from "@games/rules", and add a `describe("fusesRemainingForView", ...)` block with the four behavior cases above using the existing `baseView` helper. Run the web project tests and confirm they fail.

    GREEN: in packages/rules/src/index.ts add `MAX_FUSES` to the existing legality re-export line (keep the others). In apps/web/lib/hanabi-board-logic.ts add `MAX_FUSES` to the existing value import from "@games/rules" and export `fusesRemainingForView(view: HanabiView): number` returning `MAX_FUSES - view.fuses` (do not clamp or hardcode 3; the engine guarantees fuses never exceeds MAX_FUSES). Give it a short doc comment stating that `view.fuses` counts fuses USED on the wire and this converts it to remaining for display only. Do NOT add it to the D-12 disabling-predicate list or alter that comment's "four predicates" claim beyond noting this is a display derivation like bandForView. Do NOT change HanabiState/HanabiView/schema `fuses` semantics.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-board-logic && npx vitest run --project rules</automated>
  </verify>
  <done>New fusesRemainingForView tests pass (3/2/0/0); rules suite still green; MAX_FUSES importable from @games/rules.</done>
</task>

<task type="auto">
  <name>Task 2: Render remaining fuses on the board and run the gate</name>
  <files>apps/web/components/HanabiBoard.tsx</files>
  <action>
    In apps/web/components/HanabiBoard.tsx add `fusesRemainingForView` to the existing import block from "../lib/hanabi-board-logic" (lines 9-15), and change the fuse-tokens paragraph text from `{game.fuses} fuses left` to `{fusesRemainingForView(game)} fuses left`. Keep `data-testid="fuse-tokens"`, classes, and styles unchanged. No other edits.

    Then run the typecheck and the full unit suite. When committing, stage ONLY the four files in this plan's files_modified (plus the SUMMARY) by explicit path — do NOT stage .planning/config.json, apps/web/next-env.d.ts, or any tsconfig.tsbuildinfo files, which carry unrelated working-tree changes.
  </action>
  <verify>
    <automated>cd /home/rflor/games && grep -n "fusesRemainingForView(game)} fuses left" apps/web/components/HanabiBoard.tsx && ! grep -n "{game.fuses} fuses left" apps/web/components/HanabiBoard.tsx && npx tsc -b && npx vitest run</automated>
  </verify>
  <done>Board shows MAX_FUSES - fuses under "fuses left"; tsc -b clean; full vitest suite green; unrelated working-tree files left unstaged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server view -> client render | Client renders an already-redacted, schema-validated HanabiView; no new data crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | HanabiBoard fuse display | accept | Display-only arithmetic on a public counter already in every seat's view; no new fields, no redaction change |
| T-04-09-02 | Tampering | fusesRemainingForView | accept | Client value is cosmetic; server remains authoritative for game over at MAX_FUSES |
</threat_model>

<verification>
- npx vitest run --project web hanabi-board-logic passes including new fusesRemainingForView cases
- npx tsc -b clean; npx vitest run green
- Manual re-check of UAT test 10 (optional): fresh game shows "3 fuses left", misplay shows "2 fuses left"
</verification>

<success_criteria>
- UAT gap "fuse counter reads as fuses remaining" closed: starts at 3, counts down, 0 at fuse game over
- Engine/wire `fuses` semantics untouched
- Regression tests guard the display value
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-09-SUMMARY.md` when done
</output>
