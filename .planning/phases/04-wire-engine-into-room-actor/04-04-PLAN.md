---
phase: 04-wire-engine-into-room-actor
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - apps/worker/src/room-state.ts
  - apps/worker/src/room-do.ts
  - apps/worker/src/room-state.test.ts
autonomous: true
requirements: [RT-09]

must_haves:
  truths:
    - "D-08: a game_action whose actionId matches that seat's last applied id is not re-applied; the server re-sends that seat's current view instead of an error, so a retry after a dropped response looks like success to the player."
    - "D-08: the last applied actionId is recorded in PERSISTED room state, not in memory, because Durable Object hibernation wipes memory and a retry commonly arrives after exactly that."
    - "D-09: the dedup check runs unconditionally before adapter.applyAction for EVERY action type, because a repeated clue is perfectly legal and would otherwise spend a second token."
    - "D-10: mapAdapterError maps the adapter's 8 typed errors 1:1 onto the closed ErrorDetail enum, and the error frame still carries no game state."
    - "D-07: actionId never reaches the adapter — applyGameAction passes `request` through unchanged."
  artifacts:
    - path: "apps/worker/src/room-state.ts"
      provides: "applyGameAction dedup branch + mapAdapterError(error)"
      contains: "lastAppliedActionId"
    - path: "apps/worker/src/room-do.ts"
      provides: "actionId pass-through and detail on the error frame"
      contains: "msg.actionId"
  key_links:
    - from: "apps/worker/src/room-do.ts"
      to: "apps/worker/src/room-state.ts"
      via: "game_action branch passes msg.actionId into applyGameAction"
      pattern: "applyGameAction\\(room, actorSeatId, msg\\.actionId"
    - from: "apps/worker/src/room-state.ts"
      to: "packages/schema/src/room.ts"
      via: "records lastAppliedActionId on the actor's persisted Seat"
      pattern: "lastAppliedActionId"
---

<objective>
Make a duplicate action apply exactly once (RT-09), and give a refused action a reason the player could act on (D-10).

Purpose: a repeated play or discard is naturally rejected because the card has left the hand, but a repeated CLUE is perfectly legal and would spend a second token. Natural rejection is therefore not sufficient, and the check must be unconditional and run before the adapter is ever called.
Output: the dedup branch and error mapping in the pure room state machine, a thin pass-through in the Durable Object, and unit coverage for both.

The Durable Object stays thin glue: the dedup DECISION lives in `room-state.ts`, not `room-do.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/04-wire-engine-into-room-actor/04-PATTERNS.md

<interfaces>
Current shapes being changed (read them in place, do not work from this summary):

  apps/worker/src/room-state.ts:287-293
    function mapAdapterError(): RefusalReason { return "bad_request"; }   // parameterless today
  apps/worker/src/room-state.ts:298-325
    export function applyGameAction(state, actorSeatId, request, now): RoomResult
  apps/worker/src/room-state.ts:40-42
    export type RoomResult = { ok: true; state } | { ok: false; reason: RefusalReason }
  apps/worker/src/room-do.ts:179-188
    the `game_action` dispatch branch

Immutable per-seat update idiom already established in this same file
(markConnected at lines 192-205, transferHost at 211-223): map over
`state.seats`, returning `{ ...seat, field: value }` for the matching seatId.
Use it — do not introduce a different mutation style.

From plan 04-02: `ErrorDetail` (9-member closed enum) exported from @games/schema,
and `Seat.lastAppliedActionId?: string | null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map the adapter's typed errors onto the closed detail enum</name>
  <read_first>
    - apps/worker/src/room-state.ts (the full file — `mapAdapterError`, its docstring, and its single call site inside `applyGameAction`)
    - packages/rules/src/adapter.ts (the 8-member `AdapterError` union)
    - packages/schema/src/messages.ts (the widened `ErrorDetailSchema` / `ErrorDetail` type from plan 04-02)
  </read_first>
  <files>apps/worker/src/room-state.ts</files>
  <action>
Change `mapAdapterError` from parameterless to `function mapAdapterError(error: AdapterError): ErrorDetail`, importing the `AdapterError` type from `@games/rules` and the `ErrorDetail` type from `@games/schema`. Implement it as an exhaustive `switch` over all 8 members returning the identically-named `ErrorDetail` member, with a `default` branch that assigns to a `never`-typed local so a future widening of `AdapterError` becomes a compile error rather than a silent fallthrough — the same exhaustiveness idiom `variantConfig` uses in `packages/rules/src/hanabi/variant.ts`.

Widen `RoomResult`'s failure branch to `{ ok: false; reason: RefusalReason; detail?: ErrorDetail }`. Leave the success branch untouched. Every existing `{ ok: false, reason: ... }` return stays valid because `detail` is optional.

Replace the existing docstring on `mapAdapterError` (which currently explains the collapse onto `bad_request`) with the D-10 rationale: the wire `code` deliberately stays `bad_request` because `RefusalReasonSchema` is shared with `refused` frames and must not be widened for game-rule reasons; the specific reason rides in the closed `ErrorDetail` vocabulary instead; and the mapping is 1:1 so no refusal is lossy. State explicitly that this function must never return free text or interpolate any game value — that is the Phase 2 D-08 boundary.

Note that `AdapterError` is the GENERIC adapter vocabulary from `packages/rules/src/adapter.ts`, not a Hanabi-specific type, so referencing it here does not teach `room-state.ts` the game's name (D-04 stays intact).

Update the single call site inside `applyGameAction` to `return { ok: false, reason: "bad_request", detail: mapAdapterError(result.error) }`.
  </action>
  <acceptance_criteria>
    - `grep -c 'mapAdapterError(result.error)' apps/worker/src/room-state.ts` outputs `1`.
    - `mapAdapterError`'s signature takes one `AdapterError` parameter and returns `ErrorDetail`.
    - All 8 `AdapterError` member names appear in `apps/worker/src/room-state.ts`.
    - The `switch` has a `default` branch performing a `never` exhaustiveness assignment.
    - `grep -rn 'hanabi\|Hanabi' apps/worker/src/room-state.ts` outputs nothing (D-04 intact).
    - `npx tsc -b` exits 0, confirming no other call site of the old zero-argument signature was missed.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project worker room-state</automated>
  </verify>
  <done>Every adapter refusal reaches the wire as a specific, closed-vocabulary detail with no game state attached.</done>
</task>

<task type="auto">
  <name>Task 2: Dedup a repeated actionId before the adapter is ever called</name>
  <read_first>
    - apps/worker/src/room-state.ts (applyGameAction, and the markConnected/transferHost immutable `.map()` idiom to copy)
    - apps/worker/src/room-do.ts (the `game_action` branch at lines 179-188, the `#commit`/`#pushState` pair, and the `#send` chokepoint docstring)
    - packages/schema/src/room.ts (the `lastAppliedActionId` field added in plan 04-02)
    - packages/rules/src/hanabi/actions.ts (the exact-own-key guards — confirmation that `request` must be passed through untouched)
  </read_first>
  <files>apps/worker/src/room-state.ts, apps/worker/src/room-do.ts</files>
  <action>
Change `applyGameAction`'s signature to `applyGameAction(state, actorSeatId, actionId: string, request: unknown, now: number)`, inserting `actionId` before `request`.

Immediately after the existing `status !== "in_progress"` guard and BEFORE any adapter call, add the dedup branch: find the actor's seat in `state.seats`; if that seat's `lastAppliedActionId` equals `actionId`, return `{ ok: true, state }` with the state unchanged. Comment it with D-08's reasoning: this is not an error — the caller commits and pushes, so the retrying client receives that seat's current view and the retry looks like success, which is exactly what a retry after a dropped response should look like.

Comment the PLACEMENT explicitly (D-09, RESEARCH.md Pitfall 2): the check runs unconditionally, for every action type, before `adapter.applyAction`, because a repeated play or discard would be naturally rejected once the card has left the hand, but a repeated clue is perfectly legal and would spend a second token. Relying on the engine's accidental idempotence would therefore cover two of the three action types and silently miss the one that matters.

On the success path, record the id: build the next `seats` array with the established `.map()` idiom, setting `lastAppliedActionId: actionId` on the actor's seat only, and include that `seats` array in the returned state alongside the existing `status`/`game`/`lastActivityAt` updates.

Pass `request` into `adapter.applyAction` completely unchanged — never spread `actionId` into it (RESEARCH.md Pitfall 3; the adapter's exact-own-key guards would reject it anyway, but the boundary must hold at this layer too).

In `room-do.ts`'s `game_action` branch, pass `msg.actionId` through to `applyGameAction`, and extend the existing failure send to include the detail: `this.#send(connection, { type: "error", code: result.reason, detail: result.detail })`. Do not add a new `#send` call site, do not add a second `type: "state"` or `type: "joined"` literal, and do not put any dedup logic in this file — the structural audit asserts exact counts on all of those.
  </action>
  <acceptance_criteria>
    - `applyGameAction`'s signature is `(state, actorSeatId, actionId, request, now)` with `actionId` before `request`.
    - The dedup comparison appears textually BEFORE the `adapter.applyAction(` call in the file (verify by line numbers from `grep -n 'lastAppliedActionId === actionId' -n apps/worker/src/room-state.ts` and `grep -n 'adapter.applyAction(' apps/worker/src/room-state.ts`).
    - `grep -c 'msg.actionId' apps/worker/src/room-do.ts` outputs `1`.
    - `grep -c 'actionId' apps/worker/src/room-do.ts` outputs exactly `1` — no dedup logic leaked into the Durable Object.
    - The `adapter.applyAction` call still receives the bare `request` identifier, not a spread or a rebuilt object.
    - `npx vitest run --project worker source-structure` still passes with all exact chokepoint counts unchanged.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project worker source-structure</automated>
  </verify>
  <done>A duplicate actionId short-circuits to a re-sent view before the engine is consulted, and the applied id is persisted on the actor's seat.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-prove dedup, persistence of the key, and the error mapping</name>
  <read_first>
    - apps/worker/src/room-state.test.ts (the existing `applyGameAction` tests and fixture builders repointed to Hanabi in plan 04-03)
    - apps/worker/src/room-state.ts (the functions under test)
    - packages/rules/src/hanabi/legality.ts (to construct actions that are legal, and ones that are refused for a known typed reason)
  </read_first>
  <files>apps/worker/src/room-state.test.ts</files>
  <action>
Update every existing `applyGameAction` call site in this file for the new five-parameter signature, passing a distinct `actionId` per logical intent.

Add tests proving: applying a legal CLUE decrements `clueTokens` by exactly 1 and appends to `history`; re-applying the SAME `actionId` for that seat returns `ok: true` with `clueTokens`, `history.length`, and `turnIndex` all unchanged — assert on a clue specifically, and name the test after D-09 so the reason is visible at the call site; applying a DIFFERENT `actionId` afterwards is applied normally (proving dedup is keyed on the id and not simply freezing the seat); the actor's seat carries `lastAppliedActionId` equal to the applied id after a successful action, and no OTHER seat's `lastAppliedActionId` is set; a duplicate `actionId` presented by a DIFFERENT seat is applied normally, because the key is per-seat; and that the returned state from the dedup branch is still `in_progress` and structurally unchanged.

Add tests for the error mapping: an out-of-turn action returns `ok: false` with `reason: "bad_request"` and `detail: "not_your_turn"`; a malformed payload (an object with an extra asserted key such as a resulting score) returns `detail: "invalid_action"`; a discard at 8 clue tokens returns `detail: "discard_at_max_clues"`; and a clue that touches zero cards returns `detail: "clue_touches_nothing"`. Assert each `detail` is one of the closed enum's members, never a free-text string.

Add one test that a duplicate `actionId` is still deduped when the room state is round-tripped through `RoomStateSchema.parse` first — this is the persisted-shape proof that the key survives serialization, which is what makes the hibernation case work.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project worker room-state` is green.
    - At least one test name references D-09 and double-sends a CLUE specifically.
    - A test asserts `clueTokens` is unchanged after the duplicate send (not merely that the call returned ok).
    - A test asserts a second, different `actionId` IS applied after a deduped one.
    - Four separate tests assert `detail` equals `not_your_turn`, `invalid_action`, `discard_at_max_clues`, and `clue_touches_nothing` respectively.
    - Fault injection: temporarily move the dedup check to AFTER the `adapter.applyAction` call; the duplicate-clue test must FAIL. Revert after confirming.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project worker room-state</automated>
  </verify>
  <done>Dedup is proven at the unit level for the action type that breaks naive idempotency, and every typed refusal reaches the wire as a closed-enum detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `game_action` dedup path | A client-minted `actionId` decides whether an action is skipped |
| server -> `error` frame `detail` | A widened vocabulary must not become a state channel |
| adapter request boundary | `actionId` must never cross into game logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering / Repudiation | replayed `game_action` frame | mitigate | Per-seat `lastAppliedActionId` compared before `adapter.applyAction`, unconditionally for all three action types (D-08/D-09) |
| T-04-16 | Tampering | dedup keyed globally instead of per seat | mitigate | The key is stored on the actor's Seat record and compared only against that seat; a unit test asserts another seat presenting the same id is applied normally |
| T-04-17 | Tampering | `actionId` becoming a state-assertion channel | mitigate | `actionId` is a separate parameter and never enters `request`; the adapter's exact-own-key guards reject any extra key as defense in depth (Pitfall 3) |
| T-04-18 | Information Disclosure | error frames carrying game state | mitigate | `detail` is the closed `ErrorDetail` enum; `mapAdapterError` is an exhaustive switch over literals with no interpolation, asserted by unit tests |
| T-04-19 | Denial of Service | dedup key lost on hibernation causing double-spend | mitigate | The key lives on the PERSISTED Seat, written through `#commit` -> `saveRoom` like every other seat field; proven across a real eviction in plan `04-05` |
| T-04-20 | Elevation of Privilege | dedup logic drifting into the DO | mitigate | Decision stays in `room-state.ts`; an acceptance criterion caps `actionId` occurrences in `room-do.ts` at exactly 1 |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc -b` clean (catches any missed `mapAdapterError`/`applyGameAction` call site).
- `npx vitest run --project worker` green, including the unchanged structural audit.
- The dedup check precedes `adapter.applyAction` by line number.
</verification>

<success_criteria>
- A repeated `actionId` from the same seat never reaches the engine and never spends a second clue token.
- The applied id is persisted on the seat record and survives `RoomStateSchema` round-tripping.
- Every adapter refusal maps 1:1 to a closed-enum `detail` with no free text.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-04-SUMMARY.md` when done.
</output>
