---
phase: 04-wire-engine-into-room-actor
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/worker/src/game-registration.ts
  - apps/worker/src/room-state.test.ts
  - apps/worker/src/redaction-wire.test.ts
  - apps/worker/src/seat-projection.test.ts
  - apps/worker/src/room-do.test.ts
  - apps/worker/src/source-structure.test.ts
autonomous: true
requirements: [RT-01]

must_haves:
  truths:
    - "D-04: the toy-to-Hanabi swap happens in apps/worker/src/game-registration.ts and nowhere else in the worker — room-state.ts, seat-projection.ts and room-do.ts never learn the game's name."
    - "D-04: the compile-time assignability assertions survive the swap, repointed to HanabiView / HanabiViewWire, so schema-vs-engine drift is a build failure."
    - "D-02: the wire-level leak proof is repointed to checkHanabiViewForLeaks / secretsForHanabiSeat in the same change as the swap — the suite is never left without wire-level redaction evidence."
    - "D-02: EVERY test the swap breaks is repaired in this same plan — CR-03 and the seat-projection own-hand assertion (Task 2), and the whole layer-3 frame-capture region (Task 4). The plan closes on an unfiltered `npm test`, so the suite is never left red across a plan boundary."
    - "D-07/HIDE-03: the fail-closed gate's leak fixtures are Hanabi-shaped, so each rejection test proves the specific leak is caught rather than passing vacuously because the fixture is the wrong game's shape entirely — guarded by a positive control asserting the unleaked fixture validates."
    - "D-03: source-structure.test.ts keeps its exact chokepoint counts and its game-naming confinement check now confines hanabiGame to game-registration.ts, rewritten rather than relaxed to a vacuous zero-match assertion."
  artifacts:
    - path: "apps/worker/src/game-registration.ts"
      provides: "activeGame bound to hanabiGame + HanabiViewSchema; ActiveGameState = HanabiState"
      contains: "hanabiGame"
    - path: "apps/worker/src/redaction-wire.test.ts"
      provides: "Hanabi wire-level property + canary proof"
      contains: "checkHanabiViewForLeaks"
    - path: "apps/worker/src/seat-projection.test.ts"
      provides: "Hanabi own-hand key assertion + Hanabi-shaped fail-closed fixtures with a positive control"
      contains: "yourHand"
    - path: "apps/worker/src/room-do.test.ts"
      provides: "CR-03 repointed to a legal Hanabi clue; layer-3 frame capture rebuilt against HanabiSeatSecrets"
      contains: "checkHanabiViewForLeaks"
  key_links:
    - from: "apps/worker/src/game-registration.ts"
      to: "packages/rules/src/hanabi/adapter.ts"
      via: "activeGame.adapter = hanabiGame"
      pattern: "hanabiGame"
    - from: "apps/worker/src/game-registration.ts"
      to: "packages/schema/src/games/hanabi.ts"
      via: "activeGame.viewSchema = HanabiViewSchema (run by seat-projection's fail-closed gate)"
      pattern: "HanabiViewSchema"
---

<objective>
Point the worker at the real Hanabi engine through the single registration seam Phase 2 built (D-04), and repoint EVERY test the swap breaks in the same plan (D-02).

Purpose: this is the phase's central task. If it is done correctly, the worker diff is small and boring — a registration file and a handful of test files — which is exactly the measure of whether Phase 2's seam was worth building.
Output: a worker running the real engine, with its wire-level redaction proof, its fail-closed gate fixtures, its CR-03 turn-order proof, its layer-3 live-frame capture, and its structural confinement audit all repointed to Hanabi.

This plan has four tasks rather than the usual two or three, deliberately. Tasks 1 and 3 are small (a two-import swap; an A9 rewrite). Tasks 2 and 4 are the substance, and they are here — rather than deferred to a later wave — because the swap in Task 1 breaks them: a test that a change breaks is repaired by the change that breaks it, never left red across a plan boundary. A documented "expected failure" window is exactly the condition under which a second, genuine failure hides behind the first.

If you find yourself editing `room-state.ts`, `room-do.ts` or `seat-projection.ts` to teach them about Hanabi, stop — that is out of scope and means the seam is being eroded. Note the distinction: `seat-projection.ts` (production) must NOT change; `seat-projection.test.ts` (its test) must be repointed, because its fixtures hardcode the toy's view shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/04-wire-engine-into-room-actor/04-PATTERNS.md
@.planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md

<interfaces>
Already exported from `packages/rules/src/index.ts:32-39` — no rules-side change needed:
  hanabiGame, canPlay, canDiscard, canClue, cardsTouchedByClue,
  currentScore, scoreBand, checkHanabiViewForLeaks, secretsForHanabiSeat,
  variantConfig, handSizeFor, maxScoreFor, ALL_SUITS, RANKS
  type HanabiState, HanabiView, HanabiCardView, HanabiSeatSecrets, Suit, Rank

From plan 04-01, at `@games/schema/games/hanabi`:
  HanabiViewSchema, type HanabiViewWire, HANABI_GAME_ID

Leak-checker signatures (packages/rules/src/hanabi/hanabi-leak-check.ts):
  secretsForHanabiSeat(state: HanabiState, seatId: string, seed?: string): HanabiSeatSecrets
  checkHanabiViewForLeaks({ view: unknown; serialized: string; secrets }): string[]
  Structural reason strings it can return include:
    "structural:your-hand-entry-has-identity"
    "structural:hidden-card-has-identity"
    "structural:own-card-id-has-identity"

Hanabi action request shapes (exact-own-key guarded, packages/rules/src/hanabi/actions.ts):
  { type: "play", cardId }
  { type: "discard", cardId }
  { type: "clue", targetSeatId, clue: { type: "color" | "rank", value } }

Wire-visible HanabiView fields (packages/rules/src/hanabi/state.ts:103-118) — these are
what a test holding only WebSocket frames may assert on:
  variant, yourSeatId, yourHand[], otherHands[{ seatId, cards[] }], stacks[],
  discard[], clueTokens, fuses, deckCount, finalTurnsRemaining, activeSeatId,
  isYourTurn, score, history[]
Note `history` IS on the wire and IS populated by `toHanabiPlayerView`; there is no
`revealed` field anywhere in `HanabiView` — that was the toy's shape.

HanabiCardView (packages/rules/src/hanabi/state.ts:54-63) — the own-hand boundary:
  hidden:  { id: string; hidden: true;  facts: ClueFactsView }
  visible: { id: string; hidden: false; suit: Suit; rank: Rank; facts: ClueFactsView }
  ClueFactsView = { possibleSuits[], possibleRanks[], positiveClues[], negativeClues[] }
So a hidden own-hand card's sorted keys are exactly ["facts","hidden","id"].

The fail-closed gate under test (apps/worker/src/seat-projection.ts — production, DO NOT
change): `validateGameView(view)` returns `view` unchanged when `view.game === null`,
otherwise runs `activeGame.viewSchema.safeParse(view.game)` and returns `null` on failure,
logging only `seatId` plus each issue's `code`/`path` — never the view, game, messages or
received values. After Task 1 that schema is `HanabiViewSchema`, which is why this file's
toy-shaped fixtures must be repointed.

Harness helpers already defined in apps/worker/src/room-do.test.ts (Tasks 2 and 4 reuse,
never reinvent):
  spawnWrangler(), waitForReady(), killAndWait(proc), openSocket(code),
  collectMessages(ws) -> { raw, parsed, waitFor(predicate, timeoutMs), waitForClose },
  send(ws, msg), mintRoomCode()
Its comment at lines 112-119 warns frames arrive in tight bursts, so a test MUST
search by predicate rather than assuming "the Nth" or "the most recent" message.

Plan 04-02 Task 3 has ALREADY added an `actionId` literal to each of the three existing
`game_action` sends in room-do.test.ts (roughly lines 418, 573, 582), because `actionId`
became a required field on the wire at wave 2. Keep those literals; do not mint new ones.

Plan 04-01 Task 2 creates a `baseValidView` fixture in
`packages/schema/src/games/hanabi.test.ts` — a complete realistic Hanabi view (2 seats,
one hidden own card with facts, one visible other-hand card, one stack, one discard entry,
one of each history branch). Use its SHAPE as the template for this plan's fail-closed
fixtures rather than inventing one; do not import across package test boundaries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the registration point to the Hanabi engine</name>
  <read_first>
    - apps/worker/src/game-registration.ts (the full file — the header comment promising this exact swap, the `activeGame` literal, and both compile-time assertions)
    - packages/rules/src/index.ts (confirm the Hanabi export names)
    - packages/rules/src/hanabi/state.ts (HanabiState / HanabiView)
    - packages/schema/src/games/hanabi.ts (HanabiViewSchema / HanabiViewWire / HANABI_GAME_ID from plan 04-01)
    - apps/worker/src/room-state.ts (confirm, do not change: `adapter` is reached only via `activeGame.adapter`, and `startGame` already passes `{seatIds, variant, seed}`)
  </read_first>
  <files>apps/worker/src/game-registration.ts</files>
  <action>
Replace the four toy imports with their Hanabi equivalents: `hanabiGame` and types `HanabiState`/`HanabiView` from `@games/rules`, and `HANABI_GAME_ID`/`HanabiViewSchema` plus type `HanabiViewWire` from `@games/schema/games/hanabi`. Set `activeGame.adapter` to `hanabiGame`, `activeGame.viewSchema` to `HanabiViewSchema`, `activeGame.gameId` to `HANABI_GAME_ID`, and change `export type ActiveGameState = HanabiState`.

Keep BOTH compile-time assertions, repointed: `_AssertViewAssignable` becomes `[HanabiView] extends [HanabiViewWire] ? true : never`, and `_AssertKeysMutuallyAssignable` becomes the mutual `keyof` check between `HanabiView` and `HanabiViewWire`. Keep the one-element-tuple wrapping (it prevents conditional-type distribution over unions) and the existing eslint-disable comments.

Update the file header: it currently says Phase 4 swaps this file's two imports "and nothing else in the worker" — restate that as completed, and keep the standing rule that this is the ONLY non-test worker file permitted to name a specific game. Change the `activeGame.adapter` description to reference `GameAdapter<HanabiState, HanabiAction>`.

Change NOTHING in `room-state.ts`, `seat-projection.ts`, or `room-do.ts`. If `tsc` reports an error in one of those files after the swap, that is a signal the seam is leaking — report it rather than editing those files to accommodate Hanabi.

Note on `activeGame.gameId`: nothing currently reads it (confirmed — `room-state.ts` uses `adapter.id` directly for `adapterId`). Keep the field for symmetry with the toy's shape, but do not add a consumer for it.

Expect the worker test suite to be red immediately after this task — that is the point of Tasks 2 and 4, which repair every casualty before this plan closes. Do not attempt to fix test files here.
  </action>
  <acceptance_criteria>
    - `grep -c 'hanabiGame' apps/worker/src/game-registration.ts` outputs at least `2`.
    - `grep -rc 'forehead\|Forehead\|FOREHEAD' apps/worker/src/game-registration.ts` outputs `0`.
    - `grep -c 'ActiveGameState = HanabiState' apps/worker/src/game-registration.ts` outputs `1`.
    - Both `_AssertViewAssignable` and `_AssertKeysMutuallyAssignable` are still present and reference `HanabiView` and `HanabiViewWire`.
    - `git diff --name-only` after this task lists ONLY `apps/worker/src/game-registration.ts`.
    - `npx tsc -b` exits 0 — which is itself the proof that `HanabiView` is assignable to the strict schema's inferred type.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project worker source-structure</automated>
  </verify>
  <done>The worker runs the real Hanabi engine, reached only through `activeGame`, with drift protection intact.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the worker's leak, room-state, seat-projection, and CR-03 coverage to Hanabi</name>
  <read_first>
    - apps/worker/src/redaction-wire.test.ts (the full file — `buildStartedRoom`, `assertNoWireLeaksForEveryState`, the fast-check property, and the wire-canary test at the end, which is the most important structure to preserve)
    - apps/worker/src/room-state.test.ts (the game-action tests at roughly lines 430-490 and the `adapterId` assertion at line 485)
    - apps/worker/src/seat-projection.test.ts (the full file — the `yourCard` key assertion at lines 79-80, the `baseGame()` fixture at 108-118, the five fail-closed mutation tests at 135-166, and the console.error test at 177-210)
    - apps/worker/src/seat-projection.ts (READ ONLY — `validateGameView`'s mechanism; do not modify this file)
    - apps/worker/src/room-do.test.ts (the CR-03 test at roughly lines 396-429, the harness helpers at the top of the file, and the burst-arrival warning at lines 112-119)
    - packages/rules/src/hanabi/hanabi-leak-check.ts (the checker being adopted and the reason strings it returns)
    - packages/rules/src/hanabi/legality.ts (canPlay/canDiscard/canClue, for generating actions that are actually legal)
    - packages/rules/src/hanabi/state.ts (HanabiState field names needed to drive a game from the server side, plus HanabiCardView and the HanabiView field list)
  </read_first>
  <files>apps/worker/src/redaction-wire.test.ts, apps/worker/src/room-state.test.ts, apps/worker/src/seat-projection.test.ts, apps/worker/src/room-do.test.ts</files>
  <action>
In `redaction-wire.test.ts`, replace the `checkSeatViewForLeaks`/`FOREHEAD_CARD_VALUES`/`secretsForSeat` import with `checkHanabiViewForLeaks`/`secretsForHanabiSeat` and the `HanabiSeatSecrets` type. Keep `buildStartedRoom`, `assertNoWireLeaksForEveryState`, the `state`-frame plus `joined`-frame coverage, the parsed-object AND raw-string checks, and the fast-check property over 2-5 seats with a random seed — only the action generation and the checker calls change.

Add a local helper that derives ONE legal Hanabi action from the server-side `HanabiState` for the active seat, in this priority order: if `clueTokens > 0` and some other seat holds at least one card, return a `clue` naming that card's rank targeted at that seat (guaranteed to touch at least one card, so it cannot be refused for touching nothing); else if `clueTokens < 8`, return a `discard` of the actor's first card; else return a `play` of the actor's first card. Drive the property test's action loop through this helper instead of the toy's guess payloads, and keep advancing only when `applyGameAction` succeeds. Keep the "played to `ended`" test, driving it with the same helper until `room.status === "ended"`, and keep an explicit non-vacuousness assertion that at least one action was actually applied.

Adapt the wire canary test, preserving its structure exactly: build a clean view, then force a leak by taking the seat's own first hidden card and giving it the real `suit` and `rank` from `room.game`'s own hand while leaving `hidden: true`. Assert `validateGameView` returns `null`; assert `encodeServerMessage` still produces a string (proving `RoomViewSchema.game` being `z.unknown()` would NOT have blocked it); and assert `checkHanabiViewForLeaks` reports a structural reason — expect the returned array to contain `"structural:hidden-card-has-identity"`.

In `room-state.test.ts`, change the `adapterId` assertion at line 485 from `"forehead-card"` to `"hanabi"`, and repoint any test that drives `applyGameAction` with a toy `{type:"guess"}` payload to a legal Hanabi action using the same priority logic. Where a test asserts on toy view fields (`revealed`, `ForeheadCardView`), assert on the Hanabi equivalents instead (`history`, `clueTokens`, `isYourTurn`, `yourHand`). Note that `applyGameAction` still has its pre-plan-`04-04` signature at this point — do not add an `actionId` parameter here; plan `04-04` changes the signature and updates these call sites with it.

In `seat-projection.test.ts`, repoint the whole file to the Hanabi view shape. This file is a hard casualty of Task 1, not a soft one: at lines 79-80 it does `const game = projected?.game as { yourCard: ... }` then `Object.keys(game.yourCard).sort()`, and post-swap `game.yourCard` is `undefined`, so it throws a TypeError. Change `seat-projection.ts` NOT AT ALL — only its test.

  - Replace the own-hand assertion (lines 76-81) with the stronger Hanabi equivalent: for each seat, iterate every entry of `game.yourHand` and assert its sorted keys are exactly `["facts","hidden","id"]`, that `hidden` is `true`, and that neither `"suit" in card` nor `"rank" in card` holds. Add a non-vacuousness assertion that `yourHand` is non-empty (a 2-seat base game deals 5 cards per seat), so an empty array cannot make the loop pass trivially. Update the test name at line 62, which currently describes `yourCard` sorted keys.
  - Update the test name at line 87, which also names `yourCard` — it describes the "accepts a real clean projection" case, whose body needs no change since it builds its view from `toSeatView` on real state.
  - Repoint the `baseGame()` fixture (108-118) from the toy's `yourCard`/`otherCards`/`revealed`/`deckCount`/`activeSeatId`/`isYourTurn`/`score` to a complete valid Hanabi view, using the shape of plan `04-01`'s `baseValidView` as the template: `variant`, `yourSeatId`, `yourHand` (one hidden card with `facts`), `otherHands` (one seat with one visible card carrying `suit`/`rank`/`facts`), `stacks`, `discard`, `clueTokens`, `fuses`, `deckCount`, `finalTurnsRemaining`, `activeSeatId`, `isYourTurn`, `score`, `history`.
  - CRITICAL — add a positive control as the first test in the fail-closed describe block: assert `validateGameView(leakyView(baseGame()))` returns NON-null. Without it every rejection test below would pass for the wrong reason after the swap (the whole fixture would fail `HanabiViewSchema` because it is the wrong game's shape entirely, not because of the specific leak under test). That vacuity is the actual risk in this file, and the positive control is what forecloses it. Comment it as such.
  - Repoint the five mutation tests (135-166) to Hanabi leaks, keeping one `it` per mutation and keeping each name describing the specific leak: a hidden own-hand card with `suit: undefined` present; with `suit: null`; with the real `suit` and `rank` actually present; an extra top-level game key (`seed`); and an `otherHands` card with `hidden: true` but a `suit` key present. Keep the "never returns the input object — result is strictly null" test, repointed to a Hanabi leak.
  - Repoint the console.error test (177-210): build the leaked view as a hidden own-hand card carrying a real `suit`/`rank`, and keep every assertion — logs exactly once, first argument contains `"HIDE-03"`, and the serialized call arguments contain neither the leaked identity value nor the room seed. `validateGameView` logs only issue `code`/`path`, so this must still hold.

In `room-do.test.ts`, repoint the CR-03 test — `"CR-03: a leave sent mid-game is refused and the seat stays in turn order"`, currently at roughly lines 396-429. This test is a direct casualty of Task 1's swap, broken in two ways: its send at line 418 carries a toy `{ type: "guess", value: "Altair" }` request, which Hanabi's exact-own-key request guards refuse as `invalid_action`; and it then waits on `view.game.revealed.length === 1`, a field that does not exist anywhere in `HanabiView`. After the swap that predicate can never be satisfied and the test times out.

Preserve exactly what the test was written to prove — do not weaken it: the mid-game `leave` is still refused with `code: "bad_request"`, the post-action view still has 2 seats, and it is still Bob's turn afterwards. Change ONLY Alice's action and the predicate that waits for it.

Derive Alice's action from her own latest game-bearing frame in `c1.parsed` (searched by predicate, per the file's burst warning), not from server state — this test only ever sees wire frames. Take a rank that appears on one of Bob's visible cards in `otherHands[].cards[]` and send `{ type: "clue", targetSeatId: <Bob's seatId>, clue: { type: "rank", value: <that rank> } }`. That is guaranteed legal here: a rank actually present cannot be refused for touching nothing, and `clueTokens` is at its maximum at game start so it cannot be refused for lack of tokens. Keep the `actionId` literal plan `04-02` added to this send — do not drop it and do not mint a second one.

Replace the wait predicate with a Hanabi-shaped one: wait on `c2` for a `state` frame whose `view.game.history.length === 1`. Then assert `view.seats` has length 2 and `view.game.isYourTurn === true` on Bob's frame, exactly as the test does today. Update the test's local inline view type from `{ game: { revealed: unknown[] } | null }` to the Hanabi shape it now reads (`{ game: { history: unknown[]; isYourTurn: boolean } | null }`).

Scope note: leave the layer-3 HIDE-01/HIDE-04 frame-capture test (roughly lines 497-684) and the toy import at line 33 alone in THIS task — Task 4 of this same plan rebuilds that region wholesale, and keeping the two changes separate keeps each reviewable. The layer-3 test is red between Task 2 and Task 4; that is intra-plan ordering within a single commit sequence, not a red state shipped across a plan boundary. This is also why this task's verify filters `room-do` by test name while the plan's closing gate does not.
  </action>
  <acceptance_criteria>
    - `grep -c 'checkHanabiViewForLeaks' apps/worker/src/redaction-wire.test.ts` outputs at least `2`.
    - `grep -c 'FOREHEAD_CARD_VALUES\|checkSeatViewForLeaks\|secretsForSeat' apps/worker/src/redaction-wire.test.ts` outputs `0`.
    - The canary test asserts the leak reasons contain `"structural:hidden-card-has-identity"`.
    - `grep -c 'adapterId).toBe("hanabi")' apps/worker/src/room-state.test.ts` outputs `1`.
    - `grep -c 'yourCard\|otherCards\|revealed' apps/worker/src/seat-projection.test.ts` outputs `0`.
    - `grep -c 'Altair\|Sirius' apps/worker/src/seat-projection.test.ts` outputs `0` — the toy's card values are gone from the fixtures.
    - `seat-projection.test.ts` asserts every `yourHand` entry's sorted keys equal `["facts","hidden","id"]`, and asserts `yourHand.length` is greater than 0.
    - `seat-projection.test.ts` contains a positive-control test asserting the unleaked `baseGame()` fixture validates NON-null, and it is the first test in the fail-closed describe block.
    - Fault injection (non-vacuity): temporarily add an unknown key to the `baseGame()` fixture; the positive control MUST fail. Revert after confirming — this is what proves the five rejection tests are not passing for the wrong reason.
    - The console.error test still asserts exactly one call, `"HIDE-03"` in the first argument, and no leaked identity or seed in the serialized arguments.
    - The CR-03 test sends a `{ type: "clue", ... }` request and no longer contains the string `guess` or `Altair`; `grep -c 'Altair' apps/worker/src/room-do.test.ts` outputs `0`.
    - The CR-03 test's wait predicate references `history` and no longer references `revealed`; its `refusal.code === "bad_request"`, `seats` length 2, and `isYourTurn === true` assertions are all still present and unchanged in intent.
    - The CR-03 send still carries the `actionId` literal added by plan `04-02`.
    - `npx vitest run --project worker room-do -t "CR-03"` passes.
    - `git diff apps/worker/src/room-do.test.ts` for THIS task shows changes only inside the CR-03 test body.
    - `git diff --name-only` for this task does NOT list `apps/worker/src/seat-projection.ts` (production gate unchanged).
    - `npx vitest run --project worker redaction-wire room-state seat-projection` is green, and the fast-check property still runs 100 runs.
    - Fault injection: temporarily make `assertNoWireLeaksForEveryState` check a view built with the own hand's cards marked `hidden: false`; the property test must FAIL. Revert after confirming.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project worker redaction-wire room-state seat-projection && npx vitest run --project worker room-do -t "CR-03"</automated>
  </verify>
  <done>The wire-level redaction proof exercises the real Hanabi projection, the fail-closed gate's fixtures are Hanabi-shaped and provably non-vacuous, and CR-03 still proves a mid-game leave is refused and the seat keeps its place in turn order.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the structural game-naming confinement check for Hanabi</name>
  <read_first>
    - apps/worker/src/source-structure.test.ts (the full file — the A1-A10 assertions, the comment-stripping scanner, and especially A9 at lines 295-308)
    - apps/worker/src/game-registration.ts (the post-swap file A9 now confines)
  </read_first>
  <files>apps/worker/src/source-structure.test.ts</files>
  <action>
Rewrite the A9 assertion so it confines the ACTIVE game's identifier: change the search pattern from `/foreheadCardGame/g` to `/hanabiGame/g` and assert the hits are exactly `[{ file: "game-registration.ts", count: <actual> }]`, keeping the existing shape of the assertion and its diagnostic message. Keep the `@games\/schema\/games\//g` confinement assertion unchanged — that pattern is generic to the path prefix and now matches `@games/schema/games/hanabi` automatically.

Then add a SEPARATE assertion, with its own `it(...)` name referencing D-01/D-03, proving the toy identifier is gone rather than merely unmatched: assert `findFilesWithMatch(/foreheadCardGame/g)` returns an empty array across all scanned non-test worker sources. Add a comment explaining why these are two distinct claims: "confined to one file" and "appears nowhere" are different assertions, and silently relaxing A9 into the second would leave a test that passes vacuously against a deleted name while proving nothing about the new one.

Scope this zero-occurrence check to the IDENTIFIER `foreheadCardGame`, never the bare word "forehead". Confirmed by sweep: `packages/rules/src/hanabi/*.ts` (state, projection, actions, legality, deck, adapter, hanabi-leak-check, and both property tests) and `packages/schema/src/constants.ts` cite `forehead-card.ts` in comment prose as the template they were modelled on. That is historical design rationale, deliberately retained, and a word-level check would force deleting it.

Strengthen A9 against vacuity by also asserting the `hanabiGame` count in `game-registration.ts` is at least 1, so a future refactor that removes the identifier entirely cannot make the confinement assertion pass with an empty array.

Leave A1-A8 and A10 exactly as they are — they audit generic chokepoints (`.send(`, `encodeServerMessage(`, `toSeatView(`, `toPlayerView(`, `projectSeatView(`, `broadcast(`), none of which this phase changes.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project worker source-structure` is green with all of A1-A10 still present.
    - `grep -c 'hanabiGame' apps/worker/src/source-structure.test.ts` outputs at least `3`.
    - A9 asserts the `game-registration.ts` hit count is at least 1 (non-vacuous).
    - A separate `it(...)` asserts zero `foreheadCardGame` occurrences across scanned worker sources.
    - The zero-occurrence pattern is the identifier `foreheadCardGame`, NOT the substring `forehead` — verify the Hanabi engine's template-citing comments still exist afterwards.
    - The exact chokepoint counts in A1 (`.send(` === 1), A2 (`encodeServerMessage(` === 1), A4 (`toSeatView(` === 1), A6 (`broadcast(` === 0) and A7 are unchanged from their current values.
    - Fault injection: temporarily add `const x = hanabiGame;` to `apps/worker/src/room-state.ts`; A9 must FAIL. Revert after confirming.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project worker source-structure</automated>
  </verify>
  <done>The structural audit proves the active game's name is confined to the registration point and that the toy's name is gone from the worker.</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild the layer-3 frame-capture leak proof against the Hanabi view</name>
  <read_first>
    - apps/worker/src/room-do.test.ts (the full HIDE-01/HIDE-04 layer-3 test at roughly lines 497-684 — its three-seat setup, `findActiveSeat` helper, the two `guess` sends at roughly lines 573-588, the leak-secrets construction at roughly lines 620-660, the non-vacuousness assertions, and the reconnect capture)
    - apps/worker/src/room-do.test.ts line 33 (the toy import `FOREHEAD_CARD_VALUES, checkSeatViewForLeaks` — this task removes it)
    - packages/rules/src/hanabi/hanabi-leak-check.ts (`checkHanabiViewForLeaks` / `secretsForHanabiSeat` and the reason strings)
    - packages/rules/src/hanabi/state.ts (the HanabiView shape the type aliases in this test must describe)
  </read_first>
  <files>apps/worker/src/room-do.test.ts</files>
  <action>
This task owns EVERY remaining toy-shaped construct in this file. There are exactly four regions, and all four must be repointed here — leaving any one of them keeps the worker project red:

1. The import at line 33: replace `FOREHEAD_CARD_VALUES, checkSeatViewForLeaks` from `@games/rules` with `checkHanabiViewForLeaks` and any Hanabi types needed.
2. The local `GameViewShape`/`RoomViewShape` type aliases (roughly lines 500-511): rewrite them to describe the Hanabi view (`yourHand`, `otherHands`, `stacks`, `discard`, `clueTokens`, `fuses`, `deckCount`, `activeSeatId`, `isYourTurn`, `score`, `history`) instead of the toy's `yourCard`/`otherCards`/`revealed`.
3. The two action sends and their wait predicates at roughly lines 573-588 — `send(firstActive.ws, { ..., request: { type: "guess", value: FOREHEAD_CARD_VALUES[0] } })` followed by a `revealed.length === 1` wait, and the matching `FOREHEAD_CARD_VALUES[1]` / `revealed.length === 2` pair. Both sends and both predicates are repointed here.
4. The leak-secrets construction at roughly lines 620-660 — the `for (const entry of game.revealed)` loop and the `FOREHEAD_CARD_VALUES.filter(...)` forbidden-token list. Note this region fails at RUNTIME, not merely by assertion: after the engine swap `game.revealed` is `undefined`, so iterating it throws. That is why this region cannot be partially repaired and must be rebuilt in one go.

Keep the test's overall shape: three seats join, all three observe the full lobby, the host starts the game, two actions are taken by whichever seat is active, then Bob reconnects with his saved seat token and that reconnect `joined` frame is explicitly included in the checked set. Replace the two toy guess sends with legal Hanabi actions derived from the ACTIVE seat's own captured view — the simplest reliable choice is a rank clue naming a rank actually present in a visible other-hand card, which cannot be refused for touching nothing. Each send needs its own distinct `actionId` (plan `04-02` already placed literals on both sends; keep them distinct). Replace the two `revealed.length === 1` / `=== 2` wait predicates with `history.length === 1` / `=== 2` on the same per-seat loop, so all three seats are still confirmed to have observed each action before the capture is taken.

The leak check itself cannot use server-side `HanabiState` here (this test only sees wire frames), so build the per-seat `HanabiSeatSecrets` from what the frames reveal, mirroring how the toy version built `cardValueById`: a seat's own hand card ids come from its own `yourHand[].id`, and their true identities are observable in OTHER seats' frames under `otherHands[].cards[]` where the same card id appears with `suit`/`rank`. Use that cross-frame correspondence to populate `ownCards`, and set `allowedIdentityCounts` to the identity multiset legitimately visible in that seat's own frames. Keep an explicit non-vacuousness assertion that each seat had at least 3 game-bearing frames checked and that the reconnect frame was among them, exactly as the current test does.

Assert `checkHanabiViewForLeaks` returns an empty reason array for every captured frame of every seat. Keep the closing comment explaining that the server-only seed is covered by layers 1 and 2 rather than here.

Do not touch the CR-03 test — Task 2 of this plan already repointed it.
  </action>
  <acceptance_criteria>
    - `grep -c 'checkSeatViewForLeaks\|FOREHEAD_CARD_VALUES' apps/worker/src/room-do.test.ts` outputs `0` — covering the import at line 33, the sends at ~573/582, and the forbidden-token list at ~630.
    - `grep -c 'revealed' apps/worker/src/room-do.test.ts` outputs `0` — the type alias, both wait predicates, and the secrets-construction loop are all repointed.
    - `grep -c 'guess' apps/worker/src/room-do.test.ts` outputs `0`.
    - `grep -c 'checkHanabiViewForLeaks' apps/worker/src/room-do.test.ts` outputs at least `1`.
    - Both layer-3 sends are `{ type: "clue", ... }` requests, each with its own distinct `actionId`.
    - The layer-3 test still asserts at least 3 game-bearing frames per seat and that the reconnect `joined` frame was checked.
    - The test asserts an empty leak-reason array for every captured frame.
    - `npx vitest run --project worker` is GREEN in full, and so is an unfiltered `npm test` — no test anywhere is left red by this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration --workspace apps/worker && npx tsc -b && npm test</automated>
  </verify>
  <done>Real workerd frames are proven free of own-hand identity across join, live update, and reconnect for the real Hanabi engine, and no toy-shaped construct remains anywhere in the worker suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter state -> projected view -> socket | The swap changes WHAT is projected; the gate protecting it must not weaken |
| generic room layer -> game specifics | FDN-01: only `game-registration.ts` may name a game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | `activeGame.viewSchema` | mitigate | Swapped to `HanabiViewSchema` so `seat-projection.ts`'s fail-closed gate validates the real Hanabi view; a view failing it yields `null` and an `error` frame, never a raw fallback |
| T-04-11 | Information Disclosure | own-hand identity on the wire | mitigate | `redaction-wire.test.ts` repointed to `checkHanabiViewForLeaks` in the same change as the swap (D-02), with a canary proving the checker still fails on a forced leak |
| T-04-12 | Elevation of Privilege | a second serializer bypassing projection | mitigate | `source-structure.test.ts` A1-A8 chokepoint counts kept at their exact current values; any new `.send(`/`toSeatView(` call site fails the build |
| T-04-13 | Tampering | schema/engine drift silently accepting a wrong shape | mitigate | Compile-time assignability + mutual-key assertions repointed to `HanabiView`/`HanabiViewWire`; drift fails `tsc -b` |
| T-04-14 | Information Disclosure | vacuous confinement test after toy deletion | mitigate | A9 rewritten to confine `hanabiGame` with a non-vacuity floor of 1 hit, plus a separate zero-occurrence assertion for the toy name (Pitfall 5) |
| T-04-19 | Repudiation | a turn-order test orphaned by the swap and silently disabled or weakened | mitigate | CR-03 repointed in the SAME change as the swap (D-02) to a legal Hanabi clue with a `history.length` predicate, keeping its refusal, seat-count and turn assertions intact rather than skipping the test |
| T-04-23 | Information Disclosure | own-hand identity in genuine workerd frames | mitigate | Layer-3 frame capture rebuilt against `checkHanabiViewForLeaks` across join, live update and reconnect (D-02), in the same plan as the swap that breaks it |
| T-04-37 | Repudiation | a genuine regression hiding behind a known-expected failure | mitigate | Every casualty of the swap is repaired inside this plan and the closing gate is an unfiltered `npm test`, so no wave ends with a failure an executor must classify by hand |
| T-04-38 | Repudiation | fail-closed gate tests passing vacuously against the wrong game's shape | mitigate | `seat-projection.test.ts`'s fixtures repointed to Hanabi and gated by a positive control asserting the unleaked fixture validates, with a documented fault injection proving the control is load-bearing (D-07/HIDE-03) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc -b` clean (proves `HanabiView` matches the strict schema's inferred type).
- `npm test` green, unfiltered, across all four projects — this is the plan's closing gate and it admits no expected failures.
- `npm run test:integration --workspace apps/worker` green.
- `git diff --stat` for Task 1 touches only `game-registration.ts`; no task in this plan touches `apps/worker/src/seat-projection.ts`.
</verification>

<success_criteria>
- The worker calls `hanabiGame` through `activeGame`, and no generic worker module names Hanabi.
- Wire-level redaction is proven against the real engine, with a canary proving the checker can fail.
- The fail-closed gate's rejection tests are Hanabi-shaped and provably non-vacuous.
- CR-03 still proves a mid-game leave is refused and the seat stays in turn order, driven by a legal Hanabi clue.
- Layer-3 live-frame capture proves no own-hand identity on join, live update, or reconnect.
- No toy-shaped send, predicate, type alias, fixture, or import remains in the worker's test suite.
- The structural chokepoint counts are unchanged and the game-naming confinement is non-vacuous.
- The plan ends with an unfiltered `npm test` green — no test is left red for a later plan to repair.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-03-SUMMARY.md` when done.
</output>
