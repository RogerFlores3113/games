---
phase: 04-wire-engine-into-room-actor
plan: 06
type: execute
wave: 6
depends_on: ["04-04"]
files_modified:
  - apps/web/lib/hanabi-board-logic.ts
  - apps/web/lib/hanabi-board-logic.test.ts
  - apps/web/components/HanabiBoard.tsx
  - apps/web/app/room/[code]/RoomClient.tsx
autonomous: true
requirements: [RT-01]

must_haves:
  truths:
    - "D-11: a deliberately plain, playable board shows your own hand as face-down slots carrying their accumulated clue facts, every other hand face up, the played stacks, the discard pile, clue and fuse tokens, deck count, whose turn it is, and controls to play or discard a slot and to give a colour or rank clue to a chosen seat."
    - "D-12: client-side disabling is limited to what the view makes unambiguous — not your turn, no clue tokens left, discard at 8 tokens, and a clue that touches zero visible cards. Everything else is submitted and the server stays the authority."
    - "D-13: the end of a game shows the final score and its band from the engine and stops accepting actions."
    - "D-07: the client mints an opaque actionId per user intent and sends it alongside the action."
    - "UI-SPEC Trap 1: hand slots are rendered by mapping the actual array, never a hardcoded 5-slot grid."
  artifacts:
    - path: "apps/web/lib/hanabi-board-logic.ts"
      provides: "Pure D-12 disabling predicates and the engine-sourced score band"
      exports: ["clueTouchCountForTarget", "isGiveClueDisabled", "isDiscardDisabled", "bandForView"]
    - path: "apps/web/components/HanabiBoard.tsx"
      provides: "The interim Hanabi table"
      min_lines: 120
  key_links:
    - from: "apps/web/app/room/[code]/RoomClient.tsx"
      to: "apps/web/components/HanabiBoard.tsx"
      via: "in-progress branch renders the board and mints an actionId per action"
      pattern: "HanabiBoard"
    - from: "apps/web/lib/hanabi-board-logic.ts"
      to: "packages/rules/src/hanabi/variant.ts"
      via: "clue-touch and score band come from the engine's own functions, not re-derived"
      pattern: "variantConfig|scoreBand"
---

<objective>
Ship the interim Hanabi board: plain, playable, and cheap to throw away (D-11, D-12, D-13).

Purpose: RT-01 needs a real screen for an action to appear on. This board exists to play a game, not to look finished — Phase 6 replaces it wholesale and owns the colorblind glyph system, the luminosity theme, persistent clue-memory rendering, and the designed end screen.
Output: a pure logic module with tests, the board component, and the one-element swap in `RoomClient.tsx`.

Do not polish this screen. No card art, no animation, no glyph system, no luminosity work, no suit-to-hue mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/04-wire-engine-into-room-actor/04-UI-SPEC.md
@.planning/phases/04-wire-engine-into-room-actor/04-PATTERNS.md
@apps/web/AGENTS.md

<interfaces>
Engine functions the client may call (already exported from `packages/rules/src/index.ts`,
and `apps/web` already imports from `@games/rules`):
  variantConfig(variant) -> VariantConfig
    .colorClueTouches(suit, clueColor) -> boolean
    .rankClueTouches(rank, clueRank) -> boolean
    .cluableColors: readonly Suit[]     // rainbow is NEVER nameable; black IS
    .suits: readonly Suit[]
  maxScoreFor(config) -> number
  scoreBand(score, maxScore) -> string
  ALL_SUITS, RANKS

The per-seat view the board renders (HanabiView, packages/rules/src/hanabi/state.ts:103-118):
  variant, yourSeatId, yourHand[], otherHands[{seatId, cards[]}], stacks[{suit, topRank}],
  discard[{id,suit,rank}], clueTokens, fuses, deckCount, finalTurnsRemaining,
  activeSeatId, isYourTurn, score, history[]

  A card is { id, hidden: true, facts } or { id, hidden: false, suit, rank, facts }.
  facts: { possibleSuits, possibleRanks, positiveClues, negativeClues }
  A clue entry is { type: "color" | "rank", value }.

Action request shapes the board emits (exact-own-key guarded server-side — send
EXACTLY these keys and no others):
  { type: "play", cardId }
  { type: "discard", cardId }
  { type: "clue", targetSeatId, clue: { type: "color" | "rank", value } }

`view.status === "ended"` (room level) is the end-of-game signal.
Seat display labels come from `view.seats.find(s => s.seatId === id)?.displayLabel`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the D-12 disabling rules and score band as tested pure functions</name>
  <read_first>
    - .planning/phases/04-wire-engine-into-room-actor/04-UI-SPEC.md (the Copywriting Contract's four disabled-state captions, and Component Notes Trap 1)
    - packages/rules/src/hanabi/variant.ts (variantConfig, cluableColors, colorClueTouches, rankClueTouches, maxScoreFor)
    - packages/rules/src/hanabi/endgame.ts (scoreBand — the band the UI must render rather than compute)
    - packages/rules/src/hanabi/state.ts (HanabiView / HanabiCardView / ClueFactsView)
    - apps/web/lib/room-store.ts (the existing lib-module conventions in this app)
  </read_first>
  <files>apps/web/lib/hanabi-board-logic.ts, apps/web/lib/hanabi-board-logic.test.ts</files>
  <behavior>
    - `clueTouchCountForTarget` counts how many of a target seat's VISIBLE cards a given clue touches, dispatching through the variant config's own predicates.
    - A rank clue naming a rank present in the target's hand returns a count of at least 1; a rank absent from the hand returns 0.
    - In the rainbow variant, a colour clue touches rainbow cards as well as cards of the named colour.
    - `isGiveClueDisabled` is true when it is not your turn, when `clueTokens <= 0`, or when the selected target+clue touches zero visible cards; false otherwise.
    - `isDiscardDisabled` is true when it is not your turn or when `clueTokens >= 8`; false otherwise.
    - `isPlayDisabled` is true only when it is not your turn.
    - `bandForView` returns the engine's band string for the view's score and its variant's maximum score.
    - `cluableColorsForView` returns the variant's nameable colours, and never includes "rainbow" in the rainbow variant.
  </behavior>
  <action>
Create `apps/web/lib/hanabi-board-logic.ts` exporting the pure functions named in the behaviour block, each taking a `HanabiView` (plus a target seat id and/or a clue where relevant) and returning a boolean, number, or string. Implement clue-touch by resolving `variantConfig(view.variant)` once and dispatching to its `colorClueTouches`/`rankClueTouches` predicates over the target's `cards` entries that are visible — deliberately reusing the engine's variant predicate rather than hand-rolling a touch rule in the client.

Implement `bandForView` as `scoreBand(view.score, maxScoreFor(variantConfig(view.variant)))`. Document in a comment that this is NOT a client-side re-derivation of the band table: the thresholds live in the engine (D-13) and the client only calls the engine's own function.

Add a file-header comment recording the D-12 boundary in the style `apps/web/components/Lobby.tsx` uses for deliberately-cut scope: these four cases are the ONLY client-side disabling permitted this phase, because they are the only ones the redacted view makes unambiguous; the engine's `canPlay`/`canDiscard`/`canClue` need the full `HanabiState`, which the client does not have, and the complete RULES-11 treatment is Phase 6's. If you find yourself adding a fifth disabling rule, stop — it was cut.

Write `apps/web/lib/hanabi-board-logic.test.ts` covering every behaviour above, including the rainbow colour-clue case and the "rainbow is not a nameable colour" case. Build view fixtures as plain objects matching `HanabiView`.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project web hanabi-board-logic` passes with at least 9 assertions.
    - A test asserts `cluableColorsForView` excludes `"rainbow"` for the rainbow variant.
    - A test asserts a rainbow-variant colour clue touches a rainbow card.
    - `grep -c 'scoreBand' apps/web/lib/hanabi-board-logic.ts` outputs at least `1`, and no numeric band threshold literal appears in the file.
    - `grep -c 'variantConfig' apps/web/lib/hanabi-board-logic.ts` outputs at least `1`.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project web hanabi-board-logic</automated>
  </verify>
  <done>Every client-side disabling decision and the score band are pure, tested, and sourced from the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Build the interim board component</name>
  <read_first>
    - apps/web/components/ForeheadCardGame.tsx (the template: the `isXView` duck-type guard over `view.game`, the turn-indicator block, the card-tile markup, the action-button mapping, the end-of-game branch, and the theme-token inline style convention)
    - .planning/phases/04-wire-engine-into-room-actor/04-UI-SPEC.md (the full Screens & States layout, the Copywriting Contract table, and the required `data-testid` floor list in Component Notes Trap 2)
    - apps/web/components/Button.tsx (reuse unmodified — primary/ghost only)
    - apps/web/lib/hanabi-board-logic.ts (the predicates from Task 1)
    - node_modules/next/dist/docs/ (REQUIRED before writing web code — this Next.js version has breaking changes; read the relevant guide)
  </read_first>
  <files>apps/web/components/HanabiBoard.tsx</files>
  <action>
Create `apps/web/components/HanabiBoard.tsx` as a `"use client"` component taking `{ view: RoomView; onAction: (request: HanabiActionRequest) => void }`. Duck-type `view.game` into a `HanabiView` with a guard shaped like `isForeheadCardView`, checking for `yourHand`, `otherHands`, `stacks` and `discard` and that the array-typed ones are arrays — `RoomViewSchema.game` is `z.unknown()` at the room layer, so the client must not blindly trust it.

Lay the screen out top to bottom exactly as the UI-SPEC's Screens & States section specifies: turn indicator, other hands, the table row (played stacks, discard pile, token/counter cluster), your own hand, then action controls. Use the UI-SPEC's Copywriting Contract strings verbatim, including "Your turn" and "Waiting for {displayLabel}" — note these differ from the toy's "Your turn — guess your card".

Render every hand by mapping the ACTUAL array (`yourHand`, and each `otherHands[].cards`). Never render a fixed number of slots: hands are 4 cards in 4-5 player games and 5 in 2-3 player games, and a hardcoded grid would silently misrender.

An own-hand slot renders as a surface-filled, border-outlined tile containing ONLY its 1-indexed position label and chips for the raw `positiveClues`/`negativeClues` it has received ("Told: Red", "Told: not 3"). It must never render a suit, a rank, a colour swatch, or any placeholder glyph implying identity, and must NOT render `possibleSuits`/`possibleRanks` — those narrowed candidate sets are Phase 6's UI-05 work. Other hands, stacks and the discard pile render face up with suit as WRITTEN TEXT plus rank; do not map any suit to a hue this phase.

Action controls: selecting an own slot (accent ring, not fill) enables Play and Discard for that card id; a clue control group offers one button per other seat and one per nameable colour/rank, then a "Give clue" submit. Drive every `disabled` prop from the Task 1 predicates, and render the UI-SPEC's caption beside each disabled control. Emit exactly the three documented request shapes through `onAction` — no extra keys, or the server's exact-own-key guards will reject the payload as `invalid_action`.

When `view.status === "ended"`, REMOVE the action controls entirely (not merely disable them) and render "Game over" plus "Final score: {score} — {band}" using `bandForView`. Hands, table and tokens stay visible as they stood.

Every colour and spacing value must be a `var(--...)` token — no literal hex or px values except where the existing components already do so. Use `clsx` for conditional classes. Provide, at minimum, this stable `data-testid` floor: `turn-indicator`, `own-hand`, `own-hand-slot-{n}`, `other-hand-{seatId}`, `other-hand-card-{cardId}`, `played-stack-{suit}`, `discard-pile`, `clue-tokens`, `fuse-tokens`, `deck-count`, `play-button`, `discard-button`, `clue-target-{seatId}`, `clue-value-{value}`, `give-clue-button`, `game-over-heading`, `final-score`.

Add a file-header comment in this codebase's established style recording that this screen is deliberately plain and is replaced wholesale by Phase 6, and restating the own-hand rule (clue facts and position only, never identity or identity-adjacent signal).
  </action>
  <acceptance_criteria>
    - Every `data-testid` in the floor list above appears in `apps/web/components/HanabiBoard.tsx`.
    - `grep -c 'possibleSuits\|possibleRanks' apps/web/components/HanabiBoard.tsx` outputs `0`.
    - The own-hand rendering path contains no reference to `.suit` or `.rank` on a card from `yourHand`.
    - `grep -cE '#[0-9a-fA-F]{6}' apps/web/components/HanabiBoard.tsx` outputs `0` (tokens only, no literal hex).
    - `grep -c 'yourHand.map\|yourHand\.map' apps/web/components/HanabiBoard.tsx` outputs at least `1`, and no literal `5` is used as a slot count.
    - The three emitted request shapes carry exactly the documented keys — verify by reading each `onAction(` call site.
    - `npx tsc -b` exits 0 and `npx vitest run --project web` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project web</automated>
  </verify>
  <done>A plain, playable Hanabi table renders every wire-visible field, with own-hand identity structurally absent from the markup.</done>
</task>

<task type="auto">
  <name>Task 3: Swap the in-progress branch to the board and mint an actionId per intent</name>
  <read_first>
    - apps/web/app/room/[code]/RoomClient.tsx (the full file — the `send()` wrapper, the in-progress return branch at the end, and the `nanoid` import added in plan 04-02)
    - apps/web/components/HanabiBoard.tsx (the props contract from Task 2)
  </read_first>
  <files>apps/web/app/room/[code]/RoomClient.tsx</files>
  <action>
Replace the `ForeheadCardGame` import with `HanabiBoard`, and change the final return branch to render `HanabiBoard` with `view={view}` and `onAction={(request) => send({ type: "game_action", actionId: nanoid(), request })}`.

Leave the rest of the file untouched: `useRoomSocket`, `useRoomStore`, the `refused`/`superseded`/`abandoned` branches, the connecting placeholder, and the `Lobby` branch all stay exactly as they are. This should be a two-line diff plus the import.

Keep or add the comment at the `onAction` call site recording D-07's rule: the id is minted once per user intent (one click), and any future retry path MUST resend the same id verbatim rather than minting a new one, or dedup is defeated. The board deliberately implements no retry logic this phase, which is why minting inline here is correct.

Do NOT delete `apps/web/components/ForeheadCardGame.tsx` here — the deletion sweep is plan `04-08`, and the e2e specs still reference the toy until plan `04-07`.
  </action>
  <acceptance_criteria>
    - `grep -c 'ForeheadCardGame' apps/web/app/room/\[code\]/RoomClient.tsx` outputs `0`.
    - `grep -c 'HanabiBoard' apps/web/app/room/\[code\]/RoomClient.tsx` outputs `2` (import plus usage).
    - `grep -c 'actionId: nanoid()' apps/web/app/room/\[code\]/RoomClient.tsx` outputs `1`.
    - The `Lobby` branch and all four connection-status branches are unchanged (`git diff` shows no edits outside the import line and the final return branch).
    - `npx tsc -b` exits 0 and `npm test` is green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npm test</automated>
  </verify>
  <done>An in-progress room renders the Hanabi board, and every submitted action carries a freshly minted idempotency key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket payload -> client render | `view.game` is `z.unknown()` at the room layer; the client duck-types before trusting |
| client UI disabling -> server authority | A disabled control is cosmetic only; the server re-checks everything |
| client -> `game_action.request` | The emitted request must carry exactly the guarded keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Information Disclosure | own-hand tile markup | mitigate | The own-hand render path never reads `.suit`/`.rank` (the server never sends them) and renders no identity-implying placeholder; grep acceptance criteria enforce it |
| T-04-26 | Information Disclosure | narrowed candidate sets | accept | `possibleSuits`/`possibleRanks` legitimately reach the client but are deliberately NOT rendered this phase (Phase 6 UI-05); they are derived from clues the player already received, so this is a scope decision, not a leak |
| T-04-27 | Elevation of Privilege | client-side disabling mistaken for enforcement | mitigate | D-12 limits disabling to four unambiguous cases; the server re-validates every action regardless, and the board submits anything it does not disable |
| T-04-28 | Tampering | request payload carrying an extra asserted key | mitigate | The board emits exactly the three documented shapes; the adapter's exact-own-key guards reject anything else as `invalid_action` |
| T-04-29 | Denial of Service | malformed `view.game` crashing the board | mitigate | Duck-type guard before use, falling back to a null game rather than throwing, mirroring the toy component's established pattern |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; `nanoid` (6.0.1) and `clsx` are pre-existing exact pins. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc -b` clean.
- `npm test` green across all four projects.
- Every required `data-testid` is present for plan `04-07`'s Playwright specs to bind to.
</verification>

<success_criteria>
- A base-game Hanabi table is playable end to end in the browser through this board.
- Own-hand tiles show clue facts and position only — never identity.
- Disabling covers exactly the four D-12 cases, and the end state shows the engine's score and band.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-06-SUMMARY.md` when done.
</output>
