---
phase: 04-wire-engine-into-room-actor
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/schema/src/messages.ts
  - packages/schema/src/messages.test.ts
  - packages/schema/src/room.ts
  - packages/schema/src/constants.ts
  - apps/worker/src/persistence.test.ts
  - apps/worker/src/room-do.test.ts
  - apps/web/app/room/[code]/RoomClient.tsx
autonomous: true
requirements: [RT-09]

must_haves:
  truths:
    - "D-07: every game_action frame carries a bounded, validated actionId string at the envelope level, outside the hostile `request` payload, so it can never be smuggled into an adapter request."
    - "D-08: the persisted Seat record can record the last applied actionId, declared optional/nullable so older persisted rooms still parse, and it is absent from PublicSeatSchema and RoomViewSchema."
    - "D-10: ErrorDetailSchema is a widened CLOSED enum carrying one member per AdapterError member plus view_unavailable — never free text, never game data."
    - "D-06: ROOM_SCHEMA_VERSION is bumped so rooms persisted with the toy's state reset to an empty lobby instead of being handed to the Hanabi adapter."
  artifacts:
    - path: "packages/schema/src/messages.ts"
      provides: "actionId on GameActionMessageSchema; widened ErrorDetailSchema"
      contains: "actionId"
    - path: "packages/schema/src/room.ts"
      provides: "Seat.lastAppliedActionId (persisted-only)"
      contains: "lastAppliedActionId"
    - path: "packages/schema/src/constants.ts"
      provides: "ROOM_SCHEMA_VERSION = 3"
      contains: "ROOM_SCHEMA_VERSION = 3"
  key_links:
    - from: "packages/schema/src/messages.ts"
      to: "packages/rules/src/adapter.ts"
      via: "ErrorDetail enum carries one member per AdapterError member"
      pattern: "clue_touches_nothing"
    - from: "packages/schema/src/room.ts"
      to: "apps/worker/src/persistence.ts"
      via: "optional field so pre-existing persisted rooms still parse"
      pattern: "lastAppliedActionId"
---

<objective>
Extend the wire protocol and persisted room shape for exactly-once actions and actionable refusals: `actionId` on `game_action` (D-07), `lastAppliedActionId` on the persisted `Seat` (D-08), a widened closed `ErrorDetailSchema` (D-10), and a `ROOM_SCHEMA_VERSION` bump (D-06).

Purpose: RT-09 needs an idempotency key that survives Durable Object hibernation, and D-10 needs a closed refusal vocabulary that still carries no state.
Output: the schema changes, their tests, and the minimal call-site updates that keep every existing `game_action` sender valid now that `actionId` is required.

This plan changes NO behavior in the worker — it only makes the wire and storage shapes available. The dedup logic lands in plan `04-04`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wire-engine-into-room-actor/04-CONTEXT.md
@.planning/phases/02-per-seat-redaction-contract/02-CONTEXT.md

<interfaces>
`AdapterError` — the closed union `ErrorDetail` must cover 1:1, verified by reading
`packages/rules/src/adapter.ts:22-30`. Exactly 8 members:

  not_your_turn | invalid_action | game_over | card_not_in_hand
  no_clue_tokens | clue_touches_nothing | clue_target_invalid | discard_at_max_clues

Current shapes being changed:
  packages/schema/src/messages.ts:30-36  GameActionMessageSchema { type, request }
  packages/schema/src/messages.ts:83     ErrorDetailSchema = z.enum(["view_unavailable"])
  packages/schema/src/room.ts:74-88      SeatSchema (persisted)
  packages/schema/src/room.ts:125-131    PublicSeatSchema (client-facing, declared
                                         INDEPENDENTLY so new Seat fields cannot leak)
  packages/schema/src/constants.ts:14    ROOM_SCHEMA_VERSION = 2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add actionId to the wire and widen the closed error-detail enum</name>
  <read_first>
    - packages/schema/src/messages.ts (the full file — `GameActionMessageSchema`, `ErrorDetailSchema` and its D-08 docstring about never becoming a state-bearing channel)
    - packages/rules/src/adapter.ts (the authoritative 8-member `AdapterError` union)
    - packages/schema/src/constants.ts (the named-constant convention this file uses for every tuning number)
  </read_first>
  <files>packages/schema/src/messages.ts</files>
  <action>
Add two module-level constants `ACTION_ID_MIN_LENGTH = 1` and `ACTION_ID_MAX_LENGTH = 64`, then add `actionId: z.string().min(ACTION_ID_MIN_LENGTH).max(ACTION_ID_MAX_LENGTH)` to `GameActionMessageSchema` as a REQUIRED field, leaving `request: z.unknown()` exactly as it is. Document with a comment that `actionId` deliberately lives at the envelope level and never inside `request`: the adapter's exact-own-key guards (`isPlayRequest`/`isDiscardRequest`/`isClueRequest` in `packages/rules/src/hanabi/actions.ts`) reject any request object carrying an extra key, so keeping the key outside `request` is what lets both mechanisms hold at once; and that it is an idempotency key only, never reaching the adapter and never influencing game logic (D-07, HIDE-05).

Widen `ErrorDetailSchema` from `z.enum(["view_unavailable"])` to a closed enum containing `view_unavailable` plus one member per `AdapterError` member, named identically to the adapter's members: `not_your_turn`, `invalid_action`, `game_over`, `card_not_in_hand`, `no_clue_tokens`, `clue_touches_nothing`, `clue_target_invalid`, `discard_at_max_clues`. Preserve and extend the existing D-08 docstring: state that this stays a CLOSED vocabulary carrying no game state and no free text, that widening it to a free-form string would reintroduce the state-bearing channel Phase 2 closed, and that the members mirror `AdapterError` 1:1 so `mapAdapterError` (plan `04-04`) can map without a lossy collapse.

Do not change `RefusalReasonSchema` — the specific reason rides in `detail`, while `code` stays `bad_request`.
  </action>
  <acceptance_criteria>
    - `grep -c 'actionId' packages/schema/src/messages.ts` outputs at least `2`.
    - `GameActionMessageSchema` still declares `request: z.unknown()` and `actionId` is NOT nested inside `request`.
    - `ErrorDetailSchema` is still a `z.enum(...)` (not `z.string()`), and contains exactly 9 members: `view_unavailable` plus the 8 AdapterError names.
    - Every one of the 8 `AdapterError` member names from `packages/rules/src/adapter.ts` appears in `packages/schema/src/messages.ts`.
    - `grep -c 'RefusalReasonSchema = z.enum' packages/schema/src/room.ts` still outputs `1` with its original 6 members (the shared refusal enum is unchanged).
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project schema</automated>
  </verify>
  <done>`game_action` requires a bounded `actionId` at the envelope level, and `ErrorDetail` is a 9-member closed enum mirroring `AdapterError` 1:1.</done>
</task>

<task type="auto">
  <name>Task 2: Add the persisted per-seat idempotency field and bump the schema version</name>
  <read_first>
    - packages/schema/src/room.ts (SeatSchema, the `seed: z.string().optional()` migration precedent and its docstring, and PublicSeatSchema's independent-declaration comment)
    - packages/schema/src/constants.ts (ROOM_SCHEMA_VERSION and its bump-history docstring)
    - apps/worker/src/persistence.ts (the version-mismatch reset path this bump triggers)
  </read_first>
  <files>packages/schema/src/room.ts, packages/schema/src/constants.ts</files>
  <action>
Add `lastAppliedActionId: z.string().nullable().optional()` to `SeatSchema`, placed after `disconnectedAt`. Use the `seed` field's docstring as the template for its comment: explain that it records the `actionId` of the most recently APPLIED `game_action` for that seat (RT-09/D-08), that it is idempotency bookkeeping ONLY — never read by the adapter, never reaching the wire — and that it is optional/nullable so rooms persisted before this field existed still parse.

Do NOT add the field to `PublicSeatSchema` or `RoomViewSchema`. Add a one-line comment at `PublicSeatSchema` noting that `lastAppliedActionId` is deliberately absent, reinforcing why that schema is declared independently rather than derived from `SeatSchema`.

Bump `ROOM_SCHEMA_VERSION` from 2 to 3 in `packages/schema/src/constants.ts` and extend its existing docstring with a third bump-history line: bumped to 3 in Phase 4 when the forehead-card toy adapter was replaced by the real Hanabi engine, so a persisted room carrying toy-shaped `game` state and `adapterId: "forehead-card"` resets to an empty lobby rather than being handed to the Hanabi adapter (D-06).
  </action>
  <acceptance_criteria>
    - `grep -c 'lastAppliedActionId' packages/schema/src/room.ts` outputs at least `2` (the field plus the PublicSeatSchema note).
    - `grep -n 'lastAppliedActionId' packages/schema/src/room.ts` shows NO hit inside the `PublicSeatSchema` or `RoomViewSchema` object literals.
    - `grep -c 'ROOM_SCHEMA_VERSION = 3' packages/schema/src/constants.ts` outputs `1`.
    - A `Seat` object WITHOUT `lastAppliedActionId` still parses under `SeatSchema` (backward compatibility).
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run --project schema</automated>
  </verify>
  <done>The persisted seat can carry an idempotency key that never reaches the wire, and the schema version bump will reset toy-persisted rooms on deploy.</done>
</task>

<task type="auto">
  <name>Task 3: Cover the new shapes with tests and keep every existing game_action sender valid</name>
  <read_first>
    - packages/schema/src/messages.test.ts (if it exists — otherwise packages/schema/src/games/forehead-card.test.ts for the rejection-test convention)
    - apps/worker/src/persistence.test.ts (especially the existing "schemaVersion 1 room with adapterId counter ... resets without reading the room blob" test at ~line 130 — the template for the new case)
    - apps/worker/src/room-do.test.ts (the three existing `game_action` sends at roughly lines 418, 573, 582)
    - apps/web/app/room/[code]/RoomClient.tsx (the `send()` wrapper and the `onGuess` game_action call site at ~line 212)
  </read_first>
  <files>packages/schema/src/messages.test.ts, apps/worker/src/persistence.test.ts, apps/worker/src/room-do.test.ts, apps/web/app/room/[code]/RoomClient.tsx</files>
  <action>
In `packages/schema/src/messages.test.ts` (create it if it does not exist, following the schema project's existing test conventions), add tests proving: a `game_action` frame WITHOUT `actionId` fails `parseClientMessage`; a frame with an empty-string `actionId` fails; a frame with a 65-character `actionId` fails; a frame with a valid `actionId` and an arbitrary `request` value parses successfully; a `game_action` frame carrying an unknown extra envelope key fails (strictObject still holds); every one of the 9 `ErrorDetail` members parses; and a free-text detail such as `"you played the red 3"` is rejected by `ErrorDetailSchema` — this last one is the D-08 "error frames carry no state" regression guard, so name it accordingly.

In `apps/worker/src/persistence.test.ts`, add a case mirroring the existing counter-reset test: a persisted room stored under schemaVersion 2 with `adapterId: "forehead-card"` and a toy-shaped `game` blob resets to the fallback empty lobby without the room blob being read (D-06, closing RESEARCH.md assumption A5). Keep the existing counter-era cases as they are.

Then keep the suite green under the now-required field: add an `actionId` to each of the three existing `game_action` sends in `apps/worker/src/room-do.test.ts`, using a distinct literal per send site (these sends are repointed to Hanabi in plan `04-05` and their surrounding toy assertions are replaced then — this task only keeps them valid). In `apps/web/app/room/[code]/RoomClient.tsx`, import `nanoid` from the already-pinned `nanoid` package and change the in-progress branch's `game_action` send to include `actionId: nanoid()`, leaving the rest of the file untouched. Add a comment at that call site recording D-07's rule: the id is minted once per user intent (one click), and if a retry path is ever added it MUST reuse the same id verbatim rather than minting a new one, or dedup is defeated.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project schema` passes, including a test whose name references free text being rejected by `ErrorDetailSchema`.
    - `npx vitest run --project worker persistence` passes and includes a case asserting a `adapterId: "forehead-card"` room resets.
    - `grep -c 'actionId' apps/worker/src/room-do.test.ts` outputs at least `3`.
    - `grep -c 'nanoid' apps/web/app/room/\[code\]/RoomClient.tsx` outputs at least `2` (import plus call).
    - `npx tsc -b` exits 0 and `npm test` is green across all four projects.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npm test</automated>
  </verify>
  <done>The new wire and storage shapes are proven, the D-06 reset is proven for a toy-tagged room, and no existing sender is left invalid by the newly required field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `game_action` envelope | Untrusted `actionId` string crosses here |
| server -> `error` frame `detail` | A widened vocabulary must not become a state channel |
| persisted Seat record | Server-only bookkeeping that must never reach a view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | `actionId` on `GameActionMessageSchema` | mitigate | Bounded `.min(1).max(64)`; an oversized id is rejected at `parseClientMessage` before it can be persisted onto a Seat, proven by the 65-character test |
| T-04-06 | Tampering | `actionId` smuggled into `request` | mitigate | `actionId` is declared at the envelope level only; the adapter's exact-own-key guards independently reject any `request` carrying an extra key (defense in depth, Pitfall 3) |
| T-04-07 | Information Disclosure | widened `ErrorDetailSchema` | mitigate | Stays a `z.enum` closed vocabulary; a dedicated test asserts free text is rejected, preserving Phase 2 D-08 |
| T-04-08 | Information Disclosure | `Seat.lastAppliedActionId` | mitigate | Added only to `SeatSchema`; `PublicSeatSchema`/`RoomViewSchema` are declared independently and a grep acceptance criterion asserts the field is absent from both |
| T-04-09 | Tampering | stale toy state handed to the Hanabi adapter | mitigate | `ROOM_SCHEMA_VERSION` bump triggers `persistence.ts`'s reset-before-parse path, proven by a new forehead-card-tagged reset test |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; `nanoid` (6.0.1) and `zod` (4.5.4) are pre-existing exact pins. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc -b` clean.
- `npm test` green across schema, rules, worker, and web projects.
- `ErrorDetailSchema` remains a closed enum; `RefusalReasonSchema` is unchanged.
</verification>

<success_criteria>
- `game_action` requires a bounded `actionId` that lives outside `request`.
- The persisted `Seat` can carry `lastAppliedActionId` without it reaching any view.
- `ErrorDetail` covers all 8 `AdapterError` members plus `view_unavailable`, with no free-text path.
- A toy-tagged persisted room provably resets under the bumped schema version.
</success_criteria>

<output>
Create `.planning/phases/04-wire-engine-into-room-actor/04-02-SUMMARY.md` when done.
</output>
