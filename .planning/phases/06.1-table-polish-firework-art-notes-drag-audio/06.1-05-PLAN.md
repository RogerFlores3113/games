---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/public/backgrounds/city-night.webp
  - apps/web/public/backgrounds/CREDITS.md
  - apps/web/app/globals.css
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/lib/background-asset.test.ts
autonomous: true
requirements: [ART-02]

must_haves:
  truths:
    - "D-14: the in-game table sits on a static city-at-night photo/illustration under a dark scrim derived from --color-bg at 75%"
    - "D-14: the image comes from a clearly free-license source, with source URL, author, license name, and license URL recorded in the repo"
    - "D-14: the image is optimized (WebP, <= 300 KB) and does not move (no animation, background-attachment fixed)"
    - "Phase 6 D-01: five players still fit 1280x720 without scrolling"
  artifacts:
    - path: "apps/web/public/backgrounds/city-night.webp"
      provides: "optimized background image"
    - path: "apps/web/public/backgrounds/CREDITS.md"
      provides: "license record"
      contains: "License URL"
    - path: "apps/web/app/globals.css"
      provides: ".table-backdrop class"
      contains: ".table-backdrop"
  key_links:
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "apps/web/app/globals.css .table-backdrop"
      via: "className on the in-game <main>"
      pattern: "table-backdrop"
---

<objective>
Source, license-record, optimize, and apply a static city-at-night background behind the in-game table with a dark scrim that preserves existing contrast.

Purpose: ART-02 / D-14. Kept small and early so the owner sees it at every later checkpoint.
Output: Image + credits file, `.table-backdrop` CSS, HanabiBoard main uses it, asset test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md

<interfaces>
apps/web/components/hanabi/HanabiBoard.tsx: the in-game return is `<main className="flex min-h-screen flex-col gap-[3px] overflow-y-auto px-[length:var(--space-md)] py-[3px]" style={{ backgroundColor: "var(--color-bg)" }}>`; the loading/error `<main>` also sets backgroundColor (leave that one unchanged).
apps/web/app/globals.css: @theme block holds all hex; non-theme CSS below uses var() and rgba() scrims (EndOverlay uses rgba(11, 15, 26, 0.7)).
Tooling on this machine: python3 with PIL 12.2.0 (WebP save supported), curl. No new npm dependency may be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source, license-verify, and optimize the city-at-night image</name>
  <files>apps/web/public/backgrounds/city-night.webp, apps/web/public/backgrounds/CREDITS.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md (D-14)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Assumption A5)
  </read_first>
  <action>
    Per D-14 (discretion: specific image). Find a landscape photo or illustration of a city skyline at night on Unsplash (unsplash.com/license) or Pexels (pexels.com/license) — prefer a dark, low-detail skyline with sky space so cards stay legible; avoid images with readable text, logos, or identifiable people. Use WebFetch on the photo page to confirm author name and that it is under the Unsplash License or Pexels License (not "Unsplash+" or any paid/editorial-only listing), and WebFetch the license page to quote its one-sentence grant. Download the original with curl to /tmp, then convert with python3/PIL: resize to 1920px wide (keep aspect), save as WebP quality 70-80 to apps/web/public/backgrounds/city-night.webp, lowering quality until the file is <= 300 KB. Write apps/web/public/backgrounds/CREDITS.md with lines exactly prefixed: "Image: city-night.webp", "Source URL: <photo page URL>", "Author: <name>", "Author URL: <profile URL>", "License: <Unsplash License | Pexels License>", "License URL: <license page URL>", "License grant (quoted): <sentence>", "Retrieved: <YYYY-MM-DD>", "Modifications: resized to 1920px wide, re-encoded as WebP". If no image passes the license check, stop and report rather than using an unverified image.
  </action>
  <verify>
    <automated>cd /home/rflor/games && test -f apps/web/public/backgrounds/city-night.webp && test $(stat -c %s apps/web/public/backgrounds/city-night.webp) -le 300000 && grep -c "^Source URL: https://\|^License URL: https://\|^Author: " apps/web/public/backgrounds/CREDITS.md</automated>
  </verify>
  <acceptance_criteria>
    - city-night.webp exists, starts with RIFF....WEBP magic bytes, is <= 300000 bytes, and is 1920 px wide (python3 PIL check)
    - CREDITS.md contains non-empty "Source URL: https://", "Author: ", "License: ", "License URL: https://", and "License grant (quoted): " lines
  </acceptance_criteria>
  <done>A verified free-license city-at-night WebP and its credit record are in apps/web/public/backgrounds/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the backdrop with scrim to the in-game table</name>
  <files>apps/web/app/globals.css, apps/web/components/hanabi/HanabiBoard.tsx, apps/web/lib/background-asset.test.ts</files>
  <read_first>
    - apps/web/app/globals.css
    - apps/web/components/hanabi/HanabiBoard.tsx
    - apps/web/lib/suit-visuals.test.ts (readFileSync + @theme parsing pattern)
    - e2e/start-game.spec.ts (UI-11 no-scroll test)
  </read_first>
  <behavior>
    - background-asset.test: city-night.webp exists, has WEBP magic bytes, size <= 300000
    - CREDITS.md has Source URL / Author / License / License URL lines with non-empty values
    - globals.css outside @theme defines `.table-backdrop` containing `rgba(11, 15, 26, 0.75)`, `url("/backgrounds/city-night.webp")`, `background-size: cover`, `background-attachment: fixed`, and no hex literal
    - globals.css outside the @theme block contains no `#RRGGBB` literal at all (carry-forward rule)
    - HanabiBoard.tsx in-game <main> uses className containing "table-backdrop" and no longer sets backgroundColor inline on that element
  </behavior>
  <action>
    Per D-14 and UI-SPEC Background. In globals.css (below the body rule, outside @theme) add `.table-backdrop` with `background-color: var(--color-bg)`, `background-image: linear-gradient(rgba(11, 15, 26, 0.75), rgba(11, 15, 26, 0.75)), url("/backgrounds/city-night.webp")`, `background-size: cover`, `background-position: center`, `background-attachment: fixed`, and a comment that the 0.75 scrim is --color-bg (#0B0F1A) at 75% (discretion, tunable at owner sign-off) and that the image is static per D-14. In HanabiBoard.tsx's in-game `<main>` append ` table-backdrop` to className and remove its inline backgroundColor style (leave the loading/error main unchanged). Existing panels (tableau uses --color-surface) keep their opaque surfaces so text contrast is unchanged. Write background-asset.test.ts per the behaviour bullets (readFileSync on paths resolved via import.meta.url). Run the UI-11 Playwright test to confirm fit is unchanged.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web background-asset suit-visuals && npx tsc -b apps/web && npx playwright test -g "UI-11"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "table-backdrop" apps/web/components/hanabi/HanabiBoard.tsx apps/web/app/globals.css` matches in both files
    - `grep -n "rgba(11, 15, 26, 0.75)" apps/web/app/globals.css` matches
    - background-asset.test.ts passes; `npx playwright test -g "UI-11"` exits 0
  </acceptance_criteria>
  <done>The game table renders over the dimmed city-at-night image with fit and contrast preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party image host → repo | downloaded binary and its license claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-16 | Repudiation | CREDITS.md | mitigate | Source URL, author, license name + URL, quoted grant, and retrieval date recorded verbatim; unverifiable images rejected |
| T-06.1-17 | Tampering | downloaded image | mitigate | Re-encoded through PIL to a fresh WebP (strips metadata/embedded payloads); only the re-encoded file is committed |
| T-06.1-18 | Denial of Service | page weight | mitigate | <= 300 KB gate in test keeps link-click-to-playing fast |
</threat_model>

<verification>
- `npx vitest run --project web background-asset` green; `npx playwright test -g "UI-11"` green; `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
ART-02 is visible in-game with a recorded free license and no fit or contrast regression.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-05-SUMMARY.md` when done
</output>
