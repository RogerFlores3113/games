---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 06
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - packages/rules/src/hanabi/test-support.ts
  - packages/rules/src/hanabi/conservation.property.test.ts
  - packages/rules/src/hanabi/redaction.property.test.ts
  - packages/rules/src/hanabi/termination.property.test.ts
  - packages/rules/src/hanabi/projection.test.ts
  - apps/worker/src/room-state.test.ts
autonomous: true
requirements: [HAND-01, HAND-03]

must_haves:
  truths:
    - "D-23: conservation, redaction, and termination property suites generate reorder actions (by on-turn AND off-turn seats) and prove they ran (non-vacuous counters)"
    - "D-23: a property proves every draw lands in the vacated index across random games in every variant"
    - "D-23: after a reorder, every seat's projected view lists that hand's card ids in exactly the submitted order, and no seat's view leaks its own identities"
    - "D-22: through the room layer, an off-turn reorder is accepted, its actionId is deduplicated, a non-permutation is refused with detail card_not_in_hand, and an ended room refuses it"
  artifacts:
    - path: "packages/rules/src/hanabi/test-support.ts"
      exports: ["enumerateReorderActions"]
  key_links:
    - from: "property suites"
      to: "legality.ts canReorder"
      via: "enumerateReorderActions filters candidates through canReorder only"
      pattern: "canReorder"
---

<objective>
Extend the invariant safety net to the new engine behaviour: property suites drive reorders and check slot-preserving draws, projection tests prove order reaches every seat, and room-layer tests prove reorder flows through actionId idempotency and the status gate.

Purpose: Research Pitfall 2 — adding a union member without updating arbitraries gives false confidence. HAND-01/HAND-03 touch the per-seat redaction chokepoint, so leak tests must cover them.
Output: Extended property/projection/worker tests (no production code changes expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-01-SUMMARY.md

<interfaces>
From packages/rules/src/hanabi/test-support.ts: currentActorSeatId(state), enumerateLegalActions(state): HanabiAction[] (active seat only; filters via legality predicates — T-03-24: never re-derive rules), locateAllCards(state)
Property suite loop shape (all three files): fc.property(variant, seatCount 2..5, seed hex32, fc.array(fc.nat({max:40}))) → for each index: break on checkHanabiGameEnd; legal = enumerateLegalActions(state); action = legal[index % legal.length]; hanabiGame.applyAction(state, currentActorSeatId(state), action); termination counts `turn` against MAX_SIMULATED_TURNS = 500
From 06.1-01: canReorder(state, actorSeatId, cardIds): Legality (off-turn legal; game_over rejected); request shape { type: "reorder", cardIds: string[] }; reorder not recorded in history
From apps/worker/src/room-state.ts: applyGameAction(state, actorSeatId, actionId, request, now): RoomResult — dedup on seat.lastAppliedActionId before status gate; status !== "in_progress" → { ok:false, reason:"bad_request" }; adapter errors → detail via mapAdapterError
apps/worker/src/room-state.test.ts helpers: startedThreeSeatRoom(seed), legalActionFor(game)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Property suites drive reorder and prove slot-preserving draws</name>
  <files>packages/rules/src/hanabi/test-support.ts, packages/rules/src/hanabi/conservation.property.test.ts, packages/rules/src/hanabi/redaction.property.test.ts, packages/rules/src/hanabi/termination.property.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/test-support.ts
    - packages/rules/src/hanabi/conservation.property.test.ts
    - packages/rules/src/hanabi/redaction.property.test.ts
    - packages/rules/src/hanabi/termination.property.test.ts
    - packages/rules/src/hanabi/legality.ts (canReorder from 06.1-01)
  </read_first>
  <action>
    Per D-23. In test-support.ts add `export function enumerateReorderActions(state: HanabiState, seatId: string): HanabiAction[]` that builds candidate permutations of that seat's hand ids — reversed, rotated left by one, and swap of first two — de-duplicates identical orders, and keeps only those where `canReorder(state, seatId, ids).legal` (no local rule re-derivation, T-03-24). In each of the three property files, before choosing the main action at each step, when `index % 3 === 0` pick `reorderSeat = state.seatIds[index % state.seatIds.length]` (frequently NOT the active seat), take `enumerateReorderActions(state, reorderSeat)[index % n]` if any, apply via `hanabiGame.applyAction(state, reorderSeat, action)`, and increment a `reorderSteps` counter on success; then continue the existing main-action logic unchanged. Assertions to add: conservation — run the existing token/card conservation asserts after each reorder, and after each successful play/discard whose history gained a "draw" entry, assert the actor's hand index that held the played/discarded card id before the action now holds the drawn card id (capture the index from the pre-action state); redaction — run assertNoLeaksForEverySeat after each reorder, and assert that for every observer seat the reordered seat's card-id sequence in the view (yourHand for the actor, otherHands entry for others) equals the submitted cardIds; termination — reorders do NOT increment `turn`, and assert turnIndex, clueTokens, fuses, finalTurnsRemaining, deck length, and history length are identical before/after each reorder. Every file adds `expect(reorderSteps).toBeGreaterThan(0)` after fc.assert (non-vacuous). Keep numRuns at 200.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project rules property</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function enumerateReorderActions" packages/rules/src/hanabi/test-support.ts` matches and its body references `canReorder`
    - `grep -c "reorderSteps" packages/rules/src/hanabi/conservation.property.test.ts packages/rules/src/hanabi/redaction.property.test.ts packages/rules/src/hanabi/termination.property.test.ts` reports >= 2 in each file
    - conservation.property.test.ts contains a vacated-index assertion referencing the "draw" history entry
    - `npx vitest run --project rules property` exits 0
  </acceptance_criteria>
  <done>All three property suites exercise reorders (proven non-vacuous) and conservation proves slot-preserving draws.</done>
</task>

<task type="auto">
  <name>Task 2: Projection order and room-layer reorder flow</name>
  <files>packages/rules/src/hanabi/projection.test.ts, apps/worker/src/room-state.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/projection.test.ts
    - packages/rules/src/hanabi/projection.ts
    - apps/worker/src/room-state.test.ts (startedThreeSeatRoom, legalActionFor, D-09 dedup describe)
    - apps/worker/src/room-state.ts (applyGameAction)
  </read_first>
  <action>
    Per D-18, D-22. In projection.test.ts add describe "reorder projection": create a 3-seat base game, apply a reversed-order reorder by a NON-active seat, then assert toHanabiPlayerView for the actor lists yourHand ids in the submitted order (all hidden: true, no suit/rank keys), and each other seat's otherHands entry for the actor lists the same id order with visible identities matching the state; also assert a slot-preserving discard by the active seat shows the drawn card id at the vacated index in every observer's view. In room-state.test.ts add describe "D-22: reorder through the room layer": (a) off-turn seat reorder via applyGameAction with actionId "reorder-1" returns ok, turn/active seat unchanged, seat.lastAppliedActionId === "reorder-1"; (b) repeating "reorder-1" with a different cardIds returns ok with state deep-equal to the first result (dedup, not re-applied); (c) a cardIds array with a duplicate id returns { ok: false, reason: "bad_request", detail: "card_not_in_hand" }; (d) a payload with an extra key returns detail "invalid_action"; (e) a room with status "ended" refuses a fresh reorder with reason "bad_request".
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project rules projection && npx vitest run --project worker room-state</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reorder projection" packages/rules/src/hanabi/projection.test.ts` and `grep -n "reorder through the room layer" apps/worker/src/room-state.test.ts` match
    - Cases (a)-(e) exist and pass; `npx vitest run --project worker room-state` exits 0
    - `git diff --stat` for this plan shows no changes under packages/rules/src/hanabi/*.ts except test files and test-support.ts, and none in apps/worker/src/room-state.ts
  </acceptance_criteria>
  <done>Reorder order is proven at the projection chokepoint and through the room actor's idempotency and status gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter state → toPlayerView → wire | per-seat redaction chokepoint |
| client actionId → room dedup | retries must not double-apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-19 | Information Disclosure | toHanabiPlayerView after reorder/draw | mitigate | Redaction property suite runs the leak checker after every reorder and draw across 200 games per variant |
| T-06.1-20 | Tampering | applyGameAction reorder | mitigate | Room tests prove non-permutation and extra-key payloads are refused with typed detail |
| T-06.1-21 | Repudiation/Replay | actionId dedup | mitigate | Test proves a replayed reorder actionId is not re-applied |
</threat_model>

<verification>
- `npx vitest run --project rules` and `npx vitest run --project worker room-state` green
</verification>

<success_criteria>
Conservation, redaction, termination, projection, and room-layer tests all cover reorder and slot-preserving draw with non-vacuous evidence.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-06-SUMMARY.md` when done
</output>
