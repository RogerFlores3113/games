---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 15
type: execute
wave: 7
depends_on: ["06.1-05", "06.1-07", "06.1-08", "06.1-13", "06.1-14"]
files_modified:
  - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
autonomous: false
requirements: [ART-01, ART-02, NOTE-01, NOTE-02, HAND-01, HAND-02, HAND-03, AUD-01]

must_haves:
  truths:
    - "D-31: npm test, npx playwright test, and per-package tsc are green before sign-off is requested"
    - "D-31: the owner signs off on art, background image, notes, drag feel, and sounds, recorded verbatim"
    - "D-14: the owner approves the specific background image (or requests a replacement)"
    - "Recorded UI-SPEC deviations (note row under marks, any card-size/gap tightening) are presented for owner review"
  artifacts:
    - path: ".planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md"
      provides: "phase-end owner sign-off"
      contains: "Table polish sign-off"
  key_links:
    - from: "06.1-HUMAN-UAT.md"
      to: "phase verification"
      via: "result: pass with verbatim reply"
      pattern: "result: pass"
---

<objective>
Run the full automated phase gate, then get the owner's visual and audio sign-off on the finished table and record it verbatim.

Purpose: D-31 phase gate. Art/sound/drag feel cannot be unit-tested; the owner is the acceptance check.
Output: 06.1-HUMAN-UAT.md with the gate result and the owner's reply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
@.planning/phases/06-game-interface/06-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gate and sign-off checklist</name>
  <files>.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
    - .planning/phases/06-game-interface/06-HUMAN-UAT.md (format)
    - every 06.1-*-SUMMARY.md "Deviations" section
    - apps/web/public/backgrounds/CREDITS.md
  </read_first>
  <action>
    Per D-31. With no stray dev servers on ports 3100/8787, run sequentially from the repo root: `npm test`, `npx playwright test`, `npx tsc -b apps/web packages/rules packages/schema apps/worker`. All must exit 0; if one fails, fix it inside the owning plan's files (or stop and report) — never request sign-off on a red gate. Then start `wrangler dev` and `next dev -p 3100` as the Playwright config does and confirm http://localhost:3100 responds. Append to 06.1-HUMAN-UAT.md a section "### 2. Table polish sign-off (D-31; ART-01, ART-02, NOTE-01, NOTE-02, HAND-01, HAND-02, HAND-03, AUD-01)" with: the gate commands and their pass result; the background image author/source/license lines from CREDITS.md; a deviations list collected from every 06.1 SUMMARY (at minimum: own-hand note row stacked under the marks zone instead of the marks row's right edge; told clue marks shown as rings on pips instead of a separate row; discard overlay reopens after refresh when the player left it expanded (literal D-13 reading); canReorder refuses reorders after game over; any card-size or gap tightening); the how-to-verify steps from Task 2; and "result: awaiting owner reply".
  </action>
  <verify>
    <automated>cd /home/rflor/games && npm test && npx playwright test && npx tsc -b apps/web packages/rules packages/schema apps/worker && grep -c "Table polish sign-off" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx playwright test`, and `npx tsc -b apps/web packages/rules packages/schema apps/worker` all exit 0
    - 06.1-HUMAN-UAT.md contains "Table polish sign-off", the CREDITS.md author and license, a deviations list, and "result: awaiting owner reply"
  </acceptance_criteria>
  <done>The gate is green and the sign-off request is written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner visual and audio sign-off</name>
  <files>.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
  </read_first>
  <action>Blocking owner gate — never auto-approved. With the gate green and servers from Task 1 running, present the what-built and how-to-verify below and wait for the owner's reply. Do not change code during this task.</action>
  <verify>
    <automated>cd /home/rflor/games && grep -c "Table polish sign-off" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The owner has replied with "approved" or a list of requested changes
  </acceptance_criteria>
  <done>The owner's D-31 reply is in hand.</done>
  <what-built>The polished table: firework-burst art on every card, stack, discard, clue button, and pip; neutral card backs on your own hand; clue marks above every card; private notes under the marks on your own cards; drag to reorder (anytime), drag to the stacks to play and to the discard pile to discard (your turn), with Play/Discard buttons still there; drawn cards landing in the vacated slot; fly-to motion for departing cards; compact discard pile with a full-art overlay; synthesized tile-tap sounds with mute and volume; a city-at-night background. Automated gate is green. Web http://localhost:3100, worker localhost:8787.</what-built>
  <how-to-verify>
    1. Open http://localhost:3100, create a room (try Base, and Rainbow if you like), join from a private window, start the game
    2. Art: every suit's burst is distinct (try squinting / ignoring colour), rank counts read at a glance, your own cards show only the card back
    3. Background: the city-at-night image looks right and cards/text stay easy to read (say if you want a different image)
    4. Marks and notes: give a clue — marks appear above the touched cards on both screens; type a short note on one of your cards, refresh, it is still there; play that card, its note is gone
    5. Drag: drag one of your cards onto another slot (also when it is not your turn) — the other window shows the new order, also after refresh; on your turn drag a card onto the fireworks to play and onto the discard pile to discard; drop somewhere else and it snaps back; while dragging off-turn, the zones say why they are disabled
    6. Draws: after a play or discard the new card appears in the same slot on both screens, and the other window sees the card fly to its destination
    7. Discard pile: compact by default; the toggle opens the full-art overlay; Esc closes it
    8. Sounds: click anywhere once, then clue / play / discard / misplay / complete a stack if possible — each sound is distinct and soft; misplay plays only the fuse sound; mute and volume work and are remembered after refresh; refreshing mid-game plays no burst of old sounds
    9. Review the listed deviations in 06.1-HUMAN-UAT.md and say whether each is acceptable
  </how-to-verify>
  <resume-signal>Type "approved" or describe what to change (art, image, notes, drag feel, sounds, or deviations)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the owner's sign-off</name>
  <files>.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
  </read_first>
  <action>
    Paste the owner's reply verbatim under a "## Owner's verbatim reply (table polish)" heading. If the owner approved, set section 2 "result: pass", frontmatter status "complete", and fill the Summary counts. If the owner requested changes, set "result: issues", list each requested change under "## Gaps" as a discrete item (so `/gsd:plan-phase 06.1 --gaps` can plan them), and leave status "partial". Do not implement requested changes in this plan.
  </action>
  <verify>
    <automated>cd /home/rflor/games && grep -cE "result: (pass|issues)" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md && grep -c "Owner's verbatim reply (table polish)" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 06.1-HUMAN-UAT.md section 2 has the verbatim reply and "result: pass" or "result: issues" with a Gaps list
  </acceptance_criteria>
  <done>The owner's D-31 decision is recorded verbatim and the phase is either signed off or has explicit gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev servers → owner browser | localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-44 | Repudiation | owner sign-off | mitigate | Reply recorded verbatim; red gate blocks the sign-off request |
| T-06.1-45 | Information Disclosure | leak regression slipping to sign-off | mitigate | Full `npm test` (redaction/leak property suites, own-hand scans) must be green before the checkpoint |
</threat_model>

<verification>
- Gate green; 06.1-HUMAN-UAT.md records the owner's verbatim decision
</verification>

<success_criteria>
Phase 6.1 is closed by a green automated gate plus a recorded owner sign-off (or explicit gaps for a follow-up plan).
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-15-SUMMARY.md` when done
</output>
