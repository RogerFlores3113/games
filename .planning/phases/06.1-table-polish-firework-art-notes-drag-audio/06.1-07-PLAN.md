---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 07
type: execute
wave: 2
depends_on: ["06.1-02"]
files_modified:
  - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
  - apps/web/lib/suit-visuals.ts
  - apps/web/components/hanabi/FireworkCard.tsx
autonomous: false
requirements: [ART-01]

must_haves:
  truths:
    - "ROADMAP note / D-08: the owner reviewed all seven burst silhouettes, rank-count layouts, and the card back on the preview page BEFORE they are built into the table's cards"
    - "D-09 / Open Question 3: the owner chose burst-count-plus-numeral or numeral-only for 48x64 stack/discard cards"
    - "The owner's reply is recorded verbatim"
  artifacts:
    - path: ".planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md"
      provides: "owner's verbatim burst-art approval and small-size decision"
      contains: "Burst art preview"
  key_links:
    - from: "06.1-HUMAN-UAT.md burst decision"
      to: "06.1-09 / 06.1-10 card composition"
      via: "showBurstCount choice for 48x64 cards"
      pattern: "showBurstCount"
---

<objective>
Owner gate: show the burst shapes on the cheap preview page and get approval (or change requests) before any card, stack, or discard is rebuilt with them.

Purpose: Explicit ROADMAP instruction ("show the owner the burst shapes before building them into cards") and D-31 art sign-off. If the owner requests changes, they are made in suit-visuals.ts / FireworkCard.tsx and re-shown before this plan completes.
Output: 06.1-HUMAN-UAT.md with the verbatim reply and the 48x64 decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-02-SUMMARY.md
@.planning/phases/06-game-interface/06-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the preview and prepare the UAT record</name>
  <files>.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-game-interface/06-HUMAN-UAT.md (format to mirror)
    - apps/web/app/dev/fireworks/page.tsx
    - apps/web/lib/suit-visuals.ts (silhouette descriptors to list for the owner)
  </read_first>
  <action>
    Start the web dev server in the background on port 3100 (`npm run dev --workspace apps/web -- -p 3100`) unless one is already answering, and confirm `curl -s -o /dev/null -w "%{http_code}" http://localhost:3100/dev/fireworks` returns 200. Create 06.1-HUMAN-UAT.md with frontmatter (status: in_progress, phase: 06.1-table-polish-firework-art-notes-drag-audio, source: [06.1-CONTEXT.md D-08, D-09, D-10, D-31]) and a section "### 1. Burst art preview (ART-01, D-08..D-10)" listing: the preview URL, a one-line description of each suit's silhouette from SUIT_VISUALS descriptors, what to check (distinct in the grayscale section, Rainbow reads without colour, rank counts 1-5 legible at 64x84, whether 48x64 should use burst count or numeral only, card back reveals nothing), then "result: awaiting owner reply" and an empty "## Owner's verbatim reply" section.
  </action>
  <verify>
    <automated>cd /home/rflor/games && test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3100/dev/fireworks)" = "200" && grep -c "awaiting owner reply" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - http://localhost:3100/dev/fireworks returns HTTP 200
    - 06.1-HUMAN-UAT.md contains "Burst art preview", the preview URL, and "awaiting owner reply"
  </acceptance_criteria>
  <done>The preview is reachable and the UAT record is ready for the owner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner reviews burst shapes before they are built into cards</name>
  <files>.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
  </read_first>
  <action>Blocking owner gate — never auto-approved (workflow.auto_advance ignored). Present the what-built and how-to-verify below with the preview server from Task 1 running, then wait for the owner's reply. Do not compose burst art into cards, stacks, or discards until the reply is recorded in Task 3.</action>
  <verify>
    <automated>cd /home/rflor/games && grep -c "Burst art preview" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The owner has replied with "approved" (plus a 48x64 choice) or concrete change requests
  </acceptance_criteria>
  <done>The owner's reply to the burst preview is in hand.</done>
  <what-built>Original firework-burst silhouettes for all 7 suits (red chrysanthemum, yellow round-petal, green hollow ring, blue long/short cross-star, white sparkler, rainbow large flat burst, black double ring), rank shown as burst count plus corner numeral, and a neutral unlit-shell card back — on a dev-only preview page, including a grayscale (colour ignored) section. Nothing in the live table's cards uses the composed card faces yet.</what-built>
  <how-to-verify>
    1. Open http://localhost:3100/dev/fireworks
    2. In the "Colour ignored (grayscale)" section, confirm every suit's burst is distinguishable from every other suit's without colour, at 12px, 20px and on the 64x84 cards
    3. Confirm Rainbow reads as its own burst without any multicolour fill
    4. Check ranks 1-5 on 64x84 cards: burst count and corner numeral both readable
    5. Compare the 48x64 cards side by side: burst count + numeral vs numeral only — say which to use for stack heads and discard minis
    6. Confirm the card back looks the same for every card and hints at nothing
    7. Say whether the art feels like original firework work in the spirit of the box game (not a copy)
  </how-to-verify>
  <resume-signal>Type "approved" plus your 48x64 choice ("burst count" or "numeral only"), or describe the shape changes you want</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the owner's reply and apply any requested art changes</name>
  <files>.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
  </read_first>
  <action>
    Paste the owner's reply verbatim under "## Owner's verbatim reply" and add a line "small-card decision: showBurstCount=<true|false> at 48x64". If the owner requested shape changes, edit only apps/web/lib/suit-visuals.ts / apps/web/components/hanabi/FireworkCard.tsx to apply them, rerun `npx vitest run --project web suit-visuals firework-card-render`, record what changed under "## Changes after review", and return to Task 2 for re-review. When approved, set "result: pass" and frontmatter status to "partial" (final D-31 sign-off comes in 06.1-15).
  </action>
  <verify>
    <automated>cd /home/rflor/games && grep -c "result: pass" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md && grep -c "small-card decision: showBurstCount=" .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md && npx vitest run --project web suit-visuals firework-card-render</automated>
  </verify>
  <acceptance_criteria>
    - 06.1-HUMAN-UAT.md contains the verbatim reply, "result: pass", and "small-card decision: showBurstCount="
    - Art tests still green after any requested changes
  </acceptance_criteria>
  <done>The owner has approved the burst art (verbatim record) and the small-card rendering choice is fixed for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev server → owner browser | preview only on localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-22 | Repudiation | owner approval | mitigate | Reply recorded verbatim in 06.1-HUMAN-UAT.md before downstream card work starts |
| T-06.1-23 | Information Disclosure | /dev/fireworks | accept | Static art only; production returns notFound (06.1-02) |
</threat_model>

<verification>
- 06.1-HUMAN-UAT.md shows "result: pass" with verbatim reply and small-card decision
</verification>

<success_criteria>
The burst shapes are owner-approved before 06.1-09/06.1-10 compose them into cards, stacks, and discards.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-07-SUMMARY.md` when done
</output>
