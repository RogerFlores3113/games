---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 11
type: execute
wave: 4
depends_on: ["06.1-03", "06.1-08", "06.1-09"]
files_modified:
  - apps/web/components/hanabi/NoteChip.tsx
  - apps/web/lib/note-chip-render.test.ts
  - apps/web/components/hanabi/Hand.tsx
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/lib/own-hand-source.test.ts
  - apps/web/lib/own-hand-render.test.ts
autonomous: true
requirements: [NOTE-02]

must_haves:
  truths:
    - "D-03/D-05: each own-hand card (and only own-hand cards) has a small note chip in its zone above the card; clicking it edits inline"
    - "D-06: note input is capped at 20 characters (maxLength and storage truncation)"
    - "D-02: a saved note survives a page refresh in the same browser"
    - "D-04: a note follows its card id through reorders and is deleted when the card leaves the hand; a newly drawn card starts blank; an ended game's notes are cleared"
    - "D-01: note text never reaches onAction, the socket, or the server"
  artifacts:
    - path: "apps/web/components/hanabi/NoteChip.tsx"
      exports: ["NoteChip"]
  key_links:
    - from: "apps/web/components/hanabi/NoteChip.tsx"
      to: "apps/web/lib/hanabi-notes.ts"
      via: "readNote/writeNote"
      pattern: "writeNote\\("
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "pruneNotesForSeat / clearNotesForRoom"
      via: "effect on yourHand ids and room status"
      pattern: "pruneNotesForSeat\\("
---

<objective>
Give players private per-card notes on their own hand: a chip in the note row above each own card, inline editing capped at 20 characters, browser-local persistence, and automatic cleanup when cards leave the hand.

Purpose: NOTE-02 (D-01..D-06), using the storage logic from 06.1-03 and the note row reserved by 06.1-09.
Output: NoteChip component + render test, Hand/HanabiBoard wiring, extended identity scans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md

<interfaces>
From 06.1-03 apps/web/lib/hanabi-notes.ts: NOTE_MAX_LENGTH = 20; readNote(roomCode, seatId, cardId): string; writeNote(roomCode, seatId, cardId, text): void; pruneNotesForSeat(roomCode, seatId, liveCardIds): void; clearNotesForRoom(roomCode): void
From 06.1-09 apps/web/components/hanabi/MarksZone.tsx: MarksZone({ facts, variant, scale, testId, noteSlot? }) — scale "own" renders a 20px row `{testId}-note-row` containing noteSlot
From apps/web/components/hanabi/Hand.tsx OwnHand: renders `<MarksZone facts={card.facts} variant={variant} scale="own" testId={"marks-zone-slot-" + (i + 1)} />` above each OwnHandCard; OwnHand body may reference only card.id and card.facts (own-hand-source.test)
From @games/schema RoomView: { code, status, youSeatId, seats, game } — HanabiBoard receives `view: RoomView`
UI-SPEC copy: empty chip aria-label "Add note"; input placeholder "Note…"; editing outline --color-accent; populated chip --color-surface fill, --color-border outline, --color-text label text
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NoteChip component</name>
  <files>apps/web/components/hanabi/NoteChip.tsx, apps/web/lib/note-chip-render.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-notes.ts
    - apps/web/components/hanabi/MarksZone.tsx
    - apps/web/components/hanabi/OwnHandCard.tsx (focus ring classes, touch-target pattern)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md (Notes, Copywriting, Color note chip states)
  </read_first>
  <behavior>
    - Server render of NoteChip({ roomCode:"ABCD", seatId:"s1", cardId:"c1", slotNumber:1 }) outputs a <button type="button"> with aria-label "Add note" and data-testid "note-chip-slot-1" (initial state empty before mount effects)
    - NoteChip.tsx source imports nothing from room-socket, room-store, nanoid, or any onAction/send prop; its props type has exactly roomCode, seatId, cardId, slotNumber
    - Source contains maxLength={NOTE_MAX_LENGTH} and placeholder "Note…"
    - Source stops pointerdown propagation on the chip and input (so a future hand drag never starts from the chip)
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-05, D-06 (discretion: chip styling). Create NoteChip.tsx ("use client") exporting `NoteChip({ roomCode, seatId, cardId, slotNumber })`. State: `text` ("" initially; loaded with readNote in a mount/cardId effect so SSR and first client render match), `editing` boolean, `draft`. Not editing: a <button> (data-testid `note-chip-slot-{n}`, height 20px, full row width, invisible hit-area padding so the effective target is >= 44px without growing layout — e.g. a relatively positioned button with an absolutely positioned `::before`-equivalent span inset -12px) showing a lucide Pencil icon (aria-hidden) when empty with aria-label "Add note", or the note text at --text-label weight 600 with `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` and aria-label `Edit note: ${text}` when populated. Click → editing. Editing: an <input> (data-testid `note-input-slot-{n}`, maxLength={NOTE_MAX_LENGTH}, placeholder "Note…", autoFocus) absolutely positioned over the row with min-width 160px and z-index above neighbours so layout never shifts, outline 2px var(--color-accent). Enter or blur → writeNote(roomCode, seatId, cardId, draft), set text = draft.slice(0, 20), exit editing; Escape → discard draft, exit editing. onPointerDown={(e) => e.stopPropagation()} on both button and input. Write note-chip-render.test.ts per behaviour bullets (renderToStaticMarkup + readFileSync source checks).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web note-chip-render hanabi-notes && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "maxLength={NOTE_MAX_LENGTH}" apps/web/components/hanabi/NoteChip.tsx` matches
    - `grep -nE "room-socket|room-store|nanoid|onAction|send\(" apps/web/components/hanabi/NoteChip.tsx` returns nothing
    - `grep -n "stopPropagation" apps/web/components/hanabi/NoteChip.tsx` matches
    - note-chip-render.test.ts passes; tsc exits 0
  </acceptance_criteria>
  <done>A local-only, 20-char-capped inline note chip exists with render/source tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notes into own hand with lifecycle cleanup</name>
  <files>apps/web/components/hanabi/Hand.tsx, apps/web/components/hanabi/HanabiBoard.tsx, apps/web/lib/own-hand-source.test.ts, apps/web/lib/own-hand-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Hand.tsx
    - apps/web/components/hanabi/HanabiBoard.tsx
    - apps/web/lib/own-hand-source.test.ts
    - apps/web/lib/own-hand-render.test.ts
    - apps/web/components/hanabi/NoteChip.tsx (Task 1)
  </read_first>
  <action>
    Per D-02, D-03, D-04. Hand.tsx: add `roomCode: string` to OwnHandProps; in OwnHand pass `noteSlot={youSeatId !== null ? <NoteChip key={card.id} roomCode={roomCode} seatId={youSeatId} cardId={card.id} slotNumber={i + 1} /> : null}` to each own MarksZone (OwnHand body still references only card.id and card.facts). TeammateHand gets no note affordance (D-03). HanabiBoard.tsx: pass `roomCode={view.code}` to OwnHand; add an effect keyed on `game?.yourHand.map((c) => c.id).join(",")`, `view.youSeatId`, `view.status`, `view.code` that: if status is "ended" calls clearNotesForRoom(view.code); else if youSeatId is non-null and the hand has >= 1 card calls pruneNotesForSeat(view.code, youSeatId, currentIds) (deletes notes for cards that left the hand, including while the tab was closed, D-04). Nothing note-related is passed to onAction/act. own-hand-source.test.ts: add NOTE_CHIP_PATH and scan NoteChip.tsx with PROPERTY_ACCESS, DESTRUCTURING, FORBIDDEN_TOKENS; the existing OwnHand body card-members assertion must still equal ["facts","id"]. own-hand-render.test.ts: supply roomCode in the OwnHand props fixture; keep the byte-identical visible-vs-stripped assertion; add an assertion that the markup contains "note-chip-slot-1" and that TeammateHand markup contains no "note-chip".
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web && npx playwright test e2e/start-game.spec.ts -g "UI-11"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "<NoteChip" apps/web/components/hanabi/Hand.tsx` matches exactly once and only inside OwnHand
    - `grep -n "pruneNotesForSeat(" apps/web/components/hanabi/HanabiBoard.tsx` and `grep -n "clearNotesForRoom(" apps/web/components/hanabi/HanabiBoard.tsx` match
    - `grep -n "NOTE_CHIP_PATH" apps/web/lib/own-hand-source.test.ts` matches
    - `npx vitest run --project web` exits 0; UI-11 fit test exits 0
  </acceptance_criteria>
  <done>Own cards carry persistent private notes that follow card ids and are cleaned up when cards leave or the game ends, with identity scans extended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed note text → localStorage | user input stored locally only |
| client → server | notes must never cross (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-32 | Information Disclosure | NoteChip | mitigate | No socket/store/onAction import or prop (grep-gated); e2e in 06.1-14 asserts note text never appears in sent WebSocket frames |
| T-06.1-33 | Tampering (XSS) | note rendering | mitigate | Note rendered as React text child only (auto-escaped); no dangerouslySetInnerHTML; capped at 20 chars |
| T-06.1-34 | Information Disclosure | own-hand identity | mitigate | NoteChip receives only ids; own-hand source scan extended to NoteChip.tsx |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0; UI-11 green
</verification>

<success_criteria>
NOTE-02 works end to end in the browser with D-01..D-06 honoured and the identity boundary still enforced.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-11-SUMMARY.md` when done
</output>
