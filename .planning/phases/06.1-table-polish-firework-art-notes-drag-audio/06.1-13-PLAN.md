---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 13
type: execute
wave: 6
depends_on: ["06.1-04", "06.1-12"]
files_modified:
  - apps/web/lib/hanabi-flyto-logic.ts
  - apps/web/lib/hanabi-flyto-logic.test.ts
  - apps/web/components/hanabi/FlyToLayer.tsx
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [HAND-02, HAND-03]

must_haves:
  truths:
    - "D-21: when a card leaves any hand by play, misplay, or discard, a short fly-to animation runs from its slot to the matching stack or the discard pile on every screen"
    - "D-21: the player who dragged that card to the zone does not see a second animation; a Play/Discard button action still animates on the actor's screen"
    - "D-21: under prefers-reduced-motion no ghost animates (the stack/discard update remains the signal)"
    - "D-29 analogue: load, refresh, and reconnect catch-up produce no fly-to animations"
    - "D-23: the drawn card is rendered in the vacated slot (engine order) while the ghost flies"
  artifacts:
    - path: "apps/web/lib/hanabi-flyto-logic.ts"
      exports: ["FlyToEvent", "flyToEventsForTransition"]
    - path: "apps/web/components/hanabi/FlyToLayer.tsx"
      exports: ["FlyToLayer"]
  key_links:
    - from: "apps/web/lib/hanabi-flyto-logic.ts"
      to: "apps/web/lib/hanabi-live-history.ts"
      via: "liveHistoryEntries"
      pattern: "liveHistoryEntries"
    - from: "apps/web/components/hanabi/FlyToLayer.tsx"
      to: "useHandDrag droppedCardIdsRef"
      via: "suppressedCardIds prop"
      pattern: "suppressedCardIds"
---

<objective>
Add the fly-to animation D-21 describes: departing cards visibly travel to the stacks or discard pile on every screen, except the dragging player's own drop, respecting reduced motion and never replaying on load or reconnect.

Purpose: Makes plays/discards (and the slot-preserving draw, HAND-03) legible to teammates now that order matters.
Output: Pure event derivation + test, FlyToLayer component, board wiring, CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md

<interfaces>
From 06.1-04 apps/web/lib/hanabi-live-history.ts: liveHistoryEntries(prevHistory | null, nextHistory): HistoryEntry[]
From apps/web/lib/hanabi-visual-logic.ts: HistoryEntry (play: {cardId, seatId, suit, rank, success}; discard: {cardId, seatId, suit, rank})
From 06.1-12 useHandDrag: droppedCardIdsRef: MutableRefObject<Set<string>> (ids this client dropped onto play/discard)
From 06.1-02: FireworkCardFace({ suit, rank, width, height, exposeSuit?, numeralSize? })
DOM anchors: own card columns `[data-card-id="<id>"]` (06.1-12); teammate cards `[data-testid="other-hand-card-<id>"]`; stacks `[data-testid="played-stack-<suit>"]`; discard `[data-testid="discard-pile"]`
HanabiBoard: `reconnecting` prop; `game` HanabiView | null; drag = useHandDrag(...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fly-to event derivation</name>
  <files>apps/web/lib/hanabi-flyto-logic.ts, apps/web/lib/hanabi-flyto-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-live-history.ts
    - apps/web/lib/hanabi-audio-cues.ts (sibling transition helper style)
    - apps/web/lib/hanabi-visual-logic.ts (HistoryEntry)
  </read_first>
  <behavior>
    - flyToEventsForTransition(null, next, new Set()) → []
    - a live successful play entry → [{ cardId, seatId, suit, rank, destination: "stack" }]
    - a live misplay (success false) → destination "discard"
    - a live discard → destination "discard"
    - clue and draw entries → no events
    - an entry whose cardId is in suppressedCardIds → no event (dragging player's own drop)
    - two live entries → two events in history order
  </behavior>
  <action>
    Per D-21. Create hanabi-flyto-logic.ts exporting `type FlyToEvent = { cardId: string; seatId: string; suit: Suit; rank: Rank; destination: "stack" | "discard" }` and `flyToEventsForTransition(prevHistory: readonly HistoryEntry[] | null, nextHistory: readonly HistoryEntry[], suppressedCardIds: ReadonlySet<string>): FlyToEvent[]` built on liveHistoryEntries. Pure; no DOM. Tests per behaviour bullets.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-flyto-logic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "liveHistoryEntries" apps/web/lib/hanabi-flyto-logic.ts` matches; `grep -nE "document|window" apps/web/lib/hanabi-flyto-logic.ts` returns nothing
    - All behaviour bullets pass
  </acceptance_criteria>
  <done>Which cards fly where is a tested pure function honouring suppression and no-catch-up.</done>
</task>

<task type="auto">
  <name>Task 2: FlyToLayer component and board wiring</name>
  <files>apps/web/components/hanabi/FlyToLayer.tsx, apps/web/components/hanabi/HanabiBoard.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/hanabi/HanabiBoard.tsx
    - apps/web/components/hanabi/useHandDrag.ts
    - apps/web/components/hanabi/FireworkCard.tsx
    - apps/web/app/globals.css (reduced-motion block)
    - apps/web/lib/hanabi-flyto-logic.ts (Task 1)
  </read_first>
  <action>
    Per D-21, D-23. Create FlyToLayer.tsx ("use client") exporting `FlyToLayer({ game, reconnecting, suppressedCardIds })` where suppressedCardIds is the MutableRefObject<Set<string>> from useHandDrag. Keep `rectsRef: Map<cardId, DOMRect>` and `prevHistoryRef` (null until first game). In a useLayoutEffect on [game]: (1) if reconnecting or prevHistoryRef is null, just snapshot and set baseline; (2) otherwise compute events = flyToEventsForTransition(prevHistoryRef.current, game.history, suppressedCardIds.current), then remove consumed ids from the suppression set; for each event take the source rect from rectsRef (the previous commit's snapshot; skip if missing) and the destination rect from `played-stack-<suit>` or `discard-pile`; unless `window.matchMedia("(prefers-reduced-motion: reduce)").matches`, push a ghost { key, event, from, to } into state; (3) re-snapshot rects for every `[data-card-id]` and `[data-testid^="other-hand-card-"]` element (derive id from the attribute) into rectsRef; set prevHistoryRef to game.history. An effect on [reconnecting] resets prevHistoryRef to null while reconnecting. Render a `fixed inset-0 pointer-events-none` container (data-testid "fly-to-layer", z-index above the board, below overlays) with each ghost as an absolutely positioned 48x64 `FireworkCardFace` (exposeSuit false — history already made the identity public) with data-testid "fly-to-ghost", starting at `from` left/top and, on the next animation frame, transformed to the `to` rect centre with class `fly-to-ghost`; remove each ghost after 500ms (timer refs cleared on unmount). globals.css: `.fly-to-ghost { transition: transform 450ms ease-in-out, opacity 450ms ease-in; }` and in the prefers-reduced-motion block `.fly-to-ghost { transition: none; display: none; }`. HanabiBoard: render `<FlyToLayer game={game} reconnecting={reconnecting} suppressedCardIds={drag.droppedCardIdsRef} />` inside the in-game main, before EndOverlay. Verify fit and existing specs are unaffected (the layer is fixed and pointer-events none).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web && npx vitest run --project web && npx playwright test e2e/hanabi-realtime.spec.ts e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "<FlyToLayer" apps/web/components/hanabi/HanabiBoard.tsx` matches
    - `grep -n "prefers-reduced-motion" apps/web/components/hanabi/FlyToLayer.tsx` matches and `grep -n "fly-to-ghost" apps/web/app/globals.css` shows the transition rule plus the reduced-motion override
    - `grep -n "pointer-events-none" apps/web/components/hanabi/FlyToLayer.tsx` matches
    - `npx vitest run --project web` and the realtime/start-game Playwright specs exit 0
  </acceptance_criteria>
  <done>Departing cards fly to their destination on every screen except the dragger's own, silently degrade under reduced motion, and never replay on load or reconnect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public history entries → animation | uses already-public identities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-39 | Information Disclosure | ghost card face | mitigate | Ghost identity comes only from play/discard history entries (public after the action); nothing is read from own-hand DOM beyond rects |
| T-06.1-40 | Denial of Service | ghost accumulation | mitigate | Ghosts removed after 500ms; timers cleared on unmount; no ghosts on catch-up |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0; realtime/start-game Playwright green
</verification>

<success_criteria>
D-21 fly-to motion works on every screen with the dragger exception and reduced-motion/no-catch-up safeguards.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-13-SUMMARY.md` when done
</output>
