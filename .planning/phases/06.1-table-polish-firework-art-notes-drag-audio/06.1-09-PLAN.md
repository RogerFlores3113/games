---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 09
type: execute
wave: 3
depends_on: ["06.1-02", "06.1-07"]
files_modified:
  - apps/web/components/hanabi/CandidateStrip.tsx
  - apps/web/components/hanabi/MarksZone.tsx
  - apps/web/components/hanabi/OwnHandCard.tsx
  - apps/web/components/hanabi/TeammateCard.tsx
  - apps/web/components/hanabi/Hand.tsx
  - apps/web/lib/own-hand-source.test.ts
  - apps/web/lib/own-hand-render.test.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [ART-01, NOTE-01]

must_haves:
  truths:
    - "D-07: clue marks (told suits/ranks, candidate pips with ruled-out values struck) render in a fixed 28px zone directly above every card, own hand and teammates alike, and no longer on the card face"
    - "D-07/UI-05: automatic ruled-out tracking still shows; a card narrowed to one suit/rank still exposes confirmed-suit / confirmed-rank inside its marks zone"
    - "D-08/D-09/D-12: teammate face-up cards show the firework burst face (burst count + corner numeral) with suit in both glyph and hue"
    - "D-10: every own-hand card body shows the identical neutral card back; own-hand markup for visible vs identity-stripped cards stays byte-identical"
    - "D-15 carried: OwnHandCard, MarksZone, CandidateStrip never read suit/rank from a card (source scan covers MarksZone)"
    - "Phase 6 D-01: five players still fit 1280x720 without scrolling"
  artifacts:
    - path: "apps/web/components/hanabi/MarksZone.tsx"
      exports: ["MarksZone"]
      provides: "28px marks zone (+ 20px own-hand note row slot)"
  key_links:
    - from: "apps/web/components/hanabi/Hand.tsx OwnHand/TeammateHand"
      to: "MarksZone"
      via: "rendered above each card with facts={card.facts}"
      pattern: "<MarksZone"
    - from: "apps/web/components/hanabi/TeammateCard.tsx"
      to: "FireworkCardFace"
      via: "card-identity wrapper"
      pattern: "FireworkCardFace"
---

<objective>
Move clue marks off the card faces into a fixed zone above every card, and rebuild the card bodies with the owner-approved art: firework faces for teammates, the neutral card back for your own hand.

Purpose: NOTE-01 (D-07) and ART-01 card faces (D-08..D-10). The own-hand zone also reserves a 20px note row that 06.1-11 fills, so the layout fit is verified once here.
Output: MarksZone component, compact CandidateStrip, reworked card components and Hand composition, extended identity-boundary tests, updated e2e locators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md

<interfaces>
From 06.1-02 apps/web/components/hanabi/FireworkCard.tsx:
- FireworkCardFace({ suit, rank, width, height, exposeSuit?, showBurstCount?, numeralSize? }) — data-glyph once on wrapper when exposeSuit
- FireworkCardBack({ width, height }) — no identity props
From apps/web/lib/hanabi-visual-logic.ts: CardFacts; CandidateDisplay { suits: {suit, possible}[]; ranks: {rank, possible}[]; confirmedSuit; confirmedRank; positiveMarks: ({type:"color"; suit} | {type:"rank"; rank})[] }; candidateDisplayFor(facts, variant); luminosityStepFor(facts)
From apps/web/components/hanabi/Hand.tsx: OwnHand({ cards, variant, youSeatId, connected, isYourTurn, turnText, selectedCardId, justCluedIds, disabled, onSelectCard }) renders `own-band` > header column (turn-indicator, "Your hand", SeatStatus) + `own-hand` row of OwnHandCard (testid own-hand-slot-{n}); TeammateHand renders `other-hand-{seatId}` with TeammateCard (testid other-hand-card-{id})
own-hand-source.test.ts constraints on OwnHand body: card members used must be exactly ["facts","id"]; must contain `facts={card.facts}`; FORBIDDEN_TOKENS exposeSuit/data-suit/card-identity/data-glyph must not appear in OwnHandCard/CandidateStrip sources
e2e/start-game.spec.ts currently locates marks INSIDE `own-hand-slot-N` (confirmed-rank, confirmed-suit, positive-marks, rank-pips/suit-pips) around lines 160-185, and asserts `[data-testid="card-identity"] [data-glyph]` count 1 per teammate card (line ~113)
Owner small-card decision: read "small-card decision: showBurstCount=" in 06.1-HUMAN-UAT.md (applies to 48x64 only; not used in this plan's 64x84/88x112 cards)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarksZone and compact CandidateStrip, with identity-boundary scan</name>
  <files>apps/web/components/hanabi/CandidateStrip.tsx, apps/web/components/hanabi/MarksZone.tsx, apps/web/lib/own-hand-source.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/CandidateStrip.tsx
    - apps/web/lib/own-hand-source.test.ts
    - apps/web/lib/hanabi-visual-logic.ts (CandidateDisplay, candidateDisplayFor)
    - apps/web/components/hanabi/SuitGlyph.tsx
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Pitfall 3, Pitfall 5)
  </read_first>
  <behavior>
    - own-hand-source.test scans MarksZone.tsx: no PROPERTY_ACCESS, no DESTRUCTURING of suit/rank, no FORBIDDEN_TOKENS
    - own-hand-source.test asserts MarksZone's props type has no `card` member (source contains no "card:" prop and no "HanabiCardView" import)
    - CandidateStrip still passes the existing no-PROPERTY_ACCESS scan and FORBIDDEN_TOKENS check
  </behavior>
  <action>
    Per D-07 and UI-SPEC Marks zone (28px fixed). Rework CandidateStrip into a two-row compact layout that fits 28px: row 1 `data-testid="suit-pips"` with SuitGlyph pips (size 10 for scale "own", 9 for "teammate", gap 1-2px), row 2 `data-testid="rank-pips"` numerals at --text-label size with `lineHeight: 1` (14px row, no new font size); ruled-out pips keep opacity 0.25 + strike overlay. Positive (told) marks no longer get their own row: derive told suits/ranks by destructuring `display.positiveMarks` (destructuring is allowed here, property access is not) and mark the matching pip with `data-told="true"`, a `0 0 0 1px var(--color-card-glow)` box-shadow ring, and an accessible title "Told <Label>" / aria-label "Told <rank>". When `display.confirmedSuit` / `confirmedRank` is non-null, the single possible pip's wrapper carries `data-testid="confirmed-suit"` / `data-testid="confirmed-rank"`. Remove the `positive-marks` row. Create MarksZone.tsx exporting `MarksZone({ facts, variant, scale, testId, noteSlot })` where facts: CardFacts, scale: "own" | "teammate", noteSlot?: ReactNode: it calls candidateDisplayFor(facts, variant) and renders a column whose first child is a `height: 28px; overflow: hidden` box (data-testid={testId}) holding CandidateStrip, and — only for scale "own" — a second fixed `height: 20px` row (data-testid={testId + "-note-row"}) holding `noteSlot` (empty now; 06.1-11 fills it). Width matches the card width (88 own, 64 teammate). Header comment: marks-zone relocation per D-07; note row stacked under the marks is a recorded deviation from UI-SPEC "right edge" because an 88px card cannot fit pips and a readable note side by side (owner reviews at D-31). Extend own-hand-source.test.ts with MARKS_ZONE_PATH scanned per behaviour bullets.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web own-hand-source && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MARKS_ZONE_PATH" apps/web/lib/own-hand-source.test.ts` matches
    - `grep -n "positive-marks" apps/web/components/hanabi/CandidateStrip.tsx` returns nothing; `grep -n "data-told" apps/web/components/hanabi/CandidateStrip.tsx` matches
    - `grep -n "28" apps/web/components/hanabi/MarksZone.tsx` shows the fixed zone height
    - own-hand-source tests pass; tsc exits 0
  </acceptance_criteria>
  <done>A fixed-height marks zone exists, told marks are folded into pips, and the identity scan covers the new component.</done>
</task>

<task type="auto">
  <name>Task 2: Card faces, card back, marks above cards, and fit</name>
  <files>apps/web/components/hanabi/OwnHandCard.tsx, apps/web/components/hanabi/TeammateCard.tsx, apps/web/components/hanabi/Hand.tsx, apps/web/lib/own-hand-render.test.ts, e2e/start-game.spec.ts</files>
  <read_first>
    - apps/web/components/hanabi/OwnHandCard.tsx
    - apps/web/components/hanabi/TeammateCard.tsx
    - apps/web/components/hanabi/Hand.tsx
    - apps/web/components/hanabi/MarksZone.tsx (Task 1)
    - apps/web/components/hanabi/FireworkCard.tsx
    - apps/web/lib/own-hand-render.test.ts
    - e2e/start-game.spec.ts (lines 80-190 and the UI-11 test)
  </read_first>
  <action>
    Per D-07, D-08, D-09, D-10, D-12. OwnHandCard: remove the confirmed glyph/rank block and the CandidateStrip; render `<FireworkCardBack width={CARD_WIDTH} height={...} />` absolutely filling the card body under the existing luminosity frame layers, keep the "Slot N" label (on a --color-surface pill so it stays legible over the back), keep button semantics, testid own-hand-slot-{n}, data-luminosity/selected/just-clued, and the "renders NO identity signal" comment; remove now-unused imports (SuitGlyph, SUIT_VISUALS, candidateDisplayFor). TeammateCard: remove the inline CandidateStrip; for a visible card render `<span data-testid="card-identity">` wrapping `<FireworkCardFace suit rank width height exposeSuit numeralSize="body" />` plus the existing sr-only "<Label> <rank>" accessible text; for a hidden card (unseated viewer) render FireworkCardBack; keep luminosity frame, preview ring, just-clued overlay, and testid other-hand-card-{id}. Hand.tsx: in OwnHand wrap each card in a flex-col container rendering `<MarksZone facts={card.facts} variant={variant} scale="own" testId={"marks-zone-slot-" + (i + 1)} />` above `OwnHandCard` (OwnHand body must still touch only card.id and card.facts); merge OwnHand's header (turn-indicator, "Your hand", SeatStatus) into a single row to recover vertical space; in TeammateHand render `<MarksZone facts={card.facts} variant={variant} scale="teammate" testId={"marks-zone-" + card.id} />` above each TeammateCard. Fit: run the UI-11 test; if it fails, first reduce OwnHandCard height 112→100 and TeammateCard height 84→78, then tighten gaps to --space-xs; record any change as a deviation. own-hand-render.test.ts: keep the byte-identical visible-vs-stripped assertion, keep the narrowed case expecting confirmed-rank (now inside marks zone), and add two assertions: the markup contains "marks-zone-slot-1", and every own-hand card-back segment is identical (split the markup on `data-testid="own-hand-slot-`, extract each segment's card-back `<svg ...</svg>` substring, and assert all are equal). e2e/start-game.spec.ts: re-point the own-hand marks assertions (~lines 160-185) from `own-hand-slot-N` descendants to `marks-zone-slot-N`, replace `positive-marks` count 0 with `[data-told="true"]` count 0, and keep the own-hand no-`[data-glyph]`/`card-identity` assertions and the teammate `card-identity [data-glyph]` count 1 assertion unchanged.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web && npx playwright test e2e/start-game.spec.ts e2e/hanabi-realtime.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FireworkCardBack" apps/web/components/hanabi/OwnHandCard.tsx` and `grep -n "FireworkCardFace" apps/web/components/hanabi/TeammateCard.tsx` match
    - `grep -n "CandidateStrip" apps/web/components/hanabi/OwnHandCard.tsx apps/web/components/hanabi/TeammateCard.tsx` returns nothing; `grep -c "<MarksZone" apps/web/components/hanabi/Hand.tsx` is 2
    - `grep -n "marks-zone-slot-" e2e/start-game.spec.ts` matches
    - `npx vitest run --project web` exits 0 (own-hand-source + own-hand-render green)
    - `npx playwright test e2e/start-game.spec.ts e2e/hanabi-realtime.spec.ts` exits 0 (UI-11 fit included), except the pre-existing RT-04 frozen-tab flake if it recurs, which must be noted in SUMMARY
  </acceptance_criteria>
  <done>Cards show firework faces / neutral backs, all clue marks sit above cards, identity tests and e2e are green, and 1280x720 fit holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HanabiView own-hand cards → DOM | own identity must not render (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-26 | Information Disclosure | MarksZone / OwnHandCard | mitigate | MarksZone takes facts only; source scan extended to MarksZone; render test keeps visible-vs-stripped own-hand markup byte-identical |
| T-06.1-27 | Information Disclosure | FireworkCardBack variation | mitigate | Render test asserts card-back segments identical across all own slots |
| T-06.1-28 | Tampering | e2e locator drift | mitigate | Locators re-pointed, not deleted; identity-absence assertions kept verbatim |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0; start-game + hanabi-realtime Playwright specs green
</verification>

<success_criteria>
NOTE-01 marks-above-card and ART-01 card faces/back are live with the D-15 boundary still machine-enforced and no-scroll fit intact.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-09-SUMMARY.md` when done
</output>
