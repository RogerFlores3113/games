---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 07
subsystem: ui-art
tags: [hanabi, art, suit-visuals, firework, dev-preview, accessibility, owner-gate]

requires:
  - phase: 06.1-table-polish-firework-art-notes-drag-audio (plan 02)
    provides: "SUIT_VISUALS burst silhouettes, burstLayoutForRank, CARD_BACK_ART, FireworkCardFace/FireworkCardBack, /dev/fireworks preview"
provides:
  - "Owner sign-off (D-31) on the burst art shapes, recorded verbatim, before any card/stack/discard composition begins"
  - "Rank-5 burst layout with a 1.5x-enlarged center burst"
  - "Rainbow suit rendered as an actual multicolour gradient (overrides D-08's single-tone rule)"
  - "No corner numeral at any card size — rank reads from burst count only, plus an accessible title/aria-label"
affects:
  - "apps/web/lib/suit-visuals.ts"
  - "apps/web/components/hanabi/SuitGlyph.tsx"
  - "apps/web/components/hanabi/FireworkCard.tsx"
  - "06.1-09 / 06.1-10 card composition (stack heads, discard minis, live table faces)"

tech-stack:
  added: []
  patterns:
    - "Per-instance SVG gradient ids via React useId() to avoid DOM id collisions when many SuitGlyph instances render on one page/card"
    - "Accessible label (title/aria-label) gated by the exact same exposeSuit boolean that gates data-glyph, so own-hand identity-hiding contracts extend to the accessible tree, not just the visual/DOM-marker one"

key-files:
  created:
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-07-SUMMARY.md
  modified:
    - apps/web/lib/suit-visuals.ts
    - apps/web/components/hanabi/SuitGlyph.tsx
    - apps/web/components/hanabi/FireworkCard.tsx
    - apps/web/lib/firework-card-render.test.ts
    - apps/web/app/dev/fireworks/page.tsx

decisions:
  - "Rank-5 burst layout: center burst scale 0.35 -> 0.525 (1.5x per owner request); the four corner bursts nudged outward (0.24/0.76 -> 0.22/0.78) so they clear the larger center burst instead of clipping it."
  - "Rainbow suit renders a real multicolour SVG linearGradient (stops: --color-suit-red -> --color-suit-yellow -> --color-suit-green -> --color-suit-blue -> --color-suit-rainbow), built only from existing tokens (no new hex, D-11 intact). This OVERRIDES D-08's original 'Rainbow is always a flat single-tone fill, never a gradient' rule per explicit owner request during this review. Rainbow's silhouette (20-spike flat burst) is unchanged, so it still reads distinctly in the grayscale/colourblind-safe section."
  - "Corner numeral removed at every card size (48x64 AND 64x84/88x112) — rank is conveyed by burst count only, everywhere. This also overrides D-09's original 'burst count plus a small corner numeral' rule."
  - "48x64 small-card decision (D-09 Open Question 3): showBurstCount=true — burst count shown, no numeral (same 'no numeral' rule as every other size, so there was no separate numeral-vs-count tradeoff left to make once numerals were removed everywhere)."
  - "FireworkCardFace's numeralSize/NumeralSize prop and type were removed entirely (no visible numeral left to size). A new accessible title/aria-label of the form '{Suit} {rank}' (e.g. 'Red 3') was added to FireworkCardFace's wrapper span (role=\"img\"), gated by the same exposeSuit boolean that gates data-glyph, so rank/suit remain available to screen readers and e2e selectors only where visual identity was already exposed — own-hand cards get neither."
  - "Confirmed no e2e spec or other test file outside FireworkCard.tsx/SuitGlyph.tsx/firework-card-render.test.ts depended on the removed visible numeral; FireworkCardFace/FireworkCardBack are not yet wired into the live table (deferred to 06.1-09/06.1-10), so there was nothing downstream to fix."
  - "Card back redone as a tile with a picture-frame double outline. Owner: 'make it the outline of a rectangle - this is too cardlike, it should be tilelike', then 'the cards have the outline, then the back has another outline inside it. like a picture frame'. CARD_BACK_ART is now a flat var(--color-bg) tile fill plus TWO concentric var(--color-border) rectangular outlines (outer edge + evenly-inset inner outline, each an evenodd ring, square corners, empty interior) — the old unlit-shell diamond emblem and glow arc were removed entirely. CardBackLayer gained an optional fillRule field to support the evenodd rings. NOT YET RE-SHOWN TO THE OWNER — see 'Next Phase Readiness' / open item below."

requirements-completed: [ART-01]

duration: N/A - executed across four owner-review rounds in one continuous session (initial approval-with-changes reply, three iterative art-change rounds, final approval)
completed: 2026-09-17
---

# Phase 6.1 Plan 07: Burst Art Owner Gate Summary

Owner reviewed the original 7-suit firework-burst art on `/dev/fireworks`, requested three changes (bigger rank-5 center burst, an actually-multicolour Rainbow, and removal of the corner rank numeral at every size), all three were implemented and verified, and the owner gave final approval — recorded verbatim — before any card/stack/discard composition begins in 06.1-09/06.1-10.

## Performance

- **Started:** Task 1 (preview + UAT record) committed at `af708fb`
- **Completed:** 2026-09-17
- **Tasks:** 3 (Task 1 auto, Task 2 owner checkpoint x4 rounds, Task 3 auto — record reply + apply changes, repeated per round)
- **Files modified:** 5 code files + 1 UAT doc + 1 new SUMMARY

## Accomplishments

- Burst art preview shown to the owner and approved, satisfying the D-31/ROADMAP gate before 06.1-09/06.1-10 compose burst art into cards, stacks, and discards
- Rank-5 layout's center burst enlarged 1.5x with corner bursts repositioned to avoid overlap
- Rainbow suit now renders a genuine multicolour gradient fill (owner override of the original single-tone D-08 rule), using only existing colour tokens and collision-safe per-instance gradient ids
- Corner rank numeral removed everywhere; rank now reads from burst count alone, with an accessibility-preserving label that respects the existing own-hand identity-hiding contract

## Task Commits

1. **Task 1: Start preview + prepare UAT record** - `af708fb` (docs)
2. **Task 3 (round 1): Apply rank-5 + rainbow changes** - `faee4c9` (feat) — note: this single commit covers all three owner-requested code changes (rank-5 scale, rainbow gradient, numeral removal), since the owner's change requests arrived and were applied in quick iterative succession within the same task before the next re-review.

**Plan metadata:** _(this commit)_ `docs(06.1-07): complete burst art owner gate plan`

## Files Created/Modified

- `apps/web/lib/suit-visuals.ts` - Rank-5 `BURST_LAYOUTS` entry rescaled/repositioned; comments updated to document the owner's override of D-08's single-tone-rainbow rule
- `apps/web/components/hanabi/SuitGlyph.tsx` - Rainbow suit renders an SVG `linearGradient` (unique id via `useId()`) instead of a flat `hueVar` fill
- `apps/web/components/hanabi/FireworkCard.tsx` - Removed the corner numeral and the `numeralSize`/`NumeralSize` prop/type; added an `exposeSuit`-gated `title`/`aria-label` of `"{Suit} {rank}"`; card back wrapper background changed to transparent and its path rendering now passes through each layer's `fillRule`
- `apps/web/lib/firework-card-render.test.ts` - Updated numeral-removal assertions, added accessible-label assertions, added rainbow-gradient/unique-gradient-id assertions, and added a card-back tile-outline assertion
- `apps/web/app/dev/fireworks/page.tsx` - 48x64 row simplified to the settled "burst count only" rendering; all preview rows pass `exposeSuit` so the accessible label is visible for review; card-back section heading flags the still-pending owner look
- `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md` - Verbatim owner replies (all four rounds), `small-card decision`, `result: pass`, and a "Changes after review" log of all three art changes

## Decisions Made

See frontmatter `decisions` — summarized: rank-5 center burst 1.5x; Rainbow is genuinely multicolour (D-08 override); no corner numeral at any size (D-09 override), rank conveyed by burst count only with an accessible label; 48x64 uses burst count with no numeral.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Test regex case mismatch after adding the rainbow gradient**
- **Found during:** Task 3, verification run
- **Issue:** New tests asserted `<lineargradient` (lowercase) but React's `renderToStaticMarkup` preserves the camelCase SVG tag name `<linearGradient>`
- **Fix:** Corrected the two new test regexes/`toContain` calls to `<linearGradient`
- **Files modified:** `apps/web/lib/firework-card-render.test.ts`
- **Verification:** `npx vitest run --project web suit-visuals firework-card-render` — 19/19 passed
- **Committed in:** `faee4c9` (part of the task commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Trivial test-authoring fix caught immediately by the verification run; no scope creep.

## Issues Encountered

None beyond the auto-fixed test regex above. Three rounds of owner change requests arrived in quick succession (rank-5 size -> rainbow color -> numeral removal at 48x64 -> numeral removal everywhere) before final approval; each was applied and verified before presenting the next preview, per Task 3's re-review loop.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 06.1-09/06.1-10 can now compose the owner-approved burst-face art (including the multicolour Rainbow gradient, the enlarged rank-5 center burst, and the numeral-free/burst-count-only rank display) into stack heads, discard minis, and the live table's card faces without further art sign-off.
- `showBurstCount` remains the only rank-display toggle callers need; there is no numeral-related prop left to wire.
- `FireworkCardFace`'s `exposeSuit`-gated `title`/`aria-label` is available for 06.1-09/06.1-10 to rely on for accessibility/e2e selectors on any card whose identity is already exposed (teammates' hands, stacks, discards) — own-hand cards continue to get neither `data-glyph` nor the label.
- **Open item — card back needs one more owner look:** the picture-frame double-outline card back (change 6 in the UAT doc) was implemented after the owner's full burst-art approval and has NOT yet been shown to/confirmed by the owner. Whoever picks up 06.1-09/06.1-10 (or a follow-up review round of this plan) should get an explicit thumbs-up on `FireworkCardBack` at http://localhost:3100/dev/fireworks before treating the card back as final — everything else in this plan is fully approved.
- No other blockers.

---
*Phase: 06.1-table-polish-firework-art-notes-drag-audio*
*Completed: 2026-09-17*
