---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rules/src/hanabi/state.ts
  - packages/rules/src/hanabi/legality.ts
  - packages/rules/src/hanabi/legality.test.ts
  - packages/rules/src/hanabi/actions.ts
  - packages/rules/src/hanabi/actions.test.ts
  - packages/rules/src/index.ts
autonomous: true
requirements: [HAND-01, HAND-03]

must_haves:
  truths:
    - "D-23: after a play or discard that draws, the drawn card occupies the exact hand index the played/discarded card left; every other slot keeps its index and facts"
    - "D-23: when no draw happens (deck empty or final round running) the hand shrinks and the remaining cards keep their relative order"
    - "D-22: a reorder request { type: 'reorder', cardIds } that is an exact permutation of the actor's own hand ids reorders that hand; each card keeps its own facts"
    - "D-17: reorder is legal off-turn and spends no turn, clue token, fuse, deck card, or history entry"
    - "D-22: reorder payloads with an extra key, a non-array, a non-string id, a missing id, a duplicate id, or another seat's id are rejected and leave state unchanged"
  artifacts:
    - path: "packages/rules/src/hanabi/actions.ts"
      provides: "isReorderRequest, applyReorder dispatch, insert-at-index drawCard"
      contains: "isReorderRequest"
    - path: "packages/rules/src/hanabi/legality.ts"
      provides: "canReorder permutation predicate"
      contains: "export function canReorder"
    - path: "packages/rules/src/hanabi/state.ts"
      provides: "HanabiAction reorder member"
      contains: "\"reorder\""
  key_links:
    - from: "packages/rules/src/hanabi/actions.ts applyReorder"
      to: "legality.ts canReorder"
      via: "legality check before any state construction"
      pattern: "canReorder\\("
    - from: "applyPlay / applyDiscard"
      to: "drawCard insertAtIndex"
      via: "index captured from state.hands BEFORE removeFromHand"
      pattern: "insertAtIndex"
---

<objective>
Make the two engine changes HAND-01 and HAND-03 need: a drawn card fills the slot the played/discarded card left (not appended), and a new server-validated `reorder` action lets a seat permute its own hand at any time during the game.

Purpose: This is the first rules change since Phase 4 froze the wire contract. Hand order becomes information players rely on, so the server must own it and validate every change. No `packages/schema` change is needed — `GameActionMessageSchema.request` is `z.unknown()` and hand order already travels as array position.
Output: Modified `actions.ts`, `legality.ts`, `state.ts` with TDD unit coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-PATTERNS.md

<interfaces>
From packages/rules/src/hanabi/state.ts (current):
- HanabiAction = { type: "play"; cardId } | { type: "discard"; cardId } | { type: "clue"; targetSeatId; clue }
- Hand = { readonly seatId: string; readonly slots: readonly HandSlot[] }; HandSlot = { card: HanabiCard; facts: ClueFacts }
- HanabiState fields (construct field-by-field, never spread): variant, seatIds, turnIndex, hands, deck, stacks, discard, clueTokens, fuses, finalTurnsRemaining, history

From packages/rules/src/hanabi/legality.ts:
- export type Legality = { legal: true } | { legal: false; reason: AdapterError }
- private isGameOver(state): boolean — fuses >= MAX_FUSES || finalTurnsRemaining === 0
- export function findOwnSlot(state, seatId, cardId): HandSlot | null

From packages/rules/src/adapter.ts:
- AdapterError = "not_your_turn" | "invalid_action" | "game_over" | "card_not_in_hand" | "no_clue_tokens" | "clue_touches_nothing" | "clue_target_invalid" | "discard_at_max_clues"  (DO NOT widen — apps/worker mapAdapterError is exhaustive)

From packages/rules/src/hanabi/actions.ts:
- drawCard(input: { config, seatId, hands, deck, finalTurnsRemainingBeforeThisTurn }) — currently appends `[...hand.slots, newSlot]` (line ~159)
- removeFromHand(hands, actorSeatId, cardId) — uses .filter, loses index
- parseHanabiRequest(request): HanabiAction | null; applyHanabiAction(state, actorSeatId, request)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot-preserving draw (HAND-03)</name>
  <files>packages/rules/src/hanabi/actions.ts, packages/rules/src/hanabi/actions.test.ts</files>
  <read_first>
    - packages/rules/src/hanabi/actions.ts (whole file — drawCard, removeFromHand, applyPlay, applyDiscard, header no-spread rule)
    - packages/rules/src/hanabi/actions.test.ts (existing fixtures/helpers and any assertion that assumes the drawn card is last)
    - packages/rules/src/hanabi/test-support.ts
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Pattern 3, Pitfall 1)
  </read_first>
  <behavior>
    - Discard the card at index 0 of a 5-card hand with cards left in the deck: the new card id (from the "draw" history entry) is at index 0, indexes 1-4 hold the same ids as before
    - Successful play of the card at index 2: drawn card at index 2, others unchanged
    - Misplay of the last card (index 4): drawn card at index 4
    - The drawn slot's facts equal initialClueFacts for the variant; untouched slots keep their existing facts objects' values
    - Deck empty (or finalTurnsRemaining already non-null): no draw, hand length drops by one, remaining ids keep relative order
    - Only the actor's hand changes; every other seat's hand is deep-equal to before
  </behavior>
  <action>
    Per D-23. Write the failing tests first in actions.test.ts under a describe named "slot-preserving draw" (the Pitfall 1 index-0 case first). Then in actions.ts add a private helper `findHandIndex(hands: readonly Hand[], seatId: string, cardId: string): number` returning `hand.slots.findIndex(...)` or -1. Add `insertAtIndex: number` to drawCard's input object; replace the append with a copy of `hand.slots` via `.slice()` followed by `.splice(insertAtIndex, 0, newSlot)`, returning `{ seatId: hand.seatId, slots }` (no object spread). In applyPlay and applyDiscard, compute `const vacatedIndex = findHandIndex(state.hands, actorSeatId, cardId)` from `state.hands` immediately after the legality check and BEFORE calling removeFromHand, and pass it as `insertAtIndex`. The empty-deck / final-round short-circuit is unchanged. Update drawCard's doc comment from "appended to the end" to "inserted at the vacated index (D-23)". Run the whole rules and worker suites; any existing assertion that encoded append-at-end semantics (e.g. reading the last slot as the drawn card) must be rewritten to vacated-index semantics, not deleted.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project rules && npx vitest run --project worker room-state</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "insertAtIndex" packages/rules/src/hanabi/actions.ts` shows the drawCard parameter and both call sites (applyPlay, applyDiscard)
    - `grep -n "\[\.\.\.hand\.slots, newSlot\]" packages/rules/src/hanabi/actions.ts` returns nothing
    - actions.test.ts contains a describe "slot-preserving draw" with the index-0, middle-index, last-index, and deck-empty cases, all passing
    - `npx vitest run --project rules` exits 0 (including the three existing property suites)
  </acceptance_criteria>
  <done>Drawn cards land in the vacated index for play, misplay, and discard; no-draw paths shrink the hand in order; all rules tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server-validated reorder action (HAND-01)</name>
  <files>packages/rules/src/hanabi/state.ts, packages/rules/src/hanabi/legality.ts, packages/rules/src/hanabi/legality.test.ts, packages/rules/src/hanabi/actions.ts, packages/rules/src/hanabi/actions.test.ts, packages/rules/src/index.ts</files>
  <read_first>
    - packages/rules/src/hanabi/state.ts
    - packages/rules/src/hanabi/legality.ts
    - packages/rules/src/hanabi/legality.test.ts
    - packages/rules/src/hanabi/actions.ts (after Task 1)
    - packages/rules/src/index.ts (what legality exports are re-exported)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Pattern 4, Security Domain)
  </read_first>
  <behavior>
    - isReorderRequest accepts exactly own keys {type, cardIds} with type "reorder", cardIds an array of strings; rejects extra key, missing key, non-array, array containing a number, null
    - canReorder(state, actor, ids): legal for an exact permutation of the actor's hand ids, including the identity order, when it is NOT the actor's turn
    - canReorder rejects with "card_not_in_hand": wrong length, a duplicate id (length still matches), an id from another seat's hand, an unknown id, an unknown actor seat
    - canReorder rejects with "game_over" when fuses >= MAX_FUSES or finalTurnsRemaining === 0
    - applyHanabiAction(state, offTurnSeat, { type: "reorder", cardIds: reversed }) returns ok with that seat's slots in the submitted order, each slot's facts identical to the facts that card had before
    - After a successful reorder: turnIndex, clueTokens, fuses, deck, stacks, discard, finalTurnsRemaining, history, and every other seat's hand are unchanged (history length identical — reorder is NOT logged, per RESEARCH A4)
    - A malformed reorder payload returns { ok: false, error: "invalid_action" }; the input state object is not mutated (deep-equal to a structuredClone taken before)
  </behavior>
  <action>
    Per D-17, D-22 (discretion choice: wire shape `{ type: "reorder", cardIds: string[] }`, not logged to history — reorders stay quiet per D-18 and there is no history panel). Write failing tests first (legality.test.ts describe "canReorder", actions.test.ts describe "reorder"). In state.ts add the fourth union member `{ readonly type: "reorder"; readonly cardIds: readonly string[] }` to HanabiAction. In legality.ts add `export function canReorder(state: HanabiState, actorSeatId: string, cardIds: readonly string[]): Legality` — check game over first (reason "game_over"), deliberately NO isActorsTurn check (comment citing D-17), then own-hand lookup, length equality, Set size equality (duplicate detection), and membership of every id in the current hand's id Set, all failing with "card_not_in_hand". Do not add a new AdapterError member. In actions.ts add `export function isReorderRequest(request: unknown)` with the same exact-own-key shape as isPlayRequest (keys.length !== 2, includes "type" and "cardIds", Array.isArray, every element typeof "string"); add `if (isReorderRequest(request)) return request;` to parseHanabiRequest; add private `applyReorder(state, actorSeatId, cardIds)` that calls canReorder, builds a Map of the actor's slots by card id, maps cardIds to those slot objects, rebuilds hands with `{ seatId: h.seatId, slots: reorderedSlots }` for the actor only, and returns a field-by-field HanabiState literal copying every other field from `state` (no spread). Add the `action.type === "reorder"` branch in applyHanabiAction before the clue fallthrough. Update the file header comment "three action branches" wording to mention reorder does not advance the turn. If index.ts re-exports legality predicates (canPlay etc.), add canReorder and isReorderRequest alongside; otherwise leave index.ts untouched and drop it from the commit.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project rules && npx vitest run --project worker room-state && npx tsc -b packages/rules apps/worker apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function canReorder" packages/rules/src/hanabi/legality.ts` matches, and the canReorder body contains no call to `isActorsTurn`
    - `grep -n "export function isReorderRequest" packages/rules/src/hanabi/actions.ts` matches and `grep -n "keys.length !== 2" packages/rules/src/hanabi/actions.ts` count increases by one
    - `grep -v '^\s*//' packages/rules/src/hanabi/actions.ts | grep -c "\.\.\.state"` outputs 0
    - `packages/rules/src/adapter.ts` AdapterError union is unchanged (git diff shows no change to that file)
    - Every behavior bullet above has a passing test; `npx tsc -b packages/rules apps/worker apps/web` exits 0
  </acceptance_criteria>
  <done>reorder parses, validates as an exact permutation, applies off-turn without touching turn/tokens/history, and all rules + worker room-state tests and type checks are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RoomDO → adapter.applyAction | `request` is hostile network input; actorSeatId is bound server-side from the connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Tampering | isReorderRequest / canReorder | mitigate | Exact-own-key guard plus length + Set-size + membership check reject partial, duplicate, foreign, or extra-key payloads before any state is built |
| T-06.1-02 | Spoofing | applyReorder actor | mitigate | Hand is looked up only by server-bound actorSeatId; the payload carries no seat field |
| T-06.1-03 | Information Disclosure | drawCard insert index | mitigate | Insert index comes from the removed card's position only, never from card identity; redaction property suite extended in 06.1-06 |
| T-06.1-04 | Denial of Service | canReorder loop | accept | Length check against a 4-5 card hand runs before the membership loop; oversized arrays are rejected in O(1) |
| T-06.1-05 | Tampering | state construction | mitigate | Field-by-field literals, no spread (existing actions.ts discipline), grep-gated in acceptance criteria |
</threat_model>

<verification>
- `npx vitest run --project rules` green (unit + conservation/redaction/termination property suites)
- `npx vitest run --project worker room-state` green
- `npx tsc -b packages/rules apps/worker apps/web` exits 0
</verification>

<success_criteria>
HAND-03 engine behaviour and the HAND-01 reorder action exist, are validated at the adapter boundary, and are proven by unit tests; no AdapterError or schema widening occurred.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-01-SUMMARY.md` when done
</output>
