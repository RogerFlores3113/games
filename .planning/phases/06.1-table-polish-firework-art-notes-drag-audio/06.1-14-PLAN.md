---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 14
type: execute
wave: 6
depends_on: ["06.1-06", "06.1-08", "06.1-10", "06.1-11", "06.1-12"]
files_modified:
  - e2e/helpers.ts
  - e2e/hanabi-table-polish.spec.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [HAND-01, HAND-02, HAND-03, NOTE-01, NOTE-02, AUD-01, ART-01]

must_haves:
  truths:
    - "HAND-01: a drag reorder (including off-turn) is visible on the teammate's screen and survives refresh on both screens"
    - "HAND-02: dragging to play-zone plays and to discard-pile discards on your turn; off-turn drops change nothing and show the disabled reason; buttons still work"
    - "HAND-03: after a play/discard, the drawn card id occupies the vacated index on the actor's and teammate's screens"
    - "NOTE-01/NOTE-02: marks zones sit above cards; a note survives refresh, never appears in sent WebSocket frames, and is gone when its card leaves the hand"
    - "D-13: discard overlay opens from the toggle, closes on Esc, and its open state is remembered across refresh"
    - "AUD-01/D-29: with a counting AudioContext stub, a refresh produces zero sounds and a live teammate action after a user gesture produces at least one"
    - "D-30: proofs follow Phase 6 D-23/D-24 — pure logic unit-tested in apps/web/lib (plans 03/04), engine unit + property tests for reorder and slot-preserving draw (plans 01/06), and Playwright proofs here for reorder visible to a teammate and surviving refresh, drag-to-play/discard, and slot replacement"
    - "Phase 6 D-01: five-player 1280x720 no-scroll fit still holds with marks zones, note rows, and audio controls in viewport"
  artifacts:
    - path: "e2e/hanabi-table-polish.spec.ts"
      provides: "Phase 6.1 end-to-end proofs"
      contains: "HAND-01"
    - path: "e2e/helpers.ts"
      exports: ["dragLocatorTo", "ownHandCardIds", "teammateHandCardIds"]
  key_links:
    - from: "e2e/hanabi-table-polish.spec.ts"
      to: "real wrangler dev + next dev"
      via: "playwright webServer config"
      pattern: "startTwoPlayerGame|createRoom"
---

<objective>
Prove the phase end to end against the real worker and web app: reorder, drag play/discard, slot replacement, marks/notes, discard overlay, no-catch-up audio, and the 1280x720 fit.

Purpose: D-30 Playwright extensions; these are the behavioural evidence the verifier and owner sign-off rely on.
Output: New spec file, helper additions, extended UI-11 fit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-VALIDATION.md

<interfaces>
e2e/helpers.ts exports: createRoom(page, { name, variant? }): Promise<code>; joinAs(context, code, name): Promise<Page>; expectSeatCount(page, n); OTHER_HAND_SELECTOR; seatIdOfOtherPlayer(observer): Promise<string>; startTwoPlayerGame(...); startGameWithPlayers(hostPage, browser, names): { contexts }
e2e/hanabi-realtime.spec.ts: private selectAClueValueThatTouchesSomething(page) pattern (clue-target-*, clue-value-*, give-clue-button); turn-indicator text "Your turn"
DOM contract from 06.1-09..13: own card columns `[data-testid="own-hand"] [data-card-id]` in render order; own slots own-hand-slot-{n}; marks-zone-slot-{n} and marks-zone-{cardId}; note-chip-slot-{n} / note-input-slot-{n}; teammate cards `other-hand-{seatId}` > `[data-testid^="other-hand-card-"]`; play-zone; discard-pile with data-discard-count and data-drop-state; drop-reason-play / drop-reason-discard; discard-toggle; discard-overlay; discard-overlay-close; audio-mute-toggle; audio-volume; play-button; discard-button; deck-count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers and hand-order / drag / slot proofs</name>
  <files>e2e/helpers.ts, e2e/hanabi-table-polish.spec.ts</files>
  <read_first>
    - e2e/helpers.ts
    - e2e/hanabi-realtime.spec.ts (RT-01 and RT-03 flows)
    - apps/web/components/hanabi/Hand.tsx
    - apps/web/components/hanabi/Table.tsx
    - apps/web/components/hanabi/useHandDrag.ts
  </read_first>
  <action>
    Per D-15..D-17, D-22, D-23, D-30. helpers.ts: add `dragLocatorTo(page, source: Locator, target: Locator)` (boundingBox centres; page.mouse.move to source, down, move 10px, move to target with steps: 12, up); `ownHandCardIds(page): Promise<string[]>` reading data-card-id of `[data-testid="own-hand"] [data-card-id]` in DOM order; `teammateHandCardIds(page, seatId): Promise<string[]>` mapping `[data-testid="other-hand-<seatId>"] [data-testid^="other-hand-card-"]` testids to ids. Create e2e/hanabi-table-polish.spec.ts (copy selectAClueValueThatTouchesSomething locally) with tests named: "HAND-01: an off-turn drag reorder is seen by the teammate and survives refresh" — the passive player drags its slot 1 column onto slot 3, expect ownHandCardIds equals reorderedCardIds-style expected order on the passive page and teammateHandCardIds(activePage, passiveSeatId) equals the same, active turn indicator unchanged, then reload both pages and re-assert; "HAND-02: dragging onto the stacks plays on your turn, and an off-turn drop does nothing" — passive drags a card over play-zone, expect drop-reason-play visible during hover, release, deck-count unchanged on both pages and own order unchanged; then active drags slot 1 onto play-zone, expect deck-count to change on the passive page; "HAND-02: dragging onto the discard pile discards" — active gives a clue (so tokens < 8), the other player (now active) drags slot 2 onto discard-pile, expect discard-pile data-discard-count to increase by 1 on both pages; "HAND-02: Play and Discard buttons still work" — click own-hand-slot-1 then play-button, deck-count changes on the other page; "HAND-03: the drawn card takes the vacated slot on both screens" — record actor ids and teammate view ids, actor discards/plays slot 2 by button, expect index 1 id changed and indexes 0,2,3,4 identical on both the actor's ownHandCardIds and the observer's teammateHandCardIds. Use expect.poll for order assertions.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/hanabi-table-polish.spec.ts -g "HAND-"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function dragLocatorTo" e2e/helpers.ts` matches
    - e2e/hanabi-table-polish.spec.ts contains the five test names above
    - `npx playwright test e2e/hanabi-table-polish.spec.ts -g "HAND-"` exits 0
  </acceptance_criteria>
  <done>Reorder, drag play/discard, button fallback, and slot replacement are proven across two real browsers.</done>
</task>

<task type="auto">
  <name>Task 2: Marks/notes, discard overlay, audio no-catch-up, and fit proofs</name>
  <files>e2e/hanabi-table-polish.spec.ts, e2e/start-game.spec.ts</files>
  <read_first>
    - e2e/hanabi-table-polish.spec.ts (Task 1)
    - e2e/start-game.spec.ts (UI-11 test)
    - apps/web/components/hanabi/NoteChip.tsx
    - apps/web/components/hanabi/useHanabiAudio.ts
    - apps/web/lib/hanabi-audio-synth.ts (AudioContextLike methods)
  </read_first>
  <action>
    Per D-01..D-07, D-13, D-26, D-29, D-30. Add to hanabi-table-polish.spec.ts: "NOTE-01: clue marks sit above every card" — for own slot 1 and one teammate card, the marks zone bounding box bottom <= the card's top + 1; "NOTE-02: a note survives refresh, is never sent, and clears when its card leaves" — attach `page.on("websocket", ws => ws.on("framesent", f => sent.push(String(f.payload))))` before typing, click note-chip-slot-1, type "r5? save" + Enter, expect chip text "r5? save", reload, expect it again, assert no sent frame contains "r5? save", then (on that player's turn) play slot 1 via button and expect note-chip-slot-1 to have aria-label "Add note"; "D-13: discard overlay opens, closes on Esc, and is remembered" — click discard-toggle, expect discard-overlay visible, reload, expect visible, press Escape, expect hidden, reload, expect hidden; "AUD-01: refresh plays no catch-up sound and a live action does" — `context.addInitScript` installing a fake `window.AudioContext` class whose createOscillator/createBufferSource increment `window.__soundStarts` and return stub nodes with connect/start/stop and AudioParam stubs (setValueAtTime, linearRampToValueAtTime, exponentialRampToValueAtTime), plus createGain/createBiquadFilter/createBuffer/resume/state "running"/currentTime 0/sampleRate 44100/destination {}; on the observer page click the page body (gesture unlock) after load, give a clue from the other page, expect.poll `__soundStarts` > 0 on the observer; reload the observer, click body, wait 1500ms with no new actions, expect `__soundStarts` === 0. In start-game.spec.ts UI-11: after the existing assertions at 1280x720, also assert `marks-zone-slot-1`, `marks-zone-slot-1-note-row`, `audio-mute-toggle`, `discard-toggle`, and the first `[data-testid^="marks-zone-"]` teammate zone are in viewport.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - hanabi-table-polish.spec.ts contains tests titled with "NOTE-01", "NOTE-02", "D-13", and "AUD-01"
    - `grep -n "framesent" e2e/hanabi-table-polish.spec.ts` and `grep -n "__soundStarts" e2e/hanabi-table-polish.spec.ts` match
    - `grep -n "audio-mute-toggle" e2e/start-game.spec.ts` matches
    - `npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts` exits 0
  </acceptance_criteria>
  <done>All user-visible 6.1 behaviours and the fit constraint have passing end-to-end proofs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → worker WebSocket | verified by inspecting sent frames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-41 | Information Disclosure | notes | mitigate | e2e asserts typed note text never appears in any sent WebSocket frame |
| T-06.1-42 | Tampering | off-turn drop | mitigate | e2e asserts off-turn drag to play changes no state on either screen |
| T-06.1-43 | Repudiation | test flakiness | accept | expect.poll with default timeouts; pre-existing RT-04 flake noted separately, not masked |
</threat_model>

<verification>
- `npx playwright test e2e/hanabi-table-polish.spec.ts e2e/start-game.spec.ts` green
</verification>

<success_criteria>
Every phase requirement with a behavioural surface has a passing Playwright proof against real servers.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-14-SUMMARY.md` when done
</output>
