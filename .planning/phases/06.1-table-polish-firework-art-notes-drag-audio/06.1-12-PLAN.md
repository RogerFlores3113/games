---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 12
type: execute
wave: 5
depends_on: ["06.1-01", "06.1-03", "06.1-10", "06.1-11"]
files_modified:
  - apps/web/lib/hanabi-drag-logic.ts
  - apps/web/lib/hanabi-drag-logic.test.ts
  - apps/web/components/hanabi/useHandDrag.ts
  - apps/web/components/hanabi/HanabiBoard.tsx
  - apps/web/components/hanabi/Table.tsx
  - apps/web/components/hanabi/Hand.tsx
  - apps/web/components/hanabi/OwnHandCard.tsx
  - apps/web/app/globals.css
  - apps/web/lib/own-hand-source.test.ts
autonomous: true
requirements: [HAND-01, HAND-02]

must_haves:
  truths:
    - "D-22: dragging an own card onto another own slot sends one reorder action (one actionId per drop) and the hand renders in the server's order"
    - "D-17: reorder works off-turn; drag-to-play/discard only acts on your turn"
    - "D-15: dropping on the stacks area plays immediately and on the discard pile discards immediately; dropping elsewhere snaps back"
    - "D-16: while dragging, legal drop zones highlight with --color-card-glow and illegal zones show the existing disabled reason"
    - "D-18: nothing is sent during the drag; teammates only see the result"
    - "D-19/HAND-02: Play/Discard buttons and click-to-select still work; there is no keyboard reorder path"
    - "D-20: implemented with Pointer Events (no drag library)"
  artifacts:
    - path: "apps/web/components/hanabi/useHandDrag.ts"
      exports: ["useHandDrag"]
    - path: "apps/web/lib/hanabi-drag-logic.ts"
      exports: ["applyPendingOrder"]
  key_links:
    - from: "apps/web/components/hanabi/useHandDrag.ts"
      to: "apps/web/lib/hanabi-drag-logic.ts requestForDrop/resolveDropTarget"
      via: "pointerup handler"
      pattern: "requestForDrop\\("
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "act()"
      via: "onDropRequest → act(request) with HanabiActionRequest including reorder"
      pattern: "type: \"reorder\""
---

<objective>
Implement mouse drag on the own hand: drag onto another slot to reorder (anytime), onto the stacks to play, onto the discard pile to discard (your turn only), with zone highlights, disabled reasons, snap-back, and a brief optimistic order until the server confirms.

Purpose: HAND-01 client side and HAND-02 (D-15..D-20, D-22). Uses the engine action from 06.1-01 and pure resolution logic from 06.1-03.
Output: useHandDrag hook, pending-order helper, drop-zone wiring in Table, drag visuals in OwnHand/OwnHandCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md

<interfaces>
From 06.1-03 apps/web/lib/hanabi-drag-logic.ts: DRAG_THRESHOLD_PX; Point; Rect; DropZones { slots: {cardId, rect}[]; play: Rect|null; discard: Rect|null }; DropTarget; DropRequest; exceedsDragThreshold(start, current); resolveDropTarget(point, zones); reorderedCardIds(handIds, draggedId, targetIndex); dropZoneStatus(view, "play"|"discard", draggedCardId, ctx): { enabled; reason }; requestForDrop(target, draggedCardId, handIds, view, ctx): DropRequest | null
From apps/web/components/hanabi/HanabiBoard.tsx: export type HanabiActionRequest = play | discard | clue; act(request) guards reconnecting/ended then onAction(request); RoomClient's onAction mints one nanoid actionId per call; ctx: ActionContext = { reconnecting, ended, labelFor }; selection-hygiene effect clears selectedCardId when the card leaves
From apps/web/components/hanabi/Table.tsx (after 06.1-10): Fireworks stacks container, discard-pile block with toggle
From apps/web/components/hanabi/Hand.tsx OwnHand (after 06.1-11): column per card = MarksZone (with NoteChip) above OwnHandCard; props include cards, roomCode, selectedCardId, onSelectCard, disabled
own-hand-source.test.ts: OwnHand body card members exactly ["facts","id"]; OwnHandCard scanned for PROPERTY_ACCESS/DESTRUCTURING/FORBIDDEN_TOKENS
globals.css: motion classes degrade under @media (prefers-reduced-motion: reduce)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drag hook, pending order, drop zones in Table, board wiring</name>
  <files>apps/web/lib/hanabi-drag-logic.ts, apps/web/lib/hanabi-drag-logic.test.ts, apps/web/components/hanabi/useHandDrag.ts, apps/web/components/hanabi/HanabiBoard.tsx, apps/web/components/hanabi/Table.tsx</files>
  <read_first>
    - apps/web/lib/hanabi-drag-logic.ts
    - apps/web/lib/hanabi-drag-logic.test.ts
    - apps/web/components/hanabi/HanabiBoard.tsx
    - apps/web/components/hanabi/Table.tsx
    - apps/web/app/room/[code]/RoomClient.tsx (onAction actionId minting)
  </read_first>
  <behavior>
    - applyPendingOrder(cards [{id:"a"},{id:"b"},{id:"c"}], ["c","a","b"]) returns cards in c,a,b order (same objects)
    - applyPendingOrder(cards, null) returns cards unchanged; a pending list that is not a permutation of the card ids (length differs or unknown id) returns cards unchanged
  </behavior>
  <action>
    Per D-15..D-18, D-20, D-22. Add `export function applyPendingOrder<T extends { id: string }>(cards: T[], pendingIds: readonly string[] | null): T[]` to hanabi-drag-logic.ts with tests. Create components/hanabi/useHandDrag.ts ("use client") exporting `useHandDrag({ game, ctx, onDropRequest })` returning `{ dragState: { cardId: string; offset: Point; target: DropTarget } | null; pendingOrder: string[] | null; droppedCardIdsRef: MutableRefObject<Set<string>>; registerSlot(cardId, el | null): void; playZoneRef: RefObject<HTMLDivElement>; discardZoneRef: RefObject<HTMLDivElement>; onCardPointerDown(cardId, event: React.PointerEvent): void; consumeClickSuppression(): boolean }`. Behaviour: pointerdown (primary mouse button only; ignore when ctx.reconnecting or ctx.ended) records start point and card id and calls setPointerCapture on the target; pointermove on window starts a drag only after exceedsDragThreshold, then updates offset and target via resolveDropTarget(point, zones built from getBoundingClientRect of registered slots and zone refs); pointerup computes `requestForDrop(target, cardId, game.yourHand ids, game, ctx)`; if non-null: for reorder set pendingOrder to the new ids, for play/discard add cardId to droppedCardIdsRef (consumed by the fly-to layer in 06.1-13), then call onDropRequest(request) exactly once; always clear dragState; if a drag occurred set a flag so the following click does not select (consumeClickSuppression returns and clears it); pointercancel clears state without sending. Clear pendingOrder whenever `game` changes identity or after 2000ms. No messages are sent during pointermove (D-18). HanabiBoard.tsx: widen `HanabiActionRequest` with `| { type: "reorder"; cardIds: string[] }`; call useHandDrag before the early return with `onDropRequest: (request) => act(request)` and ctx; pass `applyPendingOrder(game.yourHand, drag.pendingOrder)` as OwnHand cards; pass drag props to OwnHand (draggingCardId, onCardPointerDown, registerSlot, consumeClickSuppression) and to Table (playZoneRef, discardZoneRef, dropStatus: when dragState is non-null, `{ play: dropZoneStatus(game, "play", id, ctx), discard: dropZoneStatus(game, "discard", id, ctx), hovered: dragState.target.kind }`, else null). Table.tsx: accept optional `playZoneRef`, `discardZoneRef`, `dropStatus` props; attach playZoneRef to the Fireworks stacks container with data-testid "play-zone" and discardZoneRef to the discard-pile element; while dropStatus is non-null, an enabled zone gets `box-shadow: 0 0 0 3px var(--color-card-glow)` (stronger when hovered) and data-drop-state "enabled", a disabled zone gets data-drop-state "disabled" and renders its reason inline in --color-text-muted label role with data-testid "drop-reason-play" / "drop-reason-discard". No new colour token.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-drag-logic && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function applyPendingOrder" apps/web/lib/hanabi-drag-logic.ts` matches with passing tests
    - `grep -n "requestForDrop(" apps/web/components/hanabi/useHandDrag.ts` and `grep -n "setPointerCapture" apps/web/components/hanabi/useHandDrag.ts` match
    - `grep -n 'type: "reorder"' apps/web/components/hanabi/HanabiBoard.tsx` matches
    - `grep -n 'data-testid="play-zone"' apps/web/components/hanabi/Table.tsx` and `grep -n "drop-reason-" apps/web/components/hanabi/Table.tsx` match
    - `grep -nE "dnd-kit|react-dnd|draggable=" apps/web/components/hanabi/useHandDrag.ts apps/web/package.json` returns nothing
  </acceptance_criteria>
  <done>The drag hook resolves drops into single actions through act(), with zone highlighting and reasons in Table and a short optimistic order.</done>
</task>

<task type="auto">
  <name>Task 2: Own-hand drag visuals, snap-back, and identity scan</name>
  <files>apps/web/components/hanabi/Hand.tsx, apps/web/components/hanabi/OwnHandCard.tsx, apps/web/app/globals.css, apps/web/lib/own-hand-source.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/Hand.tsx
    - apps/web/components/hanabi/OwnHandCard.tsx
    - apps/web/components/hanabi/useHandDrag.ts (Task 1)
    - apps/web/app/globals.css
    - apps/web/lib/own-hand-source.test.ts
  </read_first>
  <action>
    Per D-15, D-19, D-20, D-21 (own drop is the motion) and UI-SPEC Drag & Drop. OwnHandProps gains `draggingCardId: string | null`, `dragOffset: { x: number; y: number } | null`, `onCardPointerDown(cardId, e)`, `registerSlot(cardId, el)`, `consumeClickSuppression(): boolean`. In OwnHand, each card column gets `ref={(el) => registerSlot(card.id, el)}` and `data-card-id={card.id}`; OwnHandCard receives `onPointerDown={(e) => onCardPointerDown(card.id, e)}`, `dragging={draggingCardId === card.id}`, `dragOffset`, and `onSelect` becomes a handler that returns early when consumeClickSuppression() is true. OwnHandCard: add props `dragging`, `dragOffset`, `onPointerDown` (data-card-id lives on the OwnHand column, not on OwnHandCard); while dragging render the card with `transform: translate(dx, dy) scale(1.05)`, an elevated box-shadow (LUMINOSITY_FRAME "known" shadow scaled up), `z-index: 50`, `pointer-events: none` on the moving layer, and leave a dashed 1px --color-border placeholder in the slot so the hand does not reflow; add class `drag-snap` when not dragging so a release outside zones animates back. Set `touch-action: none` only on own cards (keeps future touch possible, D-20) and `cursor: grab` / `grabbing`. No keyboard handlers for reorder (D-19); keep button semantics, onClick selection, and all existing testids/data attributes. globals.css: add `.drag-snap { transition: transform 150ms ease-out; }` and inside the existing prefers-reduced-motion block `.drag-snap { transition: none; }`. own-hand-source.test.ts: add USE_HAND_DRAG_PATH and scan useHandDrag.ts with PROPERTY_ACCESS and FORBIDDEN_TOKENS (it may only read card ids); keep the OwnHand card-members assertion equal to ["facts","id"].
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web && npx tsc -b apps/web && npx playwright test e2e/start-game.spec.ts e2e/hanabi-realtime.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "consumeClickSuppression" apps/web/components/hanabi/Hand.tsx` and `grep -n "data-card-id" apps/web/components/hanabi/Hand.tsx` match
    - `grep -n "touch-action\|touchAction" apps/web/components/hanabi/OwnHandCard.tsx` matches; `grep -n "onKeyDown" apps/web/components/hanabi/OwnHandCard.tsx` returns nothing
    - `grep -n "drag-snap" apps/web/app/globals.css` shows both the transition rule and the reduced-motion override
    - `grep -n "USE_HAND_DRAG_PATH" apps/web/lib/own-hand-source.test.ts` matches; `npx vitest run --project web` exits 0
    - Existing start-game and hanabi-realtime Playwright specs (click-select + Play/Discard buttons) exit 0
  </acceptance_criteria>
  <done>Own cards can be dragged with clear feedback and snap-back, click/buttons still work, and the identity scan covers the drag hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client drag gesture → act() → WebSocket | client-composed reorder/play/discard requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-35 | Tampering | reorder request | transfer | Server canReorder (06.1-01) is authoritative; client gating is UX only |
| T-06.1-36 | Repudiation/Replay | duplicate sends | mitigate | onDropRequest called once per pointerup; RoomClient mints one actionId per call; nothing sent on pointermove |
| T-06.1-37 | Information Disclosure | drag hook | mitigate | Hook reads only card ids; source scan extended to useHandDrag.ts |
| T-06.1-38 | Denial of Service | stale pending order | mitigate | pendingOrder cleared on next server view or 2s timeout and ignored unless a permutation of server ids |
| T-06.1-SC | Tampering | npm installs | accept | No new package; a drag library would require the Package Legitimacy Gate before install (UI-SPEC) |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0; existing Playwright specs green (new drag e2e lands in 06.1-14)
</verification>

<success_criteria>
HAND-01 and HAND-02 are usable with a mouse on desktop, server-authoritative, with button/keyboard fallback intact.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-12-SUMMARY.md` when done
</output>
