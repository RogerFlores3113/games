---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/safe-storage.ts
  - apps/web/lib/safe-storage.test.ts
  - apps/web/lib/hanabi-notes.ts
  - apps/web/lib/hanabi-notes.test.ts
  - apps/web/lib/hanabi-discard-logic.ts
  - apps/web/lib/hanabi-discard-logic.test.ts
  - apps/web/lib/hanabi-drag-logic.ts
  - apps/web/lib/hanabi-drag-logic.test.ts
autonomous: true
requirements: [NOTE-02, HAND-01, HAND-02, ART-01]

must_haves:
  truths:
    - "D-02: every localStorage access in this phase goes through one defensive wrapper that never throws"
    - "D-02/D-04/D-06: notes are read/written by room+seat+card id, capped at 20 chars, empty text removes the key, and notes for cards no longer in the hand are pruned"
    - "D-01: the notes module imports nothing that can send to the server"
    - "D-13: discards group per suit with per-rank counts in variant suit order, and the compact/expanded preference persists (default compact)"
    - "D-15/D-16/D-17: drop resolution maps a pointer point to reorder/play/discard/none; play/discard legality and reasons come from disabledReasonFor, reorder is allowed off-turn but not while reconnecting or ended"
  artifacts:
    - path: "apps/web/lib/safe-storage.ts"
      exports: ["safeGetItem", "safeSetItem", "safeRemoveItem", "safeKeysWithPrefix"]
    - path: "apps/web/lib/hanabi-notes.ts"
      exports: ["NOTE_MAX_LENGTH", "noteKey", "readNote", "writeNote", "deleteNote", "pruneNotesForSeat", "clearNotesForRoom"]
    - path: "apps/web/lib/hanabi-discard-logic.ts"
      exports: ["groupDiscardsBySuit", "readDiscardViewPref", "writeDiscardViewPref"]
    - path: "apps/web/lib/hanabi-drag-logic.ts"
      exports: ["DRAG_THRESHOLD_PX", "exceedsDragThreshold", "resolveDropTarget", "reorderedCardIds", "dropZoneStatus", "requestForDrop"]
  key_links:
    - from: "apps/web/lib/hanabi-drag-logic.ts"
      to: "apps/web/lib/hanabi-visual-logic.ts disabledReasonFor"
      via: "direct import, no re-derived turn/token rules"
      pattern: "disabledReasonFor"
---

<objective>
Build the pure, unit-tested client logic for notes, discard grouping, and drag-drop resolution, plus the one shared defensive localStorage wrapper every persisted preference in this phase uses.

Purpose: D-30 — keep behaviour in `apps/web/lib/` and components thin. Later plans (06.1-10 table/discard, 06.1-11 notes UI, 06.1-12 drag) wire these modules without re-deriving logic.
Output: Four new lib modules with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-PATTERNS.md

<interfaces>
From apps/web/lib/seat-token.ts: private getLocalStorage() returning Storage | undefined (typeof window check + try/catch); readSeatToken/writeSeatToken wrap every call in try/catch — mirror this.
From apps/web/lib/hanabi-visual-logic.ts:
- export type ActionIntent (includes { kind: "play"; selectedCardId: string | null } and { kind: "discard"; selectedCardId: string | null })
- export interface ActionContext { reconnecting: boolean; ended: boolean; labelFor: (seatId: string) => string }
- export function disabledReasonFor(view: HanabiView, intent: ActionIntent, ctx: ActionContext): string | null
From @games/rules: HanabiView (yourHand: HanabiCardView[], discard: Array<{ id; suit; rank }>, variant), Suit, Rank, variantConfig(variant).suits
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: safe-storage wrapper, notes storage, discard grouping + view preference</name>
  <files>apps/web/lib/safe-storage.ts, apps/web/lib/safe-storage.test.ts, apps/web/lib/hanabi-notes.ts, apps/web/lib/hanabi-notes.test.ts, apps/web/lib/hanabi-discard-logic.ts, apps/web/lib/hanabi-discard-logic.test.ts</files>
  <read_first>
    - apps/web/lib/seat-token.ts
    - apps/web/lib/seat-token.test.ts (how window/localStorage are stubbed in the web vitest project)
    - apps/web/lib/hanabi-visual-logic.ts
    - packages/rules/src/hanabi/variant.ts (variantConfig suits order)
  </read_first>
  <behavior>
    - safe-storage: with no window, or a localStorage getter that throws, or getItem/setItem/removeItem that throw, every function returns its fallback (null / void / []) and never throws; safeKeysWithPrefix lists keys starting with the prefix
    - noteKey("ABCD", "s1", "c7") === "hanabi-note:ABCD:s1:c7"
    - writeNote trims nothing but truncates to NOTE_MAX_LENGTH (20); writeNote with "" removes the key; readNote returns "" when absent
    - pruneNotesForSeat("ABCD", "s1", ["c1","c2"]) deletes hanabi-note:ABCD:s1:c9 but keeps :c1, keeps other seats' and other rooms' keys
    - clearNotesForRoom("ABCD") removes every hanabi-note:ABCD:* key and nothing else
    - groupDiscardsBySuit(discard, variant) returns one entry per variant suit in variantConfig order: { suit, total, countsByRank: [n1..n5] }, zero-count suits included with total 0
    - readDiscardViewPref() defaults to "compact"; writeDiscardViewPref("expanded") then read returns "expanded"; an unrecognised stored value reads as "compact"
    - hanabi-notes.ts source imports nothing from room-socket, room-store, or nanoid (D-01 structural check via readFileSync)
  </behavior>
  <action>
    Per D-01, D-02, D-04, D-06, D-13. Create safe-storage.ts with a private getLocalStorage mirroring seat-token.ts and exported `safeGetItem(key): string | null`, `safeSetItem(key, value): void`, `safeRemoveItem(key): void`, `safeKeysWithPrefix(prefix): string[]` (iterate storage.length/key(i) inside try/catch). Do not modify seat-token.ts. Create hanabi-notes.ts exporting `NOTE_MAX_LENGTH = 20`, `noteKey(roomCode, seatId, cardId)`, `readNote`, `writeNote(roomCode, seatId, cardId, text)` (slice to 20; empty → remove), `deleteNote`, `pruneNotesForSeat(roomCode, seatId, liveCardIds: readonly string[])` (removes keys under prefix `hanabi-note:{room}:{seat}:` whose card id is not live), and `clearNotesForRoom(roomCode)` (stale-entry pruning for an ended game, D-04). Header comment: notes are local-only per D-01 and this module must never import a network path. Create hanabi-discard-logic.ts exporting `type DiscardView = "compact" | "expanded"`, `groupDiscardsBySuit(discard: HanabiView["discard"], variant: Variant)` and `readDiscardViewPref()` / `writeDiscardViewPref(view)` using key `hanabi-discard-view`. Tests stub `globalThis.window`/localStorage the same way seat-token.test.ts does, including a throwing-storage case per module.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web safe-storage hanabi-notes hanabi-discard-logic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "try" apps/web/lib/safe-storage.ts` is at least 4
    - `grep -n "localStorage" apps/web/lib/hanabi-notes.ts apps/web/lib/hanabi-discard-logic.ts` returns nothing (all access via safe-storage)
    - `grep -nE "room-socket|room-store|nanoid" apps/web/lib/hanabi-notes.ts` returns nothing
    - All behaviour bullets have passing tests
  </acceptance_criteria>
  <done>Notes, discard grouping, and preference persistence are pure/defensive and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drag-drop resolution and drop-zone legality</name>
  <files>apps/web/lib/hanabi-drag-logic.ts, apps/web/lib/hanabi-drag-logic.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-visual-logic.ts (disabledReasonFor, ActionIntent, ActionContext)
    - apps/web/lib/hanabi-board-logic.ts (header: no fifth disabling rule)
    - apps/web/lib/hanabi-visual-logic.test.ts (HanabiView fixture builders to reuse)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Pattern 1)
  </read_first>
  <behavior>
    - exceedsDragThreshold({x:0,y:0},{x:3,y:4}) false (distance 5 < 6); ({x:0,y:0},{x:6,y:0}) true
    - resolveDropTarget: a point inside slot rect i returns { kind: "reorder", targetIndex: i }; slot rects are checked before play/discard; inside play rect → { kind: "play" }; inside discard rect → { kind: "discard" }; outside all or null zones → { kind: "none" }
    - reorderedCardIds(["a","b","c","d"], "a", 2) → ["b","c","a","d"]; ("d", 0) → ["d","a","b","c"]; unknown id returns the input order
    - dropZoneStatus(view, "play", draggedId, ctx) returns { enabled: false, reason: <exact disabledReasonFor string> } when not your turn; "discard" disabled with the discard-at-8-tokens reason when clueTokens is 8; enabled with reason null on own turn
    - requestForDrop: reorder target producing a changed order returns { type: "reorder", cardIds } even when view.isYourTurn is false; unchanged order returns null; any target returns null when ctx.reconnecting or ctx.ended; play/discard return { type: "play"|"discard", cardId } only when dropZoneStatus is enabled; "none" returns null
  </behavior>
  <action>
    Per D-15, D-16, D-17, D-19, D-20. Create hanabi-drag-logic.ts exporting: `DRAG_THRESHOLD_PX = 6`; `type Point = { x: number; y: number }`; `type Rect = { left: number; top: number; right: number; bottom: number }` (structurally compatible with DOMRect); `type DropZones = { slots: ReadonlyArray<{ cardId: string; rect: Rect }>; play: Rect | null; discard: Rect | null }`; `type DropTarget = { kind: "reorder"; targetIndex: number } | { kind: "play" } | { kind: "discard" } | { kind: "none" }`; `type DropRequest = { type: "reorder"; cardIds: string[] } | { type: "play"; cardId: string } | { type: "discard"; cardId: string }`; `exceedsDragThreshold(start, current)` (Euclidean distance >= threshold); `resolveDropTarget(point, zones)`; `reorderedCardIds(handIds, draggedId, targetIndex)` (remove then insert, move semantics); `dropZoneStatus(view, zone, draggedCardId, ctx)` which calls `disabledReasonFor(view, { kind: zone, selectedCardId: draggedCardId }, ctx)` and adds no rule of its own; `requestForDrop(target, draggedCardId, handIds, view, ctx)`. The reconnecting/ended guard for reorder uses only ctx fields (it mirrors HanabiBoard's act() guard, not a view-derived disabling rule). No DOM access, no React import. Comment: keyboard reorder deliberately absent (D-19); pointer-event based so touch can be added later (D-20).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-drag-logic</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "disabledReasonFor" apps/web/lib/hanabi-drag-logic.ts` matches; `grep -nE "isYourTurn|clueTokens" apps/web/lib/hanabi-drag-logic.ts` returns nothing (no re-derived legality)
    - `grep -nE "document|window|from \"react\"" apps/web/lib/hanabi-drag-logic.ts` returns nothing
    - All behaviour bullets have passing tests
  </acceptance_criteria>
  <done>Drop targets, reorder permutations, zone status, and drop requests are pure functions with passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → client | stored values may be absent, corrupted, or storage may throw |
| client notes → network | notes must never cross to the server (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-09 | Information Disclosure | hanabi-notes.ts | mitigate | Module imports no socket/store/network module; grep-gated in acceptance criteria and test |
| T-06.1-10 | Denial of Service | safe-storage.ts | mitigate | Every storage call wrapped in try/catch; throwing-storage tests prove the board cannot crash |
| T-06.1-11 | Tampering | readDiscardViewPref | mitigate | Unrecognised stored value falls back to "compact" |
| T-06.1-12 | Elevation of Privilege | requestForDrop | accept | Client-side gating is UX only; server canPlay/canDiscard/canReorder remain authoritative |
</threat_model>

<verification>
- `npx vitest run --project web safe-storage hanabi-notes hanabi-discard-logic hanabi-drag-logic` green
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
All pure logic for NOTE-02, D-13 discard views, and HAND-01/HAND-02 drag resolution exists and is tested, with one defensive storage path.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-03-SUMMARY.md` when done
</output>
