---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 08
type: execute
wave: 3
depends_on: ["06.1-04", "06.1-05"]
files_modified:
  - apps/web/components/hanabi/useHanabiAudio.ts
  - apps/web/components/hanabi/AudioControls.tsx
  - apps/web/lib/audio-controls-render.test.ts
  - apps/web/components/hanabi/HanabiBoard.tsx
autonomous: true
requirements: [AUD-01]

must_haves:
  truths:
    - "D-27: every connected screen plays the cue for each live clue, play, discard, misplay, and completed stack"
    - "D-29: mounting, refreshing, and recovering from Reconnecting… play no backlog of cues"
    - "D-26: the audio context is created/resumed only after the first pointerdown or keydown on the page"
    - "D-26: a mute toggle and volume slider sit in the bottom controls row and their state survives refresh"
  artifacts:
    - path: "apps/web/components/hanabi/useHanabiAudio.ts"
      exports: ["useHanabiAudio"]
    - path: "apps/web/components/hanabi/AudioControls.tsx"
      exports: ["AudioControls"]
  key_links:
    - from: "apps/web/components/hanabi/useHanabiAudio.ts"
      to: "apps/web/lib/hanabi-audio-cues.ts cuesForTransition"
      via: "effect on each new game view"
      pattern: "cuesForTransition"
    - from: "apps/web/components/hanabi/HanabiBoard.tsx"
      to: "useHanabiAudio + AudioControls"
      via: "hook call and bottom-row render"
      pattern: "useHanabiAudio\\("
---

<objective>
Wire AUD-01 into the board: a hook that turns live history transitions into sounds (with reconnect baseline reset and gesture unlock), and icon-only mute/volume controls in the bottom controls row.

Purpose: Completes the audio requirement end to end on every screen, using only the pure modules from 06.1-04.
Output: useHanabiAudio hook, AudioControls component, render test, HanabiBoard wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-04-SUMMARY.md

<interfaces>
From 06.1-04:
- apps/web/lib/hanabi-audio-cues.ts: type Cue; cuesForTransition(prev: { history; stacks } | null, next: { history; stacks }): Cue[]
- apps/web/lib/hanabi-audio-synth.ts: createAudioEngine(deps?) → { unlock(); play(cue); setMuted(m); setVolume(v) }; readAudioPrefs(): { muted; volume }; writeAudioPrefs(prefs); DEFAULT_AUDIO_PREFS
From apps/web/components/hanabi/HanabiBoard.tsx: props { view: RoomView; onAction; reconnecting?: boolean }; `game: HanabiView | null`; bottom row `<div className="flex flex-none flex-wrap items-start justify-center gap-[length:var(--space-md)]">` holding OwnHand, CardActions, CluePicker; in-game <main> has className "... table-backdrop" (06.1-05)
lucide-react 1.39.0 is installed (Volume2, VolumeX icons)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useHanabiAudio hook and AudioControls component</name>
  <files>apps/web/components/hanabi/useHanabiAudio.ts, apps/web/components/hanabi/AudioControls.tsx, apps/web/lib/audio-controls-render.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-audio-cues.ts
    - apps/web/lib/hanabi-audio-synth.ts
    - apps/web/components/hanabi/HanabiBoard.tsx (ref-based first-render skip and timer patterns)
    - apps/web/components/hanabi/CardActions.tsx (button row styling, focus ring classes)
    - apps/web/lib/own-hand-render.test.ts (renderToStaticMarkup pattern)
  </read_first>
  <behavior>
    - AudioControls({ muted: false, volume: 0.5, ... }) markup has a button with aria-label "Mute sound" and data-testid "audio-mute-toggle", and an input type="range" with aria-label "Sound volume", data-testid "audio-volume", min 0, max 100, value 50
    - AudioControls({ muted: true, ... }) markup has aria-label "Unmute sound"
    - Both controls carry min 44px touch targets (style minWidth/minHeight var(--size-touch-min)) and the accent focus-visible outline classes
    - useHanabiAudio.ts source contains no reference to AudioContext directly (engine owns it) and registers pointerdown and keydown listeners with { once: true } semantics or equivalent removal
  </behavior>
  <action>
    Per D-24..D-29. Create components/hanabi/useHanabiAudio.ts ("use client") exporting `useHanabiAudio(game: HanabiView | null, reconnecting: boolean): { muted: boolean; volume: number; setMuted(m: boolean): void; setVolume(v: number): void }`. Inside: engineRef lazily set to `createAudioEngine()` in an effect (never during render/SSR); prefs state initialised to DEFAULT_AUDIO_PREFS then loaded from readAudioPrefs() in a mount effect and pushed to the engine; a mount effect adds window `pointerdown` and `keydown` listeners that call engine.unlock() and remove themselves after the first call (cleanup removes them on unmount); `prevRef = useRef<{ history; stacks } | null>(null)`; an effect on [reconnecting] sets prevRef.current = null whenever reconnecting is true (D-29 baseline reset); an effect on [game] that, when game is non-null and not reconnecting, computes `cuesForTransition(prevRef.current, { history: game.history, stacks: game.stacks })`, plays each cue in order (engine.play), then sets prevRef.current to the new snapshot. setMuted/setVolume update state, the engine, and writeAudioPrefs. Create components/hanabi/AudioControls.tsx exporting `AudioControls({ muted, volume, onToggleMute, onVolumeChange })`: a flex row with an icon-only button (VolumeX when muted, Volume2 otherwise, aria-hidden icons, aria-label per UI-SPEC copy, aria-pressed={muted}) and a range input 0-100 bound to Math.round(volume*100) calling onVolumeChange(value/100); colours --color-text / --color-text-muted / --color-border only (no accent except focus ring). Write audio-controls-render.test.ts per behaviour bullets (render test + readFileSync source check of the hook).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web audio-controls-render && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cuesForTransition" apps/web/components/hanabi/useHanabiAudio.ts` matches
    - `grep -n "prevRef.current = null" apps/web/components/hanabi/useHanabiAudio.ts` matches (reconnect baseline reset)
    - `grep -n "new AudioContext" apps/web/components/hanabi/useHanabiAudio.ts apps/web/components/hanabi/AudioControls.tsx` returns nothing
    - audio-controls-render.test.ts passes; `npx tsc -b apps/web` exits 0
  </acceptance_criteria>
  <done>A gesture-unlocked, reconnect-safe audio hook and accessible mute/volume controls exist.</done>
</task>

<task type="auto">
  <name>Task 2: Wire audio into HanabiBoard</name>
  <files>apps/web/components/hanabi/HanabiBoard.tsx</files>
  <read_first>
    - apps/web/components/hanabi/HanabiBoard.tsx
    - apps/web/components/hanabi/useHanabiAudio.ts
    - apps/web/components/hanabi/AudioControls.tsx
    - e2e/start-game.spec.ts (UI-11 fit test)
  </read_first>
  <action>
    Per D-26, D-27. In HanabiBoard, call `const audio = useHanabiAudio(game, reconnecting);` unconditionally near the other hooks (before the `if (!game)` early return, respecting hook order rules). In the bottom controls row, render `<AudioControls muted={audio.muted} volume={audio.volume} onToggleMute={() => audio.setMuted(!audio.muted)} onVolumeChange={audio.setVolume} />` after CluePicker. Audio controls stay enabled while reconnecting or ended (they are local preferences, not game actions). Do not change any other behaviour. Run the UI-11 fit test; if the controls push the layout past 1280x720, tighten only the bottom-row gap from --space-md to --space-sm and record the deviation.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web && npx vitest run --project web && npx playwright test -g "UI-11|RT-01"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useHanabiAudio(game, reconnecting)" apps/web/components/hanabi/HanabiBoard.tsx` and `grep -n "<AudioControls" apps/web/components/hanabi/HanabiBoard.tsx` match
    - `npx playwright test -g "UI-11|RT-01"` exits 0
    - `npx vitest run --project web` exits 0
  </acceptance_criteria>
  <done>Every screen plays live cues, stays silent on load/reconnect, and shows mute/volume controls without breaking fit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-pushed history → client audio | public entries only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-24 | Denial of Service | cue burst after reconnect | mitigate | Baseline reset to null while reconnecting; first post-reconnect view plays nothing |
| T-06.1-25 | Information Disclosure | cue selection | accept | Uses only history every seat already receives |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0; `npx playwright test -g "UI-11|RT-01"` green
</verification>

<success_criteria>
AUD-01 is live on the board with autoplay-safe unlock, no catch-up, and persisted mute/volume.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-08-SUMMARY.md` when done
</output>
