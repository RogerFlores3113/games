---
status: partial
phase: 06.1-table-polish-firework-art-notes-drag-audio
source: [06.1-CONTEXT.md D-08, D-09, D-10, D-31]
started: 2026-09-18T00:00:00Z
updated: 2026-09-17T00:00:00Z
---

## Current Test

### 1. Burst art preview (ART-01, D-08..D-10)

Preview URL: http://localhost:3100/dev/fireworks

Suit silhouettes (from SUIT_VISUALS descriptors):
- Red — chrysanthemum star burst, 12 spikes, solid
- Yellow — round-petal burst, 16 spikes, solid
- Green — hollow ring burst, 10 spikes, 1 ring
- Blue — long/short cross-star, 8 spikes, solid
- White — sparkler, 6 spikes, solid
- Rainbow — large flat burst, 20 spikes, solid single-tone fill
- Black — double ring, 8 spikes, 2 rings, hollow

What to check:
- Every suit's burst is distinct from every other in the "Colour ignored (grayscale)" section (12px, 20px, 64x84 cards)
- Rainbow reads as its own burst without multicolour fill
- Rank counts 1-5 legible at 64x84 (burst count + corner numeral)
- At 48x64 (stack heads, discard minis): burst count + numeral, or numeral only?
- Card back looks identical for every card and reveals nothing
- Art reads as original firework work in the spirit of the box game, not a copy

result: pass

small-card decision: showBurstCount=true at 48x64; corner numeral removed at all sizes (48x64 and 64x84)

## Owner's verbatim reply

"burst art looks good. For the 5, make the center burst larger by 50%ish. I'd like if rainbow was actually rainbow as well."

"Number isnt needed - the cards are obvious. just burst count please."

"peel th enumber of the 64x84 cards as well"

"large burst in the center is already improved. good. Rainbow color works, great job. Bursts only with no numbers is done, good job"

"the card back - make it the outline of a rectangle - this is too cardlike, it should be tilelike."

"no - that cards have the outline, then the back has another outline inside it. like a picture frame"

"approved."

## Changes after review

1. **Rank-5 center burst enlarged 1.5x.** `BURST_LAYOUTS[5]` in `apps/web/lib/suit-visuals.ts`: center placement scale 0.35 -> 0.525; the four corner placements nudged outward (0.24/0.76 -> 0.22/0.78) so they stay clear of the larger center burst instead of clipping into it.
2. **Rainbow suit is now an actual multicolour gradient**, not a flat single tone. `SuitGlyph.tsx` renders Rainbow's burst path with an SVG `linearGradient` (stops: `--color-suit-red` -> `--color-suit-yellow` -> `--color-suit-green` -> `--color-suit-blue` -> `--color-suit-rainbow`), using only existing `--color-suit-*` tokens (no new hex, per D-11). Each `SuitGlyph` instance mints its own gradient id via React's `useId()` so many rainbow bursts on one card (e.g. all 5 on a rank-5 card) or many cards on a table never collide on the gradient's DOM id. This **overrides** D-08's original "Rainbow is always a flat single-tone fill, never a gradient" rule — the owner explicitly asked for a real rainbow. Rainbow's silhouette (20-spike flat burst) is unchanged, so it still reads as its own distinct shape in the grayscale/colourblind-safe section.
3. **Corner numeral removed at every card size** (48x64 and 64x84/88x112) — rank now reads from burst count only. `FireworkCardFace` no longer renders any visible numeral text. It gained an accessible `title`/`aria-label` of `"{Suit} {rank}"` (e.g. `"Red 3"`) on the wrapper `<span role="img">`, gated by the same `exposeSuit` boolean that already gates `data-glyph` — an own-hand card the viewer isn't supposed to identify never gets this label either, so no new identity leak. The `numeralSize`/`NumeralSize` prop and type were removed from `FireworkCardFace` since there is no visible numeral left to size.
4. Updated `apps/web/lib/firework-card-render.test.ts` (numeral-absence assertions, new accessible-label assertions, new rainbow-gradient and unique-gradient-id assertions) and `apps/web/app/dev/fireworks/page.tsx` (48x64 row now shows the settled "burst count only" rendering with `exposeSuit`, all rows pass `exposeSuit` so the accessible label is visible for review).
5. Searched the repo for anything outside `FireworkCard.tsx`/`SuitGlyph.tsx` that depended on the visible numeral (other components, `e2e/*.spec.ts`, other test files): none found. `FireworkCardFace`/`FireworkCardBack` are not yet wired into the live table (that lands in 06.1-09/06.1-10), and no e2e spec references `FireworkCard`, `data-glyph`, or a rendered rank numeral today.
6. **Card back redone as a tile with a picture-frame double outline** (two rounds after full burst-art approval): `CARD_BACK_ART` in `apps/web/lib/suit-visuals.ts` no longer has the unlit-shell diamond emblem or the soft glow arc. First pass: a flat `var(--color-bg)` tile fill plus a single `var(--color-border)` rectangular outline. Owner then asked for a second, inset outline as well ("like a picture frame") — the final version is the tile fill plus TWO concentric `var(--color-border)` rectangular outlines (the outer edge outline, and an evenly-inset inner outline), each rendered as its own evenodd ring layer, with an empty interior and square corners. `CardBackLayer` gained an optional `fillRule` field, and `FireworkCardBack`'s wrapper background changed from `var(--color-surface)` to `transparent` (the SVG's own background rect now owns the fill). Still identical for every card, still carries zero suit/rank signal, and `FireworkCardBack`'s props (`width`/`height` only) and existing test contracts (no `data-glyph`, no `--color-suit-`, no `card-identity`, byte-identical across renders) are unchanged.

Current test: all change requests applied and owner-approved, including the picture-frame card back (owner reply: "approved."). Burst art owner gate closed; final D-31 sign-off comes in 06.1-15.

### 2. Table polish sign-off (D-31; ART-01, ART-02, NOTE-01, NOTE-02, HAND-01, HAND-02, HAND-03, AUD-01)

#### Gate commands and result

Run fresh from the repo root after killing any stray dev servers on 3100/8787 (`fuser -k 3100/tcp 8787/tcp`):

| Command | Result |
|---|---|
| `npm test` | **PASS** — 61 test files, 729/729 tests passed |
| `npx playwright test` | **PASS** — 31/31 tests passed, including the previously-flaky `RT-04: a frozen, hidden tab is detected as disconnected and resumes its seat on return without a reload` (`e2e/hanabi-realtime.spec.ts:404`) |
| `npx tsc -b apps/web packages/rules packages/schema apps/worker` | **PASS** — exit 0, no diagnostics |

All three commands were run once each, in sequence, with no retries needed — no flake occurred this run.

**Fixed during gate:** `RT-04` (`e2e/hanabi-realtime.spec.ts:404`) exposed a pre-existing bug, not a test flake: `partysocket` ignores a `reconnect()` call made while it is already mid-backoff, so a tab resumed from hidden/frozen state could wait up to 30s for its socket to actually reconnect. Fixed in commit `9acc73d` ("fix(06.1): reconnect immediately on resume while partysocket is mid-backoff", `apps/web/lib/room-socket.ts`) by swapping in a fresh socket with a stable connection id instead of relying on the stalled backoff timer. Owner approved this commit.

**Follow-up note (test quality, not a product bug):** `RT-04`'s current test attempts to suppress the browser's `online` event so the test proves the visibility-change reconnect path in isolation, but the suppression is ineffective — a capture-phase listener added later in the test does not stop listeners `partysocket` already registered earlier. The test currently passes via either trigger (visibility change or the `online` event), so it doesn't cleanly isolate the visibility path it claims to test. It should be reworked in a follow-up plan to prove the visibility-only path.

#### Background image credit (from `apps/web/public/backgrounds/CREDITS.md`)

- Image: `city-night.webp` (city-at-night, per the owner's Phase 6 request)
- Author: PattayaPatrol
- Source: Wikimedia Commons — https://commons.wikimedia.org/wiki/File:DFC_4635_Pattaya_lights_sparkle_under_a_clear_night_sky_-_the_city_glowing_with_life_from_street_level_to_skyline.jpg
- License: CC BY-SA 4.0 (Creative Commons Attribution-ShareAlike 4.0 International) — https://creativecommons.org/licenses/by-sa/4.0/
- Retrieved 2026-09-17; resized to 1920px wide and re-encoded as WebP before committing (stripping any embedded metadata from the original file)

#### Deviations for owner review

Required items (each confirmed against the SUMMARYs or current code before listing):

1. **Own-hand note row stacked under the marks zone, not beside it on the marks row's right edge.** `MarksZone.tsx`: for `scale === "own"` a second fixed 20px row is stacked *below* the 28px candidate-pip band, rather than beside it — an 88px-wide card can't fit legible candidate pips and a readable note side by side. (06.1-09, 06.1-11)
2. **Told clue marks render as a glow/ring drawn on the matching candidate pip, not as a separate row of marks.** `CandidateStrip.tsx` gives a told suit or rank pip a `boxShadow` ring (`0 0 0 1px var(--color-card-glow)`) plus a "Told …" title/aria-label, instead of a separate marks row. (06.1-09)
3. **The discard overlay reopens after refresh if the player left it expanded**, per a literal reading of D-13. `Table.tsx` persists the compact/expanded discard-view preference (`readDiscardViewPref`/`writeDiscardViewPref`) and re-applies it on mount, so an expanded overlay stays expanded across a reload. (06.1-10)
4. **`canReorder` refuses reorders after the game is over.** `packages/rules/src/hanabi/legality.ts`'s `canReorder` returns `{ legal: false, reason: "game_over" }` once the game has ended, matching every other action's game-over guard. (06.1-01)
5. **Card sizes reduced from the sizes 06.1-09 first tried, to hold the 1280x720 no-scroll fit (UI-11) after the marks-zone band was added.** `OwnHandCard` height 112→100; `TeammateCard` height 84→78. (06.1-09)
6. **Touch targets for small controls (the discard-view toggle, the note-chip's tap target) are moved out of document flow** via an absolutely-positioned, `aria-hidden` inset span sized to the full 44px touch-target minimum, so the visible control can stay small without growing the row's layout height and breaking the UI-11 fit. (06.1-10, 06.1-11)

Owner's own art overrides from the 06.1-07 sign-off (already approved there; listed again for the full-phase record):

7. **Rank numerals removed at every card size** — rank now reads from burst count alone, at both 48x64 and 64x84/88x112.
8. **Rainbow rendered as an actual multicolour gradient** (not a flat single tone), overriding the original "Rainbow is always flat" rule at the owner's explicit request.
9. **The rank-5 center burst enlarged 1.5x** relative to the other ranks' layout.
10. **The card back redone as a tile with a picture-frame double outline** (two concentric rectangular outlines with an empty interior), replacing the original unlit-shell/glow-arc design.

Other deviations recorded in this phase's SUMMARYs, included here for completeness (none change owner-visible behavior):

11. Plan 05: Unsplash/Pexels were unreachable for scripted license verification (bot-challenge walls); the background image was sourced from Wikimedia Commons instead, which has the same free-license guarantee with machine-verifiable metadata.
12. Plans 01, 03, 04, 08, 12: internal fixes only (a property-test type-narrowing gap, doc-comment rewording that tripped the plan's own greps, a `wave`→`shape` field rename, and a test fixture updated for new props) — no behavior or visual change.
13. Plans 09, 10: `numeralSize` omitted from two `FireworkCardFace` call sites, consistent with 06.1-07's numeral removal above — no additional visible change beyond item 7.
14. Plan 14: two test-only bug fixes found while writing new Playwright tests (a gesture-click coordinate and a teammate-seat-id lookup) — no application code touched.

#### How to verify (from Task 2)

1. Open http://localhost:3100, create a room (try Base, and Rainbow if you like), join from a private window, start the game
2. Art: every suit's burst is distinct (try squinting / ignoring colour), rank counts read at a glance, your own cards show only the card back
3. Background: the city-at-night image looks right and cards/text stay easy to read (say if you want a different image)
4. Marks and notes: give a clue — marks appear above the touched cards on both screens; type a short note on one of your cards, refresh, it is still there; play that card, its note is gone
5. Drag: drag one of your cards onto another slot (also when it is not your turn) — the other window shows the new order, also after refresh; on your turn drag a card onto the fireworks to play and onto the discard pile to discard; drop somewhere else and it snaps back; while dragging off-turn, the zones say why they are disabled
6. Draws: after a play or discard the new card appears in the same slot on both screens, and the other window sees the card fly to its destination
7. Discard pile: compact by default; the toggle opens the full-art overlay; Esc closes it
8. Sounds: click anywhere once, then clue / play / discard / misplay / complete a stack if possible — each sound is distinct and soft; misplay plays only the fuse sound; mute and volume work and are remembered after refresh; refreshing mid-game plays no burst of old sounds
9. Review the listed deviations in 06.1-HUMAN-UAT.md and say whether each is acceptable

result: issues

## Owner's verbatim reply (table polish)

"good improvements! next steps: drag + dropping tiles to rearrange your hand should move the other tiles to the side visually for you, so it's clear what goes where. The automatic tile notes should be remade - here's a new format for them. When someone gives you a hint, that should highlight the tile in that color, not just a yellow highlight. If someone tells you a number, it should appear on the back of that tile. This is what happens when someone gives you a hint. There should be a toggle "keep hints visible" - if you toggle this, then hints stay visible after the next player moves. If it's not toggled, the hints dissapear after the next player moves. There should be a blank text box above each tile, with a faint visible outline - barely visible note area, but easy to click, type, and autosave. Only visible to the player themselves, not others. For the game itself - The tile background should be visually distinct from the board. The board should have a wooden texture to it or something. Tiles should have a color that the player can set via a color picker. The board itself - the "played" fireworks stack should display each firework, clearly. The discard should display all of them as well, but rearrangable by any player, visible to all. Clues should be represented by a black token with a blue question mark in the middle, and each clue token should be visible as a line of tokens going top->bottom. Fuses are similar - black token, yellow-with-orange-red rim explosion in the center. Put them next to tokens. When tokens are used up, remove them from the board visually. Put a visible but unobtrusive outline around each of the board areas - and label them "Play" and "Discard". Keep the amount of tiles left as a number - so "48 x [IMG of back of tile]" and put that indicator between the play area which is the top of the board, and the discard which is the bottom. Clue and fuse tokens go on the right."

## Gaps

The owner approved the phase 06.1 work delivered so far ("good improvements!") — none of the items below are defects in what was built; they are follow-on feature requests for a subsequent plan.

1. Drag reorder should visually shift the other tiles aside during the drag so the drop position is clear, not just snap on drop.
2. Automatic tile notes should be remade in a new format. **BLOCKED:** the owner said "here's a new format for them" but no format or attachment actually came through in the reply — needs the owner's format before this can be planned.
3. A clue should highlight the touched tile in that clue's own colour, not a generic yellow highlight.
4. A number clue should cause the number to appear on the back of the touched tile.
5. Add a "keep hints visible" toggle: on = hints persist past the next player's move; off (current behavior) = hints clear after the next player's move.
6. Add a faint, always-present note box above each tile — barely visible outline, click to type, autosaves, visible only to the tile's own player.
7. The tile background should be visually distinct from the board background.
8. The board should get a wooden texture or similar surface treatment.
9. Players should be able to set their own tile colour via a colour picker.
10. The played-fireworks stack should display every firework in the stack clearly (not just the top/current card).
11. The discard pile should display every discarded tile, rearrangeable by any player, with the arrangement visible to everyone — this is shared state, so (unlike the private notes) it needs a server-side action, not client-local storage.
12. Clue tokens should render as a black token with a blue question mark in the middle, shown as a vertical line of tokens (top to bottom).
13. Fuse tokens should render as a black token with a yellow explosion with an orange-red rim in the center, placed next to the clue tokens.
14. Spent tokens (clue or fuse) should be visually removed from the board once used, not just decremented in a counter.
15. Add a visible but unobtrusive outline around each board area, labelled "Play" and "Discard".
16. Add a deck-remaining counter shown as "48 x [card-back image]", positioned between the Play area (top of the board) and the Discard area (bottom of the board).
17. Clue and fuse tokens should be positioned on the right side of the board.

