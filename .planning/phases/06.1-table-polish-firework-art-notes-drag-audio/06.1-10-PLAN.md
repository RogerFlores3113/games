---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 10
type: execute
wave: 4
depends_on: ["06.1-03", "06.1-07", "06.1-09"]
files_modified:
  - apps/web/components/hanabi/Table.tsx
  - apps/web/components/hanabi/DiscardOverlay.tsx
  - apps/web/lib/discard-overlay-render.test.ts
  - e2e/start-game.spec.ts
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "D-12: stack heads render the firework card face (per the owner's 48x64 burst-count decision) with exactly one data-glyph each"
    - "D-13: the discard pile on the board is compact by default — one suit symbol per suit with per-rank counts"
    - "D-13: a toggle opens a full-art expanded discard overlay over the table that closes on close button, scrim click, or Esc, without shifting layout; the open/closed preference is remembered per browser"
    - "discard-pile exposes data-discard-count so tests and players see totals regardless of view"
  artifacts:
    - path: "apps/web/components/hanabi/DiscardOverlay.tsx"
      exports: ["DiscardOverlay"]
  key_links:
    - from: "apps/web/components/hanabi/Table.tsx"
      to: "apps/web/lib/hanabi-discard-logic.ts"
      via: "groupDiscardsBySuit + read/writeDiscardViewPref"
      pattern: "groupDiscardsBySuit"
---

<objective>
Apply the approved firework art to stack heads and the discard pile, and implement the two discard views: compact on the board, full-art overlay on demand.

Purpose: ART-01 reach (D-12) and D-13. The compact view also keeps the 1280x720 fit as the discard pile grows.
Output: Reworked Table.tsx, new DiscardOverlay.tsx, render test, updated e2e discard-count locators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-PATTERNS.md

<interfaces>
From 06.1-03 apps/web/lib/hanabi-discard-logic.ts: type DiscardView = "compact" | "expanded"; groupDiscardsBySuit(discard, variant): { suit; total; countsByRank: number[5] }[]; readDiscardViewPref(); writeDiscardViewPref(view)
From 06.1-02: FireworkCardFace({ suit, rank, width, height, exposeSuit?, showBurstCount?, numeralSize? }); SuitGlyph({ suit, size, title?, exposeSuit? })
From apps/web/components/hanabi/Table.tsx: stack heads `played-stack-{suit}` 48x64 with data-top-rank/data-complete and flash; discard block `data-testid="discard-pile"` with <ul><li> per card; empty text "No cards discarded yet"
From apps/web/components/hanabi/EndOverlay.tsx: fixed inset-0 scrim rgba(11, 15, 26, 0.7) + role="dialog" aria-modal panel (--color-surface, --color-border), focus on mount via ref
e2e/start-game.spec.ts ~lines 211 and 226 count `[data-testid="discard-pile"] li`; ~line 103 expects one `[data-glyph]` per played stack
Owner decision in 06.1-HUMAN-UAT.md: "small-card decision: showBurstCount=<true|false>"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DiscardOverlay component</name>
  <files>apps/web/components/hanabi/DiscardOverlay.tsx, apps/web/lib/discard-overlay-render.test.ts</files>
  <read_first>
    - apps/web/components/hanabi/EndOverlay.tsx
    - apps/web/lib/hanabi-discard-logic.ts
    - apps/web/components/hanabi/FireworkCard.tsx
  </read_first>
  <behavior>
    - DiscardOverlay({ discard: [], variant: "base", onClose }) markup has role="dialog", aria-modal="true", heading text "Discard pile", data-testid "discard-overlay", and text "No cards discarded yet"
    - With discard [{id:"a",suit:"red",rank:1},{id:"b",suit:"red",rank:1},{id:"c",suit:"blue",rank:5}] markup has 3 elements with data-testid "discard-overlay-card", grouped red before blue per variant order, each with a FireworkCardFace
    - Close button has aria-label "Show compact discard pile" and data-testid "discard-overlay-close"
  </behavior>
  <action>
    Per D-12, D-13 and UI-SPEC copy. Create DiscardOverlay.tsx ("use client") exporting `DiscardOverlay({ discard, variant, onClose, showBurstCount })`: EndOverlay-shaped `fixed inset-0` scrim (rgba(11, 15, 26, 0.7)) with onClick={onClose} on the scrim only (panel stops propagation), role="dialog" aria-modal aria-labelledby panel (max-w-3xl, --color-surface, --color-border, --space-lg padding), heading "Discard pile" at --text-heading, an icon-only close button (lucide X, aria-hidden icon, 44px target) focused on mount via ref, a window keydown listener closing on "Escape" (removed on unmount), and body: for each group from groupDiscardsBySuit with total > 0, a row of FireworkCardFace 48x64 (exposeSuit false; add sr-only "<Label> <rank>") for every discarded card of that suit sorted by rank, each wrapped with data-testid "discard-overlay-card"; empty state "No cards discarded yet" in --color-text-muted label role. Write discard-overlay-render.test.ts per behaviour bullets.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web discard-overlay-render</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Escape" apps/web/components/hanabi/DiscardOverlay.tsx` and `grep -n 'aria-modal' apps/web/components/hanabi/DiscardOverlay.tsx` match
    - discard-overlay-render.test.ts passes
  </acceptance_criteria>
  <done>An accessible full-art discard overlay exists and is render-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Stack-head art, compact discard, toggle wiring, e2e locators</name>
  <files>apps/web/components/hanabi/Table.tsx, e2e/start-game.spec.ts</files>
  <read_first>
    - apps/web/components/hanabi/Table.tsx
    - apps/web/components/hanabi/DiscardOverlay.tsx (Task 1)
    - apps/web/lib/hanabi-discard-logic.ts
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-HUMAN-UAT.md (small-card decision)
    - e2e/start-game.spec.ts (lines 90-115, 200-235, UI-11)
  </read_first>
  <action>
    Per D-12, D-13. Stack heads: for topRank > 0 replace the SuitGlyph + numeral with `<FireworkCardFace suit={stack.suit} rank={stack.topRank} width={48} height={64} exposeSuit numeralSize="label" showBurstCount={<owner decision>} />`; for topRank 0 render a single SuitGlyph at size 18 with exposeSuit, opacity 0.35, plus "—" (keeps exactly one data-glyph per stack); keep testids, data-top-rank, data-complete, completed glow, flash class, and sr-only suit text. Discard block: keep `data-testid="discard-pile"` and add `data-discard-count={game.discard.length}` and `data-view={view}`; read the preference with readDiscardViewPref() in a mount effect (initial state "compact" for SSR safety). Compact view: header row "Discard" label + an icon-only toggle button (lucide Layers or Maximize2, aria-label "Show full discard pile", data-testid "discard-toggle", 44px target); then, if the pile is non-empty, one compact row per group with total > 0: SuitGlyph size 14 (title = suit label) followed by rank counts rendered as "1×2 3×1" (only ranks with count > 0) in --text-label; otherwise "No cards discarded yet". Clicking the toggle sets view "expanded" and writeDiscardViewPref("expanded"); when view is "expanded" render `<DiscardOverlay ... onClose={() => { set "compact"; writeDiscardViewPref("compact"); }} />` in addition to the compact block (overlay covers the table; layout underneath unchanged). e2e/start-game.spec.ts: replace the two `[data-testid="discard-pile"] li` counts with `Number(await hostPage.getByTestId("discard-pile").getAttribute("data-discard-count"))`; keep the one-`[data-glyph]`-per-stack assertion. Run UI-11 and the start-game spec.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx tsc -b apps/web && npx vitest run --project web && npx playwright test e2e/start-game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FireworkCardFace" apps/web/components/hanabi/Table.tsx`, `grep -n "data-discard-count" apps/web/components/hanabi/Table.tsx`, and `grep -n "groupDiscardsBySuit" apps/web/components/hanabi/Table.tsx` match
    - `grep -n 'discard-pile\"\] li' e2e/start-game.spec.ts` returns nothing
    - `npx playwright test e2e/start-game.spec.ts` exits 0 (including UI-11 fit)
  </acceptance_criteria>
  <done>Stacks and discards use firework art, discard defaults to compact with a remembered overlay toggle, and e2e is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage pref → UI state | untrusted stored value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-29 | Tampering | discard view pref | mitigate | readDiscardViewPref falls back to "compact" for unknown values (06.1-03) |
| T-06.1-30 | Information Disclosure | discard/stack art | accept | Discards and stacks are public to every seat already |
| T-06.1-31 | Denial of Service | overlay traps focus/keys | mitigate | Esc, scrim click, and close button all close; keydown listener removed on unmount |
</threat_model>

<verification>
- `npx vitest run --project web` green; `npx tsc -b apps/web` exits 0; `npx playwright test e2e/start-game.spec.ts` green
</verification>

<success_criteria>
ART-01 reaches stack heads and discards, and D-13's compact/expanded discard views work with a remembered preference.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-10-SUMMARY.md` when done
</output>
