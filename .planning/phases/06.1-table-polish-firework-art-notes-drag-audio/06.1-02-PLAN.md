---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/suit-visuals.ts
  - apps/web/lib/suit-visuals.test.ts
  - apps/web/components/hanabi/SuitGlyph.tsx
  - apps/web/components/hanabi/FireworkCard.tsx
  - apps/web/lib/firework-card-render.test.ts
  - apps/web/app/dev/fireworks/page.tsx
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "D-08: each of the 7 suits has an original firework-burst silhouette in suit-visuals.ts, pairwise distinct with colour ignored; Rainbow is a flat single-tone fill"
    - "D-09: a face-up card renders a burst count equal to its rank plus a small corner numeral"
    - "D-10: a single neutral card back exists whose markup is identical for every card and carries no suit hue or data-glyph"
    - "D-11: all art is inline SVG coloured only via var(--color-suit-*) / existing @theme tokens, behind the FireworkCardFace / FireworkCardBack component boundary"
    - "Owner can open a dev-only preview page showing every burst, every rank count, and the card back at all in-game sizes, including a grayscale view"
  artifacts:
    - path: "apps/web/lib/suit-visuals.ts"
      provides: "burst silhouettes, silhouette descriptors, burst layout per rank, card-back paths"
      exports: ["SUIT_VISUALS", "suitVisual", "burstLayoutForRank", "CARD_BACK_ART"]
    - path: "apps/web/components/hanabi/FireworkCard.tsx"
      provides: "FireworkCardFace and FireworkCardBack components"
      exports: ["FireworkCardFace", "FireworkCardBack"]
    - path: "apps/web/app/dev/fireworks/page.tsx"
      provides: "dev-only burst preview for the owner gate"
  key_links:
    - from: "apps/web/components/hanabi/FireworkCard.tsx"
      to: "apps/web/lib/suit-visuals.ts"
      via: "SuitGlyph + burstLayoutForRank"
      pattern: "burstLayoutForRank"
---

<objective>
Author the firework-burst art (7 suit silhouettes, rank-count layouts, neutral card back) in the single art module and expose it through card-face/card-back components plus a dev-only preview page, so the owner can review the shapes before they are built into the table (owner gate is plan 06.1-07).

Purpose: ART-01, D-08..D-11. Reworking `SUIT_VISUALS` in place keeps one source of truth; since `SuitGlyph` consumers (clue picker, pips, stacks, end overlay) read the same module, they switch to bursts automatically. Card composition into the live table waits for owner approval.
Output: Reworked suit-visuals.ts / SuitGlyph.tsx, new FireworkCard.tsx, preview route, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md

<interfaces>
From apps/web/lib/suit-visuals.ts (current):
- export interface SuitVisual { label: string; hueVar: string; glyphPath: string }
- export const SUIT_VISUALS: Readonly<Record<Suit, SuitVisual>>; export function suitVisual(suit)
From apps/web/components/hanabi/SuitGlyph.tsx:
- SuitGlyph({ suit, size, className?, title?, exposeSuit? }) renders <svg viewBox="0 0 24 24"> with one <path d={glyphPath} style={{ fill: hueVar }} />, data-glyph only when exposeSuit
Existing e2e expectations that must keep holding once cards use these components (plan 06.1-09/10):
- `[data-testid^="played-stack-"] [data-glyph]` count 1 per stack; `[data-testid="card-identity"] [data-glyph]` count 1 per teammate card
Tokens available (apps/web/app/globals.css @theme): --color-suit-{red,yellow,green,blue,white,rainbow,black}, --color-surface, --color-border, --color-text, --color-text-muted, --color-card-glow, --color-bg
Card sizes in use: teammate 64x84, own 88x112, stack head / discard mini 48x64, candidate pips 10-12px, clue buttons ~20px glyph
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Burst silhouettes, rank layouts, and card-back art in suit-visuals.ts</name>
  <files>apps/web/lib/suit-visuals.ts, apps/web/lib/suit-visuals.test.ts, apps/web/components/hanabi/SuitGlyph.tsx</files>
  <read_first>
    - apps/web/lib/suit-visuals.ts
    - apps/web/lib/suit-visuals.test.ts
    - apps/web/components/hanabi/SuitGlyph.tsx
    - apps/web/app/globals.css (@theme tokens)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md (Suit Art Rework table)
  </read_first>
  <behavior>
    - SUIT_VISUALS keys exactly equal ALL_SUITS; every glyphPath non-empty and pairwise distinct
    - Every suit has a `silhouette` descriptor { spikes: number; rings: 0 | 1 | 2; hollow: boolean }, and the 7 descriptor tuples are pairwise distinct (colour-ignored distinctness contract)
    - rainbow's visual has no gradient/multicolour field: hueVar is the single literal "var(--color-suit-rainbow)"
    - burstLayoutForRank(r) for r = 1..5 returns exactly r entries, each { cx, cy, scale } with 0 <= cx,cy <= 1 and scale in (0, 1]; no two entries in the same layout share the same (cx, cy)
    - CARD_BACK_ART contains only token var(...) colour references (no "#" hex, no suit hue var)
    - Every existing suit-visuals test (hue contrast, token-once, static literal hueVar) still passes
  </behavior>
  <action>
    Per D-08, D-09, D-10, D-11 (discretion: exact silhouettes). Rework SUIT_VISUALS in place (do not fork a new module). Keep the `glyphPath` field name (it is still the suit symbol) and replace each path with an original hand-authored firework burst on the 24x24 viewBox following the UI-SPEC table: red radiating 10-12 uniform spike chrysanthemum; yellow dense round-petal burst (rounded tips, more petals); green hollow ring of short trailing spikes (evenodd hole); blue 4 long + 4 short asymmetric cross-star; white sparse 5-6 long thin sparkler spikes; rainbow largest flat single-tone burst with many thin rays; black compact double ring of short spikes (two concentric rings, evenodd). Add fields `fillRule: "nonzero" | "evenodd"` and `silhouette: { spikes; rings; hollow }` to SuitVisual with values that honestly describe each path. Generate spike paths with a small pure helper inside this file (e.g. `starBurstPath(points, outerR, innerR, rotationDeg)` returning a path string) so shapes are reproducible, and freeze the resulting strings as the exported data. Add `export function burstLayoutForRank(rank: 1|2|3|4|5): ReadonlyArray<{ cx: number; cy: number; scale: number }>` using fixed box-game-style arrangements (1 centre large; 2 diagonal; 3 diagonal line; 4 corners; 5 corners + centre) in unit coordinates. Add `export const CARD_BACK_ART` describing a neutral unlit-shell night motif: an object with `viewBox`, and an array of `{ d: string; fillVar: string; opacity: number }` layers using only --color-surface, --color-border, --color-text-muted, --color-card-glow. Update SuitGlyph.tsx to pass `fillRule={visual.fillRule}` on the path; keep its props contract unchanged. Update the header comment of suit-visuals.ts to describe bursts (Phase 6.1 D-08) instead of the old glyph list. Extend suit-visuals.test.ts with the behaviour bullets above; keep existing tests (rename the describe to "suit-visuals" if needed so `-t "suit-visuals"` selects it).
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web suit-visuals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function burstLayoutForRank" apps/web/lib/suit-visuals.ts` and `grep -n "export const CARD_BACK_ART" apps/web/lib/suit-visuals.ts` both match
    - `grep -n "fillRule" apps/web/components/hanabi/SuitGlyph.tsx` matches
    - `grep -v '^\s*//' apps/web/lib/suit-visuals.ts | grep -c "#[0-9A-Fa-f]\{3,8\}"` outputs 0
    - suit-visuals.test.ts asserts distinct silhouette tuples and burstLayoutForRank counts; `npx vitest run --project web suit-visuals` exits 0
  </acceptance_criteria>
  <done>Seven original burst silhouettes with distinct descriptors, rank layouts, and card-back art live in suit-visuals.ts; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FireworkCardFace / FireworkCardBack components and dev preview page</name>
  <files>apps/web/components/hanabi/FireworkCard.tsx, apps/web/lib/firework-card-render.test.ts, apps/web/app/dev/fireworks/page.tsx</files>
  <read_first>
    - apps/web/components/hanabi/SuitGlyph.tsx (after Task 1)
    - apps/web/lib/suit-visuals.ts (after Task 1)
    - apps/web/lib/own-hand-render.test.ts (renderToStaticMarkup test pattern)
    - apps/web/components/hanabi/TeammateCard.tsx (rank numeral typography: 16px body 600 / 14px label 600)
    - apps/web/app/page.tsx (App Router page conventions)
  </read_first>
  <behavior>
    - FireworkCardFace({ suit, rank, width, height, exposeSuit: true, showBurstCount: true }) markup contains exactly one `data-glyph="<suit>"` (on the wrapper), exactly `rank` burst <svg> elements, and a corner numeral text equal to rank
    - With exposeSuit false the markup contains no `data-glyph`
    - With showBurstCount false the markup contains exactly one burst (suit symbol) plus the corner numeral (fallback for the smallest sizes, RESEARCH Open Question 3)
    - FireworkCardBack({ width, height }) markup is byte-identical across two renders and contains none of: "data-glyph", "--color-suit-", "card-identity"
    - Neither component's source contains a "#" hex colour literal
  </behavior>
  <action>
    Per D-09, D-10, D-11, D-12. Create components/hanabi/FireworkCard.tsx (no "use client" needed) exporting: `FireworkCardFace({ suit, rank, width, height, exposeSuit = false, showBurstCount = true, numeralSize = "body" | "label" })` — a relatively positioned span sized width x height; when exposeSuit the wrapper (not the inner bursts) carries `data-glyph={suit}`; it maps `burstLayoutForRank(rank)` to absolutely positioned `SuitGlyph` instances (never passing exposeSuit to them) sized from `scale` against min(width,height); a corner numeral at top-left using `--text-body` (numeralSize "body", 16px/600) or `--text-label` (14px/600) with `--color-text`; background `--color-surface`. Add `sr-only` text is NOT added here (consumers own accessible names). `FireworkCardBack({ width, height })` renders CARD_BACK_ART layers in an svg filling the box; it takes no suit/rank/facts props at all (structural D-10). Create firework-card-render.test.ts covering the behaviour bullets with renderToStaticMarkup and a readFileSync source check for hex literals. Create app/dev/fireworks/page.tsx: a server component that calls `notFound()` from next/navigation when `process.env.NODE_ENV === "production"`; otherwise renders on `--color-bg`: (a) a row per suit showing SuitGlyph at 10, 12, 20, 28, 64px with the suit label; (b) for each suit, FireworkCardFace ranks 1-5 at 64x84 (numeral body), 48x64 with showBurstCount true AND false side by side (numeral label), and 88x112; (c) FireworkCardBack at 88x112 and 64x84; (d) the whole grid repeated inside a wrapper with `style={{ filter: "grayscale(1)" }}` under a heading "Colour ignored (grayscale)". Headings use existing text roles only.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web firework-card-render suit-visuals && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function FireworkCardFace" apps/web/components/hanabi/FireworkCard.tsx` and `grep -n "export function FireworkCardBack" apps/web/components/hanabi/FireworkCard.tsx` match
    - `grep -n "notFound" apps/web/app/dev/fireworks/page.tsx` matches and the file references `NODE_ENV`
    - `grep -v '^\s*//' apps/web/components/hanabi/FireworkCard.tsx | grep -c "#[0-9A-Fa-f]\{3,8\}"` outputs 0
    - firework-card-render.test.ts passes all behaviour bullets; `npx tsc -b apps/web` exits 0
  </acceptance_criteria>
  <done>Card face/back components render the burst art per contract, and http://localhost:3100/dev/fireworks shows the full preview in dev only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server view → own-hand DOM | own card identity must never reach the DOM (D-15 carried forward) |
| production build → /dev/fireworks | dev preview must not be reachable in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-06 | Information Disclosure | FireworkCardBack | mitigate | Component accepts only width/height (no suit/rank/facts props); render test asserts identical markup with no suit hue or data-glyph |
| T-06.1-07 | Information Disclosure | app/dev/fireworks | mitigate | notFound() when NODE_ENV is production; page renders static art only, no game data |
| T-06.1-08 | Tampering | inline SVG path data | accept | Static literal strings authored in-repo, no user input reaches SVG attributes |
</threat_model>

<verification>
- `npx vitest run --project web suit-visuals firework-card-render` green
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
Burst art, rank layouts, and card back exist behind a component boundary with automated distinctness/redaction checks and a dev preview ready for the owner gate.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-02-SUMMARY.md` when done
</output>
