---
phase: 06.1-table-polish-firework-art-notes-drag-audio
plan: 04
type: execute
wave: 2
depends_on: ["06.1-03"]
files_modified:
  - apps/web/lib/hanabi-live-history.ts
  - apps/web/lib/hanabi-live-history.test.ts
  - apps/web/lib/hanabi-audio-cues.ts
  - apps/web/lib/hanabi-audio-cues.test.ts
  - apps/web/lib/hanabi-audio-synth.ts
  - apps/web/lib/hanabi-audio-synth.test.ts
autonomous: true
requirements: [AUD-01]

must_haves:
  truths:
    - "D-29: no cues are produced for the first view after mount, refresh, or a reconnect — only for history entries that arrive live afterwards"
    - "D-24/D-28: each play/discard/clue entry yields exactly one cue: misplay → fuse, completing play → stack-complete, other successful play → play, discard → discard, clue → clue, draw → none"
    - "D-25: sounds are synthesized Web Audio recipes (tone + filtered-noise layers), no audio files"
    - "D-26: nothing plays until unlock() is called from a user gesture; muted or volume 0 plays nothing; mute/volume persist per browser"
  artifacts:
    - path: "apps/web/lib/hanabi-live-history.ts"
      exports: ["liveHistoryEntries"]
    - path: "apps/web/lib/hanabi-audio-cues.ts"
      exports: ["Cue", "cueForEntry", "cuesForTransition"]
    - path: "apps/web/lib/hanabi-audio-synth.ts"
      exports: ["CUE_RECIPES", "createAudioEngine", "readAudioPrefs", "writeAudioPrefs", "DEFAULT_AUDIO_PREFS"]
  key_links:
    - from: "apps/web/lib/hanabi-audio-cues.ts"
      to: "apps/web/lib/hanabi-visual-logic.ts newlyCompletedStacks"
      via: "import — no second stack-completion derivation"
      pattern: "newlyCompletedStacks"
    - from: "apps/web/lib/hanabi-audio-synth.ts"
      to: "apps/web/lib/safe-storage.ts"
      via: "prefs persistence"
      pattern: "safeGetItem"
---

<objective>
Build AUD-01's pure logic: a shared "which history entries are live" helper (also used by the fly-to animation in 06.1-13), a cue selector with fuse/completion precedence, and an injectable Web Audio engine with synthesized tile-tap recipes and persisted mute/volume.

Purpose: D-24..D-29 are all decidable without a browser; keeping them pure makes the no-catch-up and single-cue rules testable. Wiring into the board is 06.1-08.
Output: Three lib modules with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-CONTEXT.md
@.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md

<interfaces>
From apps/web/lib/hanabi-visual-logic.ts:
- export type HistoryEntry = HanabiView["history"][number]  (play: { turn, type:"play", seatId, cardId, suit, rank, success }; discard: {..., suit, rank}; clue: { turn, type:"clue", seatId, targetSeatId, clue, touchedCardIds }; draw: { turn, type:"draw", seatId, cardId })
- export function newlyCompletedStacks(prev: HanabiView["stacks"], next: HanabiView["stacks"]): Suit[]
From apps/web/lib/safe-storage.ts (06.1-03): safeGetItem(key): string | null; safeSetItem(key, value): void
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live-history diff and cue selection</name>
  <files>apps/web/lib/hanabi-live-history.ts, apps/web/lib/hanabi-live-history.test.ts, apps/web/lib/hanabi-audio-cues.ts, apps/web/lib/hanabi-audio-cues.test.ts</files>
  <read_first>
    - apps/web/lib/hanabi-visual-logic.ts (HistoryEntry, newlyCompletedStacks, touchedCardIdsFromLatestClue)
    - apps/web/lib/hanabi-visual-logic.test.ts (fixture style)
    - apps/web/components/hanabi/HanabiBoard.tsx (prevHistoryLengthRef first-render skip pattern)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Pattern 2, Pitfall 4)
  </read_first>
  <behavior>
    - liveHistoryEntries(null, next) → []  (first view: mount/refresh/reconnect baseline)
    - liveHistoryEntries(prev, next) with next longer → next.slice(prev.length)
    - liveHistoryEntries(prev, next) with next shorter or equal → []  (new game / no change)
    - cueForEntry: play success=false → "fuse"; play success=true rank 5 with suit in completedSuits → "stack-complete"; play success=true otherwise → "play"; discard → "discard"; clue → "clue"; draw → null
    - cuesForTransition(null, next) → []
    - cuesForTransition(prev, next) for one successful red-5 play completing red → exactly ["stack-complete"] (never also "play")
    - misplay transition → exactly ["fuse"] (never also "play" or "discard")
    - discard followed by its draw entry → exactly ["discard"]
    - two live entries (clue then discard) → ["clue", "discard"] in order
  </behavior>
  <action>
    Per D-24, D-27, D-28, D-29. Create hanabi-live-history.ts exporting `liveHistoryEntries(prevHistory: readonly HistoryEntry[] | null, nextHistory: readonly HistoryEntry[]): HistoryEntry[]` with a header comment stating callers MUST pass null for the first view after mount and after any reconnect (the caller resets its baseline when `reconnecting` goes true). Create hanabi-audio-cues.ts exporting `type Cue = "clue" | "play" | "discard" | "fuse" | "stack-complete"`, `cueForEntry(entry: HistoryEntry, completedSuits: readonly Suit[]): Cue | null` implemented as ONE precedence chain (fuse > stack-complete > play), and `cuesForTransition(prev: { history: HistoryEntry[]; stacks: HanabiView["stacks"] } | null, next: { history; stacks }): Cue[]` which calls liveHistoryEntries and `newlyCompletedStacks(prev.stacks, next.stacks)` (imported, not re-derived). No React, no Web Audio in these files.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-live-history hanabi-audio-cues</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "newlyCompletedStacks" apps/web/lib/hanabi-audio-cues.ts` matches (import + call)
    - `grep -nE "AudioContext|from \"react\"" apps/web/lib/hanabi-audio-cues.ts apps/web/lib/hanabi-live-history.ts` returns nothing
    - All behaviour bullets have passing tests
  </acceptance_criteria>
  <done>Live-entry diffing and single-cue selection with D-24/D-28 precedence and D-29 baseline are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web Audio engine, tile-tap recipes, and persisted prefs</name>
  <files>apps/web/lib/hanabi-audio-synth.ts, apps/web/lib/hanabi-audio-synth.test.ts</files>
  <read_first>
    - apps/web/lib/safe-storage.ts
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-RESEARCH.md (Code Examples: Web Audio short envelope; Assumption A2 on noise layers)
    - .planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-UI-SPEC.md (Audio Cues table)
  </read_first>
  <behavior>
    - CUE_RECIPES has exactly the 5 Cue keys; JSON.stringify of each recipe is pairwise distinct; "fuse" differs from "play"
    - Every layer has attackMs >= 2 and a decay/release > attack (no hard on/off clicks) and peak gain <= 1
    - createAudioEngine({ createContext: fakeFactory }): play("clue") before unlock() creates no context and no nodes
    - After unlock(): play("clue") creates oscillator/buffer-source nodes via the fake context, each routed through a gain node whose ramp targets are scaled by volume
    - setMuted(true) → play creates no nodes; setVolume(0) → play creates no nodes
    - unlock() calls context.resume() when state is "suspended"; calling unlock twice creates only one context
    - If createContext throws, unlock() and play() do not throw (audio silently unavailable)
    - readAudioPrefs() with nothing stored returns DEFAULT_AUDIO_PREFS { muted: false, volume: 0.5 }; writeAudioPrefs({ muted: true, volume: 0.8 }) then read returns it; stored volume "abc" or 7 reads back clamped/defaulted into [0, 1]
  </behavior>
  <action>
    Per D-24, D-25, D-26 (discretion: sound design, default volume 0.5). Create hanabi-audio-synth.ts exporting: `type SoundLayer = { kind: "tone"; wave: "sine" | "triangle"; frequency: number; endFrequency?: number; startMs: number; attackMs: number; decayMs: number; gain: number } | { kind: "noise"; filterType: "bandpass" | "highpass" | "lowpass"; filterHz: number; q: number; startMs: number; attackMs: number; decayMs: number; gain: number }`; `CUE_RECIPES: Readonly<Record<Cue, readonly SoundLayer[]>>` designed as soft plastic/ceramic tile taps — clue: short bright bandpass noise tick (~2.5kHz) + faint high sine; play: warmer rounder tap (noise ~1.2kHz + triangle ~520Hz); discard: duller lower set-down (lowpass noise ~600Hz + sine ~220Hz, longer decay); fuse: two slightly detuned low triangles forming a short dissonant interval (~196Hz and ~208Hz) with a downward endFrequency; stack-complete: three ascending ceramic taps (~660/830/990Hz triangles staggered by ~90ms startMs) — all under ~500ms total. `DEFAULT_AUDIO_PREFS = { muted: false, volume: 0.5 }`; `readAudioPrefs()` / `writeAudioPrefs(prefs)` via safe-storage keys `hanabi-audio-muted` ("1"/"0") and `hanabi-audio-volume` (decimal string), clamping to [0,1]. `createAudioEngine(deps?: { createContext?: () => AudioContextLike })` returning `{ unlock(): void; play(cue: Cue): void; setMuted(m: boolean): void; setVolume(v: number): void }` where the default factory is `() => new AudioContext()` referenced lazily (never at module load, so SSR/Node import is safe), `AudioContextLike` is a minimal structural interface (currentTime, state, resume, destination, createOscillator, createGain, createBiquadFilter, createBuffer, createBufferSource, sampleRate) so tests pass a fake. Noise layers use a white-noise AudioBuffer generated once per engine (Math.random fill) through a BiquadFilterNode. Each layer: gain.setValueAtTime(0, t0), linearRampToValueAtTime(layer.gain * volume, t0 + attack), exponentialRampToValueAtTime(0.0001, t0 + attack + decay), node.stop(t0 + attack + decay + 0.03). Wrap context creation and play body in try/catch. Tests use a hand-written fake context recording created nodes and ramp calls.
  </action>
  <verify>
    <automated>cd /home/rflor/games && npx vitest run --project web hanabi-audio-synth && npx tsc -b apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new AudioContext" apps/web/lib/hanabi-audio-synth.ts` appears only inside a function body (not top-level), verified by the test importing the module in Node without AudioContext defined
    - `grep -nE "\.mp3|\.ogg|\.wav|fetch\(" apps/web/lib/hanabi-audio-synth.ts` returns nothing
    - `grep -n "localStorage" apps/web/lib/hanabi-audio-synth.ts` returns nothing (uses safe-storage)
    - All behaviour bullets have passing tests; `npx tsc -b apps/web` exits 0
  </acceptance_criteria>
  <done>A gesture-gated, mutable, volume-scaled synth engine with five distinct recipes and persisted prefs exists and is tested with a fake context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → audio prefs | stored values are untrusted strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-13 | Tampering | readAudioPrefs | mitigate | Parse + clamp volume to [0,1], muted only "1"; invalid values fall back to defaults |
| T-06.1-14 | Denial of Service | createAudioEngine | mitigate | Context creation and playback wrapped in try/catch; failure means silence, never a board crash |
| T-06.1-15 | Information Disclosure | cue selection | accept | Cues derive only from public history entries every seat already receives; no hidden identity used |
</threat_model>

<verification>
- `npx vitest run --project web hanabi-live-history hanabi-audio-cues hanabi-audio-synth` green
- `npx tsc -b apps/web` exits 0
</verification>

<success_criteria>
AUD-01 cue selection, no-catch-up baseline, synthesis recipes, autoplay gating, and prefs are all implemented as tested pure/injectable modules.
</success_criteria>

<output>
Create `.planning/phases/06.1-table-polish-firework-art-notes-drag-audio/06.1-04-SUMMARY.md` when done
</output>
